# 각 테스트가 무엇을 재는가 (`takbon-verify` 부록)

**언제 여나** — ① 테스트를 **한 개만** 골라 돌릴 때 ② **그물 자체를 고칠 때**(그 파일이 무슨 계약을 들고 있는지 확인) ③ **새 테스트를 더했을 때**(여기 한 줄을 같이 쓴다).
평소 검증·커밋엔 안 열어도 된다 — 실행 목록은 `SKILL.md` §1ⓐ의 러너가 `tests/`를 직접 훑어 파생시킨다. **아래는 실행용이 아니라 계약 정본이다.**
🔴 **이 목록의 정본은 이 파일 하나다**(세91 정돈 — 그전엔 CLAUDE.md와 **두 벌**이었고 세51·84·87에 실제로 갈라졌다). 🔴 **`SKILL.md`로 베껴 오지 마라** — 두 벌이 되는 순간 한쪽이 먼저 낡는다. 디스크와의 대조 루프는 `SKILL.md` §1ⓑ에 있다.

```bash
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_save_auto.gd            # 저장/로드 (고리 라운드트립) · 🔴**부팅만으로 자동 저장이 준비되나**(세션 26 F3 — 이 확인은 `load_game()` **호출 전**에 있어야 한다. 순서가 곧 검출력이다) · 🔴🔴**세86 ⑫ `inventory_changed` = 저장 트리거**(창고 증감만 저장을 걸고 **`add_to_bag`은 안 건다** — 드롭마다 세이브 전량 재작성은 세84가 각하한 낭비다) · 🔴**세86 ⑥ 로드가 codex를 clear하고 시드를 다시 심는다**(clear만 넣으면 **빌드가 시작 해금을 늘려도 옛 세이브에서만 사라진다** — 세이브에서 시드 키를 떼어 그 순서를 잰다). 🔴🔴**세88 시드 회수**: 시드 집합 **명시 열거 3키**(`rune_fire`·`jin_single`·`gr_radiate5`) — ⚠ **개수 검사로 바꾸지 마라**(개수는 같고 내용이 갈려도 통과해 장치가 죽는다) · 🔴**시작 도안 1장 + 슬롯 2·3이 비었다**(도안 시드가 돌아오면 빨개진다 = 재발 감지기) · 보상·제작 대상 10키가 시작엔 없다 · ⚠ 세이브에서 시드 키를 떼어 재적용을 재는 항목은 **살아 있는 시드 키**를 써야 한다(세87까지 `rune_bolt`를 썼는데 세88에 제작 대상이 됐다) · ⚠ **GDScript 람다는 로컬을 값으로 캡처한다** — 신호 카운터를 `var n := 0`으로 세면 안 와도 그린이다(리드가 밟았다, 참조 타입으로)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_ring_assembly_auto.gd   # **고리 조립 계약** (세85 ⑦⑨ 재편 — per-piece 상태기계와 「진이 칸을 여는 규칙」이 **은퇴해** 이 파일의 절반이 사라졌다): assembly 스냅샷 모양(발사·저장이 읽는 dict) · `clear_all` · 🔴**진 3종 Db 로드 + band_count·rune_slots 실값 표**(.tres 파싱 침묵사 그물, 세50) · JinDef 기본값 + 🔴**`glyph_slots` 부재 확인**(필드만 되살리면 거짓 손잡이가 된다) · 🔴`jin_slot_dots` 기하 = `slot_angle` 정본(HUD·Tab 미니 다이어그램의 라이브 소비자 — 베끼면 셋이 조용히 어긋난다) · 🔴**은퇴 API 13종 재발 감지**(`advance`/`choose_jin`/`select_slot`… 이름이 돌아오면 빨개진다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_ring_trace_auto.gd      # **손그림 탁본** (🔴 세83 **폐지 스위치를 되돌리면 살아나는 축의 유일한 상시 그물**): [0]🔴**합성 밑그림 계약**(진 윤곽·룬·밴드 0.42R/0.68R·빈 밴드·룬 센티넬·`rune_subpath` 정본·**융합진 축소 룬** — 세85 ⑪에 F6 벤치에서 **이관**) · 완성도/정밀도·획 누적·우클릭 리셋 · **정밀도 이빨(⑨⑩)·펜 보정(⑪⑫)·ACC_TOL 조임(㉔)·piece_score 바닥(㉕)** · 진·룬 밑그림이 **책 패널 실경로**로 판까지 오나(㉑㉓ — in-memory 진 주입·원상복구) · 문양 9종 궤적 구분(⑱) · 🔴🔴**세86 「층 = 띠」**: 문양이 층 선을 **안 밟는다**(`band_lane` — 띠 경계를 모티프 크기에서 파생) + **실제로 그리는 선은 `band_edge`(층을 가르는 칸막이 하나씩)**가 문양 범위 밖에 있다 + 띠끼리·진 윤곽·중심 룬과 안 겹친다. ⚠ **값을 하나도 안 박고 전부 관계식**이라 여백(`BAND_LANE_PAD`)·문양 크기를 튜닝해도 거짓 빨강이 안 난다(세79 교훈). 🔴 선의 **개수**는 헤드리스가 못 센다 — 「어디 놓이나」로 잰다
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_ring_spell_auto.gd      # **고리 발사**: 진→투사체·착탄 전개(발산 탄환·응집 기둥)·실제 적 take_hit
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_ring_design_auto.gd     # **고리 도안 통합**: RingDesign 라운드트립·ring_design_committed→GameState 자동 장착 · **등급⇔펑 경계·퍼펙트⇔화면100** (세션 24) · 🔴🔴**세86 ① 슬롯 교체**: `GameState.equip_design`(중복 장착 정리·보관 밖 도안 거부·null 해제) + **`to_assembly()`가 바꾼 도안을 내놓나**(= 발사가 실제로 바뀐다) + 저장 라운드트립 + `tab_panel`의 좌표→행 판정·고르기→지정 한 바퀴 · 🔴**`equipment_changed` 발신 횟수를 센다** — 이게 없으면 `ring_equipped`에 **직접 대입해도 값이 같아 보여 그린이다**(세86 실측: 「결과 값이 같다」는 「같은 길로 왔다」가 아니다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_base_auto.gd            # **베이스캠프 발사 배선** (세션 24 · 세83): 과녁 사거리 · 🔴**물리 레이어 계약**(내 몸/책상이 world면 진이 총구에서 죽는다 — 에러 없이 조용히) · [8] 숲길 · 🔴**[6]은 `balance.skip_drawing` 모드로 갈린다**(세56 「두 몸 복제 계약은 그물도 두 개」): 그리기 모드=미달 거부가 침묵 아님 · **폐지 모드=조립만으로 맺힘·펑 없음·부품 점수가 기준선 위** + 🔴**버튼 경로**([마법진 완성 ✦]이 리포트를 띄우나 — close() 자동 맺기만 재면 **버튼이 조용히 죽은 것**을 못 잡는다, 세83 뮤테이션 실측) · 🔴🔴**세89 [9] 문의 맥동**(빛 텍스처 실제 로드 + `energy`가 4초 창에서 출렁임 + **한 호흡 뒤에도 계속 돎**. ⚠ **한 점씩 비교하지 마라** — 두 샘플이 마루/골을 사이에 두고 대칭이면 값이 같아 「멈췄다」로 오독한다. 🔴 뮤테이션 실측 급소 둘: `set_loops` 제거해도 **진폭 검사는 통과**하고, 텍스처 경로를 깨도 **맥동 검사는 통과**한다 — 셋을 따로 재는 이유다) · 🔴🔴**세89 [10] 건물 게이트 — 세90에 두 갈래로 갈렸다**: ⓐ**표가 비어 있는 동안**(지금) = 🔴**`codex_unlocked` → `_on_station_unlocked` 배선이 사나**(이게 심장이다 — 표를 다시 채우는 세션이 가장 밟기 쉬운 함정이 「한 줄 넣었는데 아무 일도 안 일어난다」이고, 표가 비면 루프가 0회라 **안 재면 누가 배선을 걷어도 전 스위트 그린**이다) + 🔴**역방향 불변식**(씬에 `Ruin` 자식을 가진 노드가 있으면 표에 올라야 한다 = 「잔해로 서 있는데 여는 길이 없다」) + 「폐허여도 밝다」(성분 ≥0.85) · ⓑ**표에 한 줄이라도 오면** = 세89 전수 검사가 **저절로 다시 돈다**(`monitoring`·잔해/온전·빛 웅덩이·색조 이동). 🔴 **「지금 비었으니 검사를 지운다」로 가지 않은 이유** = 되살리는 세션이 그물 없이 출발하지 않게(T2의 반대편) · 🔴🔴**세90 [11] 첫 화면 = 문·잔해·마을의 셋이 통째로 든다**(⚠ 세89의 *"온전한 건물은 화면 밖"*을 **정면으로 뒤집었다** — 아래 F5 절 참조. 잔해 기대치도 「3채 이상」→**「목록 전부」**로 바꿨다: 개수를 박으면 **데이터가 줄어서** 빨개진다. 🔴 **머리끝(`_sprite_top`)까지 재라** — 실측 스샷에서 서고 지붕이 44px 잘렸는데 발밑만 재는 검사가 **그린이었다**. 세89가 문에 대해 배운 교훈을 새 항목에 안 옮긴 자리다) · 🔴**허수아비 5개가 스폰에서 사거리(≈390px) 안**(세89에 캠퍼스를 통째로 옮겼다 — 스폰을 옮기면 허수아비도 같이 옮겨야 한다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_chapter_auto.gd        # **챕터 보스방 루프** (세션58-B — 옛 test_forest_auto의 그물을 이식·계승): 챕터 3장 Db 로드+order+클리어 키 파생 · 🔴**보스 스폰 두 경로**(범용 forest_enemy·전용 씬 — 두 경로 각각 뮤테이션 검출 확인, 세56 교훈) · 출격 만HP · 🔴**적 레이어 계약**(4=enemy) · 처치→chapter_clear codex+포탈 스폰(처치 전 부재) · 포탈 연타 1회 extraction+가방→창고 · 사망→bag_lost+창고 보존 · 🔴**잠금 판정 = chapter_panel 공개 `is_chapter_open`을 직접**(복사 금지) · 🔴🔴**세88**: 배치·보상 **명시 상수 표**(옛 기대치는 `1 + mob_spawns.size()`로 **데이터에서 파생**해 **표를 비워도 통과했다** — ch2·ch3가 0마리인 채 그린이던 이유. 보상 id가 **실재하는 룬**인지도 리졸버로 잰다) · **상시 출구**(`&"exit"`은 처음부터 · `&"portal"`은 처치 후 — **둘 다**) · 🔴**보스를 안 잡고 출구로 귀환**(정산이 포탈과 같은 `_extract`인가) · 🔴🔴**클리어 문구를 `hud.say_line()`으로 읽는다**(원시 id 부재 + 「진 중심」 — **리졸버 직접 호출은 검출 0이다**. ⚠ ch2의 「표시명이 나온다」는 챕터 제목에 "바람"이 있어 검출력이 없다) · 미등록 챕터→빈 방 금지·베이스 복귀(⚠ [8]의 USER ERROR 한 줄은 의도된 것 — SCRIPT ERROR와 다르다) · 끝에 wipe_save() 뒷정리 · 🔴**Ground 클릭 도달·패널 카드 클릭·포탈/상자 렌더는 헤드리스가 못 잡는다**(새 씬+새 패널 = 세25 함정 정확히 그 자리 — 실게임 MCP 필수)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_dungeon_schema_auto.gd # 🔴 **던전 구조 core 스키마** (세99 0단계 · 정본 `docs/takbon-design/2_building/dungeon_structure_design.md` §2): `data/landmarks` 폴더 실재 · 🔴🔴**실데이터 3장이 Db를 거쳐 로드되나** — ⚠ **이 검사가 없을 때 `reload()`의 landmarks 루프를 통째로 죽여도 전 항목 그린이었다**(in-memory 주입은 로드 경로를 안 지난다). 세50 「데이터가 조용히 안 맞는다」의 재발 자리라 **실데이터를 넣어 측정 가능하게** 만들었다. 🔴 **개수 합계로 재지 마라** — 하나 죽고 하나 살아도 3이다(3종 개별 확인) · 접근자 주입→**원상복구**(안 되돌리면 뒤 테스트 오염) · 🔴`all_landmarks` **id 사전순**(뮤테이션 5/5 검출 — `sort()`로 되돌리면 `zz·mm·aa`가 나온다. **사전순과 정반대**이고 삽입 순(`zz·aa·mm`)과도 다르다 = 그냥 인터닝 순이다) · 🔴🔴**심장 = 「비면 지금 동작」**: 신설 4장 기본값(특히 **`NamedSpawn.chance == 0.0`** — 1.0이면 데이터를 안 고쳤는데 매 판 네임드가 선다. 뮤테이션 검출 확인) + **기존 챕터 3장이 새 필드를 전부 빈 값으로 들고 있다**(`.tres` 파싱 결과를 재는 회귀 0 실증) · `balance.extract_hold_sec > 0`(**값 자체는 F5가 정한다** — 안 박는다) · `Enums.LandmarkKind` 3종 · ⚠ **못 잡는 것 = 굴림·스폰·홀드가 실제로 도는가**(소비자가 아직 없다 — 설계 §7 단계 1~2에서 그물이 붙는다. **랜덤을 켜기 전에 `test_chapter_auto`를 범위형으로 먼저 바꿔라** = 단계 1.5)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_extract_hold_auto.gd   # 🔴 **탈출 홀드(D7) + 탈출구 여럿(D1)** (세99 단계 1a·1b · 정본 `dungeon_structure_design.md` §2-4·§6): 🔴🔴**왜 새 파일인가 = 기존 정산 그물이 홀드를 못 잰다** — `test_chapter_auto [4b]·[5]`는 `zone.interacted`를 **직접 emit해서**(E를 안 누른다) **홀드가 아예 안 돌아도 그린이다**(설계 S11). 그래서 전부 **`Input.action_press` + 이벤트 주입 + 프레임 진행 + `action_release`**로 잰다(선례 = `test_spell_cast_auto`) · **즉시 지점(`hold_sec=0`)은 홀드에 안 들어간다**(= 마을 책상·문 회귀 방어선 S8) · 진행률 단조 증가 · **뗀 뒤·발동 뒤 홀드 상태가 안 남는다** · 🔴**취소 4트리거**(피격·범위 이탈·모달·씬 전환) · 🔴**출구가 전부 `&"exit"`**(포탈 계약을 안 건드린다 — S5) · **막대가 안내 원문 뒤에 붙는다**(원문을 지우면 안내가 사라진다) · ⚠ **씬 스캔이 실제로 돌았나(`%d장 — 0~1이면 자명 통과`)를 같이 잰다**
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_mob_roll_auto.gd       # 🔴 **몹 풀 굴림 · 네임드** (세99 단계 2 — D5·D6): **굴림 기계가 실제로 도나.** 짝 = `test_chapter_auto [1c]`(실데이터가 성립하나) — 🔴 **둘이 있어야 한다**: 기계만 재면 데이터가 비어도 그린이고, 데이터만 재면 코드가 굴림을 안 해도 그린이다 · 🔴🔴**시드를 고정하지 않는다 — 확률·가중치의 양끝(0.0/1.0)을 주입해 잰다**(리포의 랜덤이 전역 `randf()` 하나뿐이라 `seed()`를 잡으면 `_spawn_loose`의 각도 굴림과 **같은 스트림을 소비**해 flake가 된다) · **자리 하나에 정확히 한 마리** · 🔴**풀 자리가 0마리면 빨강**(설계 S1 = 에러 0의 빈 방) · **바탕과 네임드가 둘 다 선다** · ⚠ **`boss_enemy_id` 제외 가드**(S9 — 풀이 보스를 뽑으면 잡몹 한 마리로 클리어가 나간다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_landmark_road_auto.gd  # 🔴 **지점(늑대 둥지) + 입구→지점 흙길** (세99 단계 3): 데이터 사슬(`ChapterDef.landmarks` → `Db.landmarks` → `scene_path` 실재) · **데이터가 말한 좌표에 실제로 섰나**(개수 + 씬 경로 일치) · 🔴🔴**길이 입구에서 지점까지 이어졌나**(`_road_connected` — 연결을 재지 개수를 안 잰다) · 🔴🔴**세100 「그물 사각 넷」ⓐ — 길의 「모양」**(`_check_road_shape`): 연결만 재던 동안 **`_road_between(지점, 입구)`의 인자를 뒤집어도 전 항목 그린**이었다(그 함수 머리말이 *"뒤집으면 방 아래쪽이 통째로 흙 밭"*이라 스스로 적어 둔 계약이 무증상). 🔴 **좌표를 안 박고 관계식 둘로 잰다** — ⓐ 입구 열의 줄기가 지점 행까지 끊김 없이 닿는다 ⓑ 지점 열의 run이 그보다 **짧다**(대소 비교라 방·길 폭을 조여도 거짓 빨강이 안 난다). **뮤테이션 실측: 인자 뒤집기 → 2건 빨강** · ⚠ 지점이 입구와 같은 열이면 ⓑ는 SKIP(자명 통과를 PASS로 안 찍는다) · **접지 계약**(`centered=false` + `offset` — 두 컷의 접지선이 같아야 갈아 끼울 때 안 튄다) · 🔴**프롭에 PhysicsBody2D가 0개**(있으면 world 레이어라 캐리어가 둥지마다 터진다 — 「필드 경계」와 같은 함정) · 감지 Area는 `layer=0·monitorable=false·mask=2` + 🔴🔴**세100 「그물 사각 넷」ⓑ 감지 상자의 아래 변이 접지선(원점) 위다**(형상에서 파생해 잰다 — 치수를 안 박아서 둥지를 크게 다시 그려도 안 낡는다) · `set_broken`/`is_broken` 왕복 · 🔴🔴**앞뒤 겹침 [9]**: 뒤에 서면 z↑+알파↓ · 앞으로 나오면 되돌아온다. **세100 「그물 사각 넷」ⓑⓒ에 두 기대치를 파생으로 바꿨다** — ⓑ **앞 프로브가 접지선에서 260px이라 상자를 아래로 250px 늘려도 그린이었다**(→ `FRONT_PROBE_PAD` 20px으로 당겼다. **뮤테이션: 상자 아래로 250px → 3건 빨강**, 그전엔 0건) · ⓒ **z 기대치 리터럴(2·3)을 살아 있는 `Player/Sprite`·`Player/FloatingWand`에서 읽는다**(**뮤테이션: 플레이어 스프라이트 z 2→6 → 1건 빨강**, 리터럴이면 나무 z 4 > 2라 **조용히 그린**. 같은 눈감음이 `test_daylight_tree_auto [6]`에도 있어 같이 고쳤다 — 실측으로 그 파일도 그 뮤테이션에 **통째로 그린**이었다) · 🔴**[10] 루트가 Node2D가 아닌 지점 씬**(`damage_number.tscn`=Label을 빌려 주입): 나쁜 슬롯을 **앞에** 꽂아 「거기서 루프가 죽으면 뒤의 진짜 둥지가 안 선다」를 값으로 잡는다. ⚠ 진짜 검출자는 **`SCRIPT ERROR` grep**이다(**뮤테이션: 가드 제거 → SCRIPT ERROR + 1건 빨강**) · 🔴🔴**뮤테이션 실측(세100 리드)**: `_ready`의 **`_spawn_landmarks()` → `_fill_tiles()` 순서를 뒤집으면 2건 빨강**(뒤바뀌면 지점은 서는데 **길이 안 깔린 채 에러 0**이다 — 이게 이 파일의 심장이다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_nest_open_auto.gd    # 🔴🔴 **지점 열기(D10·D11) + 우두머리 자리(D13)** (세101 「지점을 열면 재료가 쏟아진다」 3·4단계 · 정본 `dungeon_structure_design.md` §10): 🔴 **왜 새 파일인가 — 이웃 셋이 전부 「밟기 전」을 잰다**(`test_landmark_road_auto`=서 있나 · `test_chapter_auto [1d]`=산출 데이터 · `test_mob_roll_auto`=굴림 기계). **밟으면 무슨 일이 나나**는 아무도 안 쟀다 · 🔴🔴**`interacted`를 직접 emit하지 않는다**(S11) — `Input.action_press`+이벤트 주입+프레임 진행 · [1] **[E] 한 번에 즉시**(홀드 아님) + **비워진 컷**(S20) + 🔴🔴**누른 그 프레임에 프롬프트가 내려간다** + 낱개가 **둥지 자리**에 + **chance 0 줄은 안 나온다**(굴림을 실제로 지난다) · [2] 🔴🔴**S25 재열기 가드 = 이 파일의 심장**. ⚠**연타를 「프레임 경계 없이」 해야 한다** — 세101에 *한 번 누르고 `await` 뒤 또 누르기*로 짰더니 **가드를 지워도 검출 0**이었다(그 사이 `monitoring=false` deferred가 돌아 `_player_in_range`가 내려가 두 번째 [E]가 **애초에 안 들어온다** = 소진 처리가 가드를 가린다). 한 판은 한 번·다른 판은 `await` 없이 세 번을 눌러 대조한다 · [3] 🔴**S19 연 상태가 판을 넘지 않는다**(방을 다시 띄워 **다시 차 있나**를 본다 — 플래그를 안 읽는다. `GameState`에 넣으면 **영영 빈 둥지**인데 화면은 정상) · [4] ⓔ 우두머리가 **지점 좌표**에 선다 + 🔴**잠금이 아니다**(D10-b — 살아 있어도 열린다. 「선다」만 재면 *"먼저 잡아야 열린다"*를 얹어도 그린이다) · [5] 🔴🔴**S27 같은 `landmark_id` 슬롯이 둘이면 각각에 뜨고 각각 따로 열린다**(첫 채로 굳으면 둘째는 영영 빈다·에러 0) · [6] 미등록 `at_landmark`는 **에러로 승격**(S22) · 🔴 **뮤테이션 실측 9/9**: 재열기 가드 제거 1 · `set_broken` 호출 제거 7 · 프롬프트 직접 끄기 제거 1 · `monitoring` 끄기 제거 1 · `_landmark_sites` 첫 슬롯 고정 1 · 낱개를 원점에 1 · `opened` 연결 제거 5 · 해석 통째로 끄기 3(+mob_roll 1) · 노드↔id meta 제거 8(+mob_roll 1) · ⚠ **못 잡는 것** = 프롬프트가 **읽히나** · 낱개가 **겹쳐 보이나**(S23 — 흩뿌림 반경이 상수라 개수가 늘면 등급 후광이 겹친다) · 「열렸다」의 손맛. 전부 스샷·F5 몫
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_daylight_tree_auto.gd  # **낮 보스방 + 나무 투명화** (세99 곁가지): 🔴🔴**「예쁜가」를 안 잰다 — 「밤으로 되돌아갔나」를 잰다**(겉보기 판정은 F5·스샷 몫). `CanvasModulate`가 화면을 안 누른다(옛 `Dusk`로 되돌리면 빨강) · 방 조명이 **볕뉘지 등불이 아니다**(횃불 energy 0.9를 되살리면 빨강) · 🔴🔴**바닥이 안 탄다**(챕터 색이 1.0 언저리 옅은 틴트 — D8 유지) · 나무 감지 Area가 **`monitorable=false`**(켜지면 다른 감지자에 걸린다) · **뒤로 가면 z가 바뀌고 벗어나면 0으로 돌아온다** · ⚠ **조명·나무가 0개면 자명 통과**라 개수를 먼저 찍는다 · 🔴🔴**세100 [6]을 형상 파생으로 바꿨다**: 옛 프로브가 뒤 −24 · 앞 +40 **고정**이라 나무를 64→128px로 다시 그려 감지 상자가 −58..+2 → −116..0으로 **두 배가 돼도 같은 판정**이 나왔다 = **씬 값을 안 고쳐도 전 스위트 그린**이고 나무만 60px 뜬 채 커밋된다(art가 자기 작업의 이 구멍을 스스로 짚었다). → 프로브를 상자에서 파생(위쪽 8할 · 앞은 접지선 `FRONT_PROBE_PAD` 20px)시키고 **「아래 변이 접지선 위인가」 정적 검사**를 신설했다(치수 리터럴 0 = 그림을 또 키워도 안 낡는다). **뮤테이션(씬 값을 64px 시절로) 0건 → 2건**
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_prop_layout_auto.gd  # 🔴 **프롭 배치 규칙 ⓐ~ⓔ** (세100): 그전까지 이 규칙들은 `boss_room.gd` 머리말에 **주석으로만** 있었고 자리는 씬에 손으로 박혀 있었다(= 어겨도 아무도 모른다). 배치가 절차로 내려오며 세운 그물. 🔴 **[2]~[6]은 챕터 3장 모두**에서 돈다(회피 조건이 챕터마다 다르다) · 🔴 **손으로 놓은 나무 20그루도 같은 목록**(`props` 그룹 — 방이 붙인다)이라 규칙이 「누가 놓았나」를 안 본다 · [1] 네 층이 다 서 있나(**개수를 먼저 찍는다** — 0이면 아래가 전부 자명 통과) + 화면당 나무 3~6그루 + 🔴🔴**`Props` 홀더가 타일 뒤·플레이어 앞에 꽂혔나**(「바닥에 깔린다」는 z가 아니라 **트리 순서**다 — `move_child` 제거 시 3건 빨강) · [5] 🔴🔴**타일을 실제로 읽어** 밑동 발자국이 흙길을 밟나(`_road`를 안 들여다본다 = 「길은 깔았는데 회피가 그 표를 안 읽는」 어긋남까지 잡힌다) · [7] `offset == -(접지선 − 프레임높이/2)` + 감지 Area **0개**(64px 이하는 몸을 안 가려 거짓 손잡이) · [8] 🔴**결정적인가**(프롭은 지형이다 — D5) · [9] 🔴🔴**ⓑ가 실제로 도나**(지점을 비우고 **입구를 프롭 자리로 옮긴다** — 「문을 심는」 방식은 그 자리에 길이 같이 깔려 ⓓ가 ⓑ를 가려서 못 쓴다) · 🔴**뮤테이션 실측 9/11**: ⓓ끄기3 · ⓑ0→1 · ⓐ0→3 · 보스0→3 · 층끄기3 · 랜덤화2 · 그룹빼기6 · move_child제거3. ⚠**안 잡히는 둘 = ⓔ(`PROP_CLEAR_LANDMARK` 0)**(지점이 늘 자기 길 끝이고 늑대 다섯이 ⓐ로 그 바깥을 비워 ⓔ가 혼자 쳐낼 자리가 없다 — **데이터에 딸린 우연**이라 규칙은 남겼다)·**배치 순서 뒤집기**(실측 나무 35→33뿐이라 계약이 아니다) · ⚠**못 잡는 것 = 「숲으로 읽히나」 전부**(밀도·가림·접지 겉보기 — F5)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_workshop_auto.gd      # **공방 장비 제작** (세션 32): 레시피 station 분리(정제대⇔공방) · 펜 제작(spend→add) · 장착 라운드트립(equip→correction 0.35→소비, unequip→반환) · 🔴🔴**세88 해금 레시피 8장**(공방이 이제 **조립 부품도 만든다** — 「결과물은 전부 장비」 계약은 **아이템 레시피에만** 적용된다): `output_id` 빈 것 확인 · 🔴**라이브 패널 경로로** 제목(`display_name` 우선 — 폴백이면 정확히 `" ×0"`)·진행(`보유/필요`)·`_craft`를 잰다 · 🔴**`codex_unlocked` 정확히 1발** + **창고에 빈 id 키가 안 생긴다**(⚠ `get_count(&"") == 0`으로 재면 **검출력 0** — `output_count=0`이라 수량은 0인데 키가 생긴다. **키 존재**로 재라) · 이미 배운 것은 재료를 안 태운다 · 🔴**목록이 id 사전순**(`StringName.sort()`는 인터닝 순이라 실행마다 흔들린다) · 🔴**패널 클릭은 헤드리스가 못 잡는다**(실게임 push_input로 별도 검증)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_recipe_reach_auto.gd  # 🔴🔴 **레시피 도달성 = 「요구하는 재료」 ↔ 「무대에 선 것이 떨구는 재료」** (세105 「재료 경제 정리」ⓐ의 그물): **왜 새 파일인가 — 이웃 둘이 각자 반쪽만 잰다.** `test_workshop_auto`는 재료를 `add_item`으로 **직접 주입**해 「재료가 하늘에서 떨어진다」를 전제하고, `test_progression_auto`는 **적 레지스트리 전체**를 훑되 **그 적이 어느 챕터에 서는지 안 보고 지점 드롭을 아예 안 본다**. 그래서 세99 `840a7d1`이 잡몹 넷을 지워 `mat_vine` 레시피 6장이 **도달 불가**가 됐는데 **전 스위트가 초록**이었다(같은 형태가 세97 「도달 불가 해금 5종」·세99·세104로 **세 번**) · 🔴 **오른쪽을 「폴더의 적 전부」로 때우면 그물이 죽는다** — `boss_enemy_id` + `mob_spawns`(직접 `enemy_id` · `pool_tag`→`mob_pool`의 `weight>0`) + `named_pool`(`chance>0`) + `ChapterDef.landmarks`→`Db`→`drops`만 모은다. ⚠ **지금은 폴더 5종 == 무대 5종이라 두 방식의 결과가 같다** — 차이가 안 보인다고 폴더 스캔으로 되돌리지 마라 · 🔴🔴 **심장 = `KNOWN_GAPS`가 양방향이다**: ⓐ목록에 없는 새 구멍 = 빨강 ⓑ**목록에 있는데 도달 가능** = 빨강(공급원이 생겼는데 목록이 안 준다 = T3 거짓 손잡이) ⓑ에는 「그 재료를 요구하는 레시피가 이제 없다」도 든다 · 🔴 **[5] 지점이 실제로 기여하나**(`mat_beetle_shell`이 `lm_nest` 전용이라 자명 통과가 아니다) · [6] `until_unlock` **단독** 공급원 금지(해금되면 마른다 — ⚠ 지금 관문 0줄이라 자명 통과, 그 수를 같이 찍는다) · 예외 화이트리스트가 **0개다**(`coin`도 드롭으로 나온다 — 실측) · 🔴 **뮤테이션 실측 4/4**: KNOWN_GAPS 비우기 1건 · 도달 가능한 재료를 목록에 넣기 1건 · **지점 드롭 수집 제거 2건** · 죽은 목록 줄 1건 · ⚠ **못 잡는 것 = 속도(병목) · 🔴챕터 국소성(D15 — 지금 아무도 안 잰다) · 지점이 실제로 서나(`test_landmark_road_auto` 몫) · 굴림 기계(`test_mob_roll_auto` 몫) · 공방 UI 도달성(세97 「도달 불가 해금 5종」 축)**
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_audio_auto.gd         # **사운드 배선** (세션 33 · 세104 룬 6종 · 세105 인지음): `IDS`의 SFX 전부 로드·길이>0(⚠ **개수를 여기 박지 마라** — 늘 때마다 어긋난다) · Audio가 EventBus 9종에 연결 · 🔴**④ 발신→올바른 스트림**(부작용 순간은 연결만) — **이게 「`match`에 줄이 실제로 있나」의 유일한 검출자다**(`IDS`만 늘리면 배선이 빠져도 초록이다) · 🔴🔴**세105 ⑤ `stream_of` 계약**: `growl`(경계)·`howl`(발견)은 **EventBus로 안 난다** — 적 몸이 `AudioStreamPlayer2D`로 직접 재생하고 스트림만 빌린다(거리 감쇠. 비위치 풀 8개로는 방 반대편 늑대가 코앞처럼 들려 **통보가 아니라 잡음**이 된다) ⇒ ④가 이 경로를 못 봐서 **별도 갈래**다 · 🔴🔴 **캐시 단일 소스는 런타임 값으로 못 잰다 — 세105에 실증했다**: `stream_of`를 `return load(path)`로 바꿔 놓고 돌렸더니 **전 항목 그린**이었다(**Godot의 `load()` 자체가 리소스 캐시**라 우회해도 **같은 인스턴스**가 나온다 = 자명 통과. 세86 *"「결과 값이 같다」는 「같은 길로 왔다」가 아니다"*의 재현). ⚠ **그 검사를 「있으니 됐다」고 남기면 그물이 있다는 착각만 준다** — **소스 스캔으로 갈아끼웠다**(`stream_of` 본문이 `_stream()`을 부르고 `load()`를 직접 안 부른다. 선례 = `test_scene_contract_auto`가 `mouse_filter`를 `.tscn`으로 잡는 것) → **뮤테이션 0건 → 2건** · 🔴**소리가 실제로 나는지는 헤드리스가 못 잡는다**(오디오 드라이버 없음 — 버스 라우팅·playing은 에디터 실게임 exec로 별도 검증) · ⚠ **귀로만 확정되는 것**: growl↔howl이 실제로 구별되나 · 늑대 다섯이 동시에 울 때 뭉개지나(생성기 `tools/gen_alert_sfx.py`가 길이 2.07배·무게중심 7.92배·어택 3.49배로 갈랐지만 **그건 수치이지 귀가 아니다**)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_rune_unlock_auto.gd        # 🔴 **룬 해금** (세션 34 E4 · 세61 수술 · 세83 룬 복원 · **세85에 `test_decode_auto`에서 개명** — 해독대가 은퇴해 「해독」이 이름으로 거짓이 됐다): 조각 소비+룬 해금(codex_unlocked)·안 닳림 — **조각이 은퇴해 in-memory RuneDef+ItemDef 주입으로 기계를 잰다**(뮤테이션 검출 확인) · 🔴**룬 로드 = 정확히 6종 + 6종 개별 확인**(세50 침묵사 그물 — Color 3인자면 여기서만 빨개진다. 합계만 재면 하나 죽고 하나 살아도 6이다) · 🔴**주입은 원본을 보관했다 되돌린다** — 세83에 흙이 실데이터가 되자 옛 `erase`가 **진짜 룬을 지워** 6→5가 됐다(「빈 자리를 빌려 쓰는」 수법은 이제 못 쓴다) · ⚠ unlock_id 오타·순수확률 조각 스캔 그물은 지금 자명 통과 — 조각 복원 시 자동 가동(뮤테이션 재점화 확인) · 🔴**룬 탭 렌더·클릭은 헤드리스가 못 잡는다**(실게임 확인)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_quests_auto.gd        # **진행 목표(퀘스트)** (세션 36): KILL/EXTRACT/UNLOCK 배선(enemy_died·extraction·codex_unlocked → advance_quests) · 🔴**requires 사슬 게이트**(잠긴 퀘스트는 이벤트로도 안 진행) · 보상 지급 · 🔴**소급 완료**(이미 해금된 룬 노리는 UNLOCK은 열리는 순간 완료 — 안 하면 사슬이 막힌다) · 저장 라운드트립 · 🔴🔴**세90 「열쇠 ↔ 문」 양방향 대조**(`base.gd`의 `STATION_UNLOCKS`를 라이브로 읽는다: ⓐ표에 오른 건물엔 여는 퀘스트가 있다 ⓑ**`station_*`을 주는 퀘스트가 있으면 그 건물이 표에 있다**. ⚠ 표가 빈 지금 검출력을 지고 있는 건 **ⓑ**다 — ⓐ는 루프가 0회라 자명 통과) · 🔴**`reward_unlock` → `codex_unlocked` 기계는 in-memory 주입으로 잰다**(실데이터 소비자가 0곳이 됐다 — q03에 잠시 넣고 **원본 복원 + 심어진 codex 회수**. 안 되돌리면 뒤 항목이 「이미 해금」 가드에 걸려 자명 통과가 된다) · 🔴**q03·q04의 재료 보상이 살아 있나**(`reward_unlock`만 비웠다는 계약 — 「보상 없는 퀘스트」로 조용히 퇴화한 것을 잡는다. 세85가 q05의 `mat_night_bloom ×2`를 q04로 옮겨 총량을 보존한 그 규율) · 🔴**Q 패널 렌더·클릭 차단은 헤드리스가 못 잡는다**(전체화면 Control — 닫힘=발사 도달·열림=차단을 실게임 push_input으로 별도 확인, 액션 주입으론 못 잡는다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_dialogue_box_auto.gd  # **온보딩 대사 상자** (세션41): open(lines)→줄 넘김→finished · 빈 배열 즉시 finished · ESC 건너뛰기 · ui_modal_open 토글 · 🔴**클릭 진행·하단 밴드 렌더는 헤드리스가 못 잡는다**(실게임 push_input·스샷으로 별도 확인)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_hud_toast_auto.gd     # **HUD 획득 토스트** (세션51): 같은 id 합치기(수량+수명 리셋) · 🔴**합친 줄은 맨 뒤로 이동**(안 하면 FIFO가 **방금 주운 줄**을 밀어낸다 — 시각이 아니라 버그) · 최대 3줄 FIFO · 수명 만료 · 🔴**보이는지·슬롯/막대와 겹치는지는 헤드리스가 못 잡는다**(MCP 스샷으로 별도 확인)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_drop_pickup_auto.gd   # **바닥 드롭 픽업 + 자석 흡수** (세션46·51): setup→그룹 · 줍기 지연(지연 중 무시) · body_entered→add_to_bag+queue_free · 🔴**layer0/mask2 계약**(캐리어가 픽업에 안 부딪히게) · 🔴**자석**(반경 안이면 거리 단조감소·밖이면 정지·지연 뒤에만·**켜지면 취소불가**·도착 1회 뱅킹·item_collected 1회) · 🔴**null 가드 없으면 SCRIPT ERROR 36줄 내면서 OK 찍힌다**(grep 필수) · 🔴🔴**세88 두루마리(해금 드롭)**: 페이로드가 `unlock_id`에만 실린다 · 줍기 = **`codex_unlocked` 1회 · 가방 무변경 · `item_collected` 안 쏨**(그 시그널은 문자열을 못 실어 **원시 id 토스트**가 된다) · 이미 해금이면 **재발신 0인데 픽업은 사라진다**(바닥 유령 방지) · 🔴**빈 페이로드가 재호출 고리 없이 치워진다**(옛 버그: `_collect_at`이 `_collected`를 **세우기 전에** return해 픽업이 플레이어에 **붙은 채 영원히 안 사라졌다** — 에러 0) · 🔴**마름모 `Polygon2D`로 안 선다**(세54 도형 금지) + **PNG가 실제로 로드되나**(알파만 재면 PNG가 없어도 통과해 바닥에 후광만 남는다) · 🔴**반경 72px가 체감되는지·속도가 "빨려온다"로 읽히는지는 헤드리스가 못 잡는다**(실게임 좌표 실측 — 세50 감전연쇄 재발 자리)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_enemy_ai_auto.gd      # **몬스터 AI** (세션46): 방어(armor_reduction→enemy_hit dealt 경감) · 재생(regen_per_sec, 상한 _def.hp) · 분산 경감 · 🔴🔴**세88 AI leash**(mist·gale·snake의 `aggro_range` 게이트): **밖이면 안 오고 안이면 온다 ⓐⓑ 둘 다** — ⓐ만 재면 「적이 아예 안 움직인다」도 통과한다 · 🔴**leash 뒤에도 gale 쿨다운이 돈다**(멀리서 기다렸다 붙으면 **즉시** 연사 = 「이동 대입만 감싼다」의 **실제** 검출자. ⚠ 「멀리서도 페이즈2가 열린다」는 전이가 `match` 밖이라 **자명 통과**다) · 🔴**돌진/부유 움직임 "느낌"은 헤드리스가 못 잰다**(실게임 runtime_state로 속도파형·거리유지 별도 확인)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_enemy_hitbox_auto.gd   # 🔴🔴 **적 히트박스 = 「보이는 몸에 쏘면 맞는다」** (세99 · 잡몹 64px 재작의 짝): **왜 생겼나** — 잡몹 5종이 32→64px로 다시 그려졌는데 `forest_enemy.tscn`의 히트박스가 **반경 9.0 고정**이라 몸통 대비 비율이 0.56 → **0.16**이 됐다. **보이는 몸에 쏴도 탄이 그냥 지나가는데 에러 0 · 전 스위트 그린**이었다 — 「몸」과 「판정」을 **같이** 재는 그물이 한 개도 없었다. 🔴 **계약은 하나: 그림의 몸통 안쪽으로 쏘면 맞고, 몸 밖으로 쏘면 안 맞는다** — ⓐ만 재면 「반경 9999」가 통과하고 ⓑ만 재면 「반경 0」이 통과한다(`test_enemy_ai_auto [4]` leash와 같은 이유) · **몸통 폭은 PNG를 디스크에서 읽어 실측**한다(스프라이트를 다시 그리면 기대치가 따라 움직인다 = 안 낡는 그물) · `hitbox_radius`가 **`.tres`에 있다**(씬 상수로 되돌리면 종마다 못 맞춘다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_charge_telegraph_auto.gd # 🔴🔴 **돌진 예고 범위 = 「그린 것 = 맞는 것」** (세99 늑대 · 짝은 `test_enemy_hitbox_auto`와 같은 결): 헤드리스가 **재는 것과 못 재는 것을 갈라 놨다** — 못 잰다 = 「빨간가·눈에 띄나·피할 만한가」(`tools/vfx_shot.gd -- charge` 격자 PNG와 F5 몫) · 잰다 = **언제** 뜨고 꺼지나 · **얼마나** 크게 그려지나 · 🔴🔴 **[6] 그림과 판정이 같은 자리인가**. **[6]이 이 파일의 심장이다**: 예고가 실제보다 **크면** *"빨간 데 안에 있었는데 안 맞았다"*, **작으면** *"빨간 데 밖인데 맞았다"* — **둘 다 예고가 없는 것보다 나쁘다**. 그래서 수치를 비교하지 않고 **그려진 폴리곤에서 좌표를 뽑아 실제로 맞아 본다**(폭 안 ⓐ / 폭 밖 ⓑ / 앞끝 밖 ⓒ). 🔴 **뮤테이션 실측(세100 리드) — 세 종이 서로 다른 항목을 깨운다**: 그림 **길이** ×1.6 → 3건([2][4][5]이 잡고 **[6]은 안 잡는다** — [6]은 폭을 재는 자리라 길이 부풀림엔 눈이 없다) · 그림 **반경** ×1.8 → **4건**(여기서 **[6]ⓐ가 정확히 빨개진다** = 심장 실증) · 돌진 전이의 `_show_charge_band(false)` 제거 → 3건([3] 수명 + [6]ⓑⓒ) · 🔴 **[5] 덩치가 예고를 안 부풀린다**(`params.size` 1.35인 `hound_alpha`로 잰다 — 예고를 적 **자식**으로 되돌리면 scale이 새어 **범위만 1.35배**로 그려지고 예고가 거짓말한다. 그래서 관측점 `charge_band_reach()`는 params에서 되계산하지 않고 **폴리곤을 실측한다** — 되계산하면 그물이 거짓 그린이 된다, 세85 「검증 도구가 거짓말한다」) · 🔴 **[7] 「바닥에 깔린다」는 z가 아니라 트리 순서다**(Ground·적·플레이어가 전부 z 0이라 z에 그 자리가 없다 — 유일한 측정 가능 형태가 `charge_band_order()`) + **예고가 몸보다 오래 안 산다**(씬 소유라 안 지우면 **죽은 늑대의 붉은 띠가 바닥에 영영 남는다**) · **공개 API로만** 잰다(`charge_band_visible()`·`charge_band_reach()`·`charge_band_order()`)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_status_auto.gd        # **룬 상태이상·원소 반응** (세션49): 화상 DoT·젖음 감속·🔴**반응**(젖음+흙=진흙·젖음+번개=감전연쇄·화상+물=꺼짐·화상+풀=산불)·🔴**바람=확산**(자기 상태 안 남기고 옆 적에게 옮김)·중첩=갱신(누적 아님)·취약 증폭 · 🔴**DoT는 enemy_hit을 안 쏜다**(쏘면 피해숫자·히트스톱 도배) · 🔴**색으로 보이는지는 헤드리스가 못 잡는다**(실게임 _visual.modulate로 별도 확인)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_snake_boss_auto.gd    # **뱀 보스** (세션54 A): Db 로드(hp600·boss_snake) · take_hit→약점배율 · hp절반→페이즈2 전이(공개 `phase()`) · 세그먼트 몸통(마디 12==SEGMENT_COUNT·머리 이동→마디 추종) · 위브 추격 전진 · 🔴**세그먼트 물결·러시 채찍·머리 회전 "느낌"은 헤드리스가 못 잡는다**(실게임 확인) · 🔴**뮤테이션(페이즈·위브·추종) 검출력 확인됨**(세54)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_progression_auto.gd   # **진행 관문** (세션58, 정본 docs/PROGRESSION.md · 세61 수술): 적 Db 로드(세50 파싱 침묵사 그물) · 🔴**미해금=확정 드롭·해금=중단 — 관문 데이터가 은퇴해 in-memory DropEntry를 slime_elite에 주입해 기계를 잰다**(뮤테이션 검출 확인·[4]의 관문 수==0이 주입 누수 감지기 겸용) · 🔴**불변식(순수 확률 fragment 0곳·관문 수==표 줄 수)은 스캔형 유지 — 지금 관문 0줄이라 자명 통과, 관문 복원 시 자동 가동**(그 세션에서 뮤테이션 재점화 확인 + 관문 수 기대치 갱신) · 허기 잔재 0 · 🔴🔴**세88 드롭표 전수**(잡몹 5종이 각자 **어느 재료·어느 두루마리**를 떨구나 — 명시 표. 세87까지 **전부 coin만** 떨궜고 그걸 재는 그물이 0개였다): 재료 id가 Db에 실재하나(없으면 픽업이 마름모 폴백 = 세54 위반) · 두루마리 `unlock_id`가 **실재하는 고리**인가(오타면 주워도 아무것도 안 열리는데 에러가 없다) · 🔴**`until_unlock` 병용 안 함**(하면 `chance`가 죽어 확정 드롭이 된다) · `mat_night_bloom` 생산자 ≥1곳(융합진 레시피가 요구한다) · 두루마리가 **대역마다 다른 고리**(셋이 같으면 대역 설계가 무의미) · ⚠ 확률·수량은 F5 튜닝값이라 **일부러 안 잰다** · ⚠[2] 확정드롭은 chance 1.0이라 관문 판정 뮤테이션을 못 잡는다 — 검출자는 [3](해금=중단)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_gale_boss_auto.gd     # **gale 보스** (세션56): Db 로드+**params 17키 전수**(세50 그물) · 페이즈2 전이(`phase()`) · 돌풍(플레이어 hp 감소+`apply_push` 밀림 거리≈gust_push_dist) · 볼리(그룹 `"enemy_projectiles"` 수 ==volley_count — 초과도 잡음) · 적탄(히트→hp 감소+free·수명 만료 free — mask2 침묵 함정 그물) · 🔴**반응 룬**(연쇄=BOLT·증기=WATER — FIRE 하드코딩 청산 직접 그물. 연습장 몸 쪽은 test_status_auto [11]ⓑ가 잰다 — **두 몸은 따로 갈라진다**, 세56에 dummy만 되돌려도 전 스위트 그린이었다) · 🔴**링·탄 렌더·밀림 손맛·hover 거리감은 헤드리스가 못 잡는다**(세56 실게임 MCP로 링·탄 렌더 확인, 손맛=사용자 F5) · 🔴**뮤테이션 4/4 검출력**(페이즈·rune 두 몸 각각·볼리)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_spell_vfx_auto.gd     # **마법 연출 배선** (세션59): vfx가 `ring_cast_requested`·`spell_impact`에 연결(배선 침묵사 그물) · 트레일 형제 스폰 + 🔴**`player_projectiles` 그룹 무가입**(트레일이 가입하면 탄 수 세는 테스트 4곳이 거짓으로 는다) · 트리 밖 setup 무에러(null 가드 — ⚠ 이 항목의 진짜 검출자는 **SCRIPT ERROR grep**이다, 테스트는 가드가 없어도 OK를 찍는다) · 🔴**빈 진 착탄 = emit 정확히 1**(캐리어 emit 전용 그물 — 발산 탄이 있으면 캐리어 emit 부재가 가려진다, 뮤테이션으로 실증) · 발산 진 착탄 ≥2(탄 emit) · 🔴**볼 코어·자전·펄스·트레일·머즐/착탄 렌더는 헤드리스가 못 잡는다**(실게임 MCP 필수)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_mana_burst_auto.gd    # 🔴 **죽음 = 마나 폭발** (세100 · 정본 `enemy_feel_design.md` §3-B): 적이 죽으면 몸이 푸르게 터지고 파편이 흩어져 **위로 떠오르며** 사라진다. 🔴🔴**이 파일의 심장은 「없음을 적극적으로 증명하는」 둘이다** — 사용자가 각하한 것이 **코드에 안 남았나**를 잰다(그냥 두면 「일부러 안 하는 것」은 그물이 없어 조용히 되살아난다): **[4] 파편이 플레이어로 수렴하지 않는다**(*"잠깐 빨려오는건 없음"* — 수렴 궤적이 생기면 빨강) · **[8] 처치가 마나를 안 준다**(*"몬스터 잡아서 마나회복하는것도 없어"* — ⚠ **자연 재생분과 구분해서** 잰다. 뮤테이션 「처치에 마나 +5」 → 2건) · 🔴 **[5]가 [4]의 짝이다** — [4]만 있으면 **파편이 제자리에 굳어도 그린**이라(「안 온다」와 「흩어진다」는 다른 문장) 가로 폭이 늘고 평균 y가 주는지를 같이 잰다(뮤테이션 「흩어짐·떠오름 0」 → 3건) · [7] **처치당 노드 하나**(5마리를 같이 죽여도 5노드 — 파편이 개별 노드면 5+35이고 설계 §9의 다중 처치 과부하가 그 자리다) · ⚠ **시간을 물리 프레임으로 센다** — `mana_burst`가 `_physics_process`로 도는 이유가 이것이다(트윈이면 헤드리스가 시간축을 못 잰다) · 🔴**못 잡는 것 = 「터져서 돌아갔다」로 읽히나·파편 밀도**(`tools/vfx_shot.gd -- death:<적id>` 격자와 F5. ⚠ **`--headless` 금지**)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_spell_cast_auto.gd    # 🔴 **시전 상태기계** (세98 「시전 연출」 원자 ② · 정본 `docs/takbon-design/3_done/spell_cast_visual_design.md`): 좌클릭이 **즉시 발사 → 시전→duration→발사**로 바뀌었는데 **기존 그물은 전부 「emit이 왔나」만 세서 시전 시작과 완료를 구분 못 한다** — 그래서 새 파일이다. 🔴🔴**세98 후반 확정 ⑦: 「duration이 차면 자동 발사」는 더 이상 계약이 아니다** — 좌클릭을 **떼면** 나가고 다 차면 원은 **열린 채 머문다**. **[8]이 그 변경의 핵심 검출자**(자동 발사로 되돌리면 [8]만 빨개지고 나머지는 그린이다) · ⚠ [1]~[7]이 그대로 도는 이유 = `fire()`를 프로그램에서 부르면 버튼이 안 눌려 있어 **「이미 뗀 것」**으로 잡혀 발사 시점이 옛 계약과 같아진다 · 🔴 **홀드를 재는 선례가 여기다** — `Input.action_press` + 프레임 진행 + `action_release`(설계 §6 S11이 이 패턴을 지목한다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_floor_circle_auto.gd  # 🔴 **발밑 바닥 마법진 + 착탄 등급 반영** (세98): 🔴🔴**헤드리스는 「보인다」를 못 잰다** — 이 파일이 재는 건 *그림이 예쁜가*가 아니라 **「조립한 것이 실제로 그림에 들어갔나」**다(손맛·색감은 `tools/vfx_shot.gd`와 F5). vfx가 `ring_cast_started`·`ring_cast_canceled`에 연결(배선 침묵사 그물) · 🔴**z_index == 1**(Ground·프롭 0 · 플레이어 2 · 지팡이 3 사이의 **유일한 자리** — 음수로 내리면 Ground 뒤로 숨는다, 세54에 실제로 밟았다) · 🔴**크기가 점수에 단조 증가**(`bounds_radius()`가 **점열에서** 재므로 "ro만 키우고 점열을 다시 안 만들었다"도 잡힌다) · 🔴🔴**매핑표 없이 구성이 그림에 들어간다**(설계 §2의 심장 — 문양이 늘면 조각 수가 는다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_band_inverse_auto.gd  # 🔴🔴 **`rings`(정수 층 배열) ↔ `band_defs`(GlyphRingDef 배열) 왕복** (세98): 바닥 마법진이 **매핑표 없이** `RingBoard.compose_guide_paths`를 불러 「조립한 그림 자체」를 그리는데, `RingDesign`은 `.tres`에 **정수 배열만** 저장해 `band_defs_of`가 그걸 되돌린다. 🔴🔴**이 파일이 존재하는 이유 = 사본 위험(T5)** — 정방향 규칙(*"모티프를 칸 0..count−1에 채운다"*)이 `layer_rings` **안에** 있어서, 역함수가 그 규칙을 **베끼면 나중에 한쪽만 고쳐 에러 없이 갈라진다**(발사는 맞는데 바닥 그림만 틀리는 형태라 **F5로도 늦게 발견된다**). 그래서 규칙을 두 번 적지 않고 **왕복 항등식**으로 잰다: `layer_rings(band_defs_of(rings)) == RingDesign.layers_of(rings)`
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_ring_book_jin_auto.gd # **책 진 셀 격자·아이콘** (세61에 목록 편입 · 세83 계약 교체): Db 진 ≥1종 · 격자 계약 = 합성 8개(순수 함수 `jin_cell_rects`) · 🔴**아이콘 구분 축이 pattern×motion → `jin_icon_paths(shape, rune_slots)`로 바뀌었다**(세83: 셀 = 진 모양 + 룬 자리만) — 도형×자리수 조합 순회 + 자리 수(윤곽1+룬n) + 흐림 플래그 + 🔴**룬 자리가 판의 `rune_slot_positions`와 같은 좌표인가**(세83 뮤테이션이 잡은 그물 구멍: 자리를 전부 중심에 포개도 개수·지문은 그대로라 전부 그린이었다) · 🔴🔴**세86 층 탭 미리보기 아이콘이 판 정본에서 나오나**(`ring_icon_geom` — 옛 아이콘은 판을 **베끼고 있었다**: 각도 루프 사본 + 모티프 크기 `radius*0.42`(판의 3배) + 선을 모티프 중심에. 계약 = ⓐ**모양**이 `glyph_ring_subpaths`와 같다(정규화 비교 — 아이콘은 판의 픽셀 축소판이 **아니다**, `ICON_MOTIF_ZOOM`으로 모티프와 띠를 같은 배로 벌린다) ⓑ🔴**모든 모티프 점이 두 띠선 사이**(사용자가 눈으로 지적한 그것) ⓒ바깥 띠선 == 아이콘 반지름) · 🔴**실제 셀 겉보기·클릭은 헤드리스가 못 잡는다**(실게임 확인)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_floating_wand_auto.gd    # **떠있는 지팡이 + 발사 총구 계약** (세션65 — 세피리아식): 미장착→FloatingWand 숨김+발사 origin 몸중심 폴백(맨손 캐스팅 보존) · 장착→표시+🔴**발사 origin == 지팡이 끝 muzzle_position** + 몸과 뚜렷이 떨어짐(총구가 몸이 아니라 지팡이 끝이라는 계약 자체 — 뮤테이션 `_muzzle`→몸중심 되돌리면 [2] 2건 빨감) · 총구 기하 단일 소스(원점·무회전에서 tip==MUZZLE_LEN) · 🔴**둥둥·회전·flip·머즐 연출·지팡이 겉보기는 헤드리스가 못 본다**(실게임 MCP — 세65에 4방향 조준·발사 tip 스폰·bob 실측)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_vision_auto.gd         # 🔴🔴 **시야 = 「보이는 적」과 「기척만 남은 적」** (세104 「시야·부채꼴 어둡게·발견 느낌표」 · 정본 `vision_design.md`): 판정은 `src/core/vision.gd` 순수 static이라 **직접 호출해 표로 잰다**(정면 500 ✅ · 정면 600 ❌ · 옆 90° 400 ❌ · 옆 90° 200 ✅주변시 · 뒤 100 ✅) · 🔴🔴 **데이터 불변식을 리터럴 220 없이**: 보스는 건너뛰고 · `STALKERS`(지금 `[&"hound_alpha"]`)는 `sight_range > 주변시` · 바탕 종은 `sight_range ≤ 주변시`. **잡몹을 늘리는 날 그 값이 220을 넘으면 여기가 빨개진다**(⚠ 세105에 키가 `aggro_range` → `sight_range`로 **이름 승계**됐다 — 옛 이름은 폴백으로만 산다) · 🔴 **`always_visible`은 데이터 주도로** — `data/chapters/*.tres`의 `boss_enemy_id`를 **전부 모아** 단언한다(적 id를 손으로 나열하면 챕터4가 생기는 날 조용히 샌다. ⚠ `slime_elite`는 이름이 잡몹 같지만 **ch1 보스**다 — 초고도 리뷰도 둘 다 틀렸다) · 🔴 **분산 × 시야 곱셈을 네 조합 전부**(V1 — 세63 `modulate` 소유권이 3파전이 되면 안개 분산과 시야가 서로를 덮어쓴다 · 에러 0) · 🔴🔴 **돌풍 링이 안 물린다**(V2 — `gale`을 `always_visible` 없이 임시 스폰해 링 알파 1.0. ⚠ **`_charge_band`만 재면 검출력 0이다** — 그건 씬 자식이라 루트에 걸어도 안 죽는다. 링은 **적의 자식**이라 이게 유일한 검출자) · **물리 불변 + 숫자 차단을 한 항목에**(V5·V9 — 시야 밖 적을 때리면 **HP는 주는데** `DamageNumber` 수는 안 는다. HP만 재면 V9를 놓치고 숫자만 재면 「아예 안 맞았다」와 구분이 안 돼 자명 통과) · 폴백(V10 — `vision_dir()` 없는 스텁이면 알파 1.0) · 🔴 **뮤테이션 4종**: `fan_deg`→360 · `near_radius`→0 · 각도 부호 뒤집기 · **알파 대상을 `_visual`→루트로**(마지막 하나가 돌풍 링 항목의 유일한 검출자다) · ⚠ **못 재는 것 = 「긴장되나」 그 자체**(예고만 뜨는 게 「저기 뭔가 있다」인가 「버그」인가 · 네임드 등 뒤 60px가 「무섭다」인가 「억울하다」인가 — 전부 F5)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_alert_mark_auto.gd     # 🔴🔴 **발견 느낌표(!) = 시야 밖 적의 유일한 기척** (세104 · 사용자 요청 *"내 시야에 없을 때 나를 발견하면 느낌표가 뜨게"*): **왜 「있으면 좋은 것」이 아닌가**(리드 실측) — `vision_design`은 기척을 **이미 있는 것**(돌진 예고·돌풍 링)으로 때운다고 적어 뒀는데 **예고가 켜지는 거리(120·160)가 주변시(220)보다 한참 안쪽**이라 「몸은 없고 예고만」이 **구조적으로 안 나온다.** ⇒ 시야 밖 적은 **완전히 투명**하고 이 느낌표가 전부다 · 🔴🔴 **[3]이 유일한 진짜 검출자다**: 느낌표를 `_visual`의 **자식**으로 달면 시야 알파(`_refresh_alpha`)에 물려 **몸이 안 보일 때 느낌표도 같이 사라지는데 나머지 항목은 전부 그린이다**(뜨긴 뜬다 — 화면에만 안 보인다). `test_vision_auto [6]`이 돌풍 링에 대해 지키는 바로 그 자리 · 🔴 **`VisionStub`을 쓰는 이유** = 맨몸 `Node2D`면 시야가 fail-open(늘 보인다)이라 **느낌표가 한 번도 안 떠서 전 항목이 자명 통과**가 된다 · 공개 API로만(`alert_mark_visible()`·`is_seen()`·씬 노드·`modulate`) · ⚠ **못 재는 것 = 눈에 띄나**(노랑이 낮 풀밭에서 읽히나) · 0.90초가 적당한가 · 도형 느낌표가 도트 게임에서 어색하지 않나 — **전부 F5·스샷**(🔴 세105 라운드의 최대 미지수 = *"저기서 나를 봤다"*로 읽히나)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_enemy_facing_auto.gd   # **적 좌우 반전(「보는 쪽」)** (세104 · 인지 설계 §9 **단계 1**의 그물): 🔴🔴 **양방향으로 잰다 — 한쪽만 재면 「반전을 통째로 지운 코드」가 절반 통과한다.** 시트 원본이 **오른쪽**을 보므로 `flip_h`를 한 번도 안 건드려도 「오른쪽으로 갈 때 오른쪽을 본다」는 **자명 통과**다 ⇒ **왼쪽 검사가 그 구멍을 막는 유일한 자리** · ⚠ 이 계약은 `forest_enemy.SPRITE_FACES_LEFT`와 **한 몸이다** — 그 상수를 뒤집는 날 「자명 통과하는 쪽」도 반대가 되므로 머리말과 [5]ⓐ의 판 방향을 **같이** 고쳐라 · ⚠ **`test_enemy_ai_auto`에 얹지 않은 이유** = 인지가 들어오면 여기에 `turn_rate`(*"보는 쪽이 그만큼만 돌았나"*)·순찰 회전이 **형제 항목으로** 얹힌다(AI 그물에 섞으면 그 파일이 통째로 부푼다) · 공개 API로만(`facing_x()`·`charge_band_*()`·씬 노드 `Visual`) · ⚠ **못 재는 것 = 반전이 자연스러운가**(광원이 왼쪽 위 고정인데 뒤집힌 늑대가 어색하지 않나) · 방향 전환이 떨려 보이나 — F5·스샷. 🔴🔴 **세104에 전 스위트 45/45 그린인데 늑대가 뒷걸음질로 달렸다** — 그물이 오독을 굳힌 그 사건의 자리가 여기다
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_vision_overlay_auto.gd # **시야 안개(부채꼴 밖 어둡게)** (세104): 🔴 **재는 것과 못 재는 것을 갈라 놨다** — 못 잰다 = 「30%가 적당한가·부채꼴로 읽히나·경계가 오려 붙인 것 같지 않나」(F5·스샷) · 잰다 = **「그린 부채꼴이 판정과 같은 모양인가」의 배선** · 🔴🔴 **심장 = [2]·[3]**: 셰이더가 **자기 부채꼴을 따로 계산하면** 「어두운 데 서 있는데 보이는 적」이 생기는데 **에러 0이고 화면도 그럴듯하다**(T5). ⇒ ⓐ uniform 값이 balance와 같은지 **재고** ⓑ 셰이더 **소스에 그 숫자가 없는지** 훑는다 — ⚠ **값 대조만 하면 셰이더가 그 값을 안 쓰고 상수로 그려도 통과한다**(그래서 둘 다 있다) · ⚠ **못 잡는 것 = 「uniform은 맞는데 수식이 틀렸다」**(예: 각도를 또 반으로 나눔). 🔴 **리드가 실게임 스샷으로 검증한 방법이 정본이다** — 조준을 고정해 뽑은 PNG를 `Vision.is_seen`의 예측 마스크와 **픽셀 단위로 대조**했다(세104 · 3방향 · 일치 15,849 / 21,326 / 21,397 · 불일치는 전부 「그 사이에 움직인 적」). **수식을 고치면 그 대조를 다시 돌려라**
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_perception_auto.gd    # 🔴🔴 **몬스터 인지 = 「나를 아직 못 본 짐승」** (세105 「몬스터 인지」 · 정본 `enemy_perception_design.md` §10 — 그 절이 이 파일의 목차다): 🔴 **왜 새 파일인가 = 이웃은 전부 「이미 나를 본 적」을 잰다**(`test_enemy_ai_auto [4]`·`test_charge_telegraph_auto`·`test_status_auto`가 쓰는 몸은 인지 키가 0개라 늘 CHASE다) · 🔴🔴 **[1] 심장 = 「인지 키 0개인 몸은 정확히 옛 동작」**(§7-b — 그 몸이 **그물 6파일에 주입**된다. 폴백을 빠뜨리면 전부 굳거나 전부 걸어다닌다): 등 뒤에서도 온다 · 배회 0 · 한 박자 0을 **같이** 잰다. ⚠ **등 뒤를 「정확히 180°」에 두는 게 계약이다** — `Vector2.angle_to`는 float32라 `deg_to_rad(180)`(double)보다 크게 나와 **폴백 360°가 정확히 그 자리에서만 깨진다**(구현이 실제로 밟았다: 16건 빨강) · [2] 부채꼴은 **같은 거리 다른 각도**로 갈린다(거리를 다르게 두면 사거리가 답을 대신 내 각도 고정도 통과) · [3] 근접 원(없으면 「등 뒤에 붙어 따라다니기」가 최적 전략) · [4] `turn_rate` — **각도 값을 안 박고** 「예산을 안 넘었나 + 그렇다고 0도 아닌가」 둘로 · 🔴 [5] **소리 = 위력**: `quality_t` 경유의 **C4 검출자**를 좌표로 심는다(프로브를 `최소반경 × 1.43` **밖**에 세운다 — raw `score`로 되돌리면 퍼펙트로도 안 닿아 빨개진다) + **빈 사전 `emit({}, …)` 가드**(`test_audio_auto`가 실제로 쏜다) · [6] 기습은 **두 몸을 나란히**(한 몸만 재면 배수 1.0으로 되돌려도 초록) · 🔴 [7] 배회: **`_home`이 스폰 자리다**(§9-b — `add_child` **뒤**에 좌표를 주는 그물 관용구로 잰다. `_ready`에서 잡으면 원점으로 끌려간다) + 반경 안 + **자리마다 위상이 다르다**(겹침) + `patrol_radius ≤ boss_room.PROP_CLEAR_SPAWN`(리터럴을 안 박고 그 파일에서 읽는다) · 🔴🔴 [8] **`_contact`는 PATROL에서도 켠다**(§2-b C2 — 세88 「`return` 금지」와 같은 형태. 잡몹 접촉 채널을 재는 그물이 보스뿐이라 끊으면 **무증상**이다) · 🔴 [9] **보스 셋에 인지 키가 0개**(정적 스캔 — 코드가 보스를 못 가르므로 **유일하게 실행 가능한 형태**다. `boss_enemy_id`를 챕터에서 모은다) · 🔴 [10] **`sight_range > 주변시`인 종은 `STALKERS`에 등재**(§8-1-b · `test_vision_auto [2]`의 후계) · [11] 한 박자(경계 → 추격, **그 사이 벗어나면 없던 일**) · [12] 수색(놓치면 마지막 본 자리로) · 🔴 **뮤테이션 실측**: `alert_sec`→0 **1건**([11]ⓐ) · `ambush_mult`→1.0 **1건**([6] — 다른 항목은 그린 = 격리 확인) · `_contact`를 CHASE 안으로 **1건**([8]) · `turn_rate` 무시 **1건**([4]) · `sight_angle`→360 **2건**([2]ⓑⓒ) · 🔴🔴 **[14] 실데이터 짝 — 「준 키가 폴백값 그대로면 안 준 것이다」**(세105 **리드가 뮤테이션으로 구멍을 실측하고 추가**): 설계 §10이 지정한 *"`ambush_mult`→1.0"*을 **`data/enemies/hound.tres`에 걸었더니 전 스위트 48/48이 그대로 그린이었다**(검출 0) — [6]이 `_inject`로 **기계**만 재서 **실데이터가 무엇을 들고 있든 상관이 없기** 때문이다. 🔴 **이 리포의 반복 병이다**(`test_mob_roll_auto` 머리말: *"기계만 재면 데이터가 비어도 그린이고, 데이터만 재면 코드가 굴림을 안 해도 그린"*). 불변식 = **§7-b 폴백표를 뒤집은 것**(키가 있는데 값이 폴백과 같으면 **손잡이는 있고 효과는 없다** = T3): `sight_angle<360` · `turn_rate·patrol_*·alert_sec·lose_sec·search_sec·sense_radius>0` · `ambush_mult>1.0`. ⚠ **부재는 정당하다**(§7-b) — 없으면 건너뛴다. 🔴 **값을 안 박고 폴백과의 대소만 본다**(F5 튜닝에 거짓 빨강이 안 나게) · 🔴 **인지 종을 id로 나열하지 않는다**(`PERCEPT_KEYS`를 하나라도 든 종 = 새 몹이 자동으로 든다) · **뮤테이션 실측 0건 → `ambush_mult`→1.0 **1건** · `turn_rate`→0 **1건**** · ⚠ **못 재는 것** = 배회가 「살아있다」로 읽히나 · **큰읏과 짖음이 구별되나** · 한 박자가 「피할 수 있다」로 느껴지나 · 늑대 다섯이 겹쳐 보이나(전부 F5)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_gate_collider_auto.gd  # 🔴 **문 기둥 콜라이더 = 「보이는 돌은 막고, 보이는 통로는 지나간다」** (세104 「문 기둥 콜라이더 그물」): 세89에 손으로 맞춘 값(∓40·폭 34)을 **그림에서 다시 계산해** 굳힌다 — 🔴🔴**리터럴 기대치가 0개다**: `gate_arch.png`를 디스크에서 읽어 「열별 최장 연속 **불투명** run ≥ 최대run×0.75」로 기둥을 뽑는다(돌=불투명·포탈 수면=반투명이라 갈린다. 실측 기둥 119~131 vs 나머지 ≤79). **그림을 다시 그리면 기대치가 따라 움직인다**(실증: PNG 기둥을 4px 굵게 → 파생 통로 46→38) · [1] **분류 여유**(0.66 ≤ 0.70)를 같이 재서 그림이 애매해지면 **문턱에 닿기 전에** 사람이 본다 + 임포트 신선도 + 배율 1 전제 · [2]🔴🔴 콜라이더 x == 그린 기둥 x를 **바깥 끝·안쪽 끝 따로**(「덮는다」만 재면 폭 9999가, 「안 삐져나온다」만 재면 폭 0이 통과) · [3] 세로는 **대역**으로만(탑다운에서 깊이는 손맛 축이라 정확값을 안 박는다) · [4]🔴🔴 **진짜 플레이어로 걷는다**(`Input.action_press(&"move_up")`): ⓐ기둥 한가운데=막힘 ⓑ통로 한가운데=통과 — **ⓐ만 재면 「문 전체가 벽」이, ⓑ만 재면 「콜라이더가 통째로 없다」가 통과한다** · [5]🔴 **통로 안쪽 모서리를 ±3px로 못 박는다**(돌을 4px 물면 옆으로 밀리고 1px 벗어나면 안 밀린다. ⚠ **「막힌다」가 아니라 「밀린다」인 이유** = 기둥 안쪽 면은 세로 벽이라 북진과 나란해서 미끄러진다) · [6] 기둥 레이어 ∩ **플레이어 마스크**(둘 다 씬에서 읽는다) · [7] [E] 트리거가 그린 문 폭 전체를 덮는다 · [8]🔴🔴 **문을 무는 씬 전량을 훑어** `scale`·`rotation` 오버라이드 금지([1]~[7]이 전부 프롭을 따로 세워 재므로 무대가 늘리면 **전부 그린인데 화면은 어긋난다**) · 🔴**뮤테이션 실측**: ∓62 8건 · 폭24 6건 · 세89이전 통째 8건 · 콜라이더 삭제 5건 · **레이어 1→8 5건(🔴[2][3]은 전부 그린 — 좌표만 재는 그물은 레이어가 틀려도 통과한다 = [4][5]의 존재 증명)** · 높이 60 4건 · Sprite offset −60 4건 · 트리거 80 1건 · **한쪽만 4px 3건([5]ⓒ가 잡는다)** · base.tscn scale 1.4배 1건 · ⚠**못 잡는 것 = 「통로가 지나갈 만해 보이나」·「단단해 보이나」·[E] 문구가 가려지나**(F5) · ⚠ 소요 ≈26초(진짜 플레이어를 7번 걷게 한다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_feel_auto.gd            # **손맛 개편** (세션63): player_hurt 발신 단일 소스+🔴사망 가드(hp 0 뒤 무발신) · 🔴히트 플래시 material **per-instance**(공유=전원 플래시) · 텔레그래프=셰이더 uniform+**modulate 불가침**(rgb=상태 틴트·a=분산 2축 계약) · hurt 애니 굽기(있으면 비루프/없으면 무변경) · 보스 2종 hurt_sprite 로드(세50 침묵사 그물) · 그림자(z≥0·그룹 무가입·첫 자식·뱀 마디 수 일치) · dust 구르기 엣지 버스트 · 카메라 킥 방향(발사=조준 반대·피격=가해자 반대) · 플레이어 hurt 애니 가드 · 🔴허수아비 파리티(세56 두 몸 그물) · 🔴**플래시가 실제로 하얗게 보이나·그림자/먼지 겉보기·수치 손맛은 헤드리스가 못 잡는다**(세63에 그림자 가림·먼지 뭉개짐을 실게임이 잡았다 — 전 그물 그린이었다)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_jin_layers_auto.gd       # 🔴 **진별 해석 M1 — 층(밴드) 순서 = 연산 순서** (세79, 정본 `docs/takbon-design/3_done/jin_interpretation_design.md`): M1 콘텐츠 3종 Db 로드(gr_spread3·gr_explode1·jin_plain_g2 band_count=2 — 파싱 침묵사 그물, 세50) · `layer_rings` 계약(밴드→층, 칸 0부터 count개, 🔴**빈 밴드도 층 자리를 지킨다**=건너뛰면 감쌈 깊이가 조용히 밀린다) · 🔴**회귀: 밴드 1개면 층0 == `flatten_bands`**(지금 살아있는 진은 전부 band_count=1이라 이 동치가 곧 「저장 도안 발사 무변경」의 증명) · `_as_layers` 정규화(옛 8칸 한 겹→층 1개 승격·멱등) · 🔴🔴**[4] 심장 = 순서가 결과를 바꾼다**: `폭발(확산(불))`=융합 폭발 **1개**(큰 반경) vs `확산(폭발(불))`=복제 폭발 **3개**(작은 반경·자리 벌어짐) — ⚠**balance 수치를 안 박고 개수·대소 관계로만 잰다**(손맛 튜닝 한 번에 거짓 빨강이 되지 않게) · 옛 도안 무회귀(층 1개+전개형만 = 탄 8발·기둥 1개·폭발 0, **빈 진은 여전히 전개 0**=씨앗 누출 감지) · 씨앗(감쌀 게 없으면 문양이 **룬 자체**를 감싼다) · 세기 = 그 층의 칸 수 · [7] 🔴**조립 UI→발사 계약**(`build_assembly`가 rings를 다겹으로 싣나 + score 동승 — 끊기면 M1이 게임에서 통째로 안 보인다) · 🔴**뮤테이션 5/5 검출력 확인**(폭발을 「각각 터뜨림」으로·층 루프를 rings[0]만·씨앗 제거·빈 밴드 건너뜀·build_assembly를 flatten으로 — 전부 원상복구 확인) · 🔴**폭발이 실제로 「크게 터졌다」로 보이나·확산 부채가 넓어 보이나·따라 긋는 손맛은 헤드리스가 못 잡는다**(F5)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_jin_fusion_auto.gd       # 🔴 **진별 해석 M2 — 룬 2개 + 융합진** (세81, 정본 `docs/takbon-design/3_done/jin_interpretation_design.md`「M2 확정 설계」): jin_fuse Db 로드+`rune_slots==2`(파싱 침묵사 그물, 세50) · `runes_of` 승격·멱등+저장 라운드트립(옛 도안 `{rune}`→`[rune]`) · `RingAssembly` 다중 룬 계약(set_rune_slots·set_rune(type,slot)·runes_ready 게이트) · [4] `_fire_hit` **합산 = 0.7×(두 룬 단독 합)**(base_damage 무관 불변식)+rune_hits 2개+정렬(primary=WET 바탕) · 🔴🔴[5] **심장 = 융합 발사 → 한 발이 두 상태 → 반응(SHOCK) + 자리 순서 무의존**(물·번개 뒤집어도 감전) · 🔴[6] **도배 방지 = 융합 한 발 enemy_hit 발신 == 1**(보조 0-피해 히트가 안 쏨 — dummy `hits[]`엔 담겨도 발신 수로 잼) · [7] 기둥·폭발도 반응(rune_hits 이식 그물) · [8] 🔴**회귀 = 룬 1개 share 1.0·rune_hits=[primary] 하나라 옛 계산 완전 동일** · 🔴**뮤테이션 7/7 검출**(0-피해 가드 dummy·forest_enemy 각각·rune_hits·정렬·share·pillar/blast·runes_of) · 🔴**룬 소켓 2칸 클릭·룬 2개 밑그림·반응 가시성은 헤드리스가 못 잡는다**(F5) · ⚠**게임 UI 반응은 물+불=증기뿐**(번개·흙·풀 룬 데이터 없음 — 세61 리셋)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_glyph_data_auto.gd     # 🔴 **문양 효과·표현 데이터화 + 응축** (세82 M3, 정본 `docs/takbon-design/3_done/glyph_data_design.md`): [1] `Enums.GlyphCode` **전 9값**이 Db 로드 + `behavior`가 `BEHAVIORS`의 키 + `params` 실제 파싱(세50 침묵사 자리 — **예외 목록 없음**이 곧 자명통과 방지) · [2] `modifier_codes()==[6,7,8]` 명시 기대 · [3] 🔴**회귀 = 수치가 balance→.tres로 이사했는데 관계식 동일**(값을 박지 않는다) · [4] 🔴🔴**심장 = 응축은 폭발의 반대, 단조성으로 잰다**(갈래↑→응축 좁아짐·폭발 넓어짐 — ⚠**대소 비교만으론 부호 뒤집기를 못 잡는다**: 54×1.24=66.96 < 폭발 73.44라 그린이 된다) · [5] `merge_mult_per_count`가 count≥2 경로에 걸린다(고리를 count=2로 낸 이유) · [6] **응축이 새 알고리즘 없이 돈다**(behavior==blast·알고리즘 4종 유지) · [7] 미등록 code=건너뜀 + 🔴**빈 칸(-1)은 경고 대상 아님**(발사마다 경고 폭탄) + 🔴**계열 분기가 실제로 `_apply_layer`를 지난다**(없으면 behavior를 통째로 bolt로 돌려도 전부 그린 — 실측) · [8] code 중복=**결정적 승자**(id 사전순 — ⚠주입 순서를 사전순과 갈리게 넣어야 검출) · [9] 주입 후 `reindex_glyphs()` 갱신(세61 주입 관행) · [10] **같은 층 내 배치 순서 무관** · [11] 반경 부호 구멍 · [12] UI 이름·색·선택이 .tres에서(옛 `clampi`가 응축→폭발로 누르던 자리) · 🔴**뮤테이션 9/9 검출+원상복구** · 🔴**응축 나선을 손으로 그을 만한가·「집중 한 방」으로 보이나는 헤드리스가 못 잡는다**(F5)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_ui_text_auto.gd          # 🔴 **표시부 계약** (세84 감사 #12·#21·#35·#36 — **그전엔 이 넷 전부 그물이 0건이었다**): 🔴**융합진 룬 표시**(표시부 셋이 `design.rune`만 읽어 **두 번째 룬이 조용히 사라지던** 자리 — `RingDesign.runes_of()` 경유 + 씨앗 문자열 `물+번개` 합침. 발사부는 계약을 지키는데 표시부만 뒤처져 **「쏘는 것 ≠ 보이는 것」**이 됐다 = 감사 T8) · 🔴**룬 점 좌표는 `RingBoard.rune_slot_positions` 정본 호출**(각도를 베끼면 판·책 셀·HUD 셋이 조용히 어긋난다) · **`ItemText`(core) 단일 소스** + 🔴**사본 재발 감지 스캔**(문구가 세 벌로 갈라져 있던 자리 — 스캔이라 앞으로 네 번째 사본이 생기면 빨개진다) · 🔴🔴**세88 `CodexText`**(codex 해금물 이름·안내 단일 소스): 룬·진·고리 셋이 각자 카탈로그에서 나오나 · **안내가 종류마다 다르나**(룬=진 중심 · 진=바탕 · 고리=밴드 — 셋이 같으면 리졸버가 있으나 마나다) · 챕터 보상 3종이 전부 걸리나 · 🔴**획득물 아닌 키(`chapter_clear_*`)엔 문장을 안 만든다**(안 그러면 "chapter_clear_ch1 획득!"이 뜬다) · 🔴**`ItemText.count_text` 사본 스캔**(공방·정제대가 **인자 거꾸로인 사본**을 각자 들고 있었다 — 5개 필요한데 3개면 「핵 5/3」으로 찍혔다) · 퀘스트/소지품 **행 캡 관계식**(6행이 화면 밖으로 나가던 자리) · **say 수명**(경고 한 줄이 목표를 덮고 씬 끝까지 상주하던 자리 — `sticky`는 **목표·유효한 지시만**: boss_room 2곳·base 온보딩 1곳) · 🔴**겉보기·클릭은 헤드리스가 못 잡는다**(F5)
./Godot_v4.7.1-stable_win64.exe --headless --path . -s res://tests/test_scene_contract_auto.gd   # 🔴🔴 **씬 계약 — `mouse_filter` 정적 그물** (세84 감사 #14): 게임플레이 씬을 **스캔**해 「보이는 채로 화면을 덮는 Control이면 `mouse_filter == IGNORE(2)`」를 잰다. 🔴 **이 프로젝트가 두 번 밟은 최다 재발 버그**(세25 base·세26 forest — 바닥이 좌클릭을 다 먹어 **발사가 에러도 경고도 없이 죽는데 전 스위트 그린**)가 **처음으로 헤드리스에 걸린다**: 런타임 히트 테스트는 렌더가 없어 못 잡지만 **실패 형태가 늘 「씬에서 그 줄이 빠진다」라서 `.tscn` 프로퍼티로 잴 수 있다**. 뮤테이션 실증(세84 리드) = `base.tscn` Ground를 `mouse_filter=0`으로 되돌리면 그 노드를 이름·크기까지 지목해 빨개진다. ⚠ 씬 목록을 하드코딩하지 않았다 — **새 씬은 자동으로 그물에 든다**. 🔴 이건 **F5의 대체가 아니라 1차 방어선**이다(닿는지 자체는 여전히 실게임)
```
