# **04a-full - the whole shape, and the baseline the rest are read against**
#
# **Exactly what the user chose off the first sheet**, with the arm beading fixed and nothing
# else moved. Arms and transverse crests both.
#
# ⚠ **One dictionary and nothing else.** Every dial not named here is the value in `kelvin.gd`'s `BASE`,
# which is what the first sheet was shot at.
extends "res://.prototypes/wake/kelvin.gd"


func dials() -> Dictionary:
	return {}

