// **04a-full — the same mechanism as every other `04x`, at different numbers.**
//
// ⚠⚠ **There is one copy of the mechanism and this is not it.** It lives in `kelvin.gdshaderinc`; this
// file exists only so `build.py` can splice the shipped sea around it, byte for byte, the same way it
// does for every other candidate on the sheet. **The numbers are in `wake.gd` beside this file.**
#include "res://.prototypes/wake/kelvin.gdshaderinc"
