# GLOSSARY — 이 저장소가 쓰는 낱말

**용어집이다. 이 파일에는 낱말만 산다.** 무엇이 지어졌는지는 `src/` 와 `tests/nets/` 에서 읽는다.
**`naming` 스킬이 이 파일을 쓴다.**

⚠ **한국어와 코드 두 칸이 다 필요하다.** 영어 낱말만 쓴 답은 사용자에게 답이 아니고, 한국어로 지은
이름은 심볼이 아니다.
⚠⚠ **코드와 이 파일이 다르면 코드가 참이고 이 파일을 고친다.** 반대가 아니다.
⚠⚠ **뒤집힌 낱말은 지운다.** 지금 참인 뜻만 여기 산다 — 언제 무엇에서 무엇으로 바뀌었는지는 안 적는다.

---

## 판과 땅

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **판** | `Grid` + 바닥의 자국 | **걸어 다니는 바닥 전체.** 조각마다 통행·물·눈금·예약을 들고, 흐름장이 얹혀 몸은 낮은 쪽으로 한 조각씩 내려간다 | **「노드」가 아니다** — 그래프가 아니라 격자다 |
| **조각** | `tile` · `TILE_PX` | **몸이 서는 정사각형 하나, 한 변 1 미터.** 바닥의 자국 하나가 조각 하나다 | **블록이 아니다** — 블록 하나가 조각 넷이다 |
| **블록** = **칸** | `Rules.BLOCK_TILES` · `Grid.block_of` · 블렌더의 `PIECES` · `block()` | **2x2 덩어리.** 섬의 윤곽은 이 단위로만 꺾이고, 높이는 덮는 조각 넷 중 **짝수** 눈금의 최대다. **몸은 여기에 아홉까지 선다** (`Rules.BLOCK_CAPACITY`). 성채가 든 블록은 여덟만 받는다 | ⚠ **「칸」과 「블록」은 같은 것의 두 이름이다** — 둘 다 쓴다 |
| **눈금** | `level` | **반 조각.** 이 게임의 모든 높이가 이 정수에서 나온다 | — |
| **층** | — | **눈금 둘.** 1층이 0, 2층이 2 | **3층은 없다** |
| **계단** | — | **눈금 하나.** 몸은 눈금 차이 1 까지만 넘으므로 **계단이 층 사이의 유일한 문이다.** 블록 위에 얹은 물체다 | **계단 조각에는 자국이 없다** — 지나는 자리지 머무는 자리가 아니다 |
| **섬** | `Islands` | **글자표가 아니라 파일이다.** 그 파일과 화면의 메시는 블렌더 한 번이 같이 쓴다 | — |
| **건물** | 섬 파일의 `builds` | 성채·집·탑·창고·성벽 다섯. **지금 서 있는 것은 성채와 창고고 집은 0 개다.** **플레이어는 조각 단위로 짓는다** — 명령과 자원은 칸인데 건물만 조각이다 | — |
| **바리케이트** | 섬 파일의 `성벽` | **플레이어가 조각 하나에 나무로 지어서 길을 막는 것 — 성벽과 같은 것이다.** **내 편도 막는다.** 짐승은 막히면 길을 다시 찾고 길이 없으면 때려 부순다. **체력을 갖고 0 이면 사라진다** | **지형이 아니다** — 자원 칸도 막지만 그것은 플레이어가 안 세운다 |
| **창고** | 섬 파일의 `창고` | **하나뿐인 저장 건물.** 물고기 · 감자 · 밀 · 나무 · 돌 · 철이 **종류별로** 쌓이고, 배고픈 몸이 여기 와서 알아서 먹는다. **첫 판에는 없어서 지어야 한다** | **식량 창고와 자재 창고가 따로가 아니다** |
| **허기** | `Army.hunger` | **몸마다 갖는 값.** 천천히 닳아 0 이 되면 체력이 깎이기 시작하고 체력이 0 이면 죽는다. **판의 넷째 줄에 뜬다** | **버프가 아니다** — 먹어서 좋아지는 것은 없다 |
| **낚시 자리** | — | **섬에서 두 칸쯤 바깥 물 위에 보이는 좋은 자리.** 배를 타고 나가야 닿는다. 해안 아무 조각에서도 낚을 수는 있다 | **자원 칸이 아니다** — 땅이 아니라 물 위다 |
| **자원 칸** | — | **한 종류의 자원으로 찬 칸.** 나무 칸 · 돌 칸 · 철광석 칸 셋. **섬마다 나무 1~3 · 돌 1~3 · 철 1~2** 가 시드로 놓이고, **막힌다** — 옆 조각에 서서 캔다. **마르고 다시 찬다** — 다 캐면 칸이 없어져 걸어 다니는 빈 땅이 되고 **3 분 뒤 통째로 다시 선다** (05-11 이 지을 것이고 코드는 아직 아니다) | **낱개 나무·돌이 아니다** — 아래 「나무 · 돌」 줄은 손으로 그린 섬의 풍경이다 |
| **나무 · 돌** | 섬 파일의 `props` | **풍경.** 게임이 도는 동안 이것에 대해 정하는 것이 없다. 소나무 서른하나가 북동쪽 숲 하나로, 덤불 열일곱, 돌 셋, 그루터기 둘, 철광석 하나 | **「오브젝트」라고 안 부른다** — 블렌더에서는 블록도 오브젝트다. ⚠ **조약돌은 하나도 안 놓는다** |
| **나무** | `tree_pine` | **3D 저폴리 메시.** 기둥이 아래 가지 밑으로 0.80 조각 드러난다 | ⚠ **그림이 아니다** |
| **덤불** | `lp_bush_v1`~`v4` | **2D 카드.** 정점 셰이더에서 스스로 돌고, 바람에 눕고, 어긋난 어두운 복사본으로 제 외곽선을 그린다 | ⚠ **메시가 아니다.** 나무와 반대다 |
| **철광석** | `ore` | **자원 칸이고 섬에 붙는다. 섬마다 1~2 개.** 회색 바위 허리에 광석 띠가 둘린 것 | — |

## 바다

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **해안선** | `line_*` · `second_*` | **흰 선 둘.** 하나는 바위에 붙고 하나는 0.22 조각쯤 떨어져 선다. 사이는 어두운 물이다. **바깥 선은 끊긴다** | **안 움직인다** — 파도가 왔을 때만 두꺼워진다. **윤곽이 아니다** |
| **윤곽** | `coast` · 블렌더의 `waterline_point` | **땅 자체의 모양.** 블록 단위로만 꺾이고, 블렌더가 구우면서 같이 내보낸다 | **눈에 보이는 선이 아니다** — 윤곽 자체는 아무것도 안 그린다 |
| **파도** | `swell` | **열린 바다가 크고 느리게 오르내리는 것.** 섬이 없어도 있다 | **흰 선이 아니라 바다 전체의 굴곡이다** |
| **여울** | `shallow` | **땅에 가까워지며 바다가 옅어지는 넓은 띠** | **선이 아니라 색이다. 모래사장이 아니다** |

## 몸

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **검사** | `SWORDSMAN` | **검을 든 병사, 그리고 코드에 있는 유일한 몸.** 창병과 방패병 사이에서 잡은 숫자이고 사거리만 0 이다 | ⚠ **병종이 아니다** — 병종은 없고, 몸은 한 종류고, 손에 든 것이 무엇을 하는지 정한다 |
| **맨손** | — | **무기도 농기구도 안 든 몸.** 아무것도 없는 상태이고 몸의 바탕이다. **디스크의 그림 124 장이 이미 이것이다** | ⚠ **코드에 없다** — `UNITS` 에는 `SWORDSMAN` 줄뿐이다 |
| **병사** | — | **무기를 든 몸.** 검사는 검을 든 병사고, 5 주의 활을 든 몸이 둘째다 | ⚠ **코드에 없다** |
| **장비** | — | **손에 쥐는 것 전체 — 무기와 농기구.** 장비칸에 앉는 것이고, 몸이 기본으로 갖는 숫자에 더해진다. 농기구를 들면 농부다 | ⚠ **코드에 없다** |
| **장비칸** | — | **몸 하나에 붙는 자리.** 낀 것이 그 몸이 무엇을 하는지 정한다 | ⚠ **부대 전체에 하나가 아니다.** ⚠ **칸이 몇 개인지는 안 정해졌다** |
| **짐승** | `WOLF` | **적.** 배를 타고 와서 내리고, 걷고, 때린다 | ⚠ **한 종뿐이다** |
| **부대** | `Hand.ids` | **골라서 하나로 명령하는 몸들.** 손이 고른 목록 그 자체고, **하나여도 부대다** — `order` 가 목록 전체를 보내고 `routes` 가 한 몸에 한 줄씩 그린다 | ⚠ **한 칸에 같이 선 것이 아니다.** ⚠ **몸은 하나씩 관리한다** — 고정된 묶음이라는 뜻은 안 정해졌다 (03-05) |
| **이름** | — | **몸 하나마다 붙는 것.** 몸이 올 때 한국어 이름 수십 개의 목록에서 하나 뽑는다. 누르면 특성 · 체력과 같이 구석 판에 뜬다 (03-02) | ⚠ **코드에 없다** — `Army` 는 `type_id` · `alive` · `slot_id` 뿐이다 |
| **특성** | — | **몸의 성격 낱말.** 절식 같은 것 — 「어떤 사람인가」 | ⚠ **아직 하나도 없다** — 11-01 이 정한다. 판의 「특성」 줄은 그때까지 비어 있다. ⚠ **등급이 아니다** — 등급은 죽은 낱말이다 |
| **적성** | `Army.aptitudes` · `Rules.APTITUDES` | **생활 능력의 레벨.** 요리 · 제작 · 낚시 · 채광 · 벌목 다섯, **0 ~ 10**. 몸이 올 때 0 ~ 3 사이를 갖고 온다. 판의 셋째 칸에 「요리 2/10」 꼴로 뜬다 | ⚠ **아무것도 아직 이 숫자를 안 읽는다** — 채집이 쓰는 날 값이 생긴다 |
| **능력치** | `Rules.damage_of` · `Rules.defense_of` · `Rules.period_of` | **전투 숫자.** 공격력 · 방어력 · 공격간격 셋. 판의 둘째 칸에 뜬다 | ⚠ **방어력은 모두 0 이다** — 표에 열이 없고 함수 하나가 0 을 돌려준다. ⚠ **공격간격은 때리는 사이의 초라 큰 수가 느린 몸이다** |
| **상태** | `Battle.soldier_hp` · `Army.hunger` | **지금 값.** 체력 · 허기, 나중에 더. 판의 첫째 칸에 뜨고 특성이 그 밑에 붙는다 | **하는 일이 아니다** — 대기·이동·공격 같은 동작은 상태가 아니다 |
| **포로** | — | **짐승의 배에 같이 실려 오는 몸, 그리고 당분간 몸이 오는 유일한 길.** **그 배의 늑대를 다 잡고 부대를 그 해변 칸으로 보내면 내 편이 된다.** **배에 남아서 그 배의 늑대가 다 죽을 때까지 아무것도 안 한다** — 새 그림 0 장. **배마다 하나 온다.** 늑대 배는 안 타고 부숴서 나무로 쓴다. **안 부순 배는 남는다** | **짐승이 아니다** — 성채를 안 친다. **늑대와 같이 안 내린다** |
| **포획** | — | **쓰러뜨린 것을 부대가 데려오는 것.** 시체가 없어서 「쓰러뜨리고 데려온다」 모양이다. **사람만 잡힌다** | **죽음이 아니다.** **늑대는 안 잡힌다.** ⚠ **무엇이 쓰러뜨리나 · 데려오기가 무엇인가 · 얼마나 누워 있나는 미정** (10-03) |

⚠⚠ **몸의 종류는 데이터지 타입이 아니다.** 코드는 `UNITS` 표의 줄 하나를 알 뿐이고, **검사도 늑대도 그
표의 한 줄이다.** 둘째 병종이 생기는 날 고치는 것은 표 한 줄이다.

## 몸이 하는 것 — **다섯 가지고 끝이다**

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **대기** | `Anim.IDLE` · `<방향>_idle_<n>.png` | **안 움직일 때 숨 쉬는 것.** 여덟 장, 0.96 초 | — |
| **이동** | `Anim.WALK` · `<방향>_walk_<n>.png` | **걷는 것.** 네 장, 0.48 초 | — |
| **공격** | `Anim.ATTACK` · `<방향>_attack_<n>.png` | **때리는 것.** 여덟 장, 0.96 초, 한 번 돌고 돌려준다. ⚠ **피해는 시작하고 0.4 초 뒤에 들어간다** (`Rules.SWING_LAND_SEC`) — 넷째 장, 칼이 끝까지 나간 자리다 | **「물기」가 아니다** — 검사는 안 문다 |
| **피격** | `Anim.HURT` · `<방향>_hurt_<n>.png` | **맞고 몸을 움츠리는 것.** 네 장. **체력이 줄면 시작하고 공격을 끊는다** | **흔들림이 아니다** — 몸이 밀리는 것은 없다 |
| **죽음** | `Anim.DEATH` · `<방향>_death_<n>.png` | **쓰러지는 것.** 여섯 장, 0.72 초. 끝나면 몸이 사라진다 | **시체가 아니다** — 바닥에 남는 것은 없다 |

⚠⚠ **이 다섯은 파일 이름이기도 하다.** `<몸>_<동작>_<번호>.png` 가 규칙이고, 서 있는 그림은
`<몸>.png` 다. **한 줄도 안 가진 몸은 서 있는 그림을 입고 그대로 굴러간다.**
⚠⚠ **순서가 규칙이다**: 죽음 → 피격 → 공격 → 이동/대기. **맞으면 때리던 것을 놓는다 — 단, 아직 안
닿은 휘두르기는 안 놓는다.** 닿을 때까지는 휘두르기가 위고 닿은 뒤에는 피격이 위다.
⚠ **시뮬레이션은 이 다섯을 모른다.** `battle` 은 상태만 들고 있고, **공격은 재사용 대기가 올라간
것 · 피격은 체력이 내려간 것 · 죽음은 살아있음이 꺼진 것**으로 그리는 쪽이 한 프레임 차를 읽어서 알아낸다.
⚠ **타격 효과(번쩍임 · 파편 · 숫자)는 맞힌 횟수 `*_blows` 가 올라간 것으로 안다** — 대기가 올라간 것은
휘두르기의 시작이지 닿음이 아니다. 헛친 휘두르기는 횟수를 안 올린다.
⚠ **걷기와 숨쉬기는 그려지는 자리로 가른다**, 시뮬레이션의 자리가 아니라. 도착한 몸은 칸 안의 제 자리까지
미끄러지므로 시뮬레이션 자리로 재면 미끄러지며 숨을 쉰다.

## 손

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **손** | `Hand` | **플레이어가 고른 것과 그것이 갈 수 있는 자리.** 왼쪽 하나로 다 한다 — 끌면 상자, 짧게 누르면 이동, 빈 땅은 쥔 채로, 놓기는 ESC 나 빈 상자. 판은 WASD · 화면 끝으로 밀고 QE 가 90 도씩 돌린다 | **오른쪽 버튼은 아무것도 안 한다** |
| **짓기 모드** | — | **B 로 들어가고 나간다.** 안에서 왼쪽 누름은 「여기 지어라」 하나뿐이고, 지을 자리가 땅에 표시된다. ESC 로 부대를 쥔 채 나온다 | ⚠ **닿는 건물은 창고 하나뿐이다** (09-03) |
| **공격이동** | — | **A 를 누르고 누르면 싸우며 간다.** 그냥 누르면 지나간다 | ⚠ **안 지었다** (03-20) |
| **선택 상자** | `FieldView.set_box` · `Look.COL_SELECTION_BOX` · `SELECTION_BOX_*` | **왼쪽으로 끈 영역이 땅 위에 민트로 채워진 것.** **채움이 주제고 테두리는 얇다.** 8 px 마다 땅에 투영돼 판을 따라 오르내리고 판과 같이 돈다. 채움 알파 0.28 | ⚠ **화면 위의 사각형이 아니다** — 판을 돌리면 같이 돈다. ⚠ **그림이 아니다** — 기하라서 `assets/ui/` 에 파일이 없다. **검사만 잡는다** — 늑대는 상자에 안 든다 |

---

## ⚠⚠ 한국어와 코드가 엇갈리는 자리

**낱말을 뒤집으면서 심볼은 안 바꿨다.** 이름만 보고 잡으면 반대되는 것을 잡는다.
⚠⚠ **라벨이 값과 어긋나면 그 라벨로 짜인 검사가 조용히 다른 것을 재기 시작한다.**

| 이름만 보면 | 실제로는 |
|---|---|
| `coast` · `waterline` · `Islands.coast()` | **해안선이 아니라 윤곽이다.** 흰 선이 아니라 땅의 모양을 돌려준다 |
| `foam_*` | **셰이더에서는 흰색 하나뿐이다.** `look.gd` 의 `foam_lip_*` 은 **아무것도 안 읽는다** — 지금 해안선은 `line_*` 과 `second_*` 이 그린다 |
| `tile` · `tiles` · `TILE_PX` | **블록이 아니라 조각이다.** 한국어와 반대로 읽힌다 |
| `soldier` · `army` · `unit` | **진영 중립으로 지었지만 플레이어 쪽이다.** `battle.enemy_*` 가 짐승이다 |
| `cell_at` · `cells` · `title_cell_*` | **격자와 화면의 칸이다. 생물이 아니다** — 건드리지 않는다 |
| 섬 파일의 `H` | **항구가 아니다.** 테두리 글자 108 조각이고 `src/` 에서 아무것도 안 읽는다 |

## 다시 제안하지 않는 낱말

**이 낱말들은 사용자가 물렸다. 새 이름을 지을 때 고르지 않는다.**

**등급 · 일꾼 · 일반병 · 항구 · 메인 캐릭터 · 종족 · 빌드 · 지도 · 설계도 · 광산 · 세포 · 캐릭터 ·
부리 · 무리사냥 · 보상 화면 · 정비 화면 · 장비 카드 · 곰 · 까마귀 · 사자 · 거품 · 물주름**

⚠ **「세포」는 `src/` 에 한 줄도 안 남았다** — `net_title` 의 라벨 하나에만 산다.
