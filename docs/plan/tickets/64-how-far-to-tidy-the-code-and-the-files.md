Type: grilling
Status: open

# 코드와 파일을 어디까지 정비할 것인가

## 파일을 나누는 것 — **지금은 안 한다 (2026-08-30, 사용자)**

***「뚱뚱한 파일은 근데 아직 코드가 기능을 개발 중이어서 나눌까 말까 고민하고 있는데. 괜히 뭐 빨리
나누는 거 아닐까 하는.」*** 그리고 그날 끝에 ***「그럼 나눌 필요는 없으니까 여기서 마무리하는 걸로」***.

**이 세션의 목적은 원래 나누는 것이었고, 나누지 않고 닫혔다.** 사용자가 처음 연 말은
***「코드가 너무 많이 벌어져 있어서 코드들을 논리적으로 분리해서 관리하고 싶은데 좀 이른걸까?」*** 였다.

⚠⚠ **다시 열 때 이 두 가지를 섞지 않는다.**

| 무엇 | 상태 |
|---|---|
| **폴더 구조를 새로 짜기** | **필요 없다.** 안 보이는 계산 / 보이는 것 / 둘을 잇는 것, 세 갈래가 이미 서 있다 |
| **뚱뚱한 파일을 쪼개기** | **필요하다. 다만 기능이 굳은 뒤다** — 사용자의 판단이다 |

**뚱뚱한 넷 (2026-08-30 잰 값)** : 겉모습 값 **2,170** · 필드 화면 **2,403** · 격자 **1,577** ·
전투 **1,457**. 나머지 아홉 파일은 전부 300 줄 아래다.

⚠ **나누기 전에 그물이 초록이어야 한다.** 지금 빨강이 61 개라(티켓 15) 나누다 깨뜨려도
실패 개수로는 구분이 안 된다.

---

## 안 쓰는 파일 — **잰 것만 있고 아직 아무 결정도 없다**

사용자: ***「정리하지 말고 뭐뭐가 있는지 리스트부터」*** (2026-08-30). **목록만 냈고 하나도 안 지웠다.**

**리포 추적 전체 162 MB 중 146 MB 가 구운 그림이다.**

| 무엇 | 얼마 |
|---|---|
| `tools/shot/out` — 게임 화면 스크린샷 | **415 장 · 103 MB** |
| `.prototypes/*/out` — 후보를 구운 그림 일곱 폴더 | **482 장 · 43 MB** |
| 게임 코드가 안 읽는 `assets/` 그림 | **28 장** |
| 아무도 안 부르는 스크립트 | **2 개** — `tools/shot/shoot_shore.gd` · `.prototypes/pads/tone_live.gd` |
| 병합 찌꺼기 `.orig` · 짝 없는 `image.png.import` | **3 개** |

⚠⚠ **안 읽는 그림 28 장 중 둘은 살아 있는 티켓이 든다** — `boat_small.glb` 는 **티켓 54**,
`pieces.glb` 는 **티켓 01**. **그 둘이 닫히기 전에 지우면 결정할 대상이 없어진다.**

⚠ 폰트 라이선스 `assets/font/OFL.txt` 는 아무도 안 읽지만 있어야 한다.

## 무엇이 되면 끝인가

**사용자가 두 가지를 각각 답한다** — **언제 나눌 것인가**, 그리고 **위 목록에서 무엇을 지울 것인가.**
