# 던전 구조 설계 — 「열린 맵 · 지점을 밟고 · 살아 나온다」

> **세99 사용자 확정.** 발단 = *"메인 루프를 만들어서 이제는 루프 테스트를 하면서 디테일을 잡아볼 생각이야.
> 이번주 목표는 깔끔 1루프를 만드는거임"* → *"던전 구조부터 잡자"*.
>
> 🔴🔴 **이 설계가 존재하는 진짜 이유** — 사용자 원문: *"내가 생각하는 구현이 아니라 AI가 대략적으로 구현해 놓은
> 상태여서 수정 필요"*. 지금 던전은 **기획 없이 얹힌 얼개**다. 그래서 이 문서는 「기능 추가」가 아니라
> **「무대의 정체를 처음으로 정하는 것」**이다.
>
> 관련 정본: `docs/GDD.md` §6(익스트랙션) · `docs/PROGRESSION.md`(관문) · `docs/BACKLOG.md`.
> 앞선 결정: 세57 「뼈대는 확정, 살은 랜덤」 · 세58-B 「챕터 = 보스방」 · 세88 「잡몹 사냥 사슬」.

---

## 0. 착수 전에 — 지금 무대의 실체 (세99 실측)

| | 실제 |
|---|---|
| 무대 | `boss_room.tscn` **한 장을 챕터 셋이 공유**한다. 크기·타일·형태가 전부 같다 |
| 챕터 차이 | `room_ground_color`(바닥 틴트) + `mob_spawns` + `boss_enemy_id`, **그게 전부** |
| 랜덤성 | **0.** 몹이 `MobSpawn.position`에 손으로 박힌 좌표로 선다 |
| 공간 | 2400×2200 열린 한 칸. 남쪽 입구(`y=+600`) → 북쪽 보스(`y=−1350`) = **편도 1950px** |
| 귀환 | 남쪽 `&"exit"`(상시) + 보스 처치 후 `&"portal"`. 둘 다 **[E] 즉시** |
| 한 판 | 잡몹 9~13 + 보스 · 왕복 이동만 20~28초 · **≈2~4분**(추정) |

⚠ **그 2~4분은 마나 대기를 안 넣은 값이다** — 실측: ch1 전멸에 **37발**이 필요하고 마나가 6발 연발 + 발당 8초 재생이라
**마나 대기만 약 250초**다. 지점이 붙으면 더 커진다. §8의 「한 판의 목표 길이」를 정할 땐 **이 축을 같이 봐야 하고,
S15 때문에 F5로는 안 보인다**(에디터는 발사 마나가 공짜다).

🔴 **세57 원칙 「뼈대는 확정, 살은 랜덤」이 절반만 실현돼 있다** — 뼈대도 살도 다 확정이다.

🔴🔴 **실측이 찾은 루프의 실제 구멍**(이 설계가 겨냥하는 것):
**ch1 한 판의 확정 획득 = coin 18.5 + 재료 5.6개**뿐이고, ch1에서 만들 수 있는 부품은 `gr_spread3` 하나인데
갑충이 방에 **1마리**라 **8판**이 걸린다 → **ch1 클리어 후 다음 해금까지 8판이 통째로 빈다.**
= 사용자가 말한 *"텅 비어 걷기만 한다"*의 데이터판이다.

---

## 1. 확정된 뼈대 여덟 (세99 사용자 선택)

| # | 무엇 | 확정 | 각하한 것과 이유 |
|---|---|---|---|
| **D1** | **형태** | **열린 맵 + 탈출구 여럿**(타르코프식) | 방 연결(하데스·아이작)·순서 스테이지(세피리아) 각하 — GDD §6의 「가방·귀환이 긴장」과 결이 맞고 지금 코드를 가장 적게 뒤집는다 |
| **D2** | **한 판의 끝** | **탈출이 목표 · 보스는 선택** | 「보스를 잡아야 끝」 각하 — 그러면 *"더 갈까 말까"*의 답이 항상 「보스까지 간다」로 고정된다 |
| **D3** | **깊이의 표현** | **랜드마크(지점)들 · 위험도 띠 없음** | 동심원·남북 3구역 각하. ⚠ **대가를 받아들였다**: 「깊이 들어갔다」 감각이 약해지므로 **위험·보상의 층을 지점 자신이 진다** |
| **D4** | **지점의 성격** | **지점마다 다르다** — 폐허 · 둥지 · 제단 | 「전부 몹 지키는 보물」 각하 — 다섯 바퀴째에 *"또 같은 거"*가 된다 |
| **D5** | **랜덤성의 범위** | **지형 고정 · 몹 구성 랜덤** | 지형 절차 생성 각하. 🔴 **세91 선례** — 절차 생성 산출물이 규격 7/7을 통과하고도 *"계산된 것처럼 보인다"*로 각하됐다. 맵도 같은 함정이다 |
| **D6** | **네임드** | **보스=고정 · 네임드=매 판 확률** | 「보스도 확률」 각하. 🔴 **세57 사용자 원문** *"언제 어떻게 룬을 얻을지 내가 설계를 못 하니까 불안하다"* — 진행에 걸린 보상을 운에 걸면 그 불안이 그대로 돌아온다 |
| **D7** | **탈출** | **상시 열림 · 꾹 눌러 몇 초 · 맞으면 취소** | 「즉시 탈출」 각하 — 위험해지면 그냥 누르면 되니 *"쓸어담고 나간다"*의 안도감이 안 생긴다 |
| **D9** | **보스 귀환 포탈** | 🔴 **은퇴**(세99 사용자 확정 — *"보스죽으면 포탈 x"*) | 포탈만 「보스를 잡아야 열리는 특별한 물건」이라 **규칙이 둘**이었다. 없애면 **보스를 잡아도 살아서 출구까지 돌아가야 한다** = 익스트랙션의 결. 🔴 **그 대신 D1이 값을 낸다** — 보스 근처에도 상시 출구를 두면 「지름길」 기능은 남고 규칙은 하나가 된다 |
| **D8** | **챕터 구별** | **챕터마다 무대를 따로 만든다**(맵 3장) | 「지점 조합만으로 구별」 각하. ⚠ **사용자가 "이번 주 목표를 넘긴다"는 경고를 보고 선택했다** — §7 순서 참조 |

### 한 판의 흐름 (확정본)

```
입장(남쪽)
  │
  ├─ 지점을 고른다 ── 🏚폐허 / 🪦둥지 / 🪵제단  ← 매 판 내용물이 굴러간다
  │                        └ 네임드가 있을 수도, 없을 수도
  │
  ├─【판단】 지금 나갈까, 더 밟을까
  │     │
  │     ├─ 탈출구로 → [E] 꾹 ███░░ → 가방 확보 ✓
  │     │                    ↑ 맞으면 취소
  │     └─ 더 깊이 → 👹보스 → ✦문양(진행 확정) → **살아서 출구까지** → 탈출
  │                                              ↑ D9: 포탈은 없다
  │
  └─ 죽으면 bag_lost → 마을
```

---

## 2. 데이터 스키마 — 「새 X = .tres 한 장」을 지킨다

🔴 **원칙**: 기존 필드는 **의미를 안 바꾼다.** 새 필드는 전부 **비어 있으면 지금 동작**이다.

🔴🔴 **단, 그건 「데이터 축」에만 참이다**(세99 architect 리뷰가 이 문서의 초안 주장을 반박했다).
초안은 *"회귀가 구조적으로 0"*이라고 적었는데 **코드·그물에는 거짓**이다 —
필드를 **비워 두면** 아무 일도 안 나지만, **실제로 채우는 순간** 기존 계약과 기존 그물이 깨지는 자리가 넷 있고
그중 둘은 **에러가 0**이다(→ §6의 S9·S10·S11·S12).
**「되돌리기가 싸다」와 「켜는 게 안전하다」는 다른 말이다.**

### 2-1. `MobSpawn` — 좌표 고정, 정체는 굴림

```
enemy_id     : StringName   (기존) 🔴 값이 있으면 지금 그대로 — 회귀 0
position     : Vector2      (기존)
pool_tag     : StringName   (신설) 비면 기존 동작. 값이 있으면 ChapterDef.mob_pool에서 굴려 뽑는다
```

**왜 좌표는 고정인가** — 사용자 확정 *"지형은 동일하고 나오는 몬스터들이 랜덤"*.
스폰 자리는 지형이 정하는 것(길목·빈터)이라 사람이 놓는 게 맞고, **바뀌는 건 「거기 뭐가 서 있나」**다.

### 2-2. `ChapterDef` — 풀과 네임드가 붙는다

```
(기존 전부 유지: id · order · title · boss_enemy_id · boss_scene_path · boss_spawn ·
 room_ground_color · mob_spawns · reward_unlock)

room_scene_path : String              (신설) 비면 boss_room.tscn — 🔴 D8의 착지점
mob_pool        : Array[MobWeight]    (신설) 비면 랜덤 없음(지금 동작)
named_pool      : Array[NamedSpawn]   (신설) 비면 네임드 없음
landmarks       : Array[LandmarkSlot] (신설) 비면 지점 없음
extract_points  : Array[Vector2]      (신설) 비면 남쪽 출구 하나(지금 동작)
```

### 2-3. 신설 스키마 넷

```
MobWeight        enemy_id · weight(int) · pool_tag
                 → 「이 챕터의 잡몹 풀」. weight 비례로 뽑는다

NamedSpawn       enemy_id · chance(0~1) · at_landmark(StringName, 비면 자유 위치) · position
                 → 매 판 각각 굴린다. 🔴 서로 독립 — 「하나도 안 뜸」이 정상 결과다

LandmarkSlot     landmark_id · position
                 → 맵의 어느 자리에 어떤 지점이 서나 (위치 고정)

LandmarkDef      id · kind(RUIN/NEST/ALTAR) · scene_path · 종류별 params
                 → data/landmarks/*.tres. 🔴 「새 지점 = .tres 한 장 + 프롭 씬 한 장」
```

### 2-4. `InteractZone` — 홀드를 얹는다 (공용이라 조심)

```
hold_sec : float = 0.0   (신설) 🔴 0이면 지금 그대로 즉시 — 마을의 책상·문은 한 톨도 안 바뀐다
```

🔴 **값의 소유자는 `balance`다 — `hold_sec`은 「0이냐 아니냐」 스위치로만 써라.**
출구가 여럿이면 씬마다 한 벌이 생겨 **튜닝할 값이 흩어진다**(§8이 이 값을 F5 미결로 남겼으므로 반드시 한 곳이어야 한다).
⚠ 실측: **`data/balance.tres`는 값이 한 줄도 안 적힌 껍데기**이고 전부 `balance_data.gd`의 `@export` 기본값으로 돈다
— 즉 「balance에 넣는다」의 실제 작업은 **스키마에 `@export` 한 줄**이고 그건 **core = 리드 몫**이다.

🔴 **홀드는 폴링(`_process`)으로 짜라 — 「뗐나」를 이벤트로 받지 마라.**
`player_caster._tick_cast` 머리말이 **실측 세 가지**를 적어 뒀고 전부 에러가 0이다:
① 화면 덮는 Control이 `mouse_filter=STOP`이면 뗀 이벤트를 먹는다 ② 창 포커스가 빠지면 엔진이 액션만 풀고 이벤트를 안 쏜다(alt-tab)
③ `_unhandled_input`은 모달 중 아예 안 온다. **같은 함정이 [E] 홀드에 그대로 재현된다.**
✅ 덤으로 `_process` 카운트다운은 **씬 전환에 구조적으로 안전하다**(노드와 함께 죽는다 — `SceneTreeTimer`엔 그 보장이 없다).

**취소 트리거는 넷이다**(초안은 피격 하나만 적었다):
① 피격(`EventBus.player_hurt`) ② **범위 이탈**(`_on_body_exited`) ③ **모달 열림**(`GameState.ui_modal_open`) ④ 씬 전환(위 ✅로 자동).

⚠ **홀드 진행을 화면에 어떻게 보이나의 담당이 없다** — §1 다이어그램의 `███░░`는 새 물건이다.
**dev(진행 상태) + vfx(보이는 채우기)** 로 나눈다.

🔴 **`InteractZone`은 마을·보스방이 같이 쓰는 공용 배우**라 기본값이 곧 회귀 방어선이다.
피격 시 취소는 `EventBus`의 플레이어 피격 신호를 홀드 중에만 구독해 푼다(상시 구독 금지).

⚠ **`zone_id = &"exit"`와 `&"portal"`을 갈라 둔 게 계약이다** — 포탈은 보스 처치 후에만 뜨고(**매번 다시 뜬다**) 출구는 처음부터 있다.
탈출구를 여럿으로 늘려도 **전부 `&"exit"`**여야 `test_chapter_auto`의 포탈 그물이 안 빨개진다.

✅ **이중 전환 방어는 이미 서 있다** — 출구와 포탈이 같은 `_extract`로 묶여 있고 `_leaving` 가드가 두 번 나가는 걸 막는다.
**탈출구를 여럿으로 늘려도 이 가드가 그대로 커버한다**(새로 짜지 마라 — 세47 「이미 있는 배선」의 결).

---

## 3. 지점 셋 — 무엇이 다른가

🔴 **셋의 차이는 「보상 크기」가 아니라 「무슨 일이 일어나나」다.** 크기만 다르면 D4를 고른 의미가 없다.

| | 🏚 **폐허** | 🪦 **둥지** | 🪵 **제단** |
|---|---|---|---|
| **도착하면** | 전투 없음 · 걸린 상자 | 몹이 계속 기어나온다 | 조용하다 |
| **여는 값** | 열면 **소리가 나 몹이 몰려온다** | **핵(알)을 깨야** 멈춘다 | **마법을 바친다**(마나 소모) |
| **안 할 수 있나** | ✅ 지나가도 된다 | ✅ 지나가도 된다 | ✅ 지나가도 된다 |
| **주는 것** | 재료 · coin 뭉치 | 재료 **다량** | **상위 재료 · 소모품** — 🔴 **진행에 걸린 것은 안 준다** |
| **성격** | *"열고 버틸 자신 있나"* | *"들어가서 핵까지 닿을 수 있나"* | *"마나를 여기 쓸까 보스에 쓸까"* |

### 🔴 소유권 둘을 설계가 정해 준다 (구현자가 고르면 갈린다)

**ⓐ 적을 누가 스폰하나 — 「둥지」가 아니라 방이 한다.**
지금 리포의 preload 방향은 **field → props 단방향**이다(`forest_enemy`가 `drop_pickup`을, `boss_room`이 `portal`을 문다).
지점을 `src/props/`에 두고 거기서 `forest_enemy.tscn`을 물면 **그 방향이 처음으로 뒤집힌다**(takbon-rules §0 위반).
→ 🔴 **지점은 겉모습·판정만 지고, 「지금 하나 내보내라」를 부모에게 알린다. 스폰은 `boss_room`이 한다**
(이미 유일한 스폰 소유자다 — `_spawn_boss`·`_spawn_mobs`). **부모가 자식 시그널을 잇는 조합 루트라 EventBus 신설이 0**이고,
S1(스폰 수를 세는 그물)이 **한 자리에서** 성립한다.

**ⓑ 보상은 새 드롭 기계를 만들지 않는다.**
드롭 굴림·낱개 스폰은 `forest_enemy`의 `_roll_drops`/`_spawn_loose` **한 곳뿐**이고 둘 다 private다.
지점이 자기 로직을 새로 쓰면 `DropEntry`의 **배타 짝 규칙 셋**이 두 벌이 된다(takbon-rules §5-1이 「조용히 깨진다」로 못 박은 자리).
→ 🔴 **지점 보상도 `DropEntry` 배열을 쓰고, 굴림·스폰을 field 안 공용 함수로 한 번 올려** 적과 지점이 같은 것을 부르게 한다.
✅ **해금은 더 간단하다 — `EventBus.codex_unlocked` 한 발**이면 끝이다(발신처가 이미 넷).

⚠ **`until_unlock`을 지점과 같이 켜지 마라** — `DropEntry.unlock_id`와 병용하면 `if…elif` 때문에
**`chance`를 통째로 안 봐서 모든 잡몹이 확정 드롭**이 된다(takbon-rules §5-1).

### 🔴🔴 제단은 문양-고리를 주지 않는다 (세99 사용자 확정 — 초안을 각하했다)

**초안은 제단을 「문양-고리(상위)를 주는 자리」로 뒀다가 각하됐다.** 이유가 이 설계 안의 자기모순이었기 때문이다:

- 문양-고리는 **엔딩 조건(문양 전종 회수)의 유일한 통로**다 = 사실상 **진행 보상**이다.
- 그런데 제단은 **매 판 굴러가는 물건**이다.
- → **D6가 막으려던 바로 그 상태**(*"언제 어떻게 얻을지 설계를 못 하니까 불안하다"* — 세57 사용자 원문)가 된다.

🔴 **확정: 제단은 「있으면 좋은 것」만 준다** — 상위 재료·coin·소모품.
**문양-고리는 확정 경로로만 나간다**: 보스 드롭 · 공방 제작 · 퀘스트 턴인.

⚠ **그래서 「문양 9종 중 4종(`pierce`·`homing`·`bounce`·`thrust`)에 획득 경로가 없다」는 이 설계가 안 푼다**(→ §5).
실측: 통로가 문양-고리뿐인데 `data/glyph_rings/` 5장이 쓰는 motif가 `0·1·6·7·8`뿐이라 **엔딩 조건의 44%가 도달 불가**다.
🔴 **별건으로 푸는 게 오히려 확실하다** — 공방 레시피 `.tres` 몇 장이면 확정 경로가 서고, 그게 D6와도 맞는다.

### 아트 명세 (🔴 도형 플레이스홀더 금지 — 세54 확정)

| 지점 | 화면 키 | 상태 | 비고 |
|---|---|---|---|
| 폐허 | 약 64px | 닫힘 / 열림 | 세계관 = 「마법진이 있던 자리의 잔해」 |
| 둥지 | 약 56px | 활성(몹 나옴) / 파괴 | 핵이 **약점으로 읽혀야** 한다 |
| 제단 | 약 48px | 잠듦 / 빛남 | 🔴 **「빛은 없어야 할 곳에 있다」** 잣대 적용 — 바치면 빛이 든다 |

정본 = `docs/ART_SPEC.md` · 팔레트 TAKBON 60 · 담당 = `takbon-art`.

🔴 **위 표만으로는 `takbon-art`가 못 그린다 — 넘기기 전에 넷을 채워라**(세99 리뷰 지적):

| 채울 것 | 왜 |
|---|---|
| **파일 경로** | `assets/sprites/props/…`인지 `field/`인지 — 씬의 `texture` 배선이 여기서 갈린다 |
| **프레임 크기** | 화면 키 64px면 프레임은 64×64인지 96×96인지(여백 포함). ART_SPEC은 표시 배율 ×1이라 **프레임이 실루엣보다 커야 한다** |
| **상태 전환 방식** | `Sprite2D` 텍스처 2장 교체인가, `AnimatedSprite2D` 태그 2개인가. ⚠ 적의 `params.sprite` + 가로 스트립 규약은 **`forest_enemy` 전용이라 프롭엔 안 쓴다** |
| **실제 컷 수** | 「3종」이 아니라 **최소 6컷**(3종 × 2상태)이고, 핵·상자가 별 물건이면 **6~8컷**이다 — 단계 3의 art 분량을 그렇게 잡아라 |

---

## 4. 네임드 — 「오늘 뭔가 있다」

- 매 판 입장 시 `named_pool`의 각 항목을 **독립적으로** 굴린다. **하나도 안 뜨는 게 정상 결과**다.
- 뜨면 지정 지점(`at_landmark`) 또는 자유 위치에 선다.
- 🔴 **표시 = 생김새다. 빛나게 하지 마라**(세99 사용자 확정 — *"눈으로 봤을때 차이가 있는데 몸에서 빛이나는거 까진 별로임"*).
  **덩치·색·실루엣**으로 구별한다 — 화면에 들어오는 순간 *"저건 다르다"*가 읽히면 된다.
  ⚠ **화면 밖 표시(오라·HUD 화살표·입장 알림)는 안 만든다** — 각하됐다.
  ✅ **`EnemyDef.params` 한 장으로 성립한다**: `size`·**`tint`**·`sprite`·`anim_fps`가 `_apply_look`/`_setup_frames`를 지난다.
  🔴🔴 **`params.color`가 아니다 — 초안이 틀렸고 세99 구현이 실측으로 반박했다.**
  `color`는 **세45에 스프라이트로 갈아타며 `_visual.color`가 사라져** 지금은 **죽음 퍼프 색**으로만 산다
  (`forest_enemy._apply_look` 주석이 직접 그렇게 적어 뒀다). 그대로 갔으면 **네임드가 덩치만 다르고 색은 그대로**였을 것이다.
  → 몸을 물들이는 키는 **`params.tint`**(세99 신설)이고, `_refresh_tint`에서 상태이상 틴트와 **곱한다**
  (⚠ **대입하면 첫 상태이상에 색조가 영영 지워지는데 에러가 0이다**).
  🔴 **`params.size`는 루트 scale이라 덩치가 곧 히트박스**이고 `_attach_shadow`가 그림자를 파생시킨다 — 키우면 그림자도 따라온다(공짜).
  ⚠ **색만 바꾼 것을 「새 적」이라 부르지 마라**(세54의 결) — 전용 스프라이트는 나중에 `takbon-art`가 그린다.
- 드롭 = 재료 다량 + 고유 아이템. 🔴 **진행(문양)은 절대 여기 걸지 마라** — D6의 근거가 그것이다.

⚠ **네임드는 새 적 종이 아니어도 된다** — 기존 적의 강화판(HP·크기·색)으로 시작해도 성립한다.
전용 스프라이트는 나중에. 단 **색만 바꾼 것을 「새 적」이라 부르지는 마라**(세54의 결).

---

## 5. 이 설계가 푸는 것 / 안 푸는 것

### 푼다
| 불만 | 어떻게 |
|---|---|
| *"텅 비어 걷기만 한다"* | 걸어가는 곳마다 **지점**이 있다 — 이동이 「이동」이 아니라 「경로 선택」이 된다 |
| *"매 판이 똑같다"* | 몹 풀 굴림 + 네임드 확률 + 지점 내용물 굴림 |
| *"공간이 하나뿐"* | 탈출구 여럿 + 지점 = **경로가 생긴다**(방을 안 나누고) |
| *"챕터끼리 구별이 안 된다"* | D8 — 무대 3장 |
| **ch1 이후 8판 공백**(실측 ⑤) | 지점이 판마다 확정 산출을 준다 — 「잡몹만 20마리」 구간이 사라진다 |

### 안 푼다 (별건)

⚠ **새 항목으로 또 적지 마라 — 아래 넷은 `docs/BACKLOG.md`에 이미 자리가 있다**(N3 정제대·상점 · N7 잉크 축의 거취 · N9 밸런스 F5).
**기존 N 항목에 「세99 실측이 이걸 확인했다」로 붙이는 편**이 낫다(두 벌이 되면 세51·84·87의 갈라짐이 반복된다).
- 🔴🔴 **새 게임에서 오프닝 대사 6줄이 안 뜬다**(시드 도안이 `ring_designs.is_empty()` 가드를 거짓으로 만든다 · q00이 첫 프레임부터 완료)
- 🔴 **`ink_high`(피해 ×1.7)가 공짜** — 상점을 못 가는데 첫 판부터 고를 수 있고 소모도 안 된다
- 🔴 **첫 조립이 시드 도안보다 점수가 낮다**(시드 1.00 vs 시작 부품 최고 0.85) — *"조립했더니 약해졌다"*
- 🔴 **문양 9종 중 4종에 획득 경로가 없다**(엔딩 조건의 44%) — 제단으로 풀려다 **각하했다**(§3). 확정 경로(공방 레시피)로 별건
- 기념비 진행도 미구현 · 정제대/상점 도달 불가 · `until_unlock` 데이터 0줄
- ⚠ **책상의 화면 문구가 `"[E] 마법진 서고"`인데 대사·문서는 `"책상"`이라 부른다** — 지금은 대사 자체가 안 떠서
  안 드러나지만 **위 첫 항목을 고치는 순간 어긋난다.** 둘을 같이 고쳐라

---

## 6. 🔴 조용히 깨질 자리 (착수 전 필독)

| # | 자리 | 왜 조용한가 |
|---|---|---|
| **S1** | **`enemy_id`가 빈 `MobSpawn`** | 풀 굴림이 실패하면 **아무 적도 안 서고 에러가 0**이다. 방이 텅 비어도 클리어는 보스로 판정돼 그린. → 스폰 **수**를 세는 그물 필수 |
| **S2** | **`.tres`의 `Color`는 반드시 4인자** | 3인자면 파서가 **리소스 전체를 조용히 버린다**(세50 바람 룬). ⚠ **지금 §2-3의 신설 필드엔 `Color`가 하나도 없다** — `LandmarkDef.params`에 색을 넣기로 하는 **그때** 해당한다 |
| **S3** | **네임드가 「항상 뜸/절대 안 뜸」** | 확률이 0이나 1로 굳어도 **에러가 없다**. 굴림 자체를 재는 그물이 필요(시드 고정 주입) |
| **S4** | **홀드 취소가 안 걸림** | `hold_sec`이 살아도 피격 취소가 죽으면 **D7의 긴장이 통째로 없어지는데 화면은 정상**이다 |
| **S5** | **탈출구를 `&"portal"`로 만듦** | 포탈은 보스 처치 후 전용 계약이라 `test_chapter_auto`가 빨개진다. **전부 `&"exit"`** |
| **S6** | **`room_scene_path`가 빈 값** | 지금 씬으로 조용히 폴백한다 — 맵을 새로 만들어도 **안 물리면 티가 안 난다** |
| **S7** | 🔴🔴 **그물이 실게임에 없는 상태를 만들어 잰다** | 세99 실측이 실제로 잡은 것 — `test_quests_auto`가 `ring_designs.clear()`를 해서 **전 스위트 그린인데 실경로는 죽어 있었다**. 새 그물은 **입장부터 출발**해야 한다(세97 [[takbon-reachable-vs-working]]) |
| **S8** | **`InteractZone.hold_sec` 기본값** | 0이 아니면 **마을 책상·문까지 홀드가 된다.** 기본 0이 회귀 방어선 전부다 |
| **S9** | 🔴🔴 **풀·네임드가 `boss_enemy_id`를 뽑는다** | `boss_room._on_enemy_died`의 판정이 **`enemy_id` 일치 한 줄**이다. 지금은 `mob_spawns`를 손으로 박아 안 겹치는 게 **우연히 보장**되고 있을 뿐 — 풀에 보스 id가 들어가면 **잡몹 한 마리로 `chapter_clear_*` + 보상 룬 + 포탈이 전부 나간다. 에러 0** → **스폰 시 `boss_enemy_id` 제외 가드 필수** |
| **S10** | 🔴🔴 **그물이 `boss_room.tscn`만 잰다** | `test_chapter_auto`가 씬 경로를 **하드코딩**한다 → D8로 새 맵을 물려도 **한 번도 안 재진다**(S7의 정확한 재발). 씬 로드를 `room_scene_path`에서 **파생**시켜라 |
| **S11** | 🔴 **기존 정산 그물이 `interacted`를 직접 쏜다** | `[4b]`·`[5]`가 **E를 안 누르고** 시그널을 emit한다 → **홀드가 아예 안 돌아도 그린**이다. 홀드 그물은 `Input.action_press` + 프레임 진행 + `action_release`로(선례 = `test_spell_cast_auto`) |
| **S12** | 🔴 **늘린 출구가 화면에 안 보인다** | 출구는 겉모습이 없는 InteractZone이고 **유일한 시각 표시가 `_fill_tiles`의 풀길**(`_exit.position`에서 파생)이다 → 2번째 출구부터 **플레이어가 거기 나갈 데가 있는 줄 모른다 = D1이 통째로 무효**. `$Exit` 단수 참조·`_extract` 단수 연결도 같이 풀어라(**안 이으면 가방이 조용히 증발**) |
| **S13** | **sticky 목표 줄이 D2 뒤에도 「보스를 잡아라」** | `boss_room._ready`의 그 줄이 방의 **유일한 안내**인데 재는 그물이 없어 **조용히 거짓 지시로 남는다**(sticky는 「유효한 지시만」이 계약) |
| **S14** | **둥지가 무한히 뱉는다** | 「핵을 깨면 멈춘다」가 안 돌아도 방이 **더 재미있어 보일 뿐 에러가 0**이다 → 상한(동시 N·누적 N)을 **데이터로 두고 그걸 재라** |
| **S15** | 🔴🔴 **제단의 마나 트레이드오프는 F5로 검증 불가** | `debug_free_cast = OS.has_feature("editor")`라 **에디터 실행은 발사 마나가 공짜다** → *"마나를 여기 쓸까 보스에 쓸까"*라는 긴장 **자체가 에디터에 존재하지 않는다.** ⚠ HUD의 `마나 = (테스트)`가 그 신호다 |
| **S16** | **챕터 `.tres`의 SubResource 폭증** | ch1이 **이미 `MobSpawn` 9개**다. 여기 셋이 더 붙으면 한 장에 20~30개 → **S2(Color 4인자)의 표면적**이 그만큼 넓어지고 손편집 오타가 리소스를 통째로 죽인다 |

💡 **S3(네임드 굴림)의 구현법** — 시드 고정보다 **확률을 0.0/1.0으로 주입해 양끝을 각각 재는 쪽**이 안정적이다.
지금 리포의 랜덤은 **전역 `randf()`뿐**이라 테스트가 전역 `seed()`를 고정하면 `_spawn_loose`의 각도 굴림 등이
**같은 스트림을 소비**해 순서가 조금만 바뀌어도 기대값이 어긋난다(= flake).

---

## 7. 구현 순서 제안 (⚠ 사용자 검토 대상)

🔴 **D8(맵 3장)은 이번 주에 안 끝난다** — 사용자가 그 경고를 보고 선택했다.
그래서 **루프 뼈대를 먼저 돌리고 맵을 그 위에 얹자**고 제안한다. 이유는 **맵을 그릴 기준이 뼈대에서 나오기 때문**이다
(지점이 몇 개고 탈출구가 어디 서는지가 정해져야 지형을 그 주위로 그린다). 순서는 사용자가 뒤집으면 그대로 따른다.

🔴🔴 **초안의 4단계표는 그대로 못 간다 — 리뷰가 숨은 의존 다섯을 찾았다.** 아래가 반영본이다.

| 단계 | 무엇 | 왜 여기 | 축 |
|---|---|---|---|
| **0** | 🔴 **core 선행** — 스키마 4장 + `ChapterDef` 5필드 + `MobSpawn.pool_tag` + **`Db.landmarks` 레지스트리** + `balance`에 홀드 기본값 | **없으면 단계 1~3이 전부 막힌다.** ⚠ `db.gd`의 `reload()`가 폴더 아홉을 **손으로 나열**해서 `data/landmarks`가 자동 편입 안 된다 — 빠뜨리면 `_load_dir`이 `push_warning`만 하고 **빈 배열**(지점 0개인데 에러 0) | 🔴 **리드** |
| **1a** | **탈출 홀드**(D7) | 씬 한 곳으로 닫힌다. 폴링 + 취소 4트리거 + 진행 표시 | dev + vfx |
| **1b** | **탈출구 여럿**(D1) | 방 배선이라 성격이 다르다 → **출구 프롭 씬 신설** + `$Exit` 단수 해체 + **모든 출구 `_extract` 연결** + **풀길 다중화** + **sticky 목표 줄 다시 쓰기**(S13) | dev |
| **1.5** | 🔴 **그물 범위화** | **랜덤을 켜기 전에** `test_chapter_auto`의 개수·항목 검사를 **범위형**으로 바꾼다. ⚠ 이 파일은 **세84에 이미 flake로 데인 자리**라, 순서가 뒤바뀌면 「빨간 게 정상」이 되어 **그물 신뢰가 죽는다** | dev |
| **2** | **몹 풀 굴림 + 네임드(자유 위치)**(D5·D6) | 맵을 안 건드리고 *"매 판 똑같다"*를 깬다. 🔴 **`boss_enemy_id` 제외 가드 필수**(S9) · **끝났다의 조건에 「네임드를 멀리서 알아본다」를 포함**시켜라(안 그러면 굴림만 돌고 아무도 못 느낀다) | dev |
| **3** | **지점 3종**(D3·D4) + `at_landmark` 해석 | 이 설계의 심장. **아트 6~8컷**이 병렬로 붙는다 | dev + art |
| **4** | **맵 3장**(D8) | 위가 정해져야 배치 기준이 생긴다. 💡 **상속 씬(inherited scene)으로** — `boss_room.tscn`을 부모로 하면 노드 계약을 물려받아 함정 재현이 대부분 사라진다. ⚠ 딸린 것 셋: `base.gd`의 `room_scene_path` 소비 · `boss_room.gd` 머리말 정정 · 그물 씬 파생(S10) | art + dev |

⚠ **`room_scene_path`의 실제 소비자는 `base.gd`의 `_on_chapter_selected`다**(`boss_room`이 자기 `_ready`에서 읽으면 **이미 그 씬에 들어와 있어서 늦다**).
폴백은 새 문자열을 적지 말고 **그 `@export` 값**을 써라(두 벌이 되면 T5).

⚠ **D8은 `boss_room.gd` 머리말의 명시 「설계 확정」(*"챕터별 씬 3장을 만들지 않는다"*)을 뒤집는다.**
사용자가 확정했으니 뒤집는 것 자체는 문제가 아니고 — **그 주석을 같이 안 고치면 다음 세션이 그걸 계약으로 읽는다**(T4).

**이번 주 「깔끔 1루프」의 현실적 착지점 = 0 → 1a → 1b → 1.5 → 2 + §5 「안 푼다」의 🔴🔴 첫 항목**(오프닝이 안 뜨는 것).
**3·4단계는 다음 주로 넘긴다.**

---

## 8. 미결 (F5·사용자가 정한다)

- **홀드 시간** — 3초는 시작점이다. 손맛은 걸어봐야 정해진다
- **네임드 확률** — 35%/25%는 예시다. *"가끔 뜬다"*의 체감은 실제로 돌려야 안다
- ✅ **네임드 표시는 정해졌다** — 생김새(덩치·색·실루엣). 빛·오라·HUD 알림은 **각하**(§4)
- **탈출구 개수와 위치** — 3개? 4개? 입구 근처에도 두나(= 「바로 나가기」를 허용하나)
- **지점 개수** — 챕터당 3~5개 제안. 많으면 한 판이 길어진다
- **제단이 실제로 무엇을 주나** — 「상위 재료·소모품」까지만 정했다. ✅ **문양-고리는 안 준다**(확정 · §3)
- **한 판의 목표 길이** — 지금 2~4분. 지점이 붙으면 늘어난다. 몇 분이 맞나

---

## 9. 되돌리려면

- **전부** — 신설 필드를 비우면 지금 동작으로 돌아온다(설계상 그렇게 짰다).
- **D7만** — `InteractZone.hold_sec = 0.0`.
- **D5·D6만** — `mob_pool`·`named_pool`을 비운다.
- **D8만** — `room_scene_path`를 비우면 공용 씬으로 돌아온다.

⚠ **되돌림이 안 되는 것 둘** — **출구 다중화(S12)와 목표 줄 다시 쓰기(S13)는 코드·문구 변경이라 필드를 비워도 안 돌아온다.**
그 둘만 손으로 되돌린다.
