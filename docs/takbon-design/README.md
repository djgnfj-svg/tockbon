# 탁본 기획 문서 (takbon-design)

`takbon-design` 스킬로 리드가 사용자와 대화하며 확정한 **기획·설계 문서의 영구 보관소**다.
새 설계는 여기 `<주제>_design.md`로 착지한다. (구현 보고·architect 리뷰는 **일회성**이라 **`docs/_reports/`**(gitignore)에 쓰고 반영 뒤 지운다 — 여기 두지 않는다. ⚠ 세96에 리포 루트 `scratch_*.md`에서 옮겼다.)

> 🔴 **세87 재인덱싱 — 이 표는 실제 `ls` 결과와 1:1이다.** 구현이 끝나 읽을 이유가 사라진 설계 **7개를 삭제**했다:
> `book_redesign` · `progressive_assemble_gate` · `guide_visual_layers` · `assemble_trace_slice`(조립·탁본 UI 체인 4종) ·
> `first_stage`(핵심이 반려돼 실행 대상 0) · `movement_feel` · `player_hooded`(아트·필 2종).
> 전부 **리포 안 살아있는 링크가 0**이었고 라이브 코드가 대신 진실을 들고 있다
> (`ring_forge_panel.gd`의 `enum Phase` · `ring_board.gd`의 `compose_guide_paths` · `player.gd`의 가속 램프 ·
> `player.tscn`의 `player_hood_sheet.png`). 필요하면 git 이력.
> ⚠ **줄 번호로 적지 마라** — 여기 있던 인용 4건이 **같은 세션 안에** 주석 정비로 전부 밀렸다(세87 실측: 625→631·789→800·174→173).
> ⚠ **삭제 안 한 것**: `draw_tools_panel`(테스트가 아직 종이 잔재를 잰다) · `nested_design`(rune_fill 미결의 유일한 기록) ·
> `oblique_impact`(STATUS·memory가 정본으로 참조).

## 문서

> 🔴 **개수를 적지 마라 — 세96에 헤더의 「전 10개」가 먼저 낡았고, 그 사이 `visual_language_design.md`(453줄)가
> 표에서 **통째로 빠진 채** 몇 세션을 갔다(아래 인덱스 규율을 이 파일 자신이 어겼다).
> 표만 유지한다: **표 줄 수 = `ls docs/takbon-design/*.md` − 1(README)**.

**마법 모델 (읽는 순서)**
| 문서 | 내용 | 상태 |
|------|------|------|
| `takbon_model.md` | 세68 조립→탁본 마법 모델 (마법진=수식 · 세 축 · 파밍 단위=문양-고리) | 🟢구현완료 ⚠낡음 — 🔒**`GDD.md` §3(마법 모델)이 「정본 상세」로 가리킨다** — 지우면 잠긴 진실원의 링크가 끊긴다. ⚠세83 **그리기 폐지**로 「손 긋기」 서술만 현재 상태 아님(스위치라 되돌리면 부활 → 본문 보존) |
| `jin_interpretation_design.md` | 세78 진별 해석 (룬 감쌈=연산 순서 · 층 · 진 규칙) | 🟢구현완료 — 🔵**현행 마법 모델 기준점**. M1(세79)·M2(세81)·M3①(세82) 완료 · **남은 M3 착수 킷 내장**(어휘 확장ⓑ·2층 점유ⓒ·규칙 데이터화ⓓ) |
| `glyph_data_design.md` | 세82 문양 효과·표현 데이터화 + 응축 | 🟢구현완료 — `GlyphDef.behavior`+`params` · 「새 문양 = **5곳→2곳**」 레시피 정본 · ⚠F5 미확인 |
| `nested_design.md` | 중첩 마법진 (복합 룬 × 재귀 진 × rune_fill) | ⚠낡음 — **프레임 은퇴**(세72·78, `jin_interpretation`이 대체). 살아있는 건 **#5 `rune_fill` 입력 수단·#7**뿐(그 안이 여기에만 있어 존치). ①코드 인벤토리는 낡음 |

**조립·탁본 UI**
| 문서 | 내용 | 상태 |
|------|------|------|
| `draw_tools_panel_design.md` | 세71 DRAW 도구 패널(잉크·실시간 점수) + 종이 축 은퇴 | 🟢구현완료 — ⚠종이 purge는 **절반**: 정제대 dead affordance는 `refine_panel._refresh`의 PAPER 필터로 닫혔고, 🔴**남은 잔재는 지우지 마라**(`test_ring_design_auto._test_paper_size`가 `Db.paper_zoom_max`·`paper_*.tres`를 잰다) |
| `guide_editor_design.md` | 세57 밑그림 직접 제작 도구 (커스텀 가이드) | ⏸보류 — `git stash@{0}`에 **완성본이 살아 있다**(이 문서 = 복원 레시피). 단순 `pop` 불가: 카탈로그(진3·룬6·문양9)·`compose_guide` 이행·**그리기 폐지 스위치** 재정합 필요 |
| `ring_as_gear_design.md` | 세100 **마법진을 장비처럼 착용** — 「한 화면에서 같이 관리」 · 부위 재편(펜·모자 은퇴 → 가방·신발) | 📄대기 — **구현 미착수 · architect 리뷰 반영 완료**(`BACKLOG N18`). ⚠ **세101까지 이 인덱스에서 통째로 빠져 있었다**(규율 위반 — 표 줄 수가 `ls`와 갈렸다). 🔴🔴 **겨냥하는 것은 자료구조 통합이 아니라 화면이다** — 인벤토리가 `{id:개수}` 사전이라 **개체 데이터를 담을 자리가 원리적으로 없어** 「진짜 아이템화」는 막혀 있다(도안이 별도 갈래인 건 실수가 아니라 필연이고, 불편의 정체는 **탭이 갈린 것**이었다). ✅ **세이브를 한 글자도 안 바꾼다 · 발사 경로를 안 건드린다**. 🔴 리뷰가 잡은 급소 셋: `CATEGORY_ORDER` 한 줄은 **아무 일도 안 한다** · `move_speed()`의 `HAT` 상수를 안 고치면 **신발이 화면에만 존재한다** · `test_save_auto`의 `ring_equipped[2]`는 빨강이 아니라 **런타임 에러**(= 거짓 초록) |
| `jin_shape_image_design.md` | 세83 진 모양을 PNG로 등록 (밑그림 데이터화) | 📄대기 — **구현 미착수**(진 윤곽은 아직 `ring_board.jin_guide_pts`의 `match` 절차 생성). 그리기 폐지가 굳으면 점열 추출이 불필요해져 **더 단순해진다** |

**진행·경제**
| 문서 | 내용 | 상태 |
|------|------|------|
| `hunt_loop_design.md` | 세87 사냥 흐름 (숲에서 얻어 마을에서 만든다) | 🟢**세88 구현 완료** — 발단 = 사용자 *"잡아도 얻는 게 없다"*. `dopamine_design.md` D절(룬 획득 통로)의 후계였고, **임시 시드를 실제 사슬로 바꿨다**(시작 12키+도안 3장 → 3키+도안 1장). 🔴 **문서 맨 앞의 「구현이 이 문서를 정정한 것」 6건을 먼저 읽어라** — 특히 **§3-B-1 coin 열은 정본이 아니다**(실측과 어긋난다)·§13-2 좌표 3개는 스폰과 겹쳐 옮겼다(정본 = `data/chapters/*.tres`). ⚠ F5 겉보기 판정 대기 |
| `enemy_feel_design.md` | 세87 적 손맛 (48px 재작 · 죽으면 마나로 터짐 · 예고 공격 + 범위 표시) | 🟡**부분 구현 · 🔴 착수 전 문서 갱신이 선행이다** — ⚠ **세104까지 「구현 미착수」로 적혀 있었다.** 실측: **단계 2(마나 폭발) 완료**(`src/actors/mana_burst.gd` — 문서가 교체 대상으로 삼은 `_spawn_death_puff`는 **리포에 없다**) · **단계 4 절반**(`hitbox_radius`+`test_enemy_hitbox_auto` 완료 · `attack_sprite` 0건) · **단계 6 charge분 완료**(`_show_charge_band`). 🔴🔴 **그리고 문서의 전제 둘이 실측에 뒤집혔다**: ⓐ **대상 적 6종 중 4종이 리포에 없다**(`slime`·`mist`·`beetle`·`vine` — 세99 사용자 청산 ⇒ §2-A-2·§6-2의 아트 12장 표는 **4행이 무효**이고 실제 대상은 `slime_elite` 하나 + attack 시트) ⓑ 결정 ⑥ *"구르기는 계속 꺼둔다"*가 거짓이다(`player.gd`의 `ROLL_ENABLED := true` — 세97) ⇒ **「예고 길이가 난이도의 전부」라는 §4-C-1의 밸런스 전제가 깨졌다.** 갱신 전에 착수하면 **없는 파일 4장의 아트를 그리고 이미 끝난 폭발을 다시 짠다.** 발단 = 사용자 *"터지는 게 밋밋"·"피하는 맛이 있게"*. `hunt_loop_design.md`의 짝이지만 **의존 해소돼 독립 착지 가능**(마나 회수 게임플레이 철회 — 폭발은 순수 연출). 🔴실측 계약 넷 내장: **시트 프레임은 정사각**(`_bake_strip`) · **히트박스는 캔버스를 안 따라온다**(`radius 9.0` 공용 × `params.size` — size를 내리면 최대 **−47%**) · **씬에 `move_child(…,0)`은 Ground 밑**(적 자식 + `top_level`이 답) · **`_show_gust_ring`이 `top_level` 누락 지뢰**(snake에 얹으면 1.5배) |
| `dungeon_structure_design.md` | 🔴 **세99 던전 구조** — 열린 맵 + 탈출구 여럿 · **탈출이 목표(보스는 선택)** · 지점(폐허·둥지·제단) · 지형 고정 · 몹 굴림 + 네임드 확률 · 챕터마다 무대 | 🟡**단계 0·1a·1b·1.5·2·3·3-b 완료 · 남은 것 = 단계 3의 나머지(폐허·제단)와 단계 4(맵 3장)** — ⚠ **세104까지 「단계 0 완료 · 1~4 대기」로 적혀 있어 규모를 약 5배 과대평가하게 만들었다.** 실측 증거: `exit_zone.tscn`·`_build_exits` · `_roll_pool_id`·`_spawn_named` · `nest.gd`·`_spawn_landmarks` · `drop_roll.gd`. **단계 4 미착수의 증거 = `room_scene_path`의 소비자가 0곳**(`test_dungeon_schema_auto`가 *"아직 소비자가 없다"*를 직접 단언한다). 🔴 단계 4는 **`boss_room.tscn`을 하드코딩한 그물 7파일**을 건드린다(설계는 `test_chapter_auto` 하나만 지목했다) — 발단 = 사용자 *"내가 생각하는 구현이 아니라 AI가 대략적으로 구현해 놓은 상태여서 수정 필요"*(= 던전은 **기획 없이 얹힌 얼개**였다). 확정 여덟(D1~D8)은 **전부 사용자가 직접 고른 것**이라 방향을 뒤집지 마라. 🔴 **착수 전 §6 S1~S16과 §7 순서를 먼저 읽어라** — architect가 **착수 전 치명 넷**을 잡았다(0단계 부재 · **잡몹이 챕터를 클리어한다** · **랜덤보다 그물이 먼저** · 늘린 출구가 안 보인다). 🔴 **「회귀가 구조적으로 0」은 데이터 축에만 참이다**(§2가 그 반박을 싣고 있다). ⚠ **제단은 문양-고리를 안 준다**(초안을 사용자가 각하 — 진행을 운에 걸지 않는다) |
| `gate_restoration_design.md` | 🔴 **세101 문 복원** — 마을 한가운데 **부서진 마법문**이 봉납 창구·진행도·최종 챕터 입구 셋을 겸한다 · 바치는 것 = **보스 재료** · 칸 = **최종 전 챕터 수 파생**(지금 3 · 목표 7) | 📄대기 — **구현 미착수 · architect 리뷰 반영 완료(치명 2·중요 7·사소 7)**. 🔴 **문서 맨 앞의 「리뷰가 이 문서를 정정한 것」 5건을 먼저 읽어라** — 초고는 **호출 경로와 목록 방향을 낙관했다**(색조는 가드를 고쳐도 **함수가 안 불리고**, 최종 챕터는 「넣지 마라」가 아니라 **「빼라」**였다). 발단 = 실측이 **엔딩 조건이 원리적으로 성립하지 않음**을 캤다(문양엔 해금 게이트가 없고 낱개로 고를 수도 없어 「전종 회수」의 잴 대상이 0이었다 — 에러 0·전 스위트 그린). 🔴 **이 문서가 `world_and_visual_design.md` §2를 정정한다** — 진행도 분모가 「문양-고리 전체」에서 **「최종 전 챕터 수」**로 갈렸다(사용자 확정: *"굳이 소모템 넣을 필요 없이 보스 재료가 좋지 않을까"*). ✅ **아트 0장** — 온전한 판 `monument_circle.png`이 리포에 이미 있고 아무도 안 쓴다. 🔴 착수 전 §8(G1~G17)·§10(착수 순서) 먼저 — 특히 **단계 3이 여섯이 한 원자**다(수신자·색조·불·스프라이트 교체·`test_base_auto` 두 갈래 수리). ⚠ **재료 경제 청산은 일부러 뺐다**(§11 · 사용자: *"제대로 정한 몬스터는 늑대밖에 없음"*) |
| `vision_design.md` | 🔴 **세101 시야** — 내가 보는 쪽(부채꼴) + 주변시 **밖의 적은 몸이 안 보이고 「기척」만 보인다** · 지형·드롭은 늘 보인다 | 🟢**구현 완료(세104)** — ✅ 화면 검증까지(대조군 스샷으로 **히트 플래시가 알파를 통과함**을 증명 · 그림자·피해숫자 누출 0 · 마을 무영향). 🔴🔴 **구현이 이 문서의 전제 하나를 뒤집었다 — §1의 「기척」 절을 먼저 읽어라**: *"예고와 돌풍 링이 기척 자리를 이미 채운다"*가 **거짓**이었다(예고는 120~160px에서 켜지는데 주변시가 220이라 **예고가 뜰 땐 이미 보인다**) ⇒ 시야 밖 적은 **아무 신호도 없는 완전 투명**이었다. 🔴 **사용자가 그 구멍을 게임을 안 켜 보고 짚어** 세104에 둘이 더 붙었다: **부채꼴 밖 30% 어둡게**(`vision_overlay.*` — 상한 0.35를 그물이 지킨다 · 세73으로 되돌아가는 것을 막는 방어선) · **발견 느낌표**(시야 밖 적의 **유일한 신호**). ⇒ **§9의 「범위 밖」 넷 중 둘(화면 어둠 · 새 기척 표현)이 살아났다.** ⏳ 남은 것 = `N31` F5. 발단 = 사용자 *"긴장감을 넣고 싶음"*. 🔴🔴 **사용자 확정 ⓒ(지형은 다 보인다)가 이 작업을 안전하게 만들었다** — 화면을 어둡게 할 필요가 없어 세73·세99를 **한 글자도 안 되짚는다**(시야는 조명이 아니라 **적 가시성**이다). ✅ **새 연출 0**(기척 = 이미 있는 돌진 바닥 예고·돌풍 링). 🔴 착수 전 §5-2 먼저 — `_visual.modulate`엔 **세63 소유권 계약**이 있어 시야 알파가 세 번째 소유자가 되면 안개 분산과 서로를 덮어쓴다(에러 0·전 스위트 그린). ⚠ **적 분류는 이름이 아니라 `.tres`의 어느 칸에 꽂혔나로** — `slime_elite`는 잡몹이 아니라 **ch1 보스**다(초고·리뷰가 **둘 다** 틀렸다) |
| `enemy_perception_design.md` | 🔴 **세102 몬스터 인지** — 적도 **「보는 쪽」을 갖는다** · 순찰(구역 배회) → 경계 → 짖고 추격 → 수색 → 복귀 · **마법 소리는 위력할수록 크게** · 못 본 적을 치면 **기습 배수** | 📄대기 — **구현 미착수 · architect 리뷰 반영 완료(치명 4·중요 6)**. 🔴 **`vision_design.md`의 거울이다** — 그게 「내가 적을 못 본다」면 이건 「적이 나를 못 본다」. 발단 = 사용자 *"몬스터 AI 설계를 좀 하고 싶은데"*(축 셋 중 **첫째 조각** — 무리·공격 사이클이 뒤에 온다). 🔴🔴 **착수 전 §8-1 먼저** — 초고의 *"`aggro_range` 은퇴"*가 **시야 설계의 심장을 끊었다**(주변시 220이 그 키에서 파생한다) ⇒ **은퇴가 아니라 `sight_range`로 이름 승계**. 🔴 **§7-b 하위호환 표가 그물 6파일을 지킨다**(인지 키 0개인 주입 몸이 거기 산다). ✅ **EventBus·스키마 신설 0** — 리드 몫은 `balance` `@export` 1개 · **`vision.gd` 부채꼴 공유** · `test_audio_auto.IDS` |
| `dopamine_design.md` | 세66 도파민 보상 루프 + 경제 재편 | ⚠낡음(부분 구현) — 🔒**`GDD.md:72`가 「경제·보상 상세」의 유일한 포인터**라 삭제 불가. A·B·C 구현 · D 절반(해독대·q05는 세85에 **은퇴 완료**) · 🔴**남은 계획 = 룬 획득 통로**(`qR_*` 0개·예식 없음 — 지금은 임시 시드 → `hunt_loop_design.md`가 잇는다) |

**세계관·아트·필**
| 문서 | 내용 | 상태 |
|------|------|------|
| `world_and_visual_design.md` | 🔴 **세95 전면 개정** — 깨진 마법진 · 흩어진 문양(이누야샤형) · 문이 유일한 화자 · 마법=빛 | 🟡**부분 구현** — ✅길잡이 NPC 은퇴·온보딩 폐지어 청산(`N2`) · ✅시든 잔디 타일(`N1` — 실제론 타일셋 6칸 전부 재작업) · 🔴 **§2 기념비 진행도는 「남은 조각」이 아니다 — `gate_restoration_design.md`(세101)가 통째로 대체했다**(⚠ 세104까지 여기에 *"남은 큰 조각"*으로 적혀 있었다). 분모가 「문양-고리 전체」 → **「최종 전 챕터 수」**로 갈렸고 기념비는 표시만 하던 물건에서 **봉납 창구**가 됐다. **둘을 각각 태스크로 띄우면 같은 `Monument`에 서로 다른 분모로 진행도를 두 번 붙인다(둘 다 에러 0)** — 살아남은 건 「빛으로 진행도를 보인다」는 방향과 `light_pool.set_base_energy()` 계약뿐이고 그건 gate 문서 §6-1의 한 줄이다. ✅ 이 문서 §2 본문엔 각하 스텁이 이미 들어가 있다 — **README만 낡았었다**. 🔴 **세89판(「망한 마을을 한 채씩 되돌린다」)은 각하됐다** — 문서 맨 앞 표가 「무엇이 죽고 살았나」를 가르니 **거기부터 읽어라**(옛 판을 근거로 착수하면 없는 목표를 만든다). 🔴 **이 게임에 처음 생긴 세계관 문서**이고 **제목 「탁본」의 근거를 회복시킨다**(「남이 새겨놓은 것을 떠서 쓴다」). `enemy_feel_design.md`의 「마나로 터짐」에 **이유**를 준다. 🔒 **§9가 GDD 개정을 제안한다**(§1·§2·§8 + 세계관 신설 — **허락 대기**) |
| `visual_language_design.md` | 세90 비주얼 어휘 — 제작 칸 · 표시 배율 · 화면 키 · TAKBON 60 팔레트 | 🟡부분 구현 — ⚠ **세96까지 이 인덱스에서 통째로 빠져 있었다**(453줄). 🔴 **§6에 세91 각하 사건이 있다**: *"설계는 승인됐고 구현도 규격을 100% 통과했는데 화면에 올리자 사용자가 각하했다"* — 이유는 **형태**(돌덩이·잎다발이 「마법이 아니라 물체」로 보였다)였지 팔레트·하드엣지가 아니다. `BACKLOG N11`②가 그 구분에 걸려 있다 |
| `spell_cast_visual_design.md` | 🔴 **세96 시전 연출** — 조립한 마법진이 **발밑에 열린다** · 등급이 시전 시간을 판다 | 🟢**구현 완료(세98)** — ⚠ **세104까지 이 표가 「📄대기 · 구현 미착수」로 적고 있었다**(같은 리포 `BACKLOG N13`은 ✅완료로 적어 **정면 모순**이었다 — 이 README 자신의 인덱스 규율 위반이다). 실체: `src/actors/floor_circle.gd` · EventBus `ring_cast_started`·`ring_cast_canceled`·`spell_impact` · 그물 `test_spell_cast_auto`·`test_floor_circle_auto`·`test_band_inverse_auto`. **남은 것은 코드가 아니라 `N19` F5 수치 조이기(사용자 몫)**. 🔴 **이 구현이 새 소유권을 만들었다** — `vfx.gd`가 `compose_guide_paths`의 **두 번째 소비자**가 됐고, 그게 `jin_shape_image_design`의 「소비자는 정확히 둘」 계약을 깬다(그 항목 참조). 🔴 **문서 맨 앞의 「리뷰가 이 문서를 정정한 것」 6건을 먼저 읽어라** — 초고는 일을 **실제보다 쉽게 봤다**(모듈 규칙은 애초에 예외였고 · 인자 둘이 없고 · 기존 그물 2건이 깨진다). 발단 = 사용자 *"이펙트를 넣었는데도 밋밋하다"* → 실측이 **다른 원인**을 찾았다: **조립을 잘해도 화면이 픽셀 하나 안 달라진다**(위력 78→157인데 연출 상수는 전부 고정). ✅ `VFX_SPEC §6-1`(탁본다운 이펙트의 감각 축) 미결을 **이 문서가 닫는다** |
| `oblique_impact_design.md` | 세77 오블리크 파이어볼 (바닥 그림자 + 두 겹 착탄) | 🟢구현완료 — `ring_carrier.tscn`의 `Shadow` 노드 · `vfx._spawn_flare`. ⚠**아카이브지만 존치**: memory가 **정본으로 참조**하고, 살아있는 잔여(**원소 확장 시 두 겹 구조는 색·모양만 교체**)를 든다 |

🟢구현완료 = 코드에 반영됨(설계는 「왜 그렇게 지었나」 기록) · ⏸보류 = 방향은 살아있고 착수 안 함 ·
📄대기 = 설계 착지·구현 대기 · ⚠낡음 = 서술 일부가 현재 코드와 다름(각 헤더 ⚠ 스텁 참조).
🔒 = 잠긴 `docs/GDD.md`가 가리키는 문서라 **삭제하면 GDD에 죽은 링크가 생긴다**(허락 없이 못 고친다).

> 🔴 **인덱스 규율** — 이 표가 `ls docs/takbon-design/*.md`와 갈라지면 그 자체가 버그다.
> 세78 인덱스는 `jin_shape_image_design.md`를 **통째로 빠뜨렸고**, `glyph_data`를 「구현 미착수」로
> 적어 그 문서 자신의 헤더(「🟢 구현 완료」)와 **모순**했다. 문서를 더하거나 지우면 여기부터 고쳐라.
