---
node: nested
stage: building
owns: [rune_fill]
needs: [jin_interpretation]
blocked: 개념 프레임이 세72·78에 은퇴했다 — 문서대로 구현하면 은퇴한 발사 모델을 되살린다. 살아있던 rune_fill 입력 수단 안(#5)은 세111에 TODO.md 「rune_fill 거취」로 승격했다
dead: true
---

# 🪦 중첩 마법진 통합 (복합 룬 × 재귀 진 × rune_fill) (묘비)

> 🔴🔴 **이 문서는 묘비다.** 세?? architect 산출(코드 없음 · 설계안만) · **세72·78에 개념 프레임이 은퇴**.
> 본문 22KB는 **세111에 걷었다** — 전문은 git에 그대로 있다(아래 「전문을 캐려면」).
> ⚠ **자리는 아직 `building`이다** — 이 문서가 설계한 것 중 **구현된 것은 0**이라 엄밀히는 `planned`에 가깝다.
> **재배치는 리드·사용자 몫**이다.

---

## 왜 죽었나

**개념 프레임이 토대째 은퇴했다.** 이 문서의 뼈대는
「진이 날아가 **자식 진을 착탄점에서 재발사**한다(재귀 배달) · 문양 = 8칸 착탄 · 평평한 `runes[]` 자동 반응」인데,

- **세72** — 진은 안 날아간다.
- **세68** — 문양 = 조립 감쌈.
- **세78** — `jin_interpretation_design.md`(done)가 이 프레임을 **대체했다. 공존이 아니다.**

🔴 **문서대로 구현하면 은퇴한 발사 모델을 되살린다.**

⚠ **본문의 코드 인벤토리(①)도 낡았다** — *"평평하다: `rune:int` 하나"*라 적었지만
지금 `RingDesign`은 `runes: Array`와 `runes_of()`를 든다(세81). **인벤토리를 근거로 쓰지 마라.**
⚠ **#4 복합 룬은 해소됐다** — 세81 M2가 `JinDef.rune_slots`로 실현했다(`data/jin/jin_fuse.tres` · 합산은 `ring_spell_system._fire_hit`의 share).

---

## 살아남은 조각이 어디로 갔나

| 무엇 | 어디 |
|---|---|
| 🔴🔴 **#5 `rune_fill` 입력 수단 안** — 이 문서가 유일하게 들던 자산이다(휠 값을 fill로 싣는다 · 소비자는 상태이상 세기 하나 · 위력엔 곱하지 않는다 · 옛 도안 폴백) | **세111에 `docs/TODO.md` 「rune_fill 거취」로 승격했다**(ⓐ~ⓓ). 🔴 **그전엔 이 문서 안에만 있었다** |
| **#7 취약 이중 증폭을 「의도」로 볼지 「사고」로 고칠지** | `docs/TODO.md` 「취약 이중 증폭」이 이미 들고 있다(방향 = **곱하는 자리를 하나로**) |
| **원소 반응 = `jin_interpretation` ②「진 규칙」의 한 인스턴스(융합진)**라는 흡수 | 그 문서(done)가 정본이다 |
| ①~⑧의 나머지(스키마 대안 A/B/C · 재귀 UX · 밸런스 함의 · 마일스톤 M1~M3) | 💀 **옮길 데 없음** — 은퇴한 프레임 위에 서 있다 |

⚠ **세108 정정 기록** — 세87에 적힌 존치 근거 셋(`CLAUDE.md`·`PROGRESSION.md`·memory 한 장이 이 문서를 가리킨다)은
**전부 죽었다**(세98 축소 · 세97 재작성 · 세107 memory 정리). **참조 0건을 근거로 삭제하지 마라**는 규율이 그래서 여기에 걸린다 —
세87에 백틱만 세어 「0건」이 나왔는데 평문 참조 셋이 실재했던 자리다.

---

## 전문을 캐려면

```
git show 2a42e3f:docs/takbon-design/building/nested_design.md   # 걷기 직전의 전문(22KB)
git log --follow -- docs/takbon-design/building/nested_design.md
```

🔴 **지운 게 아니다 — git이 든다.**
