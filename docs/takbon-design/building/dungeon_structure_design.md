---
node: dungeon_structure
stage: building
owns: [run_flow, mob_spawning]
needs: [genre_pivot]
blocked: 세110 장르 전환에 전제가 통째로 폐기됐다 — 첫 조각이 안 정해져 폐기 범위와 자리 이동이 리드·사용자 몫이다(맨 앞 스텁)
dead: true
---

# 🪦 던전 구조 설계 — 「열린 맵 · 지점을 밟고 · 살아 나온다」 (묘비)

> 🔴🔴 **이 문서는 묘비다.** 세99 확정 · 세100·101·104에 단계 0~3-b 구현 · **세110에 전제가 통째로 폐기**.
> 본문 67KB는 **세111에 걷었다** — 전문은 git에 그대로 있다(아래 「전문을 캐려면」).
> ⚠ **자리(`planned`/`building`/`done`)는 아직 안 옮겼다** — 첫 조각이 안 정해져 폐기 범위가 확정 전이라 **리드·사용자 몫**이다.
> 🔴 **`owns`를 지우지 마라** — 죽은 문서가 `run_flow`·`mob_spawning`을 붙들고 있는 것 자체가 새 문서의 무단 재정의를 잡는 그물이다.

---

## 왜 죽었나 — **오직 템포 하나다**

**세110 장르 전환.** 정본 = `docs/takbon-design/planned/genre_pivot_design.md`(2026-07-29 사용자 확정).
제목의 세 조각이 **셋 다 죽었다** — 「열린 맵」·「지점을 밟고」·「살아 나온다」.

사용자에게 넷을 놓고 물었을 때 고른 것은 **「템포가 느리다」 하나뿐**이었고,
**실측한 느림의 출처 넷 중 셋이 이 문서에서 나왔다**: 2400×2200을 걸어서 훑는다 · 낱개 드롭을 걸어가 줍는다 · 3초 꾹 탈출.

⚠ 🔴 **「익스트랙션이 재미없다」로 오독하지 마라** — 사용자는 **가방 증발·전투 회피·왕복 길이를 아프다고 하지 않았다.**
오독하면 `bag_lost`·긴장 장치까지 같이 버린다.

**새 무대** = 방을 이어서 깬다(하데스형 문 고르기 · 보상이 문 위에 미리 보인다 · 마을로 안 돌아가고 챕터가 연쇄).

### ❌ 죽은 것
D1 **상시 출구 여럿** · D2 **탈출이 목표(보스는 선택)** · D7 **3초 꾹 탈출** · D3·D4 **지점(둥지·폐허·제단)** ·
**열린 맵 2400×2200** · D8 **챕터마다 무대 한 장**(무대 단위가 방으로 바뀌었다).

---

## ✅ 살아남은 조각이 어디로 갔나

| 무엇 | 어디 |
|---|---|
| 🔴 **몹 굴림(D5·D6)·네임드 확률(D6)** — 코드가 살아 있다(`boss_room`의 `_roll_pool_id`·`_spawn_named` · 스키마 `MobWeight`·`NamedSpawn`). **`owns: mob_spawning`이 그래서 남아 있다** | 코드가 정본. F5 미결 = `docs/TODO.md` 「던전 한 판」의 살아남은 셋 |
| **「뼈대는 확정, 살은 랜덤」**(세57 원칙 — 이 설계가 비로소 절반을 채웠다) | `docs/PROGRESSION.md` 머리말 · `docs/DECISIONS.md` 「루프 · 진행」 |
| 🔴 **「진행 보상은 굴리지 않는다」** — 제단이 문양-고리를 주려던 초안을 **사용자가 각하**했다(세57 *"언제 얻을지 설계를 못 해 불안하다"*가 돌아온다). **잣대로 계속 선다** | `docs/DECISIONS.md` 「루프 · 진행」 · `docs/TODO.md` 「던전 지점 나머지」 |
| 🔴 **뭉치를 짜는 규칙 — 세 층(바닥 `1.0` / 몸통 0.45~0.5 / 꼬리 0.05~0.1)과 「막힌 것이 주력」(D16)** | **세111에 `docs/TODO.md` 「드롭을 상자로」로 승격했다**(지점은 죽었지만 상자 안을 채우는 규칙은 안 죽었다) |
| 🔴 **§5 「안 푸는 것」 중 집이 없던 둘** — 새 게임에서 **오프닝 대사 6줄이 안 뜬다** · **첫 조립이 시드보다 약하다** | **세111에 `docs/TODO.md` 「시드 도안이 온보딩을 막는다」로 승격했다**(2026-07-31 재실측 — 둘 다 살아 있다) |
| 🔴 **`ink_high`가 공짜다**(상점 도달 불가인데 첫 판부터 무한) | **세111에 `docs/TODO.md` 「재료 경제 정리」ⓒ로 승격했다** |
| **챕터 재료 국소성의 예언**(*"`hound_alpha`를 ch1에 그대로 쓰면 D15를 어긴다"* — 세104가 그 자리를 밟았다) | `docs/TODO.md` 「챕터 재료 국소성」 |
| **`room_scene_path`의 소비자가 0곳**(D8 미착수의 증거) · 손대면 `boss_room.tscn`을 하드코딩한 그물 7파일 | `docs/TODO.md` 「챕터별 맵」 |
| **공용 드롭 굴림 추출(S17)·보스 id 제외 가드(S9)** | ✅ **구현돼 있다** — `src/field/drop_roll.gd` · `boss_room._spawn_enemy_at`. 계약 정본은 코드 머리말 |
| 지점 산출·홀드·출구 F5 미결(§8·§10-5 전량) | 💀 **옮길 데 없음** — 잴 대상이 사라졌다 |

🔴 **걷어낼 때 그물을 지우지 말고 뒤집어라**(세107 차폐가 모범) — 걸리는 그물 =
`test_extract_hold_auto`·`test_nest_open_auto`·`test_landmark_road_auto`.

---

## 🔴 이 문서가 존재한 진짜 이유 (지우면 안 되는 한 줄)

사용자 원문: *"내가 생각하는 구현이 아니라 AI가 대략적으로 구현해 놓은 상태여서 수정 필요"*.
던전은 **기획 없이 얹힌 얼개**였고(챕터 셋이 같은 씬 · 몹 좌표가 `.tres`에 손으로 박혀 랜덤 0),
이 문서는 「기능 추가」가 아니라 **「무대의 정체를 처음으로 정하는 것」**이었다.
⇒ 🔴 **새 무대를 짤 때도 같은 잣대가 선다 — 얼개부터 얹지 말고 정체부터 정해라.**

---

## 전문을 캐려면

```
git show 2a42e3f:docs/takbon-design/building/dungeon_structure_design.md   # 걷기 직전의 전문(67KB)
git log --follow -- docs/takbon-design/building/dungeon_structure_design.md
```

전문에 있는 것: 확정 여덟(D1~D9)과 각하 사유 · 지점 셋의 성격표 · S1~S28 함정 전량 ·
세100 산출 설계(§10 · 재료 기대값 계산 전문) · 단계별 진행 기록과 커밋 해시.

🔴 **지운 게 아니다 — git이 든다.**
