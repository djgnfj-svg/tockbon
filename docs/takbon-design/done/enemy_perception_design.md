---
node: enemy_perception
stage: done
owns: [enemy_ai]
needs: [vision, dungeon_structure, hunt_loop]
dead: true
---

# 🪦 몬스터 인지 설계 — 「나를 아직 못 본 짐승」 (묘비)

> 🔴🔴 **이 문서는 묘비다.** 세102 확정 · **세105 전량 구현**(`97ed25c` — §9 단계 0~8) · **세110 D8에 기능째 폐기**.
> 본문 32KB는 **세111에 걷었다** — 전문은 git에 그대로 있다(아래 「전문을 캐려면」).

---

## 왜 죽었나

**세110 장르 전환 D8** — 사용자가 **「전부 걷는다」**를 직접 골랐다.
정본 = `docs/takbon-design/planned/genre_pivot_design.md` · 경위 = `docs/DECISIONS.md` 「비주얼」.

죽는 것 = **순찰→경계→추격→수색→복귀 상태기계 · 발견 `!` · 짖음 전파 · 기습 배수 · `sight_range`**.
🔴 **제목부터 성립하지 않는다** — 시야가 없으면 적은 늘 본다. 「나를 아직 못 본 짐승」이라는 전제가 통째로 빠진다.

⚠ **세106까지 README 표가 이 문서를 「📄대기 · 구현 미착수」로 적고 있었다** — 세105에 전량 구현된 뒤였다.
**그걸 읽고 착수했으면 이미 있는 기능을 다시 짰다.** 이 축(`takbon-docs`)이 재는 실패 양식의 대표 사례다.

---

## 살아남은 조각이 어디로 갔나

| 무엇 | 어디 |
|---|---|
| 🔴 **「공격 사이클」**(멈춤→예고→가격→빈틈) — 인지와 **독립**이라 혼자 살아남았다. 🔴 **오히려 더 중요해졌다**: 인지 표시(`?`/`!`)가 걷히면 *"적이 무엇을 하려는가"*를 알리는 채널이 **예고·범위 표시뿐**이다 | `docs/TODO.md` 「공격 사이클」 · 설계 정본 = `enemy_feel_design.md`(building) |
| 🔴 **`hound_alpha`의 「60px 더 멀리 본다」**(세101 사용자 확정) — `sight_range`와 함께 죽으면 **네임드를 무엇이 구별하나**가 빈칸이 된다 | `docs/TODO.md` 「시야 코드를 지우나 끄나」 ⓐ로 승격 |
| **「무리 행동」의 목적은 살고 통로가 죽었다** — *"짖음이 전파 통로"*(§12 예약)가 사라져 **무엇이 무리를 묶나**를 새로 정해야 한다 | `docs/TODO.md` 「무리 감각」(방향 미결) |
| **배회 겹침을 물리가 아니라 「위상차」로 푼 방식**(스폰 인덱스 기반 · 새 스키마 0) — 방 단위 전투에서도 그대로 쓴다 | 같은 「무리 감각」 항목의 ⚠ 줄 |
| 인지 감각 F5 조이기(`growl`↔`howl` 구별 · 경계 한 박자) | 💀 `docs/TODO.md` 「인지 감각」 — **같이 죽었다** |
| **`aggro_range` → `sight_range` 이름 승계**(§8-1 · C1) | 코드에 이미 실현돼 있다. **은퇴가 아니라 승계였다**는 사실만 남긴다 |

⚠ **되살릴 근거로 쓰지 마라.** §7-b(하위호환 폴백 표)·§8-2(보스 셋은 설계 밖)·§10(검증 계획)은
**인지 상태기계가 존재할 때만 뜻이 있는 계약**이다.

---

## 전문을 캐려면

```
git show 2a42e3f:docs/takbon-design/done/enemy_perception_design.md   # 걷기 직전의 전문(32KB)
git show 97ed25c                                                     # 세105 전량 구현 커밋
```

🔴 **지운 게 아니다 — git이 든다.**
