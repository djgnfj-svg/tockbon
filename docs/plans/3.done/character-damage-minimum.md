# 캐릭터가 다친다 — 체력 · 피해 · 반동 · 쓰러짐

**상태**: done — ⚠ **일부가 나중에 철회됐다**(아래)
**한 줄**: 내 마법이 나와 동료를 때린다. 100 HP가 깎이고, 0이 되면 쓰러진다. 쏘면 반동으로 밀린다.

## 🔴🔴 **밀림에서 둘이 삭제됐다** — 2026-08-04, 사용자 결정

**맞아도 몸이 안 움직이고, 아래로 쏴도 안 뜬다.** 피해·무적·쓰러짐·불은 그대로고,
**발사 반동은 좌우로만 남았다.**

| | 상태 |
|---|---|
| 체력 · 피해 · 무적 · 불 지속 · 쓰러짐 | ✅ **산다** |
| **발사 반동의 가로**(`recoil_vx`) | ✅ **산다** — 「쏘면 밀린다」는 GDD 자연법칙이다.<br>⚠ 세기를 **400 → 200 → 100 → 40 px/s** 로 **세 번** 내렸다(같은 날, 전부 사용자가 화면을 보고).<br>0.5초 변위 실측 **92 → 46 → 23 → 9px** — 캐릭터 폭(32px)의 1/4, 이동 속도의 15%.<br>🔴 **여기가 「있으나 마나」의 문턱이다** — 더 내리려면 값이 아니라 축을 지워라(`character.gd`) |
| 🔴 **피격 밀림**(`KB_SPEED_PX`·`_push_dir`·`on_tick` 의 미는 갈래) | ❌ **삭제** |
| 🔴 **로켓점프**(`recoil()` 의 `vy -= d.y * RECOIL_SPEED_PX` 한 줄) | ❌ **삭제** |
| 🔴 **판정 4**(맞으면 밀리나) | ❌ **철회** — 통과했었고, 그 기능이 없어졌다 |

🔴 **로켓점프 삭제로 같이 죽은 것**: 「**위험과 기동이 같은 행동에서 나온다**」(아래 「마법 반동」 절).
발밑을 쏘는 것이 이제 **위험이기만 하다.** ⇒ **아래 빚 1번(「로켓점프가 보류다」)은 무효다** —
보류가 아니라 없어졌다. 빚 4번의 「로켓점프 109px」도 같이 무효다.

⚠ **그물은 이제 정반대를 단언한다** — `_firing_down_does_not_lift_me` 가 「안 뜬다」를 재고,
「가로는 살아 있다」를 대조군으로 같이 잰다(「안 뜬다」만 재면 반동이 통째로 죽어도 초록이라서다).
🔴 **되살리려면 그 검사부터 빨개진다.** 그게 맞는 거동이다.

⚠ **`3.done/` 에 그대로 둔다.** 폴더는 「구현이 끝났다」를 뜻하고 **실제로 끝났다** —
그 뒤에 별개의 결정으로 일부가 빠진 것이다. `2.active/` 로 되돌리면 **「지금 작업 중」이라는 거짓말**이 된다.
🔴 CLAUDE.md가 「`3.done/` 은 「구현이 끝났다」이지 「판정이 통과했다」가 아니다」라고 가른 그 자리에
**셋째 사실이 붙었다 — 「일부가 나중에 철회됐다」.** 그래서 이 배너가 제목 바로 아래에 있다.

🔴 **아래 본문에서 밀림 서술은 「그때 이랬다」로 읽어라.** 지운 자리마다 표시해 뒀고,
**검증 기록은 한 줄도 안 지웠다** — 관측은 그때 참이었고, 지우면 **왜 넣었다 뺐는지가 사라진다.**
⚠ **지금 코드를 알고 싶으면 `src/actor/character.gd` 를 봐라.** 그 파일이 스스로 삭제 사실을 적고 있다.

## 🔴 닫으며 — **판정 일곱 개가 전부 통과했다** (2026-08-04)

⚠ **`3.done/` 에 있다는 것은 「구현이 끝났다」이지 「전부 재졌다」가 아니다**(CLAUDE.md).
**이번엔 판정도 다 통과했고** 그 근거가 각 단계의 「결과」 절에 있다. 아래는 **넘기는 빚**이다.

| 단계 | 판정 | 결과 |
|---|---|---|
| 0 틱 순서를 액터로 | — (거동 변화 0) | 통과 · 되돌림 3회 · 뮤테이션 31회 |
| 1 무속성 룬 | **7** | 통과 |
| 2 체력·직격/폭발·무적·HUD | **1**, **3** | 통과 · 터널링 반증 **29자리 훑어 도약 13/13** |
| 3 불 위에 서 있기 | **2** | 통과 · **무적을 켠 채로** 재서 12프레임에 hp 2 |
| 4 밀림·반동 | **4**, **5** | 통과 · 「더해진다」를 `+20/+85/+150` 로 갈랐다<br>🔴 **판정 4는 그 뒤 철회됐다**(넉백 삭제). 판정 5(반동)는 산다 |
| 5 쓰러짐 | **6** | 통과 · hp가 0 밑으로 안 간다 |
| 6 조립창 축소 | — | 통과 · **조립 중에 불타는 캐릭터와 체력이 한 프레임에 보인다** |

**그물 1038 → 1288개.** 줄어든 그물 없음. 매 단계 `[래퍼] 통과. stderr 깨끗함.`

🔴 **남기는 빚 — 다음 사람이 먼저 읽을 것:**

1. 🔴🔴 **로켓점프가 보류다.** 땅에서 발밑을 쏘면 탄이 몸을 관통해 **직격 400이 반동 200을 이겨** 안 뜬다
   (공중에서는 96~109px로 뜬다). **사용자가 「폭발 바람에 날아가야 할 것 같다」로 방향을 남겼다.**
   ⚠ **그때까지 「동작 → 마법 반동」의 「아래로 쏘면 위로 뜬다」는 게임에서 참이 아니다**
2. 🔴 **그물이 게임과 다른 길을 지나는 우회가 셋이고, 그중 하나가 이미 결과를 가렸다** —
   `_beat_down` 의 「자기 폭발로 발밑을 판다」가 **같은 날 「쓰러지면 못 쏜다」로 닫혔는데 그물은 통과했다.**
   **넷째를 만들기 전에 「이 성질이 정말 경로와 무관한가」를 먼저 답해라**(그 자리에 박아 뒀다)
3. **낙하 피해도 격자 밖 처리도 없다** — 밀려 떨어지는 것은 **실물로 확인됐는데** 떨어져도 아무 일이 없다.
   「나중에 다시 열 것」이 「둘 중 하나는 채워야 한다」고 적은 자리가 **여전히 비어 있다**
4. **창이 안 가리는 것은 「닿을 수 없어서」다** — 무대에 창보다 위인 면이 셋 있고 **점프로는 못 닿는다.**
   🔴 **그런데 로켓점프(109px)와 밀림(59px)이 생겼다.** 무대나 기동이 바뀌면 여기가 먼저 깨진다
   ⚠ **이 빚은 두 번 무효가 됐다** — 넉백이 삭제됐고, 「창이 캐릭터를 안 가린다」 계약 자체가 폐기됐다
5. ~~**못 잰 것 둘**: 밀림이 벽에 부딪히면 · 쓰러진 채로 불 위에~~
   → 🔴 **앞의 것은 넉백과 함께 사라졌다.** 쓰러진 채로 불 위에는 **여전히 못 쟀다**
6. 🔴 **`expect_error` 사면의 수명이 무제한이다** — **다른 그물에서 · 시간을 거슬러서도** 듣는다(실측).
   **이 문서 밖의 하네스 문제**이고 `net_spell` 의 벌거벗은 `"모르는 룬"` 이 아직 그대로다

## 왜

🔴🔴 **팀킬이 사고가 아니라 재미다.**

사용자의 말 그대로다 — **「그 앞사람 비켜가 하고픈 거임. 그거 재미있거든」**.
이 한 줄이 이 문서 전체의 판정 기준이다. 아래의 모든 선택은 그 재미를 세우거나 지키기 위한 것이다.

⚠ **추천은 「환경 경유만」이었고 사용자가 뒤집었다.** 되돌리자는 게 아니라, 이 문서가
**의도적으로 위험한 쪽을 골랐다는 사실**을 다음 사람이 알아야 한다는 뜻이다.
「동료 탄에 맞아 죽었다」가 나중에 결함 보고로 올라오면 그건 결함이 아니라 **설계다.**

그리고 그 재미가 서려면 세 가지가 필요하다. 아래 「동작」이 전부 여기서 파생된다:

1. **맞아도 바로 안 죽는다** — 한 방 죽음이면 재미가 아니라 분노다 ⇒ 100 HP · 기준 10
2. **누구 탄에 맞았는지 보인다** — 몰라서 맞으면 그냥 억울하다 ⇒ 「화면」
3. **자리를 옮기면 풀린다** — 위치가 답이어야 「비켜」가 말이 된다 ⇒ 밀림

### 두 번째 이유 — GDD 자연법칙의 절반이 지금 확인이 안 된다

GDD가 적은 것: 「**물이 플레이어와 몬스터에게 닿는다.** 젖고, 젖으면 전기가 통한다」.

🔴 **지금 세상은 캐릭터를 완전히 무시한다.** 불 속을 걸어도 아무 일이 없다.
「먼저 적시고 번개를 치는 것과 번개를 먼저 치는 것은 결과의 종류가 다르다」는 이 게임의 테제인데,
**닿는 대상이 없어서 플레이어가 그걸 자기 몸으로 배울 방법이 없다.**

⚠ 몬스터를 기다릴 필요가 없다. **플레이어 자신이 첫 번째 대상**이고, 그게 제일 싸다.

## 동작

### 무엇이 나를 때리나

| 무엇 | 때리나 | 무적에 걸리나 |
|---|---|---|
| **탄 직격** (날아오는 중) | **때린다** | 타격 — 걸린다 |
| **착탄 폭발** | 때린다 | 타격 — 걸린다 |
| **번진 불 위에 서 있기** | 때린다 | 🔴 **안 걸린다** — 초당 깎인다 |
| 물웅덩이 + 번개 = 감전 | (물이 아직 없다) | 지속 쪽 |
| 무너진 지형에 깔림 | **미정** | — |

🔴🔴 **타격과 지속을 가르는 것이 이 절의 전부다.**
무적을 하나로 두면 **불 위에 서 있어도 안 아파진다** — 0.2초마다 한 번씩만 깎이는 게 아니라,
불에 닿는 것이 타격으로 취급되면 무적이 계속 갱신돼 사실상 무해해진다.
⚠ 그러면 GDD의 「연료를 어디 두느냐가 곧 레벨 디자인」이 **플레이어에게는 무의미해진다.**

### 🔴 탄은 나를 **통과한다** — 계약이 강제하는 것이지 선택이 아니다

`spell_sim._walk` 는 **고체 셀에서만** 탄을 죽인다. 아래 「기존 것과의 상호작용」이
「시뮬은 캐릭터를 모른다」를 계약으로 세웠으므로 ⇒ **탄은 반드시 나를 뚫고 지나가 뒤쪽에 착탄한다.**

⇒ 발밑을 쏘면 **탄이 나를 통과하고, 바닥에서 터진 폭발이 나를 때린다.**
⚠ 미정이 아니라 **귀결이다.** 「맞으면 탄이 사라진다」로 하려면 시뮬이 캐릭터를 알아야 하고,
그건 결정론이 캐릭터에 묶이는 것이다.

### 🔴🔴 직격은 「매 틱 사각형 겹침」으로 재면 조용히 안 걸린다

세대 0 탄은 틱당 **10셀 = 40px**을 뛰고 캐릭터 상자는 **16px**이다.
⇒ 틱 경계에서 위치만 맞대면 **40px 도약이 16px 상자를 통째로 넘어간다.**

⚠ **리포가 같은 함정을 이미 두 번 적어 뒀다** — `spell_sim.gd` 의 `_walk` 주석:

> 「도착 칸만 검사」로 짜지 마라. 첫 틱에 10셀을 뛰므로 도착 칸만 보면 1~2타일 벽을
> 그냥 통과한다. **한 프레임이라 눈으로 절대 못 본다**

🔴 **답도 이미 리포에 있다** — `spell_sim.get_prev_px()` · `get_prev_py()` 가 직전 위치를 준다.
⇒ **선분(직전 위치 → 지금 위치) 대 상자**로 재야 한다. 사각형 대 사각형이 아니다.

⚠ **그런데 그것으로도 못 재는 구간이 남는다.** 착탄한 탄은 **그 틱 안에서 지워지고**,
질의 API에 「이번 틱에 죽은 탄」이 없다. ⇒ **마지막 구간(착탄 직전)은 밖에서 원리적으로 관측 불가다.**
벽 가까이서 확산이 터지면 태어난 여덟의 첫 구간이 통째로 안 보인다.

🔴 **이건 답이 셋뿐이고 사용자 판단이 필요하다** — 아래 「사용자가 답해야 할 것」.

### 체력과 피해량

- 체력은 **100 HP**다. 숫자다
- **기준 피해 = 10** — 사용자의 말: 「일반진 문양 + 무속성 마법 10데미지로 시작하자」.
  **기본 진 · 기본 문양 자리 · 무속성 룬**의 조합이 기준선이다
- ⇒ 그 조합으로 **10대**다
- ⚠ 「가장 약한」이라고 적지 않는다 — **문양을 아예 안 넣은 쪽이 더 약할 수 있다**
  (`spell-circle-minimum` 극단값: 「층 2개를 다 비우고 쏘면 쏴져야 한다」). 이건 **기준선**이지 하한이 아니다

🔴 **아군 탄은 절반이다.** 동료가 쏜 것은 5. 이것이 「웃기는 팀킬」과 「화나는 팀킬」을 가르는 손잡이다.
⚠ **규칙이 둘이 된다는 대가를 알고 고른 것이다.** 나중에 「내 탄인데 왜 덜 아프지」가 헷갈리면
여기가 그 자리다.

### 🔴🔴 그런데 아군 절반은 **지금 코드로 구현이 불가능하다** — 탄에 「누가 쐈나」가 없다

`spell_sim.gd` 의 탄 평행 배열은 이렇다:

```
_px _py _vx _vy _prev_px _prev_py _glyphs _element _age _gen _id _behavior
                                                              ↑ 탄 일련번호지 소유자가 아니다
```

리포 전체에 `owner`·`shooter`·`caster` 류가 **하나도 없다.**

⚠ **actor 쪽에서 「내가 쏜 탄의 id」를 기억하는 우회로도 막힌다** — 확산으로 태어난 자식 탄은
**새 id를 받아서 추적이 끊긴다.** 그리고 자식 탄이야말로 동료를 때리는 주범이다.

⇒ **소유자 필드를 `src/sim/` 에 넣는 것 외에 길이 없다.**

🔴 **다만 이것은 desync 위험이 아니다.** 소유자는 **정수 하나**라 결정론에 안전하다.
「경계가 한 칸 넓어진다」이지 「두 세계가 섞인다」가 아니다. 둘을 구별해야 한다 —
캐릭터의 **위치**(float)가 시뮬에 들어가면 그건 desync지만, 발사자 번호는 그냥 정수다.

⚠ **그래도 이번 범위에서는 뺀다** — 아래 「범위 밖」을 봐라. 혼자라 **소비자가 0이고**,
소비자 없는 필드를 먼저 넣는 것이 CLAUDE.md의 거짓 손잡이다.
⇒ **이번 단계에서는 모든 탄이 같은 피해다.** 아군 절반은 멀티가 붙는 날 소유자와 함께 온다.

### 🔴🔴 수치를 어떻게 합치나 — **미정이다. 지금 안 정한다**

GDD의 「마법의 수치가 어디서 오나」가 이 문서에서 **열렸지만 안 닫혔다.**
사용자가 정한 것은 둘뿐이다:

- **룬 · 문양 · 장비 셋 다 관여할 수 있다**
- 기준선은 **무속성 = 10**

⚠ **이것을 억지로 채우지 않는다**(GDD 「뼈 먼저」·CLAUDE.md 「가짜 코드 금지」).
지금 축이 하나(확산 세대)뿐이라 **합칠 것이 아직 없다.** 룬이 둘이 되고 문양이 넷이 되는 날 정한다.

🔴 **다만 그날을 위해 함정 하나를 여기 박아 둔다.** GDD와 `docs/design/진-룬-문양.md` 가 이미 적었다:

```
확산(×0.5) → 폭발(×1.2)  =  ×0.6
폭발(×1.2) → 확산(×0.5)  =  ×0.6      ← 같다. 순서가 수치에서 죽었다
```

**곱셈은 교환법칙이 성립해서 순서를 무의미하게 만든다.** 이 게임의 테제가 거기서 조용히 사라진다.

### 세대 표가 이 문제를 **일부만** 덮는다 — 범위를 정확히 적는다

`sim_tuning.SIM_SIZES` 의 세대 표에서 확산은 계수를 곱하는 게 아니라 **세대를 올린다.**
그러면 순서에 따라 **곱해지는 대상 자체가 달라진다:**

| 조합 | 무슨 일 |
|---|---|
| **확산 → 폭발** | 확산이 세대를 1로 올림 ⇒ **약해진 8발**이 각자 세대 1 피해로 폭발 |
| **폭발 → 확산** | 세대 0에서 **온전한 폭발 1번** ⇒ 그 뒤 확산 |

⚠ 기제는 코드와 맞다 — `spell_sim._spread` 가 `gen + 1` 로 낳는다.

🔴🔴 **그런데 이게 덮는 것은 「탄을 낳는 문양을 사이에 낀 쌍」 하나뿐이다.** 반례 셋:

1. **`[폭발, 폭발]` 은 순서를 바꿔도 완전히 같다.** 폭발은 탄을 안 낳아 세대가 안 오른다.
   ⚠ 그리고 이 조합은 **지금 조립할 수 있다** — `glyph_defs` 의 폭발은 `max_per_circle: 0`(무제한)이다
2. **「바꿈」 문양끼리도 같다** — 가속·유도·회전은 전부 탄을 안 낳는다
   (`docs/design/진-룬-문양.md` 「문양 세 종류」). 계수를 붙이는 순간 교환법칙이 그대로 돌아온다
3. 🔴 **다섯째 열은 애초에 이 문제를 안 푼다.** 위에서 사용자가 정한 것은
   「**룬·문양·장비 셋 다 관여**」인데, `dmg(gen)` 은 **룬과 장비가 어떻게 접히는지에 대해
   아무 말도 안 한다.** 교환법칙 위험은 바로 거기서 나온다

⇒ **이 문서는 그 문제를 안 푼다. 「세대 축에서는 안 걸린다」까지만 참이다.**
🔴 `docs/design/진-룬-문양.md` 가 적은 「**수식을 도입하는 순간 이 성질을 어떻게 지킬지가 첫 질문이 된다**」가
여전히 살아 있다. 그 문서와 GDD의 미정이 이 문서 때문에 닫힌 것이 **아니다.**

### 피해량을 세대 표에 넣는다면 — 🔴 **그물을 새로 짜야 한다**

⚠ **여기에 이 리포의 함정이 하나 있다.** `sim_tuning.gd` 의 세대 표 주석은 이렇게 적혀 있다:

> 🔴 **모든 열이 세대마다 반드시 줄어야 한다**(`net_tables`가 잰다)

**그런데 `net_tables.gd` 는 명시 리스트로만 잰다:**

```gdscript
_strictly_decreasing(t, sim, "SIM_SIZES", ["speed", "rd", "ignite_r", "rune_r"])
```

🔴🔴 **라벨이 재는 것보다 넓다**(CLAUDE.md 「가짜 그물 금지」). 실측으로 반증했다 —
감소하지 않는 `"damage": 10` 을 두 줄에 넣고 전체 그물을 돌렸더니 **통과 1038개 · exit 0**이었고
`damage` 는 단언 한 줄에도 안 나왔다.

⇒ **피해량 열을 넣으면 그물에 이름을 손으로 더해야 한다.** 자동이 아니다.
⚠ 그리고 **`sim_tuning.gd` 의 그 주석도 같이 고쳐야 한다** — 이 문서가 만든 문제가 아니라
**리포에 이미 있던 거짓 라벨**이고, 다음 사람이 또 믿는다.

🔴 **그물에 이름을 더해도 이 축의 진짜 위험은 안 잡힌다.** 단조 검사는 `D₁ < D₀` 만 본다.
확산은 탄을 **8배**로 만드는데 그 곱은 어디에도 안 걸린다 — 다른 열처럼 `D₁ ≈ D₀/2` 로 잡으면
**확산→폭발 총 피해 = 8 × D₀/2 = 4 × D₀** 라 폭발→확산의 **4배**다.
「작은 것들이 약하다」가 총량에서 정반대가 되는데 **그물은 초록이다.**

⚠ 세대 1의 피해량 값은 **미정**이다.

### 무적 — 0.2초, 타격만

- 타격을 맞으면 **0.2초** 무적이다

🔴 **왜 0.2초인가**: 사용자가 「있는데 짧다」를 골랐다. 연달아 터지는 것은 막고
**연속 피격은 허용한다** — 즉 「가까이서 확산을 쓰면 아프다」가 규칙으로 남는다.

⚠ **지속(불)은 무적을 갱신하지 않는다.** 불 위에 선 채로 탄을 맞으면 둘 다 들어간다.
🔴 **이 갈래는 사용자가 정한 게 아니라 파생시킨 것이다.** 사용자가 고른 것은 「무적 0.2초」 하나다.
가른 이유는 위에 있다(무적을 하나로 두면 불이 무해해진다). **뒤집을 수 있는 자리로 표시해 둔다.**

### ⚠ 확산 8발이 몇 대인가 — **계산하면 1대다. 「2~3대」가 아니다**

상수 셋이 이미 답의 대부분을 정한다:

```
무적 0.2초 × 20Hz          = 4틱
확산 8발 ÷ MAX_BLASTS_PER_TICK(4) = 최소 2틱에 나눠 터진다
2틱 < 4틱                   ⇒ 전부 한 무적 창 안 ⇒ 1대
```

🔴 **그리고 이 문서 스스로 「1번이면 무적이 너무 긴 것」이라고 적었다** — 즉 지금 상수 조합은
**설계가 스스로 실패라고 부르는 값이다.**

⚠ **단정하지는 않는다.** 8방향으로 흩어진 탄이 지형에 따라 각자 다른 시각에 착탄하면
4틱을 넘길 수 있다. **지형이 답을 바꾼다.**
🔴 **그래서 판정을 바꿨다** — 아래 「판정」의 3-①②③을 봐라. 「2~3대」라는 목표 숫자를 버리고
**무적이 작동하는지·길이가 맞는지를 따로 재고, 확산 8발은 상황을 고정해 세어서 기록한다.**
목표 숫자를 두면 검증자가 **그 숫자가 나오는 지형을 고르게 된다.**

### ❌ 밀림 — 이게 「비켜」였다  🔴 **2026-08-04 삭제. 아래는 그때의 기록이다**

⚠ **지금 코드에는 없다.** 남긴 이유는 **「비켜」라는 재미의 재료가 무엇이었는지**가
다시 필요해질 수 있어서다 — 되살리려면 여기가 출발점이다.
🔴 **되살릴 때 반드시 같이 읽을 것**: 아래 검증이 「감쇠가 안 재진다」와
「폭발 밀림 방향이 안 재진다」를 **구멍으로 남긴 채 통과했다.**

맞으면 **밀린다.** 말로 비키는 게 아니라 몸이 밀리는 것이 상황을 만든다.

- 🔴 **낭떠러지 보호는 없다.** 발판 끝에서도 밀린다 ⇒ **밀려 떨어져 죽는 일이 난다**
- ⚠ **알고 고른 것이다.** 보호를 넣으면 「왜 갑자기 안 밀리지」로 읽혀 규칙이 탁하지 않다.
  그리고 이 게임은 **발판 자체가 부서지는 게임**이라 어차피 떨어진다

**밀림의 세기는 미정이다.** 다만 방향은 정해져 있다 — 탄이 온 쪽에서 반대로.

🔴🔴 **그 문장이 모호했다 — 2026-08-04에 검증자 둘이 서로 다르게 읽었다.**
verify-read는 「방향은 제한 없이 닫혀 있다 ⇒ 세로도 포함」으로, verify-run은 「세로를 안 열었다 ⇒ 미정」으로 읽었다.
🔴 **사용자가 정했다 — 「세로도 열어라」. 밀림은 가로·세로 둘 다다.**
근거: **반동은 이미 2D인데 밀림만 1D인 비대칭이 없어진다.** 그리고 점프와 반동이 있어 **공중 피격이 실제로 난다** —
그때 「위에서 떨어진 탄에 맞았는데 꿈쩍도 안 한다」가 나온다.

### 마법 반동 — 쏘면 밀린다

GDD 자연법칙에 이미 있다: 「마법의 반동으로 캐릭터가 밀릴 수 있다」.

🔴🔴 **이 게임에서 반동이 특별한 이유: 위험과 기동이 같은 행동에서 나온다.**
자기 폭발에 맞는 것이 확정이라, 가까이 쏘면 **밀리면서 동시에 아프다.**
로켓점프가 공짜가 아니다 — 그게 이 조합의 값어치다.

**반동의 세기도 미정이다.** 위력에 비례하나 고정인가가 아직 없다.

⚠ **다른 기동(대시 · 이단점프 · 벽타기)은 이 문서 밖이다.** 사용자가 「나중에 추가」로 정했다.

### 죽음 — 쓰러지고 동료가 살린다

- 100 → 0 이면 **즉사가 아니라 쓰러진다.** 행동불능 상태가 된다
- 동료가 다가와서 일으켜 준다

🔴 **이것이 「비켜」를 지키는 안전망이다.** 팀킬이 재미이려면 **복구할 길이 있어야 한다** —
사고가 런의 끝이면 그때부터 아무도 안 웃는다.

⚠ **혼자일 때는 어떻게 되나가 미정이다.** 일으켜 줄 사람이 없다.
⚠ **쓰러진 채로 얼마나 버티나도 미정이다.** 무한이면 긴장이 없고, 짧으면 구하러 갈 시간이 없다.

## 화면

무엇을 보고 「나 다쳤다」를 아는가.

- **체력이 HUD에 보인다** — 100/100. 숫자다
- **맞은 것과 무적이 눈에 보인다** — 🔴 무적을 보이게 만드는 것이 곧
  「지금은 안 맞는다」를 말하는 유일한 방법이다.
  ⚠ **깜빡임인지 별도 그림인지는 미정이다** — `character-sprite` 와 같이 정한다
- **밀리는 게 보인다** — 위치가 실제로 바뀌므로 저절로 보인다
- **불이 붙으면 캐릭터에 불이 보인다** — 지속 피해가 들어오는 동안
- **쓰러지면 쓰러진 그림이 된다** — 서 있는 것과 확실히 다른 실루엣

🔴🔴 **누구 탄에 맞았는지가 보여야 한다.** 「비켜」가 성립하려면 **누구에게 비키라고 할지**를 알아야 한다.
⚠ 방법은 **미정**이다. 후보: 피해 숫자를 쏜 사람 색으로 띄운다 / 맞은 순간 그 사람 쪽으로 표시가 튄다.

### 🔴🔴 조립창 — **이 문서가 리포에 예약돼 있던 사건이다**

`spell-circle-minimum` 이 조립창을 **각 축 90%(면적 81%)** 로 만들고 **HUD를 숨겼다.**
그런데 GDD는 「조립하는 동안 **세상은 안 멈춘다**」다.

🔴🔴 **`fx_tuning.gd` 가 그날을 이미 예약해 뒀다:**

> ⚠ **「캐릭터와 그 주변이 보인다」 계약이 유예됐다.** 지우지 않고 유예다 —
> 근거였던 GDD 「조립하는 동안 몬스터가 다가온다」에서 **지금 게임에 몬스터도 체력도 없어서**
> 못 보는 대가가 0이기 때문이다. 🔴 **캐릭터가 다칠 수 있게 되는 날 이 줄이 다시 걸린다.**

⇒ **이 문서가 정확히 그날이다.** 유예의 **유일한 근거(「체력이 없어서」)가 이 문서로 사라진다.**

⚠ **그리고 문제는 체력 숫자가 아니라 캐릭터 자체다.** `WINDOW_RECT` 가 캐릭터가 서는 자리를
**덮는다** — 조립 중에는 **내가 불에 타고 있는지 화면에 아무것도 안 보인다.**

| 창을 연 동안 | |
|---|---|
| 캐릭터 | 🔴 **가린다** |
| 총구 | 가린다 |
| HUD 체력 | 🔴 **숨는다** |

🔴 **이건 이 문서가 혼자 정할 자리가 아니다.** 90%는 **사용자 판정**이었고,
`spell-circle-minimum` 이 같은 자리를 「**계약과 사용자 지시가 충돌한다. 내가 판정할 자리가 아니다**」로
남겨 뒀다. ⇒ 아래 「사용자가 답해야 할 것」.

⚠ `net_render` 가 90%를 **단언하고 있어** 무엇을 하든 마주친다 — 조용히 넘어갈 수 없다.

## 경계

### 🔴🔴 이 단계가 원리적으로 못 재는 것 — **「비켜」 자체다**

**멀티가 아직 안 붙었다. 동료가 없다.**

⇒ 이 문서의 **재미 근거인 「앞사람 비켜」를 이 단계에서는 잴 수 없다.**

| 잴 수 있는 것 | 못 재는 것 |
|---|---|
| 내 마법에 내가 맞나 | **동료 탄에 맞나** |
| 밀리나 | 「비켜」라는 말이 실제로 나오나 |
| 불 위에서 깎이나 | **동료가 일으켜 주나** |
| 100 → 0 이면 쓰러지나 | 팀킬이 웃긴가 화나는가 |
| 무적이 작동하고 길이가 맞나 | |

🔴 **「아군 절반」은 못 재는 게 아니라 아예 범위 밖이다** — 위 「동작」이 그 이유를 적었다
(탄에 소유자가 없다). **못 재는 것과 안 만드는 것을 구별해야 한다.**

⚠ **이걸 「나중에 보면 되지」로 넘기면 안 된다.** 이 문서의 **왜**가 통째로 코옵에 걸려 있고,
혼자서 재는 것은 **그 재미의 재료가 작동하는지**까지다. **재미 자체는 멀티가 붙는 날 처음 재진다.**

🔴 그날 이 문서를 다시 연다. 그때 뒤집힐 수 있는 것: 아군 절반 · 무적 0.2초 · 밀림 세기.

### 몬스터가 없다

「누구에게 쏘나」가 없다. 지금은 **지형과 자기 자신**뿐이다.
⇒ 「위험 대비 보상」을 못 잰다 — 자해만 있고 이득이 없다.

### 물이 없다

「젖음」·「감전」은 이 문서에 **자리만 있다.** 물 시뮬이 오는 날 채운다.

### 극단값

- **체력이 0인데 또 맞으면?** → 쓰러진 상태다. **미정** — 쓰러진 채로 더 맞나
- **쓰러진 캐릭터가 밀리나?** → **미정**
- **불 위에서 쓰러지면?** → 계속 타나. **미정**
- **격자 밖으로 밀려나면?** → **미정.** 지금 캐릭터는 격자 밖 처리가 없다

## 기존 것과의 상호작용

### 🔴🔴 두 세계가 만난다 — 여기가 이 문서에서 제일 위험한 자리

탄은 `src/sim/`(정수 결정론)이고 캐릭터는 `src/actor/`(float 호스트 권위)다.
**지금까지 둘은 안 만났다.** GDD가 경고한 자리다:

> 결정론 쪽과 호스트 권위 쪽의 **경계는 좁게 유지한다.** … 두 세계가 넓게 만나면
> 그 자리가 desync 최대 위험지대다.

🔴 **GDD가 답도 이미 적어 뒀다** — 「"물에 젖었나" 같은 판정은 **호스트가 내려서 뿌린다**」.

```
   src/sim/            spell_sim이 탄 목록을 들고 있다  ← 🔴 캐릭터를 **모른다**
      │
      │ 읽기만 (기존 렌더가 이미 하는 것과 같은 방향)
      ▼
   src/actor/          여기가 「맞았나」를 판정한다      ← 새로 만드는 것
      │
      ├─▶ 체력 · 무적 · 밀림
      └─▶ HUD
```

⚠ **거꾸로 하면 안 되는 이유**: 시뮬이 캐릭터를 알게 되면 **결정론이 캐릭터에 묶인다.**
캐릭터는 호스트 권위라 클라마다 위치가 다를 수 있고, 그러면 **클라마다 격자가 갈라진다.**
에러는 안 나고 「멀티에서 지형이 서로 다르게 보인다」로만 드러난다.

🔴 **`src/sim/` 은 캐릭터라는 것을 모르는 채로 남는다.** 이게 계약이다.

### ⚠ 계약은 「캐릭터를 모른다」지 **「파일을 안 고친다」가 아니다**

**둘을 섞으면 다음 사람이 「sim은 못 건드린다」로 읽고 막힌다.** 실제로 고쳐야 할 곳:

| 파일 | 무엇 | 왜 |
|---|---|---|
| `sim_tuning.gd` | `ELEM_NONE` · `ELEM_ALL` · `TRACE_NONE` · `ELEM_DEFS` | 무속성 룬 |
| `sim_tuning.gd` | `SIM_SIZES` 다섯째 열 + 접근자 | 피해량 (넣기로 하면) |
| `spell_sim.gd` | `_rune_trace` 에 `TRACE_NONE` 갈래 | **아래 그물 항목이 강제한다** |
| `tests/nets/net_spell.gd` | `_every_rune_traces` 라벨 | 같음 |
| `tests/nets/net_tables.gd` | 열 이름 리스트 | 피해량 열을 넣으면 |

🔴 **이 중 어느 것도 「시뮬이 캐릭터를 안다」가 아니다.** 표를 늘리고 룬 갈래를 늘리는 것은
계약 **안쪽**이다. 계약이 막는 것은 오직 하나 — **캐릭터의 위치·상태가 시뮬에 들어가는 것.**

### 무속성 룬이 새로 생긴다

기준 피해 10의 담지자다. 그리고 **룬이 둘이 되는 첫 사건**이라,
`sim_tuning.gd` 가 「룬이 하나뿐이라 **룬이 진짜 축인지는 확인되지 않는다**」고 적어 둔 것이
여기서 처음 재진다.

지금 코드가 요구하는 것:

| 무엇 | 왜 |
|---|---|
| `ELEM_NONE` 을 **0이 아닌 값**으로 | 🔴 `ELEM_FIRE = 0` 이다. 0을 쓰면 무속성이 불이 된다 |
| `ELEM_ALL` 에 추가 | 순회는 명시 리스트로만 한다 |
| `TRACE_NONE` | `ELEM_DEFS` 가 모든 룬에 흔적을 요구한다 |
| `fx_tuning.ELEM_FX` 에 색 | 불과 눈으로 갈려야 한다 |

🔴 **무속성의 정의는 「흔적이 없다」다.** 불은 태우고 무속성은 세상에 아무것도 안 한다.
⚠ 거동(속도·궤적)은 **불과 같게 시작한다.** 그래야 갈리는 축이 「세상에 닿는가」 하나로 좁혀져
**룬이 진짜 축인지를 깨끗하게 잰다.**

### 🔴🔴 무속성을 `ELEM_ALL` 에 넣는 순간 **기존 그물 둘이 즉시 빨개진다**

표만 늘리고 끝나지 않는다. **둘 다 조용하지 않게 터진다** — 그게 다행이다.

**① `spell_sim._rune_trace` 가 짖는다**

```gdscript
if trace == Tuning.TRACE_IGNITE:
    grid.apply(...)
    return
push_error("SpellSim: 룬 흔적 %d에 구현이 없다" % trace)
```

`TRACE_NONE` 갈래를 안 만들면 **무속성 착탄마다 `push_error`** 이고,
래퍼가 stderr를 통째로 실패로 치므로 그물이 빨개진다.
⚠ 이 짖음은 **설계된 것이다** — 주석이 「표에만 있고 코드에 없는 룬을 래퍼의 stderr 검사에
공짜로 건다」고 적었다. 우리가 정확히 그 경우다.

**② `net_spell._every_rune_traces` 의 단언이 무속성의 정의와 정면으로 어긋난다**

```gdscript
for element: int in Tuning.ELEM_ALL:
    ...
    t.ok(g.burning_count() > 0, "룬 %d의 흔적이 세상에 남는다" % element)
```

**무속성은 정의상 `burning_count() == 0` 이다.**

🔴 **`continue` 로 무속성을 건너뛰면 안 된다** — 그게 CLAUDE.md가 금지한 **사면 넓히기**다.
⇒ 라벨을 「**룬마다 `ELEM_DEFS` 의 `trace` 가 말하는 결과가 실제로 난다**」로 바꾼다:
`TRACE_IGNITE` 면 `burning > 0`, `TRACE_NONE` 이면 **격자가 한 칸도 안 변한다.**

⚠ **그러면 이 그물이 곧 판정 7의 그물이 된다.** 검사를 새로 만드는 게 아니라 **라벨을 정확하게 만드는 것**이다.

⚠ **`spell-circle-minimum` 이 무속성을 「범위 밖」으로 미뤄 뒀다**(§3.5).
그 이유가 「흔적이 없는 룬은 새 개념이지 빈 값이 아니다」였다.
🔴 **판단이 뒤집힌 게 아니라 이제 그 차례가 온 것이다.**

### 룬 자리를 비우면 여전히 못 쏜다

⚠ 무속성 룬이 생겨도 **빈 룬 자리는 여전히 「못 쏜다」다.** 둘은 다른 것이다:

| | 룬 자리가 비었다 | 무속성 룬이 꽂혔다 |
|---|---|---|
| 쏴지나 | **안 쏴진다** | 쏴진다 |
| 총구 | 회색 빈 고리 | 무속성 색 구슬 |

🔴 이 구별이 무너지면 `spell-circle-minimum` §3.5 가 세운 총구·HUD 경고가 통째로 무의미해진다.

### 조립창은 안 고친다

체력·피해는 조립과 무관하다. ⚠ 다만 위 「화면」의 **HUD가 가려지는 충돌**은 남는다.

## 비용

**① 두 세계 경계 — 탄 목록을 읽는다**

탄 상한이 32발이고(`MAX_PROJECTILES`) 캐릭터가 2~4명이므로 최악이 **128쌍**이다.
검사는 **선분 대 상자**다(위 「동작」의 터널링 항목).

⚠ **얼마나 자주 도나가 아직 안 정해졌다.** 판정을 `src/actor/` 에 두는데 그 층은 **60Hz**이고
(`character.gd` 주석: 「60Hz로 돈다 — 시뮬 20Hz에 묶지 마라」) 탄 위치는 **20Hz로만 바뀐다.**
⇒ 60Hz로 돌면 세 번 중 두 번은 **같은 값을 다시 읽는다.**
🔴 **탄·폭발 판정은 틱에, 무적 감소·불 지속·밀림 감쇠는 프레임에** 두는 것이 자연스럽다 —
입구를 둘로 가른다.

⚠ **v1의 「투사체 32발 = 예산 0.4%」와 직접 비교하지 않는다.** 그 0.4%는 Bresenham 순회와
착탄 처리까지 포함한 값이라 쌍당 단가가 훨씬 무겁고, 여기는 항목이 **4배**다.
정직한 문장은 「**같은 자릿수이고 작을 것으로 보인다**」이고 **얹을 때 잰다** — 아래 ②가
스스로 세운 규율을 여기에도 건다.

**② 폭발 판정은 이미 있는 것을 탄다**

폭발은 이미 셀을 지우고 있다(`MAX_BLASTS_PER_TICK = 4`, **예산의 54%**).
캐릭터가 그 반경 안에 있는지는 **원 하나 대 사각형 하나** 검사라 공짜에 가깝다.

⚠ **폭발이 이미 예산의 절반이다.** 여기에 뭘 더 얹을 때는 반드시 재고 얹는다.

🔴🔴 **그런데 폭발 통지를 60Hz에서 읽으면 한 번이 세 대가 된다.** 통지 배열은
`step()` 시작에서만 지워지므로 틱 사이 세 프레임 동안 **같은 값이 남아 있다.**
⚠ **무적이 두 대를 먹어서 화면에서는 정상으로 보이고**, 무적과 무관한 불 지속에서만 3배가 드러난다.
⇒ ①의 「입구를 둘로 가른다」가 여기서 필수가 된다.

**③ 불 위에 서 있기 — 캐릭터가 밟은 셀만 본다**

🔴 **「시한부 상태가 구역을 안 재운다」**(GDD 「상태는 파면 목록으로 돈다」)**가 여기는 안 걸린다.**
젖음·불붙음이 **셀이 아니라 캐릭터 자신**에게 붙기 때문이다.
갱신 대상이 **사람 수(2~4)** 뿐이라 청크 재우기와 무관하다.

**④ 밀림 — 충돌 코드는 공짜지만 상태가 새로 필요하다**

`character.gd` 가 이미 `_move_x` · `_move_y` 로 1px씩 밀며 격자를 읽는다.
밀림은 **거기에 속도를 얹는 것**이라 새 충돌 코드가 없다.

⚠ **그런데 `character.gd` 에는 가로 속도가 아예 없다** — `vy` 만 있고 `vx` 가 없다
(`_rem_x` 는 소수 나머지일 뿐이다). ⇒ **수평 속도와 감쇠를 새로 만들어야 한다.**

⚠ **지금 `_move_x` 는 막히면 나머지를 버린다.** 밀림이 벽에 부딪히면 거기서 멈추는 게 맞는지
(아니면 튕기는지) 는 **미정**이다.

## 판정

🔴 **이 단계는 「비켜」를 못 잰다**(위 「경계」). 아래는 **그 재미의 재료가 작동하는지**까지다.

🔴🔴 **각 판정에 「뒤집어 보기」를 붙였다.** CLAUDE.md — 「안 뒤집어 본 검사는 「돈다」까지고
「잰다」가 아니다」. 뒤집기가 없으면 **무조건 깎는 코드도 초록이다.**

**1. 내 마법에 내가 맞나**
- **폭발**: 폭발 통지 + 반경으로 원 대 상자. 헤드리스로 확정
- **직격**: 벽에서 먼 빈 공간에 세우고 옆에서 수평으로 쏜다
- 🔴 **뒤집어 보기 ①**: 반경 **밖**에 세우면 100 그대로여야 한다
- 🔴 **뒤집어 보기 ②(필수)**: **터널링 반증** — 캐릭터 상자(16px)를 한 틱(40px)에 건너뛰는
  배치를 **일부러** 만들어 잰다. 점 검사로 짰으면 여기서 빨개져야 한다.
  ⚠ **이게 없으면 「선분으로 재나」가 아니라 「함수를 부르나」만 재는 것이다**

**2. 불 위에 서 있으면 깎이나 — 무적과 무관하게**
- 나무 바닥에 불을 붙이고 그 위에 세운다. 시간에 비례해 준다
- 🔴 **「무적과 무관하게」를 재는 유일한 방법**: 탄으로 먼저 맞혀 **무적을 켠 상태에서** 불 위에 세운다.
  ⚠ **무적을 안 켜고 재면 그냥 불 피해만 재는 것이고, 틀린 구현도 통과한다**
- **뒤집어 보기**: 연료가 다해 불이 꺼지면 깎임이 멈춘다

**3. 무적 — 셋으로 쪼갠다** (위 「동작」이 그 이유를 적었다)
- **3-①** 같은 틱에 폭발 둘을 맞으면 **한 번만** 깎인다.
  🔴🔴 **여기 적혀 있던 뒤집기가 틀렸었다 — 2026-08-04 실측으로 반증됐다.**
  「무적을 0으로 두면 두 번 깎인다」는 **거짓이다.** 무적을 강제 0으로 둬도 **여전히 10만 깎인다**
  (그 틱에 폭발은 실제로 둘 났다). ⇒ **같은 틱 안을 막는 것은 무적이 아니라
  `on_tick` 의 「첫 히트에서 끝내는」 구조**이고, **무적은 틱 사이를 막는다. 둘은 다른 장치다.**
  ⚠ 그대로 뒀으면 다음 사람이 그 뒤집기를 짜서 **「빨개져야 하는데 안 빨개진다」로 헤맸다.**
  ⇒ **대신 이것을 단언한다**: 「**폭발이 실제로 둘 났다**」 + 「**깎인 틱이 1회다**」
- **3-②** 무적 길이가 **4틱**인가 — 5틱 간격 두 대는 둘 다, 3틱 간격 두 대는 한 대
- **3-③** 확산 8발에 **몇 대인가** — 🔴 상황을 하나 고정하고 **세어서 문서에 적는다.**
  목표 숫자가 아니라 **기록**이다. 그 값을 보고 0.2초를 조일지 정한다

**4. 맞으면 밀리나** — ❌ **철회됨**(넉백 삭제, 2026-08-04). 아래는 그때의 판정 설계다
- x를 기록하고 옆에서 맞힌 뒤 반대쪽으로 이동했는지 본다
- 🔴 **불 룬으로는 못 잰다** — 착탄이 발판을 태워 떨어지면 「밀렸다」와 「떨어졌다」가 섞인다.
  ⇒ **무속성 + 문양 없음**으로 잰다. 격자가 한 셀도 안 변하는 것을 **같이 단언**하면
  오염이 0임을 그물이 스스로 증명한다
- 🔴 **뒤집어 보기(필수)**: 맞는 순간부터 **반대 방향 입력을 계속 넣고도** 변위가 남나.
  ⚠ 입력이 즉시 이기면 **「구현했는데 화면에서 안 보인다」가 초록이 된다**

**5. 쏘면 반동으로 밀리나**
- ⚠ **거는 자리가 함정이다** — 발사는 큐를 지나므로, 큐에 넣는 시점에 걸면
  **거부된 발사에도 밀린다**(「안 나갔는데 밀린다」). `fire()` 가 참을 준 뒤에 건다
- **뒤집어 보기**: 빈 룬으로 클릭하면 안 밀린다

**6. 100 → 0 이면 쓰러지나**
- 즉사가 아님은 **중력을 계속 받는 것**으로 잰다 — 공중에서 쓰러지면 떨어져 지표면에 붙는다
- 🔴 **행동불능을 같이 잰다**: 입력을 계속 넣어도 x가 안 변한다.
  ⚠ **안 재면 「플래그만 켜고 아무 일도 안 하는」 구현이 통과한다**
- **뒤집어 보기**: ~~체력 1~~ **체력 10**에서는 같은 입력에 x가 변한다
  🔴 **「체력 1」은 틀린 값이었다(2026-08-04 실측)** — **기준 피해가 10이라 1이 안 나온다.**
  열 대를 맞히면 100 → 0으로 바로 떨어지고, 아홉 대면 10이다. ⇒ **뒤집기의 값은 10이다.**

**7. 무속성 룬과 불 룬이 다르나**
- 같은 자리·같은 조합으로 각각 쏘고 격자를 비교. 불은 타고, 무속성은 **격자가 한 칸도 안 변한다**
- 🔴 **여기서 제일 잘 만들어지는 가짜 초록**: 무속성이 「아무것도 안 한다」라서
  **발사 자체가 안 된 것과 구별이 안 된다.**
  ⇒ **`fire()` 가 참이었고 탄이 착탄까지 살았다는 것을 같이 단언해야 한다.**
  안 하면 「무속성이면 발사가 거부된다」는 버그도 초록이다

⚠ **「아프다」와 「재미있다」는 이 단계의 판정이 아니다.** 재미는 멀티가 붙어야 재진다.

### ⚠ 판정을 짜기 전에 — 틱 순서가 지금 껍데기에만 있다

「시뮬을 돌린 직후 통지를 읽는다」는 순서가 `stage.gd` 안에만 있고, 그물은 껍데기를 씬으로 못 세운다.
🔴 **그물이 그 순서를 손으로 베끼면 그물이 게임과 다른 것을 재게 된다.**
⇒ 그 순서는 **`src/actor/` 의 함수 하나 안에** 있어야 하고 **껍데기와 그물이 둘 다 그것을 부른다.**

##### 결과 — 2026-08-04, **verify-look: 단계 6 통과. 유예됐던 계약이 실제로 풀렸다**

🔴🔴 **창을 연 채로 한 프레임에 이게 다 들어왔다** — **캐릭터가 불 위에 서서 타고 있는 것**
(주황 테두리 + 발밑 불꽃) · **`체력 91 / 100` 이 줄어드는 것** · 바닥 지형.
⇒ **문서가 이 단계를 연 이유(「조립 중에는 내가 불에 타고 있는지 화면에 아무것도 안 보인다」)가 해소됐다.**

```
창 (48,12) 864×372 ⇒ 아래 끝 384 | 캐릭터 496~512(창 아래 112px) | 점프 정점 445(61px) | 체력 라벨 494~530
```

**걸어 다녀도 계속 보인다** — 창을 연 채로 **x 48 → 768** 을 걸으며 **점프 여섯 번**, 최고 높이 `y 413`
(창 아래 끝보다 **29px 아래**). **한 번도 안 가려졌다.**

⚠🔴 **다만 「닿을 수 없어서 안 가려진다」이지 「가려질 수 없다」가 아니다.**
무대에 **창보다 위에 있는 면이 셋** 있다(위 발판 288 · 아래 발판 352 · 오른쪽 기둥 320).
**점프(51px)로는 못 닿아서 통과다.** 🔴 **그런데 로켓점프(109px)와 밀림(위 59px)이 생겼다** —
**발판을 부수고 튀어 오르면 닿는다.** ⇒ **다음에 무대나 기동이 바뀌면 여기가 먼저 깨진다.**

**조립창이 여전히 쓸 만하다** — 줄어든 창에서 클릭으로 놓고 뺐다(`[] → [1] → [] → [2]`).
팔레트 한 칸 `193.5 × 68.7px`(세로만 줄었다) · 문양 심볼 반지름 16.1px · 마법진 바깥 고리 지름 **224px** ·
층 번호·빈 슬롯 `+`·룬 색·문양 심볼 전부 또렷하다.
🔴 **「창 밖은 쏴진다」도 살아 있다** — 창을 연 채로 발밑(창 아래)을 클릭하니 **발사됐다.**

**쓰러진 채 발사 차단 확인** — 살아 있을 때 `발사 1→2` ✅ · **쓰러진 뒤 다섯 번 클릭에 `발사 11 → 11`**
(하나도 안 나갔다) · **R로 부활한 뒤 다시 `0→1`**(문이 영구히 안 닫힌다).
🔴 **「쏘는 시늉만 하고 커맨드가 버려진다」가 아니라 애초에 발사 수가 안 는다** —
막은 자리가 `_drain_queue` 인 것과 화면 거동이 일치한다.

⚠ **눈에 걸린 것 하나(실패는 아니다)**: **오른쪽 아래에서 캐릭터와 체력 라벨이 14px 차이로 가깝다.**
**발밑 불꽃·폭발 섬광이 그 아래로 번져 글자와 겹친다**(겹친 프레임을 봤고 글자는 아직 읽혔다).
🔴 **창을 줄여 생긴 띠가 「캐릭터가 보이는 유일한 곳」이자 「체력이 있는 곳」이라 둘이 같은 자리를 쓴다.**
⚠ 세대 0 섬광(반경 108px)이 그 근처에서 터지면 글자가 잠깐 묻힐 수 있다 — **안 봤다.**

##### 결과 — 2026-08-04, **verify-run: 회귀 통과 · 🔴 낭떠러지를 세 라운드 만에 닫았다**

**새 문이 설계 그대로다** — 쓰러지면 **발사 0 · 탄 0 · 반동도 없다 · 큐 0**(쌓이지 않고 **버려진다**),
부활 뒤에 **막혔던 커맨드가 되살아나지 않고** 새로 쏘면 된다.
🔴 **격자 커맨드는 안 막힌다** — 채우기·점화를 큐로 보내니 **살아있음/쓰러짐이 똑같다.** 문은 `spell_kind` 만 막는다.

🔴 **낭떠러지 — 실물로 확인됐다.** 좁은 발판(296~384px)에 세우고 옆에서 맞히니
`hp 90 (x=327) → 발판 끝 384 를 넘어 x 425..441 → y 308 → 468 로 떨어졌다`(공중 29프레임).
⇒ **기획의 「낭떠러지 보호는 없다 · 밀려 떨어지는 일이 난다」가 참이다.**
⚠ **다만 낙하 피해가 없어 「밀려 떨어져 **죽는다**」는 아직 아니다** —
문서 「나중에 다시 열 것」이 「둘 중 하나는 채워야 한다」고 적은 자리가 **여전히 비어 있다.**

⚠ **verify-run의 프로브 둘이 낡아서 재측정했다 — 코드가 아니라 배치 탓이다:**
- **3-②(무적 4틱)**: 🔴 **내가 쏘는 탄도 반동을 준다**(단계 4 이후) ⇒ 맞기도 전에 왼쪽으로 19px 흘러
  한 틱 이른 구간에 걸렸다. **수직으로 쏘게 바꾸니**(반동·밀림이 세로라 x가 안 흔들린다)
  `실제 간격 4 이하 한 대 · 5부터 두 대` — **무적 4틱 그대로다**
- **판정 2 「불+탄」**: 수직탄은 `불 9 + 탄 10 = 18` 로 더해지는데 **가로탄은 6**이다.
  🔴 **밀림이 나를 불 밖으로 밀어내서**(x −52px · 불 위 프레임 60 → 38) 합보다 작아진다.
  ⚠ **결함이 아니라 단계 4가 만든 창발적 상호작용이고 문서에 없던 사실이다.**

🔴🔴 **단계 6이 치른 대가 하나 — 「쓰러진 채로도 쏠 수 있다」가 닫히면서 배치 근거가 하나 죽었다**

verify-run이 지난 라운드에 「행동불능은 이동만이다 — 쓰러져도 발사는 된다」로 보고했고,
**구현자가 「쓰러진 채 중력을 받나」의 도달 근거로 「자기 폭발로 발밑을 판다」를 세웠다.**
⇒ **그 경로가 이제 닫혔다**(verify-run 프로브에서 그 자리가 `y 308 → 308` 로 빨개졌다).
**쓰러진 채로 떨어지는 상황을 게임에서 만드는 길은 이제 셋뿐이다** —
① 쓰러지기 **전에** 판 구멍 ② 불에 타 죽는 동안 발판이 타 없어짐 ③ **멀티에서 남이 쏨.**

🔴 **그런데 그물은 1288개 그대로 통과한다** — `_beat_down` 이 **`spell.fire()` 를 직접 불러 새 문을 안 지나기 때문**이다.
⚠ **verify-read가 경고한 「그물이 게임과 다른 길을 지난다」가 여기서 실제로 결과를 갈랐다.**
**그물은 통과하는데 게임에서는 그 배치가 성립하지 않는다.** ⇒ **판정 6의 도달 근거를 문서에 다시 적어야 한다.**

**회귀**: A/B 270프레임 다름인데 **전부 캐릭터만, 시뮬/통지 0프레임** · 진짜 씬은 달라진 줄이 **[9] 하나뿐** ·
그물 **1288개**, HEAD 1038 대비 **줄어든 그물 없음**(`net_damage 201` · `net_render 60→66`).

⚠ **아직 못 쟀다**: **밀림이 벽에 부딪히면**(계획 §4 가정 — **네 라운드째**) · **쓰러진 채로 불 위에**(기획 미정)

##### 결과 — 2026-08-04, **verify-read: 단계 6 통과 · 「남은 것 없다」** (뮤테이션 7회 + 탐침 2회, 워크트리)

**가드 자리가 `_drain_queue` 인 것이 맞고 유일하다.** 껍데기는 **로컬 마우스만** 지나고,
`spell_sim.fire()` 에는 **못 둔다**(「시뮬은 캐릭터를 모른다」가 이 기능의 핵심 계약이다).
⇒ **셋 중 `_drain_queue` 만 「모든 커맨드」와 「캐릭터」를 동시에 본다.**
⚠ `_air_fall`·`_beat_down` 이 안 깨진 것은 **우연도 설계도 아니고 「무관」이다** — 그 둘은 커맨드를 안 만든다.
🔴 **다만 그 무관함이 우회 덕이라, 우회를 걷어내는 날 이 가드와 처음 만난다.**

🔴 **`RECT_EPS` — 진단이 맞고 사면도 안 한다.** 탐침으로 재현했다:
`Rect2` 에 `0.1` 을 넣으면 **`0.10000000149011612`** 가 나오고, **새 창(372)**에서만 룬 구획이
기대보다 **−7.6e-6** 아래로 구른다(**옛 창(486)에서는 셋 다 차가 정확히 0**).
⇒ **「창을 줄인 것이 원인」까지 재현됐다.** 항목을 **1px 진짜로** 올리니 **다섯이 다 빨개진다** — 사면 안 한다.
쓰인 곳도 **한 군데뿐**이다. ⚠ **다만 실측 오차 7.6e-6에 EPS 0.01은 1300배다**(1e-4로도 통과한다).
**1px의 1/100이라 눈에 보이는 것은 못 숨기니 지금 위험은 아니지만, 주석에 그 여유가 안 적혀 있다.**

**새 그물 둘의 파생이 진짜다** — 숫자가 하나도 안 박혔고 **거동으로 증명했다**:
스폰 타일을 옮기니 자리가 따라가고, 점프를 −360 → −900으로 바꾸니 도달 높이 54 → 337px에 맞춰
사각형 위끝이 따라 올라갔다. **결합 판정도 「맞바꿈이 옳은 쪽」** — 숫자를 박으면
「스폰을 바꿨는데 이 검사만 조용히 낡는다」가 되고 **그건 이 리포가 여러 번 데인 형태**다.

🔴 **크기 가드의 전제가 거짓이다.** 주석은 「트리 밖 `Control` 의 size가 0이면 `intersects` 가 늘 거짓」이라는데
**측정은 반대다** — **창 한복판의 크기 0 사각형이 `intersects` 에서 `true` 를 준다**
(`Rect2::intersects` 는 변끼리만 걸러 퇴화 사각형이 안쪽이면 참이다).
게다가 **크기 0을 만들 수도 없다**(`Label` 이라 최소 `(1,23)`).
⇒ **가드 자체는 해롭지 않다**(다른 결함은 여전히 잡는다). 🔴 **틀린 것은 이유고, 다음 사람이
「Rect2 는 크기 0이면 안 겹친다」를 사실로 믿고 다른 데 쓴다. 주석 한 줄이다.**

**전체 점검 — 남은 것 없다**: `net_damage` 의 조기 `return` 넷이 **전부 `t.ok(false)` 나 앞선 빨간 단언 뒤**고
조건부 `continue` 가 없다(**「빨개지는 대신 없어지는」 자리가 깨끗하다**) ·
✅ **우회 셋에 값이 다 적혔고** 셋째에 「**넷째를 만들기 전에 멈추고 「이 성질이 정말 경로와 무관한가」를
먼저 답해라**」가 실제로 있다 · ✅ **`downed` 주석에 「언제 상태로 바꿔라」가 적혔다** ·
사면 그대로 · 색인/`get` 새 자리 없음.

⚠ **verify-read는 여섯 단계 통틀어 화면을 한 번도 안 봤다** — 잰 것은 **「사각형이 안 겹친다」까지고
「보인다」가 아니다.** 그 몫은 verify-look이 했다.

## 미정

🔴 **이 목록을 이 단계에서 다 채우지 않는다**(GDD 「뼈 먼저」). **미정으로 남는 것이 정상이다.**

**수치**
- **수치 합치는 식** — 룬·문양·장비가 어떻게 합쳐지나. 위 「동작」이 함정만 박아 뒀다
- **세대 1의 피해량** — 표의 다른 열과 같은 방향이어야 한다는 것만 정해졌다
- **불 위 지속 피해가 초당 얼마인가**
- **마법 반동의 세기** — 위력에 비례하나 고정인가

**~~밀림~~ — ❌ 이 미정은 통째로 사라졌다** (넉백 삭제, 2026-08-04)
🔴 **미정이 답을 얻어서가 아니라 물음이 없어져서다.** 되살리는 날 이 자리가 다시 빈다.

**죽음**
- **혼자일 때 쓰러지면 어떻게 되나**
- **쓰러진 채로 얼마나 버티나**
- **쓰러진 상태에서 더 맞나 · 계속 타나** (~~밀리나~~ — 넉백이 없다)
- **일으키는 데 얼마나 걸리나**
- 🔴 **일으켜지면 체력이 얼마인가** — 1이면 바로 또 쓰러지고, 100이면 쓰러짐이 벌이 아니다

**체력 회복 — 🔴 통째로 비어 있다**
- **깎이기만 하고 채우는 수단이 없다.** GDD에 포션이 있고 이건 로그라이크다
- ⚠ **「복구할 길이 있어야 팀킬이 웃긴다」가 이 문서의 안전망인데 복구의 절반이 비어 있다**

**화면**
- **누구 탄에 맞았는지 어떻게 보이나** — ⚠ 소유자 필드가 없어 **지금은 불가능하다**
- **맞음을 깜빡임으로 하나 별도 그림으로 하나** — `character-sprite` 와 같이 정한다

**그 외**
- **무너진 지형에 깔리면 아픈가**
- **격자 밖으로 밀려나면 어떻게 되나**
- **젖음** — 물 시뮬이 오는 날
- **장비** — 지팡이·로브·신발·가방. 수치 식이 정해질 때 같이 연다

## 🔴🔴 사용자가 답해야 할 것 — 이 넷은 혼자 못 정한다

1. **조립창을 어떻게 하나** — 90%는 사용자 판정이었고 그 유예가 이 문서로 만료된다.
   (a) 유예를 연장한다 (b) 창 안에 체력·캐릭터를 넣는다 (c) 창을 줄인다
2. **직격을 어떻게 재나** — 착탄 직전 구간이 밖에서 관측 불가다.
   (a) 폭발 통지만 쓰고 직격을 포기 (b) 시뮬에 「이번 틱 구간」 통지를 추가
   ⚠ (c) 액터가 궤적을 다시 계산하는 것은 **규칙이 두 벌이 되므로 금지다**
3. **아군 절반을 지금 넣나** — 넣으려면 시뮬의 탄에 소유자 필드가 필요하다.
   지금 문서는 **「이번엔 빼고 모든 탄이 같은 피해」**로 적어 뒀다
4. **밀림 감쇠와 입력 우선순위** — 가정으로 박고 갈 수도 있다(위 「미정」)

### 결과 — 2026-08-04, **사용자가 넷 다 답했다**

1. **조립창 — 🔴 창을 줄인다.** 유예 연장이 아니다. **조립 중에도 캐릭터와 HUD 체력이 보여야 한다.**
   ⚠ **얼마나 줄이나는 아직 안 정했다** — 기준은 크기가 아니라 「캐릭터와 그 주변이 보인다」다.
   🔴 `net_render` 가 90%를 **단언하고 있으므로 그 그물도 같이 고친다.** 안 고치면 빨개진다.
   🔴 **줄이는 방향도 사용자가 골랐다(2026-08-04) — 「세로만 줄여 위로 붙인다」.**
   가로 90%는 그대로 두고 세로만 줄인다 ⇒ `Rect2(48,27,864,486)` → `Rect2(48,12,864,372)`.
   ⚠ **대안이었던 「가로도 같이 줄여 좌우에 여백」은 사용자가 안 골랐다** — 되돌리자는 게 아니라,
   창 좌우로는 캐릭터가 여전히 안 보인다는 것을 **알고 고른 것**이라는 뜻이다.
   ⇒ 보이는 근거는 **창 아래 띠가 화면 폭 전체**라는 것 하나다. 세로를 다시 늘리면 그 근거가 죽는다.
2. **직격 — 시뮬에 「이번 틱 구간」 통지를 추가한다.** (b)다. 액터가 궤적을 다시 계산하는 (c)는 여전히 금지.
   ⇒ **판정 1의 뒤집어 보기 ②(터널링 반증)가 살아 있다.**
3. **아군 절반 — 지금 안 넣는다.** 문서 기본값 그대로 **모든 탄이 같은 피해**이고 소유자 필드를 안 만든다.
   ⚠ ⇒ **「누구 탄에 맞았는지 보인다」가 이 단계에 없다.** 위 「왜」가 센 「비켜」의 재료 셋 중
   **둘째가 빠진 채로 간다** — 그걸 알고 고른 것이다.
4. **밀림 — 입력과 더해지고 시간에 따라 감쇠한다.** 「막힌다」도 「서서히 되돌아온다」도 아니다.
   🔴 **입력이 즉시 이기지 않는다** ⇒ 판정 4의 뒤집어 보기(반대 입력을 넣고도 변위가 남나)가 성립한다.
   ⚠ **세기와 감쇠율은 여전히 미정이다** — 정해진 것은 **합치는 방식**뿐이다.

## 나중에 다시 열 것

이 문서가 **의도적으로 안 다룬 것**이다. 미정과 다르다 — 미정은 「답이 없다」이고 이건 「차례가 아니다」다.

- **대시 · 이단점프 · 벽타기** — 사용자가 「나중에 추가」로 정했다.
  ⚠ **점프는 이미 있다** — 사용자가 고른 것은 「점프 + 마법 반동」이었고 점프는 새로 정할 게 없었다
- **아군 절반 · 누구 탄에 맞았는지** — 소유자 필드가 필요하고 혼자라 소비자가 0이다
- **몬스터가 나를 때리는 것** — 몬스터가 없다
- **낙하 피해** — ⚠ **지금 아무 근거가 없다.** 낙하 상한 900px/s는 **벽을 뚫고 지나가지 않게 하는**
  안전선이지 낙하 피해와 무관하다.
  🔴 **그런데 위 「동작」이 「밀려 떨어져 죽는 일이 난다」를 확정으로 적었다** —
  낙하 피해도 격자 밖 처리도 없으면 **지금 명세대로는 밀려 떨어져도 안 죽는다.**
  ⇒ 낭떠러지 보호를 뺀 이유가 무의미해진다. **둘 중 하나는 채워야 한다**
- **캐릭터 그림** — `character-sprite` 가 맡는다

## 구현 계획

### 0. 실물 대조 — 계획을 세우기 전에 코드에서 확인한 것

🔴 **「기존 것과의 상호작용」의 표 한 줄이 이미 참이다. builder가 또 고치면 안 된다.**

| 문서가 적은 것 | 코드의 실제 |
|---|---|
| 「`sim_tuning.gd` 의 그 주석도 같이 고쳐야 한다 — 리포에 이미 있던 거짓 라벨」 | 🔴 **이미 고쳐져 있다** (`sim_tuning.gd:72~79` 가 「「모든 열」이 아니다 — `net_tables` 는 이름을 손으로 적은 넷만 잰다」로 적혀 있고 실측까지 붙어 있다). **할 일 없음** |

그 밖에 계획이 기대는 실물:

- `palette_layout.items_of(KIND_RUNE)` 가 **`Tuning.ELEM_ALL` 을 그대로 돌려준다**(`palette_layout.gd:71`)
  ⇒ 🔴 **무속성 룬은 조립창 팔레트에 저절로 뜬다.** 팔레트 코드를 안 건드린다
- `net_circle._both_ways_agree` 는 **문양만** 순회한다(`net_circle.gd:1037`) ⇒ 룬이 둘이 돼도 안 는다
- `net_circle:531` 이 룬 항목을 `Tuning.ELEM_ALL` 과 **값까지** 맞댄다 ⇒ 저절로 따라온다
- `character.gd` 의 점프 도달 높이 = `JUMP_VY_PX² / (2·GRAVITY_PX)` = 360²/2400 = **54px**
  ⇒ 아래 §7 의 조립창 크기 근거가 이 식에서 나온다(숫자를 박지 않는다)
- `book_layout.pages()` 는 창을 **늘 정확히 반으로** 가른다 ⇒ 창을 줄이면 마법진 지름이 그대로 줄고
  `net_circle.CIRCLE_DIAMETER_FLOOR_PX`(180) 가 **먼저** 빨개진다. §7 이 그 계산을 미리 해 뒀다

### 1. 구조 판단 — 변형인가 새 종류인가

**① 무속성 룬 = 기존 축의 항목 하나 더(변형).** 고칠 파일이 **셋**이다:

```
sim_tuning.gd    ELEM_NONE · TRACE_NONE · ELEM_DEFS 한 줄 · ELEM_ALL 한 줄
spell_sim.gd     _rune_trace 에 TRACE_NONE 갈래
fx_tuning.gd     ELEM_FX 한 줄
```

🔴 **`spell_sim.gd` 가 낀 것은 「룬이 늘어서」가 아니라 「흔적 *종류*가 처음 둘이 돼서」다.**
`TRACE_NONE` 갈래가 한 번 서면 그 뒤의 무속성 계열 룬은 **표 한 줄**이다.
⚠ 그물 둘(`net_spell` · 자동으로 `net_tables`)이 같이 움직이는 것은 **새 종류의 비용이 아니라
라벨을 정확하게 만드는 일**이다(문서 「기존 것과의 상호작용」이 그렇게 적었다).

**② 체력·피해·밀림·쓰러짐 = `character.gd` 안에 둔다. 새 파일을 안 만든다.**

🔴 **밀림이 `vx` 를 요구하고, 쓰러짐이 입력을 막고, 무적이 둘을 다 가른다** — 셋이 전부
이동 상태와 한 덩어리다. 별도 객체로 빼면 매 프레임 서로를 밀어 넣어야 하고,
그 밀어 넣기를 한 번 깜빡하는 순간 「체력은 0인데 걸어 다닌다」가 **에러 없이** 난다.

**③ 새 파일은 하나뿐 — `src/actor/world_step.gd`.**

문서 553줄이 요구한 것이다. 🔴 **이건 새 기능이 아니라 껍데기에 있던 순서를 옮기는 일이다.**

```
지금                                 뒤
stage._tick_sim()  ← 순서가 여기만   world_step.frame()  ← 순서가 여기 하나
                                     ├ 껍데기가 부른다
                                     └ 그물이 부른다      ← 이게 목적
```

⚠ **왜 `src/actor/` 인가**: `grid`·`spell`·`character` 셋 다 `RefCounted` 라 씬 트리가 없다.
🔴 `src/view/`·`src/stage/` 를 참조하면 `net_layers` 가 짖는다 — **뷰 통지(`spell_view.on_tick`·
`blast_fx.on_blasts`)는 여기 안 들어온다.** `frame()` 이 「이번 프레임에 틱이 돌았다」를 bool로
돌려주고, 껍데기가 그걸 보고 뷰를 친다.

### 2. 단계 — 단계마다 검증이 돈다

#### 단계 0 — 틱 순서를 액터로 뺀다 (🔴 **거동 변화 0**)

| 파일 | 무엇 |
|---|---|
| `src/actor/world_step.gd` | **새 파일.** `_phase` · 커맨드 큐 · `frame()` · `enqueue()` · `phase()` · `fire_count()` · `reset()` |
| `src/stage/stage.gd` | `_tick_sim()` · `_drain_queue()` · `_phase` · `_fire_count` · `_queue` 를 그리로 옮기고 `_world` 를 쓴다 |
| `tests/nets/net_damage.gd` | **새 그물.** 이 단계에서는 「껍데기가 순서를 안 든다」 한 검사만 |

```gdscript
# world_step.frame() 의 순서 — 🔴 이 순서가 계약이다
_phase += 1
if _phase >= Tuning.TICK_DIVIDER:
    _phase = 0
    _drain_queue(spell)          # 1. 외부 커맨드 (발사)
    grid.step()                  # 2. 격자가 한 틱 산다
    spell.step(grid)             # 3. 투사체는 **움직인 뒤의 격자**를 상대로 난다
    ch.on_tick(grid, spell)      # 4. 🔴 통지를 **여기서** 읽는다 — 다음 step()이 지운다
    ticked = true
ch.step(grid, dt, axis, jump)    # 5. 캐릭터는 60Hz
return ticked
```

🔴 **껍데기는 `frame()` 이 참을 준 프레임에만 뷰를 친다** — `spell_view.on_tick()` ·
`blast_fx.on_blasts(...)` · `_blast_count` · `_renderer.refresh()`.
⚠ 통지는 **다음 `spell.step()` 까지** 살아 있으므로 `ch.step()` 뒤에 읽어도 안전하다.

**판정**: 없다(새 기능이 없다). **뒤집어 보기가 곧 「아무것도 안 바뀌었나」다.**
- 기존 그물 전부 초록 · stderr 깨끗
- verify-run: 틱이 오르고 · 좌클릭이 발사되고 · 폭발이 나고 · 키 1~5가 전과 같다
- 🔴 **그물 하나를 새로 건다** (`net_damage._stage_does_not_own_the_order`):
  `stage.gd` 원문(주석·문자열 스트립 뒤)에 **`_grid.step(` 과 `_spell.step(` 이 안 나오고**
  `world_step` 이 나온다. ⚠ 안 걸면 다음 사람이 껍데기에 순서를 다시 베끼고
  **그물과 게임이 다른 것을 재게 된다.**

##### 결과 — 2026-08-04, **verify-look 통과** (화면)

⚠ **사용자가 본 것이 아니라 verify-look 에이전트가 에디터 브리지로 본 것이다.** 둘을 구별한다.

**거동 변화 0이 화면에서 확인됐다.** 마젠타·깜빡임·어긋남 없음, 에디터 로그 에러/경고 0건.

🔴 **제일 위험했던 자리가 관측으로 닫혔다** — 뷰 통지를 `ch.step()` **뒤**로 옮긴 것.
`_blast_count` 가 처음 1이 되는 **바로 그 프레임**에 `섬광 1` 이었고 **같은 프레임**의 HUD가 이미
`돌 5088→5076 · 나무 736→698 · 타는 셀 0→40` 이었다. ⇒ **섬광 · 지형 갱신 · 폭발 계수가 한 프레임에 떨어진다.**
한 틱 늦지도 빠지지도 않는다. 발사 직후 첫 틱에 `자취 1`(0이 아니다)이라 자취도 안 낡는다.
⚠ 위 「통지는 다음 `spell.step()` 까지 산다」가 **코드 읽기였는데 이제 관측이 붙었다.**

- 걷기·점프 `(48,496)`→`(149,448)` 착지 · 탄이 공중에 뜬 프레임(보간도 산다) · 확산→폭발로 분화구 여덟과
  `타는 셀 0→196→213` · Tab 조립창(90% 그대로가 정상) 여는 동안 **틱 78→88**로 세상이 안 멈춤 ·
  R 리셋 뒤 첫 화면과 눈으로 구별 안 됨
- 🔴 **`character.gd` 가 격자를 안 고치는 것**(`grid.apply|step|set_|fill` 매치 0)이 코드로도 확인됐다 ⇒
  `consume_changed()` 가 샐 수 없다

⚠ **못 본 것**: 밀림이 0이 아닌 순간(확산 8발이 틱 상한 4에 걸리는 틱을 못 잡았다 — 이 단계 밖) ·
화면 흔들림(게임 시간을 얼려 봐서 흔들림 자체를 눈으로 못 잰다) · 체력 HUD와 쓰러진 그림(아직 없는 게 정상)

🔴 **실측 — 다음 verify-look이 시간 안 버리게**: `toggle_assembly` 를 `godot_input` 의 `action_name` 으로
넣으면 **안 먹는다.** `stage_input._unhandled_input` 이 `event is InputEventKey` 로 거르는데 액션 주입은
`InputEventAction` 이라 그 갈래에 안 들어온다. ⇒ **raw key(`"tab"`)로 넣어야 한다.** 프리셋 키 1~5와 R도 같다.
⚠ 단계 0과 무관한 **껍데기의 원래 성질**이다.

##### 결과 — 2026-08-04, **verify-read: 코드는 통과 · 🔴 그물은 실패** (뮤테이션 7회)

**「거동 변화 0」은 잰 값으로 참이다.** 헤드리스로 `WorldStep` 을 직접 굴렸다 —
3프레임에 틱 1회 · phase 1→2→0 · 캐릭터는 매 프레임(60Hz) · `enqueue` 가 다음 틱에 탄을 낳고
큐의 격자 갈래도 산다(`burning 0→23`) · `reset()` 이 `_phase` 를 유지한다.

🔴🔴 **그런데 그 코드를 재는 그물이 없다. 뮤테이션이 셋을 뚫었다:**

| 뮤테이션 | 그물 | 뜻 |
|---|---|---|
| `frame()` 본문의 `_drain_queue()`·`_grid.step()`·`_spell.step()` **통째로 삭제** | **1056개 그대로** | **안 잰다** |
| 껍데기가 **지역 별칭**으로 순서를 다시 듦(`var g := _grid; g.step()`) | **1056개 그대로** | **안 잰다** |
| `_world` 가 **null 셋**을 들고 태어남 | **1056개 그대로** | **안 잰다** |
| 껍데기에 `_grid.step()` 되돌림 · `_world.frame(` → `.call("frame",` | 🔴 빨강 | 잰다 |

🔴 **원인은 하나다 — 리포 전체에서 `WorldStep` 을 만드는 곳이 `stage.gd:158` 하나뿐이다.**
`net_spell`·`net_character` 는 여전히 `sim.step(g)`·`ch.step(...)` 을 **손으로 부른다.**
⇒ 이 단계의 존재 이유인 「**껍데기와 그물이 둘 다 `frame()` 을 부른다**」의 **그물 쪽이 비어 있다.**
`frame()`·`enqueue()`·`_drain_queue()`·`reset()`·`phase()`·`fire_count()` **전부 자동 검사가 0이다.**

⚠ **라벨도 넓다.** `net_damage` 의 「껍데기가 틱 순서를 안 든다」가 실제로 재는 것은 **그 문자열의 부재**다.
지역 별칭 두 줄이면 게임에서 격자·투사체가 **틱마다 두 번** 도는데 그물은 초록이다.
🔴 파일 헤더가 「순서가 통째로 사라진 경우는 안 걸린다」까지만 적었고 —
**「순서가 두 벌이 된 경우」는 안 적혀 있는데 그게 이 그물이 막으려던 바로 그것이다.**

🔴🔴 **그리고 리포에 이미 있던 거짓 라벨 하나가 여기서 드러났다.**
`world_step.gd` 에 문법 오류를 넣었더니 **통과 1054개 · 실패 0개**였다(사라진 2개는 실패가 아니라 없어졌다).
**`net_layers.gd:66` 의 `t.ok(load(path) != null, "world_step.gd 가 읽힌다")` 가 초록이었다.**
⇒ `net_layers.gd:59` 의 주석 「`.gd` 는 파스 에러에 null을 준다」는 **거짓이고 CLAUDE.md 쪽이 맞다.**
⚠ **하필 단계 0이 거기 기댄다** — 지금 `world_step.gd` 를 보는 유일한 자동 검사가 그 줄이다.

**구현자가 짚은 다섯에 대한 답:**
1. 못 잡는다 — 맞고 **짚은 것보다 넓다**(위)
2. `reset()` 이 `_phase` 를 안 되돌리는 것 — **옳다**(옛 `reset_stage()` 와 같다). 다만 아무도 안 잰다
3. 선언 순서 — **조용히 안 깨진다. 매 프레임 짖고 `frame()` 이 늘 false라 화면이 통째로 멈춘다.**
   1초도 못 숨는다 ⇒ 긴급하지 않다. 🔴 다만 그물은 완전히 못 본다
4. `.uid` — 헤드리스로 그물을 한 번 돌리면 저절로 생긴다. 지금 생겨 있다.
   ⚠ **`src/view/palette_layout.gd.uid` 도 같이 생겼다 — 앞 세션에서 빠져 있던 것이다. 셋 다 커밋한다**
5. 뷰 통지 이동 — **안전하다. 이제 잰 값이다.** 400프레임 이동(걷기·점프·낙하·벽 충돌) 동안
   `consume_changed()` 누계 **0** · `frame()` 이 참을 준 직후 `blast_count=1` 로 통지가 살아 있다

⚠ **안 본 것**: 게임을 안 띄웠다(좌클릭·키 1~5·R·Tab은 verify-run/look 몫) ·
`_process`·`_update_hud` 가 실제 프레임에서 도는 모습 · 단계 1~5(구현 전)

**리포 상태**: 뮤테이션 7회 전부 `try/finally` 복원 확인, 최종 **1056개 통과 · exit 0 · stderr 깨끗**.

##### 결과 — 2026-08-04, **verify-run 통과** (헤드리스 · 거동 변화 0을 두 갈래로)

🔴🔴 **「거동 변화 0」이 이 단계에서 제일 강하게 증명됐다.** `godot_*` 안 씀, 사용자 입력 안 건드림.

**① 드라이버 대조** — `git show HEAD:src/stage/stage.gd` 의 옛 `_tick_sim()` 을 그대로 베낀 드라이버와
`world_step.frame()` 을 **한 프로세스에서 나란히** 돌리고 같은 대본(걷기·점프·낙하·발사 6회·리셋)을 먹였다.
**300프레임 전부 한 글자도 안 달랐다** — `ticked` · `spell_view` 가 읽는 값 전부 · `blast_fx` 통지 배열 넷 ·
`consume_changed()` · 격자 해시 · 캐릭터 `x y vy on_ground facing`.

🔴 **음성 대조 셋으로 그 비교가 차이를 문다는 것을 먼저 증명했다**(안 하면 「같다」가 공짜다):
`grid.step()`↔`spell.step()` 순서 바꿈 **190프레임 다름** · 통지를 한 틱 낡게 읽음 **111프레임 다름** ·
캐릭터를 안 밈 **300프레임 다름**.

**② 진짜 씬 대조** — 베끼기를 신뢰 사슬에서 빼려고 `git worktree` 로 HEAD(`bee9b50`)를 세우고
**같은 프로브를 양쪽 `stage.tscn` 에 돌렸다. 출력 133줄이 한 줄도 안 다르다.** 2회 반복 재현 동일,
diff 감지기도 자기시험했다.

**목록**: 30프레임→틱 +10(분주기 3) · 진짜 좌클릭이 큐 0→1(음성 대조: 조립창 위 클릭은 큐 그대로 ⇒ GUI 살아 있음) ·
키 4로 폭발 7회·나무 736→608 · 걷기 x48→112 · 점프 도달 51px · 낙하가 지표면 496에 정확히 · 기둥 앞 x=112에 막힘 ·
**부순 지형 위를 걷는다**(바닥을 파고 x 50→308) · **R을 진짜 키 이벤트로** 눌러 전부 0으로 · 정지 시 `consume_changed()` 0.

**통지 이동 자리**: 아무 일도 안 난다. `consume_changed()` 가 100틱 전부 같고,
`_blast_count` 가 오른 **바로 그 프레임**에 `blast_fx.active_count() > 0` 이다.

🔴 **통과 수가 줄었나도 확인했다**(사라진 검사가 없는지): HEAD **1038** → 지금 **1056**.
차이 18 = `net_damage` 13(새 그물) + `net_layers` +5(폴더 스캔이 자동으로 물었다). **어느 그물도 안 줄었다.**

⚠ **못 잰 것**: 렌더 비용(FPS — 헤드리스로 원리적으로 불가) · 화면에 무엇이 어떻게 보이나(숫자만 봤다) ·
**Tab과 키 1~5의 진짜 키 경로**(조립창은 `_toggle_assembly()` 직접 호출, 키 1~5는 시그널로 몰았다. **R만 진짜 키였다**) ·
확산 8발이 폭발 몇 회인가(3~7회로 나왔고 이 단계 판정이 아니라 안 팠다)

🔴 **다음 단계 프로브를 위한 실측**: **헤드리스 뷰포트는 960×960이고 캔버스 변환 원점이 `(0, 210)` 이다.**
화면 좌표를 그대로 밀면 **조준이 세로로 210px 어긋나** 착탄이 아예 안 난다(실제로 두 번 헛돌았다).
⇒ **월드 좌표를 캔버스 변환으로 되돌려서** 눌러야 한다.

⚠ **단계 2에 넘기는 것**: 계획 의사코드 4번 `ch.on_tick(grid, spell)` 이 구현에 없다(단계 2 것이니 맞다).
**위험 1(60Hz에서 통지를 읽으면 한 대가 세 대)이 그 자리를 요구하므로, 단계 2가 그것을 「틱 갈래 안」에 넣는지 확인할 것.**

##### 🔴🔴 단계 0 판정 — **구현 통과 · 그물 실패. 되돌린다**

**코드는 셋이 각자 다른 방법으로 통과시켰다**(화면 · 헤드리스 관측 · 직접 굴리기).
🔴 **그런데 그 코드를 재는 그물이 없다** — 위 verify-read의 뮤테이션 셋이 뚫렸다.
⇒ **단계 1로 넘어가기 전에 메운다.** 안 메우면 단계 2에서 `ch.on_tick()` 을 켤 때
**그 호출도 같은 사각지대로 들어가고, 판정 1·3이 통째로 이 파일을 지난다.**

##### 결과 — 2026-08-04, **그물 수리 1차: 셋 막힘 · 🔴 셋 더 뚫림** (verify-read 재검증, 뮤테이션 10회)

**코드는 안 바뀌었다**(`world_step.gd` md5 동일 · `stage.gd` diffstat 동일) ⇒ 「거동 변화 0」은 재검증 안 함.
그물이 14 → **42검사**, 전체 1056 → **1085개**.

**막힌 셋 — 그리고 표적을 맞혔다**(엉뚱한 검사가 우연히 빨개진 게 아니다):
`frame()` 본문 삭제 → **실패 7개**(격자 틱·**연료**·탄·발사 수 네 갈래가 동시에 물었다) ·
지역 별칭 재소유 → 실패 1 · `new(null,null,null)` → 실패 1. 캐릭터 60Hz·`reset()` 뮤테이션도 막혔다.

🔴🔴 **그런데 새 뮤테이션 셋이 더 뚫렸다:**

| 뮤테이션 | 그물 | 왜 아픈가 |
|---|---|---|
| **선언 순서만 뒤집기**(텍스트는 한 글자도 안 바뀜) | **1085개 초록** | 정규식이라 통과. ⚠ **`null,null,null` 이라고 손으로 쓰는 사람은 없다 — 선언 순서를 옮기는 사람은 있다.** `stage.gd:157` 의 「선언 순서가 계약이다」가 경고하는 **바로 그 사고가 정확히 안 잡힌다**(런타임은 매 프레임 짖고 화면이 통째로 멈춘다) |
| **투사체가 틱마다 두 번 돎** | **1085개 초록** | 🔴 **비대칭이다** — 같은 파일에서 격자 축은 「**양**」(연료 190 vs 195)으로 재는데 투사체 축은 `get_px()[0] > x0`, **「방향」으로만** 잰다. 탄이 두 배 속도로 나는데 초록 |
| 🔴🔴 **`grid.step()` ↔ `spell.step()` 맞바꾸기** | **1085개 초록** | **이 파일이 존재하는 이유가 그 순서를 한 곳에 두는 것**이다. `world_step.gd:47` 이 「아래 순서가 계약이다 — 바꾸면 멀티에서 desync다」라고 적어 놓고 **그 순서를 아무도 안 잰다.** ⇒ 옮긴 값어치의 절반이 아직 안 왔다 |

🔴 **그물이 원리적으로 못 잴 것 하나 — 문서에 남긴다.**
껍데기가 호출을 **갖고 있으면서** 단락 평가로 죽여도(`if _blast_count < 0 and _world.frame(...)`) **1085개 초록**이다.
게임은 완전히 얼어 있다. ⚠ `_physics_process` 는 **씬 트리라 헤드리스 그물 밖**이다.
⇒ **여기는 그물이 아니라 verify-run/verify-look이 메우는 자리**이고 화면에서 1초면 보인다.

**늘 보는 셋은 깨끗하다**: `net_damage` 에 `expect_error` **0개**(사면 넓힘 없음) · 조기 `return` **0개**
(전제 실패도 빨개진다) · 텍스트 라벨이 이제 전부 정확하다. **넓은 것은 생성 인자 검사 하나뿐이고 disclaimer가 붙어 있다.**

⚠ **안 잰 것**: `_drain_queue()` 위치를 미루는 뮤테이션(「안 걸릴 것」은 추정이지 측정이 아니다) · 게임 화면 ·
부수 발견 — `stage.gd` 는 **CRLF**인데 새 파일 둘은 **LF**다(헤드리스 생성. 기능 영향 없음).

##### 결과 — 2026-08-04, **verify-run 재측정: 거동 변화 0 유지** (`_broken` 가드가 들어간 뒤)

**세 판본이 전부 같다:** `옛 커밋(_tick_sim)` == `가드 전 world_step` == `가드 후 world_step`.
드라이버 대조 **300프레임 전부 같고 최종 상태가 가드 전 측정과 비트째로 같다**(`tick=67 mat=168531509 …`).
진짜 씬 대조 **133줄 한 줄도 안 다르다**(지난번 저장분과도 diff가 비어 있다).
음성 대조 셋도 다시 붙였다(순서 바꿈 190프레임 · 통지 낡게 111 · 캐릭터 안 밈 300).
🔴 트리는 측정 내내 안 흔들렸다 — `src`+`tests` 해시가 앞뒤로 같다.
그물별 대조에서 **줄어든 그물이 하나도 없다**(1038 → 1101, `net_layers` +5 · `net_damage` 58).

**`_broken` 의 대가 — 잰 값이다:**

| | 정상 경로 | 부서진 경로 |
|---|---|---|
| **가드 없음(전)** | — | `frame()` 참 0회 · **ERROR 300개**(프레임마다) · stderr 1500줄 |
| **가드 있음(지금)** | `_broken=false` · **stderr 0줄** · 300프레임→100틱 · 전부 정상 | `frame()` 참 0회 · **ERROR 1개** · stderr 5줄 |

🔴 **진짜 씬에서는 로그로 안 짖는다.** 씬을 세운 뒤 `_world` 를 부서진 것으로 갈아끼우니
**60프레임 뒤에도 tick이 +0**, HUD 첫 줄이 `틱 9` 에서 **얼어붙은 채로 계속 그려지고**, stderr는 그 1줄뿐.
⇒ ⚠ **verify-read가 「요란하게 죽으니 긴급하지 않다」의 근거로 삼았던 소음이 사라졌다.**
그 자리를 지금 지키는 것은 **HUD의 틱이 안 오르는 것**과 **`_broken` 이라는 값**(= 그물이 잡는다)이다.

🔴 **가드가 안 걸린 자리 하나 — `enqueue()`.** 부서진 세상에서 `phase()`·`fire_count()`·`reset()` 은
조용히 지나가는데 **`enqueue()` 는 짖는다**(`'get_tick' in base 'Nil'`) 그리고 큐는 0으로 남는다.
⇒ 정확히는 「한 번 짖고 조용하다」가 아니라 **「틱은 조용하고 발사는 클릭마다 짖는다」**다.

⚠ **못 쟀다**: 새 검사 58개가 진짜로 재는가(`src/` 변형이 필요해 verify-read 몫) ·
**진짜 선언 순서 사고의 재현**(`stage.gd` 를 고쳐야 해서 안 했다 — 잰 것은 씬을 세운 뒤 `_world` 만
갈아끼운 것이라 `_renderer.setup(_grid)` 같은 자리가 같이 죽는 실제 사고의 소음은 안 쟀다) · 렌더 비용 · 화면

##### 결과 — 2026-08-04, **그물 수리 2차: 여섯 막힘 · 하나 남음** (verify-read, 뮤테이션 11회)

**지난번 뚫린 셋이 다 막혔고 여섯이 표적을 맞혔다:**
선언 순서만 뒤집기 → `껍데기가 만든 세상이 분주기대로 틱을 돈다` 0 vs 1 ·
`grid`↔`spell` 맞바꾸기 → **관문 검사**가 물었다 · 투사체 두 번 → 변위 **4650 vs 2400** ·
`_drain_queue()` 위치 둘 · null 가드 삭제 → 단언 하나가 깨끗하게 빨개진다.
🔴 **관문이 자기가 관문인 줄 안다** — 관문 칸을 사거리 밖으로 옮기니 **대조군이 물었다.**

🔴 **남은 것 하나 — 구멍을 막으려고 만든 검사 안에 같은 구멍이 있다.**
`_broken` 을 `_brkn` 으로 **이름만** 바꾸면 `net_damage.gd:339` 의
`var broken: bool = w.get("_broken")` 가 **null 대입으로 터져 함수가 중단되고, 단언 둘이 빨개지는 게 아니라 없어진다**
(1101 → **1099 · 실패 0**). 러너 줄은 초록이고 래퍼만 잡는다.
⇒ `raw is bool` 을 먼저 단언하면 없어지는 대신 빨개진다. **한 줄이다.**

**`_broken` 맞바꿈 판정 — 좋다.** 정상 경로 거동 변화 0(코드로도 탐침으로도) ·
`frame()` × 60이 **완전 침묵**(전엔 60번 짖음) · 시작 시 **원인을 말하는 짖음 1회**
(옛 `'step' in base 'Nil'` 보다 진단이 낫다) · 🔴 **자동 신호는 오히려 강해졌다**(그물이 잡는다).
⚠ 다만 **`enqueue()` 가 가드 밖**이라 `frame()` 은 정중히 거절하는데 `enqueue()` 는 그냥 터진다 — 비대칭이다.

##### 🔴🔴 사면(`expect_error`)의 수명이 무제한이다 — **이 기능이 아니라 리포 전체의 성질**

**증명됐다.** `net_character`(**첫 번째로 도는 그물**)에 위조 짖음을 한 줄 넣고
선언은 `net_damage`(**세 번째**)가 하게 했다 ⇒ **짖음이 선언보다 먼저 났는데 1101개 초록 · stderr 깨끗함.**

🔴 **사면은 「그 실행 전체」가 아니라 시간을 거슬러서도, 다른 그물에서도 듣는다.**
`run_nets.ps1:54~57` 이 stdout을 **먼저 통째로 훑어 `[EXPECT]` 를 다 모은 뒤** stderr를 그 목록 **전부**와
맞대기 때문이다 — **그물별·시점별·횟수별 범위가 하나도 없다.**

지금 선언이 **23개**(`net_circle` 8 · `net_spell` 14 · `net_damage` 1)이고 **전부 실행 내내 살아 있다.**
⚠ CLAUDE.md는 사면의 **폭**만 경고했는데(「`expect_error("모르는 룬")` 이 둘을 덮는다」),
🔴 **`net_spell.gd:313` 이 아직 그 벌거벗은 `"모르는 룬"` 을 쓰고 있다.**
**폭 + 무제한 수명**이 겹치면 「아홉 그물 중 어디서 나든 · 언제 나든」 사면이다.

⇒ **이번 구현의 판단은 옳았다** — 짖음에 기대지 않고 `_broken` 을 **값으로** 잰 것이 정답이다.
⚠ **성질 자체는 하네스에 남는 문제다**(그물별 범위 · 횟수 상한). 이 문서 밖이다.
⚠ **다른 그물들이 실제로 서로를 사면하고 있는지는 안 쟀다** — **기제만** 증명됐다.

##### 결과 — 2026-08-04, **그물 수리 3차(최종): 닫혔다** (verify-read, 누적 뮤테이션 **31회**)

그물 62검사 · 전체 **1105개**. `stage.gd` md5가 **세 라운드 내내 동일**(안 건드렸다).

**「없어진다」 형태가 막혔다.** `_broken` 개명 뮤테이션이 지난 라운드엔 `통과 1099 · 실패 0 · 침묵사 7줄`
이었는데 지금은 **실패 2개 · 침묵사 0줄**이다. `Variant` 로 받아 **타입을 먼저 재는 것**이 그 구멍을 막았다.
색인·키 접근 7곳 중 **여섯이 막혔다**(⚠ `Mat.DEFS[Mat.WOOD]` 생 색인 하나가 남았는데 제일 약한 종류라 안 고친다).

🔴 **「단언하고 나서 건너뛴다」는 새 구멍이 아니다** — 구멍의 정의는 「사라지는데 **아무것도 안 빨개진다**」이고,
여기는 **둘이 빨개지고 첫 줄이 원인을 이름으로 말한다.** 건너뛴 셋은 **원리적으로 못 재는 것**이다
(`_broken` 을 못 읽는 객체에 「`frame()` 이 거절하나」를 물을 방법이 없다). `net_render` 가 이미 쓰는 모양이다.
⇒ **소리를 내며 사라지는 것과 조용히 사라지는 것은 다르다.**

🔴 **`enqueue()` 가드는 러너 단언이 원리적으로 못 잰다 — 확인됐고 라벨이 정직하다.**
가드가 있으면 즉시 반환해서 큐가 비고, 없으면 `_grid.get_tick()` 이 터지며 `append` 가 중단돼 **역시 큐가 빈다.**
⚠ **`enqueue()` 가 bool을 돌려주게 해도 안 된다 — 중단된 함수는 반환형의 기본값을 준다**(1회차에 `frame()` 이 그랬다).
남는 길은 계수기 하나인데 **게임 소비자가 0인 필드**라 이 리포가 「거짓 손잡이」로 금지한 그것이다.
⇒ 라벨을 **「부서진 세상은 커맨드를 안 받는다」**로 좁히고 「안 터진다」를 래퍼에 맡긴 것이 옳다.

**정상 경로 거동 변화 0** — `_broken` 이 거짓이면 `if` 가 거짓이라 다음 줄로 그대로 떨어진다.
🔴 **`frame()` 가드와 정확히 같은 모양이라 규칙이 한 벌이다** — 그게 이 2줄의 값어치다.

**⇒ verify-read: 「단계 0 코드에 대해 더 찾을 것이 없다.」**
누적 31회 뮤테이션 · 거동 변화 0을 세 번 확인 · 뚫으려던 자리가 전부 **표적을 맞혀** 빨개진다.

⚠ **다만 verify-read는 네 라운드 통틀어 게임을 한 번도 안 띄웠다.** 화면 판정은 첫 라운드 것이고
그 뒤 코드가 두 번 바뀌었다 ⇒ **verify-look 재확인이 있어야 단계 0이 닫힌다.**

##### 🔴🔴 결과 — 2026-08-04, **verify-look 재판정 통과 ⇒ 단계 0 닫혔다**

🔴 **그물이 원리적으로 못 메우는 자리를 화면이 메웠다.** 게임 시간을 **안 얼리고** 입력을 **하나도 안 넣은 채**로 쟀다:

| 상황 | 관측 |
|---|---|
| 실시간, 입력 0 (시작 직후) | **틱 112 → 212** |
| 실시간, 입력 0 (한 발 쏜 뒤) | **틱 284 → 500**, 캐릭터 위치 그대로 |
| 실시간, 입력 0 (발사 직후) | 폭발 0 → 8, **나무 736 → 352** — 불이 저 혼자 번져 다 태웠다 |
| **조립창을 연 채로**, 입력 0 | **틱 67 → 87**, 나무 574 → 496 |

⇒ **껍데기가 `frame()` 을 정말로 매 프레임 부른다.** 단락 평가 구멍이 화면으로 닫혔다.
`_world._broken == false` 도 직접 읽어 확인했다 — **가드는 정상 경로에서 안 켜진다.**

**지난번과 대조**: 시작 화면이 **글자 하나까지 같다** · 착탄 순간 구조 동일(`폭발 1 · 섬광 1`,
지형 수치가 같은 프레임에 이동) · 확산→폭발 `폭발 8` · R 리셋 완전 복원 · 마젠타·깜빡임·어긋남 없음 ·
**에디터 로그에 게임 에러 0건**. ⚠ 걷기+점프 x가 2px 다른데 **주입 타이밍 차이**다(물리 틱 수 72로 동일).

**새로 잰 것 — 프리셋 키 1~5**: `1` 진+룬만 · `2` 확산 · `3` 폭발 · `4` 확산→폭발 · `5` 폭발→확산 ·
**`9` 는 조용히 무시된다(명세대로)**. 조립도 실제 클릭 경로로 확인(`[1,2]` → 빼면 `[2]` → 다시 놓으면 `[1,2]`).

🔴 **다음 verify-look을 위한 실측 둘** (이번에 한 번 헛짚었다):
1. **`godot_game_time step` 을 `frames:` 로 짧게 주면 `physics_ticks: 0` 이 된다** ⇒ 클릭은 먹었는데
   `HUD/Stats` 글자가 그대로라 **「안 먹었다」로 오해한다.** `_update_hud()` 가 `_physics_process` 에만 있다.
   ⇒ **조립창 판정은 HUD 글자 말고 모델(`_circle.glyph_list()`)을 읽어라.** 또는 `duration_ms` 로 물리 틱을 확보해라
2. **층 슬롯 좌표는 눈대중하지 말고 `circle_layout.layer_slots()` 로 계산해라** — 히트 밴드가 ±10px 안쪽이다

⚠ **다른 세션의 godot-mcp가 브리지에 붙으려 했다** — 애드온 로그에 `Rejecting new connection … another
client is already connected`. 먼저 잡아서 영향은 없었지만 **CLAUDE.md가 적은 그 현상이 이번에도 실제로 났다.**

⚠ **못 본 것**: 체력 HUD·무적 깜빡임·쓰러진 그림(아직 없는 게 정상) · 밀림이 0이 아닌 순간 · 화면 흔들림
⚠ **부수 관측**: 바닥을 크게 부순 상태에서 캐릭터가 **`y=560`(격자 바닥 아래)까지 떨어졌다.**
⇒ 「미정」의 **「격자 밖으로 밀려나면 어떻게 되나」가 실제로 일어나는 상태**라는 뜻이다(R로 복구됨).

---

### ✅ 단계 0 판정 — **통과. 닫는다**

| 검증 | 결과 |
|---|---|
| **verify-run** | 거동 변화 0 — 드라이버 300프레임 · 진짜 씬 133줄, **가드 전후가 비트째로 같다**. 음성 대조 셋으로 비교가 차이를 문다는 것도 증명 |
| **verify-read** | 누적 **31회** 뮤테이션. 뚫으려던 자리가 전부 표적을 맞혀 빨개진다. 「더 찾을 것이 없다」 |
| **verify-look** | 화면이 지난번과 같고, **그물이 못 잴 자리(세상이 스스로 구르나)를 실시간 관측으로 메웠다** |

**그물 1038 → 1105개.** 줄어든 그물이 하나도 없다.
⚠ **커밋 대상에 `.uid` 셋이 있다** — `world_step.gd.uid` · `net_damage.gd.uid` · **`palette_layout.gd.uid`**(앞 세션 것).

#### 단계 1 — 무속성 룬 (판정 **7**)

| 파일 | 무엇 |
|---|---|
| `src/sim/sim_tuning.gd` | `ELEM_NONE := 1`(🔴 **0이 아니다** — `ELEM_FIRE = 0`) · `ELEM_ALL` 에 추가 · `TRACE_NONE := 1` · `ELEM_DEFS` 한 줄 |
| `src/sim/spell_sim.gd` | `_rune_trace` 에 `TRACE_NONE` → `return` 갈래 |
| `src/view/fx_tuning.gd` | `ELEM_FX[ELEM_NONE]` — 🔴 불(주황)과 **색상환에서 멀어야** 총구·룬 자리가 눈으로 갈린다 |
| `tests/nets/net_spell.gd` | `_every_rune_traces` 라벨 교정 |

🔴 **`_every_rune_traces` 를 이렇게 바꾼다**(문서가 지시한 것 그대로):

```
라벨: 「룬마다 ELEM_DEFS 의 trace 가 말하는 결과가 실제로 난다」

TRACE_IGNITE → burning_count() > 0
TRACE_NONE   → burning_count() == 0  **그리고** 격자가 한 칸도 안 변한다
그 밖        → 🔴 t.ok(false, "흔적 종류 %d에 이 그물이 단언을 안 갖고 있다")
```

⚠ **`continue` 로 건너뛰면 안 된다** — CLAUDE.md 「사면 넓히기」. `else` 가 `t.ok(false)` 인 것이
이 교정의 핵심이다: 흔적 종류가 셋이 되는 날 **그물이 먼저 짖는다.**

🔴 **판정 7의 가짜 초록을 같은 자리에서 막는다.** 「아무것도 안 한다」는 「발사가 안 됐다」와
구별이 안 되므로 **넷을 같이 단언**한다:

1. `fire()` 가 참이었다
2. 발사 직후 `active_count() == 1` — 탄이 실제로 났다
3. 착탄해서 원본 id가 사라졌다
4. **그런데 격자는 한 칸도 안 변했다**

**뒤집어 보기**: 같은 자리·같은 조합을 **불 룬**으로 쏘면 격자가 변한다(대조군).

##### 결과 — 2026-08-04, **verify-read: 그물 통과** (뮤테이션 7회). 그물 1105 → **1138개**

**새 검사 넷이 전부 표적을 맞혔고, 없어지는 게 아니라 빨개진다** — 색 삭제 · `ELEM_ALL` 밖 색 추가 ·
색을 비명 마젠타로 · 모르는 흔적 종류(`else` 의 `t.ok(false)`가 물었다).
🔴 새 코드가 **색인으로 안 판다** — `.get(..., null)` 을 `Variant` 로 받고 타입을 먼저 잰다.
**세 라운드에 걸쳐 세 번 지적된 형태가 이번엔 처음부터 막혀 있다.**

🔴🔴 **판정 7의 단언이 값어치를 실측으로 증명했다.** `fire()` 가 무속성을 거부하게 만들면
`SpellSim: 모르는 룬 1` 을 짖는데 **그 짖음이 `net_spell:313`·`net_circle:200` 의 사면에 걸려 래퍼가 깨끗하다.**
그런데도 단언 셋이 빨개졌다(`fire()가 참을 준다` · `탄이 실제로 났다` · `탄이 없어서 흔적을 하나도 못 쟀다`).
⇒ **그 단언들이 없었으면 이 버그는 완전히 안 보였다.** 문서가 「`fire()` 가 참이었고 탄이 착탄까지
살았다는 것을 같이 단언해야 한다」고 못 박은 자리가 **정확히 그것 때문에 살았다.**

**대조군은 진짜다** — 같은 루프의 불 룬이 `burning_count` · `consume_changed` **같은 두 값을 반대 방향으로**
잰다(11칸 vs 0칸). 따로 만든 대조군보다 낫다. ⚠ 다만 점화 쪽은 **양이 아니라 방향**이라(`> 0`)
불이 1칸만 태워도 통과한다 — **이번에 생긴 문제가 아니고** 단계 1의 축과 무관하다.

**마젠타 예약 걱정은 과했다** — 금지되는 것은 `glow` 가 **정확히 `Color(1,0,1)`** 인 경우 하나고 `core` 는 안 걸린다.
그리고 `fx_tuning:327` 이 **이미** 「일부러 마젠타다 — 룬을 늘렸는데 여기를 안 늘리면 화면이 비명을 지른다」로
그 규칙을 세워 뒀다. 이 검사는 **그걸 자동으로 만든 것**이라 되돌릴 자리가 아니다.

🔴 **남은 것 둘:**

1. **`_rune_trace` 의 `TRACE_NONE` 갈래를 지워도 단언이 전부 통과한다**(래퍼만 잡는다).
   ⚠ **이 단계의 시뮬 변경 전부가 그 두 줄**인데 단언 밖이다. 「아무것도 안 한다」와 「짖고 아무것도 안 한다」가
   격자 상태로 구별이 안 된다 — **설계된 것이고 라벨도 정직하다.**
   🔴 **다만 지금 안 사면되는 이유가 「말이 우연히 다른 덕」이다.** 누가 `expect_error("룬 흔적")` 을
   선언하는 날 이 갈래는 **아무도 안 보는 코드**가 된다
2. 🔴 **`sim_tuning.gd` 의 주석이 틀렸다** — `ELEM_NONE := 0` 이면 「`ELEM_DEFS` 의 한 줄이 다른 줄을
   **조용히** 덮어쓴다」고 적혀 있는데 **「조용히」가 거짓이다.** GDScript는 파스 시점에 짖는다
   (`Parse Error: Key "0" was already used in this dictionary`). **결론(0이면 안 된다)은 맞고 이유가 틀렸다.**
   ⚠ 다음 사람이 「딕셔너리 키 충돌은 이 언어에서 조용한 실패다」로 믿으면 **없는 위험에 그물을 짜게 된다**
   ⚠ 그 김에: 이 뮤테이션에서 통과 수가 **1138 → 723으로 주는데 실패는 2개**였다 —
   「src가 안 컴파일되면 검사가 없어진다」의 **세 번째 사례**다

⚠ **안 본 것**: 게임을 안 띄웠다(보라가 불·빈 룬 회색과 갈리는지는 화면 몫) · 판정 7을 눈으로 본 게 아니다

##### 결과 — 2026-08-04, **verify-look: 핵심 물음 통과 · 약한 곳 둘**

🔴🔴 **무속성 보라 ↔ 룬 자리 빈 회색 — 갈린다.** 총구를 9배로 확대해 같은 구도로 두 상태를 찍었다:

| | 보인 것 |
|---|---|
| **무속성** | **속이 찬 보라 구슬** — 연보라 심 + 보라 무리. 지팡이 끝이 안 비친다 |
| **룬 자리 빔** | **속 빈 회색 얇은 고리** — 가운데로 **갈색 지팡이가 그대로 비친다** |

**색(보라 vs 무채색)과 모양(찬 것 vs 빈 고리) 둘 다 다르다.**
⚠ 실제 크기에서는 둘 다 아주 작다(`muzzle_radius(문양 없음) = 2.5px`) — 무속성은 **보라 점**,
빈 룬은 **거의 안 보이는 흐릿한 회색 테**다. 🔴 **「못 쏜다」 쪽이 더 안 보이는 것이 오히려 뜻과 맞다**(꺼진 것).
⇒ **문서 3.5절의 「「못 쏜다」가 안 보이면 그게 곧 고장이다」를 통과했다.**

**무속성으로 쏘면 지형이 안 탄다 — 한 화면에서 보인다.** 같은 조합(진+룬만)으로 나무 덩어리 둘을 각각 쐈더니
한 프레임에 **무속성이 때린 쪽은 갈색 그대로**(불도 구멍도 그을음도 없다), **불이 때린 쪽은 활활 탄다.**
🔴 그때 `발사 1 · 비행중 0` 이라 **발사가 거부된 게 아니라 탄이 착탄까지 갔다** —
판정 7이 경계한 가짜 초록을 **화면 쪽에서도 배제했다.**

**팔레트도 문제없다** — 룬 구획이 반으로 갈려 불(주황)·무속성(보라)이 나란히 앉고 겹침이 없다.
조립창 클릭으로 실제로 놓았다(`rune 0 → 1`, `can_fire = true`). **마젠타는 어디에도 안 떴다.**

🔴 **약한 곳 둘 — 판정 7의 기준은 아니지만 적어 둔다:**

1. **무속성 탄이 화면에서 거의 흰 점이다.** `spell_view._draw_head`·`_draw_trail` 이 **`core` 를 쓰는데**
   무속성 `core = (0.85, 0.80, 1.0)` 이 **거의 흰색**이다 ⇒ 자취도 희다.
   확대하면 갈리지만(무속성=흰 심+연분홍 무리 vs 불=흰노랑 심+선명한 노랑주황) **원본 크기 세대 1 탄에서는
   「따뜻한 점 vs 흰 점」 정도로 좁아진다.** ⇒ 룬을 가르는 축이 **총구·조립창에서는 세고 날아가는 탄에서는 약하다**
   🔴 **사용자 판단(2026-08-04): 「나중에 본다」 — 지금 안 고친다.**
   판정 7의 기준이 아니고(판정은 격자로 잰다) 총구·조립창에서는 이미 갈린다.
   ⇒ **`character-sprite` 에서 연출을 몰아 볼 때 같이 정한다.** `fx_tuning` 상수 한 줄이다
2. **총구는 문양이 있으면 룬 색이 아니다** — `muzzle_tint` 가 **맨 안쪽 문양 색**을 쓴다 ⇒
   **무속성 + 확산이면 총구가 청록이고 보라가 아니다.** 설계된 동작이지만
   **문양을 끼는 순간 「무속성인지」를 총구로는 못 안다**는 뜻이다

⚠ **못 본 것**: 확산 8발이 동시에 뜬 순간(최대 `비행중 5` 까지만) · 조립창 안의 빈 룬 자리 회색 고리
(보라↔회색 대조는 **총구 쪽으로만** 봤다) · 무속성 + 폭발의 섬광 색. 에디터 로그 에러·경고 **0건**.

🔴 **⚠ 에디터를 강제 종료했다** — `CloseMainWindow` 가 두 번 안 먹어 `Stop-Process -Force` 로 껐다.
**clean close가 아니라 다음 세션에서 애드온이 죽은 클라이언트를 붙잡을 수 있다.**
증상은 「OS 소켓에 established가 하나도 없는데 `Another client` 가 계속 뜬다」이고 **에디터 재시작이 답이다.**

##### 결과 — 2026-08-04, **verify-run: 판정 7 통과** (헤드리스, 가짜 초록 막이 넷을 다 세워서)

같은 자리(원점 `(180,340)` → 표적 `(200,500)`, 나무 구역)에서 **룬만 바꿔** 쐈다:

| 기획이 요구한 것 | 관측값 |
|---|---|
| ① `fire()` 가 참이었다 | 발사수 **1** |
| ② 탄이 실제로 났다 | 탄 1개 · 실린 원소 **1(ELEM_NONE)** |
| ③ 착탄까지 살아 원본 id가 사라졌다 | **4틱째** · 마지막 자리 **셀(48,113)** — 격자 안쪽 나무 칸 |
| ④ 그런데 격자는 한 칸도 안 변했다 | `get_mat()`·`get_flag()` **바이트 동일** · 변경통지 합 **0** · 타는 셀 최대 **0** |

**뒤집어 보기(같은 자리·같은 조합의 불)**: 타는 셀 최대 **128** · 나무 **-128** · 변경통지 **256**.
🔴 **거동은 같다** — 궤적이 틱마다 동일하고(`셀(46,95)→(47,104)→(48,113)`) **같은 4틱째 같은 자리**에서 착탄한다.
⇒ **「갈리는 축이 「세상에 닿는가」 하나뿐」이 수치로 성립한다.**

⚠ **④는 처음에 거짓 초록이었다** — `get_mat()` 을 그대로 스냅샷으로 들면 **격자와 같은 버퍼를 가리켜**
「안 변했다」가 늘 참이 된다(나무가 128 줄었는데 「mat 같다」가 나왔다). **`.duplicate()` 로 고친 뒤의 값이 위 표다.**

🔴🔴 **새로 드러난 것 — 폭발 문양이 끼면 끝 상태가 불과 같아진다. 판정 4에 직결된다**

| 조합 | 타는 셀 | 나무 | 돌 | 변경통지 | **끝 상태 mat 해시** |
|---|---|---|---|---|---|
| 불 + 폭발 | 31 | -128 | -57 | 512 | 190834079 |
| 무속성 + 폭발 | 31 | -128 | -57 | 503 | **190834079 (같다)** |

**폭발이 자기 `ignite_r` 을 들고 있어서 룬과 무관하다.** 룬이 바꾸는 것은 **도중의 변경 수(512 ≠ 503)뿐**이고
**최종 지형은 구별이 안 된다.**
⚠ **결함이 아니라 경계다** — 「무속성은 세상에 아무것도 안 한다」는 **룬 흔적에 한해** 참이다.
🔴 **⇒ 판정 4가 「무속성 + 문양 없음」으로 재라고 적은 단서가 「없으면 안 된다」는 게 수치로 확인됐다.**
문양을 **하나라도** 끼우면 격자 오염이 0이 아니다.

**팔레트·조립을 진짜 클릭 경로로 끝까지 확인했다** — 창을 열고 `(789.25, 293)` 클릭 → `picked=(KIND_RUNE, ELEM_NONE)`,
진 페이지 클릭 → `rune_at(0)` 이 무속성, 창을 닫고 좌클릭 → **발사 0→1 · 타는 셀 0 · 나무 736 그대로**.
뒤집기로 룬만 불로 바꾸니 **타는 셀 85**. 색은 무속성 glow **258°** · 불 **25°**, 사이 **126°**.
🔴 **룬 자리를 비우면 여전히 못 쏜다.**

⚠ **「클릭으로 못 고른다」가 한 번 나왔는데 검증 하네스 탓이었다** — 창 전체를 훑는 방식이
**진 자리를 빈 손으로 눌러 진을 지웠고**(`_place_or_clear` 는 든 게 없으면 빈 값을 놓는다) 그러면 룬 칸이 사라진다.
**코드 문제가 아니다.**

**기존 것이 안 깨졌다** — 옛 커밋(`bee9b50`, 무속성 이전) vs 지금 진짜 씬 **133줄 한 줄도 안 다름** ·
A/B 300프레임 동일(최종 상태가 지난 두 측정과 **비트째로 같다**) · 음성 대조 111프레임.
⇒ **`ELEM_ALL` 이 길어졌어도 불 룬의 거동은 그대로다.**
그물별 대조에서 **줄어든 그물이 하나도 없다**(1038 → 1138).

⚠ **첫 실행은 버렸다** — 도중에 `sim_tuning.gd` 해시가 바뀌었다(다른 검증자의 뮤테이션).
**검증자 동시 실행의 위험이 실제로 났다.** 위 결과는 전부 앞뒤 해시가 같은 실행의 것이다.

⚠ **못 쟀다**: 화면에 실제로 그려지나(verify-look 몫) · 새 검사가 진짜 재는가(verify-read 몫) ·
**세대 1(확산 자식) 무속성 탄의 흔적**(이번엔 세대 0만)

---

### ✅ 단계 1 판정 — **통과**

| 검증 | 결과 |
|---|---|
| **verify-run** | 판정 7 통과. 가짜 초록 막이 넷(`fire()` 참 · 탄이 났다 · 착탄까지 살았다 · 격자 불변)을 다 세워서 쟀다 |
| **verify-read** | 새 검사 넷 전부 표적 명중. **판정 7의 단언이 사면을 뚫고 버그를 잡는 것을 실측으로 증명** |
| **verify-look** | 무속성 보라 ↔ 빈 룬 회색이 **색과 모양 둘 다** 갈린다. 지형이 안 타는 것이 한 화면에 보인다 |

**그물 1105 → 1138개.** 줄어든 그물 없음.

#### 단계 2 — 구간 통지 · 체력 · 직격/폭발 피해 · 무적 · HUD (판정 **1**, **3**)

| 파일 | 무엇 |
|---|---|
| `src/sim/spell_sim.gd` | 🔴 **이번 틱 구간 통지** |
| `src/actor/character.gd` | `hp` · `_invuln_left` · `on_tick(grid, spell)` · 선분↔상자 · 원↔상자 |
| `src/actor/world_step.gd` | `ch.on_tick()` 호출을 켠다 |
| `src/view/fx_tuning.gd` | 무적 깜빡임 상수 |
| `src/view/character_view.gd` | 무적 깜빡임 |
| `src/stage/stage.tscn` | `HUD/Health` 라벨 (🔴 `mouse_filter = 2`) |
| `src/stage/stage.gd` | 체력 라벨 갱신 |
| `tests/nets/net_damage.gd` | 판정 1·3 |

**구간 통지** — 폭발 통지와 **같은 어법**이다(틱마다 지우고, 시뮬은 안 읽는다):

```gdscript
var _seg_x0 := PackedInt32Array()   # 고정소수점 (셀 << 8)
var _seg_y0 := PackedInt32Array()
var _seg_x1 := PackedInt32Array()
var _seg_y1 := PackedInt32Array()
```

🔴 **끝점을 `x1` 로 두면 벽 *너머*의 캐릭터를 때린다.** 착탄한 탄은 `x1` 까지 안 갔다 —
`_walk` 가 멈춘 **착탄 셀 중심**까지 갔다. ⇒ `_walk` 가 도달점을 남기고 `_advance` 가 한 곳에서
통지한다:

```gdscript
var alive := _walk(...)
# 🔴 살면 끝까지 갔고, 죽었으면 **착탄점까지만** 갔다.
_notify_seg(x0, y0, x1 if alive else _walk_end_x, y1 if alive else _walk_end_y)
```

⚠ **세대도 룬도 안 싣는다.** 🔴 소비자가 없다 — 이번 단계는 **모든 탄이 같은 피해**다
(사용자 답 3). 안 쓰는 값을 실어 보내면 그게 거짓 손잡이다(`_notify_blast` 주석과 같은 규율).
⚠ **통지 자리는 하나다.** `_impact` 에서도 통지하면 「어떤 탄은 통지되고 어떤 탄은 안 된다」가 생긴다.

**피해 판정**(`character.on_tick`) — 🔴 **틱에서만 돈다.**

```
① 무적이면 아무것도 안 한다 (지속 피해는 여기가 아니라 step()이다)
② 구간 N개 × 캐릭터 상자 → 선분 대 상자 (🔴 사각형 대 사각형이 아니다)
③ 폭발 M개 × 캐릭터 상자 → 원 대 상자. 반경 = Tuning.blast_rd(통지의 gen)
④ 하나라도 맞으면: hp -= DAMAGE_HIT · 무적 켜기 · 그 방향으로 밀림(단계 4)
   🔴 **첫 히트 하나만 적용한다** — 그게 판정 3-①(같은 틱 폭발 둘 = 한 대)이다
```

⚠ **좌표계 변환이 두 곳이 되면 조용히 어긋난다.** 통지는 셀 고정소수점이고 캐릭터는 px다
⇒ `character.gd` 안에 변환 함수 **하나**(`fp → px = fp × CELL_PX / FP_ONE`)를 두고 거기만 쓴다.
🔴 폭발 반경도 통지에 안 실려 있으므로 **액터가 `Tuning.blast_rd(gen)` 을 읽는다** — 규칙이 한 벌.

**판정 1**
- 폭발: 벽 앞에서 폭발 문양으로 쏴서 캐릭터 옆에서 터뜨린다
- 직격: 벽에서 먼 빈 공간에 세우고 옆에서 수평으로 쏜다
- **뒤집어 보기 ①**: 반경 밖에 세우면 100 그대로
- 🔴 **뒤집어 보기 ②(필수) — 터널링 반증.** 첫 틱에 40px 뛰므로 16px 상자를 **통째로 넘는**
  배치를 일부러 만든다. ⚠ **그것만으로는 부족하다** — 그 배치가 정말 도약 배치인지를
  그물이 **스스로 단언해야** 한다:

  ```
  1. 통지 구간의 **양 끝이 둘 다 캐릭터 상자 밖**이다   ← 이 줄이 없으면 검사가 헛돈다
  2. 그런데 hp가 줄었다                                  ← 선분으로 재야만 참이다
  ```

  ⚠ 1번을 안 재면 배치가 우연히 상자 위에 걸쳐도 초록이고, 그러면 이 검사는
  **「함수를 부르나」만 재는 것**이 된다(문서 판정 1이 경계한 그대로).

**판정 3**
- **3-①** 같은 틱에 폭발 둘 → 한 번만. **뒤집기**: 무적 0으로 두면 두 번
- **3-②** 무적이 4틱인가 — 🔴 **직격으로 잰다.** 발사 커맨드를 넣으면 다음 틱에 나가서
  그 틱에 상자를 지나므로 **틱 간격을 정확히 제어할 수 있다.**
  3틱 간격 두 발 = 한 대 · 5틱 간격 두 발 = 두 대
- **3-③** 확산 8발에 몇 대인가 — 🔴 **상황을 하나 고정하고 세어서 이 문서에 적는다.**
  라벨에 숫자를 찍는다. ⚠ **목표 숫자를 단언하지 마라** — 기록이다

🔴 **판정 1·3·4는 전부 「무속성 + 문양 없음」으로 잰다.** 불 룬이면 착탄이 발판을 태워
「밀렸다」와 「떨어졌다」가 섞이고, 폭발 문양이면 지형이 바뀐다.
⇒ 격자가 한 셀도 안 변하는 것을 **같이 단언**하면 오염이 0임을 그물이 스스로 증명한다.
⚠ **그래서 단계 1이 단계 2보다 먼저다.**

**화면**: HUD `체력 100 / 100` + 무적 동안 캐릭터가 깜빡인다.
🔴 **체력을 `HUD/Stats` 에 같이 적지 않는다** — 두 곳에 적으면 갈라진다.

##### 결과 — 2026-08-04, **verify-look: 셋 다 화면에 있다 · 🔴 하나는 사용자 판정 대기**

**체력이 보인다** — 오른쪽 아래에 `체력 100 / 100`, 흰 글자에 어두운 배경이라 잘 읽힌다.
자리 `(660,494)` 크기 `292×36` 이고 🔴 **`HUD/Stats`(왼쪽 위)와 사각형이 아예 안 만난다.**
⚠ 아래 끝이 530이라 화면 바닥까지 **10px 남는다** — 잘리진 않지만 여유가 적다.

**내 폭발에 내가 맞는다** — 발밑에 쏘니 **`hp 100 → 90`**, 라벨도 같이 바뀌었다.
탄이 나를 통과해 바닥에서 터지고 그 폭발이 나를 때린다(문서 「탄은 나를 통과한다」 그대로). **R로 100 복구.**

🔴 **무적 깜빡임 — 있고 시계가 정확하다.** 실시간으로 매 렌더 프레임 `invuln_left` 를 기록했다:

```
+0ms  invuln 4  hp 100→90  밝음     +50ms  3  흐림     +100ms  2  밝음
+150ms  1  흐림            +200ms  0  밝음 ← 무적 끝
```

**깜빡임이 무적과 정확히 같이 시작하고 같이 끝난다. 뒤에 남는 깜빡임이 없다** —
「시계를 하나로 뒀다」가 화면에서 참이다. 대비도 크다(6배 확대: **속이 꽉 찬 밝은 사각형** vs **뒤가 비치는 흐린 사각형**).

🔴🔴 **그런데 폭발 섬광이 무적 전 구간을 덮는다 — 여기가 사용자 판정이다**

```
무적    200ms
섬광    FLASH_SEC = 210ms · 세대 0 반경 108px   ← 캐릭터(16px)를 통째로 삼킨다
그리기  CharacterView → BlastFx 순서 ⇒ 섬광이 캐릭터 위에 그려진다
```

⚠ **지금 맞는 경로가 「내 폭발」 하나뿐인데 그 섬광이 깜빡임과 거의 정확히 같은 200ms를 덮는다.**
확대 프레임에서는 섬광 위에서도 밝음/흐림이 **구별됐다.** 하지만
**「실제 속도로 플레이하면서 그 50ms 두 번을 섬광 너머로 알아채나」는 주관이다.**
⇒ 🔴 **verify-look이 혼자 판정하지 않고 넘겼다. 억지로 통과시키지 않았다.**

**조립창이 체력 라벨을 가린다 — 단계 6이 남았다는 뜻이다.**
`_toggle_assembly` 는 `Stats` 만 숨기고 **`Health` 는 숨는 게 아니라 덮인다** ⇒
**창을 줄이면 코드를 안 고쳐도 저절로 드러난다.** 지시대로 실패로 안 쳤다.

⚠ **못 본 것**: hp 0 근처·쓰러짐(단계 5) · 불붙은 캐릭터(단계 3) · **섬광이 없는 상황의 깜빡임**
(지금 유일한 피격 경로가 자기 폭발이라 **섬광 없이 무적인 순간이 없다**) · 연속 피격(100→90→80)의 화면.
에디터 로그 에러·경고 **0건**. 이번엔 **정상 종료로 껐다.**

##### 결과 — 2026-08-04, **verify-read: 여섯 막힘 · 🔴 구멍 하나 · 단일 실패점 하나** (뮤테이션 11회)

**표적 명중 여섯.** 🔴 **R1(터널링)이 특히 좋다 — 기준선 로그가 배치를 스스로 증명한다:**
`구간 양 끝(142.0 → 179.5)이 둘 다 상자[160,176] 밖이다 (도약 배치다)`.
**40px 도약이 16px 상자를 통째로 넘는 진짜 배치**고, 점 검사로 바꾸니 다섯이 빨개졌다.
**R2(벽 너머)도 진짜다** — 끝점이 150.0 → 179.5(상자 안)로 늘고 `벽 뒤의 캐릭터는 안 맞는다` 가 터졌다.
**1163개 초록이던 자리에 검사가 실제로 섰다.**

🔴🔴 **R7a — `_slab` 축평행 갈래의 「맞는다」 쪽이 통째로 안 재진다**

| 방향 | 결과 |
|---|---|
| 축평행 갈래가 **늘 맞는다** | 🔴 `폭발 반경 밖이면 체력 그대로` 빨강 ⇒ **「빗나가야 할 때 빗나간다」는 재진다** |
| 축평행 갈래가 **늘 빗나간다** | 🟢 **1170 전부 초록** ⇒ **「맞아야 할 때 맞는다」는 아무도 안 잰다** |

🔴 **구체적 구멍: 머리 위에서 수직으로 떨어지는 탄의 직격.**
기획이 「**발밑을 쏘면 탄이 나를 통과한다**」를 확정으로 적었으니 **도달 가능한 상황**이고,
그 갈래가 깨지면 **조용히 안 아프다.** ⇒ **검사 하나면 된다** — 캐릭터 바로 위에서 수직으로 쏘고 hp가 깎이는지.

🔴 **단일 실패점 하나** — `on_tick` 60Hz(위험 1)와 무적 가드 삭제가 **둘 다 `3틱 간격 두 발은 한 대다`
하나로만** 빨개진다. ⚠ 그 검사의 gap 상수(3·5)를 누가 손대는 날 **두 뮤테이션이 동시에 안 보이게 된다.**
⚠ 그리고 **위험 1은 「한 대가 세 대」로 안 나타난다** — 무적이 나머지 둘을 먹어 피해는 한 대 그대로이고
**무적이 3배 빨리 닳는 것으로만** 드러난다(`character.gd` 주석이 예언한 그대로). **관측 창이 좁다.**

**무적 4틱은 진짜 재진다 — 다만 그 줄이 아니다.**
`t.eq(ch.invuln_left, INVULN_TICKS)` 는 `INVULN_TICKS = 0` 뮤테이션에서 **`0 == 0` 으로 통과했다**
(CLAUDE.md 「값 단언이 우연히 맞는다」). **진짜 감지기는 3틱/5틱 쌍이고 4를 위아래로 가둔다**
(0 → 3틱 빨강 · 6 → 5틱 빨강). ⇒ 그 줄은 살릴 값어치가 있지만(무적을 아예 안 켜는 구현을 잡는다)
🔴 **라벨을 「맞은 틱에 무적이 켜진다」로 줄이면 정직해진다.**

**`on_tick(spell)` 로 줄인 것 — 판정: 타당하다.** 계획 §단계 3이 불 지속을 **`step()`** 에 두라고 했고
`step()` 은 **이미 `grid` 를 받는다** ⇒ `on_tick` 은 격자를 **영영 안 쓴다.**
⚠ 「무너진 지형에 깔림」이 붙는 날 돌아올 수 있고, **그때 넣는 게 지금 미리 두는 것보다 낫다.**

**잔여물 둘 닫혔다** — `TRACE_NONE` 텍스트 검사가 지난 라운드 초록이던 뮤테이션을 이제 빨갛게 만든다.
🔴 **라벨이 「갈래가 있다」까지고 「무속성이 아무것도 안 한다」로 안 넓혔다.** 전제 넷도 걸려 있다.
`sim_tuning` 주석도 실측과 맞다(⚠ 「1138 → 723」에서 **통과는 721**이다. 723은 통과+실패. 뜻은 안 바뀐다).

**늘 보는 셋 깨끗** — 새 `expect_error` 없음 · `stage.tscn` 이 바뀌었지만 못 세우면 `t.ok(false)` 로 짖는 형태 ·
**타입 있는 변수에 바로 대입하는 새 자리 없음**(새 게터 셋이 전부 `seg_count() == 1` 관문 뒤에 있다).

⚠ **안 본 것**: 화면 전부(verify-look 몫) · **확산 8발 「1대」를 다른 지형에서 재보지 않았다** ·
`_cell_center_fp` 의 반 칸 보정이 실제로 필요한지

##### 결과 — 2026-08-04, **verify-run: 판정 1 · 3 둘 다 통과** (헤드리스)

⚠ **첫 판은 배치가 다 틀렸다** — 손으로 계산했더니 탄이 중력으로 휘어 캐릭터 **아래로** 지나갔고
폭발 시험은 캐릭터를 **바닥 안**에 놓았다. ⇒ **궤적을 먼저 재고 그 구간에서 배치를 파생**시켜 다시 했다.

**판정 1** — 궤적 4번 구간 한가운데에 세우니 **hp 100 → 90**(궤적에서 고른 틱과 같은 4틱째),
격자는 한 칸도 안 변함. 200px 올리면 **100 그대로**.
폭발은 벽에 착탄시켜 궤적 28px 아래에 두니 **hp 90**, 🔴 **그 피해가 직격이 아님을 같이 단언했다**
(상자에 걸친 구간 **0개**). 반경 밖(86px)이면 100이고, ⚠ **그때 같은 폭발이 실제로 났다는 것도 같이 쟀다** —
안 하면 「폭발이 아예 안 났다」가 초록이 된다.

🔴🔴 **뒤집기 ②(터널링 반증) — 이번 검증에서 제일 센 것이다.**
구간 하나(29.3px) 위를 **1px씩 29자리** 훑어 **매번 새 세상에서** 쐈다:

- **29자리 전부 맞았다** — 빈틈 **0개**
- 🔴 **도약 배치가 실재한다** — 구간의 **양 끝이 둘 다 상자 밖**인 경우가 **13/29**
- 🔴 **그 13개에서 전부 hp가 줄었다 — 13/13**

⇒ **점/사각형 검사였다면 여기서 13개가 전부 빨개진다.** 「함수를 부르나」가 아니라 **「선분으로 재나」를 쟀다.**

**판정 3-②를 곡선으로 쟀다**(점 두 개가 아니라):
```
간격 → 잃은 hp:  1틱:10  2틱:10  3틱:10  4틱:10  5틱:20  6틱:20  7틱:20  8틱:20
```
**정확히 5틱에서 뒤집힌다 = 무적 4틱.** 뒤집기로 무적을 0으로 두면 간격 1틱에도 20.

##### 🔴🔴 3-③ 기록 — 확산 8발에 몇 대인가 (단언 아님. **기록이다**)

상자 `[500~516, 400~416]` · 발밑 24px를 `[확산 → 폭발]` 로 쏨. **배치만 바꿨다:**

| 배치 | 대 | 폭발(반경 안) | 맞은 틱 |
|---|---|---|---|
| ① 빈 방 | **2대** | 1회 (1) | 0, **44** |
| ② 좌우 벽 ±64px | **2대** | 6회 (2) | 0, **44** |
| ③ 사방 막힌 방 pad=32px | **1대** | 8회 (8) | 0 |
| ④ 사방 막힌 방 pad=48px | **1대** | 8회 (5) | 0 |
| ⑤ 사방 막힌 방 pad=64px | **1대** | 8회 (1) | 0 |
| ⑥ 비대칭(왼 32·오른 320px) | **2대** | 4회 (4) | 0, **44** |

🔴 **막힌 방에서 1대, 열린 지형에서 2대.** ⚠ **그런데 둘째 대의 정체가 문서의 예상과 다르다** —
「폭발이 4틱을 넘겨 흩어져서」가 **아니다**:

```
0틱   자기 폭발이 발밑 발판을 지운다 → 캐릭터가 떨어진다 (y 400 → 560)
44틱  되돌아온 확산탄의 구간이 떨어진 상자를 지나며 직격    ← 그 틱에 폭발은 0개
```

⇒ **「8발이 무적 창을 넘겨 터진다」가 아니라 「발판이 사라져 떨어지고, 40틱 뒤 포물선으로 되돌아온
자기 탄에 다시 맞는다」다.** 막힌 방은 방바닥이 받아 줘서 안 떨어지고, 그래서 1대다.

🔴 **⇒ 무적 0.2초를 조일 근거로 이 표를 쓸 때 주의해야 한다.**
**8발이 한 무적 창에 다 들어가는 것은 모든 막힌 배치에서 참이었고(1대), 2대는 무적과 무관한 다른 사건이다.**

**그 밖에**: 구간 통지가 매 틱 비워진다(탄 소멸 뒤 구간 0·폭발 0·탄 0) · `place()` 가 hp 100·무적 0으로 되돌린다 ·
회귀 — 옛 커밋 대조 **133줄 한 줄도 안 다름**, A/B 300프레임 동일, 음성 대조 111프레임 ·
그물 **1170개**, HEAD 1038 대비 **줄어든 그물 없음**.

**`on_tick(spell)` 에 대해**: 판정 1·3이 격자 없이 전부 통과했고 두 판정 경로가 **통지만 읽는다.**
⚠ **다만 「지금 안 쓴다」까지가 관측이고 「영영 안 쓴다」는 아니다** — 단계 3을 짜는 쪽이 밟을 자리다.

⚠ **못 쟀다**: 화면(verify-look 몫) · 새 검사 87개가 진짜 재는가(verify-read 몫) · hp가 0이 되는 데까지

---

### ✅ 단계 2 판정 — **통과** (판정 **1**, **3** 닫혔다)

| 검증 | 결과 |
|---|---|
| **verify-run** | 판정 1·3 통과. 🔴 **터널링 반증을 29자리로 훑어 도약 배치 13/13에서 전부 아팠다.** 무적을 곡선으로 재서 **정확히 5틱에서 뒤집힌다**(=4틱) |
| **verify-read** | 뮤테이션 11회 중 여섯 표적 명중. 벽 너머 검사가 **1163개 초록이던 자리에 실제로 섰다** |
| **verify-look** | 체력 라벨 · hp 100→90 · **깜빡임이 무적과 정확히 같이 시작하고 같이 끝난다**(뒤에 남는 깜빡임 없음) |

**그물 1138 → 1170개.** 줄어든 그물 없음.

🔴 **사용자 판단 둘 (2026-08-04) — 둘 다 「나중에」다:**

1. **무적 0.2초를 안 조인다.** 확산 8발이 막힌 곳에서 항상 1대이고 문서가 「1번이면 너무 긴 것」이라고
   적어 뒀지만, **「가까이서 확산을 쓰면 아프다」를 재려면 멀티가 붙어 남의 탄까지 쌓여야 한다.**
   지금은 혼자라 표본이 반쪽이다. ⇒ **단계 3·4를 먼저 쌓고 다시 본다**
2. **깜빡임이 섬광에 덮이는 것도 단계 3 뒤에 다시 본다.** 지금 맞는 경로가 「내 폭발」 하나뿐이라
   **섬광 없이 무적인 순간이 아예 없다.** 🔴 **단계 3(불 지속)이 붙으면 섬광 없이 맞는 경로가 생긴다** —
   그때 보는 것이 정확하다

⚠ **단계 2가 남긴 그물 빚 셋** — 단계 3에서 같이 닫는다:
**수직 직격 검사 없음**(축평행 갈래의 「맞는다」 쪽) · **단일 실패점**(위험 1과 무적 가드가 같은 한 줄) ·
**`invuln_left` 라벨이 숫자를 찍어 「4틱을 잰다」로 읽힌다**

#### 단계 3 — 불 위에 서 있기 (판정 **2**)

| 파일 | 무엇 |
|---|---|
| `src/actor/character.gd` | `step()` 안에서 밟은 셀의 `FLAG_BURNING` 을 보고 초당 깎는다 |
| `src/view/fx_tuning.gd` · `character_view.gd` | 불붙은 캐릭터 표시 |

🔴🔴 **「밟은 셀」에 발밑 한 줄을 포함시켜라. 안 하면 「불 위에 서 있는데 안 아프다」가 된다.**
캐릭터가 서 있는 자리의 셀들은 **빈칸이라 연료가 0** — 불은 **발밑 나무 셀**에 붙어 있다.
⇒ 검사 범위는 상자 + **아래 1px**(= `_grounded` 가 보는 줄)이다. ⚠ 이 한 줄을 빠뜨리면
코드는 돌고 그물도 짤 수 있는데 게임에서만 아무 일이 안 난다.

⚠ **무적을 갱신하지도 않고 무적에 걸리지도 않는다** — 이 갈래가 이 절의 전부다.
⚠ **프레임(60Hz)에서 돈다.** 초당 피해는 비율이라 프레임이 자연스럽고, 틱에 두면 3배 문제가 없다.
🔴 `hp` 는 정수로 두고 **누산기**를 따로 든다 — float HP는 HUD도 판정도 흐린다.

- **본체**: 🔴 **탄으로 먼저 맞혀 무적을 켠 상태에서** 불 위에 세운다.
  ⚠ 무적을 안 켜고 재면 「그냥 불 피해」를 재는 것이고 **틀린 구현도 통과한다**
- **뒤집기**: 연료가 다해 불이 꺼지면 깎임이 멈춘다
  ⚠ 다 탄 셀은 **빈칸이 되어 바닥이 사라진다** ⇒ 그 아래에 돌을 깔아 두고 재라

##### 결과 — 2026-08-04, **verify-run: 판정 2 통과** (헤드리스, 무적을 켠 채로)

🔴🔴 **「무적과 무관하게」를 진짜로 무적을 켜고 쟀다.** 탄으로 먼저 맞혀 무적을 켠 상태로 불 위에:

- 무적이 켜져 있던 **12프레임(=4틱)** 동안 **hp가 2 깎였다**(88 → 86).
  **12프레임 × 10dps ÷ 60 = 2.0 — 값이 이론과 같다**
- 최종 hp **81** — 직격 10 + 불 1초분이 **둘 다** 들어갔다
- 🔴 **뒤집기**: 같은 발사·같은 자리인데 **불만 없으면** 무적 동안 hp가 **한 톨도** 안 준다(0, 최종 90)

⇒ **불 경로가 `invuln_left` 를 읽지도 쓰지도 않는다는 것이 거동으로 확인됐다.**
⚠ 뒤집기를 처음엔 틀리게 짰다 — **맞은 그 프레임 자체를 「무적 중 감소」로 세는 바람에** 직격 10이 잡혔다.
**「프레임 시작 시점에 이미 무적이었나」로** 고쳤다.

**시간에 비례한다**: `30프레임:4 · 60:9 · 90:14 · 120:19` — **증분 [5, 5, 5]**, 0.5초마다 정확히 5.
🔴 네 창 모두 **창 내내 불이 살아 있었다**(매 프레임 확인).

⚠🔴 **절대값이 이상값(5·10·15·20)보다 1씩 적다 — 결함이 아니라 누산기의 부동소수다.**
`10.0/60` 을 30번 더하면 **4.999…** 라 `floori` 가 4를 준다. ⇒ **증분으로 재야 옳다.**
**절대값으로 재면 정상 구현이 빨개진다.**

**불이 꺼지면 멈춘다** — 179프레임째 불이 꺼지고(hp 81) 캐릭터는 **여전히 서 있는데**(y=312)
그 뒤 **5초를 더 서 있어도 hp가 81 그대로**다.

**발밑 한 줄**: 발밑에 불이면 깎이고, **한 칸 옆**(돌로 끊은 나무섬)과 **한 층 위**(돌 선반)는
**hp 100 · `burning=false`** — ⚠ 그때 **그 불이 창 내내 살아 있었음을 같이 단언**했다.
⚠ **처음엔 「한 칸 옆」이 빨갰는데 코드가 아니라 배치 탓이었다** — **나무로 이어 두면 불이 번져
60프레임에 4셀을 건너와 발밑까지 온다.** 돌로 끊고 다시 쟀다.

**불 + 탄이 정확히 더해진다**: `불만 9 · 탄만 10 · 둘 다 19`.

**회귀 — `step()` 과 `is_burning()` 이 바뀐 자리**: 옛 커밋 대조 **133줄 한 줄도 안 다르다** ·
🔴 **불 번짐도 숫자까지 같다**(`타는 셀 39 · 나무 736→608 · 돌 5088→4872`) ⇒
**`is_burning()` 신설과 `fuel_at` 경유 변경이 번짐을 안 건드렸다** · A/B 300프레임 동일 · 음성 대조 111프레임.
그물 **1193개**, HEAD 1038 대비 **줄어든 그물 없음**.

⚠ **실행 둘을 버렸다** — 측정 중에 트리 해시가 **두 번** 바뀌었다(다른 검증자의 뮤테이션).
**검증자 동시 실행의 위험이 두 번째로 실제로 났다.**

⚠ **못 쟀다**: 화면(테두리가 보이나 — verify-look 몫) · 새 검사 110개가 진짜 재는가(verify-read 몫) ·
불로 hp가 0까지 가는 것(판정 6) · **무대 실물에서 불 위를 걸었을 때 hp**(진짜 씬 프로브가 hp를 안 찍는다)

##### 결과 — 2026-08-04, **verify-read: 빚 셋 닫힘 · 새 검사 다섯 표적 명중 · 🔴 남은 것 하나** (뮤테이션 10회)

**빚 ① 수직 직격** — 지난 라운드 **1170개 초록**이던 뮤테이션이 이제 실패 1.
🔴 **배치도 진짜다**: `cmd_fire(…, 0, 10)` 이라 vx가 정확히 0이고 **모든 구간의 x델타가 0** ⇒
**축평행 갈래를 반드시 지난다**(다른 갈래로 통과하는 게 아니다). 그물이 스스로 단언한다 —
`발사선이 상자 안이다 (배치의 전제)`. 「첫 틱은 안 맞는다」 전제도 진짜다(구간 검사를 늘 참으로 두니 빨개졌다).

**빚 ② 단일 실패점 — 분리 성공.** `on_tick` 60Hz 뮤테이션이 지난번엔 **한 줄**이었는데 이제 **검사 셋**이 문다:
`한 틱 지나 무적이 3 남는다 — 얻은 값 1 · 기대 3` 이 **직접** 물고, 3틱 간격과 불도 같이 빨개진다.
🔴 **새 검사는 간격 상수(3·5)를 안 쓴다** — `invuln_left` 를 읽어 세어 내려간다.
⇒ **간격 상수를 손대도 위험 1이 계속 보인다.**

**빚 ③ 라벨** — 숫자를 뺐는데 **inert가 아니다**(틱당 두 번 뮤테이션에서 그 줄이 빨개진다).
**정직해지면서 잴 것은 남았다.** 3-① 주석도 실측과 맞다.

**새 검사 다섯 다 표적 명중.** 🔴 **발밑 한 줄 검사가 특히 좋다** — 실패 5개고 **배치가 스스로를 증명한다**
(`상자가 덮는 셀에는 타는 칸이 하나도 없다` 를 단언한다. 그게 참이어야 「발밑 한 줄」을 재는 것이다).
**그물 자기검사**도 섰다 — 창을 나무 수명 밖으로 늘리니 **전제가 먼저 짖어서**
「불이 꺼져서 덜 깎였다」가 「비례가 깨졌다」로 **안 읽힌다.**

🔴 **`cell_grid.is_burning()` 판정: 타당하다.** `character.gd:153` 이 이미 「「고체인가」는 격자에 물어본다 —
여기서 다시 판정하면 규칙이 두 벌이 된다」고 적어 뒀고 **`is_burning` 은 그 짝이다.**
🔴 **그리고 불 번짐은 원리적으로 못 깨뜨린다** — `is_burning` 을 늘 true로 두니 **실패 10개가 전부 `net_damage`**
였고 **`net_fire` 는 하나도 안 움직였다** ⇒ 번짐은 `fuel_at` 을 안 지난다(자기 파면 목록으로 돈다).
⚠ **대신 `fuel_at` 자신의 계약(「타는 셀이 아니면 0」)은 아무도 안 잰다** — **이번 변경이 만든 게 아니라
원래 얇던 자리**다(그물의 `fuel_at` 호출이 전부 타는 칸에서만 이뤄진다).

🔴 **남은 것 하나 — `_burn_acc` 를 안 비우는 판단은 맞는데 그물이 안 잰다.**
비우든 안 비우든 **1193개 그대로**다. **판단 자체는 정확하다**: `10dps · 1/60` ⇒ 1점 쌓는 데 **6프레임**이라
비우면 **0.1초 미만 접촉이 영원히 공짜**가 되고 무한 반복할 수 있다 —
GDD의 「연료를 어디 두느냐가 곧 레벨 디자인」이 거기서 샌다.
⚠ **다만 계획에 없는 결정이고 그물 밖이라, 「정리」한다고 비우는 사람이 나오면 아무도 안 짖는다.**
⇒ **검사 하나면 된다** — 불을 **5프레임씩 여러 번** 밟고 결국 hp가 주는지.

**늘 보는 셋 깨끗** — 새 사면 없음 · 씬 못 세우면 `t.ok(false)` · **색인/`get` 새 자리 없음**(10곳 전부 막혀 있다).

##### 결과 — 2026-08-04, **verify-look 통과 + 🔴 미뤄둔 깜빡임 판정도 내렸다**

🔴🔴 **깜빡임 — 보인다. 통과.**
⚠ **리드의 추론(「섬광이 끝난 뒤 무적이 남는 구간」)은 시간상 틀렸다** — 섬광 210ms > 무적 200ms라
겹치지 않는 구간이 없다. **그런데 답은 나왔다: 섬광이 꺼지는 게 아니라 옅어진다.**

| 무적 창 뒤 절반 | 보인 것 |
|---|---|
| `invuln = 1`(+150ms, 흐림) | 몸통이 **탁한 회색**. 섬광은 이미 **거의 안 보이는 갈색 원반**으로 죽었다 |
| `invuln = 0`(+200ms, 밝음) | 몸통이 **밝은 흰색**, 파란 발 띠까지 또렷 |

⇒ **두 번째 흐림은 섬광과 경쟁하지 않는다. 매 피격마다 최소 한 번의 흐림이 깨끗하게 보인다.**
근거 넷: 대비가 크다(알파 1.0↔0.35) · 흐림 2회 각 50ms(=3프레임, 명멸로 인지되는 길이) ·
뒤쪽 흐림은 섬광이 없다 · 시계가 무적과 정확히 일치한다.
⚠ **단서**: 200ms는 「**맞았다**」라는 **사건**으로 읽히지 「**지금 무적이다**」라는 **상태**로는 안 읽힌다.
**기획이 고른 0.2초의 성질이지 구현 결함이 아니다** — 상태로 읽히길 원하면 그건 값 조정이다.

**불붙은 표시가 아주 잘 보인다** — 창백한 몸통에 **선명한 주황 테두리**. 몸통과도 배경과도 안 섞인다.
**체력이 정확히 100ms마다 1씩 준다**(`BURN_DPS = 10/s` 그대로) 그리고 HUD가 같이 움직인다.
🔴 **불이 무적을 안 켠다 — 두 번 실측**(hp 97→81 동안, 그리고 100→79 동안 `invuln_left` 가 내내 0).

⚠🔴 **셀 불에 테두리가 안 묻히는데 이유가 「운」이다.** 캐릭터는 불붙은 나무 **위**에 서고 몸통은
그 위 **빈칸**에 있다 ⇒ 불꽃이 늘 **발밑**이라 테두리가 늘 어두운 배경을 등진다.
**지금 나무가 2타일이라 캐릭터 키 옆에 불이 설 자리가 없어서 그렇다.**
🔴 **나무가 캐릭터 키만큼 쌓인 지형이 오면 다시 봐야 한다.**

🔴🔴 **못 본 것 — 「무적 흐림 + 불 테두리」 동시. 그 상태를 만들 경로가 지금 게임에 없다.**
네 번 시도해 전부 실패했고 `burning && invuln_left > 0` 을 매 프레임 세어 **0회**를 확인했다:

- **폭발이 발밑 불을 같이 지운다** — 맞은 다음 틱에 이미 `burning = false`
- 확산 자식 탄이 나를 안 지나가 `invuln` 이 아예 안 켜진다
- 🔴 **폭발 피해 반경 = 카브 반경**(`blast_rd × CELL_PX`, 세대 0 = 48px = 3타일)이라
  **나를 때리는 폭발은 반드시 내 발판도 판다**

⚠ **결함이 아니라 지금 범위의 성질이다** — 혼자이고 피격 경로가 「내 폭발」뿐이다.
**동료·몬스터가 붙는 날 그 겹침이 처음 생긴다.** 코드상으로는 갈려 있다 —
`character_view.gd:67` 의 테두리는 **`dim` 을 안 곱한다**(몸통·발띠만 곱한다).

---

### ✅ 단계 3 판정 — **통과** (판정 **2** 닫혔다)

| 검증 | 결과 |
|---|---|
| **verify-run** | 🔴 **무적을 켠 채로** 불 위에 세워 12프레임에 hp 2(이론값과 일치). 불만 없으면 0. 시간 비례 증분 [5,5,5] |
| **verify-read** | 빚 셋 닫힘 확인. 새 검사 다섯 표적 명중. **불 번짐은 원리적으로 못 깨뜨린다** |
| **verify-look** | 주황 테두리가 잘 보이고 hp가 100ms마다 1씩. **미뤄둔 깜빡임 판정도 통과** |

**그물 1170 → 1193개.** 줄어든 그물 없음.

⚠ **단계 3이 남긴 빚 하나**: **`_burn_acc` 를 안 비우는 판단이 그물 밖**이다(비우든 안 비우든 1193 그대로).
⚠ **그리고 다음 단계가 알아야 할 것**: 🔴 **폭발 피해 반경 = 카브 반경이라 나를 때리는 폭발은
반드시 내 발판도 판다.** 판정 4(밀림)에서 **「밀렸다」와 「발판이 사라져 떨어졌다」가 섞인다.**

#### 단계 4 — 밀림 · 반동 (판정 **4**, **5**)

> 🔴🔴 **이 단계의 밀림 절반은 2026-08-04에 삭제됐다.** 반동은 산다.
> **아래 설계와 검증 기록을 한 줄도 안 지웠다** — 관측은 그때 참이었고,
> 지우면 **무엇을 재고도 놓쳤는지**가 같이 사라진다.
> ⚠ `kb_vx` 는 이름만 남아 `recoil()` 전용 축이 됐다(`character.gd` 가 그 사연을 적고 있다).

| 파일 | 무엇 |
|---|---|
| `src/actor/character.gd` | `kb_vx` · 지수 감쇠 · `knockback(dir)` · `recoil(adx, ady)` |
| `src/actor/world_step.gd` | 🔴 **`spell.fire(cmd)` 가 참을 준 뒤에** 반동을 건다 |

사용자 답 4: **입력과 더해지고 시간에 따라 감쇠한다. 입력이 즉시 이기지 않는다.**

```gdscript
_move_x(grid, (axis * MOVE_SPEED_PX + kb_vx) * dt)   # 🔴 더한다. 덮어쓰지 않는다
kb_vx *= pow(KB_DECAY_PER_SEC, dt)                   # 시간에 따라 감쇠
```

🔴 **가로는 새 축(`kb_vx`)이고 세로는 기존 `vy` 에 더한다.** 규칙이 두 벌이라서가 아니라
**세로에는 이미 감쇠 장치(중력)가 있어서**다. `character.gd` 가 「`vy` 만 있고 `vx` 가 없다」고
적어 둔 자리가 정확히 여기다.

⚠ **반동은 2D다.** 아래로 쏘면 위로 뜬다 — 그게 GDD의 로켓점프이고, 문서 「마법 반동」이
「위험과 기동이 같은 행동에서 나온다」고 적은 자리다.
⚠ **무적 중에는 피해도 밀림도 없다** — 「한 대」가 한 대여야 한다.

- **판정 4**: x를 기록하고 **수평으로** 맞힌 뒤 반대쪽으로 갔나
- 🔴 **뒤집기(필수)**: 맞는 순간부터 **반대 방향 입력을 계속 넣고도** 변위가 남나
  ⚠ **잴 창을 정해라.** 입력은 영원히 들어오고 밀림은 감쇠하므로 시간이 길면 반드시
  되돌아온다 — 재는 것은 **밀림이 도는 동안 입력을 이기나**다. 창은 **0.5초(30프레임)**.
  §4의 수치로 그 창의 순 변위가 **약 +23px(1.5타일)** 이다
- **판정 5**: 쏘면 반동으로 밀리나
- 🔴 **뒤집기**: **거부된 발사는 반동을 안 만든다.**
  ⚠ 문서는 「빈 룬으로 클릭」이라고 적었는데 **그 경로는 껍데기(`_fire_at` 의 `can_fire()`)에 있어
  그물이 못 부른다.** ⇒ 그물은 `fire()` 가 거부하는 커맨드(모르는 문양 id)를 큐에 넣고
  반동이 0인 것을 잰다. **라벨을 「거부된 발사」로 적어라** — 「빈 룬」이라고 적으면
  라벨이 재는 것보다 넓다. 빈 룬 경로는 **verify-run 이 잰다**

##### 결과 — 2026-08-04, **verify-read: 다섯 막힘 · 🔴 안 재는 것 둘 · 문서와 어긋난 것 하나** (뮤테이션 8회)

🔴 **뮤테이션을 전부 `git worktree` 에서 돌렸다. 본 리포는 읽기만 했다** — 앞선 세 번의
「검증자 동시 실행으로 실행을 버림」이 이걸로 끝났다.
⚠ **다음 사람을 위한 실측 둘** — 안 넘으면 측정이 통째로 거짓이 된다:
**`git apply` 가 줄끝을 CRLF로 바꾼다**(변경 파일을 바이트 그대로 덮어써야 한다) ·
**새 워크트리엔 `.godot/` 클래스 캐시가 없어 애드온이 stderr 54줄을 뿜는다**
(🔴 **안 옮기면 「침묵사 54줄」이 기준선이 되어 진짜 짖음이 그 안에 묻힌다**).

**다섯 다 막혔다.** 🔴 **「덮어쓴다」 검사가 진짜로 문다** — 세 답이 전부 갈린다:

| 답 | 관측 | 잡는 검사 |
|---|---|---|
| **더해진다**(사용자가 고른 것) | `free − pushed ≈ 65` | 통과 |
| 덮어쓴다 / **막힌다** | `free − pushed = 0` | ✓ |
| 입력이 즉시 이긴다 | `pushed < 0` | ✓ |

⚠ 「막힌다」와 「덮어쓴다」는 **관측이 같아서**(둘 다 입력이 변위에 안 닿는다) 한 검사가 둘 다 잡는다.

🔴🔴 **안 재는 것 둘 — 둘 다 사용자 답 4의 뒤 절반이다.**
답 4는 「**입력과 더해지고 시간에 따라 감쇠한다**」인데 **앞 절반만 재진다:**

1. **감쇠를 통째로 지워도 1222 전부 초록** — `kb_vx *= pow(DECAY, dt)` 를 `*= 1.0` 으로 두면
   **맞은 캐릭터가 영원히 미끄러진다.** 화면에서 1초면 보이는 고장인데 그물이 못 본다.
   ⇒ 창을 둘로 나눠 **둘째 창 변위 < 첫째 창 변위**(둘 다 양수인 채로) 한 줄이면 된다
2. 🔴 **폭발 밀림 방향을 뒤집어도 1222 전부 초록** — **폭발이 밀어내는 게 아니라 빨아들여도 통과한다.**
   **이쪽이 더 나쁘다**: 기획이 「발밑을 쏘면 탄이 나를 통과하고 **바닥에서 터진 폭발이 나를 때린다**」를
   확정으로 적었으니 **폭발이 실전의 주 피격 경로**다. 뒤집히면 **「비켜」가 「빨려든다」가 된다.**
   ⚠ **verify-look도 잡기 어렵다** — 밀렸는지 빨려들었는지 둘 다 「움직였다」로 보인다

🔴🔴 **세로 밀림 — 문서와 어긋난다. 판정이 명확하다.**
기획 「밀림」은 「**밀림의 세기는 미정이다.** 다만 **방향은 정해져 있다 — 탄이 온 쪽에서 반대로**」다.
⇒ **미정인 것은 세기뿐이고 방향은 제한 없이 닫혀 있다.** 구현은 가로만 연다.
🔴 `character.gd` 주석의 「기획이 **세로를 안 열었다**」는 **정확하지 않다** — 정직한 문장은
「**구현이 가로만 열었다**」다.
⚠ **비대칭이 진짜 냄새다 — 반동은 2D인데 밀림은 1D다.** 둘 다 같은 가족이고 문서가 형제로 다룬다.
지면에서 수직 피격은 규칙상 무효라 대가가 작지만 🔴 **공중에서는 다르고, 점프가 있고 반동으로 뜨기까지 한다.**
⇒ **코드 한 줄을 열거나 문서에 「세로 밀림 보류」를 적어야 한다. 둘 중 하나는 반드시.**

**배치 수정 넷 판정**: 셋은 ✅ 옳다(발사 원점 파생 — 박아 두면 `_two_shots_hp` 가 **조용히
「두 번째가 안 맞았다」를 재게 된다** · 도약 단언이 판정 시점 상자 · 판정 5를 상자 밖에서).
🔴 **넷째(불 검사가 큐를 안 지나는 것)는 받아들일 만하지만 값을 정확히 적어야 한다** —
재려는 성질(**불이 무적을 안 탄다**)은 **경로와 무관**하고(`_burn()` 이 `invuln_left` 를 읽지도 쓰지도 않는다)
라벨도 정직하다. ⚠ **잃는 것**: 그물이 **게임이 만들 수 없는 상태**(반동 없는 피격)를 세운다 ⇒
**「불 위에서 발밑을 쏘면 반동으로 떠서 불에서 벗어난다」가 아무도 안 재는 거동이 됐다**(버그인지 사양인지도 문서에 없다).
⚠ **이 리포에서 「그물이 게임과 다른 길을 지나는」 첫 사례다** — 선례라 남겨 둔다.

**늘 보는 셋 깨끗** — 새 사면 없음 · 씬 형태 그대로 · **색인/`get` 새 자리 없음** ·
라벨이 정확하다(특히 `_rejected_fire_has_no_recoil` 이 「빈 룬이라고 적지 마라 — 그 경로는 껍데기에 있어
그물이 못 부른다」를 **스스로 적어** 라벨을 좁혔다).
⚠ 부동소수 정확비교 둘(`t.eq(kb_vx, 0.0)`)은 **누산 비교가 아니라 초기값 비교**라 지금은 안전하다.

⚠ **안 본 것**: 화면 전부 · 가정값 셋이 손맛에 맞는지 · **폭발 밀림이 게임에서 실제로 어느 쪽으로 미는지** ·
🔴 **낭떠러지에서 밀려 떨어지는 것** — **기획이 확정으로 적은 거동인데 검사가 없다**

##### 결과 — 2026-08-04, **verify-run: 판정 4 · 5 둘 다 통과** (헤드리스)

🔴🔴 **「더해지나 덮어쓰나」를 수치로 갈랐다.** 같은 밀림에 **입력만 바꿔** 0.5초 창의 변위를 쟀다:

```
입력 없음  +85px      반대(-1)  +20px      같은쪽(+1)  +150px
차이가 정확히 입력 몫이다 — 150-85 = +65 · 85-20 = +65 · 이론 130px/s × 0.5초 = 65px
대조군(밀림 없이 입력만) = +65px
```

⇒ **덮어쓰기였다면 셋이 같았을 것이다. 20/85/150 으로 갈렸다.**
⚠ **「반대 입력을 넣고도 +20이 남는다」만으로는 못 가른다** — 조작을 죽인 구현이면 **+85가 그대로 나와
오히려 커진다.** 세 값을 다 재야 갈린다.

**판정 4**: 옆에서 무속성·문양 없음으로 맞히니 hp 90 · **x +85px**(탄이 가던 쪽) ·
🔴 **격자가 한 셀도 안 변했다**(오염 0을 스스로 증명) · 안 떨어졌다.
🔴 **폭발 밀림 방향도 거동으로 확인됐다** — 상자 **왼쪽**에 터뜨리면 **+85px**, **오른쪽**이면 **−85px**.
**터진 자리에서 멀어지는 쪽으로 민다.**

**판정 5**: 오른쪽으로 쏘니 **−44px** · 🔴 **아래로 쏘니 15px 떠올랐다**(이론 16.7px) — **로켓점프가 된다** ·
뒤집기 둘 다 통과(거부된 발사 → `kb_vx 0.00` · **진짜 씬·진짜 클릭으로 룬을 빼고 클릭** → 발사 0 · x 그대로).

🔴🔴 **진짜 무대에서 아래로 쏘면 — 로켓점프에 대가가 붙어 있다**

```
총구 tip = (54.6, 486.1)  ← 상자[48~64, 496~512] 의 위쪽이다
1프레임: 구간 (54,486)→(58,510) 이 상자를 관통 ⇒ 직격
결과: 15px 떠오르고 · x 48 → 112 (+64px) · hp 100 → 90
```

⇒ **아래로 쏘면 탄이 나를 통과하며 직격으로 잡히고 밀림 400이 반동 200을 덮는다.**
기획이 「발밑을 쏘면 탄이 나를 통과한다」고 확정한 그대로이고 hp도 설계대로지만,
⚠ **가로 방향이 총구의 1.4px 좌우 어긋남(54.6 vs 상자 중심 56)으로 정해진다** —
**400px/s짜리 큰 밀림의 방향이 아주 작은 기하학적 우연에 달려 있다. 뒤집을 수 있는 자리다.**

**회귀 — A/B가 271프레임 다른데 그게 정상이다.** 옛 드라이버엔 `recoil()` 이 없다.
🔴 **갈래를 갈라 봤더니 캐릭터만 다른 프레임 271개 · 시뮬/통지도 다른 프레임 0개**, 첫 갈림이 첫 발사 프레임이다
⇒ **격자 해시·탄·통지·틱은 한 프레임도 안 다르다.**
진짜 씬 대조에서 달라진 줄 넷이 전부 발사 절이고, 🔴 **`place()` 로 반동만큼 되돌리고 같은 걷기를 시키니
옛 커밋 값이 그대로 나왔다**(`x 50→308 · 공중 14프레임`) ⇒ **이동 코드는 안 변했다.**

##### 🔴🔴 세로 밀림 — **검증자 둘의 판정이 갈렸다. 문서가 모호하다**

**같은 문장**(「밀림의 세기는 미정이다. 다만 **방향은 정해져 있다 — 탄이 온 쪽에서 반대로**」)을 놓고:

| | 읽은 것 |
|---|---|
| **verify-read** | 미정인 것은 **세기뿐**이고 **방향은 제한 없이 닫혀 있다** ⇒ 구현이 가로만 연 것은 **어긋난다** |
| **verify-run** | 그 문장은 **세로를 열지 않았다** ⇒ **안 정한 자리**이고 어긋난 게 아니다 |

**측정값은 같다** — 수직 낙하 탄에 맞으면 **hp 90(맞긴 맞았다) · `kb_vx = 0.0` · 변위 +0px.**
🔴 **화면에서는 「위에서 떨어진 탄에 맞았는데 꿈쩍도 안 한다」로 보인다.**
⚠ **비대칭이 남는다 — 반동은 2D인데 밀림은 1D다.** 지면 피격은 규칙상 대가가 작지만
**공중에서는 다르고, 점프가 있고 반동으로 뜨기까지 한다.**
⇒ **사용자 판단 자리.**

⚠ **못 쟀다**: 화면(verify-look 몫) · **밀림이 벽에 부딪히면**(계획 §4 가정: 거기서 멈춘다 — 넓은 발판에서만 쟀다) ·
🔴 **밀려 낭떠러지에서 떨어지는 것**(기획이 「난다」고 확정한 자리인데 이번에 안 쟀다)

##### 결과 — 2026-08-04, **verify-look: 전부 작동한다 · 손맛에 할 말 셋**

🔴🔴 **검증 방법의 함정 — 다음 verify-look이 반드시 알아야 한다.**
**지팡이 방향은 `get_global_mouse_position()`, 즉 사용자의 실제 OS 마우스를 읽는다**(`character_view.gd:38`).
⇒ **`push_input` 으로 넣은 클릭은 조준 방향을 못 정한다.** 발사 방향이 `(클릭 셀 − 총구 끝 셀)` 인데
**총구 끝이 진짜 마우스를 따라가기 때문**이다. 처음에 「발밑에 쏜다」로 재니 수직이 아니라 **대각선**이 나왔다.
⇒ **`CharacterView.tip_px()` 를 읽어 그 기준으로 클릭 위치를 계산해야 방향이 결정된다.**

**로켓점프 — 단독으론 못 쓴다. 점프와 겹치면 두 배다.**

| | 상승 | 타일 |
|---|---|---|
| 반동만(아래로) | **15px** | **0.94타일** — 한 타일(16px)도 못 넘는다 |
| 점프만 | 51px | 3.2타일 |
| 🔴 **점프 직후 아래로 쏘기** | **109px** | **6.8타일** — 이단점프급 |

🔴 **이유가 코드에 있고 손잡이가 숨어 있다** — `character.step` 의 점프는 `vy = JUMP_VY_PX` **대입**이다.
⇒ **점프와 같은 프레임에 쏘면 반동이 지워지고, 떠오른 뒤에 쏴야 더해진다.**
**「언제 쏘느냐」가 높이를 정한다** — 기술로도 버그로도 읽힌다.

**밀림 — 400이 실제로 걸린다. 그런데 지형이 절반을 먹는다.**
같은 조준으로 두 번 쏴서 떼어 쟀다: 문양 없음 `kb_vx -132.5`(반동만) vs 폭발 **`374.8`**(≈400).
그런데 눈에 보이는 변위는 `+0.1초 17px · +0.2초 24px · +0.3초 29px` 로 **이론(0.3초 66px, 총 102px)의
절반 이하**다 — 🔴 **자기 폭발이 판 구멍에 빠져 구멍 벽에 막힌다.**
⚠ **결함이 아니라 조합의 성질이다**(나를 때리는 폭발은 반드시 내 발판도 판다).
🔴 **동료 탄에 맞는 날(멀티) 처음으로 온전한 밀림이 보인다.**
**그래도 29px ≈ 2타일이라 화면에서 「튕겨 나갔다」로 읽힌다** — 한 프레임에 `캐릭터 (400)→(431) 공중` ·
`체력 90/100` · 발밑에 큰 구멍 · 양옆에 불이 **동시에** 찍혔다. **기획의 「위험과 기동이 같은 행동에서
나온다」가 그 한 장에 다 있다.**

🔴 **반대 입력 뒤집기 통과** — 반대 키를 계속 누른 채로도 `+18f x405→x422`(**여전히 밀림 방향**),
`+30f x412 · +60f x359` ⇒ **밀림이 죽은 뒤에야 입력이 이긴다.** 사용자가 고른 「더해지고 감쇠한다」 그대로다.

**빈 룬으로 5번 연속 클릭** — `kb_vx 0.000 · 이동 0px · 발사 0 · 체력 100`.
**밀려 떨어진다** — `y 496 → 560` 으로 격자 바닥까지. ⚠ **낙하 피해도 격자 밖 처리도 없어 아무 일이 없다** —
문서가 「둘 중 하나는 채워야 한다」고 적어 둔 자리다.

⚠ **못 본 것**: 동료/원거리 피격으로 온전한 밀림(경로가 없다) · 밀림이 벽에 부딪히면 · 밀림 중 깜빡임 조합

---

### 🔴 단계 4 — **판정 4 · 5는 통과. 그런데 닫지 않는다** (사용자 판단 셋이 일을 남겼다)

| 검증 | 결과 |
|---|---|
| **verify-run** | 🔴 **「더해지나 덮어쓰나」를 수치로 갈랐다** — 입력만 바꿔 `+20 / +85 / +150`, 차이가 정확히 입력 몫(65px). 폭발 밀림 방향도 거동 확인 |
| **verify-read** | 다섯 막힘. 🔴 **워크트리 뮤테이션으로 동시 실행 문제를 끝냈다.** 안 재는 것 둘 발견 |
| **verify-look** | 밀림 400이 실제로 걸린다 · **반대 입력을 눌러도 밀림이 남는다** · 빈 룬은 안 밀린다 · 밀려 떨어진다 |

**그물 1193 → 1222개.**

🔴 **사용자 판단 셋 (2026-08-04):**

1. **세로 밀림을 연다** — 위 「동작 → 밀림」에 박았다. **코드가 가로만 열고 있어 고쳐야 한다**
2. **점프와 같은 프레임에 쏘면 반동이 지워지는 것은 「기술」이다** — `vy = JUMP_VY_PX` **대입**이라
   그렇고, **떠오른 뒤에 쏘면 51 → 109px(2.1배)**가 된다. 🔴 **「언제 쏘느냐」가 높이를 정하는 것이
   손맛이다.** ⚠ **문서와 코드에 「의도된 것」으로 적어야** 다음 사람이 「버그」로 읽고 안 고친다
3. **반동 세기(200)는 그대로 둔다** — 단독 15px가 한 타일도 못 넘지만 **점프와 겹치면 109px로 충분하고,
   단독으로 안 되는 것이 「로켓점프가 공짜가 아니다」에 오히려 맞는다.** 멀티가 붙고 다시 본다

⚠ **단계 4가 남긴 그물 빚 넷** — 다음에 닫는다:
**감쇠가 안 재진다**(지워도 초록. 영원히 미끄러지는 고장) · 🔴 **폭발 밀림 방향이 안 재진다**
(빨아들여도 초록. **「비켜」가 「빨려든다」가 되는 자리**) · **낭떠러지 낙하 검사 없음**(기획이 확정으로 적었다) ·
**불 검사가 큐를 건너뛴다**(「그물이 게임과 다른 길을 지나는」 첫 사례. 값은 정직하게 적혀 있다)

#### 단계 5 — 쓰러짐 (판정 **6**)

| 파일 | 무엇 |
|---|---|
| `src/actor/character.gd` | `downed` — 입력을 막고 중력은 그대로 |
| `src/view/fx_tuning.gd` · `character_view.gd` | 🔴 **눕힌 실루엣** — 서 있는 것과 확실히 다르게 |
| `src/stage/stage.gd` | 체력 라벨에 「쓰러짐」 |

- 즉사가 아님은 **중력을 계속 받는 것**으로 잰다
- 🔴 **행동불능을 같이 잰다**: 입력을 넣어도 x가 안 변한다
  ⚠ **밀림이 남아 있으면 x가 변한다** ⇒ 쓰러뜨린 뒤 **30프레임 기다려 밀림을 재우고** 재라
- **뒤집기**: 안 쓰러진 체력에서는 같은 입력에 x가 변한다
  ⚠ 문서는 「체력 1」이라 적었지만 기준 피해가 10이라 1이 안 나온다 —
  **라벨에 실제 값(10)을 적어라**

##### 결과 — 2026-08-04, **verify-read: 빚 셋 닫힘 · 단계 5 일곱 표적 명중 · 🔴 우회가 셋으로 늘었다** (뮤테이션 8회, 전부 워크트리)

**빚 ①감쇠**(라벨이 `다음 창은 200px뿐이다` 로 **두 창의 수를 찍어** 잦아듦이 눈에 보인다) ·
**②폭발 방향**(왼쪽·오른쪽 **양쪽**이 빨개진다. 반쪽 검사가 아니다) ·
**③낭떠러지**(`발판 위에 서 있다 (검사의 전제)` 를 같이 단언해 「원래 공중이었다」와 안 섞인다) — **셋 다 닫혔다.**
**세로 밀림 대조군도 진짜다** — 세로 성분을 지우니 라벨이 **`70px` vs `70px`** 로 붙어 버려 빨개진다.

**단계 5 일곱 다 표적 명중.** 🔴 **「발밑을 파서」 잰 배치가 판정 6의 「중력을 계속 받는다」를 실제로 잰다** —
중력을 멈추니 `쓰러진 채로 떨어진다 (y 384 → 384)` 와 착지 두 줄이 같이 터진다. **배치 변경 타당.**

**`downed` 게터 판정: 지금은 맞다.** `hp <= 0` 파생이라 「체력 0인데 걸어 다닌다」가 **구조적으로 불가능**하다.
⚠ 진짜 상태가 필요해지는 조건은 하나뿐이다 — **「쓰러졌는데 hp가 0이 아닐 수 있다」**(쓰러진 채 버티기·
부분 체력 부활). **둘 다 기획이 미정으로 미뤄 둔 것**이라 지금 파생이 옳고 **바뀌는 계기도 분명하다.**
⚠ 주석이 「파생값이다」까지고 **「언제 진짜 상태가 되나」는 안 적혀 있다.** 한 줄이면 된다.

🔴🔴 **큐 우회가 하나 → 셋으로 늘었는데 값을 적은 것은 하나뿐이다**

| 자리 | **잃는 것을 적었나** |
|---|---|
| `_fire_burns_through_invuln` | ✅ **모범적** — 「첫 사례」로 이름 붙이고 잃은 거동까지 적고 **「다음에 같은 우회를 할 때는 이 줄을 먼저 읽어라」**로 닫았다 |
| `_air_fall` | ❌ 이유만 — ⇒ **공중에서 반동(위)과 세로 밀림(아래)이 어떻게 합쳐지나**가 미검증 |
| `_beat_down` | ❌ 이유만 — 🔴 **판정 6이 통째로 우회 경로에 얹혀 있다.** 게임에서 자기를 쓰러뜨리는 것은 **반동이 붙은 자기 탄**인데 그 경로로는 한 번도 안 재진다 |

🔴 **첫 사례가 스스로 세운 규율을 같은 파일·같은 세션의 뒤 둘이 안 따랐다.**
⚠ **「우회 셋」이 되면 다음 사람은 그걸 기본값으로 읽는다.** 고칠 것은 아니지만 **두 줄을 더 적어야 한다.**

**늘 보는 셋 깨끗** — `KB_SETTLE := 120` 의 지수 감쇠 주석이 **숫자까지 정확히 그 자리에 있다**
(「계획은 30프레임이라 적었는데 실측으로 모자랐다(6px 흘렀다) … 초당 ×0.02면 2초 뒤 0.16px/s다」) ·
새 사면 없음 · 씬 형태 그대로 · **색인/`get` 새 자리 없음** ·
⚠ 부동소수 정확비교 둘은 **사슬이 한 칸 길어졌지만 아직 안전하다**(수직 발사는 `adx=0` → 고정소수점이라
`x1-x0` 가 **정확히 0**이고 `normalized().x` 도 정확히 0.0이다).

🔴 **그리고 기획 문서의 판정 6 뒤집기 값이 틀렸던 것을 찾아 고쳤다** — 「체력 1」이 아니라 **10**이다
(기준 피해가 10이라 1이 안 나온다). 위 「판정」 절에 박았다.

##### 결과 — 2026-08-04, **verify-run: 판정 6 통과 · 🔴 그런데 로켓점프가 죽었다**

🔴🔴 **세로 밀림을 열면서 「아래로 쏘면 뜬다」가 게임에서 거짓이 됐다.**
단계 4에서 잰 값은 **+15px 떠올랐다**였는데 같은 조작이 지금은:

```
시작 y=496 → 최고점 y=496 (+0px) · x 48 → 65 · hp 90
총구 tip = (54.6, 486.1)  ← 상자[48~64, 496~512] 의 **위쪽**이다
1프레임: 구간 (54,486)→(58,510) 이 상자를 관통 ⇒ 직격
         push_dir ≈ (0.16, 0.99) ⇒ vy += +394 · 반동 −200(위)을 이기고 **vy 0 → +215(아래로)**
그 뒤: 폭발이 발판을 지워 상자가 496→500→…→522 로 **가라앉는다**
```

⇒ **아래로 쏘면 뜨는 게 아니라 자기가 판 구멍으로 내려간다.**
🔴 **원인은 「반동이 약해서」가 아니라 탄이 총구(상자 위)에서 상자를 관통해 직격 400을 아래로 꽂기 때문**이다.
⚠ **기획 「마법 반동」이 「아래로 쏘면 위로 뜬다 — 그게 GDD의 로켓점프」라고 적었으니
지금 그 문장이 게임에서 참이 아니다.** 값 보고이지 결함 보고가 아니다 — **사용자 몫.**

**세로 밀림 세기 — 구현자 예측 66px이 아니라 51px이고 점프와 정확히 같다.**
순수 폭발만 맞게(탄이 상자를 안 지나게) 배치하니 `vy +20 → −360`(변화 −380), 도달 **51px = 점프와 1.00배**.
이유: 밀림 −400에 그 프레임 중력 +20이 얹혀 **−360**이 되는데 그게 `JUMP_VY_PX` 와 같은 값이다.

**세로 밀림이 실제로 열렸다** — 위에서 떨어지는 탄에 맞으면 **vy +340**(지난번엔 0), 아래에서 오면 **변화 −380**.
⚠ **절대값으로 재면 안 된다** — 낙하 중이면 −20이 나와 「안 밀렸다」로 읽힌다. **변화량으로 쟀다.**
🔴 **가로는 그대로다** — `입력 없음 +85 · 반대 +20 · 같은쪽 +150` 으로 **지난번과 똑같다.**

**판정 6**: 탄 직격만으로 100 → 0 ⇒ `downed = true` · 점프도 안 됨 ·
🔴 **hp가 0 밑으로 안 간다**(쓰러진 뒤 **12발을 더 쏴도** 0) · 즉사가 아니다(발밑을 파니 y 308 → 560) ·
쓰러진 채로도 밀린다 · **진짜 R 키로 부활**(hp 100 · 다시 걷는다).

🔴 **행동불능을 「x가 안 변한다」로는 못 잰다** — 맞고 나면 밀림이 남아 1초를 재워도 `kb_vx 7.5` 가 남아
2초에 2px 미끄러진다. ⇒ **입력만 바꿔 그 몫을 쟀다**:
```
쓰러진 채 2초 — 입력 넣음 +2px · 입력 없음 +2px      ⇒ 입력 몫 0
체력 10 에서  — 입력 넣음 +262px · 입력 없음 +2px    ⇒ 차이 +260 (=130×2초)
```

🔴🔴 **그리고 문서가 안 정한 것 하나 — 쓰러져도 쏠 수 있다.**
「발밑을 파서 떨어뜨렸다」 배치가 성립하는 이유가 그것이다(발사 +1 확인).
⇒ **행동불능은 「이동만」이다.** 문서에 없는 자리다.

**회귀**: A/B 270프레임 다름인데 **전부 캐릭터만, 시뮬/통지는 0프레임** · 진짜 씬은 달라진 줄이 **[9] 하나뿐**이고
🔴 **[7]·[7-b](걷기·점프 51px·착지·기둥 앞 막힘)는 diff에 아예 안 나온다** ⇒ **이동 코드 그대로.**
그물 **1268개**, HEAD 1038 대비 **줄어든 그물 없음**.

⚠ **못 쟀다**: 화면(verify-look 몫) · **불로 100 → 0**(나무가 타 내려가며 발밑이 비어 hp 78에서 멈췄다 —
**harness 한계**라 탄 열 방으로 몰았다. 불 지속 자체는 판정 2에서 이미 쟀다) ·
밀려 낭떠러지 · 밀림이 벽에 부딪히면 · 쓰러진 채로 불 위

##### 결과 — 2026-08-04, **verify-look: 실루엣 통과 · 🔴 구멍 하나**

🔴🔴 **쓰러진 채로 마법이 나간다.** `hp 0 · downed true` 에서 좌클릭하니 **`발사 10 → 11`** 이고
그 폭발로 **자기가 밀렸다**(`kb_vx -321` · 가로 84px · 위로 59px).
**`stage._fire_at` 이 `_circle.can_fire()`(룬이 있나)만 보고 `downed` 를 안 본다.**
⚠ 그런데 **`character_view` 는 쓰러지면 지팡이도 총구도 안 그린다** ⇒
**「지팡이가 없는 시체에서 마법이 나가고, 그 반동으로 시체가 날아다닌다」**가 지금 화면이다.
🔴 **「행동불능」의 정의와 정면으로 어긋난다** — 이동은 막혔는데(`character.step` 은 제대로 막는다) 발사가 안 막혔다.

**쓰러진 실루엣 — 확실히 갈린다. 통과.**
서 있을 때 `16×16 밝은 흰-푸른 사각형 + 지팡이 + 총구 구슬` ↔ 쓰러지면 `16×6 납작하게 누운 탁한 회보라 막대,
지팡이·총구 없음`. 🔴 **키가 1/2.5로 줄고 지팡이가 사라진다 — 모양 축이라 무적 흐림·불 테두리와 안 겹친다.**
⚠ 원본 크기에서는 작지만 **HUD가 받쳐 준다**(`체력 0 / 100   쓰러짐 — R로 다시` 가 또렷하다).
⚠ **쓰러진 색이 돌 지형색과 명도가 가깝다** — 지금은 어두운 배경을 등져 읽히지만 **돌 발판 위에 누우면 대비가 준다**(안 봤다).

**나머지 전부 확인** — 쓰러진 채 떨어진다(y 480 → 496) · 쓰러진 채 밀린다 · 행동불능(D·Space에 x 불변) ·
hp가 0 아래로 안 간다 · R로 일어난다(HUD·지형·위치 복구).

🔴 **세로 밀림 손맛 — 「로켓점프」가 아니라 「폭발에 날아간다」다.**
```
push_dir = (-0.857, -0.514)   ← 대각선이다. 「위로」가 아니다
위로 59px + 가로 84px + 체력 -10 + 발판에 큰 구멍이 한 클릭에 동시에
```
| | 상승 |
|---|---|
| 반동만 | 15px |
| 점프 | 51px |
| **발밑 폭발(세로 밀림)** | **59px — 점프보다 높다** |
| 점프 + 아래로 쏘기 | 96~109px |

🔴 **가는 방향을 못 정한다** — 폭발이 내 몸 기준 **어디서** 터지느냐가 정하는데 그건 총구 위치(마우스 방향)에
달려 있어 **조준으로 직접 못 고른다.** ⇒ **높이는 쓸모 있는데 통제감이 없다.**
「탁 튄다」는 시원한데 **「거기로 가려고 했다」가 안 된다.**

⚠ **다음 검증자를 위한 실측 둘:**
1. 🔴 **자기 탄에 「직격」당하는 경우가 실제로 있다** — 총구가 몸 밖 18px에 있어서
   **몸 쪽으로 되쏘면 자기 탄이 자기 상자를 지난다.** 앞 라운드들이 「직격은 혼자서 못 만든다」고 적은 것은 **틀렸다**
2. ⚠ **`place(400, 496)` 은 나무 블록 *안*이다** — 단계 4의 밀림 변위 29px은 **지형에 막힌 과소평가**다.
   빈 자리는 `x ≈ 48~110`

---

### ✅ 단계 5 판정 — **통과. 판정 6까지 일곱 개가 전부 닫혔다**

| 검증 | 결과 |
|---|---|
| **verify-run** | 판정 6 통과 — 열 방에 `downed` · **hp가 0 밑으로 안 간다**(12발 더 쏴도) · 발밑을 파니 떨어진다 · 진짜 R 키로 부활 |
| **verify-read** | 빚 넷 닫힘 + 일곱 표적 명중. `downed` 게터 판정 「지금은 맞다」 |
| **verify-look** | **쓰러진 실루엣이 확실히 갈린다**(`16×16 + 지팡이` ↔ `16×6 납작한 막대, 지팡이 없음`) · HUD가 받쳐 준다 |

**그물 1222 → 1268개.**

🔴 **행동불능을 「x가 안 변한다」로는 못 잰다** — 밀림이 남아 2초에 2px 미끄러진다.
**입력만 바꿔 그 몫을 재야 한다**(쓰러진 채 입력 몫 **0** vs 체력 10에서 **+260**).

🔴 **사용자 판단 둘 (2026-08-04):**

1. **쓰러진 채 발사를 막는다.** 문서가 「**행동불능**」이라고 적었고 이동은 이미 막혀 있다.
   ⚠ **`stage._fire_at` 이 `can_fire()`(룬이 있나)만 보고 `downed` 를 안 본다** — 한 줄이면 된다.
   ⚠ **그물 둘(`_air_fall`·`_beat_down`)이 그 경로를 쓰므로 같이 고쳐야 한다**
2. 🔴🔴 **로켓점프는 보류다. 지금 안 고친다.**
   **땅에 서서 발밑을 쏘면 안 뜨고 내려간다**(탄이 총구 → 몸을 관통하며 직격 400을 아래로 꽂아
   반동 200을 이긴다). **공중에서는 96~109px로 잘 뜬다.**
   ⇒ **사용자 말: 「일단 이대로 두고 나중에 수정해야 할 듯. 그… 폭발 바람에 날아가야 할 것 같긴 한데.」**
   🔴 **방향성이 나왔다 — 「직격이 민다」가 아니라 「폭발의 바람에 날아간다」 쪽으로 다시 볼 자리다.**
   ⚠ **그때까지 기획 「마법 반동」의 「아래로 쏘면 위로 뜬다」는 게임에서 참이 아니다.**

⚠ **단계 5가 남긴 것 둘**: **`downed` 주석에 「언제 진짜 상태가 되나」가 없다**(조건은 하나 —
「쓰러졌는데 hp가 0이 아닐 수 있다」) · 🔴 **큐 우회 둘(`_air_fall`·`_beat_down`)이 값을 안 적었다**
(첫 사례가 세운 「이 줄을 먼저 읽어라」 규율을 뒤 둘이 안 따랐다).

#### 단계 6 — 조립창을 줄인다 (사용자 답 1)

| 파일 | 무엇 |
|---|---|
| `src/view/fx_tuning.gd` | `WINDOW_RECT` 축소 + **유예 문구 삭제** |
| `tests/nets/net_render.gd` | 🔴 90% 단언 둘을 **캐릭터·체력이 안 가리나**로 바꾼다 |
| `tests/nets/net_circle.gd` | 실측 표에 줄 하나(주석) |

§7이 값과 근거를 든다.

### 3. 건드릴 파일 — 한 줄씩

| 파일 | 왜 | 단계 |
|---|---|---|
| `src/actor/world_step.gd` | **새 파일.** 틱 순서를 껍데기 밖으로 (문서 553줄) | 0 |
| `src/stage/stage.gd` | 순서·큐·계수기를 넘기고 뷰 통지만 남긴다 · 체력 라벨 | 0·2·5 |
| `src/sim/sim_tuning.gd` | 무속성 룬 (표 두 줄 + 상수 둘) | 1 |
| `src/sim/spell_sim.gd` | `TRACE_NONE` 갈래 · **이번 틱 구간 통지** | 1·2 |
| `src/actor/character.gd` | 체력·무적·직격/폭발 판정·불 지속·밀림·반동·쓰러짐 | 2~5 |
| `src/view/fx_tuning.gd` | 무속성 색 · 무적/불/쓰러짐 연출 · `WINDOW_RECT` | 1~6 |
| `src/view/character_view.gd` | 깜빡임 · 불 · 눕힌 실루엣 | 2·3·5 |
| `src/stage/stage.tscn` | `HUD/Health` (🔴 `mouse_filter = 2`) | 2 |
| `tests/nets/net_damage.gd` | **새 그물.** 판정 1~6 + 「껍데기가 순서를 안 든다」 | 0~5 |
| `tests/nets/net_spell.gd` | `_every_rune_traces` 라벨 교정 (판정 7) | 1 |
| `tests/nets/net_render.gd` | 90% 단언 교체 | 6 |
| `tests/nets/net_circle.gd` | 실측 주석 한 줄 | 6 |

🔴 **`net_tables` · `net_layers` 는 안 건드린다** — 룬 표도 새 파일도 **저절로** 걸린다
(`_defs_and_all_agree` 는 집합 비교, `net_layers` 는 폴더 재귀 스캔).

### 4. 가정 — 🔴 사용자가 뒤집을 자리

문서가 **미정**으로 둔 것 중 **숫자가 없으면 코드를 못 쓰는 것**들이다.
「미정을 구현 전에 다 채우라고 요구하지 않는다」(CLAUDE.md)에 따라 **가정으로 박고 나아가되,
전부 한 곳(상수)에 모아 두어 뒤집기가 한 줄이 되게 한다.**

| 무엇 | 값 | 근거 |
|---|---|---|
| 최대 체력 | **100** | 문서 확정 |
| 무적 | **0.2초 = 4틱** | 문서 확정 |
| 직격 피해 | **10** | 문서 확정(기준 피해) |
| 폭발 피해 | **10** — 직격과 같다 | ⚠ **가정.** 문서가 정한 것은 기준 하나뿐이고, 축을 안 늘리는 것이 뼈 먼저다 |
| 불 지속 | **초당 10** | ⚠ **가정.** 「불 위 1초 = 탄 한 대」로 읽힌다 |
| ~~밀림 세기~~ **400 px/s** | ❌ **삭제됨**(2026-08-04). 가정이었고, 그 가정을 쓸 코드가 없어졌다 |
| ~~밀림 감쇠~~ **초당 ×0.02** | ❌ **삭제됨**. ⚠ 감쇠 자체는 `recoil` 이 계속 쓴다 |
| 반동 세기 | **200 px/s** | ⚠ **가정.** 가로 총 변위 51px · 세로 도달 16.7px(점프의 1/3) — 「공짜가 아니다」 |
| 세대별 피해량 | **안 넣는다** | 🔴 세대 1의 값이 **미정**이다. ⇒ `SIM_SIZES` 다섯째 열도 `net_tables` 열 목록도 **안 건드린다** |
| 무적 표현 | **깜빡임** | 문서 「깜빡임인지 별도 그림인지 미정」 — 스프라이트가 없어 별도 그림을 만들 수 없다 |
| ~~쓰러진 채로 밀리나~~ | ❌ **물음이 사라졌다**(넉백 삭제) |
| 쓰러진 채로 더 맞나 | **hp가 0에서 안 내려간다** | 피해 처리는 계속 돈다(밀림은 난다) |
| 혼자일 때 부활 | **R(무대 리셋)** | 🔴 동료가 없다. `place()` 가 체력·무적·밀림을 같이 되돌린다 |
| ~~밀림이 벽에 부딪히면~~ | ❌ **물음이 사라졌다.** 끝내 못 쟀고, 이제 잴 것이 없다 |

### 5. 조립창 — 값과 근거

```
WINDOW_RECT : Rect2(48, 27, 864, 486)  →  Rect2(48, 12, 864, 372)
```

🔴 **가로는 그대로 두고 세로만 줄여 위로 붙인다.** 이유 셋:

1. **아래가 통째로 열린다.** 창 아래 끝이 **384**px — 캐릭터가 스폰 자리에서 서 있는 높이가
   480이고 **점프 정점이 426**(= `JUMP_VY_PX²/(2·GRAVITY_PX)` = 54px 위)이라,
   🔴 **걷는 동안에도 뛰는 동안에도 안 가린다.** 그리고 그 띠가 화면 **폭 전체**라
   캐릭터가 어디로 걸어가든 보인다
2. **사용자 판정 「화면 90%」가 가로에서 살아남는다.** 양축을 줄이면 그 판정이 통째로 사라진다
3. **책 펼침의 모양이 안 망가진다.** 페이지는 가로로 자라고 팔레트는 세로로 자란다

**계산으로 확인한 것**(`net_circle.CIRCLE_DIAMETER_FLOOR_PX = 180` 이 먼저 빨개지는 자리다):

```
페이지 = ((864-34)/2) × (372-46) = 415 × 326
마법진 자리 = 387 × 298      (CIRCLE_AREA_PAD_PX 14)
진 지름 = min(387,298) × 0.94 = 280.1px  ≥ 180   ✓ 여유 100px
```

⚠ **창 크기를 바꾸면 이 계산을 다시 해라.** 지름이 180 아래로 내려가면 `net_circle` 이 먼저 짖고,
그 상수의 근거는 「단계 3에서 눈이 199px를 통과시켰다」다 — **낮추기 전에 눈으로 다시 봐야 한다.**

**체력 라벨의 자리**(`stage.tscn` 의 `HUD/Health`):

```
offset (660, 494) ~ (952, 530)     mouse_filter = 2
```

- 창(아래 끝 **384**)과 안 겹친다 ⇒ **조립 중에도 보인다**
- 캐릭터 스폰 자리(48~64)와 **화면 반대쪽**이라 서로 안 가린다
- ⚠ 캐릭터가 오른쪽 끝까지 걸어가면 글자와 겹친다. 배경이 없어 **가려지지 않고 섞일 뿐**이고,
  이건 verify-look이 판정할 자리다

**`net_render` 를 이렇게 바꾼다** — 🔴 `WINDOW_SCREEN_FRAC` 과 그 두 단언을 **지운다.**

```
옛것: 창 가로/세로가 화면의 90%다        ← 크기를 쟀는데 기준은 크기가 아니었다
새것: ① 창이 캐릭터가 **걷고 뛰는 자리**를 안 덮는다
        자리 = 스폰 상자를 점프 도달 높이만큼 위로 늘린 사각형
        🔴 Stage.SPAWN_TILE · Character.JUMP_VY_PX · GRAVITY_PX 에서 **파생**한다.
           숫자를 박으면 스폰이나 점프를 바꾸는 날 조용히 낡는다
     ② 창이 `HUD/Health` 를 안 덮는다
     ③ `HUD/Health` 가 켜진 채로 시작한다
```

⚠ **②에 함정이 하나 있다** — 트리 밖 `Control` 의 `size` 가 0으로 나오면
`intersects` 가 늘 거짓이라 **검사가 조용히 통과한다.**
🔴 **`size.x > 0` 을 먼저 단언해라**(폴더 스캔 그물의 첫 줄과 같은 장치).

⚠ **`Fx.WINDOW_BG.a == 1.0` 단언은 남긴다.** 창이 여전히 `HUD/Stats`(8,8~900,210)를 덮고,
근거(「불투명해서 글씨가 안 섞인다」)가 그대로 참이다.
⚠ **`_toggle_assembly` 의 HUD 숨김도 남긴다** — 창 왼쪽 밖 40px 띠에 `Stats` 글자 앞부분이
여전히 남는다. 🔴 **체력은 `HUD/Stats` 가 아니라 `HUD/Health` 에 있으므로 안 숨는다.**

🔴 **「보인다」의 최종 판정은 눈이 한다.** 그물은 「안 겹친다」까지고, verify-look이
**창을 연 채로** 캐릭터와 체력이 보이는 스크린샷을 찍어 이 문서 「판정」 절에 적는다.

### 6. 위험 — 이 변경이 조용히 깨뜨릴 수 있는 것

🔴🔴 **1. 폭발 통지를 60Hz에서 읽으면 한 대가 세 대가 된다.**
통지는 `step()` 시작에서만 지워지므로 틱 사이 세 프레임 동안 같은 값이 남아 있다.
⚠ **무적이 두 대를 먹어서 화면에서는 정상으로 보이고**, 무적과 무관한 불 지속에서만 3배가 드러난다.
⇒ `on_tick` 은 `world_step.frame()` 의 **틱 갈래 안에서만** 불린다. 그 자리가 유일한 입구다.

🔴🔴 **2. 구간 통지 끝점을 `x1` 로 두면 벽 너머를 때린다.** §2 단계 2 참조.

🔴🔴 **3. 사각형 대 사각형으로 짜면 40px 도약이 16px 상자를 넘는다.**
⚠ **한 프레임이라 눈으로 절대 못 본다** — 사용자에게는 「가끔 안 맞는다」로만 보인다.
⇒ 판정 1의 뒤집기 ②가 유일한 감지기고, **그 검사 자신이 「도약 배치인가」를 단언해야** 한다.

🔴 **4. 무적이 지속 피해를 먹으면 불이 무해해진다.** 문서 「동작」의 핵심 갈래다.
⇒ 불 경로는 `_invuln_left` 를 **읽지도 쓰지도 않는다.**

🔴 **5. 불 검사에서 발밑 한 줄을 빠뜨리면 「불 위에 서 있는데 안 아프다」가 된다.** §2 단계 3 참조.

🔴 **6. `character.gd` 에 `Input.` 을 한 줄 쓰면 그물은 초록이고 서버만 죽는다**(그 파일 주석).
⇒ 밀림·쓰러짐이 입력을 **직접 읽지 않는다.** `step()` 인자로만 받는다.

🔴 **7. `world_step.gd` 가 `src/view/`·`src/stage/` 를 참조하면 `net_layers` 가 짖는다.**
⇒ 뷰 통지는 껍데기가 `frame()` 의 반환값을 보고 친다.

🔴 **8. `spell_sim.gd` 에 float·`Vector2`·실수 리터럴이 한 줄이라도 들어가면
`net_determinism` 이 짖는다.** 구간 통지는 **정수 배열만**이다.
⚠ 좌표 변환(px)은 **`src/actor/` 쪽에서만** 한다.

🔴 **9. `HUD/Health` 에 `mouse_filter = 2` 를 안 적으면 좌클릭이 통째로 죽는다.**
⚠ 다행히 `net_render._mouse_filter_contract` 가 짖는다 — **조용하지 않다.**

🔴 **10. `stage.gd` 의 `_phase` 를 옮기면 렌더 보간이 끊긴다.**
`_process` 의 `float(_phase)/TICK_DIVIDER` 가 `_world.phase()` 를 읽어야 한다.
⚠ 안 고치면 투사체가 틱당 40px씩 순간이동하고 **에러는 안 난다.**

🔴 **11. `place()` 가 체력을 안 되돌리면 R로 부활이 안 된다** — 쓰러진 채로 영영 남는다.

🔴 **12. HP를 float로 두면 HUD도 판정도 흐려진다.** 정수 HP + 누산기.

🔴 **13. 무속성을 `ELEM_ALL` 에 넣는 순간 기존 그물 둘이 즉시 빨개진다**(문서가 적은 것).
⇒ **단계 1 안에서 같이 고친다.** 다른 단계로 미루면 그 사이 전 그물이 빨갛다.

🔴 **14. 창을 줄이면 `net_circle.CIRCLE_DIAMETER_FLOOR_PX` 가 먼저 빨개질 수 있다.**
§5가 계산을 해 뒀다(280.1 ≥ 180). ⚠ **원인이 그 상수가 아니라는 걸 알고 만나라.**

⚠ **15. 「이 리포의 대표 가짜」** — 화면만 바뀌고 시뮬은 안 바뀌기(또는 그 반대).
⇒ **단계마다 시뮬과 화면을 같이 낸다.** 그래서 연출을 마지막에 몰지 않았다.

### 7. 판정 ↔ 단계

| 판정 | 단계 | 뒤집어 보기 |
|---|---|---|
| **1** 내 마법에 내가 맞나 | 2 | 반경 밖 100 유지 · 🔴 **터널링 반증**(구간 양 끝이 상자 밖인 것을 같이 단언) |
| **2** 불 위에서 깎이나 | 3 | 불이 꺼지면 멈춘다 · 🔴 **무적을 켠 상태에서** 재야 뜻이 있다 |
| **3-①** 같은 틱 폭발 둘 = 한 대 | 2 | 무적 0이면 두 대 |
| **3-②** 무적이 4틱인가 | 2 | 3틱 = 한 대 · 5틱 = 두 대 |
| **3-③** 확산 8발에 몇 대 | 2 | 🔴 **기록이다. 목표 숫자를 단언하지 마라** — 값을 이 문서에 적는다 |
| ~~**4** 맞으면 밀리나~~ | 4 | ❌ **철회**(넉백 삭제, 2026-08-04). 통과했었다 |
| **5** 쏘면 반동으로 밀리나 | 4 | **거부된 발사**는 반동이 없다 (빈 룬 경로는 verify-run) |
| **6** 100→0 이면 쓰러지나 | 5 | 중력은 받는다 · 입력해도 x 불변 · 체력 10에서는 x가 변한다 |
| **7** 무속성과 불이 다르나 | 1 | 🔴 fire()가 참 + 탄이 났고 착탄까지 살았다 + **격자는 0 변화** |
| **화면** | 2·3·5·6 | verify-look — 체력 숫자 · 깜빡임 · 불 · 눕힌 실루엣 · **창을 연 채로 캐릭터와 체력** |

🔴 **판정 3-③과 조립창은 결과를 이 문서에 적어야 끝난다**(CLAUDE.md 「눈으로 본 판정은 그 자리에」).

### 8. 범위 밖 — 이번에 안 하는 것

- 🔴 **아군 절반 · 탄의 소유자 필드 · 「누구 탄에 맞았는지」** — 사용자 답 3
- 🔴 **`SIM_SIZES` 의 피해량 열과 `net_tables` 의 열 이름 리스트** — 세대 1 값이 미정이다.
  ⚠ **`sim_tuning.gd` 의 라벨 주석은 이미 고쳐져 있다**(§0) — 또 건드리지 마라
- **수치 합치는 식**(룬·문양·장비) — 축이 아직 하나라 합칠 것이 없다
- **낙하 피해 · 격자 밖으로 밀려나면** — 문서 「나중에 다시 열 것」이 이미 「둘 중 하나는 채워야
  한다」고 적었다. 🔴 **이번 단계는 안 채운다** ⇒ 지금 명세대로는 밀려 떨어져도 안 죽는다
- **동료가 일으켜 준다 · 쓰러진 채 버티는 시간 · 일으켜지면 체력** — 혼자다
- **체력 회복 수단** — 통째로 비어 있다. R(리셋)만 있다
- **대시 · 이단점프 · 벽타기** — 사용자가 「나중에」로 정했다
- **물 · 젖음 · 감전** — 물 시뮬이 오는 날
- **캐릭터 그림** — `character-sprite` 가 맡는다. 🔴 이번 단계의 그림은 전부
  **사각형 · 색 · 깜빡임**이고, 스프라이트가 오면 그 자리가 통째로 열린다
- **껍데기의 커맨드 큐가 「어느 틱에」를 다는 것** — `world_step` 으로 옮기되 **로직은 그대로**다.
  ⚠ `keep` 갈래는 여전히 죽은 분기다(멀티 이음매)
