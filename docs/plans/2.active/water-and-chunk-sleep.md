# 물과 청크 재우기 — 흐르고 고이고, 그리고 멈춘다

**상태**: active (2026-08-07부터)
**한 줄**: 셀마다 물의 양을 두고, 평형에 도달하면 그 조각이 잠들어 CPU를 0 쓴다.

🔴🔴 **개념·판단·사연은 `docs/design/물.md` 가 기준이다. 여기 겹쳐 적지 않는다.**
이 문서는 **무엇을 어떤 순서로 얼마만큼 만드나**만 담는다.

⚠ **읽는 순서**: 물을 처음 여는 사람은 `docs/design/물.md` 의 「v1이 죽은 자리」를 먼저 읽어라.
이 문서의 판정 1이 왜 첫 번째인지가 거기 있다.

---

## 왜

GDD 「만드는 순서」의 **1번**이다 — 「물 + 맵」. 사용자가 2026-08-06에 정했다.

그리고 물은 혼자 오지 않는다. **청크 재우기가 같이 온다.**
`src/sim/cell_grid.gd` 가 스스로 그렇게 적어 뒀다:

> ⚠ **청크 재우기가 없다.** 이 단계에는 움직이는 셀이 0개라 잴 대상이 없고,
> 불은 파면 목록이라 청크와 독립이다. **물이 들어올 때 만든다.**

🔴 **그리고 그건 선택이 아니다** — 격자를 GDScript로 한 번 훑는 데 **62,676μs**,
20Hz 틱 예산이 **50,000μs**다(2026-08-07 실측). 청크 없이는 물이 **원리적으로** 안 돈다.

---

## 동작

### ① 청크 재우기 — 물보다 먼저 선다

```
청크 크기   16 × 16 셀
청크 격자   256 × 63 = 16,128개
```

🔴 **16이 유일하게 깔끔하게 나눠떨어진다** — `W=4096 / 16 = 256`, `H=1008 / 16 = 63`.
32나 64는 H에서 남는다(31.5 · 15.75). ⚠ **v1의 값도 16이었다**(`cell_grid.gd` 머리의
「밴드당 플래그 검사 16 → 256회」가 그 흔적이다).

**청크마다 두 비트**:

| 무엇 | 뜻 |
|---|---|
| `awake` | **이번 틱에 돈다** |
| `dirty` | 이번 틱에 뭔가 움직였다 ⇒ **다음 틱에도 돈다** |

```
틱 시작 → awake = dirty, dirty 를 비운다   ← 🔴 flip 은 여기다. 틱 끝이 아니다
        → awake 인 청크만 순회
        → 뭔가 움직이면 그 청크와 「움직여 들어간 쪽」 청크를 dirty
```

🔴🔴 **flip이 틱 시작인 이유** — 폭발은 `world_step` 순서상 `grid.step()` **뒤에** 오는
`spell.step()` 안에서 격자를 부른다. flip이 틱 끝에 있으면 그 폭발이 **갓 비워진 dirty** 를 찍고,
그게 awake 로 올라오는 데 **한 틱이 더 든다.**

> ⚠ **2026-08-07, verify-read가 이 문단의 원래 표현을 반증했다.**
> 여기와 「위험」 표가 「끝에 두면 dirty 가 **그 자리에서 지워진다**」고 적었는데,
> **flip은 「비우기」가 아니라 「넘겨주기」라 어떤 배치에서도 안 지워진다 — 한 틱 늦을 뿐이다.**
> 🔴 **틀린 채로 두면 다음 사람이 flip을 끝으로 옮겨 보고 「안 지워지네, 이 주석이 틀렸네」로
> 주석 쪽을 지운다.** 결론(시작이 맞다)은 같고 이유만 바뀌었다.

🔴🔴 **경계를 넘는 이동은 이웃 청크도 깨운다.** 안 깨우면 물이 청크 경계에서 멈춰 서고,
그건 **화면에 「보이지 않는 벽」으로만 드러난다** — 에러가 안 난다.

🔴 **격자를 바꾸는 모든 문이 청크를 깨워야 한다** — `_write_cell` · `_disc` · `_fill_rect`.
⚠ 안 깨우면 **폭발로 뚫은 그릇에서 물이 안 쏟아진다.** 그 구멍은 화면에 보이므로
「물이 고장 났다」로 보고되고, 원인은 폭발 쪽에 있다.

### ② 물의 저장

| 무엇 | 어디 | 범위 |
|---|---|---|
| 물이라는 재료 | `cell_materials.DEFS` 에 `WATER` 한 줄 | 팔레트 슬롯 1개 |
| 물의 양 | `_aux` | 0~255 |
| 얕은가 | `_flag` 비트 하나 | — |

🔴🔴 **물은 `_write_cell` 을 쓰지 않는다. 새 문이 필요하다.**
`_write_cell` 은 `_flag` 와 `_aux` 를 **같이 0으로 만든다** — 물이 그걸 지나면
**양이 그 자리에서 날아간다.** 에러는 하나도 안 난다.

⇒ `_write_water(i, amount)` 를 따로 만든다. 하는 일:
- 양이 0이면 재료를 `EMPTY` 로, 아니면 `WATER` 로
- `_aux` 에 양을 쓴다
- 임계 비교로 얕음 비트를 갱신한다
- `_changed` 를 센다 (안 세면 화면이 안 올라간다)
- **청크를 dirty 로 찍는다**

⚠ **`_write_cell` 이 물 칸을 덮을 때는 여전히 `_write_cell` 이 맞다** — 폭발이 물을 지우는 것은
양을 0으로 만드는 게 아니라 **칸을 통째로 비우는 것**이다. 두 문의 경계를 여기서 못 박는다.

### ③ 한 틱의 물

**순회 순서는 `src/sim/cell_grid.gd` 머리의 계약 그대로다.** 새로 정하지 않는다:

```
밴드(청크 행)를  아래 → 위
밴드 안의 행도   아래 → 위
행 안의 열은     선호 방향 dir 의 반대
밴드 안에서는    행 → 청크 순서 (청크를 바깥에 두면 대각 이동이 청크 경계에서 깨진다)
dir 은 매 틱 뒤집는다 (좌우 편향 제거)
```

> 🔴🔴 **2026-08-07 — 이 계약은 「세로」에만 성립한다. 가로에는 성립하지 않고 성립시킬 수도 없다.**
> builder가 단계 3에서 부딪혀 확인했다.
>
> 열을 dir 반대로 돌면 `x+dir` 은 이미 지났지만 **`x-dir` 은 아직 안 지났다** ⇒
> **가로는 한 틱에 여러 칸 번진다**(255 → 127 → 63 … 로 잦아든다).
>
> 🔴 **버그가 아니라 확산의 성질이다.** 단일 버퍼에서 **어느 순서로 돌든 한쪽으로는 연쇄가 난다.**
> 한 방향만 나누게 막으면 연쇄는 사라지지만 **평형 속도가 절반이 되고 위 ③의 실측 틱 수가
> 통째로 안 맞는다.** ⇒ **그래서 dir 이 매 틱 뒤집힌다** — 연쇄의 방향이 번갈아 서로를 상쇄한다.
>
> ⚠ **세로는 여전히 한 틱 한 칸이고 그물이 그걸 잰다.** 「한 틱에 한 칸」을 가로에도 기대하지 마라.

**칸 하나가 하는 일**, 이 순서로:

```
1. 아래로 넘긴다      아래 칸이 받을 수 있는 만큼
2. 남으면 좌우로 나눈다  양쪽 이웃과 평균에 가까워지게
3. 양이 임계 이하면 안 움직인다   ← 🔴 이것이 「젖음」이고 「멈춤」이다
```

🔴🔴 **3번이 이 기능의 심장이다.** 임계가 없으면 양이 1인 물이 영원히 좌우로 미세 이동하고,
그러면 **청크가 절대 안 잠들어 ①이 통째로 무효가 된다.** v1이 죽은 자리의 재발이다.

> 🔴🔴 **2026-08-07, spec이 뒤집어 재서 이 문단이 틀렸다는 것이 나왔다.**
> **양 임계(젖음)를 0 → 8 → 32 → 64로 바꿔도 이동 셀 수가 안 달라진다.** 멈춤을 정하는 것은
> 바로 아래 「차이가 임계 이하면 안 옮긴다」(`WATER_MIN_DIFF`)이고, 양 임계는 **화면 값**이다.
> ⇒ **둘은 다른 손잡이다.** 실측과 표는 「구현 계획」의 ②·③. **그대로 구현하면 없는 손잡이를
> 돌리며 왜 안 멈추나를 찾게 된다.**

⚠ **좌우로 나눌 때 「완전히 같아질 때까지」 옮기지 마라** — 홀수는 영원히 안 나눠지고
1씩 왕복한다. **차이가 임계 이하면 안 옮긴다**가 계약이다.

### ④ 안전망 — 한 틱 청크 상한

깨어 있는 청크가 상한을 넘으면 **나머지는 다음 틱으로 민다.**

- 화면엔 **「물이 잠깐 천천히 흐른다」**로 보인다
- 🔴 **물이 멈추거나 사라지지 않는다.** 총량은 그대로다
- ⚠ **미는 순서가 결정적이어야 한다**(멀티) — 청크 인덱스 순으로 자른다

🔴 **`MAX_BURNING` 과 같은 어법이다 — 안전망이지 조절 손잡이가 아니다.**
여기 걸리는 게 자주 보이면 값을 올리지 말고 **레벨에 물을 얼마나 뒀나**를 봐라.

⚠ **상한값은 미정이다.** 청크 하나 도는 실비를 재고 정한다 — 추정으로는 예산의 절반에
**500~1,200개**이고 화면 한 판이 506청크(480×270셀)다. **재기 전에 박지 마라.**

---

## 만드는 순서 — 뼈 먼저

🔴 **각 단계가 끝날 때마다 판정 가능해야 한다.** 다 만들고 한 번에 보지 않는다.

| 단계 | 무엇 | 이때 판정 가능한 것 |
|---|---|---|
| **1** | 청크 재우기만. **물 없이** | 폭발이 청크를 깨우나 · 아무도 안 움직이면 다음 틱에 잠드나 (활성 청크 수를 값으로) |
| **2** | 물 재료 + **아래로만** 떨어진다 | 물이 바닥에 쌓이나 · 총량이 보존되나 |
| **3** | 좌우 나눔 | 🔴 **평평하게 눕나 · 좁은 물은 0이 되나**(판정 1-a) **· 넓은 물은 잠기나**(1-b) |
| **4** | 젖음 임계 + 화면 두 가지 | 얕은 물이 화면에서 갈리나 |
| **5** | 물 룬이 물을 만든다 | 착탄에 물이 생기나 |
| **6** | 불과 물 | 물이 불을 끄나 |

⚠ **단계 1이 물 없이 판정 가능한 것이 요점이다.** 청크가 잘못 서면 물도 같이 틀리고,
그때는 **어느 쪽이 원인인지 못 가른다.**

---

## 화면

**물은 두 가지로 보인다** — 물과 얕은 물. 세부와 근거는 `docs/design/물.md` 「화면」.

- 재료 슬롯 **1개**만 쓴다. 깊이는 `_flag` 비트
- 셰이더는 `_flag` 텍스처를 **이미 읽는다**(불이 그 축) ⇒ **텍스처 추가 0**
- 색은 `cell_materials.DEFS` 의 `rgb` 한 줄에서 나온다. 🔴 셰이더에 색을 박지 마라

⚠ **깊이에 따라 색이 연속으로 변하는 것은 이번 범위가 아니다** — 세 번째 텍스처가 필요하고
업로드가 7.88MB/틱 → 11.8MB/틱이 된다. **물이 굴러가는 걸 본 다음에 연다.**

---

## 경계

**안 하는 것**:

- **번개** — 룬 자체가 없다. `TRACE_WET` 도 그때 는다
- **젖은 나무** — 사용자가 2026-08-07에 「나중 문제」로 미뤘다
- **캐릭터가 젖는 것** — 셀의 젖음과 별개 축이다. 지속 시간도 효과도 미정
- **시간이 지나 마르는 것** — `docs/design/물.md` 「비용」이 왜 비싼지 든다
- **증기** — 물+불 융합. 융합진이 없다
- **물의 압력·분수** — 아래에서 위로 솟는 것. 양 기반이면 가능하지만 이번엔 안 한다

🔴🔴 **그리고 「스테이지1 맵에 물을 어디 두나」는 이 문서가 아니다.**
지형 문서가 따로 선다 — GDD 순서 1번의 「물 + 맵」에서 **맵 쪽 절반**이다.
⚠ 안 갈라 두면 「물이 다 됐는데 게임에 물이 하나도 없다」가 **고장으로 보고된다.**

---

## 기존 것과의 상호작용

| 무엇 | 어떻게 걸리나 |
|---|---|
| **`_write_cell`** | 🔴 **물이 지나면 양이 날아간다.** 위 ②의 새 문이 그래서 있다 |
| **`_disc`(폭발·파기)** | 🔴 **청크를 깨워야 한다.** 안 깨우면 뚫은 그릇에서 물이 안 샌다 |
| **불의 파면 목록** | ⚠ **독립이다.** 파면은 청크와 무관하게 돈다 — 그게 원래 설계다. 건드리지 마라 |
| **`is_solid()`** | 물은 **고체가 아니다**(`BEHAVIOR_NONE`). 캐릭터도 탄도 물을 통과한다 |
| **`count_material()`** | ⚠ **지금 이미 새고 있다** — HUD가 프레임마다 2회 부르고 한 번에 588μs다(CPU의 7%). 물과 별개로 손봐야 한다 |
| **팔레트 16슬롯** | 물까지 **5개**. 여유 11개 |
| **`net_determinism`** | 🔴 물 코드는 `src/sim/` 이라 **float·`sqrt`·`randi` 가 하나도 없어야 한다.** 평균·나눗셈 전부 정수 |
| **멀티** | 물은 **각자 계산한다**(GDD). 커맨드만 같으면 같은 물이 나와야 한다 ⇒ 결정론이 곧 계약이다 |

---

## 비용

**실측은 `docs/design/물.md` 「비용」에 있다. 여기는 이번 구현의 예산만 적는다.**

| 항목 | 값 |
|---|---|
| 청크 상태 배열 | 16,128바이트 × 2 (awake · dirty). 무시 가능 |
| 잠든 격자 | **0μs.** 이게 전부의 이유다 → 🔴 **그냥은 안 나온다. 아래 상자를 봐라** |
| 청크 하나 도는 비용 | ~~안 쟀다~~ → **쟀다: 흐름 41μs · 평형 60μs · 물 없음 12.5μs** |
| 한 틱 상한 | ~~미정~~ → **512로 정했다**(「구현 계획」의 근거) |

🔴 **「청크 하나 20~40μs」는 추정이지 실측이 아니다.** 구현이 그 값을 **처음 재는 사람**이고,
**잰 값은 그 자리 주석에 남긴다**(CLAUDE.md 「실측은 그 자리에 남긴다」).

> 🔴🔴 **2026-08-07, spec이 쟀다. 「잠든 격자 0μs」는 조건부다.**
> 계약대로 「행 → 청크」로 돌면 **밴드당 플래그 검사가 `256 × 16 = 4,096`회**이고, 전부 잠들어도
> **6,016μs/틱 = 예산의 12%** 가 나온다. ⚠ `cell_grid.gd` 머리의 「밴드당 16 → 256회」는
> **v1 격자 기준**이다 — v1은 밴드에 청크가 16개, 지금은 **256개**라 16배다.
> ⇒ **밴드마다 「깨어 있는 청크 수」를 따로 들어야 1μs가 된다.** 「구현 계획」 단계 1의 `_band_awake`.

---

## 판정

🔴🔴 **판정 결과는 본 사람이 그 자리에서 이 절 아래에 적는다**(CLAUDE.md). 다음 턴으로 미루지 않는다.

1. 🔴🔴 **물이 멈추나** — ⚠ **두 판정이다. 한 검사로 묶지 마라.**
   **2026-08-07에 (나)로 정해졌다**(team-lead 판단 · 사용자 부재 · **아침에 뒤집힐 수 있다**).
   실측 근거는 「구현 계획」의 ③④, 개념 쪽 결론은 `docs/design/물.md` 「왜 양인가」에 있다.

   **1-a. 좁은 물은 진짜로 0이 되나** — 폭 32셀 그릇 · `WATER_MIN_DIFF` 16 ⇒
   **약 2,800틱에 활성 청크가 0.** 🔴 **이쪽이 「청크 재우기가 실제로 논다」의 증거다.**
   ⚠ **그물이 잰다**(헤드리스로 몇 초다). 화면에서 140초를 기다릴 이유가 없다.

   **1-b. 넓은 물은 상한 아래로 잠기나** — 폭 128셀 이상은 **0이 원리적으로 안 나온다.**
   반씩 나누는 규칙은 확산이라 평평해지는 데 폭의 제곱에 비례하는 시간이 든다.
   🔴 **재는 것은 「활성 청크 수가 줄다가 한 자리로 잠기나」다** — 실측 7~18(≈740μs/틱 = 예산의 1.5%).
   ⚠ **총량은 정확히 보존된다 — 새는 게 아니라 안 끝나는 것이다.** 1-b가 「실패」로 읽히면
   그건 총량이 새거나 활성 청크가 **계속 느는** 때뿐이다.

   ⚠ **눈으로 「멈춘 것 같다」는 판정이 아니다** — 활성 청크 수를 **값으로** 봐야 한다.
   v1은 물이 멈춘 것처럼 보이는데 안 멈추고 있었다. ⇒ HUD의 `활성 청크` 줄이 그 자리다(단계 1).
   🔴 **화면에서 볼 것은 1-b뿐이다**: 그 숫자가 **줄다가 한 자리로 잠기는 것.**
2. **평평하게 눕나** — 계단이 아니라 수면이 되나
3. **총량이 보존되나** — 부은 양과 며칠 틱 뒤의 총량이 같나. 🔴 **값으로 잰다**
4. **그릇을 뚫으면 쏟아지나** — 폭발이 청크를 깨우는지가 여기서 드러난다
5. **좌우 편향이 없나** — 대칭 지형에 물을 부으면 좌우 분포가 **거의** 같나(dir 뒤집기가 도나)

   > 🔴🔴 **2026-08-07 — 원래 「좌우 절반의 양이 *정확히* 같다」였고, 그건 이 규칙으로 원리적으로
   > 안 나온다.** builder가 실측으로 뒤집었다.
   >
   > 한 칸에서 **dir 쪽을 먼저 나누고 그 결과로 줄어든 양을 반대쪽에 나눈다** ⇒ **한 틱 안의 좌우가
   > 대칭이 아니다.** dir이 뒤집혀도 `diff >> 1` 이 **내림**이라 잔돈이 남고 정확히 상쇄되지 않는다.
   > **실측 차이는 3,060 중 1 (0.03%)**.
   >
   > ⇒ **재는 것을 「편향」으로 좁혔다**: 좌우 차이가 **물 한 칸(255)을 넘으면** 빨개진다.
   > ⚠ **임계를 0으로 두면 그 검사는 영원히 빨갛다** — 없는 성질을 요구하는 것이다.
6. **얕은 물이 화면에서 갈리나** — 젖음이 눈에 보이나
7. **화면 가득 물이 흘러도 20Hz가 유지되나** — 🔴 **FPS로만 답이 난다.** 헤드리스로 못 잰다

---

### 결과 — 2026-08-07, verify-look 이 화면으로 봤다

🔴 **사용자가 본 것이 아니다.** 에이전트가 godot-mcp로 에디터를 띄워 게임 화면을 보고 적었다.
⚠ **색과 「물처럼 보이나」는 여전히 사용자 판정이 남아 있다** — 아래 「색」 항목이 그 재료다.

**장면 세우기**: 돌 그릇(안쪽 폭 71셀 · 높이 32셀)을 `_fill_rect` 로 세우고
`stage._pour_water_at()`(F가 부르는 그 함수)로 반경 16셀·255를 부었다.

| 판정 | 결과 | 화면에서 무엇이 |
|---|---|---|
| **F 키가 먹나** | 🟢 **통과** | `godot_input` 으로 F 주입 ⇒ HUD `활성 청크 0 → 8` · `물 0 → 797칸`. **시그널·입력·좌표 변환 셋 다 산다** |
| **1-b 활성 청크가 잠기나** | 🟢 **통과** | 부은 직후 8 → 6~7로 **한 자리에 잠긴 채** 유지 · 2,000틱 뒤 4 · **2,500틱에 0**. 한 번도 안 늘었다 |
| **2 평평하게 눕나** | 🟡 **결국 눕는다. 다만 느리다** | 아래 상자 |
| **3 총량 보존** | 🟢 **통과** | 그릇 안 `_aux` 합이 240틱 내내 **196,350으로 한 톨도 안 변했다** |
| **4 뚫으면 쏟아지나** | 🟢 **통과** | 그릇 바닥에 `CMD_BLAST`(r=6) ⇒ 수면이 구멍 쪽으로 **깔때기 모양으로 빨려 들며** 아래로 줄기가 떨어진다 |
| **청크 경계 「뜬 물」** | 🟢 **통과** | 물 바닥을 `y=815`(밴드 50 끝)에 재우고 **바로 아래 `y=816`(밴드 51)만** 팠다 ⇒ 20틱 안에 `aux 255 → 0`. **안 떠 있다** |
| **6 얕은 물** | ⚫ **못 봤다** | 단계 4가 없다. `src/` 에 `SHALLOW` 가 한 글자도 없고 물은 **한 색**이다 |
| **7 20Hz 유지** | ⚫ **못 봤다** | 이 장면은 물이 700~1,000칸이라 부하가 아니다. FPS 2,000~3,500 |

> 🔴🔴 **판정 2는 「눕나」가 아니라 「언제 눕나」였다.**
> 폭 71셀 그릇 · `WATER_MIN_DIFF = 4` 에서 수면 높이(셀):
>
> ```
> 260틱   23 22 19 13  7  3  1  0     ← 왼쪽에 쌓이고 오른쪽으로 흘러내리는 비탈
> 500틱   16 15 14 12  9  7  6  5
> 1,000틱 13 12 12 11 10  9  8  8
> 2,500틱 11 11 11 10 10 10 10 10     ← 여기서 활성 청크 0
> ```
>
> **눕기는 눕는데 2,500틱 = 125초가 든다.** ⚠ 계단이 아니라 **매끈한 비탈**이다 —
> 「블록이 쌓인다」로는 안 보인다. 그건 좋다.
> 🔴 **그런데 실시간으로 1.4초를 봤더니 비탈이 거의 안 움직인다**(틱 190 → 218에서 눈으로 차이가 안 난다).
> ⇒ **사용자가 볼 장면은 「부으면 떨어지고, 비탈이 되고, 그 뒤 2분 동안 가만히 있는 것처럼 보인다」**이다.
> ⚠ **「멈춘 줄 알았는데 안 멈췄다」가 아니라 그 반대다** — 실제로는 계속 눕고 있고 **눈에만 안 보인다.**
> `WATER_MIN_DIFF` 를 올리면 빨리 멈추는 대신 비탈이 남는다(「정한 것」의 축 설명 그대로).

**색 — `0x2C5F8A` 를 화면에서 처음 봤다** (verify-look 의 판단이고, 사용자가 뒤집을 수 있다)

- 🟢 **갈리는 것은 확실하다.** 배경(거의 검정) · 돌(따뜻한 회색) · 나무(갈색) · **불(주황)** 과
  색축이 안 겹친다. 불이 물 바로 위에서 타는 화면을 봤고 **둘이 서로 안 먹는다**
- 🟡 **다만 「물」보다 「파란 벽돌」에 가깝다.** 이유 셋:
  ① **변화가 0이다** — 수면 한 줄도 안쪽과 같은 색이라 **면이 통째로 한 덩어리**로 보인다
  ② **완전 불투명**이라 뒤가 안 비친다
  ③ 고인 뒤 **윗변이 자로 그은 직선**이다
  ⚠ **떨어지는 동안(둥근 덩어리)에는 훨씬 액체처럼 보였다** — 모양이 값을 대신 내고 있었다
- ⇒ 🔴 **단계 4(얕은 물 색)가 들어오면 ①이 상당 부분 풀린다** — 수면 쪽이 얕아 밝아지면 면이 갈린다.
  **색값 자체를 지금 고치기보다 단계 4를 보고 정하는 쪽을 권한다.**

---

## 🔴🔴 그물 — 짖음이 아니라 값으로 잰다

CLAUDE.md 「가짜 그물 금지」가 이 기능에 그대로 걸린다. **물의 실패는 전부 조용하다.**

| 재는 것 | 어떻게 |
|---|---|
| **총량 보존** | N을 붓고 200틱 뒤 `_aux` 합이 N인가. 🔴 새는 물은 화면에서 안 보인다 |
| **평형 후 잠듦** | 평형 뒤 **활성 청크 수가 0**인가. ⚠ 「물이 안 움직인다」로는 부족하다 — **안 움직이는데 안 자는 것**이 v1이었다 |
| **좌우 편향** | 대칭 지형 · 대칭 투입 ⇒ 좌우 절반의 양이 같은가. ⚠ v1이 이 그물을 갖고 있었다 |
| **경계를 넘는 깨움** | 청크 경계에 물을 놓고 다음 틱에 **이웃 청크가 awake 인가** |
| **폭발이 깨우나** | 잠든 청크에 `cmd_blast` ⇒ awake 인가 |
| **결정론** | 같은 커맨드 열을 두 격자에 먹이고 `_aux` 전체가 바이트 단위로 같은가 |

🔴 **새 검사를 넣으면 뒤집어 봐라**(CLAUDE.md). 임계 비교를 지우고 · 이웃 깨우기를 지우고 ·
dir 뒤집기를 고정해서 **각각 빨개지는지** 확인한다.
⚠ **안 물리면 검사를 의심하기 전에 뮤테이션이 실제로 들어갔는지 먼저 봐라** — 이 리포가 두 번 데였다.

---

## 구현 계획

**2026-08-07, spec.** 코드를 쓰기 전에 **헤드리스로 먼저 쟀다** — 이 절의 값은 전부 실측이고,
재는 데 쓴 대리 구현은 `src/` 밖(스크래치)에 있었다. 표에 「추정」이라 적힌 것은 안 잰 것이다.

### 🔴🔴 먼저 잰 것 — 이 계획이 위 본문 셋을 고친다

측정 조건: `Godot_v4.7.1 --headless --script` · 실제와 같은 자료구조(`PackedByteArray` 3장 ·
4,128,768셀) · 같은 인덱싱(`i = (y << 12) | x`) · **`cell_grid.gd` 머리의 순회 순서 계약대로**
(밴드 아래→위 · 밴드 안 행도 아래→위 · **행 → 청크** · 열은 dir의 반대).
⚠ **대리 측정이다** — 실제 `_water_step` 이 아니라 같은 모양의 루프다. 셀당 하는 일은 같지만
분기 하나만 늘어도 값이 움직인다. **구현이 다시 재고 그 자리 주석에 남겨라.**

| 잰 것 | 값 | 예산(50,000μs) 대비 |
|---|---|---|
| 잠든 격자 · **밴드 요약 없이** 「행 → 청크」 훑기 | **6,016μs/틱** | **12%** |
| 잠든 격자 · **밴드 요약 있음**(밴드 63개만 검사) | **1μs/틱** | 0% |
| 청크 하나 — 물이 **흐른다**(아래로 실제 이동) | **40.5~41.7μs** | — |
| 청크 하나 — 물이 **평형인데 깨어 있다**(셀당 최악 경로) | **53.9~67.8μs** | — |
| 청크 하나 — **물이 한 칸도 없다**(재료 검사에서 튕긴다) | **12.5μs** | — |

🔴🔴 **① 「잠든 격자 0μs」는 그냥은 안 나온다.** 본문 「비용」 표가 그렇게 적었는데,
계약대로 「행 → 청크」로 돌면 **밴드당 플래그 검사가 `256 × 16 = 4,096`회**다
(`cell_grid.gd` 머리의 「밴드당 16 → 256회」는 **v1 격자 기준**이다 — v1은 밴드에 청크가 16개였고
지금은 **256개**라 16배다). 전부 잠들어도 6,016μs = 예산의 12%를 낸다.
⇒ **밴드마다 「깨어 있는 청크 수」를 따로 든다.** 0이면 그 밴드를 통째로 건너뛴다 ⇒ 1μs.

🔴🔴 **② 「양이 임계 이하면 안 움직인다」가 멈춤의 심장이 아니다.** 본문 ③-3이 그렇게 적었다.
뒤집어 쟀다 — 젖음 임계만 0 → 8 → 32 → 64로 바꾸고 나머지를 고정하면:

```
WET  0 → 마지막 틱 이동 아래 42 / 좌우 79
WET  8 →                 42 / 81
WET 32 →                 42 / 81
WET 64 →                 41 / 83
```

**아무것도 안 달라진다.** 멈춤을 정하는 것은 **좌우 차이 하한**(아래 `WATER_MIN_DIFF`)이고,
젖음 임계가 하는 일은 **화면을 가르는 것과 퍼짐의 꼬리를 자르는 것**이다. ⇒ **둘은 다른 손잡이다.**
⚠ 본문은 둘을 한 줄에 묶어 놨다. 그대로 구현하면 **없는 손잡이를 돌리며 왜 안 멈추나를 찾게 된다.**

🔴🔴 **③ 넓은 물은 완전히 안 멈춘다. 이게 이 기능에서 제일 큰 발견이다.**
돌 그릇 한쪽에 물을 붓고 평형까지 돌렸다(WET=0, dir 매 틱 뒤집음):

| 그릇 폭(셀) | MIN_DIFF 1 | 2 | 4 | 8 | 16 | 32 |
|---|---|---|---|---|---|---|
| 32 | 안 멈춤 | 안 멈춤 | 안 멈춤 | 안 멈춤 | **2,798틱**(140초) | **1,662틱**(83초) |
| 64 | 안 멈춤 | 안 멈춤 | 안 멈춤 | 안 멈춤 | 안 멈춤 | **3,958틱**(198초) |
| 128 | 안 멈춤 | 안 멈춤 | 안 멈춤 | 안 멈춤 | 안 멈춤 | 안 멈춤(>4,000틱) |

**총량은 네 경우 다 정확히 보존됐다** — 새는 게 아니라 **안 끝나는** 것이다.
⚠ 원인은 구조다: 반씩 나누는 규칙은 **확산**이라 평평해지는 데 폭의 제곱에 비례하는 시간이 든다.
그리고 정지 조건(이웃 차이 ≤ MIN_DIFF)은 폭이 넓을수록 **기울어진 수면도 정지로 인정**한다.

🔴 **`docs/design/물.md` 의 「양이 좌우로 같아지면 더 옮길 것이 없어서 계산이 스스로 끝난다」는
좁은 물에서만 참이다.** 그 문장이 양을 고른 **결정적 이유**로 적혀 있으므로, 여기 실측을 남긴다.

🔴🔴 **④ 그런데 비용은 괜찮다 — 깨어 있는 청크가 안 는다.** ③이 무섭게 읽히므로 바로 이어 잰다.
안 멈추는 동안 **틱마다 dirty 청크 수**를 셌다(MIN_DIFF=4):

| 그릇 폭 | 물이 든 청크 | 깨어 있는 청크 — 10틱 / 100틱 / 500틱 / 2,000틱 / 최대 |
|---|---|---|
| 32 | 6 | 7 / 7 / 7 / 7 / **7** |
| 64 | 12 | 12 / 13 / 13 / 14 / **14** |
| 128 | 18 | 12 / 14 / 15 / 10 / **16** |
| 256 | 30 | 12 / 14 / 16 / 12 / **18** |

**폭이 8배가 되어도 깨어 있는 청크는 7 → 18이다.** 물이 든 청크의 60%가 아니라 **표면 띠만** 깬다 —
호수 속은 전부 255라 위아래가 꽉 차 아무것도 안 옮기고, 그 청크는 다음 틱에 잠든다.
⇒ **18청크 × 41μs ≈ 740μs/틱 = 예산의 1.5%.** 안 멈추는 물이 **싸다.**

🔴 **v1과 갈리는 자리가 정확히 여기다.** v1은 움직이는 물이 매 틱 전하 파면에 재등록돼
**움직임이 곧 비용**이었다. 여기서는 움직임이 **표면에 갇힌다.**
⚠ **다만 「청크가 0이 된다」에 기대는 것은 전부 다시 봐야 한다** — 아래 「main에 올리는 질문」 1번.

---

### 구조 판단 — 변형인가 새 종류인가

| 무엇 | 판단 | 근거 |
|---|---|---|
| 물이라는 **재료** | **변형** | `DEFS` 한 줄. `WATER := 4`(다음 빈 id, 팔레트 16·셰이더 0~15 안) |
| 물의 **양** | **변형** | `_aux` 를 이미 불이 쓴다. 뜻만 한 줄 더 |
| **청크 재우기** | 🔴 **새 종류** | 격자에 「어디를 도나」라는 축이 지금 없다. 불은 파면 목록이라 이 축과 무관하다 |
| **얕은 물의 색** | **변형** | 불이 이미 같은 길을 냈다(`_flag` 비트 → 셰이더 분기 → `fx_tuning` 색) |

**청크 재우기가 새 종류인 이유**: 지금 격자를 도는 코드는 `_burn()` 하나이고 그건 **파면 목록**이라
좌표 순서도 공간 구조도 안 쓴다. 「공간을 조각으로 나눠 조각 단위로 돈다」를 얹을 자리가 없다.
⇒ 새 상태(청크 배열 셋)와 새 순회(`_water_step`)가 선다.

**건드리는 파일 수** — 단계별로 센다:

| 단계 | src 파일 | 수 |
|---|---|---|
| 1 | `sim/cell_grid.gd` · `sim/sim_tuning.gd` | 2 ✅ |
| 2 | `sim/cell_grid.gd` · `sim/cell_materials.gd` · `sim/sim_tuning.gd` | 3 ✅ |
| 3 | `sim/cell_grid.gd` | 1 ✅ |
| 4 | `sim/cell_materials.gd` · `view/fx_tuning.gd` · `view/cell_renderer.gd` · `view/cell_grid.gdshader` | **4 ⚠** |
| 5 | `sim/sim_tuning.gd` · `sim/spell_sim.gd` | 2 ✅ |
| 6 | `sim/cell_grid.gd` | 1 ✅ |

🔴 **단계 4가 계약(셋)을 넘는다. 넘은 것을 인정하고 이번엔 안 고친다.**
⚠ **불이 이미 같은 넷을 지났다** — `FLAG_BURNING`(cell_materials) · `FIRE_LO/HI`(fx_tuning) ·
`set_shader_parameter`(cell_renderer) · `if ((flags & …) != 0)`(gdshader).
⇒ **구조가 틀린 것이 맞다.** 「상태 비트 → 색」이 표에서 파생되지 않고 **네 곳에 손으로 배선**된다.
고칠 자리는 「비트 → 색」을 `cell_materials` 의 표로 만들고 셰이더가 배열을 도는 것이다.
🔴 **이번에 안 한다**(GDD 「뼈 먼저」 · 불 경로를 같이 갈아엎어야 한다). 대신 **두 번째 사례가
생겼다는 것을 여기 남긴다 — 세 번째 상태 색이 오면 그때는 표를 만들어야 한다.**

---

### 단계 1 — 청크 재우기만. 물 없이

**`src/sim/sim_tuning.gd`**
- `CHUNK_CELLS := 16` — 🔴 값을 새로 정하지 않는다. `4096/16 = 256` · `1008/16 = 63` 으로
  **양쪽이 나눠떨어지는 유일한 후보**이고 v1도 16이었다(본문 ①)
  ⚠ **`MAX_CHUNKS_PER_TICK` 은 여기가 아니다 — 단계 3이다**(소비자가 거기 생긴다).
  🔴 **2026-08-07에 이 자리에 적혀 있었고 verify-read가 잡았다** — builder가 계획대로 안 넣었는데,
  그건 **`sim_tuning.gd` 가 스스로 「자리를 미리 만들지 않는다」고 적어 둔 파일이라 옳은 판단**이었다.
  ⇒ **틀린 것은 구현이 아니라 이 문서였다.** 값과 근거는 아래 「정한 것」 표에 그대로 있다.

**`src/sim/cell_grid.gd`**
- 상수: `CHUNK_W = W / CHUNK_CELLS`(256) · `CHUNK_H = H / CHUNK_CELLS`(63) · `CHUNK_COUNT`(16,128).
  ⚠ `W`·`H` 가 16의 배수인지 `_init()` 에서 짖게 한다 — `X_SHIFT` 검사와 같은 자리·같은 이유
- 상태 **넷**: `_awake`·`_dirty`(`PackedByteArray`, 16,128) ·
  **`_band_awake`·`_band_dirty`(`PackedInt32Array`, 63)**
  - 🔴 `_band_awake` 는 최적화가 아니라 **전제다** — 없으면 잠든 격자가 6,016μs다(위 ①)
  - 🔴 **`_band_dirty` 는 이 문서에 원래 없었다. 짝이 있어야 증분 유지가 성립한다** —
    flip이 awake↔dirty 를 갈아 끼우므로 dirty 쪽 요약이 없으면 flip마다 `_awake` 를 다시 훑어야 한다.
    **verify-read가 값으로 확인했다: 재계산 ≈232μs/틱 vs 증분 유지 O(1)**
    (`band_awake_count()` 0.123μs · `scan_awake_in_band()` 3.68μs × 63밴드)
- `_touch(i)` — 셀 인덱스가 든 청크를 dirty 로 찍고 `_band_awake` 를 유지한다
- 🔴🔴 **`_touch` 를 부르는 자리는 `_write_cell` 하나다.** `_fill_rect` 도 `_disc(destroy)` 도
  다 탄 칸 비우기(`_burn`)도 **전부 `_write_cell` 을 지난다**(읽어서 확인했다:
  `cell_grid.gd:280` · `:319` · `:151`) ⇒ 본문 「`_write_cell` · `_disc` · `_fill_rect` 셋 다」는
  **한 곳으로 접힌다.**
  ⚠ **`_ignite_cell` 은 안 부른다** — `_flag`·`_aux` 만 만지고 재료를 안 바꾼다. 그리고 점화는
   연료 > 0 이 조건인데 **물은 연료가 0**이라 물 칸에 원리적으로 못 닿는다
- `_reset()` — **넷을 다 직접 비운다.** `fill()` 이 `_write_cell` 을 안 지나는 것은
  `_burning` 과 똑같은 함정이고, 그 파일이 이미 그렇게 적어 뒀다
- `step()` 에 `_chunk_flip()` — 🔴 **틱 끝이 아니라 틱 시작이다.** 이유는 위 「동작 ①」의 상자.
  ⚠ **「그 자리에서 지워진다」가 아니라 「한 틱 늦는다」**다(2026-08-07 verify-read 정정)
- 질의 넷: `active_chunk_count()` · `chunk_awake_at(x, y)` ·
  `band_awake_count(band)` · **`scan_awake_in_band(band)`(그물 전용 독립 측정)**
  - 🔴 **판정 1과 그물이 값으로 읽는 자리다.** 「안 움직인다」가 아니라 **이 숫자**를 본다
  - ⚠ **독립 측정을 둔 것만으로는 부족하다** — 아래 「그물」의 구멍 3을 봐라

**`src/stage/stage.gd`** — HUD 한 줄에 `활성 청크 %d` 를 붙인다(껍데기라 파일 수 계약 밖).
🔴 **판정 1은 화면에서 이 숫자로만 난다.** 없으면 사용자가 「멈춘 것 같다」로만 말하게 되고
그건 v1이 속은 방식이다.

**이 단계가 끝나면 값으로 잴 수 있는 것**
- 빈 격자 · 아무 커맨드 없음 ⇒ `active_chunk_count() == 0`, 그리고 **몇 틱을 돌려도 0**
- `cmd_blast(x, y, …)` ⇒ 그 자리 청크가 `chunk_awake_at` 참 · 다음 틱에 다시 거짓
- `cmd_fill` 로 지형을 깐 뒤 1틱 ⇒ 깬 청크 수가 지형이 덮은 청크 수와 같다
- 잠든 격자 `step()` 1,000회의 벽시계 시간 (⚠ `Time.` 은 `src/sim/` 금지 — **그물에서** 잰다)

**그물** — `tests/nets/net_water.gd`(새 파일. 러너가 폴더를 훑으므로 등록은 없다)
- `잠든 격자는 안 깨어난다` · `폭발이 청크를 깨운다` · `깨운 청크는 한 틱 뒤 잠든다`
- `_reset` 뒤 활성 청크가 0
- 🔴 **뒤집어 봐라**: `_write_cell` 의 `_touch` 를 지우면 「폭발이 깨운다」가 빨개져야 한다.
  ⚠ 안 물리면 검사부터 의심하지 말고 **치환이 실제로 들어갔는지 먼저 봐라**(본문 「그물」)

#### 🔴🔴 검증 결과 — 2026-08-07, verify-read가 뮤테이션 34개를 돌렸다

**개별 단언 39개는 전부 물었다. 헛도는 검사는 없다.** 그런데 **검사 사이의 틈에 구멍이 넷** 있었다.
⚠ 「검사가 다 물린다」와 「덮인다」는 다른 말이다 — 이 구분이 이 절의 요점이다.

| # | 뮤테이션 | 그물 | 막는 법 |
|---|---|---|---|
| **1** | `_reset` 의 `_band_awake.fill(0)` 만 제거 | 🟢 39/39 초록 | 리셋 직후 **밴드 요약 합이 0**인지 한 줄 |
| **2** | `_chunk_flip()` 을 `step()` **끝**으로 | 🟢 초록 | 🔴 **불로 잰다**(아래) |
| **3** | `_band_awake`·`_band_dirty` 를 **통째로 죽인다** | 🟢 초록 | 🔴 **단계 1에선 못 막는다**(아래) |
| **4** | `_ignite_cell` 이 `_touch` 를 부른다 | 🟢 초록 | `ignite()` 뒤 활성 청크가 0인지 한 줄 |

🔴🔴 **구멍 2가 builder의 자기 진단을 뒤집었다.** builder는 「단계 1의 그물은 flip 배치를 못 잰다 —
틱 도중에 움직이는 것이 있어야 하고 그건 `_water_step` 이다」라고 코드 주석에 적었다.
**틱 도중에 움직이는 것이 이미 있다 — 불이다.** `_burn()` 이 `step()` **안에서**
다 탄 칸에 `_write_cell(idx, EMPTY)` 를 부르고 그게 `_touch` 를 탄다. verify-read가 실증했다:

```
flip 앞(현재):  다 탄 직후 active=0 · 한 틱 더 돌리면 active=1
flip 뒤(뮤테이션): 다 탄 직후 active=1          ← 여기서 갈린다
```

⇒ **나무 한 칸을 태워서 배치를 재는 검사를 넣어라.** 없으면 배치를 뒤집는 사람이
「그물이 다 통과한다」를 근거로 삼는다.

🔴🔴 **구멍 3이 제일 크고, 단계 1에서 못 막는다.**
`cell_grid.gd` 와 `net_water.gd` 가 「독립 측정과 맞대는 것이 이걸 잡는 유일한 길」이라고 적었는데
**그 주장이 틀렸다** — 맞대기는 **갈라짐**만 잡고 **사라짐**은 못 잡는다.
두 경로를 하나로 접으면 그 단언이 **항진명제**가 되고, 절대값 단언도 접근자가 스캔으로 바뀌면 그대로 맞는다.

⚠ **그리고 지금은 시간으로도 못 잰다** — `_band_awake` 를 읽는 곳이 `src/` 안에 하나도 없다
(순회가 단계 3에 온다). **단계 1에서 이 배열은 그물만 읽는 상태다.**
🔴 **⇒ 단계 3에서 「잠든 격자 `step()` 의 벽시계 시간」 검사를 반드시 넣어라.**
그게 이 구멍을 막는 유일한 자리이고, **안 넣으면 `_band_awake` 가 조용히 사라져도 영영 초록이다.**

---

### 단계 2 — 물 재료 + 아래로만 떨어진다

**`src/sim/cell_materials.gd`**
- `WATER := 4` · `ALL` 에 추가 · `DEFS` 한 줄: `behavior: BEHAVIOR_NONE` · `fuel: 0` · `rgb`
  - 🔴 `BEHAVIOR_NONE` 이라 `is_solid()` 가 거짓 ⇒ 캐릭터도 탄도 통과한다(본문 「기존 것과의 상호작용」)
  - 🔴 `fuel: 0` 이 「물엔 불이 안 붙는다」를 **파생시킨다.** 따로 안 막는다
- `_aux` 의 뜻 절에 한 줄: `WATER → 물의 양(1~255)`
  - 🔴 **이 절이 갈라지면 다음 사람이 `_aux` 를 다른 뜻으로 읽는다.** 그 파일이 스스로 그렇게 적었다

**`src/sim/sim_tuning.gd`** — `WATER_MAX := 255` · `WATER_MIN_DIFF` · `WATER_WET`(아래 「정한 것」)

**`src/sim/cell_grid.gd`**
- 🔴🔴 **`_write_water(i, amount)` — 물이 지나는 유일한 문.** `_write_cell` 은 `_flag`·`_aux` 를
  0으로 만들어 **양을 날린다**(`cell_grid.gd:335-336`). 하는 일은 본문 ②가 정한 다섯 가지
- `_water_step()` — 밴드 아래→위 · 밴드 안 행 아래→위 · **행 → 청크** · 열은 dir의 반대.
  이번 단계는 **아래로 넘기기만** 한다
- `step()` 순서: `_chunk_flip()` → `_water_step()` → `_burn()`
  - ⚠ 물이 불보다 **먼저**다: 불이 다 탄 칸을 EMPTY 로 만들면 그 칸은 **다음 틱에** 물을 받는다.
    같은 틱에 받게 하려면 순서를 뒤집어야 하는데, 그러면 이번 틱에 생긴 물 위에 불이 붙는지가
    순서에 매달린다. 🔴 **한 틱 늦는 쪽이 읽기 쉽고, 화면에서 구별이 안 된다**(20Hz에서 50ms)
- `_dir` — `_tick & 1`. 🔴 **상태를 새로 안 든다** — `_tick` 이 이미 결정론적이다

**이 단계가 끝나면 값으로 잴 수 있는 것**
- `_aux` 총합이 부은 양과 같다 (200틱 뒤에도)
- 공중에 놓은 물이 바닥에 닿는다 — 바닥 행의 `aux_at` 이 오른다
- 떨어지는 동안 활성 청크가 > 0, 다 쌓이면 **0으로 떨어진다**
  - 🔴 **좌우가 없으므로 이 단계는 진짜로 0이 된다.** 위 ③의 문제는 단계 3에서 온다 —
    ⇒ **단계 2의 0이 「청크 재우기가 실제로 논다」의 가장 깨끗한 증거다.** 여기서 0이 안 나오면
    단계 3으로 가지 마라

**그물** — `총량 보존` · `아래로 떨어진다` · `평형 뒤 활성 청크 0` ·
`_write_cell 이 물 칸을 덮으면 양이 0이 된다`(두 문의 경계 — 본문 ②의 마지막 줄)
- 🔴 **뒤집어 봐라**: `_write_water` 를 `_write_cell` 로 바꾸면 총량 보존이 빨개져야 한다

---

### 단계 3 — 좌우 나눔

**`src/sim/cell_grid.gd`** 한 파일. `_water_step` 에 ② 좌우를 붙인다.
- 아래로 못 갈 때만 좌우를 본다(아래가 꽉 찼거나 고체)
- **차이가 `WATER_MIN_DIFF` 이하면 안 옮긴다.** 🔴 **여기가 멈춤의 심장이다**(위 ②)
- 옮기는 양은 `diff >> 1` — ⚠ **정수 시프트가 안전하다**: `diff` 는 늘 양수라
  `sim_tuning.DRAG_NUM` 주석의 「음수에서 `>>` 가 비대칭」 함정에 안 걸린다
- dir 쪽 이웃을 먼저 본다

🔴🔴 **경계를 넘는 이동은 도착지 청크도 dirty 로 찍는다.** 안 찍으면 물이 청크 경계에서 서고
**화면에 「보이지 않는 벽」으로만 드러난다**(본문 ①).

🔴🔴 **상한을 정할 때 청크만 세면 틀린다** — 위 「정한 것」의 비용 모델.
깨어 있는 **밴드 하나가 128μs**이고 그건 그 밴드의 청크 수와 무관하다. 좌우로 퍼지는 이 단계에서
**밴드 수가 처음으로 늘어난다** ⇒ 63밴드가 다 깨면 청크가 몇 개든 **8,000μs = 예산 16%가 바닥**이다.
⚠ **잰 값을 그 자리 주석에 남겨라** — 좌우가 붙은 뒤 밴드 항이 실제로 얼마인지는 아무도 아직 모른다.

🔴🔴 **한 틱 상한을 여기서 붙인다 — 자르는 자리가 밴드 앞이다.**
⚠ **밴드 안에서 「행 → 청크」로 도니까 한 청크의 16행이 순회에서 연속된 구간이 아니다.**
「N번째 청크에서 멈춘다」를 루프 안에서 하면 **한 청크의 행 절반만 도는 일이 난다.**
⇒ **순회 전에** 이번 틱에 돌 청크 집합을 순회 순서(밴드 내림차순 → dir 방향 열 순서)로 모으고,
앞에서 `MAX_CHUNKS_PER_TICK` 개만 남긴다. **잘린 청크는 awake 를 유지한다**(다음 틱에 돈다).
🔴 결정론이 여기 걸린다 — 자르는 순서가 곧 상태다.

**이 단계가 끝나면 값으로 잴 수 있는 것**
- 대칭 지형 · 대칭 투입 ⇒ 좌우 절반의 `_aux` 합이 **정확히 같다**(dir 뒤집기가 도나)
- 청크 경계에 물을 놓고 1틱 ⇒ 이웃 청크가 `chunk_awake_at` 참
- 총량 보존이 좌우 나눔 뒤에도 유지된다
- 좁은 그릇(폭 32셀 · `MIN_DIFF` 16)이 **약 2,800틱에 활성 청크 0**이 된다
- 🔴 **넓은 그릇(폭 128셀)은 0이 안 된다. 대신 활성 청크가 20 아래로 잠긴다** — 위 ④.
  ⚠ **이 둘을 다른 검사로 잰다.** 한 검사로 묶으면 라벨이 재는 것보다 넓어진다

**그물** — `좌우 편향 없음` · `경계를 넘는 깨움` · `총량 보존` · `좁은 그릇이 멈춘다` ·
`넓은 그릇의 활성 청크가 상한 아래로 잠긴다` · `결정론`(같은 커맨드 열 ⇒ `_aux` 가 바이트 단위로 같다)
- 🔴 **뒤집어 봐라 셋**: ① `MIN_DIFF` 를 0으로 ⇒ 「좁은 그릇이 멈춘다」가 빨개진다
  ② 도착지 청크 찍기를 지운다 ⇒ 「경계를 넘는 깨움」이 빨개진다
  ③ `_dir` 을 0으로 고정 ⇒ 「좌우 편향 없음」이 빨개진다

---

### 단계 4 — 젖음 임계 + 화면 두 가지

**`src/sim/cell_materials.gd`** — `FLAG_SHALLOW := 1`(비어 있는 최하위 비트. 하위 4비트 안)
**`src/view/fx_tuning.gd`** — `WATER_SHALLOW: Color`
  - 🔴 **왜 여기인가**: 불이 이미 그렇게 한다(`FIRE_LO`·`FIRE_HI`). 재료 색은 `DEFS.rgb` 의
    정수이고, **상태에서 나오는 색**은 뷰 폴더의 `Color` 다 — `src/sim/` 은 `Color` 가 금지다
**`src/view/cell_renderer.gd`** — `flag_shallow`·`water_shallow` 를 uniform 으로 주입
**`src/view/cell_grid.gdshader`** — `flags & flag_shallow` 분기 한 줄
  - ⚠ **불 분기보다 뒤에 둔다.** 물은 안 타므로 겹칠 수 없지만, 순서를 정해 두면 다음 상태가
    올 때 「어느 게 이기나」를 다시 안 정한다

**`_write_water` 가 임계 비교로 `FLAG_SHALLOW` 를 갱신한다** — 양과 비트가 **한 자리에서** 움직인다.
🔴 둘을 따로 만지면 갈라지고, 갈라진 쪽은 **화면에만** 나온다.

**이 단계가 끝나면 값으로 잴 수 있는 것**
- 양이 `WATER_WET` 이하인 칸은 `flag_at() & FLAG_SHALLOW != 0`, 넘으면 0 — **경계값 양쪽을 다 잰다**
- 셰이더가 컴파일된다 (⚠ `net_render` 가 이미 그걸 잰다 — 안 그러면 **화면만 검게 뜨고 그물은 초록**이다)
- 양이 임계를 넘나들 때 비트가 따라 뒤집힌다

**그물** — `net_water` 에 비트 갱신, `net_render` 에 셰이더/uniform.
🔴 **뒤집어 봐라**: `_write_water` 의 비트 갱신 줄을 지우면 경계값 검사가 빨개져야 한다

---

### 단계 5 — 물 룬이 물을 만든다

**`src/sim/sim_tuning.gd`**
- `ELEM_WATER := 2` · `ELEM_ALL` 에 추가 · `TRACE_WET := 2` · `ELEM_DEFS` 한 줄
  - 🔴 `TRACE_WET` 자리는 그 파일이 이미 「물이 들어오면 는다 — **자리를 미리 만들지 않는다**」고
    적어 뒀다. **지금이 그 때다**
- `SIM_SIZES` 에 `water_r` 열 추가 — 세대마다 줄어야 한다
  - 🔴🔴 **`net_tables` 의 이름 목록에 `"water_r"` 을 같이 넣어라.** 안 넣으면 아무도 안 재는데
    **통과 수는 그대로 초록이다** — 그 파일이 실측으로 그렇게 적어 뒀다(`damage` 열 사례)

**`src/sim/spell_sim.gd`** — `_rune_trace` 에 `TRACE_WET` 갈래. `cmd_water(x, y, r, amount)` 를 부른다
  - ⚠ 새 커맨드가 필요하다(`_disc` 는 `_write_cell` 을 부르므로 물엔 못 쓴다)
    ⇒ `cell_grid` 에 `CMD_WATER` + `_water_disc()`(정수 원반 · `_write_water` 를 부른다)
  - 🔴 이러면 단계 5가 `cell_grid.gd` 도 건드려 **3파일**이 된다. 계약 안이다

**값으로 잴 수 있는 것** — 물 룬으로 쏘면 착탄점의 `_aux` 합이 0에서 오른다 ·
무속성/불 룬으로 쏘면 안 오른다 · 그 청크가 깨어난다

**그물** — `net_water` 에 착탄, `net_tables` 에 `water_r` 단조 감소

---

### 단계 6 — 불과 물

**`src/sim/cell_grid.gd`** 한 파일. `_burn()` 안에서 타는 칸의 네 이웃에 물이 있으면 끈다.
🔴 **파면 쪽에서 본다** — 물 쪽에서 「내 옆에 불이 있나」를 보면 **물이 있는 모든 칸이 매 틱
이웃 넷을 읽고**, 그건 고인 호수 전체를 깨우는 것이라 ④가 무효가 된다.
⚠ 파면은 청크와 독립이므로(그 파일이 그렇게 적었다) 이 방향은 공짜다.

**값으로 잴 수 있는 것** — 물 옆 나무에 불을 붙이면 N틱 안에 `burning_count()` 가 0이 된다 ·
물이 없으면 안 꺼진다 · 끈 만큼 물의 양이 준다(규칙을 그렇게 정하면)

**그물** — `net_water` 에 `물이 불을 끈다` · `물이 없으면 안 꺼진다`(반대쪽을 같이 잰다)

---

### 정한 것 — 값과 근거

| 무엇 | 값 | 근거 |
|---|---|---|
| `CHUNK_CELLS` | **16** | 새로 안 정했다. `4096/16` · `1008/16` 이 둘 다 떨어지는 유일한 후보(본문 ①) |
| `MAX_CHUNKS_PER_TICK` | **512** | 아래 |
| `WATER_MIN_DIFF` | **4** | 아래 |
| `WATER_WET` | **32** | 아래 |
| `WATER_MAX` | **255** | `_aux` 가 `PackedByteArray` 다 |
| `WATER` id | **4** | 다음 빈 id. 팔레트 16 · 셰이더 안전선 0~15 안(재료 5개, 여유 11) |
| `FLAG_SHALLOW` | **1** | 비어 있는 최하위 비트. 하위 4비트 계약 안 |
| `WATER` rgb | **0x2C5F8A** | 아래 |
| `WATER_SHALLOW` | **Color(0.42, 0.68, 0.85)** ≈ 0x6BADD9 | 아래 |

**`MAX_CHUNKS_PER_TICK := 512`**
화면 한 판이 480×270셀 = 30×17 = **510청크**다. 상한이 그보다 작으면 **보이는 물이 상시 밀리고**,
그러면 안전망이 조절 손잡이가 된다 — `MAX_BURNING` 어법 위반이다.

> 🔴🔴 **2026-08-07 — 「청크당 μs × 512」라는 계산 자체가 틀렸다. verify-run이 뒤집었다.**
>
> 계획은 41μs, builder는 78μs를 쟀는데 **둘 다 자기 시나리오에서만 맞는 값**이었다.
> 시나리오를 흔드니 청크당 값이 **10~169μs로 5.7배 벌어진다** — 좁은 물기둥을 외삽하면
> **예산의 169%** 가 나온다. 물 한 방울이 512청크를 채울 리가 없으므로 그건 숫자가 아니라 착시다.
>
> 🔴🔴 **비용에 청크 항 말고 「밴드 항」이 따로 있다.** `_water_step` 은 깨어 있는 밴드마다
> **행 16 × 청크슬롯 256 = 4,096회** 플래그를 본다 — 그 밴드에 깨어 있는 청크가 **1개든 256개든 같다.**
>
> ```
> 틱당 ≈ 5μs(flip)
>      + 128μs × (깨어 있는 밴드 수)     ← 🔴 청크 수와 무관하다
>      +   8.7μs × (빈 청크)
>      +  46.8μs × (평형 물 청크)
>      +  ~58μs  × (낙하 중 물 청크)
> ```
>
> 밴드 기울기를 세 구간에서 따로 쟀고(125.0 · 124.0 · 131.8μs) 이 모델이 **실측 여섯을 다 설명한다.**
> ⚠ **물방울 하나가 떨어지는 것만으로 틱당 141μs다** — 청크는 1개인데 밴드 항이 그렇다.

**`MAX_CHUNKS_PER_TICK = 512` 의 실제 값** (verify-run이 512청크를 **직접 채워서** 쟀다):

| 512청크가 | 틱당 | 예산 대비 |
|---|---|---|
| 물 0칸(빈 청크) | 5,190μs | **10%** |
| **평형 물로 꽉 찼을 때** | **23,894μs** | **48%** ← 직접 잰 값 |
| 전부 낙하 중(파생, 실측 아님) | 30,000~38,000μs | 60~76% |

⇒ **계획의 42%도 builder의 80%도 아니다.** 물의 상태와 **밴드 분포**가 정한다.

🔴 **값은 512 그대로 둔다.** 위 ④가 **평소엔 여기 안 닿는다**고 말하고(호수는 7~18청크),
512에 닿는 것은 댐이 터지는 순간이며 **그때 물이 느려지는 것이 정확히 안전망이 하라는 일**이다.

🔴🔴 **단계 3에서 이 모델을 다시 봐라 — 상한을 「청크 수」로만 자르면 밴드 항을 놓친다.**
지금은 물이 아래로만 가서 세로로 모여 있어 밴드가 몇 개 안 깨어난다.
**좌우로 퍼지면 밴드 수가 늘고 그때 밴드 항이 지배한다** — 63밴드가 다 깨면 청크가 몇 개든
**8,000μs = 예산 16%가 바닥값**이다.
⚠ **지금 고치라는 말이 아니다**(뼈 먼저). 단계 3의 상한을 정할 때 **청크만 세면 틀린다**는 것이다.

⚠ **그리고 판정 7의 무게는 여전히 크다** — 업로드 비용을 헤드리스로 못 잰다(`cell_renderer.gd`).
🔴 판정 7이 실패하면 **최적화하지 말고 값을 내려라.** 화면 한 판이 510청크라 그 아래로 내리면
「보이는 물이 밀린다」가 눈에 보이기 시작하고, **그때는 안전망과 손잡이의 경계를 사용자가 다시 긋는다.**

**`WATER_MIN_DIFF := 4`**
🔴 **뼈 먼저라 「돌아가는 최소」로 고른다.** 위 ③의 표가 말하는 것은 **어떤 값도 넓은 물을
멈추게 못 한다**이므로, 멈춤을 이 값으로 사려 하면 안 된다.
- 1은 **안 된다** — 확산이 가장 느리고 32폭도 4,000틱에 못 멈춘다
- 16~32면 좁은 물이 83~198초에 멈추지만 **수면이 눈에 띄게 기운다**(이웃마다 최대 32/255 = 12.5% 차)
- 4는 수면이 거의 평평하고(판정 2), 깨어 있는 청크가 ≤18로 잠긴다(위 ④에서 잰 값이 `MIN_DIFF=4`다)
⇒ **화면을 보고 바꿀 값이다.** 판정 2(평평하게 눕나)와 판정 1-a(좁은 물이 0이 되나)가
**서로 당기는 축**이라는 것을 여기 적어 둔다 — 올리면 빨리 멈추고 기울고, 내리면 평평하고 오래 산다.
⚠ **1-b(넓은 물이 잠기나)는 이 축에 거의 안 걸린다** — 위 ④를 `MIN_DIFF=4`로 쟀고 그때도 7~18이었다.

**`WATER_WET := 32`**
위 ②에서 **멈춤에 아무 영향이 없다는 것을 쟀다.** ⇒ 이 값은 **순전히 화면 값이다**(255의 1/8).
🔴 **그러니 화면을 보기 전에는 아무 근거가 없다.** 32는 자리를 채우는 값이고, 판정 6(얕은 물이
화면에서 갈리나)에서 사용자가 정한다.

**물 색 — `WATER` rgb `0x2C5F8A` · `WATER_SHALLOW` `Color(0.42, 0.68, 0.85)`**
중간 파랑. 배경 `0x0E0E13` · 돌 `0x5C574F` · 나무 `0x6B4524` 와 **색축이 안 겹친다**(전부 무채/난색).
🔴 **얕은 쪽이 더 밝다.** 실제로 얕은 물은 바닥이 비쳐 밝고, 반대로 두면 **「깊을수록 밝다」가 되어
화면이 거짓말을 한다.**
⚠ **둘 다 자리를 채우는 값이고 근거는 화면뿐이다** — 아직 아무도 안 봤다(판정 6은 화면 판정이다).
아침에 사용자가 고칠 값으로 두고, **고쳤으면 이 표와 그 자리 주석을 같이 고쳐라.**

---

### ✅ 올렸던 질문 셋 — 2026-08-07에 답이 나왔다

🔴🔴 **셋 다 team-lead가 답했다. 사용자는 자러 갔고, 아침에 뒤집힐 수 있다.**
⚠ 그래서 **화면 판정(6·7)은 못 했고, 이 문서는 `3.done` 으로 안 간다.**

**1. 판정 1의 기준 ⇒ (나) 「상한 아래로 잠기나」로 바꾼다.** 근거 셋:
- (가)는 좌우 나눔 규칙을 다시 설계해야 하고 **GDD 「뼈 먼저」를 정면으로 벗어난다**
- 위 ④가 결정적이다 — 폭이 8배가 돼도 깨어 있는 청크가 7 → 18이고 예산의 1.5%다.
  🔴 **비싸지 않은 것을 비싸다고 가정하고 설계를 바꾸는 것이 더 나쁘다**
- **v1과 다른 점이 살아 있어야 한다** — v1은 움직임이 곧 파면 재등록이었고 여기서는
  움직임이 **표면에 갇힌다.** 🔴 **그 구분이 번개가 올 때 다시 온다**
⇒ 위 「판정」 1을 **1-a(좁은 물은 0) · 1-b(넓은 물은 잠긴다)로 갈랐다.**
⇒ `docs/design/물.md` 는 team-lead가 고쳤다 — 「스스로 끝난다」가 「움직임이 표면에 갇힌다」로 바뀌었고,
  **「물은 반드시 멈춘다」가 아니라 「물은 싸게 안 멈춘다」가 맞는 문장**임을 못 박았다.

**2. 눈으로 볼 때 ⇒ 활성 청크 수가 내려가는 것으로 충분하다.**
사용자가 볼 것은 **그 숫자가 줄다가 한 자리로 잠기는 것**이다. 0은 그물이 좁은 그릇으로 잰다 —
화면에서 140초를 기다릴 이유가 없다.

**3. 물 색 ⇒ 먼저 넣고 아침에 사용자가 고친다.** 이 리포 관행이다(마법진 그림도 후보를 뽑아
놓고 사용자가 골랐다). 값은 아래 「정한 것」에 넣었다.

⚠ **나머지 미정(물이 불을 끄는 규칙 · 물 룬이 만드는 양)은 안 물었다** — 단계 5·6 차례가 와야
잴 대상이 생긴다(GDD 「뼈 먼저」).

---

### 위험 — 조용히 깨질 것

| 위험 | 왜 조용한가 | 어디서 잡나 |
|---|---|---|
| 🔴🔴 물이 `_write_cell` 을 지난다 | `_flag`·`_aux` 가 0이 되어 **양이 그 자리에서 증발**. 에러 0 | 그물 `총량 보존` |
| 🔴🔴 도착지 청크를 안 깨운다 | 물이 청크 경계에 **보이지 않는 벽**을 만든다. 에러 0 | 그물 `경계를 넘는 깨움` |
| 🔴🔴 **출발지 쪽(위/좌우) 청크를 안 깨운다** | **↑ 이 표에 원래 없던 방향이고, 2026-08-07에 실제 버그로 발견됐다** | 아래 상자 |

> 🔴🔴 **「도착지」만 적어 둔 것이 이 표의 구멍이었다. verify-read2가 원본 코드에서 재현했다.**
>
> `_touch(i)` 는 **쓴 칸의 청크만** 찍는다. ⇒ 잠든 물이 청크의 **마지막 행**(`y % 16 == 15`)에 있고
> 그 **바로 아래 칸**이 비워지면 — 폭발이든 파기든 다 탄 나무든 — **물의 밴드는 잠든 채로 남고
> 물이 영영 안 떨어진다.** 에러 0, 화면엔 **「공중에 뜬 물」**로만 보인다.
>
> ```
> 물 y=335(밴드 20) · 아래 y=336(밴드 21)은 돌 → 3틱에 완전히 잠듦
> cmd_fill(100,336,100,336, EMPTY) 로 바로 아래만 판다
> 10틱 뒤: 물 y=335 aux=255 그대로 · 아래는 비어 있음 · 두 청크 다 잠듦
> 대조군(같은 청크 안에서 판 경우): 정상적으로 떨어진다
> ```
>
> ⚠ 확률은 **대략 16번에 1번**이고 **모든 폭발·파기가 이 경로다.**
> 🔴 **단계 3에서 좌우가 붙으면 열 경계에서도 같은 일이 나고 그쪽이 훨씬 자주 걸린다.**
>
> ⚠ **넓게 고치면 다른 값을 문다** — 깨어 있는 밴드 하나가 128μs이고 그건 청크 수와 무관하다
> (「정한 것」의 비용 모델). 8이웃을 다 찍으면 **밴드 수가 늘어 그 항이 커진다.**
> ⇒ **「물이 흘러들 수 있는 방향」만** 깨우는 쪽이 좁고 정확하다.

| 🔴 `cmd_fill(…, WATER)` 가 **양 0짜리 물**을 만든다 | `_write_cell` 을 지나 `_aux = 0` 이 된다. 🔴 `cell_materials.gd` 가 **「원리적으로 없다」고 못 박은 상태**인데 있다 ⇒ 20틱 안에 통째로 증발 | ⚠ **맵에 물을 두는 날 지형 굽기가 이 경로를 쓴다** |
| 🔴 `claimed == burning` 은 **유령을 못 잰다** | **상호 일관성**만 잰다 — 「**물 칸이** 파면 소속을 주장한다」일 때도 두 수는 같다 | 🔴 **질의가 없다**: 「`_burning` 안에 WATER 칸이 없나」. **단계 6의 전제다** |
| 🔴 `_band_awake` 를 안 만든다 | **돌긴 다 돈다.** 잠든 격자가 12%를 먹는데 아무도 안 짖는다 | 🔴 **단계 3의 「잠든 `step()` 을 시간으로 잰다」뿐이다.** ⚠ 단계 1엔 그 검사가 없고 **만들 수도 없다** — 아래 |
| 🔴 `_chunk_flip` 을 틱 **끝**에 둔다 | ~~그 자리에서 지워진다~~ → **한 틱 늦는다**(2026-08-07 정정) | 🔴 **`폭발이 깨운다` 가 아니다 — 그건 안 물린다.** **나무를 태워서** 잰다 — 아래 |
| 🔴🔴 상한을 밴드 **안**에서 자른다 | 한 청크의 행 절반만 돈다 ⇒ 결정론이 깨진다. 싱글에선 안 보인다 | 🔴🔴 **잡는 자리가 없다. 아래 상자** |

> 🔴🔴 **이 표에서 「잡는 자리 없음」이 적히는 첫 줄이고, 그 답에 두 번 틀린 뒤에 왔다.**
>
> | 언제 | 뭐라고 적었나 | 왜 틀렸나 |
> |---|---|---|
> | 처음 | `net_determinism` | float·`randi` 를 찾는 **소스 텍스트 스캔**이라 밴드 중간 절단을 **원리적으로 못 본다** |
> | verify-read가 고침 | `net_water` 의 거동 검사(같은 커맨드 열 ⇒ `_aux` 가 바이트 단위로 같다) | 🔴 **이것도 못 잡는다** — 아래 |
>
> **같은 프로세스에서 같은 코드를 두 번 돌리면 「틀린 순서」도 똑같이 틀려서 늘 통과한다.**
> 거동 검사가 재는 것은 **재현성**이지 **정확성**이 아니다. 두 실행이 같은 방식으로 틀리면 초록이다.
>
> ⇒ 🔴 **「한 청크의 행 절반만 돈다」를 막는 것은 순회 구조 자체다. 구조 논증이지 그물이 아니다.**
> **자르는 자리가 밴드 순회 「전」이라는 것**이 유일한 방어이고, **그것을 지키는 것은 코드 주석뿐이다.**
> ⚠ **그러니 이 줄을 옮기는 사람은 그물의 초록을 근거로 삼으면 안 된다** — 그물은 아무 말도 안 한다.
| `_reset` 이 청크를 안 비운다 | `_burning` 과 **똑같은 함정**. 새 무대가 옛 dirty 를 들고 태어난다 | 그물 `_reset 뒤 활성 0` |
| 🔴 `src/sim/` 에 float 이 든다 | `Color`·`sqrt`·`randi`·`Time.` — 평균/나눗셈을 실수로 쓰기 쉽다 | `net_determinism`(폴더를 재귀 스캔한다) |
| ⚠ R을 누르면 지형 전체가 깨어난다 | `cmd_fill` 이 `_write_cell` 을 지나 **약 9,800청크**를 찍는다 ⇒ 상한 512로 **19틱(≈1초)** 밀린다 | 고장 아님. **모르면 「R 뒤에 렉이 있다」로 보고된다** ⇒ 여기 적어 둔다 |
| ⚠ `count_material()` 이 이미 샌다 | HUD가 프레임마다 2회 × 588μs = CPU의 7%. **물이 재료를 하나 늘려도 안 는다** | 물과 별개. 이번 범위 밖 |
| ⚠ `_set` 은 쓰지 마라 | `Object._set` 과 충돌해 **파스가 통째로 실패한다**(대리 구현에서 실제로 났다) | 부딪히면 즉시 보인다 |
| ⚠ 못 부시는 벽만 맞은 폭발은 **청크를 하나도 안 깬다** | `_indestructible` 에 걸려 `_write_cell` 을 아예 안 지난다 | 🔴 **고장이 아니다.** 「`_write_cell` 이 유일한 문」의 당연한 귀결이고, verify-run이 그걸 **음성 대조**로 썼다 |

🔴🔴 **위 세 줄의 「어디서 잡나」가 2026-08-07에 셋 다 틀린 것으로 드러났다**(verify-read의 뮤테이션 34개).
⚠ **위험을 적는 것과 그 위험을 실제로 잡는 검사를 지목하는 것은 다른 일이고, 후자가 훨씬 자주 틀린다** —
이 표는 「잡힌다」고 적혀 있어서 **아무도 다시 안 봤다.** 세부는 「단계 1」의 「검증 결과」 절.

---

### 범위 밖 — 이번에 안 하는 것

본문 「경계」에 더해:

- **「상태 비트 → 색」 표로 만들기** — 단계 4가 파일 넷을 건드리는 원인. 불 경로를 같이 갈아야 한다
- **`count_material()` 호출부 고치기** — HUD의 7%. 물과 독립이다
- **깊이에 따라 색이 연속으로 변하는 것** — 세 번째 텍스처가 필요하다(본문 「화면」)
- **좌우 나눔 규칙을 바꿔 넓은 물도 완전히 멈추게 하기** — 🔴 **질문 1이 (나)로 답해서 확정 제외다.**
  ⚠ 되살아나는 조건은 하나뿐이다: **번개가 와서 「안 멈추는 물」이 다시 비용이 될 때**
- **스테이지1 맵에 물 두기** — 지형 문서의 몫이다(본문 「경계」)

---

## 미정

🔴 **억지로 채우지 않는다**(GDD 「뼈 먼저」).

- ~~**한 틱 청크 상한**~~ → **512로 정했다**(구현 계획 「정한 것」). ⚠ 판정 7로만 확정된다
- ~~**젖음 임계**~~ → **32로 자리만 채웠다.** 🔴 멈춤과 **무관하다는 것을 쟀다** — 순전히 화면 값이라
  판정 6에서 사용자가 정한다
- ~~**판정 1의 기준**~~ → **(나) 「상한 아래로 잠기나」로 정했다**(2026-08-07, team-lead).
  판정 1을 **1-a(좁은 물은 0) · 1-b(넓은 물은 잠긴다)**로 갈랐다.
  🔴 **사용자가 아니라 team-lead가 정했다 — 아침에 뒤집힐 수 있다**
- ~~**물 색**~~ → **`0x2C5F8A` · `Color(0.42, 0.68, 0.85)` 로 자리를 채웠다.**
  ⚠ **아무도 안 봤다.** 판정 6에서 사용자가 고친다
- **물이 불을 끄는 규칙** — 양이 얼마면 끄나 · 물도 같이 주나 (단계 6 차례에 정한다)
- **물 룬이 만드는 양** — 마법 세기마다 다르다는 것만 정해졌다(`SIM_SIZES` 자리)

🔴🔴 **이 문서는 화면 판정(6·7)이 남아 있어 `3.done` 으로 가지 않는다.**
`3.done` 은 「구현이 끝났다」이지 「판정이 통과했다」가 아니지만(CLAUDE.md), **이번 건은 구현
자체가 사용자 판정 둘에 매달려 있다** — 판정 1의 기준과 물 색. 아침에 그 둘이 서면 그때 옮긴다.
