# 탄 머리 그림 — 원을 스프라이트로

**상태**: active — 🔴 **코드까지 들어갔고 사용자가 한 번 봤다. 거기서 멈췄다** (2026-08-05)

**한 줄**: 날아가는 마법 탄의 머리를 `draw_circle` 에서 그림으로 바꾼다.

🔴🔴 **다음 사람이 제일 먼저 읽을 것은 아래 「사용자가 눈으로 본 결과」다.** 지금 코드는
**돌아가지만 사용자 판정에서 실패했고**, 실패 원인과 다음 한 수가 거기 적혀 있다.

## 🔴 사용자가 눈으로 본 결과 — 2026-08-05

게임을 띄워(`Godot_v4.7.1 --path .`) 사용자가 직접 봤다.

> **보고 있긴한데... 이게 빛이 따라가니까 안보임**

**판정: 실패.** 🔴 **글로우(코드가 그리는 무리)가 탄 머리 그림을 덮어서 그림이 안 보인다.**

⚠ **원인은 규격이 아니라 「원 시절의 잔재」다.** `BOLT_GLOW_RATIO`(2.4) · `BOLT_GLOW_A`(0.45)는
머리가 **단색 원**이던 때 「빛나는 것」으로 읽히게 하려고 있던 값이다.
그림에는 **밝은 코어 → 어두운 가장자리 그라데이션이 이미 들어 있어** 자체가 빛이고,
그 위에 반경 19.2px 무리를 가산으로 얹으니 **디테일이 씻겨 나간다.**

⚠ **확인 범위 — 안 본 것:**
- **판정은 「안 보인다」 하나다.** 머리가 실제 위치와 어긋나는지 · 세대1(8px)이 절반으로 그려지는지 ·
  확산 8발이 어떻게 보이는지는 **아무도 안 봤다**
- **물·번개는 화면에 나올 수가 없다** — 시뮬에 그 룬이 없다(아래)
- 스크린샷 없음. 사용자의 말 한 줄이 전부다

## ⇒ 다음 한 수 (금요일 저녁부터)

1. 🔴 **글로우를 빼고 다시 본다.** `spell_view._draw_head` 의 `draw_circle` 한 줄과
   `fx_tuning.BOLT_GLOW_RATIO`·`BOLT_GLOW_A` 를 지운다.
   ⚠ **지우기 전에 세대1을 생각해라** — 8px 그림이 무리 없이 보이는지가 안 쟀다.
   무리가 필요하다면 **알파를 크게 낮추는 것**(0.45 → 0.1대)이 대안이다
2. **탄 속도를 줄인다** (사용자가 「좀 빨라 줄일꺼임」). 아래 「속도를 줄일 때 같이 움직이는 것」
3. 물·번개 룬을 시뮬에 추가 — 그림은 이미 있다

## 끝난 것

| 무엇 | 어디 |
|---|---|
| 그림 넷 뽑고 사용자가 골랐다 | `assets/spell/bolt_{fire,none,water,thunder}.png` (16×16) |
| 자르는 도구 | `tools/pixel/crop_head.py` — 32×16 → 16×16, 밝기² 무게중심으로 코어를 중앙에 |
| 뽑는 프리셋 | `tools/pixel/gen.py` 의 `bolt` |
| 그림 경로 표 | `fx_tuning.BOLT_SHEETS` — 룬 하나 추가 = 한 줄 |
| 그리는 코드 | `spell_view._draw_head` 가 `draw_texture_rect`. `_ready` 에서 한 번 `load()` |

🔴 **규격과 색·시드는 `docs/design/진-룬-문양.md` 의 「탄 머리 그림」이 기준이다.** 여기 겹쳐 적지 않는다.

## 🔴 물·번개 그림은 있는데 화면에 안 나온다 — 빠뜨린 게 아니다

`sim_tuning.ELEM_ALL` 이 **불(0)·무(1) 둘뿐**이라 물·번개 룬이 **아직 없다.**
`BOLT_SHEETS` 에 두 줄이 비어 있는 것은 그 때문이고, `fx_tuning` 주석에 적어 뒀다.

⚠ 룬을 늘리는 것은 **시뮬 작업**이다 — `ELEM_*` 상수 · `ELEM_ALL` · `ELEM_DEFS` 의 `trace`.
🔴 **`trace` 를 `TRACE_NONE` 으로 두면 무속성과 같아진다** — 「흔적을 안 남긴다」가 무의 정의라
그 자리가 겹치면 무가 죽는다. 물은 `TRACE_WET` 가 필요하고 **그건 물 시뮬의 전제다.**

## 잰 것 — 헤드리스, 값으로

⚠ **그물이 아니라 일회용 스크립트로 쟀다.** 아래 「그물이 이걸 안 잰다」를 봐라.

| 잰 것 | 값 |
|---|---|
| `BOLT_SHEETS` 키 ↔ `ELEM_ALL` | 둘 다 `[0, 1]` — 마젠타가 될 룬이 없다 |
| 텍스처 로드 | 둘 다 **16×16**, null 아님 |
| 밝은 픽셀(v>0.25) | 불 **81/256** · 무 **53/256** — 통째로 검정이 아니다 |
| 최대 밝기 | 둘 다 **1.00** |
| 그려지는 지름 | 세대0 **16.0**(원본과 1:1) · 세대1 **8.0**(정확히 절반) |
| 그물 | **통과 1465개 · `[래퍼] 통과. stderr 깨끗함.`** |

## 🔴🔴 그물이 이 변경을 하나도 안 잰다

`character-sprite` 가 실측으로 남긴 그대로다 — **그리는 코드의 뮤테이션이 전부 초록**이었다
(`draw_set_transform` 복원을 지워도 통과 1296개). `spell_view` 도 같은 자리다.

⇒ **「그물이 초록이니 그림이 그려진다」로 읽으면 거짓이다.** 위 표는 따로 잰 값이고,
**화면은 사용자가 한 번 본 것이 전부이며 그 판정은 실패였다.**

⚠ **그물을 짤 수 있는 자리는 있다**: `BOLT_SHEETS` 의 키가 `ELEM_ALL` 을 덮나 ·
그림이 16×16 인가 · **밝은 픽셀이 0이 아닌가**(`character-sprite` 가 「통째로 투명한 시트가 초록」
구멍을 찾은 그 검사다). 🔴 **셋 다 아직 안 짰다.**

## 속도를 줄일 때 같이 움직이는 것

```
프레임당 이동 = speed × 4px × 20Hz ÷ 60fps = speed × 1.33px
그림 꼬리 16px 과 같아지는 지점 ⇒ speed = 12
```

🔴 **`speed` 를 12 아래로 내리면 「꼬리는 코드가 그린다」는 근거가 뒤집힌다** —
그때는 그림 꼬리가 화면에 보이기 시작하므로 **꼬리를 그림에 넣는 쪽이 다시 후보가 된다.**
⚠ 꼬리 있는 원본(32×16)이 `tools/pixel/out/` 에 있지만 **gitignore 라 사라진다** —
`docs/design/진-룬-문양.md` 의 시드 표로 다시 뽑아야 한다.

🔴 **사거리가 제곱으로 준다.** 포물선 사거리가 **v²/g** 라 speed 절반이면 사거리 **1/4**이다.
⇒ **중력을 같이 안 내리면 조용히 틀린다**(`sim_tuning` 주석의 「하나만 올리면 조용히 틀린다」).

🔴 **대신 GDD 「알고 가는 대가」 5번이 풀린다** — 「폭발이 화면 밖에서 터진다」(사거리 40타일 >
보이는 폭 30타일). GDD가 적어 둔 세 후보(줌아웃 · 무대 축소 · **사거리 축소**) 중 셋째다.

⚠ **자취도 짧아진다** — `trail_ticks` 12틱 × speed. speed 20에서 960px이던 것이 10이면 480px.

## 판정 — 무엇을 봐야 끝났다고 하나

1. 🔴 **탄 머리 그림이 보이나** — **2026-08-05 실패**(위)
2. **머리가 실제 위치와 어긋나지 않나** — 코어를 무게중심으로 맞췄지만 눈으로 안 봤다
3. **세대1(확산 8발)이 절반 크기로 보이나** — 안 봤다
4. **불과 무가 화면에서 갈리나** — 🔴 **무를 무채색으로 골라서 「못 쏜다」의 회색과 색축이 겹친다.**
   사연은 `진-룬-문양.md` 의 「알고 깨는 계약」
5. **꼬리(코드 자취)가 머리와 이어져 보이나** — 그림 꼬리를 없앴으므로 자취가 꼬리의 전부다
