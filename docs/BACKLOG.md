# BACKLOG — 아직 안 한 것 · 다듬을 것

> 세션 7 신설 (2026-07-13). **확정 스펙은 GDD, 계약은 TECH_SPEC, 현재 상태는 STATUS.**
> 여기는 **아이디어와 미결 항목**만 — 확정되면 GDD로 승격하고 여기서 지운다.
>
> 🔴 **아래 세션 13 표는 옛 자유드로잉 기준이다** (세션 21에 그 시스템은 삭제됐다) — 사실로 믿지 마라.
> **지금 유효한 TODO = 바로 아래 「고리 모델의 미결」뿐이다.**

---

## ✅ 숲 원정 — **세션 26에 만들었다** (F1·F2·R4 완료)

> 사용자: *"마법을 사용할 사용처가 필요할꺼같은데?"* → 이제 있다.
> **베이스 왼쪽 숲길 [E] → 숲(슬라임 7) → 들어온 자리 [E] → 베이스.** 죽으면 그냥 베이스로.
> 상세는 `docs/STATUS.md` 「세션 26」이 정본. 아래 표는 이력으로만 남긴다.

| # | 항목 | 상태 |
|---|---|---|
| **F1** | 숲 씬 + 적(쫓아오고 접촉 피해) + 귀환 지점 + 죽으면 베이스로 | ✅ **세션 26** — `src/field/` |
| **F2** | 베이스에 [E] 숲으로 출구 · 숲 ↔ 베이스 왕복 | ✅ **세션 26** — 실제 게임에서 왕복 확인 |
| **R4** | 발사·조준·슬롯 로직이 base와 field에 중복될 뻔했다 | ✅ **세션 26** — `src/actors/player_caster.gd`로 뽑았다 (복사했으면 `to_assembly()` 함정까지 복사됐다) |

### 🔴 숲 원정이 남긴 미결 — **다음 세션**

| # | 항목 | 상태 |
|---|---|---|
| **F3** | ✅ **저장이 돈다** (세션 26 — 사용자 확정: *"켜면 이어서 한다"*) | `SaveManager._ready` → `load_game()`. 그전엔 `load_game()`을 **아무도 안 불러**(세션 21에 부팅 흐름 삭제) `_ready_to_save`가 영원히 false라 **모든 저장이 조용히 return**했다 → 껐다 켜면 그린 마법진이 사라졌다. 🔴 **테스트가 이 버그를 못 잡은 이유**: `test_save_auto`가 `load_game()`을 **직접 불러** `_ready_to_save`를 켜 줬다 — 게임은 아무도 안 부르는데. 이제 **`load_game()` 호출 전에** 회귀 확인이 있다(순서가 곧 검출력). ⚠ **새 게임 수단이 없다** — 세이브를 지우려면 `SaveManager.wipe_save()`뿐이다 |
| **F8** | 🔴 **「새로하기」가 없다** (세션 26에 저장을 켜면서 생긴 빈칸 — 사용자: *"나중에 새로하기 할 때 까먹을 수 있으니까"*) | **아래 절이 정본.** `wipe_save()`는 **새로하기가 아니다** — 실측으로 확인했다 |
| **F7** | ⚠ **씬끼리 PackedScene으로 물지 마라 — 경로를 써라** | 세션 26에 base⇄forest **순환 preload**로 숲의 `base_scene`이 **노드 0개 껍데기**가 돼 **귀환·사망해도 베이스로 못 돌아갔다**(`Parameter "new_scene" is null`). 🔴 **헤드리스는 절대 못 잡는다** — 테스트가 forest.tscn을 먼저 로드하면 base preload가 멀쩡히 끝난다. **베이스로 부팅한 실제 게임에서만** 깨졌다. 지금은 `@export_file`+`change_scene_to_file`(백로그가 원래 그렇게 적어 뒀었다). 새 씬을 이을 때 되살아난다 |
| **F4** | **원정 손맛** — 슬라임 7마리·속도 55(내 속도 120)·접촉 피해 4가 맞나 | 🔴 **사용자가 직접 해 봐야 정해진다.** 리드의 측정은 시뮬레이션이다(세션 25가 이 방식으로 8건을 찾았다). 수치는 전부 `data/enemies/slime.tres` — **새 적 = .tres 한 장** |
| **F5** | **필드 밖 회색 void** — 벽이 없어 걸어 나가면 바닥이 끝난다 | Ground를 넉넉히(-1200..1200 × -1000..900) 깔아 **입구 근처에선 안 보이지만**, 계속 걸으면 나온다. 벽이냐 카메라 limit이냐 미결. ⚠ 나무를 StaticBody2D로 만들지 마라 — world 레이어라 **캐리어(마스크 5)가 나무마다 터진다** |
| **F6** | ✅ **드롭 — 세션 27에 붙였다** | `forest_enemy._die()`가 `EnemyDef.drops`를 굴려 `add_to_bag`. 인벤은 이미 다 배선돼 있었다(가방→창고 회수·사망 상실) — **넣기만 했다.** 🔴 **익스트랙션 루프가 처음 닫혔다**: 잡기→가방→귀환하면 창고·죽으면 증발. 룬 조각은 이 풀에 안 들어간다(보스 퀘스트 보상). 신규 테스트 [8][9] |

---

## 🔴 경제 흐름 — 세션 27에 지도를 그렸다 (사냥→획득→가공→그리기)

> 사용자: *"일반몹이 뭘주고 그걸로 뭘하고 이런 흐름을 정해야할듯"* → GDD §5·§6에 **이미 설계돼
> 있다**(상점 없음 · 재료→정제·제작 · 보스 탁본으로 룬). 문제는 "정하기"가 아니라 **부품은
> 다 있는데 사이 배선이 비었다**. `godot_exec`로 실측·정리했다.

| # | 항목 | 상태 |
|---|---|---|
| **E1** | ✅ **드롭** (=위 F6) | 세션 27 |
| **E2** | ✅ **창고 보기 UI** — 세션 28에 만들었다 | `src/hud/inventory_panel.gd`(+`.tscn`). **`I` 키 토글 모달**, 베이스·숲 공용(덕코프 스타일). 창고(영구)+가방(있을 때) 두 구역·등급색·종류·★·수량. `get_inventory_snapshot()`을 이제 쓴다. 🔴 **`ui_modal_open`을 처음 배선** — player·caster가 폴링해 열린 동안 멎고, 책(base.gd)도 이 플래그를 켜 겹침 방지. 닫히면 `visible=false`라 클릭이 안 먹힌다(`test_base_auto` 좌클릭 PASS = 회귀 가드) |
| **E3** | ✅ **재료의 쓸 데 — 세션29에 배선 완료**(가공 고리가 닫혔다) | **배선 끝 · 튜닝/희소성 남음.** 슬라이스: 재료태깅✅ → 잉크선택(색)✅ → 잉크등급=위력✅ → **정제대(레시피 데이터+GameState.spend/add)✅** → **특별잉크 그리는 중 실시간 소모+화상 증폭 효과✅** → **종이=규모(확대 상한↑→큰 진=데미지↑)✅**. 🔴 **확정 계약**: 잉크등급=피해 축 · 특별잉크=상태(화상) 축 · 종이=크기 축(피해에도 기여). 데미지 조합=`ring_power.power_of` 한 곳, id→배수 리졸버=Db. 정제대=`src/base/refine_panel`, 레시피=`data/recipes/*.tres`(새 레시피=.tres 한 장). ⚠**남은 것**: (1) 🔴 사용자 플레이 확인(헤드리스는 클릭·레이아웃 못 봄) (2) 밸런스(잉크간격·종이 세기·정제비용·소모속도·화상×1.5 — 직접 쏴 봐야) (3) 잉크·종이 무한/持有라 **아직 상급이 최적** → 소모·희소성으로 조이면 진짜 선택. 중첩 마법진 담을 공간·장비제작·밥·물은 미구현 |
| **E4** | ✅ **룬 해금 — 세션 34에 완료** (보스→조각→탁본 해독→새 룬을 그린다) | 🔴 부품은 다 있었는데 **룬 사슬이 전 구간 불로 고정**돼 있었다: `ring_assembly`가 `RUNE_FIRE`, 발사가 `Db.get_rune(FIRE)`를 하드코딩해 물을 그려도 불로 맞았다. 뚫었다(assembly `_rune`·`ring_board.choose_rune(type)`+룬별 밑그림·`ring_book` 룬탭 다중셀·발사가 `assembly.rune`). `slime_elite`를 숲에 넣고 `fragment_water` 확정 드롭, 베이스 새 **탁본 해독대**(`decode_panel`)에서 조각 소비→`codex_unlocked`→룬 해금. 🔴 **시작=불만, 물·바람은 해독으로만**(사용자 확정). `RuneDef.unlock_id` 신설. 신규 `test_decode_auto`. **남음**: gale 풀 보스 AI · 바람 조각을 줄 보스 · 손맛(밑그림·강도·드롭). 정본 = STATUS 「세션 34」 |
| **E5** | ✅ **적 5종 다양화 + 확률 드롭 — 세션 30에 완료** | 숲에 **비엘리트 5종 8마리**(슬라임·안개·사냥개·덩굴·갑충, `forest.tscn`에 `enemy_id` 오버라이드). 드롭이 E3 재료 전 갈래로 흐른다(잉크재료=슬라임/안개 · 종이재료=덩굴 · 장비재료=사냥개/갑충 + 낮은 확률 장비 charm/wand/robe). 🔴 **외형도 .tres가 쥔다** — `forest_enemy._apply_look()`이 `params.color`·`params.size`를 Visual/scale에 반영(새 적=.tres 한 장이 생김새까지). ⚠ **표시일 뿐 AI는 하나**(추격+접촉) — 사냥개=빠름·덩굴=제자리·갑충=느린 탱커(스탯 손맛). 🔴 **엘리트(gale·slime_elite)는 안 넣음** — fragment_*는 일반 드롭이 아니라 E4 보스 보상. `test_forest_auto` [8]을 로스터 합집합 검사로 재작성(뮤테이션 검출력 확인). ⚠ **남은 것**: 드롭률·적 강도·로스터는 직접 싸워 봐야 · 크기 조정된 적 마법 피격 테스트 미검증(표준 동작이라 수용) |

📊 **흐름 지도**: 세션 27에 아티팩트로 그렸다 (사냥→획득→가공→그리기 4단계 · 있음/부품만/빈칸).
지금 **03 가공(E3)이 통째로 비어 고리가 안 닫힌다** — 잡아도 갈 데가 없다.

---

## 🔴 F8 — 「새로하기」 (세션 26 신설. **손대기 전에 이 절을 다 읽어라**)

> 세션 26에 저장을 켰다(F3 — *"켜면 이어서 한다"*). **그 대가로 「처음부터」가 사라졌다**:
> 부팅이 무조건 세이브를 이어 받는데 **버릴 길이 없다.** 사용자가 기록을 요청한 항목이다.

### 🔴 `wipe_save()`는 새로하기가 **아니다** — 세션 26에 실제로 돌려 확인했다

```
① 진행을 만들고 저장   → 보관 1 · 잉크 9 · Day 7 · 세이브=true
② wipe_save() 직후    → 세이브파일=false · 그런데 **메모리엔 보관 1 · 잉크 9 · Day 7 그대로**
③ 귀환 한 번          → 세이브파일=true  ← **옛 진행이 도로 써진다**
```

`wipe_save()`는 **파일만 지운다**(save.json + `user://save/rings/ring_*.tres`). `GameState`·`Clock`은
**오토로드라 메모리에 그대로 살아 있고**, 다음 자동 저장(귀환·사망·하루시작)이 그걸 그대로
다시 쓴다. **에러도 경고도 없다** — 새로하기를 눌렀는데 조용히 안 된 것처럼 보인다.
⚠ `wipe_save()`의 주석은 *"테스트·디버그용"*이라 적혀 있다. **그 말이 맞다. 새로하기에 쓰지 마라.**

### 그래서 진짜 새로하기가 지워야 할 것 = **저장이 담는 것 전부**

`save_manager.save_game()`이 쓰는 8종이 곧 목록이다 (여기 베끼지 말고 **그 함수를 열어 맞춰라** —
필드가 늘면 이 표가 먼저 낡는다):

| 어디 | 무엇 | 새 값 |
|---|---|---|
| `GameState` | `inventory` · `equipment` · `codex` · `ring_designs` · `ring_equipped` · `mana` | 비우기 / `mana_max()` |
| `GameState` | `bag` · `hp` | 비우기 / `hp_max()` (저장엔 없지만 판의 상태다) |
| `Clock` | `day` · `time_sec` | 1 / 0.0 |

🔴 **그리고 시작 해금을 다시 심어야 한다** — `GameState._ready`가 `codex[&"rune_fire"]`와
`codex[&"glyph_thrust"]`를 심는데, **새로하기는 `_ready`를 다시 안 탄다**(오토로드는 부팅에 한 번).
안 심으면 **아무것도 못 그리는 새 게임**이 된다.

### 설계 판단 (안 정했다 — 사용자 몫)

- **어디서 누르나?** 지금 **타이틀 화면이 없다** (부팅 = 곧바로 베이스). 후보: 타이틀 씬을
  새로 만들기 / 베이스에 메뉴 / 개발용 키 하나. ⚠ 타이틀을 만들면 **부팅 흐름을 다시 짜는
  일**이라 F3(`SaveManager._ready`의 `load_game()`)이 그리로 옮겨 가야 한다
- **덮어쓰기 경고** — 이어할 세이브가 있는데 새로하기를 누르면 되돌릴 수 없다(자동 저장이라
  즉시 굳는다). 확인을 물을지
- 🔴 **구현 방식은 `GameState.new_game()`을 core에 하나 두는 게 맞다** — 씬마다 필드를 손으로
  비우면 **필드가 늘 때마다 한 곳만 고치고 갈라진다**(ring_power를 core에 둔 것과 같은 이유).
  그 함수가 `_ready`의 시드와 **같은 코드를 부르게** 해라 — 두 벌이면 시드가 갈라진다

---

## 고리 모델의 미결 (세션 25~, 현행)

| # | 항목 | 사용자 원문 | 상태 |
|---|---|---|---|
| **R1** | 🔴 **잉크·종이·마나 소모** — 그리는 데 재료가 든다 | *"잉크랑 종이가 사용됨 ㅇㅇ 사용할때 마나도 들고 TODO야"* (2026-07-17) | **나중에 할 것으로 확정.** 지금은 그리기가 공짜다 — 재료가 붙으면 "막 그려도 손해가 없다"가 사라진다. ⚠ 옛 자유드로잉의 잉크 경제(`balance.tres`의 `ink_per_stroke_length` 등)는 스키마에 **껍데기만 남아 있다**(아무도 안 읽음) — 되살릴지 새로 짤지는 그때 판단 |
| **R2** | **문양 모양이 빈약하다** — 결과물이 "원 + 삼각 + 작대기"라 마법진으로 안 보인다 | 사용자: *"융합해서 예쁜 마법진"* · *"지금 살짝 노잼"* (2026-07-17) | 세션 25에 조립 모드를 만들어 견줘 봤지만 **모드와 무관한 문제**로 판명됐다(조립으로 100점을 내도 똑같이 안 예쁘다). 세션 25에 문양이 **작대기 → 화살표**가 되고 크기도 붓보다 커졌지만(R2a 참조) 어휘는 여전히 둘(응집←/발산→)뿐이다. ⚠ [[takbon-glyph-design-principle]] — **어휘를 늘리지 말고 조합을 늘려라** |
| **R2a** | 🔴 **문양 그리는 손맛이 어색하다** (세션 25 말, 사용자가 직접 그려 보고) | *"문양쪽 그리는게 조금 어색한느낌이긴 한데 다음으로 가도 될듯? 그래서 차차 사용감 좋아지게 만드는거지"* (2026-07-17) | **사용자가 "지금은 넘어가고 차차 고치자"로 확정.** 세션 25에 문양은 크게 바뀌었다(작대기→화살표 · 크기 0.12→0.17 · 깃 0.34→0.62 · 칸 클릭 선택 · 여러 획). ⚠ **어색함의 정체는 아직 안 밝혀졌다** — 후보: 화살표가 칸에 비해 큰가 · 한붓그리기 가이드(머리→깃→머리→깃)가 손 가는 순서와 다른가 · 칸 클릭과 긋기가 한 동작으로 안 붙나 · 8칸을 하나씩 그리는 게 지루한가. 🔴 **다음에 손댈 땐 사용자에게 "어디가 어색한지" 먼저 물어라** — 리드가 짐작으로 고치면 또 딴 데를 판다(세션 25에 실제로 그랬다) |
| **R3** | **손맛 튜닝** — 판정 반경·65점 기준선이 맞나 | — | 🔴 **사용자가 직접 그려 봐야 정해진다.** 헤드리스·좌표 재생은 이탈이 0이라 **뭘 바꾸든 정밀도 100**이 나온다(리드가 세션 23에 실제로 이 착각을 했다). ⚠ 손잡이 3개(`ACC_TOL_FRAC`·`CONSIDER_FRAC`·`REVEAL_RADIUS_FRAC`)가 `trace_scorer.gd`에 **const로 박혀 있다** — "수치는 balance.tres" 규칙 위반이고, 튜닝을 반복하려면 balance로 올리는 게 낫다 |

---

## ✅ 정비 세션 (2026-07-15, 세션 13) — 아래 표는 **전부 처리 완료**

> 이 표의 미정 9건(+파생 B3)은 세션 13에서 하나씩 물어 확정했다. **결과·현재 스펙은 `docs/TRUTH.md`
> §4 로그 + §1~3, 요약은 `docs/STATUS.md` 세션 13.** 아래 원본 표는 **당시 진단의 기록으로만** 남긴다
> (지웠다가 같은 진단을 다시 하느니 남겨 둔다 — 이 repo의 방식). **더는 "다음 세션 최우선"이 아니다.**
> 다음 = 만드는 세션, 1순위 **룬 N개 + 중첩 진**.

### A. 문양 — **대원칙(§4.3)에 비추어 판단할 것**

> **"룬을 감싸는 작은 도형마다의 힘이 있고, 그걸 조합하는 자유도가 목적."**
> **어휘를 늘리지 말고 조합을 늘려라** — 이 셋 다 그 잣대로 답이 갈린다.

| # | 미정 | 선택지 | 지금 상태 |
|---|---|---|---|
| A1 | **팅김·유도·관통 획도 착탄 충격파를 뿜는가?** (사용자: *"아직 미정"*) | (a) 곧은 화살표만 (현행) — 그린 것과 나가는 것을 눈으로 재기 쉽다 / (b) 전부 뿜는다 — 한 획이 **두 층**(비행·착탄)을 정해 자유도↑, 대신 읽기 어려워진다 | (a)로 구현됨 |
| A2 | **증폭·응집이 v2.1에 흡수됐는가?** | (a) 흡수됐다 → **어휘 표에서 지운다** (모으면 응집·펼치면 증폭이 이미 나온다) / (b) 별도로 만든다 → **어휘가 둘 는다. 대원칙에 걸린다** | GDD 어휘 표에 **"미구현"으로 이름만** 떠 있다 (§4.3) |
| A3 | **충격파·기둥 밸런스** (속도·수명·피해·기둥 반경) | 손맛 문제 — **F6 시험대에서 만져 보고** 정한다 | balance 노브 9종, 초기값 |

### B. 룬

| # | 미정 | 선택지 | 지금 상태 |
|---|---|---|---|
| B1 | **룬 상극(불+물) 증발이 벌인가 보상인가?** (사용자: *"전투에서 확인할 듯… 그 자리에 수증기가 남는 느낌"*) | (a) 벌 — 둘 다 사라진다 / (b) 보상 — 증기(새 속성) / (c) 적에게서 터진다 (상태이상 레이어) | **룬 N개가 없어 발생할 수 없다** |
| B2 | **룬 N개를 언제 구현하나** | 진 안에 종류별 하나씩. **$P가 꼭 필요한가?** (룬은 1획이라 $1로도 여러 개 될 수 있다 — 확인 필요) | 스칼라 3개(`rune_type`·`fill`·`accuracy`). 세이브 마이그레이션 1회 필요 |

### C. 지팡이

| # | 미정 | 선택지 | 지금 상태 |
|---|---|---|---|
| C1 | **지팡이 수급** (사용자: *"일단 지금은 그냥 지팡이만"*) | (a) 필드 드롭 / (b) 거점 제작 / (c) 둘 다 (종이·잉크 공식) / (d) 탁본으로 배운다 | 3종 존재, **시작 장비 외엔 얻을 길이 없다** |
| C2 | 🔴 **등 뒤 견제 패턴이 사라졌다** | v1.5~v1.9엔 문양을 아래로 그어 만드는 견제탄이 있었다. **회전이 없어진 지금 지팡이가 채워야 할 자리** — 후방 패턴 지팡이? | 세 지팡이(단발·산탄·전방위) 어디에도 없다 |

### D. 진

| # | 미정 | 선택지 | 지금 상태 |
|---|---|---|---|
| D1 | **진 모양(별·용)이 이제 무엇을 정하나** | v1.8의 "출구 지형 → 전방위"는 **지팡이가 가져갔다.** 남은 건 **탄의 생김새**뿐인데 — 히트박스도 별 모양이 되나? 그게 재미있나? | **원 하나.** $P 전제라 미뤄져 있다 |
| D2 | **중첩 진** (사용자 아이디어 — 진 안에 진, 룬을 각각) | 열려면 `circle_radius`가 배열이어야 한다. **룬 N개 마이그레이션 때 같이 열면 공짜** | 미구현 |

### E. 인식기

| # | 미정 | 선택지 | 지금 상태 |
|---|---|---|---|
| E1 | **$1 → $P 교체를 언제 하나** | 🟢 **더 이상 선행 조건이 아니다** (v2.0이 별 진의 이유를 없앴다). **진 모양·다획 글자를 원할 때** 하면 된다 | $1 유니스트로크 (한붓그리기 전용) |

**⚠ 정비 세션이 아닌 것**: 새 문양·새 지팡이·새 시스템을 **만들지 않는다.** 확정만 한다.
만들 것이 정해지면 **그다음 세션**에서 만든다.

---

## 0. ✅ **역할 축 재설계** — 해결됨 (세션 12, v2.0 — GDD·TECH_SPEC 승격 완료)

> 세션 11이 "사용자 고민 중"으로 남긴 이 항목에 사용자가 답했다: **"마법 발사를 그냥 지팡이로
> 대체하자."** 아래 진단(방향·기점이 문양 표 밖에서 새고 있었다는 것)이 정확히 v2.0의 출발점이
> 됐다 — **방향+기점을 지팡이라는 새 부품으로 떼어냈다.** 상세 계약은 TECH_SPEC §4.0-a·GDD
> §4.0(v2.0)을 볼 것. 이 절은 **진단의 기록으로만 남긴다** (지웠다가 같은 진단을 다시 하는 것보다
> 왜 그 결론에 도달했는지 남겨 두는 편이 낫다).

~~**지금 최우선 미결.** 다른 어떤 구현보다 먼저 정해져야 한다 — 진 모양·룬 N개보다도.~~

### 축 표가 거짓말을 하고 있다

| 부품 | 표에 적힌 것 | **실제로 정하는 것** |
|---|---|---|
| **진** | 규모 (1개) | 위력 · 탄 크기 · 사거리 — **3개를 혼자.** 갈라 쓸 수 없다: 큰 위력을 원하면 큰 탄과 긴 사거리가 **딸려온다** |
| **룬** | 속성 + 농도 (2개) | 그대로 (2개) — 유일하게 정직하다 |
| **문양** | 방식 + 세기 (2개) | + **방향** + **기점** = **5개.** 축 밖에서 새고 있다 |

사용자: *"앞뒤옆 이게 좀 복잡함"* — **복잡함의 정체가 이것이다.** 그리고 **방향은 그림으로 읽기
어려운 축**이다: 거꾸로 그으면 정반대로 날아가는데 그린 사람은 모른다 (세션 11에 실제로 겪음 →
화살촉을 붙여 완화했으나 **축 자체는 그대로**).

### 아무도 안 정하는 것 — 빈 칸

| 요소 | 지금 | 비고 |
|---|---|---|
| **속도** | `projectile_base_speed` 260 **고정** | 느린 탄/빠른 탄이 없다. **빈 자리가 정확히 여기 하나** |
| ~~**착탄 후 처리**~~ | ~~아무 일도 안 일어난다~~ | ✅ **v2.1에서 채워짐** — 곧은 화살표(BASIC) = 충격파 하나가 가는 방향. 충격파끼리 만나면 기둥(규칙이 아니라 결과). GDD §4.3, TECH_SPEC §4.0-b |
| **히트박스 모양** | **언제나 반지름 5의 원** (크기만 진 규모로 배율) | 그린 획은 그 위에 얹힌 **그림일 뿐**. **보이는 모양과 맞는 모양이 다르다** |
| 화상 지속시간 | 3초 고정 | 세기만 변하고 길이는 안 변한다 |
| ~~여러 발의 퍼짐~~ | ~~없음 — 발수를 늘려도 각자 그린 방향으로~~ | **v2.0에서 채워짐** — 지팡이 패턴(단발·산탄 24도·전방위 8발)이 퍼짐을 정한다. GDD §4.0 |

### 사용자가 짚은 뼈대 — **두 층** — ✅ 해결됨 (세션 12 후반, v2.1)

> *"날아가는 처리랑 맞은 다음에 퍼짐 처리가 고민이네"*

- **날아가는 동안** → **문양**이 정한다 (팅김·유도·관통) ✅ 채워져 있다
- **맞은 다음** → ~~🔴 비어 있다~~ ✅ **v2.1에서 채워졌다** — GDD §4.3 "착탄 후" 절, TECH_SPEC §4.0-b

~~**룬이 그 빈 칸에 맞는다.**~~ **예상이 빗나갔다 — 실제로는 문양(BASIC)이 채웠다.** 당시 가설은
속성별 착탄 연출(아래 표)이었는데, 사용자가 다시 짚었을 때 나온 답은 **"화살표 하나하나가 착탄 후
충격파 하나를 갖는다"**였다 — 문양 축 안에서 해결됐다. 아래는 **폐기된 가설**로 남긴다 (왜 이
방향을 검토했었는지 기록):

| 룬 | ~~착탄 후~~ (폐기된 가설 — 실제로는 룬이 아니라 문양이 채웠다) |
|---|---|
| 불 | 터진다 (범위 폭발) |
| 물 | 고인다 (장판) |
| 충격 | 밀어낸다 (넉백은 이미 있다) |
| 바람 | 빨아들인다 / 흩어진다 |

⚠ 이 룬별 착탄 연출 아이디어 자체가 죽은 것은 아니다 — **화살표 충격파와 별개로 룬이 여전히 상태
이상(§4.2)을 담당**하므로, 룬마다 다른 착탄 비주얼/부가 효과를 나중에 얹는 것은 여전히 가능하다.
다만 "맞은 다음의 방향/구조"라는 원래 빈칸은 **문양이 채웠다.**

**규칙을 안 늘리고 빈 칸만 채운다.** 축이 이렇게 갈린다 (v2.1로 갱신):
**진 = 얼마나 크게 / 룬 = 무엇으로 / 문양 = 어떻게 날아가서 + 맞은 다음 어디로**

### 미결 (사용자 결정 필요)

1. ✅ ~~**방향을 마우스에 넘길 것인가**~~ → **v2.0: 넘겼다.** 문양은 "효과 + 세기" 둘로 깨끗해졌다
   (§4.3). 잃은 것도 정확히 예상대로였다 — *"뒤쪽으로 견제탄 심기"* 같은 배치 설계가 사라졌고,
   세 지팡이(단발·산탄 24도·전방위 8발) 중 후방 패턴이 아직 없다 (GDD §4.0, STATUS 세션 12 미해결)
2. **속도를 무엇에 물릴 것인가** — 아직 미결. `reach`(길이)는 이미 사거리·반사수를 정한다. 길이에
   속도까지 얹으면 **길이 하나가 셋을 정하게** 된다 (또 다른 과부하). 후보: 필압 · 그은 속도 · 획 굵기
3. **퍼짐 범위를 무엇이 정할 것인가** — **v2.1이 "어느 방향인가"는 답했지만("착탄 후 처리" 자체는
   더 이상 빈 칸이 아니다, §0 위), "얼마나 넓게/오래"는 여전히 미결이다.** 지금은
   `balance_data.gd`의 충격파 4종·기둥 5종 노브가 값을 쥐고 있을 뿐 — 진 규모(큰 마법 = 큰 폭발)에
   물릴지, 룬 농도에 물릴지는 아직 안 정했다
4. **진이 3개를 혼자 쥔 것을 그대로 둘 것인가** — **v2.0에서 답이 나왔다: 그대로 둔다.** 위력·
   크기·사거리는 이제 "규모"라는 하나의 개념으로 묶여 있고(§4.1), 갈라 쓸 필요 자체가 없어졌다 —
   갈라 쓰고 싶으면 **진을 여러 개**(중첩 진, 아래 §1-6) 쓰면 된다는 쪽으로 정리됐다

---

## 1. ✅ **"문양이 너무 빈다"** — 해결됨 (v1.9). 남은 건 **구현 순서**다

> 이번 주 목표: **마법진을 만들어서 원하는 대로 출력하기.** 여기가 그 핵심이었다.

### 결론 — 세 축이 다 찼다 (GDD v1.9로 승격 완료)

원래 진단은 "문양만 정체성이 없다"였는데, **실은 축 배분이 통째로 어긋나 있었다.**
세션 8~10에서 사용자와 함께 재배분해 **전부 채웠다.**

| 부품 | 모양이 정하는 것 | 크기가 정하는 것 | 상태 |
|---|---|---|---|
| **진** | **구조** — 원·별·용 | **규모** — 위력·탄 크기·기준 사거리 | 규모 ✅ 구현됨(v1.6) / **구조 ⚠ 미구현 — $P 선행** |
| **룬** (N개) | **속성** — 원소·상태이상 | **농도** — 상태이상 세기 (÷ 진) | 농도 ✅ 구현됨(v1.7) / **N개·배선 ⚠ 미구현 — $P 선행** |
| **문양** (N개) | **발동 방식** — 팅김⚡·유도∿·관통‖ | **세기** — 사거리 배율·반사 횟수 (÷ 진) | **✅ 전부 구현됨 (v1.9, 세션 10 — 커밋 a9d6146)** |

**핵심 통찰 (전부 규칙을 안 늘리고 나왔다):**
- **"진을 뚫고 나가는가"가 곧 의미의 경계다.** 나가는 획 = 탄, 안 나가는 획 = 속성.
  ⚠ 단 이건 **종이 위의 문법이지 탄의 행동이 아니다** (v1.9) — 팅김도 획은 곧게 뚫고 나가지만
  **탄은 벽에 튕긴다**. v1.8까지 문양이 직선뿐이라 두 층이 겹쳐 보였을 뿐이다
- **진의 모양이 곧 출구의 지형이다.** **별 진 = 전방위.** 별을 그리면 별처럼 퍼진다
- **진 안은 룬만.** 구조 글자를 진 안에 넣는 안은 기각 (진은 좁고, 룬과 농도를 깎아먹는다)
- **세 부품 전부 "모양 = 무엇을 / 크기 = 얼마나"** (v1.9). 배울 규칙이 셋이 아니라 **하나**다.
  그리고 크기는 셋 다 **진 대비 비율** — 잉크 상한이 자연히 제동을 건다
- **룬의 위치가 룬과 문양을 잇는다** (v1.9 배선) — 왼쪽 룬이 왼쪽 문양을 지배한다

### ✅ 끝난 일 — **문양 글자 + 세기** (v1.9, 세션 10 · 커밋 a9d6146)

`glyph`(팅김⚡·유도∿·관통‖·기본) + `reach`(획 길이 ÷ 진 반지름). **죽어 있던 문양 길이가 살아났다.**
`magnitude`는 v1.6에서 "소비 금지"로 묶어 둔 필드인데 여기서 되살아났다 (**위력이 아니라
사거리**라 축 위반이 아니다). 손그림 인식률 100% · 사용자 플레이 확인 완료.

> 🔴 **순서를 바꿨다 — 아래 "1번이 모든 것의 전제"는 문양엔 해당되지 않았다.**
> $P를 먼저 갈라는 근거는 *"1획용으로 구부린 글자는 다획에서 다시 그려야 한다"*인데
> **팅김·유도·관통은 애초에 1획이 자연스럽다.** 구부릴 게 없었다.
> **다획이 진짜로 필요한 건 진(별·용)뿐이다.**
> **교훈: 문서의 결론이 아니라 그 근거를 확인할 것.**

### 🔴 남은 일 — **이 순서를 지켜야 한다** (v2.0에서 급함이 내려갔다 — §0 참조)

> 🟢 **세션 12 갱신**: $P가 급했던 유일한 이유(별·용의 진)의 존재 이유가 "전방위"였는데, v2.0에서
> 전방위가 지팡이로 넘어갔다 (§0, GDD §4.0). **원 하나로도 이번 주 목표가 굴러가므로, 아래는
> "다음 세션 최우선"이 아니라 "언젠가 할 일"로 내려갔다.** 지금 다음 작업은 증폭·응집 문양 구현이다
> (STATUS 세션 12).

**1. 인식기 $1 → $P 교체** (**진 모양·룬 N개의 전제** — 급함은 아니다)
- **$1은 유니스트로크 = 한붓그리기 전용.** **별 진·용의 진을 원리적으로 그릴 수 없다**
- **$P** (Vatavu·Anthony·Wobbrock 2012) — 획을 점 구름으로 본다. 다획 지원, 회전 불변 on/off,
  $1보다 견고, 템플릿 형식이 비슷해 **매칭 함수 교체 수준**
- 🔴 **진짜 비용은 인식이 아니라 UX**: **"이 획이 앞 글자의 연장인가 새 글자인가"** —
  1획 시절엔 없던 문제다. 시간(멈추면 끝)·위치(겹치면 같은 글자)·명시적 입력 중 택일.
  **잘못 끊으면 그린 대로 안 나온다 = 게임 정체성이 무너진다** (GDD 리스크 8)
- ⚠ **회귀 위험**: 룬 4종은 지금 **99.5%**, 문양 4종은 **100%**다. $P가 이 수치를 떨어뜨리면
  안 된다 — 교체 후 **양쪽 스트레스 테스트를 다시 돌려 실측할 것**

**2. 진 모양 축** — 원 → 원·별·(용). 닫힌 도형이면 원이 아니어도 된다 (안/밖 판정은 그대로 성립).
   **v2.0에서 의미가 바뀌었다** — 별을 그려도 더 이상 "전방위 출구"가 아니라 **별 모양 탄**이 나간다
   (GDD §4.1). 히트박스를 원 아닌 모양으로 어떻게 잴지는 미결

**3. 룬 N개** — 스키마를 `Array[RuneInstance]`로 확장 (TECH_SPEC §4.0-a). **세이브 마이그레이션 1회**.
   ~~`angle`(위치 방위각) 신설~~ — **v2.0에서 폐기됐다.** 배선(어느 문양이 어느 룬을 지배하는가)이
   방향 있는 문양과 함께 죽었기 때문 (§0). 대신 **같은 종류는 하나뿐 + 상극(불↔물)은 증발**로
   합류 규칙이 바뀌었다 (GDD §4.2) — `angle` 필드는 필요 없다
   - 🔴 **이때 `circle_radius`도 배열로 열 것** — **중첩 진**(아래 §1-6)이 오면 어차피 필요하다.
     **지금 안 열면 마이그레이션을 두 번 한다**

### 미결 — 억지로 정하지 않은 것들

1. ✅ ~~**"팅긴다"(벽 반사)**~~ → **v1.9 해결.** 별도 축이 필요 없었다 — **문양 글자**가 답이었다.
   *억지로 정하지 않고 미룬 것이 옳았다*
2. ✅ ~~**룬 각도**~~ → **v1.9 해결: (a) 배선** (아래는 그 기록) → **v2.0에서 배선 자체가 폐기됐다.**
   방향 있는 문양이 사라지면서 "배선을 받아 줄 대상"이 없어졌다 — 대신 **같은 종류는 하나뿐 +
   상극(불↔물)은 증발**로 대체됐다 (GDD §4.2, §0 참조). v1.9의 기록은 참고용으로 남긴다:
   ~~룬을 진 안 어디에 두었는가 = 그 룬이 지배하는 방향. 문양은 각도상 가장 가까운 룬의 속성을
   싣는다 (룬 1개면 전부 그 룬 → 하위 호환)~~
   - 🔴 **함정을 피해 갔다** (v1.9, 참고용): 쓰는 값은 **룬이 놓인 위치의 방위각**이지
     `indicative_angle()`(글리프 회전각)이 **아니다**. 회전각을 썼다면 불△(3회 대칭)이 120도
     돌려도 눈에 똑같아 플레이어가 영원히 알 수 없었다. **위치는 눈에 보인다** — 방위 바늘도 필요 없다
3. ✅ ~~**룬 2개의 피해 계수**~~ → **v1.9: `fill` 가중 평균** (룬 1개일 때 지금과 정확히 같다 →
   하위 호환. 진하게 그린 룬이 피해 성격을 더 지배한다는 뜻도 된다)
4. **원소 반응(증기 등)은 룬 쌍이 아니라 상태이상 레이어에** — 룬 쌍에 걸면 N²로 폭발한다.
   상태이상(화상·젖음·넉백·흐름)에 걸면 **룬이 늘어도 규칙 수가 안 늘어난다.**
   적의 `_statuses`는 **이미 Dictionary라 상태이상을 여러 개 동시에 갖는다** — 그쪽은 준비돼 있다
5. **문양 어휘 확장** (MVP 3종 이후) — 폭발◇ · 분열Y · 설치/지속형.
   🔴 **설치·지속형은 "날아가는 탄"이라는 틀 자체를 깬다** (사용자가 이 이유로 MVP에서 뺐다).
   나머지 3종은 전부 *날아가는 방식*인데 설치만 결이 다르다 — **별도 설계가 필요하다.**
   MVP 3종을 손으로 만져 본 뒤 다시 본다

6. 💡 **중첩 진 — "진 안에 진을 그리고 룬을 각각 넣는다"** (사용자 아이디어, 2026-07-14)

   > *"룬 2개 같은 경우... 진 안에 진을 그리고 룬을 각각 넣는 경우도 있을 듯"*

   v1.9 시절엔 *"왼쪽 룬이 왼쪽 문양을 지배한다"*(방위각 배선)와 나란히 놓이는 두 번째 배선
   수단으로 구상됐다. **그 배선은 v2.0에서 폐기됐지만(§0), 중첩 진 자체의 논리는 안 죽는다** —
   중첩 진은 *"어느 진에 속했느냐"*로 룬을 묶는 **위상 배선**이고, 이건 방향 있는 문양에 기대지
   않는다. ⚠ v2.0의 "한 진 안엔 같은 종류의 룬 하나뿐, 다른 종류는 함께 가능(상극이면 증발)"
   규칙과는 **별개의 수단**이다 — 한 진 안에서도 이미 서로 다른 룬 여러 종류가 섞인다. 중첩 진이
   더해 주는 건 **"섞이지 않게 나눈다"**(안쪽/바깥쪽 진이 각자 다른 규모를 갖는다)는 쪽에 가깝다.

   **왜 자연스러운가 (규칙을 안 늘린다):**
   - 인식기는 이미 **안/밖**으로 의미를 가른다 (§4.0). 진이 겹치면 **"몇 번째 껍질 안인가"**가
     저절로 생긴다 — 새 규칙이 아니라 **기존 규칙이 중첩을 만나면 나오는 결과**다 (별 진과 같은 논리)
   - **규모 축과 맞물린다**: 안쪽 진은 작으니 규모가 작다. 그래서 **"작고 빠른 탄 + 크고 느린 탄을
     한 장에"** 가 가능해진다 — 지금은 도안 하나에 규모가 하나뿐이다
   - 문양이 **어느 진을 뚫고 나가느냐**로 그 탄의 규모·속성이 정해진다. 이것도 기하에서 공짜로 나온다

   **미결 (구현 전에 답해야 할 것):**
   - 안쪽 진을 뚫었지만 바깥 진 안에 머무는 획은 **룬인가 문양인가?** (지금 규칙으로는 판정 불가)
   - 마나·잉크 비용은 진마다 무는가, 합치는가
   - **UX**: 진 두 개를 그리는 게 손으로 즐거운가, 번거로운가 — 만져 봐야 안다

   🔴 **지금 당장의 실무 함의 — 스키마를 진 1개로 못 박지 말 것.**
   `SpellDesign.circle_radius`는 지금 **스칼라 하나**다. 중첩 진이 오면 배열이어야 하고, 그러면
   **세이브 마이그레이션을 또 해야 한다.** 어차피 룬 N개(§1-3) 때문에 마이그레이션을 한 번 하므로,
   **그때 진도 함께 열어 두면 공짜다.** 구현은 미루되 **구조는 막지 않는다.**

---

## 2. 보정 — "약간 보정이 필요한 듯" (사용자, 세션 7)

**⚠ 구체 항목 미정.** 사용자가 시험대를 만져 본 뒤 남긴 소감이라 정확한 대상을 확인해야 한다.
다음 세션은 **무엇의 보정인지부터 물을 것.** 후보:

- **인식 보정** — 룬 인식률은 세션 7에 96.7% → **99.5%**로 올렸다(불△ 감김 게이트 재설계 +
  충격> 타이브레이크 수정). 그래도 손맛이 안 맞을 수 있다. 자동보정 버튼의 유도가 약한지도 확인
- **필압 보정** — 마우스 누적형 합성 상수(`P_MIN 0.35 / P_MAX 1.5 / SPEED_REF 750 / TAU 0.35`).
  머물렀을 때 굵어지는 정도·속도가 손에 맞는지
- **거부 UX** — 순서 위반·인식 실패 시 획이 사라지는 게 답답한지 (잉크는 안 깎이지만)
- **캐스팅 손맛** — 마법진 연출 0.4초, 투사체 속도·크기 배율

---

## 3. ~~곡선 궤적~~ — **폐기 (v1.9)**

**문양이 글자를 택하면서 임의의 곡선 궤적은 버렸다** (GDD §4.3). "그린 모양=궤적"과
"그린 모양=글자"는 같은 획에 못 얹는다 — 지그재그가 *지그재그로 날아가라*인지 *팅겨라*인지
인식기가 가를 수 없다. **v1.9는 글자를 택했다.**

- **궤적이 죽은 게 아니라 글자가 궤적을 정한다** — 유도∿는 휘어서 따라가고, 팅김⚡은 벽에서 꺾인다.
  잃은 것은 **"내 맘대로 S자"** 하나다
- **`ArrowData.path`는 계속 쓴다 — 먹선 렌더 전용**(비주얼). "내가 그린 획 그 자체가 날아간다"는
  세션 7의 연출은 유지된다. **날아가는 길만 글자가 정한다**
- **사거리 문제는 `reach`로 해결됐다**: 아래 세 안 중 **(B)를 골랐다** — 문양 길이 ÷ 진 반지름 =
  **사거리 배율**. 진이 기준 사거리를 주고 문양이 배율을 정하므로 **축을 도로 뺏지 않는다**
  - ~~(A) 그린 길이 = 사거리 (절대값)~~ → 축 위반 위험 (사거리는 진의 축)
  - **(B) 진이 정한 사거리에 배율** ✅ **채택** — 룬 농도(`rune_fill`)와 정확히 같은 문법
  - ~~(C) 모양은 그대로, 끝나면 접선 직진~~ → 궤적을 버렸으므로 무의미

---

## 3.5 제작대(책 펼침) — 남은 조각 (세션 8 신설)

`src/drawing/forge_panel.gd`는 만들었고 시험대(`tests/test_forge.tscn`)에서 돈다. **본편 배선은 아직이다.**

- **거점 이젤 배선** — 지금 `base.gd`의 이젤은 `scene_change_requested(&"drawing")`으로 **씬을 갈아탄다**
  (옛 `drawing_room`). 이걸 forge 오버레이로 바꾸면 거점에서도 ESC 왕복이 된다.
  ⚠ **선행 조건이었던 무료 수리 버그(BUG-2)는 세션 8에서 고쳤다** — 이제 배선해도 안전하다
- **종이 등급을 고를 수단이 forge에 없다** — 항상 최저 등급 자동. 옛 드로잉룸엔 드롭다운이 있었다.
  그래서 지금은 마나 감면·내구 보너스가 붙은 상급 종이를 **의도적으로 쓸 방법이 없다**(GDD §5 무력화).
  왼쪽 페이지 상단에 종이 칸을 두는 게 자연스럽다
- 배선이 끝나면 `drawing_room.gd`·`test_drawing_main.gd`(옛 시험대 UI)와 `design_book.gd`(옛 한 줄 책자)를
  정리한다 — 지금은 forge와 **종이 선택 로직이 중복**돼 있다

## 4. 이월 (세션 5~6에서 넘어옴)

- **사람 플레이 검증** — GDD 1개월차 관문(테스터 5명 중 4명이 자발적으로 두 번째 도안).
  export_presets.cfg 작성 → exe 빌드 → 배포. **아직 아무도 실제로 재미를 확인하지 않았다**
- **아트 "자글자글"** — 고립된 1픽셀 고대비 노이즈. 타일셋·UI·캐릭터 전 에셋 감사 필요.
  방향 미결 (평탄화 / 저대비화 / 완전 평탄) — STATUS 2순위 참조
- 거점 주인공이 정적 1프레임 (필드는 AnimatedSprite2D) · 필드 밖 회색 void
