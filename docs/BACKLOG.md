# BACKLOG — 아직 안 한 것 · 다듬을 것

> 세션 7 신설 · **세91에 전면 정리**(407줄 → 이 크기). 여기는 **아이디어와 미결**만 둔다 —
> 확정되면 `docs/GDD.md`로 승격하고 여기서 지운다. 완료된 것도 지운다(기록은 `git log`. ⚠ 세92에 세션 로그·아카이브를 없앴다).
>
> 🔴 **세91 정리의 이유**: 이 파일의 90%가 **세21에 삭제된 옛 자유드로잉 세대**를 설명하고 있었다
> (룬 인식률 99.5%·필압 보정 `P_MIN`·`SpellDesign.circle_radius`·곡선 궤적 폐기 논의…).
> 완료 표시(✅)가 28건이었고 그중 E1~E5·F1~F8은 **전부 끝난 일**이었다. 살아있는 미결은 아래 여섯뿐이다.
> ⚠ 옛 전문이 필요하면 git 이력(`git show HEAD~1:docs/BACKLOG.md` 부근).
>
> 관련 정본: 진행 관문·해금 미결 = `docs/PROGRESSION.md` · 진행 중 설계 = `docs/takbon-design/`.
> 🔴 **세98: 「코드에 붙은 빚」이 CLAUDE.md에서 여기로 왔다**(아래 절) — 그전엔 이 줄이 CLAUDE.md를 가리켰다.

---

## 살아있는 미결

| # | 항목 | 상태 |
|---|---|---|
| **R1** | 🔴 **마법을 맺는 데 재료가 든다** — 지금은 **공짜**라 "막 만들어도 손해가 없다" | 사용자 확정 *"잉크랑 종이가 사용됨 ㅇㅇ 사용할때 마나도 들고 TODO야"*(2026-07-17) → **나중에 할 것**. ⚠ **세83에 그리기가 폐지돼 전제가 바뀌었다** — 옛 잉크/종이는 「긋는 비용」이었는데 지금 비용을 물릴 자리는 **조립**이다. 옛 잉크 경제(`balance`의 `ink_per_stroke_length` 등)는 스키마에 껍데기만 남았다(아무도 안 읽음). 되살릴지 새로 짤지는 그때 판단 |
| **R2** | **문양 모양이 빈약하다** — "원 + 삼각 + 작대기"라 마법진으로 안 보인다 | 사용자: *"융합해서 예쁜 마법진"*·*"지금 살짝 노잼"*. 🔴 **CLAUDE.md 세86 이월의 「문양 모티프를 예쁘게」와 같은 건이다** — 손대는 곳은 `RingBoard.glyph_guide_pts` 하나(판·아이콘·책 셀이 거기서 파생된다). ⚠ [[takbon-glyph-design-principle]] — **어휘를 늘리지 말고 조합을 늘려라** |
| **G1** | **설치·지속형 문양** — "날아가는 탄"이라는 틀 자체를 깬다 | 사용자가 이 이유로 MVP에서 뺐다. 나머지 문양은 전부 *날아가는 방식*인데 설치만 결이 다르다 — **별도 설계가 필요하다**(지금 문양 9종을 만져 본 뒤 다시 본다) |
| **W1** | **필드 밖 경계가 없다** — 계속 걸으면 바닥이 끝난다 | 벽이냐 카메라 limit이냐 미결. ⚠ **나무를 StaticBody2D로 만들지 마라** — world 레이어라 캐리어(마스크 5)가 나무마다 터진다. 지금 무대는 `boss_room`(2400×2200) |
| **A1** | **아트 "자글자글"** — 고립된 1픽셀 고대비 노이즈 | 🔵 **세90이 어휘를 줬다**(제작 칸·표시 배율·화면 키 · 팔레트 두 층 · Apollo 준수) — 정본 = `docs/takbon-design/visual_language_design.md`. 전 에셋 감사는 아직 |
| **P1** | 🔴 **아무도 실제로 재미를 확인하지 않았다** | GDD 1개월차 관문(테스터 5명 중 4명이 자발적으로 두 번째 도안). `export_presets.cfg` → exe 빌드 → 배포가 **전부 미착수**다 |

## 🔴 코드에 붙은 빚 (세98에 CLAUDE.md에서 이관 — 전부 「사용자 결정 대기」)

> 위 「살아있는 미결」이 **방향**이라면 여기는 **이미 코드에 박혀서 조용히 새고 있는 것**이다.
> ⚠ 넷 다 **에러가 0이고 전 스위트가 그린**이다 — 그래서 문서에 안 적으면 아무도 못 본다.

| # | 빚 | 실측 |
|---|---|---|
| **C1** | **`rune_fill`(룬 농도) 소비자가 0곳** | 살릴지 접을지 미정. `ring_spell_system._fire_hit` 위 주석이 그 사실을 명시한다. = **T3 거짓 손잡이** |
| **C2** | 🔴 **BOLT·EARTH·GRASS 전용 피격음이 없다** | `audio._on_enemy_hit`의 `match rune_type`이 FIRE·WATER·WIND **세 줄뿐**이라 나머지 셋은 **무음**이다. 한 세트 = **wav 3장 + match 3줄**(세33 「새 소리 = wav 한 장」). ✅ 볼 애니는 이미 6종 전원 배선됐다(`ring_carrier.BALL_ANIM` — 옛 「fireball 폴백」 서술은 낡았다) |
| **C3** | **취약 이중 증폭** | 반응 산물에 배수가 **두 번** 곱해 DoT가 의도의 **2배**(초당 13.5)이고 clamp가 없다(`status_holder`의 `apply_incoming` → `_resolve_reaction` → `add`가 배수를 각각 얹는다). 감속은 **상한 포화라 레버가 이미 죽었다**. 🔴 수정 = **곱하는 자리를 하나로** |
| **C4** | **반응 VFX 스테이지3~4 보류** | 세52 설계의 남은 단계 |

⚠ **미결 「결정」**(D3 문양 게이트·D5 진 카탈로그·D6 흙/번개 네임드·잡몹 공급원 무대 0곳)은 여기가 아니라 **`docs/PROGRESSION.md` 「미결」절**이다.

## 다음에 할 것 (세88~90 이월 — 세92에 STATUS 폐지하며 여기로 옮겼다)

> 🔴 세92에 `STATUS.md`가 닫히면서 그 최상단의 「다음 세션 이월」이 갈 곳을 잃어 여기로 왔다.
> 위 「살아있는 미결」이 **아이디어·방향**이라면, 아래는 **바로 집을 수 있는 작업**이다.

| # | 항목 | 메모 |
|---|---|---|
| **N0** | ✅ **「이미지를 뽑는 방식」은 세94에 확정됐다** — 남은 건 실행 | 🔴 **확정 = ⓐ 생성기는 밑그림까지, 완성은 손질이 한다**(사용자 확정). 세91 각하(*"규격 7/7 통과인데 별로네"*)의 원인이 **절차적 생성기 산출물이 「계산된 것」으로 보인 것**이라, 규율을 `ART_SPEC` §4에 박았다(**「기계적으로 보인다」 진단 여섯** + **손질 루프** 2회 이상). ✅ 팔레트(TAKBON 60)·크기·광원·하드엣지도 `ART_SPEC`이 한 기준으로 흡수. ⏳ **남은 것 = 세계관에 맞는 잡몹 디자인**(✅ GDD §8 개정은 세97에 완료) — 상세는 `visual_language_design.md` §6 |
| ~~**N1**~~ | ✅ **완료(세95)** — 시든 잔디 타일 | 🔴 실제로는 「타일 1장」이 아니라 **`tileset_campus` 6칸 전부 재작업**이었다(칸 위치·개수·64px·512×256 **전부 유지** → **코드 변경 0**). 팔레트 위반 0 · 반투명 0 · 18색. 🔴 **가장 값진 건 「꽃」 칸이었다** — 시안/마젠타 1px 십자가 **「마법 가루」**로 읽혀 세계관(*"빛은 마을에 없다"*)과 **정반대**였는데 아무도 못 보고 있었다. ⚠ **`(1,1)`은 코드 참조 0곳**(미사용)인데 (0,1)과 테두리를 한 픽셀도 안 어긋나게 맞춰 뒀다 — 쓰게 되면 그대로 이어진다 |
| ~~**N2**~~ | ✅ **완료(세95)** — 온보딩 폐지어 청산 | 🔴 실제로는 **5곳이 아니라 7곳**이었다: `q00` desc · 문 대사(옛 `DRAW_TUTORIAL_LINES`) · 온보딩 sticky · 거부 문구(`_on_ring_rejected`) · **`player_caster`의 "그려 장착해라"**(옛 목록에 없었다) · `q01`·`q02` desc의 **거짓 조건**. 🔴🔴 **가장 나빴던 건 폐지어가 아니라 「없는 버튼」이었다** — `q00`이 안내하던 `[분석 ▶]`은 `skip_drawing` 때문에 **도달 불가**고, 옛 튜토 대사는 *"손으로 따라 긋게"*라는 **지금 게임에 없는 조작**을 가르쳤다 |
| **N3** | ✅ **공방은 열렸다(세97)** — ⏳ **정제대·상점이 남았다** | 공방 = **책상 [E] → 책 → [⚒ 부품 제작]**(건물을 안 세웠다 — 세90 「마을은 셋」·세95 세계관을 안 건드리는 길. 사용자 확정). 되살릴 땐 `base.gd`의 `_open_workshop_panel`을 본으로 삼아라(형태가 같다) · 건물로 세울 거면 `STATION_UNLOCKS` 머리말 ①~④. ⚠ **정제대·상점은 지금도 접근 불가**다(패널·레시피는 살아 있고 여는 문만 없다) |
| **N4** | **적 손맛 8단계 전량 미착수** | `docs/takbon-design/enemy_feel_design.md` — 적 48px 재작(12장)·마나 폭발·예고 공격·범위 표시. ⚠ `world_and_visual` §5가 *"적의 「삼킨 빛」을 그 재작에 규격으로 얹으라"*고 지정한다(두 문서가 얽혀 있다). 🔴 세88 사용자 F5의 여운(*"뭔가 나쁘지 않게 되기는 하는데…"*)이 **이 축**일 가능성이 높다 — 「부품이 늘었다」와 「때리는 맛」은 다른 축이다 |
| **N5** | 🔴 **엔진 ERROR 2건** (기존 버그 · `SCRIPT ERROR` grep에 안 걸린다) | `Can't change this state while flushing queries (area_set_shape_disabled)` — `forest_enemy._spawn_loose`(세46부터)·`boss_room._spawn_return_portal`(세58부터). 물리 콜백 중 `add_child`가 원인. 고치려면 `add_child`를 deferred로 미루고 픽업이 자기 `_ready`에서 페이로드를 적용(자리 셋: `forest_enemy`·`drop_pickup`·`boss_room`). **영향 미측정 · 사용자 결정 대기** |
| **N6** | **문 기둥 콜라이더 그물이 없다** | 세89에 손으로 맞췄지만(∓62→∓40·폭 24→34) 문을 다시 그리면 또 어긋난다. 실패 형태가 **정적(좌표)**이라 정적으로 잴 수 있다(세84 교훈) |
| **N7** | **잉크·펜·종이 경제의 거취** | 그리기 폐지로 죽은 축인데 상점·정제대·레시피 7장이 걸려 있다. ⚠ R1(마법에 재료가 든다)과 **같은 자리**다 |
| **N8** | 온보딩 퀘스트가 은퇴한 「건설」을 가르친다 | `q01_first_hunt`의 `target = &""`(아무 잡몹으로 완료) · `q02_come_home`의 *"보스를 잡으면 열리는 포탈"* 문구도 이제 거짓(둘 다 `.tres` 한 줄). ⚠ 퀘스트 거취는 세57↔58에 **두 번 뒤집힌 갈래**라 사용자 결정이 필요하다 |
| **N9** | 사용자 눈·손으로만 정해지는 것 (세88 F5 잔여) | 잡몹 무리에 **광역 마법이 시원한가**(세88 설계의 목적 그 자체) · 2400×2200이 걸어서 지겹지 않나 · 남쪽 출구가 멀리서도 눈에 띄나 · mist `disperse`가 *"안 죽는다"*로 읽히지 않나 · **coin 690닢 요구 대 수급**. ⚠ HUD의 **`마나 = (테스트)`** = `debug_free_cast`가 켜져 있다는 표시 — **밸런스 F5 전에 꺼라** |
| **N10** | 세86 UI 잔여 | Tab 마법진 탭 **머리글이 탭 버튼과 겹친다** · 완성 연출이 **은은하다**(`FINISH_*` 상수만 조인다) · 무성함이 감속으로 진흙과 **차별화가 안 된다**(상한 포화 — 답은 `status_overgrowth_sec`이지 감속이 아니다) · 접힌 보관 도안을 **UI로 못 만진다** · `display_name`이 전 도안 "고리 마법진"이라 육안 구분이 안 된다 |
| **N11** | 🔴 **마법 색 통일 ②③** (①은 세94에 끝났다 — 룬 색 6종이 TAKBON 60으로 정렬됐다) | **진단이 다 돼 있어 바로 집을 수 있다.** ② **볼 스프라이트 6종**이 규격 **0/7**(팔레트 밖 + 반투명 2,600~9,400px). 🔴 **형태를 건드리지 마라** — 세91 각하 사유가 정확히 형태였고 팔레트·하드엣지는 **각하 사유가 아니었다**. ⚠ 자동 양자화 금지(수동 매핑). ③ **VFX가 색을 죽이는 두 자리**: `_rune_flash_color`의 `lightened(0.35)`가 **흰색을 섞어 채도를 죽이고**(그래서 ①만으로는 화면이 안 변한다 — `vfx_shot`으로 실측), `status_rules.tint_of`가 **modulate 배수**(1.0 초과)인데 직접 색으로 쓰여 **감전 노랑이 회색으로** 나간다. 정본 = `docs/VFX_SPEC.md` §1 |
| ~~**N14**~~ | ✅ **완료(세97)** — 비전 문서 현행화 | 🔴 **GDD**(허락 받음): §1이 **세계관을 흡수**(절 번호를 밀면 다른 문서의 「GDD §3」 류 참조가 어긋나서 새 절을 안 만들었다) · §2 심장 = **「조립 + 그 결과가 화면에 보이는 것」**(사용자 선택 — 세96 시전 연출 진단을 GDD 차원에 못박았다) · §3 손 긋기 = **휴면 각주** · §8 = TAKBON 60·화면 키. **PROGRESSION**은 전면 현행화. 🔴🔴 **현행화가 새 사실을 하나 캤다 — 아래 N15**(문서가 낡아서 **아무도 못 보고 있던 구멍**이다). ⚠ 「대기 중」이라 적어 둔 역참조 6곳(CLAUDE.md·DECISIONS·ART_SPEC·visual_language·world_and_visual §9)도 같이 닫았다 |
| ~~**N15**~~ | ✅ **완료(세97)** — 도달 불가 해금 5종을 열었다 | 발단: `rune_bolt`·`rune_earth`·`jin_plain_g2`·`jin_fuse`·`gr_condense2`가 **공방 제작이 유일 경로**인데 세90 축소로 **공방 [E]가 없었다**. 고리 3종도 확정 경로가 같이 닫혀 2~3% 두루마리만 남아 **확정 획득이 챕터 룬 3종뿐** = 세57 원칙이 뒤집혀 있었다. → **책상 [E] → 책 → [⚒ 부품 제작]**으로 열었다(건물 0채 추가 · 사용자 확정). 🔴 **가장 값진 교훈**: 문이 닫힌 내내 `test_workshop_auto`가 **그린이었다** — 그 그물은 패널을 **직접 열어** 재고 마을을 안 지난다. **「기능이 돈다」와 「플레이어가 거기 갈 수 있다」는 다른 계약**이고, 새 UI를 붙일 땐 **도달 경로를 재는 그물**을 따로 세워야 한다(`test_base_auto [16]`). ⚠ 버튼 첫 자리가 `SayLabel`과 **글자가 겹쳤는데 헤드리스는 못 봤다** — 스샷이 잡았다 |
| **N18** | ✅ **설계 확정(세100) — 🔴 정본 = `docs/takbon-design/ring_as_gear_design.md`** · ⏳ **구현 대기** | 🔴 **착수 전 그 문서의 §4(조용히 깨질 자리 G1~G19)와 §5(구현 순서)를 먼저 읽어라** — 특히 **0단계는 「core」가 아니라 「core + 그것이 깨뜨리는 그물 수리」**다(안 그러면 1~5단계 내내 스위트가 빨갛거나 **거짓 초록**이라 「내가 방금 깬 건가」를 못 가린다). ✅ **아래 옛 갈림길 넷은 설계가 전부 닫았다**: ⓐ `equip_design` **위에 얹는다**(새로 안 짠다) ⓑ **부위를 재편했다**(펜·모자 은퇴 → 가방·신발) ⓒ 🔴 **세이브는 안 갈린다 — 저장 포맷을 한 글자도 안 바꾼다**(인벤이 `{id:개수}`라 개체 데이터를 못 실어서 「진짜 아이템화」가 원리적으로 막혔다 = 마이그레이션이 형식이 아니라 **의미** 셋뿐이다) ⓓ **발사 경로를 안 건드린다**. 🔴 **리뷰가 잡은 급소 셋**(그대로 갔으면 밟았다): `CATEGORY_ORDER` 한 줄은 **아무 일도 안 한다** · `move_speed()`의 `HAT` 상수를 안 고치면 **신발이 화면에만 존재한다** · `test_save_auto`의 `ring_equipped[2]`는 빨강이 아니라 **런타임 에러**(= 거짓 초록). ⚠ 아래는 착수 전 기록 — 사용자 원문: *"마력 주입 이후 인벤토리에 마법으로 써 들어가는 거 ㅇㅇ 마법진을 장비처럼 착용하자"*. 지금은 맺은 도안이 `GameState.ring_designs`라는 **전용 목록**에 들어가고 슬롯 1·2·3은 `ring_equipped`가 따로 쥔다 — **아이템·장비 체계와 완전히 별개**다. 이걸 **인벤토리 아이템 + 장착 부위**로 합치자는 것. 🔴 착수 전 볼 것: ⓐ **`equip_design`(세86)이 이미 「중복 장착 정리·보관 밖 도안 거부·null 해제」를 지는 단일 소스**다 — 새로 짜지 말고 그 위에 얹어라 ⓑ 장비 5부위는 `Enums.ItemKind`(세42)이고 도안은 거기 없다 — **부위를 늘릴지 별도 축으로 둘지가 첫 갈림길** ⓒ 세이브가 갈려 있다(`ring_designs`·`ring_equipped`·`ring_equipped_paths` vs `inventory`·`equipment`) → **마이그레이션이 필요하고 옛 세이브가 조용히 깨질 자리**다 ⓓ 🔴 **발사는 `to_assembly()`가 도안을 읽고 codex를 안 본다**(세88 시드 교훈) — 저장 구조를 바꾸면 **두 곳**이다 |
| **N19** | ⏳ **세98 시전 연출 수치 F5 조이기** (구현은 끝났고 감각만 남았다) | 🔴 **무난한 진(alpha 0.42)이 마을 밝은 바닥에서 보이나**(vfx가 *"제일 먼저 조일 값"*으로 꼽았다 — `floor_circle.ALPHA_PLAIN`) · 시전 시간 0.15~0.75s · 감속 0.85~0.45 · 🔴 **다 찬 원이 「이제 놔도 된다」로 안 읽힌다**(채워진 뒤 시각 신호가 없다 — `takbon-vfx` 몫) · **문양 8개가 낱개로 안 세진다**(레버 = `FLOOR_RADIUS_PERFECT` 하나뿐. `MOTIF_SIZE_FRAC`은 판·책 공용이라 **혼자 만지면 안 된다** → `R2`와 같은 자리) · **alt-tab 하면 마법이 나간다**(엔진이 액션을 강제로 푼다 — 취소가 맞으면 한 줄 분기) · 히트스톱 중 시전 |
| ~~**N13**~~ | ✅ **완료(세98)** — 시전 연출 「조립한 마법진이 발밑에 열린다」 | 🔴 **F5가 설계를 두 번 뒤집었다**(이게 이 항목의 가장 값진 기록이다): ⓐ *"처음에 기를 모은 데에서 발사됨"* → `origin`·`aim` 얼림을 **각하**(스냅샷은 `assembly`만) ⓑ *"한번 클릭하면 차징이 되네 이러면 안 됨"* → **꾹 누르고 있어야 시전이 살고, 덜 차고 떼면 불발**(마나는 태운다). **둘 다 헤드리스·스샷이 못 잡고 손으로만 나온 것**이다. 그 밖 실측: `_deploy_now`를 테스트 17곳이 옛 인자로 불러 **`SCRIPT ERROR`를 쏟으며 `_OK`를 찍었다** · **테스트끼리 세이브를 공유해** 앞 테스트가 낀 지팡이가 뒤 테스트를 빨갛게 했다(→ `takbon-verify`에 이관) · `GlyphRingDef` 기본값이 실물값이라 합성본이 **「그럴듯하게」 틀린다** |
| ~~**N13-old**~~ | (아래는 착수 전 기록 — 참고용) | 🔴 **정본 = `docs/takbon-design/spell_cast_visual_design.md`** (세96 · architect 리뷰 반영 완료). 발단 = 사용자 *"이펙트를 넣었는데도 밋밋하다"* → 실측 진단은 **다른 것**이었다: **조립을 잘해도 화면이 픽셀 하나 안 달라진다**(위력 78→157인데 `MUZZLE_RADIUS`·`DECAL_RADIUS`·`FLARE_H`가 전부 고정). 🔴 **착수 전 반드시**: ⓐ 1번은 **넷이 한 원자**다(core 시그널 + caster + emit 3곳 + **그물 갱신**) — ④를 빼면 *"원자를 지켰는데도 스위트가 빨갛다"*가 나온다 ⓑ **core 2건은 리드 몫**(`ring_cast_started` 신설 · `spell_impact`에 `score` 추가) ⓒ 조용히 깨질 자리 **13개**가 §8 표에 있다 ⓓ `rings → band_defs` **역변환이 리포에 없다**(새로 짜야 하고 = 사본 위험 → 왕복 그물 필수) ⓔ 🔴 **세97이 자리를 하나 더 만들었다** — 구르기(무적)가 생겨 *"감속으로 위험을 만든다"*(확정 ⑤)가 무효화될 뻔했다. **구르면 시전 취소·마나는 태운다**로 확정(⑥ · 사용자 확정) |
| ~~**N12**~~ | ✅ **완료(세97)** — 적 애니 박자를 데이터로 내렸다 | `EnemyDef.params.anim_fps` 신설(안개 3.0 · 덩굴/갑충 4.0 · 보스 5.0 · 슬라임 6~7 · 사냥개 12.0 = `ART_SPEC` §8-2 기준선). 🔴 **hurt는 일부러 데이터를 안 본다**(피격은 짧은 반응이라 성격 축이 아니다 — 종마다 다르면 *"맞았다"*가 일관되게 안 읽힌다). 그물 = `test_enemy_ai_auto [6]`이 **「값이 SpriteFrames까지 도달하나」**를 잰다(뮤테이션 2종 각 9개). ⏳ **값 자체는 F5로 조인다** — 박자는 헤드리스도 스샷도 못 잰다 |

| **N23** | 🔴 **던전 비주얼 재작 — 남은 셋** (세99 사용자 요청 · **설계가 필요하다**) | 사용자 원문: *"나무랑 풀도 다시만들고 / 나무뒤에 갔을때 나무 투명화 / 나무도 idle이 있어서 풀입떨어지든 해야할듯"*. ✅ **투명화는 세99에 구현됐다**(`tree.gd`). ⏳ **남은 것 = ⓐ 나무·풀 스프라이트 재작 ⓑ 나무 idle(잎 떨어짐) ⓒ 잡몹 5종 64px 재작**(→ 착수분은 `enemy_resize` 보고서). 🔴 **ⓐ·ⓑ는 착수 전 설계가 필요하다** — 나무가 지금 **작고 듬성듬성**해서 「숲」으로 안 읽히는데, 그건 **그림 문제가 아니라 배치·밀도·크기 규격 문제**일 수 있다(세90 「제작 칸·표시 배율·화면 키」 어휘로 먼저 재라). ⓑ는 **애니 규격이 `ART_SPEC` §8에 있다**(박자 기준선) — 프롭 idle은 그 표에 없으니 새로 정해야 한다 |
| **N24** | ⏳ **아트 작업이 느린 구조 — 분할 전략 미결** (세99 사용자 지적: *"아트 굉장히 오래걸리던데 이유가뭐임?"*) | 🔴 **원인 넷(실측)**: ⓐ **Aseprite MCP가 픽셀 단위 도구**라 호출 수가 그림 면적에 비례한다(64×64는 32×32의 4배) ⓑ **자기 결과를 이미지로 읽어 판단하는 왕복**이 규율상 최소 2회(이미지 Read는 무겁고 느리다) ⓒ **검사기 통과가 「좋다」가 아니라서**(세91 규격 7/7 각하) 눈 루프를 못 뺀다 ⓓ **프레임 수가 곱해진다**(`hound`만 8프레임). ✅ **기존 우회로 = `tools/art_gen/*.lua` 생성기로 밑그림을 한 번에 뽑고 손질만 MCP로**(세94 「생성기는 밑그림까지」가 품질 규율이자 속도 대책이다 — 타일 40칸이 그 방식). 🔴 **미결 = 종별로 쪼개 병렬로 갈지.** 파일 충돌은 없다(종마다 다른 PNG) — 걸리는 건 **결이 갈리는 것**이다(적은 나란히 서는 물건이라 음영 두께·외곽선·실루엣 언어가 어긋나면 「한 게임」으로 안 보인다). 후보: **기준 한 종을 먼저 그려 그걸 보고 나머지가 따라간다** |
| ~~**N25**~~ | ✅ **완료(세100)** — 그물 사각 넷을 막았다 | 🔴 **성과 지표는 뮤테이션 검출 수다**(넷 다 「0건 → N건」): ⓐ 길 인자 뒤집기 **0→2**(리드가 직접 재현) · ⓑ 감지 상자 250px **0→3** · ⓒ 플레이어 z 2→6 **0→2** · 잔가지 가드 제거 **그물 없음→SCRIPT ERROR+1**. 🔴🔴 **지시 밖에서 하나를 더 캤다** — ⓒ의 같은 눈감음이 `test_daylight_tree_auto`에도 있어(나무 z 4 > 리터럴 2라 **조용히 통과**) 같이 고쳤다. **리터럴로 든 기대치는 「우연히 큰 값」이면 영영 안 빨개진다**는 게 이 항목의 일반 교훈이다. ⚠ **남은 잔가지 둘**: `scratch_shots/`는 gitignore에 넣었고, **`docs/_reports/_shot_behind_tree.gd`는 일부러 안 지웠다**(`res://` 안이라 Godot이 파싱하지만 **나무 128px 검수에 곧 쓴다** — 그때 `tools/shot_room.gd`에 `behind` 모드로 흡수하는 게 맞다). 🔴 **N23이 오면 할 일**: 나무 쪽엔 ⓑ의 짝이 **아직 없다**(`BehindSense` 아래 변 정적 검사 부재 · `[6]`ⓒ 프로브도 +40px로 헐겁다) — `test_landmark_road_auto`의 `_sense_bottom`을 옮겨 붙여라(art 갈래를 거짓 빨강으로 막지 않으려고 일부러 미뤘다). 아래는 착수 전 기록 — 🔴🔴 **ⓐ 실측으로 확증했다**: `boss_room._landmark_road_cells`의 **`_road_between(지점, 입구)` 인자 순서가 길 모양을 정하는데**(그 머리말이 *"뒤집으면 방 아래쪽이 통째로 흙 밭"*이라 직접 적어 뒀다) **뒤집어도 `test_landmark_road_auto`가 전 항목 그린**이다(세100 리드가 md5 대조로 확인). `[3]`이 **「연결」만 재고 「모양」은 일부러 안 잰다.** 처방 = 입구 칸에서 **북쪽으로 몇 칸 끊김 없이 이어지나**를 세거나 **지점 열의 남쪽 절반이 흙길이 아니다**를 잰다(좌표를 박지 말고 파생). ⓑ **`[9]` ⓑ 프로브가 접지선에서 260px이라** `BehindSense` 상자를 아래로 **250px 늘려도 그린**이다 → *"둥지 앞에 섰는데 둥지가 앞으로 튀어나온다"*가 무증상. 처방 = 프로브를 **접지선 바로 아래(y+20)**로 당긴다. ⓒ **z 스택 숫자(2·3)가 네 곳에 흩어졌다** — 출처는 `player.tscn` 스프라이트와 `floating_wand.gd`인데 `tree.gd`·`nest.gd`가 문장을 복제했고 **그물이 리터럴 2·3을 든다** → 플레이어 z를 바꾸는 날 프롭 둘이 조용히 몸 뒤로 숨는데 그물도 같이 눈을 감는다(T5). 처방 = **테스트가 실제 노드에서 z를 읽는다**(⚠ **공용 부모는 만들지 마라** — `nest.gd`가 *"소비자 둘짜리 추상"*이라 거절한 판단이 옳다). ⓓ **`.tscn`·`.tres`의 `;` 주석 40줄 중 사본이 없는 두 덩이**(`BehindSense` **상자 치수 근거** · `ch1.tres`의 **둥지·늑대 자리 근거 ⓐ~ⓓ**)를 `nest.gd` 머리말·설계문서로 옮겨라 — 에디터·`ResourceSaver`가 저장하면 **통째로 증발한다**(takbon-rules §5). 🔴 **이 자리가 특히 위험한 이유**: 테스트 머리말이 *"사용자가 F5로 수를 조이면 이 값도 같이 고쳐라"*라고 적어 **누군가 반드시 `ch1.tres`를 편집한다**고 전제한다. ⚠ 잔가지 셋: `_spawn_landmarks`의 가드 넷 뒤 **`node.position` 한 줄이 무방비**(루트가 Node2D가 아니면 SCRIPT ERROR — 「새 지점 = 프롭 씬 한 장」이 이 함수의 선언된 계약이라 남이 밟는다) · **`EXIT_PATH_ROWS` 머리말이 갈렸다**(이제 나가는 길만 끈다 — 지점 길 손잡이는 `ChapterDef.landmarks`다) · **`scratch_shots/`가 `.gitignore`에 없다**(+ `docs/_reports/_shot_behind_tree.gd`가 `res://` 안이라 Godot이 파싱한다) |
| **N26** | 🔴 **한 판의 산출 — 지점을 열면 무엇이 남나** (세100 설계 확정 · **구현 대기**) | 🔴 **정본 = `dungeon_structure_design.md` §10**(D10~D16 · 함정 S17~S28 · 착수 순서 0~4). 발단 = 세99에 둥지를 세웠는데 **`lm_nest.tres`가 `params = {}`이고 `drops` 줄이 없어** 밟을 방법도 주는 것도 없었다(전 스위트 그린 · 에러 0). 확정 = **[E] 한 번에 즉시 열린다**(홀드 아님 · 지점 3종 공통) · **판당 한 번** · 산출은 **3층 무작위 뭉치**(바닥 1.0이 허탕을 막고 꼬리가 대박) · **뭉치엔 그 챕터에서 막힌 것을 섞는다**(D16). 🔴 **착수 전 반드시**: ⓐ **그물이 데이터보다 앞이다** — `test_chapter_auto`가 `at_landmark`가 **비어 있기를 단언**해서 데이터를 채우는 순간 전 스위트가 빨개진다(S22) ⓑ `nest.tscn`에 **InteractZone이 아예 없다**(씬 구조 변경 · S26 mask 주의) ⓒ **드롭 굴림 추출이 룬 해금 사슬을 지난다** — 뮤테이션으로 검출력 확인 필수(S17) ⓓ **재열기 가드를 같은 원자로** 묶어라(빠지면 무한 파밍이 최적 전략 · S25). ✅ **core 신설 0**(EventBus·balance 둘 다) — 리드 몫은 `LandmarkDef.params`의 **NEST 주석 재작성**뿐이다(D10·D11이 각하한 무한 스폰 키를 계약으로 들고 있다) |
| **N21** | ⏳ **세99 던전 F5 조이기** (기계는 다 섰고 감각만 남았다) | 🔴 **지금이 「한 판을 새 규칙대로」 돌려볼 수 있는 첫 시점이다.** ⓐ **홀드 3초**(`balance.extract_hold_sec`) · `███░░` 막대가 읽히나 ⓑ **출구 위치·개수**(`ChapterDef.extract_points` — 지금은 잠정 좌표) · 🔴 **풀길이 「저기가 나가는 길」로 보이나** — 출구는 겉모습이 없고 **보이는 표시가 풀길뿐**인데 서·동·북 방향은 이번이 처음이다 ⓒ **네임드가 「저건 다르다」로 읽히나** — ⚠ `hound_alpha`는 바탕 hound와 **같은 PNG**라 지금은 「큰 사냥개 + 보랏빛」이다 ⓓ 🔴 **몸 색조가 TAKBON 60을 깨나** — `params.tint`가 **곱셈**이라 팔레트 밖 색이 나온다(**세69 relight가 정확히 그 이유로 은퇴했다**). MCP 스샷 확인 필요 ⓔ **덩치 2.2배 갑충이 나무·좁은 길에 끼나**(`params.size`는 히트박스라 통행이 달라지는데 **그물이 없다**) ⓕ 네임드 0.3이 「가끔」으로 느껴지나 · 풀 굴림 비율이 「매 판 다르다」로 읽히나 ⓖ **목표 줄이 화면에서 안 잘리나**(35자 → 약 55자) · **ⓗ 단계 3 추가분**(세100 reviewer): 🔴 **꺾인 자리의 오토타일 이음매** — 단계 1b가 표만 세워 두고 *"지금은 안 쓴다"*고 적어 둔 **안쪽 모서리(nub)가 이번에 처음 화면에 나온다** · **길이 「길」로 읽히나** · **둥지가 바닥에 떠 보이나·파묻혀 보이나**(접지는 값으로만 쟀다) · **비침 알파 0.5·페이드 0.14초**(나무는 0.45라 **나란히 놓고** 정해야 한다) · **늑대 다섯이 「무리」로 읽히나 · 줄기에서 350px가 「지나갈 수 있다」로 체감되나**. 🔴 **뽑는 도구는 `tools/shot_room.gd`**(세99에 임시물에서 정식 승격) — ⚠ 손맛 축이라 **리드 스샷으로 결론 내지 마라** |
| **N22** | ⏳ **네임드 전용 스프라이트** (세99 — 지금은 `params` 강화판이다) | 세99에 네임드 3종(`beetle_ancient`·`hound_alpha`·`mist_elder`)을 **`.tres` 한 장씩**으로 세웠다 — `size`·`tint`만 다르고 **그림은 바탕 적과 같은 PNG**다. ⚠ **색만 바꾼 것을 「새 적」이라 부르지 마라**(세54의 결)이므로 전용 도트가 필요하다. 🔴 **착수 전 사용자 확정을 지켜라**: *"눈으로 봤을때 차이가 있는데 몸에서 빛이나는거 까진 별로임"* — **덩치·색·실루엣으로만** 구별하고 **오라·발광은 금지**다. 담당 = `takbon-art` · 규격 = `ART_SPEC`(TAKBON 60 · 화면 키) |
| **N20** | ⏳ **전 스위트가 너무 오래 걸린다** (세99 사용자: *"검증이 좀 오래걸리네 … 너무걸린다"*) | 🔴 **원인은 테스트 수가 아니라 부팅 횟수다** — `for t in tests/*_auto.gd`가 파일마다 **Godot 프로세스를 새로 띄워** 오토로드·리소스 로드가 **33번** 반복된다(실제 검증 시간보다 부팅이 지배적). 후보 셋: ⓐ **한 프로세스에서 여러 테스트를 연달아** 돌리는 러너(가장 효과 크다 — 부팅 1회) ⓑ **병렬 실행** — ⚠ 🔴 **그냥 병렬로 돌리면 깨진다**: 테스트끼리 `user://save_test/save.json`을 **공유**해서 앞 테스트가 남긴 장비가 뒤 테스트를 빨갛게 한다(세98 실측). 병렬로 가려면 **세이브 뿌리를 테스트별로 갈라야** 한다(`save_manager`의 `_save_root`) ⓒ 변경 영향만 선별 실행 — ⚠ **위험**: 「절반만 돌고 다 돌렸다고 믿는」 실패가 세51·84·87에 **세 번** 났다. 선별을 쓰면 커밋 전엔 반드시 전량. 🔴 **검증 규율 자체는 줄이지 마라** — 느린 건 실행 방식이지 규율이 아니다 |
| **N16** | ⏳ **이동 손맛 수치 F5 조정** (세97 이동 개편의 남은 절반) | 넣은 것 = 스페이스 **탭 구르기 · 홀드 달리기**(`ROLL_ENABLED`를 세71f 이후 다시 켰다) · 걷기 120→**140** · `player_run_mult` **1.45** · 커서 쪽 좌우 반전. 🔴 **값은 전부 잠정이다 — 손맛은 F5가 정한다**: 달리기 배수 · 걷기 기본값 · `RUN_ANIM_SCALE`(1.5, 달릴 때 발 박자) · `AIM_FLIP_DEADZONE`(0.15). ⚠ **달리려면 구르기가 늘 한 번 선행한다**(즉발을 골랐으므로 — 쿨다운 `dash_cooldown_sec` 0.6이 연속 구르기를 막는다). 어색하면 대안은 홀드 판정(0.15초)인데 **구르기가 그만큼 늦어진다** |
| **N17** | ⏳ **플레이어 측면 스프라이트** — 방향 전환의 진짜 해법 | 세97에 `flip_h`만 넣었다(정면 그림이라 효과가 약하다). 🔴 세90이 방향 분기를 걷은 이유가 **`left`/`right` 두 셀의 픽셀 차이가 0**이었던 것 — penzilla 원본이 정면뿐이다. 측면 idle·run 시트를 그리면 `player.gd`의 `_apply_anim`에서 방향 애니로 올린다(자리에 주석이 있다). ⚠ **오리지널 캐릭터 재디자인과 한 덩어리**로 가는 게 맞다(`visual_language_design.md` §5) |

## 휴면 (스위치를 되돌리면 살아난다)

- **R3 손맛 튜닝** — 판정 반경·65점 기준선이 맞나. 🔴 **사용자가 직접 그려 봐야 정해진다**(헤드리스는 이탈이 0이라 뭘 바꾸든 정밀도 100 — 세23에 리드가 실제로 이 착각을 했다). ⚠ 손잡이 3개(`ACC_TOL_FRAC`·`CONSIDER_FRAC`·`REVEAL_RADIUS_FRAC`)가 `trace_scorer.gd`에 **const로 박혀 있다** — "수치는 balance.tres" 규칙 위반이라 튜닝을 반복하려면 balance로 올리는 게 낫다.
- **R2a 문양 긋는 손맛이 어색하다** — 세25에 사용자가 *"차차 사용감 좋아지게"*로 넘겼고 세83에 그리기 자체가 폐지됐다. 🔴 **어색함의 정체는 끝내 안 밝혀졌다** — 다시 손대면 **사용자에게 "어디가 어색한지" 먼저 물어라**(리드가 짐작으로 고치면 딴 데를 판다 — 세25에 실제로 그랬다).

## 세91에 지운 것 (행선지)

| 옛 항목 | 어디로 |
|---|---|
| F1~F8 (숲 원정·저장·새로하기) | ✅ 전부 완료 — 숲은 세58-B에 은퇴, 새로하기는 세37. 경위는 `git log` |
| E1~E5 (경제 흐름 드롭→가공→룬 해금→적 5종) | ✅ 전부 완료. E4의 조각→해독대 경로는 세85에 은퇴(관문이 대신한다) |
| §0~§2 축 재설계·문양 세기·보정 | ✅ GDD로 승격됐거나 옛 자유드로잉 세대(세21 삭제) |
| §3 곡선 궤적 · §3.5 제작대 조각 | 폐기·은퇴(후계 = `ring_forge_panel`) |
| 「중첩 진」 아이디어 | 세72·78이 **진별 해석 모델**로 대체 — 정본 = `docs/takbon-design/jin_interpretation_design.md`. 살아남은 `rune_fill` 논의는 `nested_design.md` |
| 원소 반응을 상태이상 레이어에 | ✅ 세49 구현(`status_rules.gd`) |
| 문양 어휘 확장(폭발·분열) | ✅ 세47·79·82 구현(`GlyphCode` 9값) |
