# TODO — 할 것

> 🔴🔴 **세109에 `BACKLOG.md` 하나를 둘로 쪼갰다**(사용자: *"너무 길어 · N* 이건 뭔지도 모르겠고"*).
> **여기 = 「할 것」** · **`BACKLOG.md` = 「이번 세션에 무엇을 했나」** · **`DECISIONS.md` = 「무엇을 왜 정했나」** · **`PROGRESSION.md` = 관문·해금.
>
> ## 이 파일의 규율 (세109 · 어기면 67KB로 돌아간다)
>
> - 🔴 **한 항목 = 한 줄 요약 + 정본 가리키기.** 경위·근거·실측 전문을 여기 쓰지 마라 — 그건 설계 문서·`DECISIONS.md`·`git log` 몫이다.
>   ⚠ 쪼개기 전 이 파일은 **179줄 67KB = 한 줄 평균 375자**였다. 표 한 칸에 문단이 들어가 **표가 표로 안 읽혔다.**
> - 🔴 **완료되면 여기서 지우고 `BACKLOG.md`에 한 줄만 남긴다.** ⚠ 지우기 전에 **「딸린 미결」을 건져라** — 끝난 항목 안에 안 끝난 조각이 섞여 있다(세106·109에 실제로 나왔다).
> - 🔴 **항목은 낫표 이름으로 부른다**(`「도달성 그물」`). 세109에 옛 항목 코드(`N*`·`C*`·`R*`·`M3`·`G1`·`W1`·`A1`·`P1`)를 **전부 없앴다** — 8종인데 범례가 없었고 접두가 의미를 안 갈랐다.
>   **이름은 「무엇을 해결하는가」로 짓는다**(`「exit_zone 추가」`✗ → `「출구 여러 개」`○) — 구현 방식으로 지으면 방식이 바뀔 때 이름이 거짓말한다.
> - ⚠ **설계 문서는 폴더 경로 없이 파일 이름으로 가리킨다** — 세109에 `docs/takbon-design/`이 재편 중이라 경로가 움직인다. 인덱스 = `docs/takbon-design/README.md`.

---

## 🔴 지금 할 것 (블로커 없음 — 바로 집을 수 있다)

> ✅🔴🔴 **2026-08-01 세112 — 방침이 섰다. 「첫 조각」을 사용자가 골랐다**(9라운드 왕복 · 정본 = **`room_loop_design.md`**).
> **이번 라운드 = 「방 루프」 하나다** — 걷어내기 + 방 5~6개를 문으로 잇고 **F5로 「방으로 끊으면 호쾌한가」를 잰다.**
> 🔴 **그 F5가 나오기 전엔 방 개수·한 판 길이·던전 안 조립을 정하지 마라** — 이 조각의 목적이 그 답을 만드는 것이다.
> ⚠ 옛 방침(*"이번주는 핵심 루프만"*)은 세110에 뒷줄 둘이 거짓이 됐다(마을로 안 돌아간다 · 시야 폐기).
> 🔴 **새 몬스터·새 챕터·새 콘텐츠를 늘리지 마라**(사용자: *"천천히 확장할꺼"*) — 이건 그대로 유효하다.
> ⚠ **아래 표에서 「죽었다」로 표시된 줄을 집지 마라.** 지우지 않은 것은 **무엇이 왜 죽었는지가 근거이기 때문**이다.

| 이름 | 무엇 | 착수 전에 볼 것 |
|---|---|---|
| 🔴🔴 **「방 루프」** | **장르 전환의 첫 조각 — 이번 라운드는 이것 하나다.** ✅ **단계 1(걷어내기) 완료**(세112 · `b76f008`+`0f89c12` · 전 스위트 43/43). ⏳ **다음 = 단계 2(방 축소 ≈1600×1000)** → 3(`_ready`→`_enter_room` 분해) → 4(상자 배치) → 5(문·방 전환) → 6(판 종료) → 7~8(아트) → 9(F5).<br>🔴 **단계 3이 진짜 리팩터다** — `boss_room.gd` 1278줄에서 「판 1회」와 「방마다」를 가른다(설계 §5-2 표). 🔴 **단계 2~6은 전부 `boss_room.gd`라 병렬이 안 된다** | 🔴 정본 = **`room_loop_design.md`** — **§4의 R1~R9와 §7 착수 순서를 먼저 읽어라.** 🔴 **단계 1(걷어내기 + 그물 정리)이 한 원자다** — 쪼개면 리포가 빨간 채로 남는다. 🔴🔴 **R3이 관행을 일부러 어긴다 — 시야를 스위치가 아니라 코드째 지운다**(뒤집기를 하려 들지 마라: 대상이 사라지면 뒤집을 것이 없다) ⇒ 그물 넷을 지우고 **`test_perception_auto [15]`(세107 차폐 재발 감지기)가 사라진다는 것을 커밋 메시지에 남겨라.** 🔴 **상자가 아래 「물리 콜백 add_child」 빚을 정면으로 밟는다** — `call_deferred`로 붙이고 **그 김에 재발 그물을 세워라**. ✅ **architect 리뷰 반영 완료** — 🔴 **그 리뷰가 초고를 넷 정정했으니 §B 머리말의 「정정한 것」 표를 반드시 먼저 읽어라**: ⓐ **`sight_range`를 걷으면 안 된다**(시야 값이 아니라 **어그로·리쉬** — 지우면 모든 추격 적이 굳고 `push_warning` 하나뿐이라 grep에 안 걸린다) ⓑ **상자는 `nest.gd` 개명이다**(새로 짓지 마라 — 덤으로 `test_recipe_reach_auto`의 **재료 경제 회귀**가 사라진다) ⓒ **그물이 9개가 아니라 19개** 걸린다 ⓓ **단계 1은 1a만 원자**고 넷은 독립 커밋이다(🔴 **1d 챕터 선택은 혼자 커밋** — 세95 소프트락). ⚠ **방 씬은 `boss_room.tscn` 유지 · 방 전환은 재배치**(씬 교체면 `reset_player_hp`가 **방마다 돌아 소모가 통째로 증발**하고 에러가 0이다) · 아트는 **`takbon-artref` 선행** |
| 💀 **「경계 표시」** | ~~적이 **경계(ALERT)에 든 것을 알리는 `?` 표시**~~ | 🔴🔴 **[세110 D8에 죽었다 — 착수하지 마라]** 시야·인지를 전부 걷기로 확정했다. **이 항목이 존재하던 이유 자체가 사라진다** — 「주변시 110px 구간이 무정보」였는데 주변시가 없어지면 적은 늘 보인다. ⚠ **건질 미결 0건**(발견 `!`도 같이 걷는다) |
| ⚠ **「무리 행동」** | 반경 안 같은 종이 함께 달려든다 | 🔴 **[세110 D8에 통로가 죽었다 — 목적은 살았다]** 예약된 구현(*"짖음이 전파 통로"* · `enemy_perception_design.md`)은 인지 상태기계와 함께 사라진다. **그러나 「늑대 다섯이 무리로 읽히나」는 세99 사용자 확정이고 방 단위 전투에서 오히려 더 중요하다** ⇒ 🔴 **새 전파 통로가 필요해져 「방향 미결」의 「무리 감각」으로 옮겼다 — 이 줄은 이름 자취일 뿐이니 집지 마라** |
| **「적 손맛 설계 갱신」** | `enemy_feel_design.md`의 전제 **셋**이 실측에 뒤집혔다 — 대상 적 6종 중 **4종이 리포에 없고**(세99 청산) · 결정 ⑥(구르기 꺼둠)이 거짓이며 · 🔴 **「적만 규격 미달」의 잣대인 48px이 은퇴한 「프레임」 값이다**(`ART_SPEC` §1은 세90에 **화면 키 56px**으로 갈렸고, 그 절이 *"프레임을 실루엣으로 착각하지 마라"*로 그 사고를 직접 기록한다). ⚠ **문서 머리가 「구현 미착수」라 적어 인덱스(부분 구현)와 정반대다** — 인덱스를 안 보고 문서를 직접 연 사람이 속는다 | 🔴 **`takbon-docs` 몫 · 「적 손맛」·「공격 사이클」의 선행이다.** 안 고치고 착수하면 **없는 파일 4장의 아트를 그리고 이미 끝난 마나 폭발을 다시 짠다** |
| **「공격 사이클」** | 예고 공격 · 범위 표시 (인지 조각 3) | 설계 = `enemy_feel_design.md` · **인지와 독립**이라 세110 D8을 **혼자 살아남았다**. 🔴 **오히려 더 중요해졌다** — 인지 표시(`?`/`!`)가 걷히면 *"적이 무엇을 하려는가"*를 알리는 채널이 **예고·범위 표시뿐**이다. ⚠ 위 「적 손맛 설계 갱신」이 선행 |
| **「적 손맛」** | 적 48px 재작 · 마나 폭발 · 예고 공격 · 범위 표시 8단계 | ⚠ 부분 구현이다(마나 폭발 완료 · 히트박스 완료 · charge 예고 완료). `world_and_visual_design.md` §5가 *"적의 「삼킨 빛」을 그 재작에 규격으로 얹으라"*고 지정한다. ⚠ 위 갱신이 선행 |
| 🔴 **「시드 도안이 온보딩을 막는다」** | 시작 도안 한 장(`_seed_starting_rings` · 점수 **1.00**)이 온보딩 둘을 조용히 끈다 — ⓐ **오프닝 대사 6줄이 새 게임에서 안 뜬다**(`base.gd`의 `_on_gate_talk` 가드가 `GameState.ring_designs.is_empty()`를 보는데 시드가 그걸 거짓으로 만든다 ⇒ `GATE_INTRO_LINES` = 세계관 4줄 + 책상 유도 + [Tab] 안내가 **한 번도 화면에 안 온다**) ⓑ **첫 조립이 시드보다 약하다**(시드 1.00 vs 시작 부품 최고 0.85 — *"조립했더니 약해졌다"*) | 🔴 **세99 실측인데 그 뒤 두 해 세션이 지나도록 `dungeon_structure_design.md` §5 안에만 있었다**(세111에 승격). **2026-07-31 재실측: 둘 다 여전히 살아 있다.** 🔴 **시드를 걷지 마라** — `DECISIONS.md` 「시작 지급을 청산했다」가 **되살리지 마라**의 반대 방향으로 못 박은 자리고, 도안 한 장은 *"1·2·3으로 뭐라도 쏜다"*를 지는 물건이다. 고칠 자리는 **가드 조건**(ⓐ)과 **부품 점수 곡선**(ⓑ)이지 시드가 아니다 |
| **「마법진을 장비처럼」** | 맺은 도안을 소지품 화면에서 장비처럼 착용한다 | 🔴 정본 = `ring_as_gear_design.md` — **§4(조용히 깨질 자리)와 §5(구현 순서)를 먼저 읽어라.** **0단계는 「core」가 아니라 「core + 그것이 깨뜨리는 그물 수리」**다. ⚠ 리드 core 7파일 + 신설 1 + 그물 5 수리가 **한 커밋**이라 루프 밖 라운드에 넣어라 |
| **「정제대·매점 문」** | 패널·스크립트·레시피는 **살아 있고 여는 길만 없다**(실측: `refine_panel`·`shop_panel`) | 본 = `base.gd`의 `_open_workshop_panel`(공방이 같은 형태로 열렸다) · 건물로 세울 거면 `STATION_UNLOCKS` 머리말 ①~④ |
| **「Tab UI 잔여」** | 마법진 탭 **머리글이 탭 버튼과 겹친다** · 완성 연출이 은은하다(`FINISH_*`) · 무성함이 감속으로 진흙과 차별화가 안 된다(답은 `status_overgrowth_sec`이지 감속이 아니다 — 상한 포화) · 접힌 보관 도안을 UI로 못 만진다 · **`display_name`이 전 도안 "고리 마법진"**이라 육안 구분이 안 된다 | 이름 상수는 `base.gd`가 넘긴다(`ring_design.gd` 머리말) |
| **「볼 스프라이트 색」** | 볼 스프라이트 6종이 규격 **0/7**(팔레트 밖 + 반투명 2,600~9,400px) | 🔴 **형태를 건드리지 마라** — 세91 각하 사유가 정확히 형태였고 팔레트·하드엣지는 **각하 사유가 아니었다**. ⚠ 자동 양자화 금지(수동 매핑). 정본 = `VFX_SPEC.md` §1 · ✅ **VFX가 색을 죽이던 두 자리는 세97에 닫혔다**(`vfx.gd` 머리말이 그렇게 적는다) |
| **「잡몹 디자인」** | 세계관에 맞는 잡몹 그림 — 「이미지를 뽑는 방식」은 세94에 확정됐고 **남은 건 실행**이다 | 확정 = **생성기는 밑그림까지, 완성은 손질이 한다**(`ART_SPEC` §4 「기계적으로 보인다」 진단 여섯 + 손질 루프 2회 이상) · 상세 = `visual_language_design.md` §6 |
| **「플레이어 측면 그림」** | 지금은 `flip_h`뿐이라 방향 전환이 약하다(정면 그림) | 자리 = `player.gd`의 `_apply_anim`(주석이 있다). ⚠ **오리지널 캐릭터 재디자인과 한 덩어리**로 가는 게 맞다 — `visual_language_design.md` §5 |
| **「나무 idle」** | 나무에 idle이 없다(잎 떨어짐) — 실측: `tree.gd`에 idle 0건 | ⚠ **`ART_SPEC` §8 애니 박자 표에 프롭 idle 줄이 없다**(적·플레이어만) → 그 줄부터 만들어야 한다 |
| **「자글자글 노이즈」** | 고립된 1픽셀 고대비 노이즈 — **전 에셋 감사가 아직**이다 | 🔵 세90이 어휘를 줬다(제작 칸·표시 배율·화면 키 · 팔레트 두 층 · Apollo 준수) — 정본 = `visual_language_design.md` |
| **「빌드·배포」** | `export_presets.cfg` → exe 빌드 → 배포가 **전부 미착수**다 | 🔴 **아무도 실제로 재미를 확인하지 않았다** — GDD 1개월차 관문(테스터 5명 중 4명이 자발적으로 두 번째 도안). ⚠ **언제 할지는 사용자 몫** |
| 🔴 **「설계문서 경로 정합」** | `docs/` **밖 44개 파일**(`src/`·`tests/`·`tools/`·`data/chapters/ch1.tres`·`assets/aseprite/takbon.gpl`)이 **단계 폴더 없는 옛 설계문서 경로**를 든다 — 전부 실재하지 않는다. `src/core/vision.gd`는 한 세대 더 낡아 **세106 휴지통 폴더**(세109에 `done`이 된 그 이름)를 가리킨다 | 🔴🔴 **치환과 `tools/docs_audit.py` 사정거리 확대가 한 커밋이어야 한다** — 검사기의 죽은 링크 스캔 대상이 `docs/**`+`CLAUDE.md`+`.claude/**`로 **하드코딩**돼 코드를 한 글자도 안 본다(**옛 휴지통 이름 전용 검사를 갖고도 그 사각 때문에 못 본다**). 범위를 넓히는 순간 44건이 즉시 빨강이라 따로 하면 리포가 빨간 채로 남는다. ⚠ 세109에 폴더를 가른 뒤로 죽어 있었고, *"낡은 폴더명을 전량 정합"*이라 적은 후속 커밋의 실제 범위는 `.claude/` 8장 + 검사기뿐이었다 |
| **「전환에 안 따라온 문서」** | 세110 장르 전환이 **문서 넷을 지나쳤다** — ⓐ `dopamine_design.md`: 🔒**GDD §6이 「경제·보상 상세」로 가리키는 유일한 문서인데** 아직 *"무사 귀환해야 갖는다 · 익스트랙션 판돈"*으로 돈다(스텁 0건 · 그 문서 자신이 *"지우면 안 된다"*고 적어 못 지운다) ⓑ `ring_as_gear_design.md`: E6 「가방의 축」의 각하 근거가 *"익스트랙션의 심장"*인데 **「가방 거취」가 방향 미결**이다(스텁 0건) ⓒ `dungeon_structure_design.md` 머리말이 *"관련 정본: GDD §6(**익스트랙션**)"*이라 적는다 ⓓ `takbon-verify`의 `test_contracts.md`가 **세99에 은퇴한 포탈 계약**을 살아있다고 적는다(실제 테스트는 *"처치가 새 길을 열지 않는다"*를 단언한다) | 🔴 **ⓐ가 급하다** — 잠긴 진실원이 가리키는 유일한 포인터라 **읽는 쪽이 반드시 지난다.** ⓓ는 dev·reviewer가 **검증 직전에** 여는 자리다 |
| **「설계문서 줄 인용」** | 살아있는 설계문서에 `파일:숫자` 인용 **34건**(`enemy_feel` 15 · `dopamine` 11 · `guide_editor` 6 · `nested` 2). 실측으로 **`dopamine`의 8건은 전량 어긋났다** — `GDD.md:72`는 §3 각주에, `base.gd:146`은 **빈 줄**에 떨어진다 | 🔴 **이름으로 바꿔라**(함수·상수 이름은 grep으로 찾히고 안 늙는다). ✅ **최상위 문서(GDD·TODO·PROGRESSION·DECISIONS·ONBOARDING·ART/VFX_SPEC·BACKLOG)는 0건이다** — 규율이 거기까진 닿았고 설계문서에서 멈췄다. ⚠ `done/`의 83건은 아카이브라 후순위 |

---

## 🎮 F5로만 정해지는 것 (사용자 손 · 위임 불가)

> 🔴 **전부 「기계는 다 섰고 감각만 남았다」**이다 — 헤드리스도 스샷도 못 잰다.
> ⚠ **리드 스샷으로 결론 내지 마라**(손맛 축이다). 뽑는 도구 = `tools/shot_room.gd`.

| 이름 | 무엇을 보나 | 손잡이 |
|---|---|---|
| 💀 **「인지 감각」** | ~~`growl`↔`howl` 구별 · 경계 한 박자 · 배회가 「살아있다」로 읽히나 · 차폐가 「숨을 만한가」~~ | 🔴🔴 **[세110 D8에 죽었다 — 조이지 마라]** 인지 상태기계(순찰·경계·수색)가 통째로 걷힌다. ✅ **건진 것 하나 → 「무리 감각」**(방향 미결): *"늑대 다섯이 겹쳐 보이나"*(배회 겹침을 물리가 아니라 **위상차**로 푼 것)는 방 단위 전투에서도 그대로 값을 한다. ⚠ **`sight_range` 220/280의 60px 차이는 `hound_alpha`의 정체성이었다**(세101 사용자 확정) — 시야를 걷으면 **네임드를 무엇이 구별하나**가 빈칸이 된다 |
| 💀 **「시야·안개 수치」** | ~~시야 각·거리 · 안개 어둡기 · 느낌표 크기~~ | 🔴🔴 **[세110 D8에 죽었다 — 이 항목이 폐기 사유 1순위였다]** *"부채꼴 100°+주변시 110이라 앞이 안 보여 천천히 간다"*가 실측된 느림의 출처 넷 중 첫째다. **조이는 게 아니라 걷는다.** ⚠ **건질 미결 0건** · 🔴 걷으면 세73 「밝게 유지 + 빛 웅덩이만」으로 완전히 돌아간다 |
| ⚠ **「던전 한 판」** | ~~홀드 3초 막대 · 풀길이 「나가는 길」로 보이나 · 둥지가 떠 보이나~~ · ✅ **늑대 다섯이 「무리」로 읽히나** · ✅ **우두머리 0.3이 「가끔」인가** · ✅ 목표 줄이 화면에서 안 잘리나 | 🔴🔴 **[세110에 절반이 죽었다]** 탈출·출구·지점이 폐기돼 앞 넷은 잴 대상이 없다. ✅ **남은 셋은 살아 있다** — 몹 굴림·네임드 확률은 장르 전환에서 살아남았다. ⚠ **방 크기가 정해지면 여기에 「방 하나가 한 호흡인가」가 새로 붙는다**(설계 §8 「방 개수·한 판 길이」 미결) |
| ⚠ **「발사 손맛」** | 🔴🔴 **[세112에 옛 「시전 연출 수치」에서 갈아 끼웠다 — 차징·발밑 원이 폐기됐다]**(사용자 *"차징도 없애줘 바로발싸되도록"* · `edf2bba`). ✅**지금 잴 것** = **즉발이 손에 붙나**(누른 순간 나가는 게 「시원한가」 vs *"실수로 나갔다"*) · 🔴 **연사 제한이 아예 없다 — 난사가 되나**(시전 시간이 연사 하한을 겸했는데 0이 되며 같이 사라졌다. 3연타 = 실제 3발·마나 3배) · **좌클릭이 실제 화면에서 닿나**(헤드리스는 Control 히트 테스트를 못 잰다).<br>💀 **죽은 넷 — 잴 대상이 사라졌다**(지우지 않는 이유 = 차징을 되살리면 넷이 그대로 돌아온다): ~~무난한 진 alpha 0.42가 밝은 바닥에서 보이나~~·~~다 찬 원이 「이제 놔도 된다」로 읽히나~~ ⇒ **발밑 원이 아예 안 열린다**(`vfx._on_ring_cast_started`의 `duration <= 0` 가드) · ~~alt-tab 하면 마법이 나간다~~ ⇒ **차징 구간이 없어 「뗌」 판정 자체가 안 돈다**(`_tick_cast`가 한 번도 안 불린다 · 발사는 `_unhandled_input`의 눌림뿐이라 포커스 상실이 발사를 못 만든다) · ~~히트스톱 중 시전~~ ⇒ **늘어날 구간이 0이다** | 🔴 **손잡이가 통째로 갈렸다** — 옛 넷(`floor_circle.ALPHA_PLAIN`·시전 0.15~0.75s·감속 0.85~0.45·`vfx.FLOOR_RADIUS_PERFECT`)은 **지금 아무 일도 안 한다.** 되살리는 스위치는 balance의 **`cast_time_plain_sec`·`cast_time_perfect_sec` 둘**(0.0/0.0)이고, **되살리면 무엇이 같이 사는지는 `spell_cast_visual_design.md` 맨 앞 스텁**이 든다(🔴 완료 문서라 근거로 쓰지 말고 **복원 목록으로만** 봐라).<br>🔴 **「문양 8개가 낱개로 안 세진다」는 안 죽고 「문양 모양」으로 옮겼다** — 손잡이 `MOTIF_SIZE_FRAC`이 **판·책 공용**이라 무대만 발밑 원에서 조립 판·책으로 줄었다.<br>⚠ **위 「난사가 되나」는 F5로 답이 나와도 손잡이가 아직 없다**(쿨다운 필드 0개) → 「강한 진의 대가」와 한 자리다 |
| **「이동 손맛」** | 달리기 배수 · 걷기 기본값 · 달릴 때 발 박자 · 조준 반전 데드존 | `player_run_mult`(1.45) · 걷기 140 · `RUN_ANIM_SCALE`(1.5) · `AIM_FLIP_DEADZONE`(0.15).<br>⚠ **달리려면 구르기가 늘 한 번 선행한다**(즉발을 골랐다 · `dash_cooldown_sec` 0.6). 어색하면 대안은 홀드 판정(0.15초)인데 **구르기가 그만큼 늦어진다** |
| **「광역 마법 체감」** | 잡몹 무리에 광역 마법이 **시원한가**(세88 설계의 목적 그 자체) · ~~2400×2200이 걸어서 지겹지 않나~~(🔴 **세110에 답이 나왔다 — 지겨웠다.** 그게 폐기 사유 넷 중 하나다) · ~~남쪽 출구가 멀리서도 눈에 띄나~~(출구 폐기) · mist `disperse`가 *"안 죽는다"*로 읽히지 않나 · coin 690닢 요구 대 수급 | ⚠ HUD의 **`마나 = (테스트)`** = `debug_free_cast`가 켜져 있다는 표시 — **밸런스 F5 전에 꺼라** |
| **「룬 피격음 구별」** | 번개·흙·풀이 서로, 그리고 기존 셋(불·물·바람)과 **귀로** 구별되나 | 생성기 = `tools/gen_hit_sfx.py`(결정적). ⚠ **파형으로는 갈랐다** — earth는 처프 바닥 72Hz(55Hz면 노트북 스피커에서 무음), grass는 wind와 무게중심이 붙어 6k 위를 잘랐다 |
| **「적 애니 박자」** | `EnemyDef.params.anim_fps` 값 자체(안개 3.0 · 보스 5.0 · 사냥개 12.0) | 🔴 **hurt는 일부러 데이터를 안 본다**(피격은 종마다 다르면 *"맞았다"*가 일관되게 안 읽힌다) · 기준선 = `ART_SPEC` §8-2 |

---

## 🕳 방향 미결 (사용자 결정이 선행 — 🔴 착수하지 마라)

> 🔴 **여기 있는 것을 서브에이전트에 위임하면 그가 혼자 정한다**(세71). 리드가 `takbon-design` 스킬로 사용자와 정한다.
> ⚠ 관문·해금의 미결(문양 게이트·진 카탈로그·흙/번개 네임드·잡몹 공급원 무대)은 여기가 아니라 **`PROGRESSION.md` §6**이다.

| 이름 | 무엇이 안 정해졌나 |
|---|---|
| 🔴 **「네임드 위상」** | **`hound_alpha`가 무엇인가** — 미니보스로 방 하나를 차지하나, 무리의 우두머리로 잡몹 사이에 섞이나. 🔴 **세112에 실측이 전제를 무너뜨렸다**: `hound`와 `hound_alpha`가 **`ai: "charge"`로 같고**(돌진은 둘 다 갖고 있다) 차이가 **HP 26↔140 · 크기 1.0↔1.35 · 보라 tint · 드롭** — **전부 스칼라**라 세43 진단(*"축이 세기만 바꾸면 재미가 없다"*)에 정면으로 걸린다. 스프라이트도 같은 PNG에 tint만이다(세102 지적). ⇒ 🔴 **세112에 스폰에서 뺐다**(`room_loop_design.md` R8 — 사용자: *"네임도쪽 다시 정하자"*). ⚠ **스키마(`named_spawn.gd`)는 살아 있다 — 자리만 비었다.** 🔴 **「재료 경제 정리」의 몬스터 카탈로그와 같은 사이클로 묶어라**(*"제대로 정한 몬스터는 늑대밖에 없음"*) · 출발점 = `room_loop_design.md` §4 아래 실측 표.<br>✅ **블로커 하나가 저절로 풀렸다** — 옛 「시야를 지우면 `hound_alpha`의 60px이 사라진다」는 **`sight_range`를 안 지우기로 하면서 무효가 됐다**(architect 실측: 그건 시야 값이 아니라 **어그로·리쉬 거리**라 지우면 모든 추격 적이 굳는다). ⇒ **220 대 280의 차이는 코드에 그대로 남아 있다** |
| 🔴 **「D10 명시 확인」** | *"죽으면 맨 처음부터 · 이번 판 문양은 증발"*의 근거가 **간접**이다(선택지 본문에 딸려 왔다 · 설계 D10이 그렇게 표기했다). **한 번 명시로 확인해라** — 이게 뒤집히면 세이브 계약이 통째로 달라진다(런 상태를 저장해야 한다) |
| 🔴 **「강한 진의 대가」** | **세112에 차징을 걷으며 밸런스 계약 하나가 조용히 죽었다.** `spell_cast_visual_design` §4 *"강한 마법진은 DPS가 아니라 「한 방」이다"*는 **대가를 시전 시간으로 물리던** 계약인데, 시전이 0이 되며 **강한 진이 그냥 우월해졌다**(위력은 78→157로 오르는데 무는 값이 0). 슬롯 3개가 **「속사용 하나 · 한방용 하나」로 갈리던 덤**도 같이 죽었다.<br>🔴 **미결 = 대가를 다시 물릴 것인가 · 물린다면 무슨 축인가**(마나 · 쿨다운 · 이동 배수). ⚠ **시전 시간만은 답이 될 수 없다** — 되돌리는 순간 차징이 통째로 살아나 사용자가 걷으라고 한 그것이 돌아온다.<br>⚠ **「안 물린다」도 답이다** — 즉발 자체가 목적이었으니 F5에서 시원하면 그대로 둔다 ⇒ **「발사 손맛」 F5가 선행**이다(거기 「난사가 되나」가 같은 질문의 다른 얼굴이다).<br>🔴 **이 사실을 든 곳이 `balance_data.gd`의 `cast_time_plain_sec` 머리말 하나뿐이었다** — 그래서 여기로 올렸다 |
| 🔴🔴 **「던전 안 조립 UI」** | **장르 전환의 가장 큰 미지다.** 방마다 주운 문양을 그 자리에서 마법진에 끼워야 하는데, 지금 조립은 `ring_forge_panel`(마을 책상 전용)이다 — **던전용 경량 UI인가, 그 패널을 그대로 여는가**. ⚠ **여기서 「조립이 던전 안으로 들어온다」가 서거나 무너진다**(D3의 재미 근거 그 자체) |
| 🔴🔴 **「기념비 개연성」** | **분모는 세111에 닫혔다**(엔딩 조건 = **기념비 완성** · 채우는 것 = **챕터마다 주는 「마법문 조각」** · 🔴 조각과 문양은 **다른 물건**). 🔴 **남은 미결 = 왜 끝까지 모으나** — 사용자: *"기념비는 애초에 온전한 상태에서 부셔져서 그걸 다 회수하는 거였는데 그러면 처음부터 끝까지 깨는 개연성이 부족해서 그부분 고려해서 채워야할듯함"*. ⏳ **조각 개수도 미정**(*"갯수는 미정임"*). 🔴 **「조각 = 보스 재료인가」가 안 닫혔다** — `gate_restoration` §2 ⓑ는 봉납물을 **보스 전용 재료**로 확정했고 세111은 **「챕터마다 주는 조각」**이다. **챕터 보스가 주는 것이면 같은 물건이고, 아니면 §5가 다시 설계된다** — 지금은 **두 문서가 둘 다 맞다고 적을 수 있다.** 🔴 **정하기 전에 진행도 코드를 얹지 마라** — 무엇을 모아 채우는지가 여기 달렸고, 칸 파생식이 그 답에서 나온다. 🔒정본 = `GDD.md` §1(반영 완료) · 설계 = `gate_restoration_design.md` §4 · 경위 = `DECISIONS.md` 「루프 · 진행」 |
| **「가방 거취」** | 문양이 판마다 증발하면 가방에 남는 건 재료뿐이다 — `bag_lost`를 **유지하나 걷나**. 🔴 **D2상 없앨 이유가 없다**(사용자가 *"죽으면 다 잃는 게 스트레스"*를 **안 골랐다**) |
| **「무리 감각」** | 🔴 **목적은 살고 통로가 죽었다**(옛 「무리 행동」). 「늑대 다섯이 무리로 읽히나」는 세99 사용자 확정인데, D8이 **짖음 전파와 인지 상태기계를 걷어** 예약해 둔 구현 경로가 사라졌다 ⇒ **무엇이 무리를 묶나**를 새로 정해야 한다. ⚠ 배회 겹침을 **위상차**로 푼 방식은 그대로 쓸 수 있다 |
| **「방 개수 · 한 판 길이」** | 한 챕터에 방 몇 개인가 · 한 런이 몇 분인가. ✅ **선행이던 방 크기가 세112에 닫혔다**(≈1600×1000 카메라 추적 — `room_loop_design.md` R2). 🔴 **이제 이건 대화로 정할 것이 아니라 F5가 답한다** — 「방 루프」가 **5~6개로 한 번 돌려 보고**(R4 — 확정이 아니라 재보기 위한 값) 그 체감으로 정한다. ⚠ **2400×2200을 줄이나 방 씬을 새로 파나**는 architect 몫으로 넘어갔다(`room_loop_design.md` §8) |
| 🔴🔴 **「재료 경제 정리」** | **순서가 정해져 있다 — 몬스터가 정해져야 재료가, 재료가 정해져야 가게가 정해진다**(사용자: *"제대로 정한 몬스터는 늑대밖에 없음"*). 세 갈래를 **한 사이클로 묶어라**:<br>ⓐ **`mat_vine` 공급원이 게임 전체에 0곳**(세99에 `vine.tres` 삭제) — 레시피 6장이 막히고 `gr_gather3`·`rune_earth`는 codex 해금의 유일 경로라 **도감에 영구 빈칸**이 된다. 진(陣)은 ch1만으로 한 장도 안 열린다<br>ⓑ **마법을 맺는 데 비용이 0이다** — 사용자 확정 *"잉크랑 종이가 사용됨 · 마나도 들고 TODO야"*(2026-07-17). ⚠ 세83에 그리기가 폐지돼 **전제가 바뀌었다**(옛 잉크는 「긋는 비용」이었고 지금 비용을 물릴 자리는 **조립**이다)<br>ⓒ **잉크·펜·종이 축의 거취** — 죽은 축인데 상점·정제대·레시피 7장이 걸려 있다. 옛 스키마는 껍데기만 남았다(소비자 0곳).<br>　🔴 **여기 딸린 실측 하나**(세99 발견 · 2026-07-31 재확인 · 세111에 승격 — 그전엔 `dungeon_structure_design.md` §5에만 있었다): **`ink_high`(피해 ×1.7)가 공짜다.** 상점(`shop_ink_high`)에 도달할 길이 없는데 **첫 판부터 고를 수 있고 소모도 안 된다** — 이유는 `Db.ink_is_special`이 **`params.special_rune` 유무로만** 특별잉크를 가르는데 `ink_high.tres`엔 그 키가 없어 **기본잉크(무한)로 분류**되기 때문이다(`ring_forge_panel._collect_inks`의 보유 필터가 그래서 안 걸린다). ⇒ 잉크 축을 접든 살리든 **이 한 줄이 밸런스를 먼저 흔든다**<br>✅ **그물은 이미 섰다** — `tests/test_recipe_reach_auto.gd`의 `KNOWN_GAPS`가 **양방향**이라, 공급원이 생기면 *"이 줄을 지워라"*로 **채우는 날 알아서 알려준다**.<br>🔴 재료 데이터를 임의로 채우지 마라 — 사용자 확정 *"레벨디자인하면서 채울 거라 임의로 채우고 싶지 않아"*(2026-07-28) |
| **「문양 모양」** | "원 + 삼각 + 작대기"라 **마법진으로 안 보인다**(사용자: *"융합해서 예쁜 마법진"*·*"지금 살짝 노잼"*). 손대는 곳은 `RingBoard.glyph_guide_pts` **하나**(판·아이콘·책 셀이 거기서 파생된다). ⚠ **어휘를 늘리지 말고 조합을 늘려라**(GDD §2).<br>🔴 **세112에 「문양 8개가 낱개로 안 세진다」가 여기로 합류했다**(옛 「시전 연출 수치」에서 건졌다) — **잴 무대만 발밑 원에서 조립 판·책으로 줄었고 질문은 안 죽었다.** 손잡이 = `RingBoard.MOTIF_SIZE_FRAC`(0.14 · 띠 반지름 비)이고 **판·아이콘·책이 전부 그걸 파생한다** ⇒ 혼자 만지면 셋이 같이 움직인다 |
| **「진 해석 남은 셋」** | 문양 어휘 확장 · 문양 2층 점유 · 진 규칙 데이터화. ⚠ **셋이 서로 독립이라 한꺼번에 하지 마라** — 각자 `takbon-design` 사이클을 돈다.<br>🔴 **가장 싼 진입점 = 잠든 문양 4종** — `data/glyphs/`는 9장인데 `data/glyph_rings/`는 5장이라 `bounce`·`homing`·`pierce`·`thrust`는 **고리가 없어 게임에 안 나온다**. **새로 그리지 않고 `gr_*.tres` 4장만으로 어휘가 거의 2배**가 된다(레시피 = `PROGRESSION` §5).<br>2층 점유는 `GlyphRingDef.layer_span` 스키마 신설 · 규칙 데이터화는 **진 규칙이 2~3개 생긴 뒤라야** 값이 산다(지금 융합진 하나뿐이라 추출할 공통 패턴이 없다). 착수 킷 전문 = `jin_interpretation_design.md`(휴지통) 「M3 착수 킷」절 |
| **「설치형 문양」** | 설치·지속형 문양은 **"날아가는 탄"이라는 틀 자체를 깬다** — 사용자가 이 이유로 MVP에서 뺐다. **별도 설계가 필요하다**(지금 문양 9종을 만져 본 뒤 다시 본다) |
| **「필드 경계」** | 계속 걸으면 **바닥이 끝난다.** 벽이냐 카메라 limit이냐 미결. ⚠ **나무를 StaticBody2D로 만들지 마라** — world 레이어라 캐리어(마스크 5)가 나무마다 터진다.<br>🔴 **세110에 이 미결이 더 커졌다** — 방 단위가 되면 경계는 「끝이 있나」가 아니라 **「방의 벽과 문이 무엇인가」**다.<br>✅🔴 **세112에 닫혔다 — 코드가 이미 limit으로 답했다**(architect 실측: `boss_room._clamp_camera_to_room()`이 **`Ground` rect 파생**이라 방 크기만 바꾸면 따라온다). ⇒ 「방 루프」 단계 2에 흡수. ⚠ **나무를 StaticBody2D로 만들지 마라**는 그대로 유효하다 |
| **「문 복원」** | 🔴 **설계는 확정됐고**(`gate_restoration_design.md` — 부서진 마법문이 봉납 창구·진행도·최종 챕터 입구 셋을 겸한다) **블로커가 사람 쪽에 있다: 보스 재료 3종의 이름·성격이 안 정해졌다**(설계 §11이 일부러 뺐다).<br>🔴 **세111에 블로커가 하나 더 늘었다** — 분모 단위가 **「마법문 조각 수」**로 갈렸는데 **조각 개수가 미정**이라 §10 **단계 0(파생 함수)부터 착수 불가**다 → 위 「기념비 개연성」.<br>착수하게 되면 §8(G1~G17)·§10(착수 순서)을 먼저 — 특히 **단계 3이 여섯이 한 원자**다 |
| 💀 **「던전 지점 나머지」** | ~~폐허·제단 — 아트 선행 + 어느 챕터에 몇 채인지~~ 🔴🔴 **[세110에 전제가 사라졌다]** 지점(둥지·폐허·제단)이 폐기됐다 — **열린 맵을 걸어 다니며 밟는 물건**이었는데 방 단위로 바뀌었다. ✅ **건진 것**: *"제단은 문양-고리를 안 준다 — 진행을 운에 걸지 않는다"*(사용자 각하)는 **잣대로 살아 있다** — D7의 「종류 개방」을 굴림으로 주려는 안이 나오면 이 각하가 그대로 적용된다 |
| ⚠ **「드롭을 상자로」** | 사용자가 *"쏟아지지말고 상자단위면 좋겠는데"*로 **세66의 「상자 기각」을 뒤집었다** — 정본 = `DECISIONS.md` 「루프 · 진행」.<br>✅🔴 **세112에 자리가 정해졌다 — 「방 가운데, 마지막 적이 죽으면 뜬다」**(`room_loop_design.md` R5 · [E]로 연다). 세110에 지점이 폐기되며 자리를 잃었던 것이 방 단위에서 되살아났다. ⇒ **이 항목은 「방 루프」 단계 3에 흡수됐다** — 남은 것은 **상자 안을 무엇으로 채우나**(아래 ⓐⓑ)뿐이다.<br>🔴 **낱개 겹침을 여전히 고치지 마라**(6개면 최소 간격 16~17px) — 상자가 되면 사라질 문제다.<br>✅ **상자 안을 채울 때 그대로 쓸 자산 둘**(세111에 `dungeon_structure_design.md` §10에서 건졌다 — 지점은 죽었지만 **뭉치를 짜는 규칙**은 안 죽었다): ⓐ **세 층 = 바닥(`chance 1.0`) · 몸통(0.45~0.5) · 꼬리(0.05~0.1)**. 🔴 **바닥 층이 「허탕 없음」을 진다** — 줄마다 독립 굴림이라 `chance 1.0` 줄이 없으면 **확률적으로 빈손인 판이 반드시 나오고 에러가 0**이다. ⚠ **바닥에 coin을 두둑이 앉히지 마라** — 판당 총량을 가장 크게 흔들어 병목이 지갑에서 빠진다(바닥은 얇게, 큰 뭉치는 꼬리에). ⓑ **무엇을 섞나 = 「그 챕터에서 막힌 것」이 주력 · 지역 재료는 구색으로 소량**(D16). 🔴 **고르는 방법이 결론보다 값지다 — 판당 기대값을 계산해 무엇이 병목인지부터 찾아라**(세100엔 「늑대 둥지 = 늑대 이빨」이 겉보기로만 맞고 이빨은 이미 남아돌았다). ⚠ **그 계산은 잡몹 카탈로그가 서고 나서 다시 돌려라** — 지금 돌리면 몹이 느는 날 또 낡는다 |
| ⚠ **「챕터별 맵」** | ~~던전 단계 4 — 챕터마다 다른 무대~~ 🔴 **[세110에 전제가 바뀌었다 · 「지금 할 것」에서 여기로 옮겼다]** 무대 단위가 **챕터 한 장**에서 **방**으로 바뀌어, 「챕터마다 씬 하나」가 맞는지부터 다시 정해야 한다(설계 §5가 *"방 크기로 축소하거나 방 씬을 새로 판다"*를 **미결**로 남겼다).<br>✅ **살아있는 실측**: 미착수 증거 = **`room_scene_path`의 소비자가 0곳** · 🔴 손대면 **`boss_room.tscn`을 하드코딩한 그물 7파일**을 건드린다(설계는 `test_chapter_auto` 하나만 지목했었다) |
| **「아트 병렬 전략」** | 아트가 느린 원인 넷은 실측됐다(픽셀 단위 MCP · 눈 왕복 2회 이상 · 검사기 통과가 「좋다」가 아님 · 프레임 수). 🔴 **미결 = 종별로 쪼개 병렬로 갈지** — 파일 충돌은 없고(종마다 다른 PNG) 걸리는 건 **결이 갈리는 것**이다. 후보 = **기준 한 종을 먼저 그려 나머지가 따라간다** |
| **「퀘스트 거짓 안내」** | 온보딩 퀘스트가 **은퇴한 「건설」을 가르친다** — `q01_first_hunt`의 `target = &""`(아무 잡몹으로 완료) · `q02_come_home`의 *"보스를 잡으면 열리는 포탈"*도 거짓(둘 다 `.tres` 한 줄). ⚠ 퀘스트 거취는 세57↔58에 **두 번 뒤집힌 갈래**다.<br>🔴🔴 **세110에 셋째 거짓이 늘었고, 이번 건 실측이다** — `q02_come_home`의 `desc`가 **폐기된 규칙을 그대로 가르친다**: *"출구는 여러 곳에 늘 열려 있다 — [E]를 꾹 눌러 버텨야 나간다."* 출구 여럿·탈출 홀드가 D1로 죽었으니 **온보딩이 없는 조작을 가르친다**.<br>🔴 **넷째(세111 실측)** — `q01_first_hunt`의 `desc`가 *"문 [E]로 나가 **챕터를 고르고**, 문양을 삼킨 짐승을 잡아라"*다. **D9가 챕터 선택 화면을 제거한다** ⇒ 첫 사냥 퀘스트가 없어질 화면을 가르친다 |
| **「챕터 재료 국소성」** | **ch1이 다른 챕터 재료(`mat_night_bloom`)를 준다** — `dungeon_structure_design.md` §10-5가 *"`hound_alpha`를 ch1에 그대로 쓰면 D15를 어긴다 → 챕터별로 `.tres`를 가르는 것이 답일 수 있다"*고 **명시로 예언**했는데 세104가 그 자리를 밟았다.<br>🔴 **지금 실질 피해는 0이다**(밤꽃 레시피가 덩굴·달수액 때문에 어차피 막혀 있다). ⚠ **덩굴 공급원이 생기는 날 D15가 조용히 무효가 된다** — 🔴 **이걸 재는 그물이 하나도 없다**(`test_recipe_reach_auto`는 **전역** 도달성만 재고 챕터 국소성은 안 잰다 — 그 파일 머리말이 명시한다).<br>지금 방침상 **고치지 않고 기록만** 한다 |

---

## 💸 코드에 붙은 빚 (이미 새고 있다 — 🔴 전부 「에러 0 · 전 스위트 그린」)

> ⚠ **그래서 문서에 안 적으면 아무도 못 본다.** 아래 실측 증거(주석·머리말 위치)는 **그게 유일한 증거**라 지우지 마라.

| 이름 | 실측 |
|---|---|
| 🔴🔴 **「물리 콜백 add_child」** | **원인은 세105에 확정됐다** — 물리 flush 중 드롭 픽업 `add_child`(`_die` → `spawn_loose` → `drop_pickup`, 드롭 하나당 에러 하나). ⚠ 「`monitoring`」은 **거짓 단서**였다(`area_set_shape_disabled`가 같이 쓰는 **사본** 문자열).<br>🔴 **남은 것 = 재발 그물이 없다** — 수정을 되돌려도 전 스위트 그린이다(스위트가 `take_hit()`을 직접 불러 죽여 **물리 콜백을 아예 안 지난다**).<br>**처방**: 최소 리그(가짜 `Area2D`의 `body_entered`에서 `add_child`)를 `test_drop_pickup_auto`에 한 항목으로 얹고 출력에서 🔴 **`ERROR:`를 grep**한다 — **`SCRIPT ERROR:`가 아니다**(실측: `tools/run_tests.gd`는 `SCRIPT ERROR:`만 본다. 엔진 에러엔 그 접두가 없어서 **기존 그물을 전부 통과한다**).<br>⚠ **같은 병이 더 있을 수 있다** — 물리 콜백에서 `CollisionShape2D`를 가진 씬을 즉시 `add_child`하는 자리를 **리포 전체로 훑는 건 아직 안 했다** |
| **「취약 이중 증폭」** | 반응 산물에 배수가 **두 번** 곱해 DoT가 의도의 **2배**(초당 13.5)이고 clamp가 없다 — `status_holder`의 `apply_incoming` → `_resolve_reaction` → `add`가 배수를 각각 얹는다(**그 파일 머리말이 스스로 그렇게 적고 있다**). 감속은 **상한 포화라 레버가 이미 죽었다**. 🔴 수정 = **곱하는 자리를 하나로** |
| **「테스트 세이브 격리」** | `-s` 부팅이면 세이브 뿌리가 `user://save_test`로 갈라지지만(`save_manager._save_root`) **테스트끼리 그 파일을 공유한다** — 앞 테스트가 낀 지팡이가 뒤 테스트를 빨갛게 한다(`test_floating_wand_auto` 머리말이 그 사례를 든다). 🔴 **테스트 병렬 실행의 선행 조건**이다 |
| **「rune_fill 거취」** | **소비자가 0곳**이다(살릴지 접을지 미정). 증거 = `ring_spell_system._fire_hit` 위 주석 · `balance_data.gd`가 같은 자리에 적어 뒀다(`rune_density_min/max`도 같다). = **거짓 손잡이** — 돌려도 아무 일이 안 일어난다.<br>🔴 **살릴 때 쓸 안을 여기로 옮겼다**(세111 — 그전엔 `nested_design.md` #5에만 있었고 그 문서는 프레임이 은퇴해 묘비가 됐다):<br>ⓐ **입력 = 룬 크기 휠**(`ring_board`의 `_rune_scale`)을 0..1로 정규화해 싣는다. ⚠ **먹선 bbox 실측으로 시작하지 마라** — 옛 `rune_fill_of` 방식은 **회전 불변이 아니라** 「룬을 돌려 그리면?」 함정을 다시 밟는다(실측은 손맛 불만이 나오면 후속).<br>ⓑ **생산자 부활 지점 = `ring_board.get_assembly()`** — 지금 진 크기(`size`)만 싣고 룬 크기는 버린다.<br>ⓒ **소비자는 상태이상 세기 하나로 한정한다** — `status_power ×= lerp(rune_density_min, rune_density_max, fill)`(balance 필드가 이미 있다). 🔴 **위력(피해)에는 곱하지 마라** — 피해 축은 score×ink×size로 **이미 셋**이라 겹치면 잉크·종이 경제가 죽는다(세29 「축 겹침」).<br>ⓓ **하위호환** = 옛 도안은 **배율이 1.0이 되는 값**으로 폴백해야 기존 세이브의 상태 세기가 안 변한다(`lerp(0.5,1.8,x)=1.0` → x≈0.385 · balance에 한 줄).<br>⚠ 같이 걸려 있던 #7은 아래 **「취약 이중 증폭」**이 이미 들고 있다 |
| 🔴 **「실눌림 그물이 0이 됐다」** | **세112에 `test_extract_hold_auto`가 죽으면서 `Input.action_press`로 [E]를 진짜 누르던 유일한 그물이 사라졌다.** 남은 `test_chapter_auto [4b]·[5]`는 `interacted`를 **직접 emit**해 정산 채널만 잰다 ⇒ 🔴 **「플레이어가 상호작용 존에 걸어가 실제로 [E]를 눌러 무언가 일어난다」를 재는 곳이 리포에 0곳**이다.<br>⚠ **이건 곧 상자·문에도 걸린다** — 단계 4·5가 [E]로 여는 물건 둘을 새로 세우는데 그 채널에 그물이 없다.<br>🔴 **처방 = R9(판 종료)로 나가는 길을 다시 세울 때 실눌림 그물을 같이 세워라.** 안 하면 **세25 자리다**(클릭이 안 닿는데 전 스위트 그린 — 화면 덮는 Control이 좌클릭을 먹어 발사가 통째로 죽었던 그 사건). 경위 = `b76f008` |
| **「반응 VFX 남은 단계」** | 세52 설계의 스테이지 3~4 보류 |

---

## 😴 휴면 (스위치를 되돌리면 살아난다)

- **「채점 손맛」** — 판정 반경·65점 기준선이 맞나. 🔴 **사용자가 직접 그려 봐야 정해진다**(헤드리스는 이탈이 0이라 뭘 바꾸든 정밀도 100 — 세23에 리드가 실제로 이 착각을 했다). ⚠ 손잡이 3개(`ACC_TOL_FRAC`·`CONSIDER_FRAC`·`REVEAL_RADIUS_FRAC`)가 `trace_scorer.gd`에 **const로 박혀 있다** — "수치는 balance.tres" 규칙 위반이라 튜닝을 반복하려면 balance로 올리는 게 낫다.
  🔴🔴 **`CONSIDER_FRAC`(0.32)은 조이면 오히려 관대해진다 — 비단조(non-monotonic)다.** 그 값은 위력이 아니라 **「이 획이 이 조각을 겨눈 것인가」(획 귀속)** 판정이라, 조이면 어중간히 삐끗한 획이 **감점 대신 무효**가 돼 **공짜로 빠진다.** ⇒ 엄격하게 하려면 `ACC_TOL_FRAC` 쪽이다.
- **「긋기 손맛」** — 세25에 사용자가 *"차차 사용감 좋아지게"*로 넘겼고 세83에 그리기 자체가 폐지됐다. 🔴 **어색함의 정체는 끝내 안 밝혀졌다** — 다시 손대면 **사용자에게 "어디가 어색한지" 먼저 물어라**(리드가 짐작으로 고치면 딴 데를 판다 — 세25에 실제로 그랬다).

