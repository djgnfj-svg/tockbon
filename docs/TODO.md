# TODO — 할 것

> 🔴 **한 항목 = 한 줄 요약 + 정본 가리키기.** 경위·근거는 `DECISIONS.md`·`git log` 몫이다.
> 🔴 **완료되면 여기서 지운다.** 지우기 전에 **딸린 미결을 건져라.**

---

## 🔴 지금 — 정하는 게 먼저다 (코드 금지)

**`GDD.md` §6의 미정 둘이 이후 전부를 가른다. 대화로 정하기 전에 그 위에 코드를 얹지 마라.**

| 이름 | 무엇이 안 정해졌나 |
|---|---|
| 🔴🔴 **「중력을 쓰나」** | 플랫포머(점프·발판)면 **레벨 디자인이 곧 게임**이 되고, 중력 없이 옆에서 보는 형태면 **방 단위 구조를 그대로** 쓸 수 있다. 이동·충돌·방 구조·아트 프레임이 전부 여기서 갈린다 |
| 🔴🔴 **「조립을 언제 하나」** | **깊이와 속도는 서로 잡아당긴다.** Noita는 깊이를 택하고 속도를 버렸고(인벤토리에서 몇 분), 아이작은 속도를 택하고 조립을 없앴고, 하데스는 절충했다(보상 3지선다 5초). 갈래 = 방 사이에 짧게 / 전투 중 실시간 / 판 시작에 한 번. **리듬 전체가 여기서 정해진다** |

## 🔴🔴 그 다음 — v2의 첫 목표는 하나다

**「강한 마법이 세게 보이는 것」.**
v1은 위력이 78 → 157인데 총구·바닥 장식·섬광이 전부 고정이었다 — 숫자만 오르고 화면은 그대로.
🔴 **방 구조·진행·아트보다 이게 먼저다.** 안 되면 나머지를 다 만들어도 v1과 같은 자리에 다시 선다.

⚠ **시점을 바꿨다고 저절로 되는 게 아니다** — 사이드가 유리할 뿐이다(축이 하나 · 벽을 부수면 결과가 남는다).

## ⏳ 그 뒤 (순서 미정)

- **마법진 조립 UI를 다시 짓는다** — 🔴 **캐릭터 앞에 수직으로 세운다**(바닥에 깔면 동심원이 납작한 타원이 된다). v1 구현은 `git show main:src/drawing/…`에 있지만 **탑다운·바닥 전제**라 그대로 못 쓴다
- **타일 파괴** — 멀티 동기화의 기준 단위(`DECISIONS.md`)
- **멀티 뼈대** — 🔴 나중에 못 붙인다. 게임플레이 코드를 짜기 전에 「누가 권위를 갖나」를 정해야 한다
- **아트 파이프라인** — 픽셀랩인가 직접인가. v1 penzilla 에셋은 버렸다
- **세계관** — v1의 「깨진 마법진, 흩어진 문양」을 이어받을지 미정
