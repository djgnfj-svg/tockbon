# NAN 2026 예선 제출 — 안내판

**대회**: NHN 게임 × AI 해커톤 `NAN 2026` (https://nan2026.nhn.com)
**접수 마감**: **2026-08-10 23:59:59 KST**
**본선**: 2026-09-04 14:00 ~ 09-06 19:00, 판교 NHN 플레이뮤지엄 (전일 참여 필수, 온라인 불가)
**규모**: 10팀 선발 · 총상금 3.5억 · 수상자 전원 최종 면접 직행
**참가 형태**: **개인 (1인)**

이 폴더만 한국어다. `docs/`의 나머지는 영어(CLAUDE.md의 규칙) —
**제출물은 심사위원이 읽는 글**이므로 인게임 텍스트와 같은 범주로 본다.

---

## 제출물 5개 — **하나라도 누락되면 심사 대상에서 제외된다**

| # | 항목 | 형식 | 이 리포의 대응 | 상태 |
|---|---|---|---|---|
| 1 | 플레이 가능한 빌드 + **전체 소스** | GitHub Pages 링크 | 아래 두 링크 | **배포 완료** |
| 2 | 플레이 동영상 | YouTube 링크, **30~60초** | 아래 구성안 | **촬영 필요** |
| 3 | 게임 소개 및 설명 문서 | **PDF** | [`plan.md`](plan.md) | 작성 완료, PDF 변환 필요 |
| 4 | AI 활용 기술 문서 | **PDF** | [`ai-tech.md`](ai-tech.md) | 작성 완료, PDF 변환 필요 |
| 5 | 팀원 롤 기술서 | PDF | [`roles.md`](roles.md) | **개인 참여이므로 제출 불필요** |

**제출 링크**

| | |
|---|---|
| 플레이 | **https://djgnfj-svg.github.io/tockbon/** |
| 소스 | **https://github.com/djgnfj-svg/tockbon** (공개) |
| 영상 | `(업로드 후 기입)` |

**5번은 요강상 "2인 이상 팀인 경우에만" 제출한다.** `roles.md`는 참고용으로 남겨 두되 제출하지 않는다.

### 요강 원문과 한 줄씩 대조 — **2026-08-10 20:45, 사이트를 다시 열어 확인**

「참가 안내 → 사전 과제 → 상세보기」의 **「사전 과제 제출물 안내」** 모달이 원문이다.
**모달이라 검색에 안 걸린다** — 페이지 본문만 읽으면 제출물 목록 자체가 안 보인다.

| 요강이 요구하는 것 | 지금 |
|---|---|
| 3번 PDF: 게임 제목 및 한 줄 소개 | **있다** (`plan.md` 첫 표) |
| 3번 PDF: 게임 방법 — 목표·조작·**종료 조건** 포함 | **셋 다 있다** |
| 3번 PDF: 실행 방법 (설치 및 실행) | **있다** |
| 3번 PDF: 플레이 링크 | **있다** |
| 3번 PDF: **플레이 영상 링크** | **비어 있다** — 영상이 아직 없다 |
| 4번 PDF: AI 활용의 기술적 설명 — 구조 설명, **AI 대상 주요 프롬프트 및 지시 사항** | 구조는 있다. **지시 사항 원문은 「4. AI에게 실제로 준 지시」 한 절이 든다** |
| 4번 PDF: 외부 에셋 / 오픈소스 출처 | **있다** (`ai-tech.md`의 「오픈소스 출처 · 라이선스」) |
| 저장소 공개, 커밋 기록 유지, `.exe` 금지 | 충족 |

**유의사항에서 새로 확인한 것 넷**:

- **접수 마감 후에는 제출 내용을 변경할 수 없다.** 영상 URL을 적어 낸 뒤에는 못 고친다
- **제출 링크(GitHub·YouTube)는 심사 종료 시점까지 접근 가능해야 한다.** 저장소를 비공개로 되돌리거나
  영상을 내리면 그 시점에 제출이 죽는다
- **AI 도구 사용은 허용·권장이나, 사용 도구와 활용 내역을 반드시 기재해야 한다**
- **외부 에셋의 출처와 라이선스는 4번 문서에 명시**해야 한다 — 폰트 하나뿐이고 적혀 있다

---

## 1번 — 빌드와 소스

**`.exe`는 받지 않는다.** 요강 원문: "보안 상의 이유로 PC 실행 파일(.exe 등) 직접 제출은 받지 않습니다."
⇒ **웹 빌드가 정답이다.** 이미 나와 있다.

```powershell
New-Item -ItemType Directory -Force build\web
.\Godot_v4.7.1-stable_win64.exe --headless --export-release "Web"
```

산출물 `build/web/` — **44.1MiB** (`index.wasm` 37.68 + `index.pck` **6.10** + 나머지 0.3).
**2026-08-10 11:17 실측** (`Get-ChildItem build\web | Measure-Object Length -Sum`).
익스포트 자체는 **6.1초**, 종료 코드 0, stderr 깨끗함.
pck가 1.5 → 2.31 → **6.39MB**로 자란 것은 그림이 44 → 85장이 됐고 **한글 폰트 4.6MB가 들어갔기** 때문이다.
**wasm은 템플릿 고정이라 안 변한다** — 세 번 재배포하는 동안 바이트가 같았다.
**스레드 없는 빌드**라 COOP/COEP 헤더 없이 GitHub Pages에서 그대로 돈다.
로컬 확인은 `python -m http.server 8123 --directory build\web`.

### 배포한 방식 (재현용)

`gh-pages` **orphan 브랜치**에 `build/web/`의 내용만 올린다. `.nojekyll`을 넣어야
Jekyll이 파일을 건드리지 않는다.

```powershell
git worktree add --detach C:\temp\ghp
cd C:\temp\ghp; git checkout --orphan gh-pages
# 내용을 비우고 build/web/. 를 복사, .nojekyll 생성
git add -A; git commit -m "웹 빌드 배포"; git push -f origin gh-pages
gh api repos/<user>/<repo>/pages -f "source[branch]=gh-pages" -f "source[path]=/"
git worktree remove --force C:\temp\ghp; git worktree prune
```

**워크트리는 반드시 지운다** — 자동 정리는 거의 안 걸린다(하룻밤에 700MB, 실측).

### 요강이 요구하는 저장소 조건 — 전부 충족

- **전체 소스 코드를 같은 저장소에 포함** — `main` 브랜치
- **커밋 기록 유지** — **182개** 커밋(2026-08-10 20:40 측정, `git rev-list --count HEAD`). squash하지 않았다
- **공개(public)** — 전환 완료. 요강은 비공개일 경우 심사 계정 `dl_gameai_reviewer@nhn.com`을
  초대하라고 적고 있는데, **공개이므로 초대할 것이 없다**

### 공개 전 시크릿 검사 (완료)

`.mcp.json`에 PixelLab API 키가 평문으로 들어 있어서 **전체 히스토리를 훑었다.**

```bash
git rev-list --all | xargs -n 50 git grep -lE "Bearer [a-f0-9]{8}-|gho_|ghp_|sk-[A-Za-z0-9]{20}"
```

**결과 0건.** 과거에 커밋된 `.mcp.json`은 godot·aseprite만 있던 버전이고,
PixelLab이 추가된 뒤로는 gitignore에 걸려 한 번도 올라가지 않았다.

---

## 2번 — **30~60초** 영상

요강 원문: "30~60초 분량으로, **실제 게임 플레이 장면을 중심**으로 제작해 주세요."
"**AI를 이용한 조작·합성이나 타인 영상의 도용은 불가**합니다. 실제 플레이 화면 그대로 담아야 합니다."

⇒ **하네스 화면이나 개발 과정을 넣을 자리가 없다.** 60초 전부 플레이 화면이다.
AI 이야기는 4번 문서가 전담한다.

### 60초 구성안 — **컷 리스트는 [`video-cuts.md`](video-cuts.md) 하나가 센다**

**여기에 표를 두 번 적지 않는다.** 그 문서에 구성안 **두 개**가 컷 단위로 있고, **택일은 사용자 몫이다.**

| | A | B |
|---|---|---|
| 첫 화면 | **순서가 결과를 바꾼다** (`퍼짐→폭발` vs `폭발→퍼짐`) | **영구 해금을 산다** (마을 연구대) |
| 루프를 이해하는 시점 | 마지막 8초 | 첫 8초 |
| 위험 | 루프가 늦게 와서 **못 보고 끝날 수 있다** | 차별점이 늦게 와서 **밋밋하게 시작할 수 있다** |

그 문서는 **촬영 전 검증 목록이기도 하다** — 적힌 장면 중 **정상 속도로 본 것이 하나도 없다.**

**공개 또는 일부 공개(링크 공유)로 업로드.** 심사 종료까지 접근 가능해야 한다.

---

## 3·4번 — PDF 변환

**변환기가 리포에 있다. 한 줄이면 두 개가 같이 나온다.**

```powershell
python tools\submission\md2pdf.py
```

산출물 `build/submission/` — **`plan.pdf` 8쪽 · `ai-tech.pdf` 13쪽** (2026-08-10 12:26 실측).
**이 폴더는 커밋하지 않는다** — 소스는 마크다운이고 PDF는 그것의 출력이다.

| 단계 | 무엇 |
|---|---|
| 마크다운 → HTML | `python-markdown` + `tables` · `fenced_code` · `md_in_html` |
| HTML → PDF | **설치된 Chrome을 `--headless --print-to-pdf`로 부른다.** A4 · 여백 16mm |

**왜 `npx md-to-pdf`가 아닌가**: 이 문서들은 **표가 대부분이고 전부 한국어**다.
기성 변환기는 표 확장이 기본으로 꺼져 있거나 **한글 폰트를 안 싣는다** —
**그게 정확히 이 프로젝트가 웹 빌드에서 당한 결함이다.** Chrome은 OS 폰트 스택에서 인쇄한다.
`Malgun Gothic`이 PDF에 **서브셋으로 임베드된 것을 확인했다**(`fitz`로 폰트 목록을 읽어 확인).

**인쇄에서만 드러난 것 두 개** — 스크립트가 후처리로 고친다:

- **`<details>`가 접힌 채 인쇄된다.** 종이에는 펼칠 삼각형이 없으므로 `plan.md` 첫 페이지의
  "소스에서 직접 빌드하려면" 블록이 **통째로 사라졌다.** 전부 `open`으로 강제한다
- `| | |`로 쓴 키/값 표가 **빈 헤더 줄**을 회색 띠로 남긴다. 빈 `<thead>`를 지운다

**PDF 첫 페이지에 플레이 링크와 영상 링크를 박는다.** 요강이 3번 문서에 요구하는 항목이고,
두 문서 다 첫 표가 그것이다. **⚠ 영상 URL 칸은 아직 `(업로드 후 기입)`이다 —
업로드하면 두 마크다운을 고치고 위 명령을 다시 돌린다.**

**문서 안의 상대 링크(`review.md` 등)는 PDF에서 죽으므로** 변환기가 공개 저장소 URL로 바꿔서 넣는다.

---

## 작업 순서 (마감 역산)

**2026-08-10 11:20 기준. 앞의 둘은 끝났다.**

1. ~~웹 빌드를 다시 굽고 재배포한다~~ — **끝. `gh-pages`에 배포 커밋이 7개** 쌓였고 마지막이 최신이다
2. ~~브라우저에서 열어본다~~ — **끝. 그리고 폰트 결함을 잡아서 고쳤다**
3. **브라우저에서 한 판을 끝까지 돌린다 — 포그라운드 탭에서.** 남은 육안 검증이 전부 여기 있다:
   정상 프레임레이트 · 클리어 연출 · 정산 화면 · 원석이 실제로 9~11 모이는지.
   **여기서 처음 보는 버그가 나올 것을 전제로 시간을 잡는다**
4. **60초 영상 촬영 + 업로드** — 컷 리스트는 [`video-cuts.md`](video-cuts.md), **구성안 택일이 먼저다**
5. **숫자를 마지막으로 한 번 재고 PDF 2종 변환** — 영상 URL이 나와야 첫 페이지가 완성된다.

   **`ai-tech.md`가 검사 수를 세 곳에서 말한다** (머리말 · 두부 사건 · 그 표). **셋을 같이 고쳐야 한다.**
   그리고 **편집이 멈춘 상태에서 재야 한다** — 다른 세션이 `src/`를 만지는 동안 잰 값은 경합이고,
   실제로 이 세션 중에 라운드가 한 번 빨갛게 나왔다(진행 중이던 글리프 작업).
   **문서 개수(설계 · 기각 · 계획)도 워킹트리가 아니라 커밋된 것을 센다** — 심사자는 공개 저장소를 본다
6. **신청서 제출** — 마감 **23:59:59**

**3번과 4번은 같은 작업이다.** 촬영하려면 어차피 한 판을 끝까지 돌려야 하므로,
**녹화를 켜 두고 검증한다.** 처음 보는 버그가 없으면 그 녹화가 그대로 소재가 된다.

---

## 적대적 검토 — 3차까지 진행됨

| 차수 | 물은 것 | 나온 것 |
|---|---|---|
| 1차 | 이 문서의 문장이 사실인가 | **거짓 3건** — 클리어 판정 · 사망 재시작 · 디버그 키 번호 |
| 2차 | **이 빌드가 게임인가** | **몬스터가 0마리.** "게임이 아니라 샌드박스다" |
| 3차 | **이 문서가 지금도 사실인가** | **대부분 아니다.** 하룻밤에 통째로 낡았고, **배포된 빌드가 문서와 다른 게임이다** |

무엇이 틀렸었는지는 전부 [`review.md`](review.md)에 남겼다 — **지운 것이 아니라 기록했다.**
**1·2차가 "없다"고 정직하게 고쳐 놓은 문장들이 구현이 앞지르면서 새 거짓이 됐다** — 3차가 그것을 고쳤다.

---

## 4차 적대적 검토에서 나온 고칠 것 — **여덟 중 일곱이 닫혔다**

`plan.md`를 코드와 대회 사이트 원문에 각각 대조한 결과다. **위에서부터 심각했다.**
**2026-08-10 20:40 재대조** — 코드를 다시 읽고, 그림을 다시 찍고, 사이트 원문을 다시 열어 확인했다.

| # | 무엇 | 지금 |
|---|---|---|
| 1 | **황소를 잡아도 아무것도 안 나온다.** 보상 수령이 F3→L 개발자 키 전용이었다 | **닫혔다.** `stage.gd`가 `is_reward_pending`에서 `_take_boss_reward()`를 스스로 부른다 — 개발자 키를 안 눌러도 불 룬이 나온다 |
| 2 | 요강 필수 항목 누락 — **조작 · 실행 방법 · 종료 조건 · 영상 링크** | **셋은 돌아왔다.** `plan.md`에 조작 표 · 실행 방법 · 종료 조건이 있다. **영상 링크만 비어 있다** |
| 3 | **순서 비교 그림이 캡션과 반대다** | **닫혔다.** 아래 「그림을 다시 찍었다」 참조 |
| 4 | 거짓 문장 넷 — 번개 전도 · "변형은 비행을 바꾼다" · "문양이 사라진다" · "속성마다 날아가는 모양이 다르다" | **닫혔다.** 번개는 「아직 아닌 것」으로, 변형은 "위력까지 들어갔고 가속·유도·회전은 준비 중"으로 내려 적었다 |
| 5 | **팔레트가 모든 문양을 공짜로 준다** | **닫혔다 — 코드가 앞질렀다.** `palette_layout.items_of()`가 소유로 거른다. 조립창 그림에 문양이 **둘만** 뜨는 것이 그 증거다 |
| 6 | 「이 빌드가 아직 아닌 것」이 사라졌다 | **돌아왔다.** 다만 사운드는 이제 **있다**(코드 합성) |
| 7 | 「거대 닭」 → 게임 안 이름은 **거대 수탉** | `plan.md`는 고쳤다. **이 파일이 아직 「거대 닭」이라 적고 있었고 그것도 고쳤다** |
| 8 | 이 README의 거짓 둘 — 푸터에 영상 링크가 없다, `R`은 게이트 앞 플레이어 키다 | `R`은 확인했다 — **게이트 앞이 맞다**(`stage_input.gd`가 그 이유까지 적어 뒀다: 제 발밑을 파면 R 말고 나갈 길이 없다). **푸터는 여전히 플레이·소스 둘뿐이다** |

### 그림을 다시 찍었다 — **문양이 커진 뒤로 낡아 있었다**

`shots.py`를 다시 돌리는 것으로 끝나지 않았다. 재촬영이 세 가지를 드러냈다.

| 무엇 | 어떻게 드러났나 |
|---|---|
| 두 순서 그림이 **무속성 룬**으로 찍혀 있었다 | 가운데가 불꽃이 아니라 못이었다. 마을을 거쳐 무대를 세우는 동안 룬이 되돌려졌고, `_seat_fire()`를 프리셋 **뒤로** 옮겨 고쳤다 |
| 두 순서 그림의 하늘이 **마을의 노을**이었다 | `set_backdrop`이 인자를 둘 받다가 하나로 줄었는데 호출이 그대로였다. **에러가 `await` 안에서 났기 때문에 촬영은 끝까지 돌았고**, 종료 코드 255만 남았다 — 그림은 이미 디스크에 있었다 |
| 「여덟 갈래가 각각 터지는 순간」이 **찍히지 않는다** | 30·45·60·75·90프레임을 훑었다. 갈래마다 제 궤적으로 날아가 제각기 터져서, 90프레임이면 둘만 터진 채 화면 양끝에 있고 그중 하나는 캐릭터를 맞힌다(90/100). **순서를 보여주는 순간은 부채꼴 그 자체**라 45로 내리고 캡션을 그림에 맞췄다 |

**조립창 그림은 팔레트를 「문양」 탭으로 열어 찍는다.** 창은 열릴 때마다 「진」 탭으로 돌아가는데,
그 탭에는 가진 진이 하나뿐이라 **"고를 것이 없다"로 읽혔다.**

---

## 남은 위험

**심각한 순서대로.** **2026-08-10 11:20 갱신 — 제출 당일 아침 이후로 목록이 크게 줄었다.**

- ~~배포된 웹 빌드가 낡았다~~ — **닫혔다.** 그 뒤로 **네 번 더** 재배포했고, 지금 배포본은
  `gh-pages` `f6d062e` (main `4dcae4d`)다. 그 뒤의 main 커밋(`7492222`)은 **문서만 고친다** —
  ⇒ **지금 링크를 열면 심사자가 보는 것은 이 문서가 말하는 그 게임이다.**
- ~~웹 빌드가 브라우저에서 실제로 도는지 아무도 확인하지 않았다~~ — **닫혔다. 그리고 이 항목이
  이번 제출에서 가장 값비싼 것을 잡았다.**
  **검사 8,435개가 전부 초록인 채로, 웹 빌드의 한국어가 전부 `□□□`(두부)였다.** 콘솔 에러 0줄.
  Godot 내장 폰트는 라틴만 들고 있고 **데스크톱은 시스템 폰트로 조용히 폴백**한다 —
  **웹에는 폴백할 폰트가 없다.** 헤드리스로도 데스크톱 실행으로도 원리상 볼 수 없는 결함이었다.
  Noto Sans KR(SIL OFL 1.1, `assets/font/OFL.txt` 동봉)을 넣어 고쳤고 **브라우저에서 다시 열어 확인했다** —
  체력 게이지 · 연구대 · 조립대 · 조립창 팔레트 아홉 칸 · 등급 고리 전부 정상.
  (부팅 파스 에러의 원인이던 자동로드 제외 문제는 그 전에 이미 닫혔다.)
- **정상 프레임레이트에서 본 사람이 아직 없다.** 위의 브라우저 확인은 **백그라운드 탭**에서 이뤄졌고,
  브라우저가 그 탭을 **FPS 8로 쓰로틀**하고 있었다(`docs/design/hud.md`가 기록한다).
  **화면이 맞다는 것은 봤지만, 움직임이 맞다는 것은 못 봤다.** 영상 촬영이 이 검증을 겸한다
- **클리어 연출을 아무도 화면에서 보지 않았다.** 무너지는 벽 · 떠오르는 아치 ·
  `스테이지 1 클리어` 정산 화면 · 개발 종료 안내 — **구현됐고 넷은 초록이지만 눈이 닿은 적이 없다.**
  검증 중 **개발 종료 안내가 크기 0으로 그려져도 검사 320개가 통과**하는 구멍이 실제로 나왔다(수정됨).
  ⇒ **초록인 넷은 「본 게임」이 아니다.** 촬영이 사실상 첫 육안 검증이 된다
- ~~HUD에 개발용 안내 두 줄이 항상 떠 있다~~ — **닫혔다. 그리고 이 항목은 두 번 틀렸다.**
  「두 줄」이 아니라 **열다섯 줄**이었고, 그건 **이 문서를 쓴 뒤 이미 F3 뒤로 들어가 부팅 때 꺼져 있었다.**
  오늘 닫은 건 그 다음 구멍이다 — **readout은 숨었는데 키는 살아 있었다.**
  M 한 번이면 심사자 머리 위에 황소가 서고, 1 한 번이면 조립이 바뀌고, R 한 번이면 런이 날아간다.
  셋 다 「키를 눌렀다」로 안 읽히고 **게임이 깨진 걸로 읽힌다.**
  ⇒ **F3가 개발자 모드가 됐다** (`stage_input._debug_on`). 게이트 뒤: 1~6 · M/N/B/C/V · T · G · L · R · 줌.
  게이트 앞(플레이어 키): 이동 · 점프 · 발사 · Tab · V · ESC · **F(상호작용)**.
  `net_render._developer_keys_are_gated_behind_f3`가 실제 키 이벤트로 잰다
- ~~사운드가 하나도 없다~~ — **닫혔다.** 오디오 파일은 여전히 0개지만 효과음을 부팅 때 코드로 합성한다 (`src/view/sfx_bank.gd`)
- **스테이지가 하나뿐이다.** GDD는 첫 빌드를 두 스테이지로 적고 있다.
  셸에 스테이지 식별자 자체가 없고 맵은 구운 상수 하나다 —
  구조 조사 결과는 Track D/E가 들고 있다

### 고쳐진 것 (이전 판의 위험 목록에서 내린다)

**내렸다고 검증된 것은 아니다. 코드가 들어갔고 넷이 초록이라는 뜻이다.**

| 이전 위험 | 지금 |
|---|---|
| 몬스터가 맵에 0마리 — "샌드박스다" | **배치됨.** 잡몹 3무리(18) + 황소 + 거대 수탉. 자는 몹은 깨기 전에 보인다 |
| 클리어 판정이 없다 | **있다.** 벽 붕괴 → 아치 → `스테이지 1 클리어` 정산 화면 |
| 사망 시 자동 재시작이 없다 | **닫혔다.** 쓰러지면 정산 화면이 열리고 그 버튼이 마을로 보낸다 |
| 넷 3개가 빨갛다 | **초록.** **8,484 검사 · 38 그물 · 0 실패 · 22.7s** (2026-08-10 20:35, 편집이 멈춘 상태에서 측정) |
| 미커밋 에셋 13장 | 커밋됨. `assets/`의 png는 **88장**이고 워킹트리와 커밋이 일치한다 |
| HUD가 디버그 라벨 문장이었다 | **게이지가 됐다.** 체력은 좌하단 바 **안**에 숫자, XP는 얇은 선, 돈은 우하단 코너 |
| 웹에서 한국어가 두부였다 | **닫혔다.** Noto Sans KR 번들 (위 목록 참조) |
