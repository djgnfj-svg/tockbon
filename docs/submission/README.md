# NAN 2026 예선 제출 — 안내판

**대회**: NHN 게임 × AI 해커톤 `NAN 2026` (https://nan2026.nhn.com)
**접수 마감**: **2026-08-10 23:59:59 KST**
**본선**: 2026-09-04 14:00 ~ 09-06 19:00, 판교 NHN 플레이뮤지엄 (전일 참여 필수, 온라인 불가)
**규모**: 10팀 선발 · 총상금 3.5억 · 수상자 전원 최종 면접 직행
**참가 형태**: **개인 (1인)**

이 폴더만 한국어다. `docs/`의 나머지는 영어(CLAUDE.md의 규칙) —
**제출물은 심사위원이 읽는 글**이므로 인게임 텍스트와 같은 범주로 본다.

---

## 제출물 5개 — **하나라도 누락되면 심사 대상에서 제외된다**

| # | 항목 | 형식 | 이 리포의 대응 | 상태 |
|---|---|---|---|---|
| 1 | 플레이 가능한 빌드 + **전체 소스** | GitHub Pages 링크 | 아래 두 링크 | **배포 완료** |
| 2 | 플레이 동영상 | YouTube 링크, **30~60초** | 아래 구성안 | **촬영 필요** |
| 3 | 게임 소개 및 설명 문서 | **PDF** | [`plan.md`](plan.md) | 작성 완료, PDF 변환 필요 |
| 4 | AI 활용 기술 문서 | **PDF** | [`ai-tech.md`](ai-tech.md) | 작성 완료, PDF 변환 필요 |
| 5 | 팀원 롤 기술서 | PDF | [`roles.md`](roles.md) | **개인 참여이므로 제출 불필요** |

**제출 링크**

| | |
|---|---|
| 플레이 | **https://djgnfj-svg.github.io/tockbon/** |
| 소스 | **https://github.com/djgnfj-svg/tockbon** (공개) |
| 영상 | `(업로드 후 기입)` |

**5번은 요강상 "2인 이상 팀인 경우에만" 제출한다.** `roles.md`는 참고용으로 남겨 두되 제출하지 않는다.

---

## 1번 — 빌드와 소스

**`.exe`는 받지 않는다.** 요강 원문: "보안 상의 이유로 PC 실행 파일(.exe 등) 직접 제출은 받지 않습니다."
⇒ **웹 빌드가 정답이다.** 이미 나와 있다.

```powershell
New-Item -ItemType Directory -Force build\web
.\Godot_v4.7.1-stable_win64.exe --headless --export-release "Web"
```

산출물 `build/web/` — **40.3MB** (`index.wasm` 37.68 + `index.pck` **2.31** + 나머지 0.3).
**2026-08-10 04:39 실측** (`Get-ChildItem build\web | Measure-Object Length -Sum`).
익스포트 자체는 **6.1초**, 종료 코드 0, stderr 깨끗함.
pck가 1.5 → 2.31MB로 자란 것은 그림이 44 → 85장이 됐기 때문이고, **wasm은 템플릿 고정이라 안 변한다.**
**스레드 없는 빌드**라 COOP/COEP 헤더 없이 GitHub Pages에서 그대로 돈다.
로컬 확인은 `python -m http.server 8123 --directory build\web`.

### 배포한 방식 (재현용)

`gh-pages` **orphan 브랜치**에 `build/web/`의 내용만 올린다. `.nojekyll`을 넣어야
Jekyll이 파일을 건드리지 않는다.

```powershell
git worktree add --detach C:\temp\ghp
cd C:\temp\ghp; git checkout --orphan gh-pages
# 내용을 비우고 build/web/. 를 복사, .nojekyll 생성
git add -A; git commit -m "웹 빌드 배포"; git push -f origin gh-pages
gh api repos/<user>/<repo>/pages -f "source[branch]=gh-pages" -f "source[path]=/"
git worktree remove --force C:\temp\ghp; git worktree prune
```

**워크트리는 반드시 지운다** — 자동 정리는 거의 안 걸린다(하룻밤에 700MB, 실측).

### 요강이 요구하는 저장소 조건 — 전부 충족

- **전체 소스 코드를 같은 저장소에 포함** — `main` 브랜치
- **커밋 기록 유지** — **131개** 커밋(2026-08-10 측정, `git rev-list --count HEAD`). squash하지 않았다
- **공개(public)** — 전환 완료. 심사 계정 초대 불필요

### 공개 전 시크릿 검사 (완료)

`.mcp.json`에 PixelLab API 키가 평문으로 들어 있어서 **전체 히스토리를 훑었다.**

```bash
git rev-list --all | xargs -n 50 git grep -lE "Bearer [a-f0-9]{8}-|gho_|ghp_|sk-[A-Za-z0-9]{20}"
```

**결과 0건.** 과거에 커밋된 `.mcp.json`은 godot·aseprite만 있던 버전이고,
PixelLab이 추가된 뒤로는 gitignore에 걸려 한 번도 올라가지 않았다.

---

## 2번 — **30~60초** 영상

요강 원문: "30~60초 분량으로, **실제 게임 플레이 장면을 중심**으로 제작해 주세요."
"**AI를 이용한 조작·합성이나 타인 영상의 도용은 불가**합니다. 실제 플레이 화면 그대로 담아야 합니다."

⇒ **하네스 화면이나 개발 과정을 넣을 자리가 없다.** 60초 전부 플레이 화면이다.
AI 이야기는 4번 문서가 전담한다.

### 60초 구성안 — **아래는 원안이고, 대안 둘이 따로 있다**

> **이 원안은 오늘 밤 이전에 쓰였고, 지금은 사실이긴 하지만 불완전하다.**
> 마지막 줄까지 전부 실재하게 됐지만(게이트 통과 포함), **오늘 밤의 핵심인 세션 루프가 한 프레임도 없다** —
> 원석이 런에서 나와 영구 해금을 산다는 것, 죽어도 끝이 아니라는 것이 영상에 안 나온다.
> ⇒ **[`video-cuts.md`](video-cuts.md)에 60초 구성안 두 개를 컷 단위로 적어 뒀다.** 택일은 사용자 몫이다.
> 그 문서는 **촬영 전 검증 목록이기도 하다** — 적힌 장면 중 사람이 본 것이 하나도 없다.

| 시간 | 화면 |
|---|---|
| 0:00–0:12 | **`퍼짐→폭발`과 `폭발→퍼짐`을 연달아.** 같은 글리프, 순서만 바꿔서. 자막 두 줄만 |
| 0:12–0:25 | 지형이 4px 단위로 파이고, 불이 나무에만 붙는다. **물은 이제 디버그로 못 붓는다** (F·K 제거) — 물 장면은 방① 보상 이후에만 찍을 수 있다 |
| 0:25–0:40 | 나무벽에 막힘 → 구덩이 → 황소 → **불 룬 획득** → 벽을 태우고 통과 |
| 0:40–0:55 | 거대 닭 보스, 물이 차오르는 탈출 |
| 0:55–1:00 | 게이트 통과 |

**첫 12초가 전부다.** 순서가 결과를 바꾸는 것이 이 게임에서 한 눈에 보이는 유일한 차별점이다.
설명하지 말고 화면 둘을 붙여서 보여준다.

**공개 또는 일부 공개(링크 공유)로 업로드.** 심사 종료까지 접근 가능해야 한다.

---

## 3·4번 — PDF 변환

`plan.md`와 `ai-tech.md`를 PDF로 뽑는다. 표가 많으므로 마크다운 렌더러를 거친다.

```powershell
# 예: VS Code 확장 "Markdown PDF" 또는
npx md-to-pdf docs/submission/plan.md docs/submission/ai-tech.md
```

**PDF 첫 페이지에 플레이 링크와 영상 링크를 박는다.** 요강이 3번 문서에 요구하는 항목이다.

---

## 작업 순서 (마감 역산)

1. **웹 빌드를 다시 굽고 재배포한다** — 지금 배포된 것은 오늘 밤 이전의 샌드박스 빌드다.
   **이걸 안 하면 나머지 넷이 전부 무의미하다**
2. **실제로 브라우저에서 한 판 끝까지 돌려본다** — 웹 빌드가 검증된 적이 없고,
   **오늘 밤 들어간 것들은 헤드리스 밖에서 한 번도 안 보였다.** 이 단계가 사실상 첫 육안 검증이다
3. **60초 영상 촬영 + 업로드**
4. **PDF 2종 변환, 링크 삽입**
5. **신청서 제출** — 마감 23:59:59

---

## 적대적 검토 — 3차까지 진행됨

| 차수 | 물은 것 | 나온 것 |
|---|---|---|
| 1차 | 이 문서의 문장이 사실인가 | **거짓 3건** — 클리어 판정 · 사망 재시작 · 디버그 키 번호 |
| 2차 | **이 빌드가 게임인가** | **몬스터가 0마리.** "게임이 아니라 샌드박스다" |
| 3차 | **이 문서가 지금도 사실인가** | **대부분 아니다.** 하룻밤에 통째로 낡았고, **배포된 빌드가 문서와 다른 게임이다** |

무엇이 틀렸었는지는 전부 [`review.md`](review.md)에 남겼다 — **지운 것이 아니라 기록했다.**
**1·2차가 "없다"고 정직하게 고쳐 놓은 문장들이 구현이 앞지르면서 새 거짓이 됐다** — 3차가 그것을 고쳤다.

---

## 남은 위험

**심각한 순서대로.** 2026-08-10 07:45 갱신.

> **오늘 아침에 들어간 것** (커밋 `16fa0b7`) — 아래 위험 목록보다 먼저 읽어야 순서가 맞는다.
> **몬스터가 때리고 물러난다.** 돼지는 겹쳐 있는 동안 **초당 4회** 데미지를 넣고 있었고(막는 건 무적 4틱뿐),
> 사용자 판정은 「비비비 비비니까 재미가 없고」였다. 이제 쿨다운(돼지 1초·늑대 0.7초)이 박자고,
> 충전 중엔 뒤로 물러난다. **늑대는 데미지가 0이었다** — 분기가 없어서 모션만 도는 장식이었다.
> 공격 모션도 실제 스윙에 붙였다(전엔 근처에만 있으면 계속 재생하며 0을 줬다).
> 그리고 **카메라를 화면 픽셀에 붙였다** — 캐릭터는 정수 위치, 카메라는 float이라 세상 전체가
> 매 프레임 1px 떨렸고 캐릭터가 버벅이는 것으로 보였다.

- ~~배포된 웹 빌드가 낡았다~~ — **닫혔다. 2026-08-10 07:44에 재배포했다** (`gh-pages` `2fca23c`,
  main `b0d00f8`). 바뀐 건 `index.html`과 `index.pck` 둘뿐이고 **`index.wasm`은 템플릿 고정이라
  바이트가 같다** — 이 문서가 적어둔 그대로다. pck 2.43MB, 전체 40.3MB, 파일 9개.
  구운 pck를 헤드리스로 120프레임 돌려 **파스 에러 0줄**을 확인하고 올렸다.
  ⇒ **이제 심사자가 여는 것은 「샌드박스」 빌드가 아니다.**
- **웹 빌드가 브라우저에서 실제로 도는지 아직 아무도 확인하지 않았다.** **다만 이유 하나는 찾아서 고쳤다** —
  `project.godot`의 자동로드(`MCPGameBridge`)가 가리키는 스크립트를 웹 프리셋이 pck에서 빼고 있었고,
  **그 pck는 부팅에서 파스 에러를 쏟는다**(되돌려 굽고 재현·확인함). `export_presets.cfg`에서 그 제외를
  없앴고, 고친 pck는 헤드리스로 **깨끗하게 부팅한다.**
  ⇒ **그래도 브라우저에서 연 사람은 아직 없다.** wasm 로딩·캔버스·입력은 헤드리스로 증명되지 않는다.
  **재배포 직후 반드시 열어볼 것**
- **아무도 오늘 밤의 결과물을 화면에서 보지 않았다.** 클리어 연출(무너지는 벽 · 떠오르는 아치),
  `스테이지 1 클리어` 화면, 개발 종료 안내, 2단 점프와 그 표시, 선반 위의 닭, 연구대 클릭 —
  **전부 구현됐고 넷은 초록이지만 눈으로 본 사람이 없다.**
  검증 중 **뮤테이션이 초록으로 살아남는 구멍 4개**가 나왔고, 그중 하나는
  **개발 종료 안내가 크기 0으로 그려져도 검사 320개가 통과**하는 것이었다(수정됨).
  ⇒ **초록인 넷은 「본 게임」이 아니다.** 영상 촬영이 사실상 첫 육안 검증이 된다
- ~~HUD에 개발용 안내 두 줄이 항상 떠 있다~~ — **닫혔다. 그리고 이 항목은 두 번 틀렸다.**
  「두 줄」이 아니라 **열다섯 줄**이었고, 그건 **이 문서를 쓴 뒤 이미 F3 뒤로 들어가 부팅 때 꺼져 있었다.**
  오늘 닫은 건 그 다음 구멍이다 — **readout은 숨었는데 키는 살아 있었다.**
  M 한 번이면 심사자 머리 위에 황소가 서고, 1 한 번이면 조립이 바뀌고, R 한 번이면 런이 날아간다.
  셋 다 「키를 눌렀다」로 안 읽히고 **게임이 깨진 걸로 읽힌다.**
  ⇒ **F3가 개발자 모드가 됐다** (`stage_input._debug_on`). 게이트 뒤: 1~6 · M/N/B/C/V · T · G · L · R · 줌.
  게이트 앞(플레이어 키): 이동 · 점프 · 발사 · Tab · P · ESC · **F(상호작용)**.
  `net_render._developer_keys_are_gated_behind_f3`가 실제 키 이벤트로 잰다
- **사운드가 하나도 없다** — 영상에서 무음이 된다. 확인함: 오디오 파일 0개
- **스테이지가 하나뿐이다.** GDD는 첫 빌드를 두 스테이지로 적고 있다.
  셸에 스테이지 식별자 자체가 없고 맵은 구운 상수 하나다 —
  구조 조사 결과는 Track D/E가 들고 있다

### 고쳐진 것 (이전 판의 위험 목록에서 내린다)

**내렸다고 검증된 것은 아니다. 코드가 들어갔고 넷이 초록이라는 뜻이다.**

| 이전 위험 | 지금 |
|---|---|
| 몬스터가 맵에 0마리 — "샌드박스다" | **배치됨.** 잡몹 3무리(18) + 황소 + 거대 닭. 자는 몹은 깨기 전에 보인다 |
| 클리어 판정이 없다 | **있다.** 벽 붕괴 → 아치 → `스테이지 1 클리어` 정산 화면 |
| 사망 시 자동 재시작이 없다 | **닫혔다.** 쓰러지면 정산 화면이 열리고 그 버튼이 마을로 보낸다 |
| 넷 3개가 빨갛다 | **초록.** 7,699 검사 · 32 그물 · 0 실패 |
| 미커밋 에셋 13장 | 커밋됨. 워킹트리와 커밋이 png 85장으로 일치 |
