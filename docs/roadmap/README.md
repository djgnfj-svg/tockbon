# 로드맵 — 12 월 **얼리액세스 출시**까지

**도착지** : 섬 하나를 지키는 한 판이 **이기고 끝나고**, 그 얇은 판을 12 월에 판다.

⚠⚠ **이 파일은 「무엇을 · 언제 · 끝났나」 셋과 이 폴더의 모양만 담는다.** 왜 그렇게 정해졌는지, 사용자가
무슨 말을 했는지, 무엇이 뒤집혔는지는 **전부 결정 로그(`log.md`)에 있다.**
⚠ **인용을 여기 안 적는다.** 이 파일이 146 줄까지 불어난 이유가 그것이었다 (2026-08-27 에 잘라 냈다).

**상태 넷** : ✅ 끝남 · ⏳ 하는 중 · ⬜ 안 시작 · ⏸ 접힘

---

## 여는 절 — **2026-09-03 기준, 다음 세션이 여기부터 읽는다**

### ⚠⚠⚠ **먼저 할 일 하나 — 넷을 돌린다**

**2026-09-03 밤 라운드가 커밋 열넷을 올렸고 그중 한 줄도 검증이 안 됐다.** 그 라운드는 클라우드에서 돌았고
**그 기계에 고도가 없다** — 저장소가 쓰는 것은 윈도우 실행 파일이고 깃에 안 올라가며, 고도 공식 배포처는
프록시가 막는다. **마지막 실측은 9/2 밤의 통과 3187 · 실패 4 이고, 그 뒤로 아무 숫자도 안 났다.**
⚠ **가지는 `claude/main-push-timing-8kvpv1` 이고 `main` 이 아니다.** 티켓마다 커밋이 따로 끊겨 있어서
아닌 것만 되돌릴 수 있다.

### ⚠⚠ **9/3 밤에 지어진 것 — 티켓 일곱, 전부 열린 채**

| 티켓 | 무엇이 지어졌나 |
|---|---|
| **03-14 결함 2** | 넘친 몸이 층을 안 넘는다. 찬 위층 칸을 누르면 아무도 안 가고 `sent` 이 0. ⚠ **옛 검사 하나가 정확히 반대를 재고 있어서 같이 뒤집었다** |
| **08-01** | `IslandGen` — 시드 하나로 글자판 · 높이 · 성채 · 자원 칸. **파이썬으로 400 시드 먼저 돌려서 규칙을 다 지키는 것을 확인했다.** ⚠⚠ **3D 는 안 지었다** |
| **08-02** | 자원 칸이 생성된 섬에 선다 — 08-01 안에서 같이 |
| **05-08** | 창고 — 여섯 종류가 따로 쌓인다. 첫 판에는 없다 |
| **05-07** | 허기 — 안 먹으면 죽고, 배고프면 스스로 창고로 간다 |
| **05-09** | 낚시 — 물가에 서면 10 초마다 물고기. **좋은 자리는 배가 없어서 못 지었다** |
| **05-10** (새 티켓) | 채집 — 자원 칸을 누르면 부대가 그 옆으로 가서 캔다 |
| **09-02** | 바리케이트 — 서고, 양쪽 다 막고, 갇힌 늑대가 부순다 |

**고리가 하나 닫혔다** — 잡아서 창고에 넣고, 배고프면 먹는다. 그전까지 창고에 무언가를 넣는 것이 하나도 없었다.

### ⚠⚠ **9/3 밤이 새로 연 것 — 사용자가 답해야 한다**

- **「그 칸의 나무를 다 캔다」 vs 「칸은 계속 나온다」** — 사용자가 워크래프트 얘기를 하며 둘 다 댔고 **둘은 같이 못 산다.** 코드는 9/2 에 정한 「계속 나온다」에 서 있다 (05-10)
- **자원 칸에 마우스를 올렸을 때 뜨는 테두리** — 사용자가 요청했고 **화면이라 안 지었다.** 시안 라운드가 필요하다
- **바리케이트와 창고를 「여기 지어라」 할 손짓** — 오른쪽 버튼이 없어지면서 자리가 없다. 지금은 게임에서 세울 방법이 아예 없다
- **바리케이트 값 둘** — 나무 5 · 체력 30. **둘 다 지은 쪽이 잡은 첫 값이다**
- **허기 값 셋** — 닳는 속도 0.4/초 · 굶을 때 체력 2.0/초 · 찾아가는 선 40. **전부 첫 값이다**

### ⚠⚠ **9/3 밤에 잰 것 — 다음 세션이 믿어도 되는 사실**

- **섬 굽는 스크립트는 저장소에 없고, 없는 게 맞다.** 2026-08-31 에 일부러 지웠고 원본은 `blend/island.blend` 다.
  `src/` 열넷이 아직 그 파일을 가리키고 있어서 전부 고쳤고, **코드가 기하와 짝지어진 규칙 넷을
  `docs/manual/blender.md` 에 표로 적었다** — 계단 입 방향 · 계단 여섯 단 · 눈금 높이 · 부품 2.0 폭.
  **이 넷은 이제 눈으로밖에 못 맞춘다**
- **`pieces.glb` 에 든 부품은 열이다** — 이 지도가 말하던 열둘이 아니다
- **정비 라운드: 제안 일흔다섯 중 일흔둘이 기각됐다.** 170 MB(스크린샷 더미 · 시안 · 프로토타입)는
  **사용자가 9/1 에 「안 건드린다」고 한 것**이라 그대로 있다 (티켓 02-07)

---

### ✅ **9/3 아침 — 9/2 의 일곱 가지가 전부 `main` 에 들어왔다 (PR 15 ~ 21)**

**한 세션이 합치기만 했다 — 지은 것도 정한 것도 없다.** 세션 시작의 `main` 은 넷 1691 · 4 였고 끝은 **3568 · 4** 다.
빨강 넷은 그대로 (`boats` 1 · `fx_view` 2 · `shell` 1), `camera` 는 여전히 끝까지 못 간다.

- **들어온 순서** — 15 자동 뽑기 삭제 · 16 08-01 캐묻기 · 17 03-16 · 03-17 · 18 03-02 판 · 19 03-19 칼끝 ·
  20 여섯째 라운드 캐묻기 · 21 03-11 · 03-12 · 03-13 · 07-01
- **손으로 맞춘 자리** — 지도 · 로그 · 용어집 · 태스크 03 문서는 양쪽 절을 다 남겼다. 코드는 셋: 판 그리기
  둘은 남겼고, 착지 둘이 눈금 경비(`_can_hit`)를 지나게 했고, 넷 한 줄이 휘두르기 0.4 초를 기다리게 했다
- ⚠⚠ **결함 1(두 땅에 갈린 부대)에 답이 둘이다** — PR 20 의 여섯째 라운드는 「갈 수 있는 몸만 보내고 수를
  줄인다」, PR 21 의 03-12 는 「교집합이라 아무 데도 못 간다」로 지어 닫았다. **지금 도는 것은 교집합이다.**
  03-14 에 둘 다 적었다
- ⚠ 루트의 `CLAUDE.md` 는 아직 「12 월은 데모지 출시가 아니다」다 — 9/2 에 뒤집힌 줄이고 사용자만 고친다

**사용자가 답할 것 — 이름으로** : 결함 1 은 두 답 중 어느 것인가 · 03-02 판이 통과인가 · 03-19 가 화면에서
맞아 보이나 · 07-01 의 눈 · 07-02 의 가르기 (붙어서 싸우기와 지나가기를 어떻게 가르나) · 03-18 적성 · 능력치를
판에 두나 · 11-01 특성의 내용 · 태스크 07 을 몇 주에 놓나.

---

## 여는 절 — **2026-09-02 기준 (앞 라운드)**

✅ **PR 21 로 `main` 에 합쳐졌다 (2026-09-03).** PR 17 에서 갈라져 나와 PR 18 · 19 · 20 뒤에 들어갔고, 이 파일 ·
`log.md` · `GLOSSARY.md` · `03.task.md` · `battle.gd` · `hud_view.gd` · `net_draw_leaf.gd` 를 손으로 맞췄다.

### ⚠⚠ **9/3 — 하네스만 만진 라운드다. `src/` 0 줄, 티켓 0 장**

**규칙 문서 171 → 115 줄, 스킬 1476 → 1235 줄, 에이전트 675 → 577 줄.** 규칙은 하나도 안 지웠고,
줄인 것은 같은 말의 반복 · 왜 그렇게 됐나의 내력 · 문장이 이미 말한 그림이다. 가지
`claude/project-harness-review-v82cvx` 에 있고 `main` 에는 아직 없다.

- **나눈 것 둘** — 그림·메시 규칙과 MCP 다리 문서가 `docs/manual/` 로 갔다. 매 턴 실리던 것이
  필요할 때만 읽는 것이 됐다. 그 폴더에 안내문이 섰다
- **고친 것** — 규칙 문서가 12 월을 「데모」라고 말하고 있었다. 이 지도는 9/2 에 「얼리액세스 출시」로
  바뀌었고, 규칙 문서만 안 따라왔다
- **지킨 것** — 「블록 모서리는 45° 가 아니라 살짝 기울인다」가 규칙 문서에만 있었다. 블렌더 문서로 옮겼다
- ⚠ **매 턴 실리는 것은 이제 317 줄이고 그중 202 줄이 용어집이다** — 남은 가장 큰 덩어리다
- ⚠⚠ **지도 1246 줄과 결정 로그 5036 줄은 안 건드렸다.** 마무리만 쓰는 파일이고, 로그를 어떻게 할지는
  할 일 목록에 사용자 결정으로 서 있다

**하네스 실측** — 스킬 16 · 에이전트 5 · 티켓 55 (끝남 34 · 열림 15 · 잡힘 3) · 그물 20 · 검사 자리 2243.
⚠ **이 환경에서는 그물을 못 돌린다** — 고도 실행 파일도 파워셸도 없다. 마지막 실측은 9/2 밤의
**통과 3187 · 실패 4** 그대로다.

**사용자가 답해야 열리는 것 — 이 라운드에서 하나 늘었다**

- **`docs/design` 을 다시 쓰는 일** — 사용자가 9/3 에 말했고 할 일 목록에 섰다. 열일곱 장 중 열넷이
  아무 데서도 안 불린다. ⚠ 사용자가 댄 낱말 하나(「매일/패션 루프 디자인」)를 못 알아들었다

### ⚠⚠ **다음 세션이 먼저 볼 것 — 9/2 여덟째 라운드: 알람이 울리면 돌아온다**

**코드는 한 줄도 안 지었다.** 캐묻기 라운드이고, **원격 컨테이너에 고도가 없어서 게임도 그물도 못 돌렸다.**
⚠ **커밋이 `main` 이 아니라 `claude/misc-cleanup-k61sy1` 에 있다** — 이 라운드를 돌린 하네스가 가지를 못박았다.

- **태스크 12 가 섰다** — 웨이브가 예고되고, 그 사이가 나가 있는 창이다. 티켓 둘: `12-01` 알람 · `12-02` 원정.
  ⚠⚠ **8/30 의 「안 알아채는 게 맞겠다」를 뒤집는다** — `Rules.BOAT_SPEED_TILES` 1.2 가 그 줄 때문에 고른 값이다
- **웨이브는 한 번에가 아니라 천천히 시간을 두고 온다.** ⚠ **지금 코드엔 웨이브가 없다** — 30 초마다 배 한 척이다
- **나가 있어도 내 섬은 돈다**, 두 판을 한 키로 오간다, **배는 오가는 데 시간이 걸린다**
- **처음 오는 적은 바이킹·해적**, 밖에서 얻는 것은 **뼈 · 가죽 · 특수 광석**. 늑대만 오는 건 개연성이 없다고
- **특성은 특이사항이고, 값을 미는 것과 하는 짓을 바꾸는 것 둘 다다** (11-01 에 적었다). **잠과 계절은 뺐고
  밤은 넣을 것 같다** — 저녁 · 밤 · 아침이 도는 것, 분위기 때문에
- **까마귀를 주석에서까지 지웠다** — 일곱 파일 일흔네 줄. 코드는 이미 8/31 에 없었고 남은 건 옛 기록이었다

**사용자가 답할 것** — 웨이브 예고가 몇 분 전인가 · 웨이브 하나가 몇 척인가 · 원정에서 못 돌아오면 어떻게
되나 · 밤을 넣나 · 온도를 넣나 · 포탑을 넣나 · 특성이 몇 개고 나쁜 것이 있나.
⚠ **사용자가 「내일 티켓 정리하면서 진행하겠다」고 했다.**

### **9/2 여섯째 라운드 — 손이 섰고, 몸이 서로를 알아챈다**

**닫힌 것 넷** — `03-11` · `03-12` · `03-13` · `07-01`. **정해졌지만 안 지은 것 하나** — `07-02`.

- **조작은 버튼 하나다.** 왼쪽 끌기 = 상자로 고르기 · 왼쪽 짧게 누르기 = 불 켜진 칸으로 이동 · 빈 땅 ·
  물 · 어두운 칸을 누르면 손을 **쥔 채로** 둔다 · 놓기는 ESC 나 아무도 안 잡힌 상자. **오른쪽은 아무것도
  안 한다.** 03-11 이 오른쪽 이동을 지었다가 **같은 날 사용자가 써 보고 뒤집었다** — 동작이 단순해야 한다고
- **부대의 갈 자리는 교집합이고 이동선은 한 줄이다** — 이동이 하나처럼 떠야 한다는 사용자의 말에서. 두 땅에
  갈린 부대는 아무 데도 못 간다 — 03-14 의 결함 1 이 이걸로 닫혔다
- **선택 상자는 땅에 깔리는 민트 채움(04)이다.** 프로토타입 다섯 중 사용자가 켜 보고 골랐고, 선이 아니라
  끈 영역 안쪽의 색이 주제다. 채움 알파 0.28, 8 px 마다 땅에 투영, 24 칸 한도. **그림 `selection_box.png`
  는 지웠다.** 진 넷은 지웠고 시트는 `docs/reference/2026-09-02-selection-box-prototypes/`
- **걷는 몸이 자기 자리 차이를 들고 간다** — 출발할 때 안 튀고 셋이 안 겹친다. 사용자가 켜 보고 찾은 둘.
  ⚠ 늑대도 같은 표를 쓰는데 걷는 것은 안 쟀다
- **끌기 렉** — 사용자가 켜 보고 찾은 셋째. 상자는 프레임당 한 번: 220x100 에서 58 → 2.7 ms, 화면 전체
  1731 → 3.2 ms
- **검사 3.0 · 늑대 6.0 으로 서로를 알아챈다** (07-01). 성채에 이미 닿은 늑대는 안 끌려나오고, 눈금 둘
  건너서는 양쪽 다 못 때린다. **검사는 아직 안 걷는다.** 사용자가 켜 보고 알아챈 늑대가 그냥 지나간다고
  했다 → 07-02
- **07-02 에서 정해진 것** — 늑대도 한 칸 목줄, 알아챈 자리 기준, 넘으면 성채로 돌아간다 (추천을 받았다).
  계단에서 스치는 것은 계단 결함이 아니다 — 재사용 대기 2.0/2.4 초가 0.3~1.1 초 스침보다 길다

**넷: 이 가지 시작 2668 · 4 → 끝 2822 · 4, 18 개 그물.** 빨강은 그대로 넷(`net_boats` 1 · `net_fx_view` 2 ·
`net_shell` 1), `net_camera` 는 여전히 끝까지 못 간다, `net_wake` 의 stderr 480 줄은 전부터다.
`net_hand` 106 → 141 · `net_fight` 419 → 446 · `net_shell` 은 오늘 387 통과 · 1 실패.

**사용자가 답해야 열리는 것 — 이름으로**

- **07-01 의 눈** — 검사가 세 조각에서 고개를 돌린 그림(`07-01-noticed.png`, 늑대가 북서에 있어 등이
  보인다)이 알아챈 그림으로 읽히나. 사용자가 켜 봤고 이것에 대해서는 아무 말이 없었다
- **07-02 의 가르기** — 붙어서 싸우기와 이동해서 도망가기를 게임이 어떻게 가르나. 늑대 칸 누르면 싸움 ·
  빈 땅 누르면 안 멈추고 감, 을 물었고 답이 없다. **이게 안 정해지면 07-02 계획을 못 쓴다**
- **03-15** — `net_hand` 이 가끔 시작하자마자 죽는 것
- **03-14 의 남은 둘** — 꽉 찬 2층 칸을 누르면 아래층에 앉는 것 · 507 쌍 중 6 의 목적지 튐
- **태스크 07 을 몇 주에 놓나** — 주 표에 아직 없다

⚠ **이름만 적힌 구멍** — 놓을 때 상자가 한 프레임 늦게 내려온다 · 늑대의 걷는 자리 차이 안 잼 ·
03-11 의 오른쪽 사거리 경비 구멍은 오른쪽이 없어져 사라졌다.

### ⚠⚠ **9/2 밤 — 피해가 칼끝에 들어간다 (03-19), 사용자가 아직 안 봤다**

**사용자가 `main` 게임을 켜 보고 「애니메이션하고 코드적 액션이 안맞음」이라 했고, 셋이 어긋나 있었다** —
피해가 휘두르기 첫 장에 들어가고 · 맞으면 아직 안 닿은 휘두르기까지 지워지고 · 자리로 미끄러지는 몸이
숨을 쉬었다. **시뮬레이션에 휘두르기 0.4 초가 생겼다** (`Rules.SWING_LAND_SEC`, 24 서브스텝) — 대기가 0 이면
휘두르기만 시작하고, 피해는 시작 때 잠근 상대에게 0.4 초 뒤 들어가며 그새 죽었거나 사거리 밖이면 헛친다.
타격 효과는 맞힌 횟수(`*_blows`)가 오른 것으로 뜨고, 걷기와 향하는 방향은 그려지는 자리로 잰다.
✅ **PR 19 로 `main` 에 합쳐졌다 (2026-09-03).** 03-19 는 `claimed` — 「사용자가 보고 맞다고 한다」만 남았다.

- **넷 : 통과 3187 · 실패 4 · 그물 20** — `fight` 297 → 323, 빨강 넷은 그대로 (`boats` 1 · `fx_view` 2 ·
  `shell` 1), `camera` 는 여전히 끝까지 못 간다. 세션 시작 기준선 3161 · 4
- **정한 것** : 휘두르는 중에 맞아도 피해는 들어간다 — 끊으면 주기가 짧은 쪽이 늘 끊어서 검사가 한 대도 못 때린다
- **용어집** : 공격이 「네 장」이라 적혀 있었는데 코드는 여덟 장 — 코드대로 고쳤고, 「맞으면 때리던 것을
  놓는다」에 「안 닿은 휘두르기는 빼고」가 붙었다
- ⚠ **싸움은 여전히 시뮬레이션 자리로 잰다** — 쉬는 몸이 최대 1.65 조각 떨어져 그려지는 줄은 그대로 산다

**사용자가 답할 것** : 03-19 가 화면에서 맞아 보이나.


### ⚠⚠ **9/2 저녁 — 03-02 판이 섰다, 낱말이 넷으로 갈렸다, run 에 시드가 생겼다**

**몸을 누르면 왼쪽 아래에 480x180 진홍 판이 뜨고 갈무리 15px 글 열다섯 줄이 세 칸으로 선다** — 이름 ·
**상태** (체력 · 허기 · 특성) · **능력치** (공격력 · 방어력 · 공격간격) · **적성** (요리 · 제작 · 낚시 · 채광 · 벌목
N/10). 여럿이면 「이름 ○○ x 4」, 안 고르면 판이 없다. ✅ **PR 18 로 `main` 에 합쳐졌다** — 충돌은 없었다.

- **넷 : 통과 3161 · 실패 4 · 그물 20** — 새 그물 `names` 58 · `panel` 662, 빨강 넷은 그대로 (`boats` 1 ·
  `fx_view` 2 · `shell` 1), `camera` 는 여전히 끝까지 못 간다. 세션 시작 기준선은 1691 · 4 였다
- **낱말** : 아침의 「특성 = 적성」이 저녁에 **적성 · 특성 · 능력치** 셋으로 갈렸고 **상태**가 섰다 — 용어집에 있다
- **시드** : `Run.seed` 가 처음 섰다 (8/28 이후 `src/sim/` 에 시드가 없었다). 첫 몸 넷보다 먼저 심는다.
  **08-01 이 이 시드를 읽는다** — 그 티켓에 적었다
- **시안** : 픽셀랩 판 셋 · 글 배치 넷 · 아이콘 배치 둘을 게임 안에 세워 보였다. 사용자가 아이콘을 물렸고
  (「아니야 아이콘하지마」), 「일단 만들고 이후에 수정하자」로 글 세 칸이 섰다. **판을 고른 사람은 없다** —
  세션이 잡았고 되돌릴 수 있다
- ⚠⚠ **03-02 는 claimed 다** — 「사용자가 보고 통과라고 한다」 한 줄이 남았다. 사진은 보냈다
- ⚠ **사용자의 의심이 티켓에 그대로 있다** — 「이게 게임성이 옳은지 항상 흔들림」. 지도의 「1 편 디펜스
  로그라이크 · 2 편 림월드 쪽」 줄과 오늘 판에 올린 적성 · 능력치는 같은 자리에 있지 않다. **03-18 이 그 물음이다**

**사용자가 답할 것** : 03-02 판이 통과인가 · 03-18 적성 · 능력치를 판에 두나 · 11-01 특성의 내용 (성격 낱말이고
좋은 것과 나쁜 것이 있다는 데까지 왔다).

⚠⚠ **12 월이 데모에서 얼리액세스 출시로 바뀌었고, 얇게 내기로 했다.** 2026-08-26 의 「12 월은 데모지
출시가 아니다」가 뒤집혔다. **데모가 안 지던 다섯이 돌아온다** — 저장 · 옵션 화면 · 소리 · 스토어 페이지와
빌드 · 언어.

**2026-09-02 에 나란히 돈 세션 하나가 코드를 짰다 — 사용자가 화면에서 찾은 결함 둘(`03-16`·`03-17`)이 같은 날
지어지고 두 번씩 검증되고 사용자가 게임을 켜 보고 「됐다」로 닫혔다.** 앞의 두 라운드는 코드를 한 줄도 안
짰다. 출시에 날짜가 붙고, 접는 규칙과 이기는 조건이 정해진 라운드들이다.

### ⚠⚠ **9/2 여섯째 라운드 — 캐묻기 티켓 일곱을 돌아 열셋을 정했다. 코드 0 줄**

**2026-09-02, 사용자: 「로드맵에서 그랄링 필요한거 진행」.** 열린 캐묻기 일곱(03-13 · 03-14 · 04-04 · 06-02 ·
10-02 · 10-03 · 11-01) 중 **넷이 닫혔다 — 03-14 의 규칙 둘(짓는 것이 남아 open) · 04-04 · 06-02 · 10-02.**
사용자는 열세 물음 전부에 「추천대로」라고 답했고, 라운드 셋째부터 **「질문 한번에 여러개」**로 물었다.

| 정해진 것 | 값 |
|---|---|
| **03-14 결함 1** | 못 가는 몸이 섞인 부대는 **갈 수 있는 몸만 보내고 보낸 수를 줄인다** |
| **03-14 결함 2** | **자리 흘리기는 층을 안 넘는다** — 가리킨 층이 다 차면 아무도 안 간다 (`sent` 0) |
| **04-04** | **맨손**(무기도 농기구도 안 든 몸) · **병사**(무기를 든 몸) · **장비**(손에 쥐는 것). **검사 = 검을 든 병사**, 「유일한 병종」 줄은 뒤집힌 낱말 표로 |
| **06-01** | **땅 107 칸쯤 (반쯤).** 블렌더로 손 섬을 넓히는 일은 **08-01 이 대신한다** — superseded. **태스크 06 은 08 에 접혔다** |
| **06-02** | **고원이 땅의 1/3** (지금 1/6). 3층은 그대로 없다 |
| **10-02** | 포로는 **배에 남아** 그 배의 늑대가 다 죽을 때까지 아무것도 안 한다 (새 그림 0) · 늑대 배는 **안 타고 부숴서 나무** · **배마다 하나** · 안 부순 배는 남는다 |
| **10-03** | **사람만 잡힌다** — 늑대는 안 잡힌다 |

⚠⚠ **10-03 의 셋은 답 없이 열려 있다** — 무엇이 쓰러뜨리나 · 데려오기가 무엇인가 · 얼마나 누워 있나. 추천을
붙여 물은 채로 사용자가 **「음 일단 마무리하자 뭔가 애매하다」**로 닫았다. **다음에는 같은 셋을 그대로 다시 묻지
말고 그 위의 것을 찾는다.** 「얼마나 누워 있나」는 `research` 가 바깥 넷을 찾아 `docs/reference/` 에 남겼다.
⚠ **11-01 특성은 다른 세션이 정하고 있다** (사용자: 「특성관련해서는 다른 세션에서 진행중」) — **이 세션은 11-01 에
한 줄도 안 썼고, 여기서 나온 「일 하나를 빨리」도 안 적었다.** 그쪽 세션이 참이다.
⚠ **03-13 은 물을 수 없었다** — 후보 넷을 게임 안에 세워 찍기 전에는 고를 것이 없다. `tools/shot/` 의 슈터가 먼저다.
⚠ **넷은 안 돌았다** — 이 기계에 Godot 이 없다. 마지막 실측은 PR 17 의 **통과 2429 · 실패 4** 그대로고, 이
라운드는 `src/` 를 한 줄도 안 건드렸다.

### ⚠⚠ **9/2 셋째 라운드 — 성채가 20 초마다 검사를 내놓던 것을 지웠다**

**2026-09-02, 사용자: 「자동 병사 생성 지워줘 코드들」.** 02-09 가 9/1 에 세운 「이십 초에 하나」가
게임에서 나갔다. **뽑는 문과 천장 아홉은 그대로다** — 시계만 없고, **지금 그 문을 부르는 곳은 없다.**
⚠ **02-09 의 상태는 그대로 resolved 다** — 뒤집힌 줄은 그 티켓 안에 써 넣었다.

**넷: 1679 → 1691 통과, 실패 4 그대로** (`fight` 285 → 297). ✅ **PR 15 로 `main` 에 합쳐졌다.**
**넷째 라운드(문서만, 코드 0 줄)가 마무리에서 다시 쟀다 — 통과 1691 · 실패 4, 같은 넷** (`boats` 1 ·
`fx_view` 2 · `shell` 1) **· `camera` 는 여전히 끝까지 못 간다.** ⚠ **`net_process` 가 이 라운드의 티켓
여섯을 한 번 물었다** — `Status:` 뒤에 설명을 붙인 것과 `Blocked by: 08-01` 두 겹 번호. **허용값은 open ·
claimed · resolved · superseded 넷뿐이고 설명은 다음 줄에 쓴다.** 고치고 다시 재서 초록이 됐다.
✅ **PR 16 으로 `main` 에 합쳐졌다** — 03-16 · 03-17 가지와 겹친 지도·로그 두 파일은 합칠 때 손으로 맞췄다.

### ✅ **앞 라운드 — 사용자가 화면에서 찾은 결함 둘 (PR 17)**

✅ **03-16 · 03-17 은 PR 17 로 `main` 에 합쳐졌다.** 같은 날 다른 세션이 `main` 의 용어집과 이 지도를
움직였으므로(08-01 캐묻기 · 허기 · 바리케이트 · 포로) 지도와 로그 두 파일은 합칠 때 손으로 맞췄다.

**닫힌 둘이 무엇이었나**

- **`03-16`** — 판 집기가 아니라 **몸 누르기의 반 조각 원점 오류**였고, 90°·180° 에서 가슴·머리를 누르면
  0/8 로 안 잡혔다. 사용자가 「몸은 화면에서 잡자」로 답해 **몸은 이제 그려진 사각형(잉크 폭)으로 잡는다.**
  각도도 기울기도 상관없다. 덤으로 계단 레이의 2층 0.071 조각 편향과 절벽 모서리 건너뜀도 고쳤다
- **`03-17`** — 자리를 북서 조각부터 셋씩 채우던 것이 **격자 점유표(`Grid.block_seats`)** 로 옮겨져 몸이
  칸에 들어오는 순간 가운데부터 받는다. **6번 3x3 격자가 칸 가운데에 서고, 사용자가 「격자는 명령 방향으로」
  라고 답해 명령 방향으로 돈다.** 몸은 자리로 미끄러져 들어간다. ⚠ 40° 에서는 세 줄이 겹쳐 보이고 60° 에서
  3x3 로 읽힌다 — 사용자가 보고 이의 없었다

**넷: 통과 1679 · 실패 4 → 통과 2429 · 실패 4.** 빨강은 그대로 넷(`net_boats` 1 · `net_fx_view` 2 ·
`net_shell` 1), `net_camera` 는 여전히 불완전. 새 넷 둘 — `net_pick` 600 · `net_seat` 89.

⚠ **닫히지 않은 채 이름만 적힌 것** — 아홉을 빈 칸에 보내면 다섯만 도착(03-14) · 쉬는 몸이 시뮬레이션
자리에서 최대 1.65 조각 떨어져 그려지는데 싸움은 시뮬레이션 자리로 잰다 · `_clamp_cam` 의 안 돌린 반폭 ·
호버가 스윕에 한 걸음 뒤처짐 · 네 번 돌리면 `cam_yaw_deg` 가 360.

⚠⚠ **사용자의 지적을 그대로 옮긴다** — 「뭔가 세세하게 체크를 안 하는 거 같긴 하네」. **03-08 과 03-10 은
800 번 눌러 보고 스무 번 변이시켜 검증했는데, 티켓이 이름 댄 것만 쟀다.** ⇒ **회전 티켓은 누르기를,
누르기 티켓은 회전을 같이 몰아 본다.** ✅ **이 라운드가 그렇게 했다** — 03-16 의 넷은 네 각도 × 세 기울기를
다 누르고, 03-17 의 넷은 네 각도에서 격자가 안 흔들리는 것을 잰다. **그리고 `verify-look` 이 헤드리스가 못
본 것을 잡았다** — 잡히는 네모가 몸의 2.2 배였다. 화면을 안 보면 안 나온다.

### ⚠⚠ **태스크 08 이 새로 섰다 — 섬이 판마다 새로 생성된다**

**2026-09-02, 사용자: 「맵이 항상 새롭게 생성되는 알고리즘 필요함」.** 로그라이크의 「매번 다르다」가 이
게임에 하나도 없다. ⚠ **섬은 지금 코드가 아니라 블렌더가 구운 파일이다** — 5 MB 메시와 걷는 데이터를 한
번에 굽는다. ⚠⚠ **부품은 열두 벌이 아니라 열이다** (2026-09-03 실측, `pieces.glb`: `top_0` `top_0b` `top_1` `top_2a` `top_2o` `top_3` `top_4` `wall_coast` `wall_cliff` `stair`). **바뀌는 것이 배치지 메시가 아닌 것은 맞지만, 판 자국과 해안선은 부품이 아니라 섬마다 따로 구워진다** — 생성된 판에는 둘 다 없다.
✅ **`08-01` 은 넷째 라운드에서 캐물어 열다섯이 정해졌다** — 다 바뀐다 · 첫 판부터 · 같은 크기 · 시드 ·
성채는 고원 위 3 칸 안쪽 · 항구 없음 · 자원은 칸(나무 1~3 · 돌 1~3 · 철 1~2, 막힌다) · 계단 1~3 · 고원 1~2.
**`08-02` 가 자원 칸으로 섰고, 05-06 은 「철광석이 섬에 붙는다」로 녹았다.** ⚠ **태스크 09 가 같은 라운드에서
섰다** — 건물은 조각 단위 · 바리케이트로 길을 막는다.
⚠⚠ **다섯째 라운드 (같은 날) — 몸의 계기 · 바리케이트 · 채집을 캐물어 스물아홉을 물었다.** 당분간 포로
하나 · 바리케이트는 성벽이고 내 편도 막고 체력이 있고 나무 · **안 먹으면 죽는다** (허기 값이 닳아 0 이면
체력이 깎인다) · 창고 하나에 종류별로 쌓이고 지어야 한다 · 물고기가 첫 음식, 감자 · 밀 · 밭도 12 월 안 ·
낚시는 해안 어디서나, 좋은 자리는 두 칸 밖이라 배가 있어야 한다 ⇒ **배 짓기가 9 주에서 4 주로 당겨졌다.**
⚠⚠ **「배가 내려놓고 사라진다」(02-04) 가 뒤집혔다 — 배는 남고, 늑대를 다 잡은 뒤 배로 가서 포로를 구출한다.**
**05-07 허기 · 05-08 창고 · 05-09 낚시 · 09-02 바리케이트가 섰다.** 미정 셋은 10-02 에 있었고 ✅ 여섯째 라운드에 닫혔다.
⚠⚠ **같은 날 넷째 라운드 뒤쪽에서 몸의 단위가 바뀌었다** — 「하나씩 옮기는 게 정설」, **몸은 하나씩 관리하고 한
종류다.** 일반병이 죽고 03-06 이 녹고 03-07 이 답 없이 닫혔다 (섬이 커지면 다시 연다). **03-02 는 이름 · 특성 · 체력이 구석 판에 뜨는
것으로 정해졌다.** 부대는 「살짝 애매하다」로 남았고 03-05 가 그 답을 기다린다.

**넷을 돌렸다 — 통과 1679 · 실패 4.** 티켓 03-09 와 03-10 이 약속한 숫자 그대로고, **빨강 넷은 이
라운드 앞에 있던 것들이다** (`net_boats` 1 · `net_fx_view` 2 · `net_shell` 1).
⚠ **러너의 새 경비가 처음 돌아 셋을 물었다** — `net_camera` 가 함수 하나를 중간에 버리는 것, 그리고
`net_camera`·`net_fx_view`·`net_wake` 의 엔진 짖음. **합치기 전 커밋에서 셋 다 똑같이 나왔다** (stderr
19 · 380 · 480 줄) ⇒ **합침이 만든 것은 하나도 없다.**

### ⚠⚠ **12 월 출시에 날짜가 붙었다 (2026-09-02)**

| 언제 | 무엇 |
|---|---|
| **9 월 안** | **$100 을 낸다.** ⚠ **30 일 시계는 페이지가 아니라 결제에서 돈다** |
| **10 월 21 일쯤** | **스토어 페이지 제출.** 밸브가 「띄우고 싶은 날의 7 영업일 전」이라고 적어 뒀다 |
| **10 월 말** | **페이지가 선다. 그리고 게임이 만들어져 있다** |
| **10 ~ 11 월** | **지켜보는 두 달. 마케팅이 같이 돈다** |
| **12 월** | **얼리액세스 출시** |

⚠⚠ **2026-09-02 에 등록이 9 월 말에서 10 월 말로 밀렸다.** 9 월 말에 찍을 스크린샷은 자원도 보스도 없는
게임이고, **10 월 말이면 게임이 다 서 있다.** ⚠ **위시리스트가 접는 기준이 아니게 됐기 때문에 한 달 덜
모으는 값이 싸졌다.**
⚠⚠ **돈과 페이지를 떼어 놓는 것이 이 결정의 핵심이다** — 둘을 붙이면 10 월 말 결제 → 11 월 말에야 낼 수
있어서 슬랙이 없다.

⚠⚠ **접는 판단은 출시 뒤고, 위시리스트가 아니라 판매를 본다.** 같은 라운드에서 합의됐던 「위시리스트
3000」이 뒤집혔다 — **3000 은 「6 개월 다 해 보고」의 숫자**이고 이 페이지는 두 달을 조용히 서 있는다.
조용한 페이지의 첫 두 주 중앙값은 **149** 다.
⚠⚠ **마케팅은 이름만 있고 정의가 없다** (사용자: 「마케팅은 뭐라 할지 모르겠네」). **티켓도 태스크도 주도
아니고, 날짜만 붙은 구멍이다.**

**스팀 실측** — **$100, 게임 하나당 한 번.** 매출 $1,000 넘으면 회수된다. **결제 뒤 30 일은 못 낸다**
⇒ 9 월 말 결제면 12 월 출시가 열린다. **Coming Soon 페이지는 공짜로 위시리스트를 모으고 최대 기간이
없다.** ⚠ **본인 명의 은행 계좌와 세금 서류가 필수고, 그건 돈이 아니라 시간이다 — 아무도 안 잡아 뒀다.**

### ✅✅ **이기는 것이 생겼다 — 정해진 시각에 오는 보스를 잡으면 클리어**

⚠⚠ **이 게임에 이기는 조건이 생긴 것은 이번이 처음이다.** 전투 코드에는 진 것만 있고 이긴 것이 없었다.
**보스는 드래곤이고 새로 뽑는다** — 이 저장소의 몸 그림은 늑대와 사람 두 벌뿐이라 **새 그림 124 장**이다.
⚠ **드래곤이 걸어오는지 날아오는지는 안 정했다** (사용자: 「보스가 온다는 것만 정해 놓으면 되잖아」).
**「걷는다」를 정해진 것으로 읽지 마라** — 몸이 움직이는 규칙은 흐름장 하나뿐이고 나는 것은 없는 규칙이다.

### ⚠⚠ **게임 이름이 `tockbon` 이 아니다 — 9 월에 정한다**

**`tockbon` 은 폴더 이름이고 고른 사람이 없다.** 프로젝트 파일의 `config/name` 에만 있고 설계 문서 어디에도
없다. ⚠ **이름 없이는 스토어 페이지를 못 열고, 등록 뒤에는 주소에 박혀서 안 바뀐다** ⇒ 9 월의 막는 것이다.

### ⚠⚠ **얇게 낸다 — 1 편은 디펜스 로그라이크, 2 편이 림월드 쪽이다**

**들어가는 다섯** : **이길 수 있는 한 판** · **판이 점점 세지는 것** · **화면 셋 (제목·일시정지·결과)** ·
**소리** · **스토어 페이지와 빌드**.

⚠⚠ **「얇게」가 「주를 지운다」는 뜻이 아니다 — 2026-09-02 에 사용자가 직접 잡았다.**

> ***「채집 트리도 들어가고 포탑은 없어져도 될 거 같고. 짐승 종류 들어가고 골렘 들어가고 나무 배 들어가고
> 새 땅 들어가는데? 자꾸 니 멋대로 뭘 없애는데. 그러지 마. 애초에 이 주 동안 개발한 양도 꽤 되고. 만들
> 수 있어. 애초에 AI 로 만들고 있는데.」***

**돌아온 것** : **채집과 테크트리 · 짐승 종류 · 골렘과 땅 넓히기 · 나무 배 · 새 땅.**
**빠진 것은 포탑 하나뿐이다** (사용자: 「포탑은 없어져도 될 거 같고」).
**여전히 빠지는 것** : 장비창 · 포획 · 등급과 특성과 종족.

⚠⚠ **모델이 주 표에서 여덟 주를 지웠다가 되돌렸다.** 앞 세션 문서의 「4 주부터 11 주까지가 통째로
빠지는 것」이라는 줄을 사용자 확인 없이 실행한 것이고, **그 줄은 사용자가 고른 적이 없다.**
⚠ **속도를 남의 속도로 재지 마라** — 이 저장소는 AI 로 만들고 있고, 2 주에 티켓 아홉이 닫혔다.

⚠⚠ **멀티는 아직 자리가 없다.** 2026-09-01 의 「멀티가 와야 맞아. 돈 벌어야지」가 살아 있고,
**사용자가 아직 그 자리를 고르지 않았다.**

### ⚠⚠ 이 세션이 코드보다 먼저 건드린 것 — **용어집이 거짓말을 하기 시작했다**

- **병종이 없어졌다** — 낀 무기가 동작을 정한다. 용어집의 「검사 — 플레이어의 몸, 그리고 유일한 병종」이
  죽는 줄이다
- **등급 · 종족이 되살아났다** — 둘 다 죽은 낱말 표에 있고, 등급은 사용자가 두 번 직접 물렸던 것이다
- **장비 슬롯 셋 (머리·몸·손) 이 되살아났고 몸마다 붙는다** — 2026-09-01 의 「슬롯 셋을 뺀다」와
  「장비는 부대 전체가 한 번에」 둘을 같이 뒤집는다
- **적에 사람이 더해진다** — 늑대는 남는다. 여러 종류와 떼는 나중이다
- ⚠ **용어집은 아직 하나도 안 고쳤다.** `naming` 이 도는 라운드가 따로 필요하다

### ⚠⚠ 실측 — 이 라운드가 잰 것 넷

| 무엇 | 값 |
|---|---|
| **몸 한 벌** | **124 장 = 네 방향 × 서른한 장** (대기 8 · 공격 8 · 죽음 6 · 이동 4 · 피격 4 · 정지 1) |
| **사람 그림** | **`assets/human/man` 한 벌뿐.** ⇒ **적을 사람으로 해도 새 그림 0 장** |
| **몸이 가진 것** | `type_id` · `alive` · `slot_id` **셋뿐이다.** 다섯 숫자는 전부 `UNITS` 줄에서 온다 |
| **이기는 것** | **코드에 없다.** `battle.gd` 에 `lost` 만 있고 `won` 이 없다 |

### ⚠⚠ 사용자가 답해야 열리는 것

⚠⚠ **「이기는 조건이 정확히 무엇인가」는 2026-09-02 에 답해졌다** — 정해진 시각에 오는 보스를 잡으면
클리어. **이 줄은 지운 것이 아니라 답해져서 내려갔다.**

**마케팅이 무엇인가** (사용자가 직접 모르겠다고 했고 10 · 11 월 두 주가 여기 걸려 있다) · **게임 이름**
(9 월에 정하기로 했다) · **얇은 판의 다섯을 줄 단위로 확정** · **멀티가 정말 12 월에서 빠지나** ·
**드래곤이 걸어오나 날아오나** (사용자가 「지금 정할 필요 없다」고 미뤘다 — 재촉하지 마라).
**첫 라운드에서 물었는데 안 답해진 셋** — ~~**일반병이 싸우나**~~ (✅ 넷째 라운드에 녹았다 — 일반병이 없다) · **첫 배가 그대로 5 초에 오나** ·
~~**「덜 평면」이 셋 중 무엇인가** (고원을 넓히나 · 계단을 더 놓나 · 3 층을 여나)~~ (✅ 여섯째 라운드 — 고원을 땅의 1/3 로).

**2026-09-02 셋째 라운드가 새로 연 것** — **병사는 이제 무엇으로 뽑나** (자동 시계가 나갔고 그림은 02-09 가 미뤘고 값은 05-01 의 물음이다 — 셋 중 무엇도 안 정해졌다).

**2026-09-02 둘째 라운드가 새로 연 것** — **장비칸이 몇 개인가** (「몸마다 붙는다」까지만 정해졌고 머리·
몸·손 셋은 확인된 적이 없다). ✅ **`08-01` 의 넷은 넷째 라운드에서 답이 다 나왔다.**

**2026-09-02 넷째 라운드가 새로 연 것** — **바리케이트가 성벽인가 여섯째 건물인가 · 몸도 막나 · 부서지나 ·
값이 무엇인가** (티켓 09-01) · ~~**섬을 몇 칸으로 넓히나** (06-01 — 08-01 이 「같은 크기」를 이 숫자에 걸었다)~~ (✅ 여섯째 라운드 — 107 칸쯤).

### ⚠ 앞 세션에서 그대로 넘어온 것

~~**선택 상자 그림을 어느 것으로 하나**~~ (✅ 09-02 저녁 — 사용자가 04 땅 채움을 골랐다, 그림이 아니다) ·
~~**늑대도 목줄을 차나**~~ (✅ 09-02 저녁 — 한 칸, 07-02) · **태스크 07 을 몇 주에 놓나**.
**앞 라운드의 결함 넷 중 셋이 그대로다** — ~~두 땅에 갈린 부대~~ (✅ 03-12 의 교집합) · 꽉 찬 2층 칸 ·
한 프레임 목적지 튐 (507 쌍 중 6) · `net_hand` 이 가끔 시작하자마자 죽는 것. **티켓 03-14 와 03-15 가 들고 있다.**
✅ 03-14 의 남은 둘 중 자리 흘리기는 여섯째 라운드에 「층을 안 넘는다」 규칙이 섰다 — 짓는 것이 남았다.

### ⚠ 아직 티켓이 없는 것

**`net_boats` 의 빨강 하나** — 배가 못 서는 해변이 일곱. 티켓 02-05 가 배를 작게 하면서 만들었다.
✅ **바리케이드** — 태스크 09 가 됐다 (2026-09-02 넷째 라운드). ✅ **섬이 랜덤** — 태스크 08. ✅ **특성** —
태스크 11. ✅ **포로와 포획** — 태스크 10.
**태스크 05 의 티켓 없는 것들** — 현상수배범이 어느 문서에 사는가 · 비가 언제 오나 · 사냥 바깥 콘텐츠 ·
부대 쪼개기와 합치기.


## 이 폴더가 놓인 모양

```
docs/roadmap/
  README.md                 ← 이 파일. 지도 그 자체
  log.md                    ← 왜 그렇게 됐나. 인용은 전부 여기
  task-01-<영문이름>/
    01.task.md              ← 이 태스크가 끝나면 화면에 뭐가 있나, 한 문장
    01-<영문이름>/           ← 티켓 하나. 폴더다
      01-01.ticket.md       ← 무엇을 짓나, 화면, 끝난 기준, 구현 계획, 답
      <스크린샷·측정값>       ← 이 티켓이 어떻게 됐는지 남은 것
    02-<영문이름>/
      01-02.ticket.md
  task-02-<영문이름>/
    02.task.md
    ...
```

- **태스크 폴더는 이 폴더 바로 밑에 선다.** ⚠ 사이에 `tasks/` 같은 칸을 하나 더 두지 않는다
- ⚠⚠ **파일 이름에 번호가 들어간다.** `TASK.md` · `TICKET.md` 로 두면 **열어 놓은 탭이 전부 같은 이름**이라 어느 태스크인지 분간이 안 된다
- ⚠⚠ **티켓도 폴더다.** `.ticket.md` 가 설명이고, **그 티켓이 어떻게 됐는지 남은 것은 같은 폴더에 쌓인다**
- **티켓은 자기 태스크 폴더 안에서만 살고**, 태스크마다 **`01` 부터 다시** 번호가 붙는다
- **티켓은 두 겹으로 부른다** — 태스크 `03` 의 둘째 티켓은 **03-02**. 「티켓 02」는 답이 아니다
- **파일 안에도 번호와 이름이 같이 적힌다** — 맨 위 제목과 `Ticket:` · `Name:` 줄
- ⚠⚠ **티켓은 하루치가 아니다** (2026-09-01, 사용자: 「티켓은 뭐 하루에 하나 한다고 안 했어 ... 티켓은 여러 개여도 돼. 태스크가 두 개라는 거지. 주당」). **주당 태스크 둘이고, 태스크 하나가 드는 티켓 수는 일이 정한다** — 하나일 수도 여덟일 수도 있고 한자리에서 여럿을 끝낼 수도 있다. ⚠ **어디서 쪼갤지는 여전히 사용자가 정한다**
- **`task-00-example/` 은 형태일 뿐 일이 아니다.** 베끼되 짓지 않는다

**티켓 파일 맨 위가 그 티켓의 전부다**

```markdown
Ticket: 03-02           ← 두 겹 번호. 파일 이름과 같다
Name: 배가 남쪽에도 온다   ← 번호와 이름은 절대 떨어지지 않는다
Task: 03 — 짐승이 오고 싸운다
Type: grilling          ← grilling(결정) · task(작업)
Status: open            ← open · claimed · resolved
Blocked by: 01, 03
```

⚠⚠ **파일은 폴더 사이를 옮겨 다니지 않는다.** 상태는 파일 안의 줄이다.

## 주 지도 — **큰 그림**

⚠⚠ **핵심 재미 한 줄** (2026-08-30, 사용자): ***「부대를 조종하고 지어서 몬스터를 잡는 게임」***.
**정해진 적이 없던 줄이고, 이 표의 모든 주가 이 한 줄로 판정된다.**

| 주 | 날짜 | 무엇 | 덩어리 | 상태 |
|---|---|---|---|---|
| **1** | **2026-08-27 ~ 08-30** | **맵** — 섬이 눈으로 합격한다 | 1 | ✅ |
| **2** | **2026-08-31 ~ 09-06** | **짐승이 오고 싸운다** | 3 | ⏳ **← 오늘이 여기다 (2026-09-02, 수).** 내용은 **09-01 에 닫혔다** — 늑대가 작은 배로 와서 내리고 계단으로 올라가 성채를 부수고 GAME OVER 가 뜬다. **티켓 아홉 전부, 그리고 태스크 04 도 닫혔다.** ⚠ **닷새 일찍 끝나서 3 주 내용이 09-02 에 벌써 돌고 있다.** ✅ **09-02 저녁에 태스크 07 이 이 주 안에서 열렸다** — 검사와 늑대가 서로를 알아챈다(07-01 닫힘), 쫓는 것은 07-02 |
| **3** | **2026-09-07 ~ 09-13** | **부대를 쪼개고 합치고 명령한다 · 이동선** | 2 | ⏳ **주가 오기 전에 시작됐다 (09-02).** 명령이 칸을 받게 됐고 손 배치가 전부 정해졌다. ✅ **손이 부대를 고르는 방법이 09-02 저녁에 섰다** — 왼쪽 하나: 끌면 상자(땅에 깔리는 민트 채움), 짧게 누르면 이동, 오른쪽은 없음. ⚠ **티켓 열일곱: 열셋 끝남(`03-01`·`03-03`·`03-04`·`03-06`·`03-07`·`03-08`·`03-09`·`03-10`·`03-11`·`03-12`·`03-13`·`03-16`·`03-17`) · 하나 하는 중(`03-15`) · 셋 열림(`03-02`·`03-05`·`03-14`).** 쪼개기·합치기는 티켓이 없다 |
| **4** | **2026-09-14 ~ 09-20** | **자원 셋과 채집** — 부대를 캐러 보낸다 · **+ 나무 배 짓기** | 5 | ⬜ ⚠ **농사와 요리가 여기 붙었다 (2026-09-01)** ⚠⚠ **배 짓기가 9 주에서 여기로 당겨졌다 (2026-09-02)** — 낚시 좋은 자리가 두 칸 밖 물 위라 배가 있어야 닿는다. 나가서 약탈하는 것은 9 주 그대로 |
| **5** | **2026-09-21 ~ 09-27** | **연구대와 테크트리 · 장비 첫 줄인 활** | 5 | ⬜ ⚠⚠ **게임 이름을 정하고 $100 을 낸다** — 둘 다 주를 안 먹지만 9 월 안에 끝나야 한다 |
| **6** | **2026-09-28 ~ 10-04** | **짐승 종류를 늘린다 · 원거리로 쏘는 짐승** | 3 | ⬜ ⚠ **곰·까마귀·사자 줄을 2026-08-31 에 지웠다** — 숫자는 비석에 남아 있고, **드는 값은 줄이 아니라 그림이다** |
| **7** | **2026-10-05 ~ 10-11** | **골렘과 땅 네 칸 늘리기** | 4 | ⬜ |
| **8** | **2026-10-12 ~ 10-18** | **보스 · 시계 · 보스 아이템** — **이기는 판이 여기서 닫힌다** | 6 | ⬜ ⚠⚠ **보스는 드래곤이고 새 그림 124 장이다** |
| **9** | **2026-10-19 ~ 10-25** | **나무 배를 타고 나간다** · **+ 스토어 페이지 제출 (10-21)** | 7 | ⬜ ⚠⚠ **캡슐 여덟 장과 스크린샷 다섯 장이 이 주 전에 서 있어야 한다.** ⚠ **배 「만들기」는 4 주로 갔다 (2026-09-02)** — 여기는 나가는 것만 남는다 |
| **10** | **2026-10-26 ~ 11-01** | **도착한 새 땅과 거기서 캐는 것** | 7 | ⬜ ⚠⚠ **10 월 말이 사용자가 준 날짜다 — 「게임 일단」이 여기고 페이지가 여기서 선다** |
| **11** | **2026-11-02 ~ 11-08** | **화면 셋 (제목·일시정지·결과) · 소리** | — | ⬜ ⚠ **도구로 만든다. 코드로 안 그린다** |
| **12** | **2026-11-09 ~ 11-15** | **저장 · 옵션 화면 · 언어 · 빌드** | — | ⬜ |
| **13** | **2026-11-16 ~ 11-22** | **한 판을 끝까지 돌린다** | — | ⬜ |
| **14** | **2026-11-23 ~ 11-29** | **다듬는다** | — | ⬜ ⚠ **11 월 말이 사용자가 준 날짜다** |
| — | **12 월** | **얼리액세스 출시 — 얇게** | — | ⬜ ⚠⚠ **2026-09-02 에 데모에서 출시로 바뀌었다** |

⚠⚠ **마케팅은 주를 안 먹는다 — 10 · 11 월에 개발과 나란히 돈다** (사용자: 「마케팅까지 하면서」).
⚠⚠ **마케팅이 무엇인지는 안 정해졌다** (사용자: 「마케팅은 뭐라 할지 모르겠네」).

### ⚠⚠ **오늘은 2 주차다 (2026-09-02, 수)**

**2026-08-31 월요일에 시작한 주다.** ⚠ **이 지도가 3 주를 「진행 중」으로 달고 있었던 것이 틀렸다** —
2 주 내용이 09-01 에 닷새 일찍 닫혀서 3 주 내용이 벌써 돌고 있었고, 지도는 그것을 「3 주가 왔다」고 적었다.
**9 월 말까지는 이번 주를 넣어 네 주다** (사용자: 「구 월 말이면 사 주 남았잖아」).

### ⚠⚠ **스토어 페이지에 반드시 있어야 하는 것 — 실측 2026-09-02**

| 무엇 | 얼마 |
|---|---|
| **캡슐 그림** | **넷** — 920x430 · 462x174 · 1232x706 · 748x896 |
| **라이브러리 그림** | **넷** — 600x900 · 920x430 · 3840x1240 · 로고 PNG |
| **스크린샷** | **다섯 장, 1920x1080 이상.** ⚠ **게임 화면만** — 컨셉아트·시네마틱·마케팅 문구는 반려된다 |
| **그 밖** | 태그 다섯 · 짧은 설명 · 긴 설명 · 콘텐츠 설문 · 내부 출시일 |
| **심사** | **3~5 영업일.** 밸브가 **「띄우고 싶은 날의 7 영업일 전에 넣으라」**고 적어 뒀다 |
| **페이지가 서 있어야 하는 기간** | **출시 2 주 전부터** |

⚠⚠ **캡슐에는 읽히는 제목이 들어가야 하고, 작은 것은 120x45 로 줄어도 읽혀야 한다** ⇒ **게임 이름이
캡슐 그림보다 먼저다.**
⚠ **영상은 밸브 문서 둘이 서로 다르다** — 예고편 페이지는 「필수」, Coming Soon 페이지는 「있으면 좋다」.
**2025-03 에 필수가 풀렸다는 관측이 하나 있고, 없이 냈을 때 무엇을 잃는지는 아무도 숫자로 안 냈다.**
**출처는 `docs/reference/2026-09-02-steam-coming-soon-page-requirements.md` 에 줄마다 붙어 있다.**

⚠⚠ **이 표는 2026-09-02 에 모델이 여덟 주를 지웠다가 사용자가 되돌린 것이다.** 지워졌던 것 —
**채집 · 테크트리 · 짐승 종류 · 골렘 · 나무 배 · 새 땅** — 이 전부 제자리로 왔고, **빠진 것은 포탑
하나뿐이다.** ⚠ **앞 세션 문서의 「4 주부터 11 주까지가 통째로 빠지는 것」은 사용자가 고른 적이 없다.**
⚠ **사용자가 준 날짜는 넷이다** — 9 월 말 등록 · 10 월 말 게임 · 11 월 말까지 · 12 월 출시.
**보스는 원래 자리인 9 주에서 한 주만 당겨 8 주에 있다** — 이기는 조건이라 10 월 말 안에 서 있어야 한다.

⚠⚠ **4 주부터는 잰 것이 아니라 정한 순서다** (2026-08-30). **앞 주가 밀리면 뒤가 통째로 민다.**
⚠ **티켓은 그 주가 올 때 만든다.** ⚠⚠ **2026-09-01 에 태스크 넷과 티켓 열하나가 새로 섰다** — 아래 태스크 표를 본다. ⚠ **`03-03` 과 `03-04` 는 같이 정해진다** — 끌어서 고르기가 왼쪽 끌기를 가져가면 판을 옮길 손이 없어진다.
⚠⚠ **멀티가 2026-09-01 에 정해졌고 표에는 아직 주가 없다.** 8/30 의 「혼자 하는 것으로 가고 보류」가 뒤집혔다 — 아래 절을 본다.

## 태스크 표 — **핵심 태스크와 그 밑의 서브태스크, 두 겹뿐이다**

⚠⚠ **두 겹으로 끝난다** (2026-09-01, 사용자: 「대신 핵심 태스크랑 서브태스크로 해서 반드시 두 개로만
해주고」). **셋째 겹은 없다.**

⚠⚠ **그리고 주당 태스크는 둘이다** (2026-09-01, 사용자: 「태스크가 두 개라는 거지. 주당」).
**티켓 수는 여기 안 걸린다** — 태스크 하나가 티켓 하나를 들 수도 여덟을 들 수도 있고, 한자리에서 여럿을
끝낼 수도 있다. **세는 것은 태스크지 티켓이 아니다.**

⚠ **이번 주는 그래서 02 와 04, 정확히 둘이다.**

| # | 핵심 태스크 | 언제 | 서브태스크 | 상태 |
|---|---|---|---|---|
| **00** | 본보기 | — | 둘 (본보기) | 일이 아니다 |
| **01** | **블록으로 맵을 만들고 눈에 맞춘다** | 1 주 | **없다 — 기록이다** | ✅ |
| **02** | **몬스터가 내려서 싸우고 성을 부순다** | **2 주 (이번 주)** | **아홉** — 전부 닫힘 | ✅ |
| **03** | **부대를 조종한다** | 3 주 | **열아홉** — 열셋 끝남, 셋 claimed, 셋 열림 | ⏳ ✅ **09-02 저녁에 고르기와 보내기가 섰다 — 왼쪽 하나** |
| **04** | **낱말과 문서를 맞춘다** | **이번 주** | **넷** — 전부 닫힘. `04-04` 는 09-02 여섯째 라운드에 캐물어 닫혔다 (맨손 · 병사 · 장비) | ✅ |
| **05** | **자원을 캔다 — 채집·농사·낚시·광석** | 4 주 | **열** — 셋 닫힘(05-01 · 05-05 · 05-06). ⚠⚠ **2026-09-03 밤에 창고 · 허기 · 낚시(해안) · 채집이 지어졌고 05-10 이 새로 섰다 — 검증은 하나도 안 됐다.** 밭 · 우물 · 요리는 손도 안 댔다 | ⬜ ⚠ **2026-09-01 에 아홉 가지가 정해졌다** — 병사가 다 캔다 · 주인공은 없다 · 요리는 건물 · 작물 둘 · 농사의 값은 자리와 시간. **상인·포로·생활 레벨·등급은 12 월에서 뺐다** |
| **06** | **기본 섬을 넓히고 높인다** | **08 에 접혔다** | **둘** — 06-01 superseded (08-01 이 대신), 06-02 닫힘 | ⏸ ✅ **09-02 여섯째 라운드: 땅 107 칸쯤 · 고원 1/3 · 3층은 그대로 없다.** 손 섬을 블렌더로 넓히는 일은 없어졌고 숫자만 남았다 |
| **07** | **몸이 서로를 알아채고 쫓는다** | **아직 안 붙었다** | **둘** — 07-01 닫힘, 07-02 목줄만 정해짐 | ⏳ ⚠⚠ **모델이 자른 태스크다** — 자르는 것은 사용자의 몫. ✅ **09-02 저녁에 07-01 이 지어졌다** — 검사 3.0 · 늑대 6.0. ⚠ **07-02 는 「싸우러 가기 / 지나가기」를 어떻게 가르나가 열려 있다** |
| **08** | **섬이 판마다 새로 생성된다** | **아직 안 붙었다** | **둘** — ⚠⚠ **2026-09-03 밤에 판 데이터가 지어졌다. 3D 는 통째로 남았고 그래서 생성된 섬을 아직 아무도 못 봤다** | ⬜ ⚠⚠ **2026-09-02 에 사용자가 요구해서 섰고 같은 날 열다섯이 정해졌다** — 로그라이크의 「매번 다르다」가 이 게임에 하나도 없다. ⚠ **섬은 지금 코드가 아니라 블렌더가 구운 파일이다** |
| **09** | **건물을 조각 단위로 짓고, 바리케이트로 길을 막는다** | **아직 안 붙었다** | **둘** — 09-01 닫힘. ⚠⚠ **2026-09-03 밤에 09-02 가 지어졌지만 게임에서 세울 손짓이 없어서 아무도 못 세운다** | ⬜ ⚠⚠ **2026-09-02 넷째 라운드에서 사용자가 말해서 섰다** — 「바리게이트를 지어서 오는걸 막아야할듯」. 항구가 없어져 짐승이 아무 변에서나 오니 길목은 플레이어가 만든다 |
| **10** | **몸이 온다: 사건으로 · 포로로 · 포획으로** | **아직 안 붙었다** | **셋** — 10-01 · 10-02 닫힘, 10-03 은 「사람만」까지 답하고 셋이 열려 있다 | ⬜ ⚠⚠ **2026-09-02 넷째 라운드, 사용자: 「포로 오는것도 티켓있나? ... 다 만들어두려고」.** 자동 생성이 지워져 지금 몸이 오는 길이 하나도 없다. ⚠ **포획은 얇은 출시에서 뺀 목록에 있다** — 주는 사용자가 붙인다 |
| **11** | **특성: 몸 하나가 저마다 갖는 것** | **아직 안 붙었다** | **하나** — 캐묻는 것 | ⬜ ⚠⚠ **2026-09-02 넷째 라운드, 사용자: 「특성도 아직 티켓이 없나?」.** 요리사 하나 말고 정해진 것이 없고 `Army` 에 앉을 자리가 없다. ⚠ **얇은 출시에서 뺀 목록에 있다** |
| **12** | **웨이브가 예고되고, 그 사이가 나가 있는 창이다** | **아직 안 붙었다** | **둘** | ⬜ ⚠⚠ **2026-09-02 여덟째 라운드, 사용자: 「알람이 울리고 돌아오는 이 루프가 중요함 ... 꼭 티켓으로도 만들고」.** 알람이 무엇이 언제 오는지 말하고, 그 사이에 나가서 파밍하다 듣고 돌아온다. ⚠⚠ **2026-08-30 의 「안 알아채는 게 맞겠다」를 뒤집는다** — `Rules.BOAT_SPEED_TILES` 1.2 가 그 줄 때문에 고른 값이다 |

⚠⚠ **6 주부터 14 주까지는 태스크가 없다 (2026-09-02).** 4 주는 태스크 05 가 들고 있고 5 주도 그
연장이지만, **짐승 종류 · 골렘 · 보스 · 나무 배 · 새 땅 · 화면 셋 · 소리 · 저장 · 빌드는 주 이름만 있고
태스크가 안 섰다.** 「티켓은 그 주가 올 때 만든다」가 태스크에도 걸린다.
⚠ **스토어 페이지도 태스크가 없다** — 9 월 말이라 가장 먼저 서야 하는 것인데 5 주에 얹혀만 있다.

⚠⚠ **04 는 덩어리에 안 붙는다.** 게임이 아니라 저장소고, 사용자가 이번 주에 넣으라고 직접 말했다.
⚠⚠ **06 부터는 안 만들었다** — **다음 주부터는 아직 안 정해져 있다** (2026-09-01, 사용자: 「다음
주부턴 아직 안 정해져 있는 게 맞아」). **5 주 뒤의 주 줄들은 위 표에 그대로 있고, 태스크는 그 주가 올 때
선다.**

### 03 의 서브태스크 열아홉 — **열셋 끝남, 셋 claimed, 셋 열림**

| # | 무엇 | 상태 |
|---|---|---|
| **03-01** | 몸을 골라 갈 수 있는 자리를 보고 보낸다 | ✅ resolved |
| **03-02** | 고른 것의 정보가 화면에 뜬다 | ⏳ claimed — ✅ **2026-09-02 저녁에 지어지고 두 번 검증됐다: 480x180 판, 글 열다섯 줄, 상태 · 능력치 · 적성.** 가지 `worktree-ticket-03-02-panel`, PR 18. ⚠ **「사용자가 보고 통과라고 한다」 한 줄이 남았다** |
| **03-18** | 판에 적성 · 능력치가 남나 — 「일단 만들고 이후에 수정하자」의 「이후」 | ⬜ open — `Type: grilling`. **사용자: 「이게 게임성이 옳은지 항상 흔들림」** |
| **03-06** | 무기를 들면 부대로만 움직이나 | ✅ resolved — **녹았다 (09-02).** 몸은 하나씩 움직이고 한 종류다. 일반병이 같이 죽었다 |
| **03-07** | 이동이 재미있어야 한다 | ✅ resolved — **답 없이 닫았다 (09-02). 섬이 커졌을 때 다시 연다** |
| **03-03** | 판을 무엇으로 움직이나 | ✅ resolved — **WASD · 화면 끝 · QE 90 도** |
| **03-04** | 무리를 끌어서 고르고 보낸다 | ✅ resolved — **왼쪽 끌기가 상자, 오른쪽이 이동** |
| **03-08** | 칸 단위로 명령한다 | ✅ resolved — **이 세션이 지은 하나** |
| **03-09** | 판을 WASD 와 화면 끝으로 민다 | ✅ resolved |
| **03-10** | QE 가 90 도씩 돌리고 오른쪽 끌기 회전이 나간다 | ✅ resolved |
| **03-11** | 오른쪽 버튼이 이동 명령이 된다 | ✅ resolved — **09-02. 명령은 오른쪽을 누르는 순간 나가고 떼는 것은 아무것도 안 한다.** `net_shell` 248 → 326. **같은 날 사용자가 실험실에서 써 보고 뒤집었다** — 동작이 단순해야 하니 왼쪽 하나로. 오른쪽은 다시 아무것도 안 한다, 03-12 |
| **03-12** | 끌어서 부대를 고른다 | ✅ resolved — **09-02. 왼쪽 하나가 다 한다 — 끌면 상자, 짧게 누르면 불 켜진 칸으로 이동, 빈 땅은 쥔 채로.** 부대의 갈 자리는 교집합, 이동선은 한 줄. **사용자가 켜 보고 셋을 더 고쳤다** — 상자를 04 땅 채움으로 · 걷는 몸이 자리 차이를 들고 감 · 끌기 렉 58 → 2.7 ms. 사용자가 보고 닫았다 |
| **03-13** | 선택 상자 그림을 고르고 세운다 | ✅ resolved — **09-02. 사용자가 켜 보고 04 를 골랐다 — 땅에 깔리는 민트 채움. 그림이 아니라 기하라 그림 파일은 지웠다.** 시트는 `docs/reference/2026-09-02-selection-box-prototypes/` |
| **03-14** | 칸 단위 명령이 남긴 결함 셋 | ⬜ open — ✅ **결함 1(두 땅에 갈린 부대)은 03-12 의 교집합이 닫았다** — 아무 데도 못 간다. ⚠ **여섯째 라운드(PR 20)는 같은 결함에 「갈 수 있는 몸만 보내고 수를 줄인다」로 답했다 — 둘이 어긋나고 사용자가 정한다.** 결함 2 는 「자리 흘리기는 층을 안 넘는다」 규칙만 섰고, 꽉 찬 2층 칸과 한 프레임 목적지 튐이 남았다 |
| **03-15** | `net_hand` 이 가끔 시작하자마자 죽는다 | ⏳ claimed — **재는 절반만 섰다.** ✅ **러너의 새 경비가 2026-09-02 에 처음 돌아 셋을 물었다** |
| **03-16** | 판을 돌리면 이동이 살짝 어긋난다 | ✅ resolved — **09-02. 판 집기가 아니라 몸 누르기의 반 조각 오류였고, 몸은 이제 화면에 그려진 사각형으로 잡는다.** 계단 레이의 2층 편향도 같이 고쳤다. 사용자가 켜 보고 「됐다」 |
| **03-17** | 칸이 중앙이 아니라 모서리부터 채워진다 | ✅ resolved — **09-02. 자리가 격자 점유표에 살고, 6번 3x3 격자가 칸 가운데에 서서 명령 방향으로 돈다.** 옛 티켓 63 이 여기서 닫혔다. 사용자가 켜 보고 「됐다」. ⚠ 40° 에서는 세 줄이 겹쳐 보인다 — 이의 없었다 |
| **03-19** | 피해가 칼끝에 들어간다 — 애니메이션과 시뮬레이션의 때 맞추기 | ⏳ claimed — **09-02 밤, PR 19. 휘두르기 0.4 초 뒤 피해, 타격 효과는 맞힌 순간.** 사용자가 화면에서 아직 안 봤다 |

⚠ **열둘이라고 열이틀이 아니다.** 티켓 수는 날짜가 아니고, **어디서 자를지는 사용자가 정한다.**

### 07 의 서브태스크 둘 — **주가 안 붙었다, 하나 끝남**

| # | 무엇 | 상태 |
|---|---|---|
| **07-01** | 검사와 늑대가 서로를 알아챈다 | ✅ resolved — **09-02. 검사 3.0 · 늑대 6.0 으로 서로를 겨누고, 성채에 이미 닿은 늑대는 안 끌려나오고, 눈금 둘 건너서는 못 때린다.** 검사는 아직 안 걷는다. **사용자가 켜 봤고 한 말은 07-02 의 것뿐** — 고개 돌린 그림이 알아챈 그림으로 읽히나는 답이 없다 |
| **07-02** | 알아챈 것을 한 칸까지 쫓아간다 | ⬜ open — ✅ **늑대 목줄 정해짐: 한 칸, 알아챈 자리 기준, 넘으면 성채로 돌아감.** ⚠ **「붙어서 싸우기 / 이동해서 도망가기」를 어떻게 가르나가 열려 있어 계획을 못 쓴다** |

⚠⚠ **숫자는 다 정해졌고 하나도 화면에서 안 봤다** — 검사 탐지 **3.0 조각** · 늑대 탐지 **6.0** (표에
원래 있던 값) · 목줄 **2.0 조각 = 한 칸**. **3.0 알아채고 · 2.0 쫓고 · 1.75 때리는 줄어드는 사다리다.**

### 08 의 서브태스크 둘 — **주가 안 붙었다, 규약은 다 정해졌다**

| # | 무엇 | 상태 |
|---|---|---|
| **08-01** | 섬을 판마다 새로 만든다 | ⬜ open — ✅ **2026-09-02 에 캐물어 열다섯이 정해졌다.** 티켓에 사용자 말 그대로 있다 |
| **08-02** | 나무 칸 · 돌 칸 · 철광석 칸이 생성된 섬에 선다 | ⬜ open — **08-01 에 막혀 있다.** 나무 1~3 · 돌 1~3 · 철 1~2, 막힌다 |

⚠ **항구는 고칠 코드가 없다** — 배는 이미 열린 바다에서 떠서 섬 둘레 아무 해변에나 댄다. 섬 파일의 `H` 는
테두리 글자 108 조각일 뿐이고, 생성기는 그 글자를 안 쓴다.

### 09 의 서브태스크 하나 — **주가 안 붙었다**

| # | 무엇 | 상태 |
|---|---|---|
| **09-01** | 바리케이트가 무엇이고 어디에 서나 | ✅ resolved — **성벽이다 · 내 편도 막는다 · 짐승은 돌아가고 길이 없으면 부순다 · 체력 0 이면 사라진다 · 나무.** 값과 그림은 09-02 |
| **09-02** | 바리케이트가 서고, 늑대가 돌아가거나 뚫는다 | ⬜ open — **09-01 에 막혀 있다** |

### 10 의 서브태스크 셋 — **주가 안 붙었다, 둘 닫힘, 10-03 이 캐묻는 것**

| # | 무엇 | 상태 |
|---|---|---|
| **10-01** | 몸이 어떤 계기로 오나 | ✅ resolved — **당분간 포로 하나** |
| **10-02** | 포로가 짐승의 배에 실려 온다 | ✅ resolved — **여섯째 라운드.** 그 배의 늑대를 다 잡고 부대를 그 해변 칸으로 보내면 내 편. **포로는 배에 남아 아무것도 안 한다 · 늑대 배는 안 타고 부숴서 나무 · 배마다 하나 · 안 부순 배는 남는다.** 새 그림 0 장 |
| **10-03** | 쓰러뜨린 것을 포획한다 | ⬜ open — `Type: grilling`. ✅ **사람만 잡힌다.** ⚠ **무엇이 쓰러뜨리나 · 데려오기가 무엇인가 · 얼마나 누워 있나는 답 없이 남았다** — 사용자: 「뭔가 애매하다」. ⚠ 얇은 출시에서 뺀 목록에 있다 |

⚠⚠ **지금 몸이 오는 길이 하나도 없다** — 셋째 라운드가 20 초 시계를 지웠고, `Battle.recruit` 는 아무도 안
부르는 문이다. **첫 판에 앉은 몸이 끝까지 그 몸이다.**

### 11 의 서브태스크 하나 — **주가 안 붙었다**

| # | 무엇 | 상태 |
|---|---|---|
| **11-01** | 특성이 무엇이고 몇 개고 무엇을 바꾸나 | ⬜ open — `Type: grilling`. **무엇을 바꾸나 · 몇 개 · 나쁜 것도 있나 · 판 중에 바뀌나 · 어디서 읽나(03-02)** |

⚠ **「눌렀을 때 뜨는 UI」는 티켓 03-02 다** — 사용자가 2026-09-02 에 그 이름으로 찾았다. 무엇이 뜨는지는
아직 안 정해졌고, 특성과 타고난 숫자가 후보에 들어갔다.

### 02 의 서브태스크 여덟 — **이번 주**

| # | 무엇 | 상태 |
|---|---|---|
| **02-01** | 성채가 낮은 땅에서 타는 것을 막는다 | ✅ resolved — **이 주를 닫은 하나** || **02-02** | 체력바가 보인다 — 몸과 성채 | ✅ resolved |
| **02-03** | 게임 오버가 빨간 글씨로 뜬다 | ✅ resolved |
| **02-04** | 배가 내려놓고 사라진다 | ✅ resolved |
| **02-05** | 오는 배를 작은 배 늑대 넷으로 바꾼다 | ✅ resolved |
| **02-06** | 바닥의 마름모를 없앤다 | ✅ resolved |
| **02-07** | 안 읽히는 그림 스물넷을 지운다 | ✅ resolved |
| **02-08** | 낡은 검사를 지운다 | ✅ resolved |
| **02-09** | 병사를 뽑을 수 있게 한다 | ✅ resolved **기능만. 그림은 사용자가 미뤘다.** ⚠ **9/2 에 20 초 자동 뽑기가 지워졌다** — 문은 남고 시계만 없다 |
⚠ **여덟이라고 여드레가 아니다** — 티켓 수는 날짜가 아니다. 한자리에서 여럿을 끝낼 수 있다.

### 04 의 서브태스크 셋 — **이번 주**

| # | 무엇 | 상태 |
|---|---|---|
| **04-01** | 「부대」를 용어집에 세우고 어긋난 셋을 고친다 | ✅ resolved |
| **04-02** | 주석의 낱말을 용어집에 맞춘다 | ✅ resolved — **스물일곱 자리** |
| **04-03** | 용어집을 CONTEXT 에서 뺀다 | ✅ resolved — **`GLOSSARY.md` 가 됐다** |

## 이번 주가 여는 자리 — **2026-09-01 이 여는 자리**

### ⚠⚠ **다음 세션이 먼저 읽을 것 — 2026-09-01 마무리 기준**

**이 세션은 코드를 한 줄도 안 짰다. 태스크 다섯과 티켓 열넷을 세운 라운드다.**

| | |
|---|---|
| **닫힌 티켓** | **여덟** — 02-01 부터 02-07 과 02-09. ⚠ **이 표는 2026-09-01 까지 전부 `open` 이라고 적고 있었고, 티켓 파일들이 참이었다** |
| **이번 주 태스크** | **02 (티켓 아홉) · 04 (티켓 셋)** — 주당 둘 규칙에 맞는다 |
| **이번 주를 닫는 하나** | **02-01 성채가 낮은 땅에서 타는 것을 막는다** — 결정이 먼저다 |
| **그물** | ⚠⚠ **못 돌렸다.** 이 라운드는 고도가 없는 데서 돌았다. **마지막 실측은 9/1 의 통과 1227 · 실패 64** |

**⚠⚠ 이 라운드에서 뒤집힌 것 셋 — 전부 사용자가 직접**

- **멀티** — 8/30 의 「보류」가 **간다**로 (덩어리 8 이 살아났다)
- **체력바** — 8/28 의 「체력바 없이」가 **넣는다**로 (티켓 02-02)
- **배는 쌓인다** — 티켓 41 의 줄이 **사라진다**로 (티켓 02-04)

**⚠ 사용자가 답해야 다음이 도는 물음 — 이름으로**

**02-01 성채를 무엇으로 막나** · **02-09 무엇을 내고 병사를 뽑나** · **05-01 먹으면 무엇이 좋아지나** ·
**06-01 얼마나 넓히나** · **06-02 「덜 평면」이 셋 중 무엇인가** · **04-03 용어집을 뭐라고 부르나** ·
**03-03 판을 무엇으로 움직이나** · **03-04 끌어서 고르면 판은 무엇이 옮기나** · **03-02 정보에 무엇이 뜨나**

⚠⚠ **그리고 12 월.** 사용자가 「진짜 오래 걸리더라도 제대로 만들어 볼까, 십이 월 빼고」와 「개발 기간보단
돈 벌어야지」를 같은 세션에 말했다. **「12 월을 뺀다」는 말은 아직 안 나왔고, 이 줄은 사용자가 마지막에 한
말로만 정해진다.**

### ✅✅ **멀티로 간다 — 2026-09-01. 8/30 의 보류가 뒤집혔다**

> 사용자: 「멀티하자. 멀티가 와야 맞아. **돈 벌어야지. 개발 기간보단 돈 벌어야지**」

**2026-08-30 에 「혼자 하는 것으로 가고 결정론만 계속 지킨다」로 정해 덩어리 8 을 ⏸ 로 두었던 것이
뒤집혔다.** ⚠ **덩어리 8 이 산다. 주는 아직 안 붙었다.**

**⚠⚠ 「나중에 붙일 수 있다」는 실측이고, 2026-09-01 에 더 세게 다시 쟀다**

- **`src/` 전체에 난수가 한 줄도 없다** — `randi` · `randf` · `randomize` · `shuffle` · `pick_random`
  **전부 0 회.** grid 의 세 군데는 주석 안의 낱말이지 호출이 아니다
- **`src/sim/` 에 `Time.` 도 `OS.get_ticks` 도 없다** — 시계로 상태를 만드는 자리가 없다

⇒ **게임이 지금 통째로 결정론이다.** ⚠ **필요조건이지 충분조건이 아니다** — 그물망·호스트·입력 맞추기는
하나도 없고, 결정론은 그것들을 **다시 짓지 않아도 되게** 해 줄 뿐이다.

**⚠ 멀티가 정해지면서 가정이던 물음 둘이 진짜 물음이 됐다**

- **성이 셋이면 각각 뽑나** — 티켓 02-09 가 이걸 들고 있다
- **자원이 공유되나** — 사용자: 「성에 자원을 넣을 거였거든」

**⚠⚠ 12 월 물음이 이 줄로 한 번 더 무거워졌다**

**사용자가 「개발 기간보단 돈 벌어야지」라고 했고, 앞 라운드에서 「십이 월 빼고 제대로 만들어 볼까」를 열어
뒀다.** ⚠ **그래도 「12 월을 뺀다」는 말은 아직 안 나왔다.** **이 줄은 사용자가 마지막에 한 말로만
정해지고, 논쟁으로는 안 정해진다** — `CLAUDE.md` 의 규칙이다.

### ✅ **병사 뽑기 — 기능부터, 그림은 미룬다 (2026-09-01)**

> 사용자: 「병사 뽑는 UI 는 좀 생각해 봐야 될 거 같은데? 일단 뽑을 수 있는 기능은 만들어 놔야 될 것 같아.
> 기능 구현하고 UI 는 더 생각해 보는 걸로」

⇒ **티켓 02-09 가 됐고 화면은 `none` 이다.** ⚠⚠ **이건 「보이는 것은 도구로 만든다」의 예외가 아니다** —
**그림을 아예 안 만드는 것이고**, 만드는 날은 별도 티켓이다. **임시 단추를 놓는 것이 바로 이 티켓이 피하는
것이다.**

⚠ **자원이 `src/sim/` 에 0 회 등장하므로 「무엇을 내고 뽑나」에 답이 없다.** 그것이 02-09 의 첫 물음이다.

### ⚠⚠ **섬을 넓히는 것이 정해졌고, 「너무 평면」이 값으로 재졌다 — 2026-09-01**

> 사용자: 「그냥 기본적으로 주는 섬을 늘리자는 거. 그 섬이 너무 평면이어서」

**⚠⚠ 이것이 옛 티켓 46 「섬이 커져야 하나」의 답이다.** 8/30 부터 「부대를 쪼개고 합친다」를 막고 있던
물음이고, **답은 넓힌다다.** 태스크 **06** 이 됐다.

**섬을 2026-09-01 에 다시 쟀다 — 「평면」은 느낌이 아니다**

| 무엇 | 값 |
|---|---|
| **판 전체** | 30 × 26 = **780 조각** |
| **땅** | **284 조각 = 71 칸** |
| **고원 (눈금 2)** | **48 조각 — 땅의 16.9%** |
| **1층 평지** | **236 조각 — 땅의 83.1%** |
| **계단** | **4 조각 = 1 칸.** ⚠⚠ **섬 전체에 올라가는 문이 하나다** |
| **눈금 3 이상** | **0 조각** |

⇒ **땅의 다섯 중 넷이 한 층이고, 올라가는 길이 하나다.**

⚠⚠ **「덜 평면」이 세 가지 다른 것을 가리킨다** — 고원을 넓히나 · 계단을 더 놓나 · 3층을 여나.
**셋의 값이 전혀 다르고, 셋째는 2026-08-29 에 사용자가 직접 정한 「3층 없음」을 뒤집는다.**
**티켓 06-02 가 그것만 묻는다.**

**⚠ 낚시가 무엇에 쓰이는지도 같은 라운드에 나왔다**

> 사용자: 「병사들 잘 싸우게 하려면 낚시 이제 서브컨텐츠이긴 맞지」

⇒ **식량이 올라가는 숫자가 아니라 병사가 얼마나 잘 싸우는지에 붙는다.** ⚠⚠ **이 저장소가 자원을 왜 캐는지
말한 첫 줄이다.** **티켓 05-01 이 무엇이 좋아지는지를 캐묻는다** — 체력인가 공격인가 수인가.

### ⚠⚠ **2026-09-01 늦은 라운드 — 병사 뽑기가 정해졌고, 지도에 없는 것 다섯이 같이 나왔다**

**정해진 것 넷**

| 무엇 | |
|---|---|
| **병사를 뽑는다** | ✅ 사용자: 「병사는 뽑자」. ⚠ **언제 뽑는지는 본인이 모르겠다고 했다** |
| **태스크 05 의 묶음** | ✅ 「태스크 오 잘 묶어놨고」 — 채집·농사·낚시·광석 한 덩어리 |
| **농사와 낚시를 안 가른다** | 사용자가 차이를 직접 대 보고 (**농사는 시간이 들고 채소, 낚시는 즉각이고 고기**) **본인이 물렸다** — 「사실 그럴 필요까진 없겠다」 |
| **낚시는 메인 콘텐츠가 아니다** | 「낚시가 메인 콘텐츠까진 아니고」 |

**⚠⚠ 지도에 이미 있는 것 셋 — 새 일이 아니다**

- **섬을 넓힌다** — **8 주 「골렘과 땅 네 칸 늘리기」**, 그리고 옛 티켓 **46 「섬이 커져야 하나」**
- **섬 여러 개** · **배 타고 간다** — **10 주와 11 주**, 덩어리 7 「약탈」
- **오는 것을 막는다** — **2 주**, 지금 하는 것이다

**⚠⚠ 지도에 없는 것 다섯 — 이번에 처음 나왔다**

| 무엇 | 사용자의 말 |
|---|---|
| **요리** | 「요리가 좀 있긴 해야겠다」 |
| **사냥 — 바깥 콘텐츠** | 「사냥 외부 콘텐츠도 또 만들어야 되고」 |
| **섬이 랜덤하다** | 「섬이 좀 랜덤하고」 — ⚠ **지금 섬은 파일 하나고, 그 파일과 메시를 블렌더 한 번이 같이 쓴다.** 랜덤은 그 저작을 통째로 바꾼다 |
| **멀티에서 성 셋으로 시작한다** | 「멀티 기준으로는 성이 세 개로 시작할 거 아니야」 |
| **자원이 공유되나** | 「자원은 공유가 어떻게 되는가. 성에 자원을 넣을 거였거든」 |

**⚠⚠ 멀티가 물음에서 전제로 바뀌어 있다**

**바로 앞 라운드에서 사용자가 「솔로로 할지 멀티로 할지도 아직 못 정하고 있네」라고 했고, 같은 대화에서
「결국에 좀 멀티 컨텐츠여 가지고」와 「멀티 기준으로는 성이 세 개로 시작할 거 아니야」로 말했다.**
⚠ **둘 다 적어 둔다. 어느 쪽인지는 사용자가 마지막에 한 말로만 정해지고, 아직 그 말은 안 나왔다.**
⚠ **실측은 그대로다** — `src/sim/` 에 난수가 한 줄도 없어서 **나중에 붙이는 일이지 다시 짓는 일은 아니다.**

**⚠ 세부로 내려가면서 사용자가 직접 연 물음 셋**

- **뽑은 병사가 어떻게 내 부대로 들어오나**
- **성이 셋이면 각각 뽑나**
- **자원이 공유되나** — 성에 자원을 넣을 생각이었다

⚠⚠ **다섯 다 티켓이 없고 만들지 않았다.** 아무것도 안 정해졌고, **안 정해진 것을 티켓으로 만든 라운드가
이 저장소에서 두 번 물렸다.**

⚠⚠ **12 월 물음이 이 라운드로 더 무거워졌다.** 앞 라운드에서 사용자가 「진짜 오래 걸리더라도 제대로
만들어 볼까, 십이 월 빼고」를 열어 뒀고, **이 라운드가 지도에 없는 것을 다섯 더했다.** **여전히 물음이지
결정이 아니다.**

### ⚠⚠ **2026-09-01 늦게 열린 물음 넷 — 하나도 안 정해졌고 하나는 12 월을 건드린다**

**티켓으로 안 만들었다.** 아무것도 정해진 것이 없어서고, **안 정해진 것을 티켓으로 만든 라운드가 이
저장소에서 두 번 물렸다.**

| # | 무엇 | 왜 이게 큰가 |
|---|---|---|
| **가** | **병사를 어떻게 뽑나** | 사용자가 한 문장 안에서 **「무기를 쥐어준다」에서 「따로 뽑고 싶다」로 바꿔 말했다.** ⚠⚠ **9/1 에 방향이 한 겹 더 나왔다** — 성채에서 뽑고 거기 모이고 **그 옆에 연구 테크트리를 짓는다.** 테크트리는 5 주라서 **이대로 두면 5 주가 이번 주로 끌려온다** |
| **나** | **처음부터 늑대가 오면 안 된다** | ⚠⚠ **2 주의 끝나는 조건 자체를 건드린다.** 지금은 검사 넷으로 시작하고 배가 5 초에 온다 — **플레이어가 무엇을 하기 전에 「막아 내거나 진다」가 이미 정해져 있다** |
| **다** | **솔로냐 멀티냐** | **2026-08-30 에 「혼자 하는 것으로 가고 보류」로 정한 것을 사용자가 다시 열었다.** ⚠ 실측: `src/sim/` 에 난수가 한 줄도 없어서 **나중에 붙이는 일이지 다시 짓는 일은 아니다** |
| **라** | ⚠⚠ **12 월을 뺄까** | 사용자: 「진짜 오래 걸리더라도 제대로 만들어 볼까, 십이 월 빼고」. **이 지도 전체가 12 월 데모에 맞춰 그려져 있다** |

⚠⚠ **라는 물음이지 결정이 아니다.** **12 월 데모는 2026-08-26 에 사용자가 정한 것이고, 이 줄은 두 번
뒤집힌 적이 있으며 논쟁이 아니라 사용자가 마지막에 한 말로만 정해진다.** **아직 그 말은 안 나왔다.**

⚠ **가와 나는 태스크 02 의 `02.task.md` 에도 적혀 있다** — 그 태스크의 조건을 직접 건드리기 때문이다.

### ⚠⚠ **한 세션이 게임의 고리를 처음으로 끝까지 말했다 — 낚시·호기심, 그리고 여섯을 잘랐다**

**아무것도 안 지었다.** 사용자가 순간순간 무엇을 하는 게임인지를 끝까지 말한 라운드고,
**이 저장소가 한 번도 가진 적 없던 고리 한 벌**로 끝났다. **인용은 전부 로그에 있다.**

**고리** — 낚시한다 → 바다에 더 좋아 보이는 낚시터가 보인다 → 배가 필요해진다 → 나간다 →
새 섬에서 새 몬스터에게 맞는다 → 돌아온다 → **그동안 기지가 털려 있다** → 병사를 다시 배치한다.

⚠⚠ **이게 두 번째 죽은 게임의 사망 문장에 대한 해독제다.** 「쪼개는 데 값이 안 들어서 쪼개는 것이
결정이 아니었다」였는데, **나가 있는 동안 집이 비는 것이 쪼개기에 처음으로 값을 붙인다.**

| 정해진 것 | |
|---|---|
| **마법** | ❌ 뺀다 |
| **주인공 캐릭터** | ❌ 뺀다. ⚠ 2026-08-30 「관찰자지 영웅이 아니다」를 안 뒤집는 쪽으로 남았다 |
| **레어도** | ❌ 뺀다. **이미 두 번 지워진 것이다** |
| **장비 슬롯 셋** | ❌ 뺀다. **손 하나만 남는다** — 무엇을 쥐어주냐만 |
| **식량** | ✅ **물고기에서 나온다.** ⚠ 자원이 셋에서 넷이 되고, 2026-08-30 의 「농사·낚시 → 채집」이 뒤집힌다 |
| **장비를 누가 입나** | ✅ **부대 전체가 한 번에.** 몸 하나씩이 아니다 |
| **멀티** | ⏸ 보류 그대로. **실측: `src/sim/` 에 난수가 한 줄도 없다** — 나중에 붙이는 일이지 다시 짓는 일이 아니다 |
| **부대 고르기** | ✅ **스타크래프트처럼 끌어서 고른다** |

**⚠⚠ 이 세션이 값으로 잰 것 다섯**

| 무엇 | 값 |
|---|---|
| **몸 하나의 그림 수** | **124 장.** 디스크에서 셌다. ⚠ 같은 대화에서 108 이라고 한 것은 틀렸다 |
| **자르기 전 판의 그림 값** | **744 장** — 주인공 124 · 새 몬스터 124 · 보스 124 · 장비 셋 372 |
| **없는 UI 화면** | **여덟 개, 지금 0 개.** 지도는 화면 전체에 **한 주(12)** 만 줬다 |
| **시뮬레이션에 없는 개념** | **열여덟.** 나무·돌·철·식량·채집이 **전부 0 회 등장**하고, 「부대」도 없다 |
| **카메라가 나갈 수 있는 거리** | **20 조각.** 짐승 배는 **24 조각**에서 뜬다 — **화면 중앙에 놓을 수조차 없다** |

**⚠⚠ 검토 둘을 동시에 돌렸고, 셋에 대해 서로 다른 근거로 같은 답을 냈다** — 식량 · 주인공 · 레어도.
**그 셋이 이 라운드에서 가장 센 신호다.** 표는 로그에 있다.

**⚠⚠ 둘이 정면으로 부딪힌 자리 하나, 그리고 실측**

- **전투는 섬 파일을 안 읽는다** — 판·성채·문간을 받는다. ⇒ **코드로는 두 번째 섬이 싸다**
- **`Islands` 는 경로 하나만 읽고**, 그 assert 가 가리키는 만들기 스크립트는 **`main` 에서 지워졌다**
- ⚠⚠ **매뉴얼이 직접 적어 뒀다** — 「`.json` 둘이 메시 옆에 있는데 어떤 `.blend` 도 그걸 안 들고 있다」
- ⇒ **두 번째 판은 코드가 아니라 저작이 없다.** ⚠ 사용자가 같은 라운드에서 풀었다 — **「맵은 더 크게 만들 거고 지금은 임시 맵이다」**

**⚠ 아직 안 정해진 것 다섯**

| # | 무엇 | 왜 사용자가 정하나 |
|---|---|---|
| **가** | **일꾼이 병사와 다른 몸인가** | ⚠⚠ **일꾼이 캐고 병사가 지키면 둘이 안 부딪혀서 쪼개기의 값이 사라진다** |
| **나** | **방어구도 손 옆에 두나** | 뺀다고 했다가 한 문장 뒤에 본인이 다시 열었다 |
| **다** | **카메라를 어떻게 돌리나** | 자유 회전이냐 각도 고정이냐, 오른쪽 버튼이냐 휠이냐. **사용자가 아이디어를 요청했다** |
| **라** | **기지를 누르면 무엇이 뜨나** | 무기 관리는 뜬다고 했고, **테크트리는 「애매하다」고 열어 뒀다** |
| **마** | **없는 동안 털리면 무엇을 잃나** | **지금은 부활 20 초가 전부고 성채 말고 체력이 있는 건물이 없다** |

**⚠ 자동화가 나왔고 아무것도 안 정해졌다** — 나무와 돌을 캐는 것, 그리고 곡괭이.
**`src/sim/` 에 캐는 코드가 하나도 없어서 이어 붙일 것이 없다.**

**⚠⚠ 다음에 할 것은 사용자가 직접 지목했다** — ***「이동할 때 쓰는 UI/UX 를 정하자. 그게 진짜 핵심이다」***.
**티켓 03-04 로 세워 뒀고, 사용자가 클라우드에서 이어서 정하기로 했다.**

- **03-04 무리를 끌어서 고르고 보낸다** — 스타크래프트식 끌어서 고르기. ⚠⚠ **왼쪽 버튼에 세 번째 일이 붙는다**
- **03-03 판을 무엇으로 움직이나** — 위엣것이 왼쪽 끌기를 가져가면 **판을 옮길 손이 없어진다.** 둘은 같이 정해진다

⚠ **8/31 에 「태스크 03 이 가지 둘에 따로 있다」고 적힌 것은 해소됐다** — 2026-09-01 에 한 폴더로 접혔고,
안 지어진 티켓이 `03-01` 에서 `03-03` 으로 옮겨 갔다.

---

### ⚠⚠ **9/1 첫 세션이 가지 다섯을 `main` 으로 합쳤다 — 갈라져 있던 밤이 하나로 모였다**

**8/31 밤에 다섯 갈래로 갈라져 돌던 세션들이 여기서 만난다.** 지은 것은 없고, 지어진 것을 합쳤다.

| 합친 것 | 무엇이 들어왔나 |
|---|---|
| **props-and-ore** | 소나무 서른하나짜리 숲 · 덤불 열일곱 · 돌 셋 · 철광석 하나 |
| **drop-qe-turn** | QE 회전키 · 화면끝 자동이동 · WASD 삭제 |
| **pick-then-move** | 몸을 골라 갈 수 있는 자리를 보고 보낸다 (`Hand`) |
| **twodo-folder** | `TODO.md` 한 줄. `main` 이 이미 들고 있었다 |
| **island-findings** | 빨강 넷의 범인이 위성 섬이라는 8/30 실측 |

**⚠⚠ 손으로 판단해야 했던 자리 넷**

| 무엇 | 어떻게 갈랐나 |
|---|---|
| **한 이름이 둘을 가리켰다** | 두 가지가 서로 다른 테두리 풀에 `_outlines` 를 붙였다. **몸마다 붙는 검은 것이 이름을 지켰고, 고른 몸에만 붙는 흰 것이 `_rims` 가 됐다** |
| **`_pointer_at` 을 되살렸다** | 화면끝 이동과 같이 지워졌는데 이동선이 매 프레임 읽는다. **커서가 멈춰 있어도 고른 몸은 걷고 판은 끌려 움직인다** |
| **화면끝 이동을 재던 그물 두 줄을 지웠다** | 기능이 없어졌으니 남기면 없는 것 위의 초록이다 |
| **태스크 03 폴더가 둘이었다** | 두 가지가 서로 못 본 채 같은 밤에 각자 `03-01` 을 만들었다. **지어진 둘이 번호를 지키고 안 지어진 것이 `03-03` 이 됐다** |

**⚠⚠ 이번 세션이 값으로 잰 것 셋**

| 무엇 | 값 |
|---|---|
| **검사 파일 일곱이 작업본에서만 CRLF 였다** | `net_title` 의 텍스트 훑개가 자기 줄끝으로 남의 파일을 잘라 **한 줄로 읽고 아무것도 못 찾았다.** LF 로 되돌리니 두 줄이 초록 — **저장소 내용은 원래 옳아서 커밋할 게 없었다** |
| **섬의 실제 크기** | **26 행 × 30 자 · 땅 284 칸 · 물 388 · 항구 108 · 고원 48 칸 · 계단 4 칸 · 짐승 0 마리** |
| **늑대 그림틀** | **92 × 66.** 검사는 아직 92 × 92 로 안다 |

**⚠ 정리한 것**: 세션 번호를 시계순으로 다시 매겼다(셋이 「다섯째」를 겹쳐 썼다) · 「`main` 에 안 들어갔다」 네 줄을 합친 날로 고쳐 썼다 · 죽은 `docs/roadmap.md` 가 되살아나려는 것을 막았다 · **워크트리 여섯과 원격 가지 열셋을 지웠다** — 원격에 남은 것은 `main`·`gh-pages`·`archive-full-history`·`salvage/cell-harness` 넷뿐이다.

---

### ⚠⚠ **8/31 밤 여덟째 세션이 섬을 꾸몄다 — 나무·덤불·돌·철광석이 다 정해지고 다 섰다**

**2026-09-01 에 `main` 으로 합쳤다.** `src/look.gd` 과 `src/view/field_view.gd` 을 이 세션과
`main` 이 둘 다 크게 고쳤고, 합칠 때 양쪽을 다 남겼다.

| 무엇 | 상태 |
|---|---|
| **돌** | ✅ 시안 여섯 중 **r2** — 아홉 면 납작한 덩어리. 옆에 붙던 작은 돌은 없앴다 |
| **철광석** | ✅ **오늘까지 이 게임에 없던 것.** 시안 열둘 + 변주 여섯 중 **o8b, 허리에 광석 띠** |
| **나무** | ✅ **3D 소나무**, 기둥 노출 0.80 조각. **뽑은 그림 여덟은 사용자가 물렸다** |
| **덤불** | ✅ **2D 카드.** 넷이 서 있고 어느 것 하나로 좁히지는 않았다 |
| **그루터기** | ✅ 벨 날을 위해 미리 만들었다 |
| **흔들림** | ✅ 메시는 밑동을 축으로 눕고, 카드는 정점 셰이더로 눕는다 |
| **연구대** | ⏸ **시안 넷 있고 사용자가 「고민 안 해」로 접었다** |
| ~~**포탑**~~ | ❌ **2026-09-02 에 지웠다** — 사용자: 「그 포탑 티켓은 지우자 일단 아마 나중에 건축쪽으로 해서 모일듯」. 돌아온다면 태스크 09 로 온다 |

**⚠⚠ 이번 세션이 값으로 잰 것 다섯**

| 무엇 | 값 |
|---|---|
| **프롭 색이 게임에서 흰색이 된다** | 알베도 0.290 회색이 화면에서 **(237,245,252)**. **빛이 선형 알베도를 약 2.9 배 하고 한 칸이라도 0.31 을 넘으면 잘린다.** 프롭·건물 팔레트 전체가 블렌더 조명에 맞춰져 있었다 |
| **프롭은 오늘까지 하나도 안 놓여 있었다** | 그래서 **아무도 게임에서 본 적이 없었고**, 위 결함이 여태 안 보였다 |
| **판때기는 메시보다 작게 선다** | 같은 나무가 108 px 대 133 px. 판이 서 있는데 카메라가 40 도로 눕혀 본다 |
| **`Sprite3D` 는 못 흔들리고 외곽선도 못 입는다** | 흔들림 0.00 px · 검은 픽셀 0 개. **쿼드를 직접 들면 둘 다 된다** — 4.00 px · 257 개 |
| **40 도에서 지붕은 안을 통째로 가린다** | 연구대 시안 둘이 다리 달린 색 상자로 나왔다 |

**⚠ 밟은 덫 넷** — 스프라이트 크기를 `TILE_PX` 로 두 번 나눠 1/40 조각이 됐다 ·
`cam_px` 는 화면 중심이 아니라 보이는 땅의 왼쪽 위 모서리다 · 카메라를 옮긴 프레임을 바로 읽으면
이전 화면이 저장된다 · **감기는 샘플러가 카드 위에 검은 막대를 그린다** (UV.y=0 이 맨 아랫줄을 물어 온다).

**⚠⚠ 사용자가 답해야 하는 것 넷**

| # | 무엇 | 왜 사용자가 정하나 |
|---|---|---|
| **A** | **철광석에 어떻게 가나** | **외딴 블록에 놓아 달라고 해서 놓았고, 거기로는 걸어갈 수 없다.** 배·다리·블록을 물가에 붙이기 셋 중 하나다 |
| **B** | **맵을 2 배로 키우나** | ⚠⚠ **섬 데이터를 만드는 파일이 없다** — 통행·눈금·항구·윤곽을 담은 파일은 어떤 원본도 안 들고 있고, 메시와 같이 뽑던 스크립트는 지워졌다. **넓이 2 배인지 한 변 2 배인지도 안 정해졌다** |
| **C** | **덤불 넷 중 어느 것** | 넷이 다 서 있다. 좁힐지 넷 다 쓸지가 안 정해졌다 |
| **D** | **연구대를 언제 꺼내나** | 시안 넷 만들어 뒀고 사용자가 접었다. 5 주가 오면 그 시안부터 본다. ❌ **포탑은 2026-09-02 에 이 줄에서 지웠다** — 「나중에 건축쪽으로 해서 모일듯」, 태스크 09 |

---

### ⚠⚠ **8/31 밤 일곱째 세션이 몸에 동작 다섯을 붙이고 타격감 여섯을 넣었다**

**두 몸이 이제 대기·이동·공격·피격·죽음을 한다** — 검사와 늑대, 네 방향씩, 몸마다 그림 108 장.
**2026-08-29 에 통째로 지워졌던 공중 층이 돌아왔다.**

| 무엇 | 상태 |
|---|---|
| **동작 다섯** | ✅ **대기 8 · 이동 4 · 공격 8 · 피격 4 · 죽음 6 장**, 두 몸 다 |
| **타격감 여섯** | ✅ **히트스톱 · 히트 플래시 · 넉백 · 히트 스파크 · 슬래시 트레일 · 데미지 넘버** |
| **공중 층** | ✅ **빌보드 풀과 글자 풀 둘로 돌아왔다.** 예전 것은 `ImmediateMesh` 였고 이것은 노드다 |
| **공격 동작** | ⏳ **엔진이 네 박자로 휘두른다** — 사용자가 아직 눈으로 안 봤다 |
| **검은 늑대 시안** | ⏳ **스물넷 뽑았고 하나도 안 골랐다.** 여전히 g5 가 서 있다 |

**⚠⚠ 이번 세션이 값으로 잰 것 다섯**

| 무엇 | 값 |
|---|---|
| **생성기는 공격 자세를 안 그린다** | **다섯 번 시도해서 다섯 번 실패.** 턱·세우기·덮치기·여덟 장 나눠 부르기·서 있는 그림 직접 고치기. **여덟 장 내내 늑대 윤곽이 64 px 몸에서 7 px 움직인다** |
| **캔버스가 몸 크기를 흔들고 있었다** | 그리는 배수가 **캔버스의 배수**였다. 동작이 캔버스를 넓힐 때마다 몸이 작아지고 그림자가 커졌다. **이제 잉크 비율로 나눈다** |
| **동작 프레임이 발 높이 표에 없었다** | 서 있는 그림만 재고 있어서, **걷기 시작하면 몸이 뛰어오를 자리였다** |
| **검사 좌우 두 장이 다른 방향이었다** | 게임의 `right`·`left` 는 **돌린 정면**인데 걷기는 **옆모습**으로 뽑혀 있었다 |
| **엔진 휘두름의 이동 거리** | **뒤 3.08 px + 앞 12.01 px = 제 몸의 72%**, 늘어남 1.18 배 |

**⚠⚠ 이번 세션이 남긴, 사용자가 답해야 하는 것 넷**

| # | 무엇 | 왜 사용자가 정하나 |
|---|---|---|
| **A** | **엔진이 휘두르는 게 「공격답나」** | **눈으로 안 봤다.** 사인 곡선 하나짜리 앞 판은 「왔다 갔다 한다」고 물렸고, 이번 것은 예비동작·튐·멈춤·복귀 넷이다. **보는 법**: 게임을 켜고 섬이 열리면 짐승이 올 때까지 두거나 병사를 짐승 위로 찍어 보낸다 — **때리기 직전에 몸이 뒤로 당겼다가 튀어나가고 잠깐 멈춘다** |
| **B** | **데미지 넘버를 남기나** | **이 게임 결에 반대다** — 체력바를 이미 뺐고 배드노스에는 숫자가 없다. 「넣어줘」로 들어갔다. **보는 법**: 싸우는 몸 위에 흰 숫자가 떠올랐다 사라진다 |
| **C** | **검은 늑대를 어느 것으로 하나** | 스물넷이 시안 폴더에 있다. **로컬 검정은 틴트를 이기고 픽셀랩 검정은 갈색으로 꺾인다** |
| **D** | **네 방향 고르개를 언제 고치나** | **두 축 중 큰 쪽**으로 고른다. 대각선 넷은 **두 축의 부호**로 갈려서, 지금 넣으면 둘만 나온다 |

**⚠ 다음에 뭘 더 붙일 수 있나 — 사용자가 물었고 근거까지 조사해서 답했다**: **소리**(이 게임에 하나도
없다) · **예비동작**(공격 여덟 장에 이미 들어갔다) · **바닥에 남는 것**. ⚠ **화면 흔들림과 카메라 킥은
2026-08-25 에 사용자가 물렸다.** 자세한 것은 `2026-08-31-hit-feel-elements` 에 있다.

---

### ⚠⚠ **8/31 밤 여섯째 세션이 손에 몸을 쥐여 줬다 — 고르고, 갈 수 있는 자리를 보고, 보낸다**

**3 주의 손동작이 한 주 먼저 들어갔다.** 그전까지는 **누른 자리에서 가장 가까운 몸이 답했고**, 고른다는
개념 자체가 없었다.

| 무엇 | 상태 |
|---|---|
| **고르기** | ✅ 몸을 누르면 골라진다. **고른 것은 언제나 목록이라** 무리가 생겨도 길이만 는다 |
| **갈 수 있는 자리** | ✅ 판이 설 수 있는 조각에만 켜진다. 물·계단·정원 찬 블록이 빠진다 |
| **이동선** | ✅ 호버하면 미리 뜨고 걷는 몸을 따라간다. **실제로 걷는 그 경로다** |
| **흰 테두리** | ✅ 고른 몸의 그림을 1.22 배로 키워 뒤에 깔았다. ⚠ **밝은 바닥에서 대비가 약하다** |
| **명령** | ✅ 쥐고 있으면 이동이 우선이다 — 몸이 선 조각도 목적지다 |
| **놓기** | ✅ 명령하면 자동으로, 아니면 **ESC 로만** |
| **정보 패널** | ⬜ **안 만들었다.** 화면에 뜨는 것은 도구로 만들어야 해서 그림 작업이 따로 필요하다 |

**2026-09-01 에 `main` 으로 합쳤다.** ⚠ **흰 테두리 풀의 이름이 `_rims` 로 바뀌었다** — 같은 날 밤
동작 세션이 기법 17 의 검은 테두리를 `_outlines` 라는 같은 이름으로 넣었고, 한 이름이 둘을 가리킬 수 없었다.

---

### ⚠⚠ **8/31 밤 다섯째 세션이 카메라 조작 셋을 지웠다 — 남은 것은 왼쪽 끌기 하나다**

**사용자가 한 대화에서 셋을 차례로 지우게 했다**: QE 회전 · 화면 끝 자동 이동 · WASD.
**화면 끝 이동은 8/30 에 사용자가 직접 넣게 한 것이고, 하루 만에 나갔다.** 인용은 전부 로그에 있다.

| 무엇 | 상태 |
|---|---|
| **QE 회전** | ✅ 지웠다. **오른쪽 버튼 끌기는 그대로** — 100 px 에 18° |
| **화면 끝 자동 이동** | ✅ 지웠다. 띠 28 px · 입술 0.30 · 깃발 둘 · 알림 처리기까지 |
| **WASD** | ✅ 지웠다. **매 프레임 카메라를 밀던 줄 자체가 없어졌다** |
| **상수 넷** | ✅ `CAM_PAN_KEY_PX_PER_SEC` 와 띠의 셋. **`src/` 에 카메라를 시계로 미는 것이 하나도 없다** |
| **도구 셋** | ✅ 촬영 도구 둘과 프로토타입 하나가 **키 대신 카메라를 직접 민다** — 나오는 사진은 그대로 |
| **넷** | ✅ 마흔세 줄이 지운 기능과 같이 나갔다. **실패 집합은 세 번 다 손대기 전과 같았다** |

**⚠⚠ 이 세션이 값으로 잰 것 넷 — 「판이 안 움직인다」는 전제가 숫자와 다르다**

| 무엇 | 값 |
|---|---|
| **가로 300 px 끌기** | 판이 **393.8 px** 간다. **물에서든 몸이 선 땅에서든 같다** |
| **땅에서 끌었을 때 걷기 명령** | **0 건** — 6 px 을 넘겨 끌면 명령이 안 나간다 |
| **세로 300 px 끌기** | **584.9 px.** 기울기 때문에 세로가 더 간다 |
| **판이 갈 수 있는 동서 폭** | ⚠⚠ **1120 px, 화면은 1280 px.** **갈 곳이 한 화면보다 좁다** — 이게 「안 움직인다」로 보이는 자리다 |

**⚠ 사용자가 답해야 하는 것 하나** — **판을 무엇으로 움직이나.** 티켓 `03-03` 이 그것이고,
**모델이 고르지 않는다**: 지운 셋이 전부 사용자의 말이었다.

**2026-09-01 에 `main` 으로 합쳤다.**

---

**⚠ 아래는 8/31 밤 앞 세션 셋의 자리다.**

### 병사 그림 — ✅ `main` 에 들어갔다

**8/31 밤 세션이 병사 그림을 통째로 바꿨다.** 사용자가 후보 열여섯 중 하나를 골랐고,
**네 방향으로 게임에 서 있으며 크기까지 화면에서 정해졌다.** ✅ **`main` 에 들어갔다.**

| 무엇 | 상태 |
|---|---|
| **병사 그림** | ✅ 옅은 덩어리 · 둥근 대머리 · 검은 점 둘. **옷도 무기도 얼굴도 없다** |
| **네 방향** | ✅ 좌우가 정옆이 아니라 **튼 것**이다 (사용자: 「지금 너무 좌여서」) |
| **크기** | ✅ **27 px**. 41·33·27·23 을 한 화면에 세워 골랐고, **23 도 괜찮다**는 말이 상수에 같이 적혀 있다 |
| **빌보드 Y 축 고정** | ✅ 개발지식 01 기법 1 |
| **카메라 피치 보정** | ✅ 기법 22. 여는 각도에서 정확히 1.0 이라 고른 27 px 이 기준으로 남는다 |
| **빛** | ⚠⚠ **넣었다가 뺐다** — 기법 16. `shaded = true` 로 찍으니 **진영색 파랑이 하얗게 날아갔다**. 빌보드 법선이 카메라를 보니 해가 정면으로 때린다 |

**⚠⚠ 병사에 대해 사용자가 답해야 하는 것 넷**

| # | 무엇 | 왜 사용자가 정하나 |
|---|---|---|
| **가** | **병사 색** | 검정이라 했다가 흰색이라 했다가 **「뭔가 좀 달랐으면」으로 끝났다.** 정해진 적이 없다 |
| **나** | **3D 블록 색** | 같이 말이 나왔다. **섬 전체가 바뀌는 일**이라 병사와 같이 못 묶는다 |
| **다** | **뒤우 · 뒤좌를 넣나** | 그림은 이미 뽑혀 있다. **고르개를 「어느 축이 큰가」에서 「두 축의 부호」로** 바꿔야 하고 **늑대가 그 고르개를 같이 쓴다** |
| **라** | **걷기를 어디서 뽑나** | 사용자가 보류했다. 픽셀랩 것은 **머리가 절반으로 줄어 못 쓴다**. 콤피 UI 는 늑대 걷기를 그렇게 만들었고 공짜다 |

**⚠ 병사에서 잰 것 하나** — **튼 두 장에서 뒤쪽 발이 앞발보다 2 px 위에서 끝난다.** 크게 보면 뒤로 뺀
발이지만 27 px 에서는 한쪽이 없어 보인다.

### 아홉이 서는 모양 — **6번으로 정해졌다**

**블록 3x3 격자가 부대의 보는 쪽을 따라 돈다.** 시안 여섯 벌을 화면에서 보고 사용자가 골랐다.
⚠ **정해진 것은 모양이고, 게임에는 아직 안 들어갔다** (63). 지금 게임은 조각마다 고리로 흩뿌린다.
⚠⚠ **자리는 조각이 아니라 블록이 나눠 준다** — 실측이다. 조각 단위로 나누면 걸어온 결과(3·3·2·1)와
안 맞아 자리를 못 받는 몸이 생긴다.
⚠⚠ **병사가 27 px 로 줄면서 「지금 크기가 이미 아홉의 한계」는 깨졌다** — 그 값은 옛 그림(그리는 배율
1.0)에서 잰 것이고, 지금 배율은 0.65 다. **아홉을 놓고도 몸을 키울 자리가 생겼다.**

---

**⚠ 아래는 8/31 낮까지의 자리이고 그대로 열려 있다.**


⚠⚠ **1 주가 닫혔고 2 주는 지어졌지만 안 닫혔다.** 월~수의 배도 목~일의 전투도 2026-08-30 에 섰다 —
**엿새 일찍.** **남은 것은 짓는 일이 아니라 닫는 일이다.**

**⚠ 8/31 낮 세션은 `src/` 를 하나도 안 건드렸다.** 개발지식 폴더와 `knowledge` 스킬만 손봤다.

**⚠⚠ 8/31 저녁 세션은 아래 물음 둘에 손을 안 댔다. 대신 만드는 방식이 바뀌었다.**
**메시는 이제 `blend/` 의 원본을 열어서 고친다** — 스크립트로 다시 짓지 않고, `tools/blender/` 는
지웠다. 사용자가 여러 번 말했는데 계속 무시돼 온 것이고, **무시된 이유가 그 폴더의 안내문이
「`.blend` 는 없다」고 적어 둔 것이었다.** 손대기 전에 읽을 문서 한 장이 `docs/manual/` 에 있다.
**같이 선 것**: 뽑은 그림을 절대 안 버리는 시안 폴더, 늑대 시안 여덟 방향 셋, 짧아진 배.

**⚠⚠ 8/31 밤 세션이 늑대를 뽑았고, 늑대가 바뀌었다.** 사용자가 고른 것은 **g5 — 회색에 배가 밝은,
털 질감 없는 단순한 늑대**다. 여덟 방향으로 돌려서 `assets/beast/wolf_h/` 넉 장을 갈아 끼웠다.

**같이 움직인 숫자 둘** — 왜 그래야 했는지는 로그에 있다.

- **늑대 그리는 크기 1.70 → 2.60.** 그림이 66칸인데 화면에 30픽셀로 그려져 **칸의 절반 넘게 버려지고
  있었다.** 이제 64로 뽑아 64로 그리니 **한 칸이 화면 한 픽셀**이고 안 깨진다
- **배 위 늑대가 땅 위와 같은 식을 쓴다.** 배는 늑대 크기 숫자를 아예 안 읽어서 **같은 짐승이 배에서
  37% 크기**였다. ⚠⚠ **고쳤더니 여덟 마리가 배를 덮는다** — 2026-08-30 에 둘을 일부러 떼어 놓은 이유가
  그것이었고 같은 일이 다시 났다. **다음은 의자 배치나 선체지 두 번째 크기 규칙이 아니다**

**⚠ 그림체가 안 정해져 있다는 것이 이 세션의 실측이다.** 몸 다섯을 키 맞춰 나란히 놓으니 **스타일이
다섯**이었다 — 검사는 외곽선 없는 만화체, 곰과 황소는 각진 3D 렌더, 까마귀는 거의 사진, 늑대는
외곽선 두른 픽셀아트. **맞출 대상이 없어서 새로 뽑는 것마다 안 맞았다.**

**⚠⚠ 팔레트는 세 칸 중 둘이 실측됐다** (로그에 값 전부). 숫자로 적힌 색은 그대로 쓰고, 조명 받는 메시
색은 실제 화면에서 쟀다. **몸 색 칸은 비워 뒀다** — 물린 그림에서 팔레트를 뽑으면 그 뒤섞인 상태가
굳는다.

**⚠⚠ 이번 주 태스크는 아직 안 만들었다. 사용자와 상담하다 말았다.**
2026-08-31 에 태스크 둘과 티켓 열넷을 만들었다가 **통째로 물렸다** — 쪼개는 것도 어느 태스크로 보낼지도
사용자 몫인데 혼자 정했다. **답이 안 나온 물음 둘이 아래에 있고, 그 둘이 답해져야 티켓이 생긴다.**

**⚠⚠ 사용자가 답해야 티켓이 생기는 물음 둘**

| # | 무엇 | 왜 이게 먼저인가 |
|---|---|---|
| **1** | **이번 주를 2 주 닫기까지만 잡나, 3 주(부대)를 당겨오나** | 이번 주에 무엇이 들어가는지가 이 답에서 나온다 |
| **2** | **성채가 낮은 땅에서 타는 것을 무엇으로 막나** | 2 주의 끝나는 조건 그 자체다. 방식이 여럿이고 사용자가 고른다 |
| **3** | **배에 여덟이 타면 배가 안 보인다 — 의자를 줄이나, 배를 키우나** | 늑대와 배 크기를 같게 맞춘 결과다. **두 번째 크기 규칙으로 돌아가는 것은 사용자가 이미 물린 것** |
| **4** | **적 몸에 씌우는 연어색 틴트 45% 를 계속 쓰나** | 고른 늑대는 회색인데 **화면에서는 보라빛 갈색으로 나온다.** 틴트가 진영을 말하는 유일한 수단이라 지우면 대신할 것이 필요하다 |
| **5** | **몸 색 팔레트를 g5 에서 뽑아 확정하나** | 팔레트 세 칸 중 둘은 실측이 끝났고 **몸 칸만 비어 있다.** 채우면 앞으로 뽑는 모든 그림을 거기 눌러 담는다 |

**⚠⚠ 2 번의 원인이 여기 틀리게 적혀 있었다. 2026-08-31 저녁에 코드에서 다시 쟀다.**
**「높이를 안 보고 평면으로만 잰다」가 틀렸다** — 성채까지의 거리는 **이미 높이를 제곱해서 더한다.**

- **1층에서 2층 성채 조각까지** : 평면 1 조각 · 높이 1 조각 → **1.414**
- **대각선으로 붙어도** → **1.732**
- **늑대 사거리는 1.75** (사거리 칸 0 + 보너스 1.75)

⇒ **성채를 둘러싼 1층 여덟 자리 전부에서 닿는다.** **높이를 안 봐서가 아니라, 보고도 사거리가 더 길어서다.**
⚠ **사거리 값으로는 못 고친다** — 1.75 는 계단 위의 몸이 2층 적을 때리라고 실측으로 고른 값이고,
낮추면 계단에서 아무도 못 때린다 (그때 162 판 중 26 판을 그렇게 졌다). `rules.gd` 의 주석이
「높이를 아는 자리에 두라」고 이미 지목해 뒀다.

**⚠ 안 끝난 것이 열한 개다.** 하나에 하루씩 잡으면 열하루치이고 이번 주는 엿새다 —
**무엇이 밖으로 나가는지도 1 번 물음이 정한다.**

**⚠⚠ 2 주를 닫지 못하게 붙들고 있는 것 하나**

| # | 무엇 | 티켓 |
|---|---|---|
| **1** | ⚠⚠ **성채가 1층 땅에서 탄다** — 해변 61 중 24 에서 계단을 안 밟고 이기고, **그쪽이 세 배 빠르다**. 「막아 내거나 진다」가 끝나는 조건인데 **막을 이유가 없는 판이다** | **57** |

**그다음 넷 — 전부 사용자가 화면에서 짚은 것이다**

| # | 무엇 | 티켓 |
|---|---|---|
| ~~**2**~~ | ~~**늑대에 걷기·물기가 없다** — 판 위의 늑대가 미끄러진다~~ | ✅ **닫혔다 (2026-08-31)** — g5 가 걷고 물고 맞고 쓰러진다 |
| ~~**3**~~ | ~~**바닥에 마름모가 뜬다**~~ | ✅ **닫혔다 (2026-09-02)** — 사용자: 「마름모 해결했음」. 티켓 02-06 이 그 자리다 |
| **4** | ⏳ **판떼기가 3D 에 서는 법 셋** — ✅ **빌보드 축**과 ✅ **카메라 피치**는 2026-08-31 에 들어갔다. **빛은 넣었다가 뺐다** — 진영색이 날아간다 | **59** |
| ~~**5**~~ | ~~**한 칸에 아홉이라 했는데 열둘이 선다**~~ | ✅ **닫혔다 (2026-08-31)** — 블록 천장 아홉이 규칙과 판에 섰다 |

**⚠ 아직 자리표시로 서 있는 값 둘 — 사용자도 실측도 아니다**

- **검사가 4 로 시작한다** · **부활이 20 초다** — 둘 다 상수에 「자리표시」라고 적혀 있다
- ✅ **셋째는 답이 나왔다** — 「졌다는 것이 화면에서 어떻게 보이나」는 **엔딩씬 하나** (사용자, 2026-08-30)

**새로 생긴 물음 다섯 — 전부 이번 세션이 화면에서 본 것들이다**

| 티켓 | 무엇 | 왜 사용자가 답해야 하나 |
|---|---|---|
| **52** | **배가 찾지 않아도 화면에 들어온다** | 「안 알아채는 게 맞다」가 코드에 안 서 있다 |
| **53** | **섬 남쪽에 배가 영영 안 온다** | 해안 86 중 25 가 한 번도 안 쓰인다 |
| **54** | **작은 배를 어디에 쓰나** | 만들어 뒀는데 읽는 코드가 없다 |
| ~~**55**~~ | ~~**물결 꼬리 길이**~~ | ✅ **닫혔다 (2026-08-31)** — 사용자가 4.8 을 화면에서 보고 「이미 괜찮으니까 건들 필요 없다」. **값은 한 줄도 안 건드렸다** |

**미뤄 둔 것**

- **내 배의 다른 물결** (`04b-arms`) — 사용자가 직접 미뤘다. **10 주에 배가 생기는 날** |
- **문서 둘이 얼룩 원인에 대해 틀렸다** — 티켓 56

## 1 주 · 2026-08-27 ~ 08-30 — **맵**

**끝나는 조건** : **사용자가 화면의 섬을 보고 「됐다」고 말한다** (티켓 04). ✅✅ **2026-08-30 에 닫혔다** — 「이번 주에 하기로 했던 맵은 끝났어」.

| 날짜 | 무엇 | 티켓 | 상태 |
|---|---|---|---|
| **08-28 금** | **정비** | **12** · 08 · 09 | ✅ |
| **08-29 토** | **정비** | ~~**05**~~ · ~~**07**~~ · ~~**08**~~ · ~~**14**~~ · ~~**19**~~ · ~~**21**~~ · **18 · 20 · 22 · 23 · 24** · ~~**06**~~ · ~~**37**~~ | ✅ |
| **08-30 일** | ⚠⚠ **눈으로 훑고 닫았다. 그리고 2 주를 미리 시작했다** | **04** | ✅ |

⚠⚠ **일요일은 만드는 날이 아니다.** 사용자가 화면을 보고 「됐다」나 「여기가 아직 아니다」를 말하는
날이고, **그 한마디가 덩어리 1 을 닫는다.**

**정비 이틀이 무엇을 뜻하나**

| 무엇 | 티켓 |
|---|---|
| ✅✅ **계단을 다시 세워 섬에 올렸다** (2026-08-29) — 후보 **열세 벌**을 화면에 세워 고르고, **블록 하나 · 단 넷 · 2층과 같은 마감**으로 정해졌다. ⚠ **계단이 든 블록은 평지로 남고 계단은 얹은 물체다** — 그래서 옆 조각의 판이 안 묻힌다 (자국 284 → 280) | ~~**06**~~ |
| ✅✅ **몸이 곧게 걷는다** (2026-08-29) — 대각선이 공짜라 동쪽으로 가라면 북쪽 끝까지 돌아갔다. 값 10/14 · 힙 · 목적지 쪽 타이브레이크 · 명령 순간 경로 뽑기 · 길 펴기. **직선에서 벗어남 4.06 → 0.00, 그물 통과 521 → 629** | ~~**37**~~ |
| ✅ **사용자가 본 버그를 받아 적었다** (2026-08-28) — 판·해안·칸 색·외곽선. 전부 그날 고쳤고 티켓 08·09 에 들어갔다 | 없음 |
| ✅ **가지를 `main` 으로 들였다** (2026-08-28) — 검사 걷기 · 마우스 올린 칸 · 조각 열아홉 섬. ⚠ **그물 통과 1346 → 1294** 로 떨어졌고 티켓 **15** 가 그것을 든다 | 없음 |
| ✅ **시작을 누르면 곧장 섬이 뜬다** (2026-08-28) — 누름 하나. 카드도 개편도 안 지웠고 시작 경로에서만 뺐다 | **12** |
| ✅ **죽은 값을 다시 걷어냈다** (2026-08-30) — 물 상수 **서른다섯** · 죽은 함수 하나 · 죽은 상수 하나, **257 줄**. 2026-08-28 에 바다 셰이더가 갈리면서 버려진 옛 물결·거품·파도 다이얼이다. ⚠ **그물은 정비 전후가 같다 (1234·62 → 1235·61, 차이는 다른 세션이 낸 것)** | 없음 |
| ✅ **죽은 코드를 걷어냈다** (2026-08-27) — 도구 스물 · 그림 스물 · 상수 열여덟 · 규칙 여섯 덩어리. **그물 통과 1126 → 1346** | 없음 |
| ✅ **해안선을 검증했고, 그날 저녁에 통째로 다시 만들었다** (2026-08-28) — 후보 일곱 벌 중 **평평한 바다 + 경계선**을 사용자가 골랐다. 셰이더가 갈렸고 눈금 마흔이 열여덟이 됐다 | **08** |
| ✅ **판을 검증했다** (2026-08-28) — ⚠⚠ **판이 얹는 그림에서 블렌더가 굽는 지형으로 바뀌었다** | **09** |
| ⚠ **물주름을 사용자가 골랐고, 같은 날 저녁에 화면에서 사라졌다** (2026-08-28) — 그것을 그리던 셰이더가 교체됐다. **결론이 틀린 게 아니라 적용될 자리가 없어졌다** | **05** |
| ✅ **바다는 평평한 채로 간다** (2026-08-29) — 거품 한 겹을 넣어 화면에서 보고 사용자가 뺐다. **평평한 바다가 두 번 확인됐다** | **21** |
| ⏸ **열린 바다를 후보 열 벌로 물었고 전부 물렸다** (2026-08-29) — 무늬 다섯과 빛 다섯. **평평한 바다가 세 번째로 확인됐지만 이번엔 이긴 게 아니라 나은 것이 없어서다.** 사용자: 「나중에 해야할껄로 정리」 | **35** |
| ✅ **화면에서 UI 가 통째로 사라졌다** (2026-08-28~29) — 시작 버튼 · 소환 칸 · 적 수, 그리고 그 뒤 입력 전부. **카드 화면과 정비 화면도 지웠다** | 없음 |
| ✅ **판이 「설 수 있는 곳」에서 「갈 수 있는 곳」이 됐다** (2026-08-29) — 갈 수 있는 이웃끼리 경사 다리가 놓이고, 못 가는 벽에는 없다. Tab 을 누르는 동안만 뜬다 | **14** |
| ✅ **절벽면이 보라에서 밝은 돌로** (2026-08-29) — 후보 넷을 게임 화면으로 찍어 사용자가 B 를 골랐다. **계단은 원래 서 있었고 벽과 같은 색이라 파묻혀 있었다** | **06** 일부 |
| ✅ **1층과 2층 사이 빈틈이 닫혔다** (2026-08-29) — 모서리 깎기가 절벽에도 걸려 있었다 | **20** 일부 |
| ✅ **계단이 네 군데에서 고쳐졌다** (2026-08-29) — 그림과 발 높이의 방향 · 비탈 대신 단 · 반 조각 어긋난 표본 · 옆구리 진입. **그래도 아직 뚫는다** | **22** 로 넘김 |
| ✅ **몸이 대각선으로 벽 모서리를 못 지나간다** (2026-08-29) | **19** |
| ✅ **죽은 코드를 6800 줄 걷어냈다** (2026-08-29) — 보상·정비 화면 · HUD · 카드 · 장비 · 등급 · 태그 · 출혈 · 배 · 소환. **그물 실패 155 → 79.** ⚠ 바다 옛 상수 마흔하나와 잡다 마흔은 안 지웠다 | **13** 닫힘 |
| ✅ **전투를 통째로 지웠다** (2026-08-29, 사용자: 「그냥 지워도됨 아직 전투 전혀 없어」) — 적 · 조준 · 공격 · 피해 · 죽음 · 승패 · 연출 열둘. **5400 줄.** ⚠ `Rules.UNITS` 의 수치 칸은 남겼다 — 유일한 실측이다 | 없음 |
| ✅ **바다가 어떻게 움직이는지 한 줄이 정해졌고 그대로 산다** (2026-08-28) — 해안선이 밀려왔다 물러나고 줄 하나를 놓아 보낸다. 저녁에 **움직임이 넷으로 늘었다** | **07** |
| ✅ **블렌더가 진짜 해안선을 내보낸다** (2026-08-28) — 축 정렬 스물에서 꺾인 선 마흔여덟으로. **섬 메시는 바이트까지 그대로** | 없음 |
| ✅ **그물을 정리했다** (2026-08-28) — 실패 258 → 209, 통과 1330 → 1378. 섬·소환 그물이 완전히 초록 | 없음 |
| ✅ **집과 검사를 0.45 배로 줄였다** (2026-08-28) | **17** |
| ✅ **섬이 조립식이 됐다** (2026-08-29) — 열린 변으로 갈리는 여섯 종을 층마다, 열두 벌. 섬은 그 부품으로 세운다 | **30 · 31** |
| ✅ **블록 겉면을 다시 잡았다** (2026-08-29) — 테두리 자기교차 · 오돌토돌 · 뒤집힌 면 · 물 아래 벽 열두 겹. **정점 5453 → 1505**, 그 뒤 판·턱이 붙어 7991 | ~~**29**~~ ⚠ **티켓을 지웠다 (2026-08-30, 사용자: 「이것도 수정했어」)** |
| ✅ **1층과 2층이 같은 색 구조를 쓴다** (2026-08-29) — 위는 흙판, 턱부터 바위. 두 층 풀색도 가까워졌다 | 없음 |
| ~~⚠ **계단을 지웠다** (2026-08-29)~~ — ⚠ **이 줄은 같은 날 저녁에 뒤집혔다.** 계단은 다시 서 있고(위의 첫 줄), `net_tiers` 의 빨강 중 몇은 계단이 아니라 **그물이 쓰는 판에 2층이 없어서다** | ~~**06**~~ · ~~**40**~~ ⚠ **티켓을 지웠다 (2026-08-30, 사용자)** |
| ✅ **판이 전시용으로 바뀌었고, 그대로 가기로 했다** (2026-08-29) — 열두 종이 다 나오게 30x26. 사용자가 같은 날 저녁에 **이대로 괜찮다**고 답했다 | **31** |
| ✅ **고른 바다가 게임에 들어갔다** (2026-08-29) — 흰 선 둘, 다이얼 열여덟이 들어오고 열하나가 나갔다. 사용자가 켜 보고 ***"너무 좋다"*** | **28** |
| ✅ **판이 조각 단위가 됐다** (2026-08-29) — 칸마다 하나에서 **조각마다 하나로 284 장**, 호버도 조각 하나다. 가장자리는 땅에 맞춰 깎았고, 계단에는 안 놓는다. ⚠ **판끼리 잇던 다리가 없어졌다** | **32 · 33** |
| ✅ **멀면 칸으로 합쳐지는 판이 게임에 들어갔다** (2026-08-29) — 네 벌 중 사용자가 **키우기**를 골랐다. 줌이 사이를 움직이고 호버가 따라간다 | **34** |
| ✅ **판의 밝기와 틈이 정해졌다** (2026-08-29) — 밝기 여섯을 게임에서 눌러 가며 보고 가장 옅은 밝은 쪽. ⚠ **「다 별론디」가 그대로 실측이다** | **33** |
| ✅ **섬 모양은 이대로 간다** (2026-08-29, 사용자: 「지금 이대로 괜찮은 거 같고」) — 전시판이 그대로 놀 섬이 된다 | **31** |
| ⏸ **섬 위의 나무를 카드 한 벌로 세워 봤고 사용자가 정하지 않았다** (2026-08-29) — 단색 나무 하나, 크기만 굴린다. 빛·그림자·테두리·바람이 붙어 있다. ⚠ **`src/` 는 한 줄도 안 건드렸고, 이 주 표에 장식 행이 원래 없었다** | **38** |
| ⏸ **판끼리 잇는 다리는 없다** (2026-08-29) — 계단을 만들 때 필요하면 그때 잇는다 | **32** |
| **계단이 유일한 길로 읽히게 한다** | **06** |
| 캐릭터가 걷는다 | ✅ **들였다** (2026-08-28) |
| ✅ **마우스 올린 칸이 밝아진다** | ✅ **2026-08-29 에 돌아왔다** — 판이 다시 제 물체가 됐다. 티켓 **14** |

⚠ **가지는 `wip/2026-08-27-mats-walk-small-island` 이다.**
✅ **05 · 07 · 08 은 2026-08-28 에, 14 · 19 · 21 · 28 은 2026-08-29 에 닫혔다.**
✅ **고른 바다가 게임에 들어갔다** (2026-08-29) — 스물일곱 벌 중 사용자가 고른 `27-gaps` 를
`.prototypes/swash/` 에서 `src/` 로 옮겼다. **흰 선 둘, 다이얼 열여덟이 들어오고 열하나가 나갔다.**
사용자가 게임을 켜고 봤다: ***"너무 좋다"***. 티켓 **28**.
⚠ **다시 재라던 세 값은 안 돌렸다** — 눈으로 보고 그대로 통과했다. 대신 **섬이 처음 열리는 거리에서는
흰 선 둘이 하나로 뭉개진다**(고를 때보다 네 배쯤 멀다)는 것과 **블록 옆면이 거의 흰색이라 바위에 붙은
선이 묻힌다**는 것이 남았다. 섬이 전시판을 그만두는 날(티켓 **31**) 사용자 눈앞에 올린다.

⚠ **남은 것은 18 · 20 과 22 · 23 · 24 다.** ✅ **06 은 2026-08-29 에 닫혔다** — 계단이 섬에 다시 서 있고 사용자가 「이 정도면 돼」라고 했다. ⚠ **22 와 23 은 지워진 옛 계단을 재고 쓴 것이라 다시 재야 한다.** ✅ **31 · 33 · 34 는 2026-08-29 에 닫혔고, 32 는 계단에 붙여 접었다.** ⚠⚠ **20 은 크게 진전됐지만 사용자 판정이 없다** — 빈틈은 고쳤고, 「몸이 아직 사라지나」는 사용자가
화면을 보고 답할 것이다.

⚠⚠ **08-28 저녁에 바다가 통째로 바뀌었고 티켓 넷이 생겼다.** 해안선을 그리는 방법 일곱 벌을 조각 하나 위에 세워 놓고(`.prototypes/shoreline/`, 새 스킬 `prototype` 이 그 판을 만든다) 사용자가 **평평한 바다에 경계선 하나**를 골랐다 — Bad North 가 자기 입으로 말한 그것이다. **파도·물주름·거품·여울은 그리는 코드가 없어졌고**, 그 상수 스물몇 개는 「주차」 표시를 달고 `look.gd` 에 남아 있다.
⇒ **새 티켓**: **18**(안으로 꺾인 모서리가 뾰족하다) · **19**(몸이 대각선으로 벽을 지나간다) · **20**(2층 경계에서 몸이 사라진다) · **21**(바다가 다시 움직여야 하나 — 사용자가 답할 질문).

⚠⚠ **08-29 에 티켓 넷이 더 생겼다.** **22**(몸이 아직 계단을 뚫는다 — 넷을 고친 뒤에도 남았다) ·
**23**(계단이 붕 떠 보인다 — 어디가 뜨는지 사용자가 답할 질문) · **24**(섬 윤곽을 조각 단위로) ·
**25**(캐릭터 좌우앞뒤 네 방향을 로컬 생성기로). ⚠ **25 는 2026-08-30 에 지웠다.**
⚠ **21 은 같은 날 답이 나와 닫혔다** — 거품을 넣어 보고 사용자가 뺐다.

⚠⚠ **08-29 저녁에 티켓 둘이 더 생겼다. 둘 다 사람이 본 것이 아니라 재다가 나왔다.**
**39**(대각선 걸음이 묻는 방향에 따라 다르게 판정된다 — 흐름장이 몸이 못 걷는 길을 걸을 수 있다고 적는다) ·
~~**40**(층 그물의 판에 2층 조각이 하나도 없다 — **로드맵이 「계단이 없어서 빨갛다」고 적어 둔 것 중 몇 줄이 이것이다**).~~ ⚠ **2026-08-30 에 지웠다.**

## 2 주 · 2026-08-31 ~ 09-06 — **짐승이 오고 싸운다**

**끝나는 조건** : **짐승이 배로 와서 내리고, 검사가 그것을 잡거나 본채가 타서 진다.**

⚠⚠ **한 덩어리로 둔다** (2026-08-30, 사용자: 「티켓에 너무 디테일한 점이 들어가 있음, 좀 더 큰
덩어리로 시작하는 게 맞음. 네가 혼자서 디테일을 정하는 일은 없어야 함」).
**쪼개는 것도 순서도 사용자가 정한다.**

| 날 | 무엇 | 티켓 | 상태 |
|---|---|---|---|
| **월 ~ 수** | **배 타고 오는 것** | **41 · ~~47~~** | ✅ **2026-08-30 에 끝냈다 — 하루 일찍** |
| **목 ~ 일** | **전투** | **41 · ~~48~~ · 57 · 58** | ⏳ **2026-08-30 에 지어졌다 — 엿새 일찍.** 짐승이 내려서 걷고 때리고 죽고, 성채가 타면 진다. ⚠⚠ **닫지는 못한다 — 성채가 1층에서 탄다 (티켓 57)** |

| 같이 도는 것 | 티켓 | 상태 |
|---|---|---|
| **짐승이 오고 싸운다** | ~~41~~ | ✅ **태스크 02 가 됐다 (2026-09-01)** — 티켓 아홉이 그 밑에 선다. **어디까지가 이번 주인지는 여전히 사용자 답이다** |

| 이 주에 같이 도는 것 | 티켓 | 상태 |
|---|---|---|
| **그물이 아직 옛 섬을 재고 있다** | **15** | ⏳ ⚠⚠ **2026-09-01 에 합친 나무에서 다시 쟀다: 통과 1227 · 실패 64 · 열다섯 벌 중 여섯이 빨갛다** — `tiers` 25 · `islands` 17 · `fx_view` 9 · `shell` 6 · `camera` 6 · `wake` 1. **원인 여섯이 값으로 갈렸다**: ① **섬이 26 행 × 30 자 · 땅 284 조각 · 고원 48 조각 · 계단 4 조각인데 검사는 12 행 × 16 자 · 땅 76 · 고원 16 · 계단 1 로 안다** ⚠⚠ **2026-09-01 에 단위를 고쳤다 — 여기 「칸」이라고 적혀 있던 것이 전부 조각이다.** 284 칸이면 1136 조각이라 판 전체보다 크다 ② **섬에 짐승이 0 마리다** — 글자가 `H`·`~`·`.` 뿐이고 짐승은 배로 오는데 검사는 여섯을 센다 ③ **위성 섬 4 칸** — 설계 31 이 결함이 아니라고 정했는데 검사는 「상륙지는 다 섬으로 이어진다」를 잰다 ④ **늑대 그림틀이 92 × 66 인데 검사는 92 × 92 로 안다** ⑤ **손으로 만든 판에는 지형 메시가 없다** — 메시는 `island.glb` 하나뿐이라 물어볼 것이 없다 (`shell` 3 줄) ⑥ **배 반길이가 2.1 조각이라 `want` 가 1.95 인데 자가 점검이 2.0 초과를 요구한다** (`wake` 1 줄, 진짜 검사 두 줄은 통과한다). ⚠⚠ **여섯 다 검사가 낡은 것이고 게임 쪽 결함은 하나도 없다.** ⚠ **앞선 실측 넷은 서로 안 맞아 보였는데 나무가 서로 달랐던 것이다** — 이 줄이 그 넷을 대신한다 |
| ~~**층 그물의 판에 2층 조각이 하나도 없다**~~ | ~~40~~ | ⚠ **티켓을 지웠다 (2026-08-30, 사용자)** |
| **서브스텝 분해를 아무도 안 보고 있다** | **26** | ⬜ ⚠ **전투가 서면 이것이 재는 대상이 생긴다** |

⚠ **티켓 27 과 10 은 `superseded` 로 남기고 안 지웠다** — 묘비 목록이 길고, 무엇을 만들든 그것을 읽는다.

## 3 주 · 2026-09-07 ~ 09-13 — **부대**

⚠⚠ **2026-09-01 기준 3 주는 태스크 둘로 읽힌다 — 03 부대를 조종한다 · 06 기본 섬을 넓히고 높인다.**
**06 은 옛 티켓 46 의 답이고, 46 이 바로 「부대를 쪼개고 합친다」를 막고 있던 물음이다.** ⚠ 주를 붙이는
것은 사용자다.

| 무엇 | 티켓 | 상태 |
|---|---|---|
| **부대를 판 위에서 아무 때나 명령한다** | **02** | ⬜ ⚠ **화면에서 어떻게 명령하나가 아직 사용자 답이다** |
| **부대를 쪼개고 합친다** | 없음 | ⬜ ✅ **티켓 46 은 2026-09-01 에 답이 나왔다 — 넓힌다.** 태스크 06 이 그것이고, 3 주가 03 과 06 둘이 된다 |
| **아홉이 서는 모양 — 6번** | **`03-17`** | ⬜ ✅ **모양은 정해졌다 (2026-08-31)** — 블록 3x3 격자가 보는 쪽으로 돈다. **넣는 일만 남았고 2026-09-02 에 사용자가 화면에서 안 넣힌 것을 봤다** ⚠ **옛 번호 63 이 `03-17` 로 들어갔다** |
| **이동할 때 이동선이 보인다** | **11** | ⬜ |
| **그려지는 몸이 실제로 닿는 범위보다 작다** | **17** | ⬜ |
| **몸이 무는 순간 화면에서 무슨 일이 일어나나** | **03** | ⬜ |
| ~~**캐릭터 좌우앞뒤 네 방향**~~ | ~~25~~ | ⚠⚠ **티켓을 지웠다 (2026-08-30, 사용자)** — 검사를 무엇으로 뽑을지가 안 정해져서 너무 이른 티켓이었다 |

⚠ **티켓은 그 주가 올 때 만든다** — `grilling` 이 무엇을 만드는지 캐묻고 `wrap-up` 이 그 답을 적는다.

## 덩어리 아홉 — **주보다 큰 단위**

| # | 덩어리 | **끝나는 조건** | 티켓 | 언제 |
|---|---|---|---|---|
| ~~**1**~~ | **맵** | ✅✅ **닫혔다 (2026-08-30)** — **2026-08-30 에 훑어서 정리했다** — 남은 것은 **01 · 20 · 39** 뿐이고 전부 **주를 붙들지 않는 다듬기**다 | ~~04~~ · 01 · **20 · 39** · ~~18~~ · ~~22~~ · ~~23~~ · ~~24~~ · ~~25~~ · ~~29~~ · ~~06~~ · ~~05~~ · ~~07~~ · ~~08~~ · ~~09~~ · ~~14~~ · ~~19~~ · ~~21~~ · ~~28~~ · ~~31~~ · ~~33~~ · ~~34~~ · ~~35~~ | **1 주** |
| **2** | **움직임** | 검사가 마음에 쏙 들게 움직인다 — 명령 반응 · 타격감 · 공격 동작. ⏳ **타격감 여섯과 공격 동작은 2026-08-31 에 섰고, 사용자 눈 검사만 남았다.** 남은 것은 **명령 반응** | 02 · 03 · 11 · 17 · **46** · ~~37~~ | **3 주** |
| **3** | **한 판** | 짐승이 배로 오고, 막아 내거나 본채가 타서 진다. ⏳ **2026-09-01 에 화면에서 성립했다** — 성채가 눈금 0 에서 안 타고, 늑대가 계단으로 올라와 38.8 초에 부순다. **남은 것은 낡은 검사 하나뿐이다** | **41** · 26 · **50 · 51 · 52 · 53 · 54 · 57 · 58 · 60 · 61** · ~~55~~ · ~~47~~ · ~~48~~ · ~~10~~ · ~~27~~ | **2 주** |
| **4** | **건설** | 골렘을 잡으면 땅이 네 칸 는다 | **16** | **7 주** ⚠⚠ **포탑이 여기서 빠졌다 (2026-09-02, 사용자: 「포탑은 없어져도 될 거 같고」)** — 땅 넓히기는 남는다 |
| **5** | **자원과 테크트리** | 나무·돌·철을 캐고 연구대에서 다음 것을 연다 | 없음 | **4 · 5 주** ⚠ **등급은 안 쓴다** (2026-08-30, 사용자) |
| **6** | **보스** | **정해진 시각에 보스가 오고, 잡는 것이 이 게임의 클리어다** | 없음 | **8 주** ⚠⚠ **보스는 드래곤이고 새 그림 124 장이다 (2026-09-02)** — 이기는 조건이라 10 월 말 안에 서 있어야 해서 9 주에서 한 주 당겼다 |
| **7** | **약탈** | 나무 배를 타고 나가고, 그동안에도 시계가 돈다 | 없음 | **9 · 10 주** ⚠ **배 짓기는 4 주(낚시)로 갔다 (2026-09-02)** |
| **8** | **멀티** | ✅✅ **간다 (2026-09-01)** — 사용자: 「멀티하자. 멀티가 와야 맞아. 돈 벌어야지」. **2026-08-30 의 보류가 뒤집혔다.** ⚠ 무엇을 어떻게 하는지는 하나도 안 정해졌다 | 없음 | ⬜ |
| ~~**9**~~ | ~~**농사와 낚시**~~ | ⚠ **덩어리 5 에 흡수됐다** (2026-08-30) — 캐는 것이 나무·돌·철 셋으로 정해졌다 | 없음 | — |

⚠⚠ **순서가 1 부터 7 까지 정해졌다** (2026-08-30). **9 는 없어졌고 8 은 보류다.**
⚠ **덩어리 5 · 6 · 7 은 티켓이 아직 하나도 없다** — 그 주가 올 때 캐물어서 만든다.

## 아직 태스크에 안 붙은 것들

⚠⚠ **왼쪽 번호는 죽은 참조다.** 2026-08-30 에 지운 티켓 마흔다섯의 번호이고 **그 파일들은 없다.**
남겨 둔 이유는 **무엇이 아직 안 됐나가 이 표에만 적혀 있어서**다. 하나가 태스크로 옮겨질 때마다 그
줄을 지운다.

| 옛 번호 | 무엇 | 어디로 |
|---|---|---|
| ~~**38**~~ | ~~섬 위의 나무는 무엇으로 만드나~~ | ✅ **닫혔다 (2026-08-31)** — **블렌더 저폴리 3D 메시**다. 소나무 하나로 정해졌고 서른한 그루가 숲으로 서 있다. ⚠ **덤불은 반대로 2D 카드다** |
| ~~**46**~~ | ~~섬이 커져야 하나~~ | ✅ **닫혔다 (2026-09-01)** — 사용자: 「기본적으로 주는 섬을 늘리자는 거」. **태스크 06 이 됐다** |
| ~~**49**~~ | ~~그림을 뽑기 전에 캐묻는 스킬이 없다~~ | ✅ **닫혔다 (2026-08-30)** — `commission` 스킬이 섰다 |
| **36** | 바깥 사실이 `scout` 스킬까지 안 간다 | ⚠ **게임이 아니라 하네스다** — 사용자가 올려 두라고 해서 여기 있다 |
| **01** | 한 조각이 무엇인가 | ⚠ **블렌더를 열기 전에 읽는 문서** — 주에 붙는 일이 아니다 |
| **52** | 배가 찾지 않아도 화면에 들어온다 | **2 주 안** — 배가 이미 서 있다 |
| **53** | 섬 남쪽에 배가 영영 안 온다 | **2 주 안** |
| **54** | 배가 두 척인데 한 척만 쓴다 | **정해지기 전에는 아무것도 안 짓는다** |
| ~~**55**~~ | ~~물결 꼬리가 고른 것보다 세 배 짧다~~ | ✅ **닫혔다 (2026-08-31)** — 사용자가 화면에서 보고 「이미 괜찮으니까 건들 필요 없다」. **값은 안 건드렸다** |
| **56** | 문서 둘이 얼룩 원인에 대해 틀렸다 | ⚠ **게임이 아니라 문서다** |
| **57** | **성채가 1층 땅에서 탄다** | ⚠⚠ **2 주를 닫으려면 이것부터다.** 방식이 여럿이고 사용자가 고른다 |
| ~~**58**~~ | ~~H 늑대에 걷기·물기 그림이 없다~~ | ✅ **닫혔다 (2026-08-31)** — g5 가 다섯 동작을 다 갖췄다 |
| **59** | 판떼기가 3D 에 서는 법 셋을 안 지킨다 | **12 주** 가 화면을 도구로 다시 만드는 주다. 그전에 해도 된다 |
| ~~**60**~~ | ~~바닥에 마름모가 뜬다~~ | ✅ **닫혔다 (2026-09-02)** — 사용자: 「마름모 해결했음」 |
| **61** | 회전 중에 가장자리 이동이 같이 먹는다 | **2 주 안** — 사용자가 아직 화면에서 못 봤다 |
| — | **병사 색을 무엇으로 하나** | ⚠ **번호 없음, 2026-08-31 에 생겼다.** 사용자가 검정·흰색을 대 보고 안 골랐다 |
| — | **3D 블록 색을 바꾸나** | ⚠ **번호 없음.** 병사와 같이 말이 나왔고, 섬 전체가 바뀐다 |
| — | **병사 뒤우·뒤좌를 넣나** | ⚠ **번호 없음.** 그림은 이미 있고, 늑대와 같이 쓰는 고르개를 바꿔야 한다 |
| — | **병사 걷기를 어디서 뽑나** | ⚠ **번호 없음.** 픽셀랩 것은 머리가 절반으로 줄어 못 쓴다 |
| — | **병사 한쪽 발이 2 px 짧다** | ⚠ **번호 없음.** 튼 두 장에서만 그렇고, 27 px 에서 한쪽이 없어 보인다 |
| — | **`hit_lab.gd` 와 `move_lab.gd` 가 안 컴파일된다** | ⚠ **게임이 아니라 도구다.** `var st = _stage` 에 타입이 없어 아래 `var big :=` 가 못 정해진다. **지금 켜 둔 폴더에는 그 파일이 없어서 안 잡힌다** |
| ~~**62**~~ | ~~한 칸에 아홉이라 했는데 열둘이 선다~~ | ✅ **닫혔다 (2026-08-31)** — `Rules.BLOCK_CAPACITY` 아홉, `Grid.block_of`. 그물 열세 줄이 잰다 |
| **63** | **아홉이 서는 모양을 게임에 넣는다 — 6번** | **3 주** — 자리가 조각이 아니라 블록에 붙는다. 시안은 뽑혔고 사용자가 골랐다 |
| **64** | **멈춘 몸이 마지막 걸음 모양으로 굳는다** | ⚠⚠ **아홉을 나란히 세우는 것과 직접 걸린다** — 몸마다 그려지는 키가 달라 줄이 안 맞아 보인다 |
| **65** | **손으로 세운 아홉과 걸어온 아홉이 25 픽셀 어긋난다** | ⚠ **원인 미확인.** 좌표·카메라·걸음·방향을 다 맞춰도 남는다 |

## 이 파일을 다루는 규칙 넷

| 누가 | 무엇을 하나 |
|---|---|
| **`compass`** | 이번 주 칸과 티켓을 읽어 「지금 뭐 하지」에 답한다 |
| **`grilling`** | 이 주를 정할 때 **무엇을 만드는지**를 끝까지 캐묻는다 |
| **`roadmap`** | 이 표 전체를 실제 커밋과 대조한다 |
| **`wrap-up`** | ⚠⚠ **끝난 것의 상태 칸을 ✅ 로 바꾼다.** 상태가 안 바뀌면 다음 세션이 안 끝난 줄 안다 |
