# 04-01 — 「부대」를 용어집에 세우고 어긋난 셋을 고친다

Ticket: 04-01
Name: 「부대」를 용어집에 세우고 어긋난 셋을 고친다
Task: 04 — 낱말과 문서를 맞춘다
Type: task
Status: resolved

## What this ticket builds

**「부대」 gets a row of its own in the glossary, and the three rows that are false get corrected.**

> The user, 2026-09-01, choosing the word: *"so now you can command one at a time, and let's go with
> 부대."* (「이제 조종할 때 한 명씩 조종할 수가 있고, 부대로 하자」)

### ⚠⚠ The word the repo is built on is not in the glossary

| Word | State |
|---|---|
| **부대** | **No row.** It appears twice, inside other rows' prose. **Task 03 is named after it** |
| **무리** | **Dead** — survives only as 「무리사냥」, retired 2026-08-27 |
| **집단** | **Does not appear at all** |

⚠ **Two nameless things, not one.** The bodies that happen to stand on one 칸 (the code calls that
`crowd`, in English only, and the comments call it a *pile* and a *lump* — both pejoratives), and the
thing you pick and command as one. **Whether those share a word is what this day settles.**

### The three rows that are false, each checked against `src/`

| The glossary says | The code says |
|---|---|
| 건물: 「지금 서 있는 것은 집 하나뿐이다」 | 섬 파일에 **성채 하나, 집 0 개** |
| 「코드 주석 두 줄에 「세포」가 남아 있다 (`look.gd`·`rules.gd`)」 | **`src/` 에 0 줄.** 검사 한 줄에만 산다 |
| (그리는 쪽 주석) 건물은 「몸을 안 막는다」 | **막는다** — 판에 성채가 채워진다. 같은 주석의 「안 탄다」는 맞다 |

⚠ **`GLOSSARY.md`'s own rule decides the direction**: where the code and the glossary disagree, **the
code is true and the glossary is corrected** — never the reverse.

## Screen

**none.**

## Done when

**「부대」 has a row with both columns filled, the three false rows read what the code does, and the
reversal of 「체력바 없이」 is on the 뒤집힌 낱말 table** — that table's rule is that a reversed word is
never deleted, only added to.

## What the user has to answer first

- **한 칸에 같이 선 것과 골라서 명령하는 덩어리가 같은 낱말인가** — if not, the second one needs a name too

## Implementation plan

### The question this ticket was blocked on is answered

> The user, 2026-09-01, taking the recommendation: *"go ahead with everything except 3."*
> (「3번 빼고 진행」)

**The recommendation taken**: **they are NOT the same word. 「부대」 is given only to the thing you pick
and command.**

⇒ **The bodies that happen to stand on one 칸 get NO Korean name at all.** They are the result of the
seat-handing-out, not something the player makes. ⚠ **`crowd` stays as it is** — an English identifier
inside `src/`, naming an implementation detail, and the glossary does not have to carry it.
⚠⚠ **The two comments calling it a *pile* and a *lump* are the defect** — a name the code is
embarrassed by is a name that will get re-invented. **They are reworded to say what it is: the bodies
standing on one 칸.**

### The 「부대」 row, both columns filled

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **부대** | `Hand.ids` | **골라서 하나로 명령하는 몸들.** 손이 고른 목록 그 자체고, 하나여도 부대다 | ⚠⚠ **한 칸에 같이 선 것이 아니다** — 그건 자리를 나눠 준 결과지 고른 것이 아니고, 이름이 없다 |

⚠ **The code column is `Hand.ids` and it is checked**: `Hand` holds the picked list, `order` sends all
of it, and `routes` draws one line per member. **That is the seam the word actually names.**

### The three false rows, corrected to what `src/` does

| Row | Corrected to |
|---|---|
| 건물: 「지금 서 있는 것은 집 하나뿐이다」 | **성채 하나. 집은 0 개** |
| 「코드 주석 두 줄에 「세포」가 남아 있다 (`look.gd`·`rules.gd`)」 | **`src/` 에 0 줄.** 검사 한 줄에만 산다 |
| (그리는 쪽 주석) 건물은 「몸을 안 막는다」 | **막는다** — 판에 성채가 채워진다. 같은 주석의 「안 탄다」는 맞다 |

⚠ **Each is re-read against `src/` before it is written**, not copied from this table. The direction is
`GLOSSARY.md`'s own rule: **the code is true and the glossary is corrected.**

### The 뒤집힌 낱말 table — three reversals to add, not two

**The table's rule is that a reversed word is never deleted, only added to.**

1. **「체력바 없이」 → 「체력바를 넣는다」** (2026-09-01, ticket 02-02)
2. ⚠⚠ **「판 자국이 조각마다」 → 「칸 단위로 명령한다」** (2026-09-01, the user: 「블록단위로 하자」).
   **This reverses the 2026-08-29 row that is already on the table**, and that row stays where it is.
3. ⚠ **「부대」 itself** is a new word, not a reversal — it goes in the main table only.

### Seams

**None.** ⚠⚠ **This ticket touches no code and writes no net.** It edits one document.

### Order

1. Re-read the three false rows against `src/` and write what is actually there.
2. Add the 「부대」 row.
3. Add the three reversal lines.
4. Reword the two pejorative comments in `src/` that call the 칸-crowd a pile and a lump.

### Screen

**none.**

### Acceptance

- **「부대」 has a row with both the Korean and the code column filled.**
- **The three corrected rows say what `src/` does**, each re-read rather than copied.
- **The two reversals are on the 뒤집힌 낱말 table** and nothing was deleted from it.
- **`tests/run_nets.ps1`'s red set is unchanged** — this ticket must move no row.

### Out of scope

- ⚠⚠ **Renaming the file.** That is `04-03`, and it runs after this.
- ⚠ **The other dead words in comments.** That is `04-02`, which this ticket unblocks.
- **Giving the 칸-crowd a Korean name.** The user decided it does not get one.

### Risk

- ⚠ **`04-03` renames this very file.** ⇒ **This ticket finishes first**, or the rename lands on top
  of an edit in flight.
- ⚠ **Rewording a comment in `src/` touches files `04-02` will also touch.** **Only the two
  pile/lump comments move here**, and they are named above so the two tickets cannot collide blindly.


## Answer

**Done, and verified. 「부대」 has a row, the three false rows now read what `src/` does, and the
뒤집힌 낱말 table gained three lines and lost none** — 63 rows to 66.

### ⚠⚠ The plan was wrong about the third row, and the build was right

**The plan said only 「몸을 안 막는다」 was false and that 「안 탄다」 was correct. All three of that
comment's claims were false.** Builder found it by re-reading `src/`; `verify` proved it by driving the
game headless rather than reading it:

| Measured | |
|---|---|
| **The 성채 blocks** | **0 of its 4 조각 will take a body.** Handed an empty keep list, all 4 will |
| **It burns** | `keep_hp` first drops at **t = 25.0 s** |
| **The run is lost on it** | **`lost = true` at t = 39.0 s**, in the sub-step the health reaches zero |

⚠ **It is `Battle` filling those 조각, not the terrain** — which is what the new comment says.

### What else was measured

- **`Hand.ids` is an honest code column.** It holds the picked bodies, `order` sends every one of them,
  and `routes` draws one line per member.
- **The 몸 table is three columns and the plan's row was four.** Builder folded it rather than breaking
  the table, and did not mention it. ⚠ **A plan that would not have parsed.**
- **Three `.gd` files changed and every changed line is a comment.** Not one statement moved.
- **This ticket moved no net row.** Every net's pass count is identical to HEAD except `net_tiers`,
  which is `02-08`'s work.
- **Both new quotations are verbatim against their sources.**

### ⚠⚠ What is left standing, and it is `04-02`'s

**「무리」 — the dead word retired 2026-08-27 — appears eight times, and always naming exactly what the
glossary now calls 부대**: seven in `src/sim/hand.gd` and one in `src/view/field_view.gd`.

⇒ ⚠ **A reader who follows the new row's own code column into `hand.gd` meets 무리, not 부대.** **That
is the largest pocket of the thing `04-02` exists to fix, and it sits in the file this row cites.**

**Seven 「lump」/「pile」 comments survive in `src/` and none of them is a defect** — five are about
boats, pads, sprites and the 2x2 블록, and the two about a squad describe a design that no longer
exists. ⚠ **They make the row incomplete, never false**: none of them gives the 칸-crowd a name.

### A new mismatch found and deliberately not fixed

**The glossary calls the five buildings 성채·탑·성벽; the building file labels them 본채·망루·돌담.**
**Only the count of five agrees.** ⚠ **Out of this ticket's scope** — it is a glossary row nobody
asked about today, and it is written here so the next round does not have to find it again.
