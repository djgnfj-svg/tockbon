# 04-03 — 용어집을 CONTEXT 에서 뺀다

Ticket: 04-03
Name: 용어집을 CONTEXT 에서 뺀다
Task: 04 — 낱말과 문서를 맞춘다
Type: grilling
Status: resolved

## What this ticket builds

**Nothing yet — this is a decision about a name and a place.**

> The user, 2026-09-01: *"could the glossary be pulled out as its own thing? Does the glossary have to
> live in something called CONTEXT? — that is what I was wondering."* (「용어집은 용어집으로 따로 뺄 수
> 있을까? 용어집이 콘텍스트라는 폴더에 있어야 되나요? 싶었네」)

## What stands today, measured

- **It is not a folder.** `CONTEXT.md` is **one file at the repository root**
- **It is already only the glossary.** Its own first line says so: 「용어집이다. 이 파일에는 낱말만
  산다」 — the reasons and the user's words live in the log instead
- **Sixteen places name it** — the instructions file, the map, the log, five nets, the knowledge folder,
  two design docs, and a candidate note
- ⚠⚠ **One of those sixteen is why it is at the root**: the instructions file pulls it in with a single
  line, so **it arrives with every session and nobody has to remember to open it.** That line exists
  because a round was spent building the wrong thing after 「판」 was guessed at instead of read

⇒ **What is actually being asked is the NAME, not the contents.** The file already is what the user
wants it to be; it is called something that does not say so.

## Screen

**none.**

## Done when

**The user has said what it is called and where it sits.** If the answer is 「as it is」, this ticket
closes on that sentence with nothing moved.

⚠ **Not done when the model picks a name.**

## What the user has to answer first

- **What it is called**
- **Whether it stays at the root** — it has to, if it is to keep arriving automatically every session

## Implementation plan

> The user, 2026-09-01, choosing the recommendation: *"do 1 and 6 as recommended."*
> (「1번 6번 은 추천대로 해줘」)

⇒ **`CONTEXT.md` becomes `GLOSSARY.md`, and it stays at the repository root.**

### Why the root, and it is not a preference

**`CLAUDE.md` pulls the file in with a single `@` line**, so it arrives with every session and nobody
has to remember to open it. **That line exists because a round was spent building the wrong thing
after 「판」 was guessed at instead of read.** ⇒ **Moving it into a folder is free; moving it out of
the root is not.**

### What it is called

**`GLOSSARY.md`.** ⚠ **English, like every other file name in this repo** — the Korean column inside
it stays Korean because the Korean word IS the name, and that rule is unchanged by this ticket.

### Measured today, and the ticket's own count was low

**The ticket above says 「sixteen places name it」. Re-counted 2026-09-01: 28 files, ~48 lines**
(the leftover worktree under `.claude/worktrees/` is excluded and is not touched).

| Where | Files | Note |
|---|---|---|
| **The instructions file** | `CLAUDE.md` (4) | ⚠⚠ **One of the four is the `@CONTEXT.md` import.** Get this wrong and the glossary stops arriving |
| **The map and the log** | `docs/roadmap/README.md` (1) · `log.md` (5) | ⚠ **See the quotation rule below** |
| **Tickets and tasks** | five files under `docs/roadmap/task-*/` | includes this ticket and `04.task.md` |
| **The code** | `src/look.gd` · `src/sim/army.gd` · `src/sim/rules.gd` (1 each) | comments only |
| **The nets** | `net_boats` · `net_fight` · `net_fx_view` · `net_islands` · `net_roster` · `net_shell` · `net_tiers` (1 each) | ⚠⚠ **comments only — measured: no net opens the file** |
| **The docs** | `docs/design/07-how-the-sea-moves.md` · `docs/개발지식/README.md` | |
| **The skills** | `.claude/skills/` — `naming` (7) · `README` (3) · `diagnosing-bugs` (2) · `knowledge` (1) · `codebase-design` (1) | ⚠ **`naming` is the skill that WRITES this file** |
| **Loose notes** | `TODO.md` · `.prototypes/palette/NOTES.md` · `.candidates/ore/notes.md` | |

⚠⚠ **Nothing reads the file by path at runtime.** All seven net hits are prose in comments, so
**the rename cannot redden a net by itself** — which also means **no net will catch it if a reference
is missed.** The check is a repo-wide search, run after.

### ⚠⚠ The one rule that decides which lines change

**`CLAUDE.md`: where the user's own words are cited, the words are not swapped.** A file name inside a
quotation is part of the quotation.

- **A pointer changes** — 「용어집은 `CONTEXT.md` 에 있다」 becomes `GLOSSARY.md`
- **A quotation does not** — the user's own sentence in `04-03` above asks *"does the glossary have to
  live in something called CONTEXT?"* and **that sentence keeps the old name**
- ⚠ **`docs/roadmap/log.md` is the decision log and most of its five hits are historical.** Builder
  reads each one: **a line describing what a past round did keeps the old name**; a line telling a
  reader where to look now gets the new one

### Order

1. **`git mv CONTEXT.md GLOSSARY.md`** — the history follows the file.
2. **`CLAUDE.md`'s `@CONTEXT.md` import line first**, alone, and confirm the file still arrives.
3. **The other 27 files**, one at a time, each hit read before it is changed.
4. **A repo-wide search for `CONTEXT`** — every remaining hit is either a quotation or is explained
   in the Answer.
5. **`tests/run_nets.ps1`** — not because a net measures this, but because the fingerprint line
   catches a `.gd` file broken by a careless edit.

### Screen

**none.**

### Acceptance

- **`GLOSSARY.md` is at the repository root and `CONTEXT.md` does not exist.**
- **`CLAUDE.md` imports the new name**, and the file's first line still says 「용어집이다」.
- **Every surviving `CONTEXT` hit is listed in the Answer with why it survived.**
- **The net run's red set is unchanged** — this ticket must move no row in either direction.

### Out of scope

- **Changing anything inside the glossary.** ⚠ **`04-01` is the ticket that edits its rows**, and it
  is still `open`.
- **Renaming `docs/개발지식/` or any other folder.** The user was asked about one file.
- **Touching `.claude/worktrees/session-notes-fishing-loop/`** — a leftover copy, already merged.

### Risk

- ⚠⚠ **The import line is the whole reason the file is at the root.** Break it and the glossary
  silently stops arriving, **and nothing goes red** — exactly the failure `how-nets-lie` collects.
- ⚠ **`.claude/skills/naming/SKILL.md` has seven hits and is the skill that writes this file.**
  A missed one sends a future round to a file that is not there.
- ⚠ **The rename touches `tests/nets/net_tiers.gd`, which `02-08` is also editing.** ⇒ **The two
  tickets do not run at the same time.**


### ⚠⚠ Corrections after the adversarial read — 2026-09-01

**`adversary` broke nine places in the plan above. Seven were settled here; none needed the user.**
**The plan above is superseded by this section wherever the two disagree.**

**1. The count above is wrong and the true one is bigger.**
**58 files, 87 lines**, not 28 and ~48. Excluded: `.git/`, `.godot/`, the engine binary, and the
leftover worktree. **What the table above missed:**

| Missed | Why it matters |
|---|---|
| **24 shaders** under `.prototypes/{sea,wake,wave}/*/water.gdshader` | all tracked, all the identical line |
| ⚠⚠ **`src/view/water.gdshader`** | **a live file in `src/`.** The code row named three `.gd` files and did not look at shaders |
| `export_presets.cfg` | the export config names it |
| `.prototypes/palette/island_build.py.orig` | ⚠ **a `.orig` backup — left alone**, and said so in the Answer |

**2. The file's own H1 is IN scope.** Line 1 reads `# CONTEXT — 이 저장소가 쓰는 낱말`. **A file called
`GLOSSARY.md` whose title says `CONTEXT` is exactly the label-disagrees-with-value failure this repo
collects.** ⚠ **This is not 「editing the glossary」** — `04-01` edits its ROWS; the title is the file's
own name written twice.

**3. The Acceptance line above is factually wrong about the file.** 「the file's first line still says
「용어집이다」」 — **it does not.** Line 1 is the H1 and 「용어집이다」 is line 3. ⇒ **The real acceptance
is: line 1 says `# GLOSSARY`, line 3 still says 「용어집이다」.**

**4. What confirms the import — and nothing in the repo can.** `CLAUDE.md` line 5's `@CONTEXT.md` is
**the only `@` import in the whole repo**, it resolves at session start, and **no git hook, no CI and
no net validates it.** `net_citations`' `LOOSE_DOCS` does not list this file.
⇒ **The confirmation is stated flatly and not pretended to be a test**: `CLAUDE.md` line 5 reads
`@GLOSSARY.md`, and a file exists at that exact path. ⚠ **Whether it actually arrived is only visible
in the NEXT session.**

**5. Step 5's stated reason is wrong — drop it.** The runner's fingerprint hashes every `.gd|.tscn|.tres|.md`
under `src`, `tests`, `docs` plus `CLAUDE.md`, so **this ticket moves it by construction and it can
signal nothing.** ⇒ **What catches a broken file is the net run's own parse failure.** That is the check.

**6. The collision with `02-08` is four files, not one** — `net_tiers` · `net_islands` · `net_fx_view` ·
`net_shell`. **And `04-01` is the harder collision**: it edits this very file's rows.
⇒ **The order is fixed: `02-08` finishes, then `04-03`, then `04-01`.** ⚠ **A fresh full run is taken
as the baseline AFTER `02-08` lands**, or 「the red set is unchanged」 measures two tickets at once.

**7. The pointer-versus-quotation rule leaves three hits it cannot decide.**
`log.md` lines 1125 and 2857, and `.claude/skills/README.md` line 73, are **present-tense pointers
sitting inside a past session's record.** ⇒ **A pointer is updated wherever it sits**: it tells a
reader where to look now, and the file it points at has moved. **Only the user's quoted sentences keep
the old name.**

**8. `naming/SKILL.md` line 13 is left alone and said so.** It forbids a `CONTEXT-MAP.md`, which is a
**forbidden name, not a pointer** — the rule above does not reach it. ⚠ **It is written into the Answer
as a surviving hit with its reason**, rather than silently left.

## Answer

**It is called `GLOSSARY.md` and it stays at the repository root** (2026-09-01, the user, taking the
recommendation: *"do 1 and 6 as recommended."* 「1번 6번 은 추천대로 해줘」).

**Renamed with `git mv`, so the history follows** — git records it as `RM CONTEXT.md -> GLOSSARY.md`.
**66 lines changed across 51 files.** The H1 became `# GLOSSARY — 이 저장소가 쓰는 낱말`; line 3's
「용어집이다」 is byte-identical to what it was.

### ⚠⚠ Nothing in this repo could have caught a mistake here, and that was measured

| Checked | Result |
|---|---|
| **Anything that opens the file at runtime** | ⚠⚠ **Zero hits, repo-wide** |
| **`net_citations`' `LOOSE_DOCS`** | The glossary is not in it |
| **`net_citations`' doc-path regex** | Matches only `docs/….md`, so a bare `GLOSSARY.md` in a comment is invisible **by construction** |
| **`project.godot` · `.gitignore` · settings · agent definitions** | No hits |
| **`export_presets.cfg`** | Its one hit is a comment; the live filter is `*.md` |
| **Korean 콘텍스트/컨텍스트, lowercase and spaced variants** | All quotations, the ticket's title, the English word, or `bpy.context` |

⇒ **No net could ever have caught this rename.** ⚠ **The import line is the only thing that matters
and nothing validates it**: it reads `@GLOSSARY.md` and a file exists at that path, but **whether the
glossary actually arrives is only visible in the next session.**

### The counts the plan got wrong, and the one it got wrong twice

**The ticket said sixteen places. The adversarial read said 58 files / 87 lines. The real sweep found
93 hits.** ⚠ **The original plan's code row named three `.gd` files and missed every shader** — including
a live one in `src/view/`.

⚠⚠ **And the corrections were wrong about one file too.** They sanctioned leaving
`.prototypes/palette/island_build.py.orig` as 「a `.orig` backup」. **`git ls-files` shows no
`island_build.py` — the `.orig` is the only tracked copy**, so it is a backup of nothing, and its line
was a present-tense pointer carrying the dead name. **Fixed after `verify` found it.**

### What survives on purpose

| Hits | Why |
|---|---|
| **This ticket's own 17** | It is the record of the job |
| **The ticket's NAME** — 「용어집을 CONTEXT 에서 뺀다」, in the map and its task file | ⚠ **Renaming it would rewrite what was asked** |
| **Two lines saying 「`CONTEXT.md` 를 이름 바꾼다」** | ⚠⚠ **The old name is the OBJECT of the job, not a pointer.** 「renames `GLOSSARY.md`」 would be false |
| **A user quotation** | Never reworded |
| **`CONTEXT-MAP.md` in the naming skill** | **A forbidden name that never existed here**, not a pointer |
| **Three records in `log.md`** | ⚠ **「we fixed these ten places」 is a record; 「the agreed three are in X」 is a pointer.** The first two stay, and the third, three lines away, was changed |
| **`bpy.context` and the English word** | Not this file |

⚠ **One record in `log.md` is borderline and was kept**: it quotes the literal `@CONTEXT.md` inserted
on 2026-08-28, **and `CLAUDE.md` no longer contains that string.** A reader consulting it to learn how
the glossary arrives now reads something the file does not say.

### The nets

**1476 pass · 4 fail before and after, the same fingerprint both rounds.** ⚠ **That proves no `.gd`
file was broken, and nothing else** — no row here measures the rename.

### Out of scope and left alone

**The folder this ticket lives in is still called `03-glossary-leaves-context`.**
