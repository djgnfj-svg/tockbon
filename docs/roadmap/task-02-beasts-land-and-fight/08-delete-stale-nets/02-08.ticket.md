# 02-08 — 낡은 검사를 지운다

Ticket: 02-08
Name: 낡은 검사를 지운다
Task: 02 — 몬스터가 내려서 싸우고 성을 부순다
Type: task
Status: resolved

## What this ticket builds

**The stale nets go, rather than being taught the new numbers.**

> The user, 2026-09-01, correcting this ticket: *"about the stale tests — I asked you to delete them,
> not fit them to the current island."* (「낡은 검사에 대해서는 지워달라고 했어 지금 섬에 맞추는 게
> 아니라」)

⚠⚠ **THIS TICKET SAID 「맞춘다」 UNTIL THE USER CORRECTED IT.** The previous version proposed correcting
six expected values so the 64 rows would go green. **The user wants them gone.**

### What is failing, measured 2026-09-01 on the merged tree

**통과 1227 · 실패 64**, six of fifteen nets red — `tiers` 25 · `islands` 17 · `fx_view` 9 · `shell` 6 ·
`camera` 6 · `wake` 1.

**Every one of the 64 is a net holding a number from a smaller island. Not one is a defect in the
game.** The six values behind them:

| # | What the nets believe | What is true |
|---|---|---|
| 1 | 12 행 × 16 자 · 땅 76 · 고원 16 · 계단 1 | **26 행 × 30 자 · 땅 284 · 고원 48 · 계단 4** |
| 2 | 섬에 짐승 여섯이 있다 | **0.** 글자는 `H`·`~`·`.` 뿐이고 짐승은 배로 온다 |
| 3 | 상륙지는 다 섬으로 이어진다 | **위성 섬 4 칸.** 설계 31 이 결함이 아니라고 정했다 |
| 4 | 늑대 그림틀 92 × 92 | **92 × 66** |
| 5 | 손으로 만든 판에 지형 메시가 있다 | **없다.** 메시는 `island.glb` 하나뿐 |
| 6 | 자가 점검이 배 반길이 2.0 초과를 요구한다 | **2.1 이라 `want` 가 1.95 다** |

## ⚠⚠ What deleting costs, measured on this repo

**On 2026-09-01 a branch deleted the rows measuring a camera it had removed, and the run lost 15
passes.** Deleting is not free: **a red row and a green row often sit in the same net**, and the six
red nets carry passing rows too.

⇒ **What gets deleted has to be named**: the 64 red rows only, or the six net files whole. **Those are
different numbers and the second one throws away passing coverage.**

⚠ **`how-nets-lie` is the standing rule here** and it cuts both ways — a net that measures an old
island is a false green waiting to happen, and a net deleted to make a run green is the same thing.

## Screen

**none.** ⚠ **Nothing the player looks at changes.** If it does, one of the 64 was a real defect.

## Done when

**The stale nets are gone and the run is green** — and a count is written down saying how many rows
were deleted and how many passing rows went with them.

## What the user has to answer first

- **The 64 red rows only, or the six net files whole** — the second one deletes passing coverage as well
- **Whether anything replaces them** — after this the island has no net measuring its shape at all

## Implementation plan

### What the user answered, 2026-09-01

| Question | Answer |
|---|---|
| **64 red rows, or six net files whole** | **The 64 rows only. The files stay** |
| **What replaces them** | **Nothing** |

### Re-measured today, on this tree

**통과 1227 · 실패 64 · 15 그물 · 16.4s.** The ticket numbers hold.

| Net | Red rows |
|---|---|
| `tiers` | 25 |
| `islands` | 17 |
| `fx_view` | 8 |
| `camera` | 6 |
| `shell` | 6 |
| `wake` | 1 |

### A fact the ticket does not carry

⚠⚠ **Three nets did not finish.** `fx_view`, `camera` and `tiers` each **abandoned a function part-way**
and the runner says so out loud: 「그물 3개가 끝까지 못 갔다 — 이 라운드의 통과 수는 잰 것의 일부다」.

⇒ **1227 is a floor and not the count.** **Deleting a red row inside an aborted function makes the rest
of that function run for the first time**, and some of what runs may be red too.

⚠⚠ **The after-count therefore cannot be worked out from the before-count.** The ticket asks how many
passing rows went with the deletion; **that number comes from two full runs, never from arithmetic.**

### Seams

**None new.** The nets already sit at the three agreed seams and this ticket only removes rows.

### Files to touch and why

| File | Why |
|---|---|
| `tests/nets/net_tiers.gd` · `net_islands.gd` · `net_fx_view.gd` · `net_shell.gd` · `net_camera.gd` · `net_wake.gd` | **Rows deleted. The six files stay** |

### Order

1. **A full run, saved.** That is the before.
2. **One net at a time**: delete its red rows, run that net alone, move on.
3. **A full run.** That is the after.
4. Write **both counts** and **one line per deleted subject** into this ticket Answer.

### Risk

- ⚠⚠ **On 2026-09-01 a branch deleted the rows measuring a camera it had removed and the run lost 15
  passes.** Deleting is not free — **a red row and a green row often sit in the same net.**
- ⚠⚠ **And here it cuts the other way too**: un-aborting three functions may raise the count or lower it.
  **Neither direction is a defect on its own; both have to be read.**
- **After this the island has no net measuring its shape at all.** The user said nothing replaces them.
  ⚠ **Task 06 widens the island** — that is when a shape net is written again, against a settled island.
- ⚠ **`how-nets-lie` cuts both ways.** A net measuring an old island is a false green waiting to happen,
  and **a net deleted to make a run green is the same thing.** ⇒ **Every deleted row gets one line
  saying what stopped being measured.**
- ⚠ **`net_boats` self-check about the hull half-length is GREEN today and belongs to 02-05**, not here.
  It goes red when the small boat lands, and it moves with the hull rather than being deleted.

### Acceptance

- `tests/run_nets.ps1` exits 0.
- ⚠ **No 「불완전」 line in the summary** — that is what says the three aborted functions actually ran.
- This ticket Answer carries the before count, the after count, and one line per subject that stopped
  being measured.

### Out of scope

- Writing any new net.
- Correcting an expected value so a row goes green. ⚠ **The user corrected this ticket on that exact
  point**: 「지워달라고 했어 지금 섬에 맞추는 게 아니라」.

## Answer

⚠⚠ **The second half, done and verified 2026-09-01.**

**`net_tiers` went from 98 pass · 17 red · aborted to 110 pass · 0 red · stderr clean.**
**The suite went from 1464 pass · 21 fail · two nets unfinished to 1476 pass · 4 fail · one unfinished.**

⚠ **Builder read the before as 1463 · 22.** `verify` re-took it in a clean scratch worktree and got
**1464 · 21**, and the stray row is in someone else's net moving under a concurrent tree — **not in
`net_tiers`, and not this ticket's.** ⇒ **`verify`'s numbers are the ones on the record.**

**The four reds left are scoped out and named**: `net_fx_view` 2 · `net_shell` 1 · `net_boats` 1.

### What stopped being measured — one line each, which is the rule

| Deleted | What no longer has a check |
|---|---|
| `_the_reach_check_can_actually_fail` + `SEALED_*` | **「the reachability check can actually fail」** — that an enemy sealed on a plateau with no stair is reported unreachable. **Nothing else in the repo asserts it** |
| The `(22,2)` block — four rows, its `landing`, and the `Grid`/`Battle` built for it | **that a real landing 조각 on the loaded island hands back free tiles and none of them are high.** ⚠ It was measuring this from a **water** 조각 |
| `_spawns_of_rows` · `_landings_of` · `_unreachable_pairs` | nothing — **they had no caller left.** ⚠ `_is_landing` is kept; it still has one |

### ⚠⚠ Not one row went green by getting weaker — this was measured, not argued

**`verify` broke the code four ways and watched the rows bite:**

| What was broken | What went red |
|---|---|
| **A body may cross two 눈금 instead of one** | **8 rows** — all three 「경계는 못 넘는다」, the two path costs, the UNREACHABLE count, both strips |
| **The plateau flattened again — the pre-fix condition** | **all 15** of the red-to-green rows, **and 5 more** |
| **The collect guard dropped** | 「그중 계단 칸은 없다」 — 얻은 값 2 · 기대 0 |
| **The walk guard weakened to bare passability** | both strip rows |

**And no expectation moved.** The only non-comment, non-board line in the whole diff is the inline
board. **Every failure line in the before-run reads `얻은 값 X · 기대 Y`, and every `Y` is the same
literal sitting in the file today** — the observed value moved, the expectation did not.

### ⚠ A pre-existing hole `verify` found, which this ticket did NOT cause

**Deleting the level test inside `_shoulder_open` leaves the net green.** `can_step` refuses the
diagonal on the destination test first, **so no board in this file can isolate the shoulder guard.**
⚠ **The diagonal row still bites for its own subject** — two of the four mutations redden it.

### ⚠ A duplicate this round created and deliberately left

**`TREAD_ROWS`/`TREAD_TIERS` are now character-for-character identical to `FIXTURE_ROWS`/`FIXTURE_TIERS`.**
That block existed for exactly one reason — 「it uses its own board because `FIXTURE_TIERS` is broken」 —
and **fixing the letter spent that reason.** ⚠ **Not merged**: the final list forbade touching
`TREAD_TIERS`. **It is written into the file's own header so the next reader does not re-derive it.**

### What the round cost, and it is worth keeping

- ⚠⚠ **The whole second half of this ticket was erased by a `git checkout --` and restored by hand.**
  **A plan that lives only in the working tree is one command from gone.**
- ⚠ **A justification written here was wrong and `verify` measured it so** — see the `CLIMB_TIERS` note
  above. **The edit was right and the argument for it was not**, which is why the argument is corrected
  rather than quietly dropped.

## ⚠⚠ 안 닫혔다 — 무엇이 모자란가 (2026-09-01)

**`Done when` 은 「낡은 검사가 없어지고 실행이 초록이다」인데 초록이 아니다. 실패 21 이 남았다.**

### 한 것

**실패 64 → 21.** 낡은 섬 숫자를 든 줄들은 지웠다.

### 안 한 것 — 그리고 이건 빠뜨린 게 아니다

**`net_tiers` 의 빨간 줄 열여덟에 손을 안 댔다.** 짓던 에이전트가 멈추고 물었고, 그 물음이 아직 열려 있다.

**그 열여덟은 낡은 섬 숫자가 아니다.** 손으로 지은 판이 고원을 글자 `1` 로 적는데,
**`grid.gd` 가 2026-08-26 에 `1` 의 뜻을 눈금 2 에서 눈금 1 로 바꿨다** (사용자가 계단을 넓히면서).
그래서 그 판들이 이제 **넘을 수 있는 한 눈금 단**을 짓고 줄이 빨개진다.

**그 열여덟이 재던 것**: 「층 경계는 못 넘는다」 · 「반대 방향으로도 못 넘는다」 · 「대각선으로도 못 넘는다」 ·
「계단을 막으면 고원이 봉쇄된다」.

⚠⚠ **지우면 「눈금 차이 2 는 못 넘는다」를 재는 것이 저장소에 하나도 안 남는다.**
**오늘 02-01 이 바로 그 규칙 위에 `can_strike` 를 세웠다.**

### 사용자가 답할 것

**그 열여덟을 지우나, 판 글자 `1` 을 `2` 로 고쳐 살리나.**
⚠ **글자 하나를 고치는 것은 「지금 섬에 맞추는 것」이 아니다** — 그 판들은 섬이 아니라 손으로 지은 것이다.

### 그리고 줄만 지워서는 초록이 안 된다

**`net_wake` 와 `net_fx_view` 가 엔진이 짖는 것 때문에 빨갛고, 그건 HEAD 부터 있던 것이다** —
그리는 쪽이 트리 밖에서 카메라의 전역 변환을 읽는다. **이 티켓의 범위가 아니다.**

**남은 빨강 21 의 갈래**: `net_tiers` 17 · `net_fx_view` 2 · `net_shell` 1 · `net_boats` 1.

---

## ⚠⚠ Implementation plan — the second half, after the user answered on 2026-09-01

> The user, choosing between the two branches this ticket had left open: *"do 1 and 6 as
> recommended."* (「1번 6번 은 추천대로 해줘」)

⇒ **The seventeen red rows are KEPT and the fixture's board letter is corrected.** They are not
deleted. **This is not 「fitting the nets to the current island」** — the boards in question are
hand-built fixtures, not the island, and the letter they use has meant the wrong thing since
2026-08-26.

### What is actually wrong, read out of `src/sim/islands.gd`

**The tier legend says it outright**, and has since 2026-08-27:

> *"Write the plateau as `1` and it becomes one notch above the ground, a body walks up it anywhere
> along the cliff, no error is raised, no net goes red, and the second storey quietly stops being a
> place you have to earn. This legend said `1` was the high ground until 2026-08-27 and it was wrong
> the whole time."*

- **A storey is TWO notches, a stair is ONE.** Storeys are the even levels, stairs the odd ones
- ⇒ **A plateau is `2`. A stair is `1` (old spelling `/`).**
- **The fixtures spell their plateau `1`**, so on those boards the plateau and the stair sit at the
  same level, there is no wall for the stair to be the door through, and seventeen rows measure
  nothing

⚠⚠ **`net_tiers` ALREADY CARRIES THIS DIAGNOSIS IN TWO PLACES** — the header above `TREAD_ROWS`
and the one above `_the_field_climbs_only_by_the_stair`. Both say **「re-reading that fixture is its
own round」**. **This is that round.**

### The fixtures to correct, and the one that must not be touched

| Constant | What its plateau is spelled | Action |
|---|---|---|
| `FIXTURE_TIERS` | `1` | **`1` → `2`** ⚠ **the `/` at (4,3) stays** — it is the stair |
| `FIXTURE_TIERS_NO_STAIR` | `1` | **`1` → `2`**, and it keeps having no stair |
| `SEALED_TIERS` | `1` | **`1` → `2`** |
| `PAIR_TIERS` | `1` | **`1` → `2`** |
| `CLIMB_TIERS` | `1` | **`1` → `2`** |
| `HOLD_TIERS` | `1` | **`1` → `2`** |
| `SPLIT_TIERS` | `1` | **`1` → `2`** |
| **`TREAD_TIERS`** | **already `2`** | ⚠⚠ **DO NOT TOUCH.** It was written to today's legend on purpose |

⚠ **Builder checks each board against its own header comment before changing it.** A `1` that a
comment calls a stair is a stair and stays a `1`. **The table above is the reading; the comment beside
each constant is the authority.**

### Seams

**None new.** `Grid` is constructible with `.new()` and is the `src/sim/` seam the glossary names.
**This ticket edits fixtures only** — no line of `src/` moves.

### Order

1. **Run `net_tiers` alone and save the output.** That is the before: **17 red, one function aborted.**
2. **Correct the fixtures**, one constant at a time, re-running `net_tiers` after each.
3. ⚠⚠ **Read `_the_reach_check_can_actually_fail`, which is the aborted function.** Correcting the
   board makes it run for the first time — **what it then says is a measurement, not a regression.**
4. **Fix the hand-counted companions if they move**: `FIXTURE_HIGH` 12, `FIXTURE_STAIR` 1,
   `FIXTURE_LOW` 15, `FIXTURE_LOW_NO_STAIR` 16, `SEALED_LANDINGS` 26. ⚠ **These are counts of
   tiles, not of levels, so they should NOT move** — if one does, builder stops and says so rather
   than retyping it.
5. **A full run.** That is the after.

### Acceptance — ⚠⚠ scoped, and the old one on this ticket cannot be met

- **`net_tiers` has 0 red rows and no 「불완전」 line against it.**
- ⚠⚠ **`run_nets.ps1` still will NOT exit 0**, and that is not this ticket's failure. **Four reds
  belong elsewhere**: `net_fx_view` 2 · `net_shell` 1 · `net_boats` 1. **The Acceptance written
  further up this file predates them and is wrong.**
- **No row is deleted in this half**, and no expected value is loosened to make a row pass.
- **The before and after pass counts are written into the Answer**, both from full runs, never
  from arithmetic.

### Risk

- ⚠⚠ **Un-aborting `_the_reach_check_can_actually_fail` may raise the count or lower it.** Neither
  direction is a defect on its own; **both have to be read and reported.**
- ⚠ **A board that gains a real wall may make an unrelated row in the same function red** — a route
  that used to walk over the plateau now has to go round. **That is the rule being measured for the
  first time**, and builder reports it rather than widening the board.
- ⚠ **`net_boats` went red AFTER this ticket's own measurements were taken** (the small boat, 02-05).
  **It is not in scope here** and the note further up this file calling it green is stale.

### ⚠⚠ Corrections after the adversarial read — 2026-09-01

**`adversary` drove the corrected boards headless and broke the plan in three places.** It also
attributed all seventeen: **fourteen are covered by the table above** (`FIXTURE_TIERS` 7 ·
`FIXTURE_TIERS_NO_STAIR` 4 · `SPLIT_TIERS` 3). **Three are not**, and they are below.

**⚠ Every `*_TIERS` constant was checked against its own header comment and the table above is right**
— no `1` on any of the seven legitimately means a stair, and `TREAD_TIERS` really is the only named
constant already at `2`.

**1. The board table missed an inline board.** `_a_short_tier_board_is_flat_where_it_ends` holds
`["..1"]` written straight into the row and it expects level 2. ⇒ **`["..2"]`.** Verified: it then
reads `level_at(2,0) = 2`.
⚠ **A builder following the table exactly would have left this one red.**

**2. `(22,2) 는 승인된 상륙지다` is a stale island number and is DELETED.** Measured on the loaded
30×26 island: **(22,2) is water** (`passable=0, water=1`). **No fixture edit reaches it** — it is the
same class the first half of this ticket already deleted at the user's word.
⚠⚠ **AND THE TWO ROWS UNDER IT ARE A FALSE GREEN.** They run from that water 조각 and pass:
`_free_tiles_from(water, 10)` cheerfully returns ten land 조각, none of them high. **Deleting the
self-check without deleting them leaves two rows measuring nothing.** ⇒ **All three go, and the
Answer carries one line saying what stopped being measured.**

**3. The aborted function does NOT un-abort, and the board letter was never why.**
`_the_reach_check_can_actually_fail` dies on `spawns[0]` because
`_spawns_of_rows(SEALED_ROWS)` returns **zero**. `SEALED_ROWS` writes its defender as **`S`** — the
shieldbearer, **deleted** — and `Islands.SPAWN_ROWS` binds exactly one letter, **`W`**. With `S`,
tile (5,4) is a hole: `passable=0, water=0`.

⇒ **`S` → `W` repairs the abort**, and that is a terrain board, which the tier table above does not
cover. ⚠ **Doing it is what makes the rest of the function run for the first time**, and what it then
says is the blocker below.

### ⚠⚠ BLOCKED — one thing the user has to answer

**Repairing that board makes three rows run that have NEVER run, and the fixture's own stated design
is no longer expressible.**

`_is_landing` was re-aimed on 2026-08-29 to mean **「could a body be stood here at all」** — passable,
not water, not a stair — **with no adjacency-to-water test at all.** `grid.gd` says so
outright. So `SEALED_LANDINGS`' own comment (「the 8-way coast is its perimeter: 2 x (9 + 6) - 4 = 26」)
describes a predicate that no longer exists.

**Measured on the repaired board:**

| Row — none has ever run | Expects | Actually |
|---|---|---|
| `landings.size()` | `SEALED_LANDINGS` = 26 | **54** |
| `on_high == 0` (self-check) | 0 | **9** |
| `_unreachable_pairs(...)` | `SEALED_LANDINGS` = 26 | **45** |

⚠⚠ **The fixture's design sentence — 「the plateau is inland so that no landing tile is on it」 — is
dead.** Under today's predicate **a plateau 조각 IS a landing**, which is why the third row comes out
45 and not 54. **One constant can no longer serve both rows.**

⇒ **This is a re-derivation, not a retype**, and 「repair it」 here produces a net whose comment says
one thing and whose numbers measure another. **That is the shape `how-nets-lie` collects**, so it is
not settled by consistency with the user's earlier 「repair, do not delete」.

**The question**: repair the function with two hand-derived numbers and rewrite its design sentence,
or delete this one function.

### What the adversarial read CONFIRMED, driven headless on the corrected boards

- **The tier table above is right.** Every `*_TIERS` header names its stair as `/` or says it has none,
  so **no `1` on the seven is a stair.**
- ⚠⚠ **THREE MORE INLINE BOARDS ARE ALREADY `2` AND MUST NOT BE TOUCHED** — one in
  `_a_corner_stair_picks_one_mouth_and_keeps_it`, two in `_a_stair_is_entered_at_its_ends_only`.
  **Only the `["..1"]` in `_a_short_tier_board_is_flat_where_it_ends` changes.**
- **Four of the five hand-counted companions hold**: `FIXTURE_HIGH` 12 · `FIXTURE_STAIR` 1 ·
  `FIXTURE_LOW` 15 · `FIXTURE_LOW_NO_STAIR` 16, measured as 12 / 12 / 12 / 16 / 0 with 12 + 16 = 28.
  **`SEALED_LANDINGS` is the only one that moves**, and it is the blocker above.
- **The corrected boards produce exactly what the red rows ask for**: `cost(5,1) = 62`, stair cost 38,
  cheapest plateau 48 — one orthogonal step above the stair — and on the split board `level_at(4,3) = 2`,
  strip 14, crossed 0.
- **No green row that actually runs goes red.** The one at risk — the stair half of
  `_a_landing_never_puts_a_body_on_the_plateau` on `CLIMB_TIERS` — measured green after the correction.
- **Nothing outside `net_tiers` reads these constants**, and **no line of `src/` has to move**:
  every one of the seventeen traces to a fixture letter or to stale island data, not to a `Grid` defect.
- ⚠ **`PAIR_TIERS` and `HOLD_TIERS` sit on boards NO ROW READS** — `_pair_battle`, `_step_for` and
  `_ashore` are dead and `HOLD_ROWS`, `CLIMB_START`, `CLIMB_ENEMY` have no callers. **Correcting them
  changes nothing either way**, and that is worth one line in the Answer rather than a silent edit.
- ⚠ **`ACROSS_THE_WALL` (2.236) is stale** — derived when a 층 stood 2.0 조각, today's answer is 1.414 —
  **but it has no reader**, like `FIXTURE_W`, `SQUEEZE_W`, `CLIMB_W` and `HOLD_W`.
- **`log.md` carries no decision about `SEALED_ROWS`, the `S` letter, or the landing predicate**, so
  nothing raised here re-opens something the user already settled.

### ⚠⚠ The blocker is answered — 2026-09-01

> The user, on whether to repair `_the_reach_check_can_actually_fail` or delete it: *"go ahead with
> everything except 3."* (「3번 빼고 진행」) — taking the recommendation, **which was: delete that one
> function.**

⇒ **`_the_reach_check_can_actually_fail` is DELETED**, and with it `SEALED_ROWS`, `SEALED_TIERS` and
`SEALED_LANDINGS`, which nothing else reads.

**Why deleting is right here and repairing was right for the other fourteen**: the other fourteen keep
measuring exactly what their comments say once the board letter is fixed. **This one cannot** — its
design sentence 「the plateau is inland so that no landing tile is on it」 is false under today's
`_is_landing`, and repaired it would be a net whose comment says one thing and whose numbers measure
another.

⚠⚠ **What stops being measured, and it goes in the Answer**: **「the reachability check can actually
fail」** — that an enemy sealed on a plateau with no stair is reported unreachable. **Nothing else in
the repo asserts it.** ⚠ **`S` → `W` is therefore NOT done** — the board it repaired is going away.

### The final list builder works from

| # | What | Action |
|---|---|---|
| 1 | `FIXTURE_TIERS` (7 rows) · `FIXTURE_TIERS_NO_STAIR` (4) · `SPLIT_TIERS` (3) | **`1` → `2`**, `/` untouched |
| 2 | The inline `["..1"]` in `_a_short_tier_board_is_flat_where_it_ends` | **`["..2"]`** |
| 3 | `(22,2) 는 승인된 상륙지다` **and the two rows under it** | **deleted** — stale island number plus the false green it was propping up |
| 4 | `_the_reach_check_can_actually_fail` + `SEALED_ROWS` + `SEALED_TIERS` + `SEALED_LANDINGS` | **deleted** |
| 5 | `PAIR_TIERS` · `HOLD_TIERS` | **`1` → `2` anyway**, and one line in the Answer saying no row reads them |
| 6 | `TREAD_TIERS` and the three inline boards already at `2` | ⚠⚠ **NOT TOUCHED** |
| 7 | `FIXTURE_HIGH` · `FIXTURE_STAIR` · `FIXTURE_LOW` · `FIXTURE_LOW_NO_STAIR` | ⚠ **NOT TOUCHED** — measured to hold |

### Two gaps in the final list, closed while building — 2026-09-01

**Builder stopped on both rather than guessing. Neither needed the user.**

- ⚠⚠ **`CLIMB_TIERS` was dropped from the final list by mistake and goes `1` → `2`.** The earlier
  fixture table named it and the adversarial read drove it corrected — which is why the row on it
  measured green. **It IS read**, by `_a_landing_never_puts_a_body_on_the_plateau`, and `2` is what
  the board's own stated design asks for.
  ⚠⚠ **THE REASON FIRST WRITTEN HERE WAS WRONG AND `verify` MEASURED IT SO.** This said 「left at `1`
  the board has no wall, so the row is a green measuring nothing」. **It is not.** Put back to `1` in a
  scratch copy with the collect guard removed, **the row went red — 얻은 값 3 · 기대 0.** It bit at `1`
  too. ⇒ **The edit is right; the argument for it was not.**
- **The `(22,2)` deletion is the whole block** — the `landing` variable and **four** rows, not two,
  plus the `Grid` and `Battle` built only for them. ⚠ **The fourth is `그리고 낮은 층이다`**, a
  self-check on a water 조각 whose 「그리고」 would otherwise point at a deleted row.
- **`_spawns_of_rows` · `_landings_of` · `_unreachable_pairs` are deleted too.** All three lost their
  only caller with the sealed-board function. ⚠ **An instrument nothing inverts is exactly what this
  ticket exists to remove**, and dead scaffolding in a net file is how the next round mistakes it for
  something that measures. ⚠ **`_is_landing` is KEPT** — `_the_first_island_carries_a_real_plateau`
  still calls it.

### ⚠⚠ A round-cost worth keeping: this file was wiped and restored

**Reverting an edit it had been told not to make, builder ran `git checkout --` on this ticket.**
**The whole second half was uncommitted, so that erased it** — the plan, the corrections, the
adversarial read, the blocker answer and the final list, all of it.

**It was restored from the copy builder had read at the start**, and the section above was lost with
the wipe and written again by hand.

⇒ ⚠⚠ **A plan that lives only in the working tree is one `git checkout` from gone.** **`wrap-up`
commits; until it does, nothing here is safe.**
