# 02-07 — 안 읽히는 그림 스물넷을 지운다

Ticket: 02-07
Name: 안 읽히는 그림 스물넷을 지운다
Task: 02 — 몬스터가 내려서 싸우고 성을 부순다
Type: task
Status: resolved

## What this ticket builds

**The pictures the game does not load stop sitting beside the ones it does.**

> The user, 2026-09-01: *"there are a lot of stale tests, stale assets. Clean them up — you keep
> getting confused."* (「낡은 검사 낡은 애셋들이 많거든? 그 정리 좀 해줘 네가 자꾸 헷갈린다」)

⚠⚠ **THE USER NAMED THE COST AND IT IS NOT DISK.** It is that a reader — a person or a model — picks up
a dead file and reasons from it. **The 늑대 walk pictures loose in the folder are not what walks.**

### What was counted, on disk and against the code

| What | Number |
|---|---|
| **Loose `.png` beside the live folders** | **24** |
| — 황소 | **1** (`bull_r.png`) — **there is no bull in this game** |
| — 늑대 물기 | **8** — named with a word that died 2026-08-31 (「물기」→「공격」) |
| — 늑대 걷기 | **8** |
| — 활 · 방패 · 창 · 검 | **5** — the game has one 병종 and it is 검사 |
| **How many of the 24 the game loads** | **0** |
| **Where the live art actually is** | `wolf_h/` and `man/`, **124 pictures each** |

⚠ **Three of the 24 are named in code and none of those is a load**: two appear only in comments that
record why the H 늑대 replaced them, and one is a net asserting the wolf's texture is **not** that file.
**A string comparison keeps working when the file is gone.**

## Screen

**none.** Nothing the player looks at changes — that is the point. ⚠ **If anything on screen changes,
one of the 24 was live and the count was wrong.**

## Done when

**The 24 are gone, the game launches, and every 몸 still wears its picture.**

⚠⚠ **The repository does not get smaller.** 300 MB of history keeps every one of them; deleting from
the working tree changes what a reader sees, not what git stores. **Saying otherwise would be a green
that measures less than its label.**

## What the user has to answer first

- **Whether the screenshot pile goes in the same day** — `tools/shot/` is **123 MB across 494 tracked
  files**, and `wrap-up`'s standing rule is that a shot is never deleted without asking
- **Whether the 시안 and 프로토타입 folders are in scope** — **15 MB / 284 files** and **54 MB / 891
  files**, both tracked

## Implementation plan

### What the user answered, 2026-09-01

**The 24 pictures only.** The screenshot pile, the 시안 folder and the 프로토타입 folder are **not**
touched this day.

### What was counted again on the merged tree, 2026-09-01

**26 loose `.png` sit under `assets/` — not 24.** The split:

| Where | How many | In scope |
|---|---|---|
| `assets/beast/` | **19** — 황소 1, 늑대 물기 8, 늑대 걷기 8, 서 있는 늑대 2 | ✅ |
| `assets/human/` | **5** — 활·방패·창·검 좌우 | ✅ |
| `assets/fx/` | **2** — `slash.png`, `tooth.png` | ❌ **Not in scope.** These are effects, not a 몸 |

⇒ **19 + 5 = 24, and that is the number the ticket names.** `wolf_h/` and `man/` hold **124 each** and
neither is touched.

### One live reader the ticket did not name

⚠⚠ **`tools/pixel/install_anim.py` prints `assets/beast/wolf_r.png` in its own help text** as the
suggested white-balance reference. **It is a tool default and not a game load** — nothing on screen
changes — but **it is a path that stops working**, and the next person running that tool gets a missing
file with no error to read.

### Seams

**None.** ⚠ **Nothing in `src/` changes and nothing on screen changes.** The check is that the game
launches and every 몸 still wears its picture.

### Files to touch and why

| File | Why |
|---|---|
| 19 files under `assets/beast/` and 5 under `assets/human/` | Deleted, **with their `.import` siblings** |
| `tools/pixel/install_anim.py` | Its help text points at a picture that will be gone |
| `src/look.gd` comments | ⚠ **Left alone.** They record why the H 늑대 replaced these, and a comment is not a load |
| `tests/nets/net_fx_view.gd` | ⚠ **Named, not fixed here.** One row asserts the wolf texture does **not** end with `wolf_r.png` — **a string comparison keeps passing when the file is gone**, so after this it measures nothing. It belongs to 02-08 |

### Order

1. Grep once more for a load of any of the 24 — the merge landed since the ticket was counted.
2. Delete the 24 and their `.import` files.
3. Move the tool's help-text default.
4. Launch. A 검사 and a 늑대 both draw.

### Risk

- ⚠⚠ **The repository does not get smaller.** 300 MB of history keeps every one of them. Deleting from
  the working tree changes what a reader sees, not what git stores. **Saying otherwise would be a green
  that measures less than its label.**
- **A `.import` left behind without its `.png` is a broken import**, and Godot is quiet about it.
- ⚠ **If anything on screen changes, one of the 24 was live and the count was wrong.**

### Acceptance

- The 24 files and their `.import` siblings are gone.
- The game launches, a 검사 draws, a 늑대 draws.
- The net run count does not fall.

### Out of scope

- `tools/shot/out/` — **494 tracked files, 123 MB.**
- 시안 — **284 files, 15 MB.** 프로토타입 — **891 files, 54 MB.**
- `assets/fx/slash.png` and `assets/fx/tooth.png`.

## Answer

**스물넷만 지운다. 스크린샷·시안·프로토타입은 안 건드린다** (2026-09-01, the user, choosing the
recommendation: *"do all sixteen of those as recommended"* 「이 위에 열여섯 개 물어봤던 거 다 추천대로 좀
해줘」).

**The reason put with it**: what the user said they kept getting confused by was the body pictures, and
this repo standing rule is that a screenshot is never deleted without asking.

## 닫으며 — 잰 것

**닫혔다 (2026-09-01).** 그림 22 장과 `.import` 22 장, 합쳐 44 개를 지웠다.
**화면에서 확인**: 한 판 전체에서 걸린 그림 파일이 쉰넷이고 **지운 것은 한 장도 안 나왔다.**
타이틀 화면을 지우기 전후로 픽셀 비교했고 다른 것은 물결 위상뿐이었다. 콘솔은 한 줄도 안 떴다.
⚠ **저장소는 안 작아졌다** — 히스토리가 전부 갖고 있다.
