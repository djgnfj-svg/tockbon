# 09-03 — 짓기 모드는 창고만 세운다 — 무엇을 지을지 고르는 자리가 없다
Ticket: 09-03
Name: 짓기 모드는 창고만 세운다 — 무엇을 지을지 고르는 자리가 없다
Task: 09 — 건물을 조각 단위로 짓고, 바리케이트로 길을 막는다
Type: grilling
Status: open
Blocked by: —

## ⚠⚠ Read this first — **손짓은 이미 있다**
**짓기 모드가 서 있고, 사용자가 그것을 골랐다: 「짓기모드가 맞을듯」** — *"build mode seems right"* —
건물 목록보다도, 오른쪽 버튼을 되살리는 것보다도.

**What stands today, measured:**

- **B toggles 짓기 모드.** ESC leaves it **with the 부대 still held**
- **Inside the mode a left press means 「build here」 and nothing else** — the mode exists to keep a
  fourth meaning off that one button
- **A drag inside the mode is not a box**
- **`Hand.building` carries a `Builds` kind**, so the mode is already parameterised by what is built
- **지을 자리 is marked on the ground**, two colours off one refusal rule (`Battle.can_place_store`)

⚠ **The earlier recommendation — a button below the screen — was never answered by the
user, and it had already lost to the mode before it was asked.** The mode is the answer.

## ⚠⚠ What is still missing — and it is NOT the gesture
**`Builds.STORE` is the only kind the key can reach.** `game.gd` asks for it by name and there is no
way to ask for anything else. ⇒ **바리케이트 cannot be placed by a player at all**, and 09-02 built it.

⇒ **The open question is not 「how do I say where」. It is 「how do I say WHICH」.**

## What has to be answered
**How the player picks which building the mode is holding.** A second key per building, a cycle on B,
a small list once the mode is on, or something else.
**Whether the five buildings share one mode** — 성채 · 집 · 탑 · 창고 · 성벽 is the glossary's list and
only two of the five exist.
**What it looks like once there is more than one.** ⚠⚠ **Made, never typed** — a picker is a picture
and nothing is drawn for it.

## ⚠ Left undecided by the build that shipped
**What a 창고 costs** — free today. **Whether it has a capacity** — none today.
**Whether a second build is refused or moves the first** — it moves.
**바리케이트's two values** — 나무 5 · 체력 30, both first values chosen by the side that built it and
neither seen by the user.

## What is NOT in this ticket
- **What a 바리케이트 is and where it may stand** — 09-01, resolved
- **The 바리케이트 standing and the 늑대 going round it** — 09-02, built and unseen

## Answer
*Written here when the user answers, and this ticket sets `Status: resolved`.*
