# 07-01 — a 검사 and a 늑대 notice each other

Ticket: 07-01
Name: a 검사 and a 늑대 notice each other
Task: 07 — bodies notice each other and give chase
Type: task
Status: resolved
Blocked by: —

## What this ticket builds

**Two bodies that come near each other stop walking past.** A 검사 who is standing names the nearest
늑대 within **3.0 조각** as his target, and a 늑대 names the nearest 검사 within **6.0 조각** and walks
at that 검사 instead of at the 성채. ⚠⚠ **Nothing new gets hit**: both blow gates stay at reach
**1.75**, so what this ticket changes is what a body is AIMING at, not what it can damage.

> **The user watched a 검사 and a 늑대 pass within two 조각 of each other and walk on, 2026-09-02.**
> ⚠ **No quotation of that moment was recorded.** What the log holds from that day is the decision it
> produced: *"they would chase a little bit; the wolf does not run away."*
> 「약간만 쫒아갈듯 늑대가 도망가지는 않음」

⚠ **The 검사 still does not move here.** Ticket 07-02 gives him the chase and the leash.

### What is true today, measured in code

| What | Number today |
|---|---|
| **검사's detect cell** | `NO_DETECT` — **-1.0**, with a comment saying the player's bodies are commanded, not triggered |
| **늑대's detect cell** | **6.0**, and it has sat there through the side swap |
| **Readers of the detect column** | ⚠⚠ **zero.** `detect_of` was deleted 2026-08-29 and the accessor header still says it stays deleted |
| **검사 reach · 늑대 reach** | **1.75 both** — range column 0 plus `REACH_BONUS` |
| **What target choice reads** | **`army.reach_of(i)` — 1.75**, the same number the blow itself reads |
| **검사 speed · 늑대 speed** | **3.2 · 4.0** |
| **A 검사 with no order** | **finishes the 조각 he reserved and stands.** The chase site is a comment |
| **A 늑대 with `TARGET_NONE`** | walks the flow field at the 성채, and at nothing else |
| **A 늑대 with ANY other target** | ⚠ **finishes the 조각 it reserved and then stands** — it stops walking at the 성채 too |
| **성채 HP** | **120** — fifteen seconds of ONE undefended boat of **4 늑대**, and boats come every **30 s** |

⇒ **Two bodies 2 조각 apart are both outside 1.75 and both hold `TARGET_NONE`.** ⚠ **The 늑대 walks on
at the 성채; the 검사 walks on only under an order and otherwise stands.** That is the thing the user
watched.

### 검사 detect — **3.0 조각**, a new number

**A 칸 is 2 조각, so its diagonal is 2.828, and 3.0 is that rounded up.** A 검사 notices anything
standing on a 칸 that touches his own, **and nothing further**. The order has spoken in 칸 since
2026-09-01, so *one 칸 out* is the smallest radius that answers the complaint.

- **It is half the 늑대's 6.0, so the hunter always notices first** — the asymmetry the unit table
  already writes down
- **3.0 < 8.06**, the gap the existing out-of-reach control stands its pair at, so that fixture's three
  rows stay green and this number costs the suite nothing
- ⚠⚠ **`detect_of` must NOT add `REACH_BONUS`.** `reach_of` is the ONE place that sum is written;
  mirroring it makes the 늑대's detection **7.75** with nothing on screen or in the table saying so
- ⚠ **Distance is 3D and a 층 is 1.0 조각 of height**, so at 3.0 a 검사 on the 눈금 2 plateau notices a
  늑대 2.83 조각 away on the flat below. **Noticing across a height is allowed by this ticket** —
  ticket 02-01 already left body-against-body strikes across that gap in scope for a later round, and
  the walk that would refuse it belongs to 07-02
  > ⚠ **Repair note, 2026-09-02, after the HELD answers.** Noticing across a height stays allowed.
  > **Striking across it is closed by this ticket**: HELD question 2 was answered and the body blow gets
  > `can_strike` — see 「Answered 2026-09-02」 below. The walk is still 07-02's

### 늑대 detect — **6.0, and it stands unchanged**

**It is the only detect number any design in this repo ever wrote.** Changing it in the same edit that
gives it its first reader would make the result unattributable — the same argument the unit table
already makes about the beasts' other columns.

⚠⚠ **Say out loud what 6.0 means: it is THREE 칸.** Every 늑대 within three 칸 of a 검사 diverts off
the 성채, and **that has never been played.** The 성채's 120 HP was sized against **undefended** boats,
so these two numbers are coupled — a wave that never reaches the house is the failure this radius can
produce, and it is judged on screen, not in the table.

### ⚠⚠ The beast's half is ONE edit and cannot be cut smaller

**Today ANY non-`TARGET_NONE` target stops a 늑대's walk.** So widening its target choice alone
**freezes every 늑대 six 조각 short of the 검사 it just noticed** — and it stops walking at the 성채 at
the same time, which is a board-wide freeze.

⇒ **Widening the target choice and changing the movement gate are one edit**, and **the game does not
run between the two halves.** A 늑대 whose target is a 검사 must walk at that 검사; only `TARGET_KEEP`
and a body already inside 1.75 keep the stand-still branch.

⚠ **The two prose blocks this reverses are rewritten, never deleted** — the unit table's *"NO_DETECT
because the player's bodies are commanded, not triggered"* and the movement phase's *"the beasts walk
at the 성채 and at nothing else … there is no detect radius in this path"*.

## Screen

**No new art. Nothing is drawn that was not drawn before** — no Blender, no pixel tool. What changes is
**which way an existing sprite faces** and **where an existing body walks**.

1. **A 검사 who is standing turns to FACE the 늑대 he noticed, up to 3.0 조각 away.** This arrives free:
   the still-body branch already points the head at whatever the target column names, and the sprite
   flips through the yaw so the turn reads at every camera angle.
   ⚠⚠ **Whether that is the "noticed you" picture wanted is decided by eye in this ticket.** The other
   shape is to leave the facing reading 1.75 while the target column carries 3.0. ~~**It is one line
   either way, and it is the first thing on screen the user can judge.**~~
   > ⚠ **Repair note, 2026-09-02 (adversary).** **It is not one line either way.** The facing is read by
   > the view off the target column and off nothing else, so the 3.0 facing costs **zero** lines — and
   > the 1.75 shape costs **a guard in the view**, in a file this plan did not list and no net measures.
   > The plan below names the file and the condition under which it is touched. **It is still the first
   > thing on screen the user can judge.**
2. **A 늑대 that used to walk past a defender turns and comes at him.** What is watched is whether the
   wave still reaches the 성채 at all.
   > ⚠⚠ **Measured 2026-09-02, on the shipped island, before the build.** With the four 검사 standing at
   > the door where the run stands them, **the first wave never reaches the 성채 and never fights**: the
   > 늑대 climb the 계단, notice the watch from the plateau's edge, and the field to the door leads back
   > down the 계단 — so they turn round, lose sight at 6.0, turn back, and pace. **Today the same board
   > is lost at 38.98 s** with the watch never lifting a hand. Both are in the plan below as one row
   > (**[watch]**) and one screenshot, and **that screenshot is what the user judges this line on.**

⚠ **No mark on the ground says what a body has noticed.** A ring, a highlight, a line — none of that is
in this ticket, and any of it is made in a tool and loaded.

## Done when

- **`Rules.detect_of(Rules.SWORDSMAN)` is 3.0 and `Rules.detect_of(Rules.WOLF)` is 6.0.** ⚠ **6.0 and
  not 7.75** — that is the row that catches the reach bonus being added a second time
- **The complaint, measured.** A 검사 and a 늑대 placed **2.236 조각** apart — the 검사 at (2,2), the
  늑대 at (3,4), one 조각 across and two down — with no 성채 on the board. ⚠ **2.5 was written here first
  and is not a gap two tile centres can have**: bodies snap to integer centres, so a gap is
  `sqrt(dx² + dy²)` over integers and 6.25 is not a sum of two squares. **2.236 sits above reach 1.75 and
  below both detect radii**, so it is the one gap the change is about (2.828 = (2,2) is the other; 3.0 =
  (3,0) sits ON the 검사's radius, where `EPS` puts it inside, and is avoided). Today after 5 s both are at
  full HP (**18.0** and **14.0**) and `soldier_target[0] == -1`. **After this ticket neither is at full
  HP** — they fought. ⚠ Do not assert the 늑대's death at 5 s: the 검사's third blow lands at about
  **4.9 s**, a margin of one tenth of a second
- **Aiming is not hitting.** From the same placement, after ONE sub-step (**1/60 s**) **both target
  columns name each other and both HP are still full.** ⚠ The 늑대 covers at most **0.067 조각** in that
  sub-step (speed **4.0**), the 검사 does not move, so the pair is **no closer than 2.17**. Reach is 1.75
- **The out-of-reach control stays green** — the existing pair at **8.06 조각**: both HP unchanged after
  5 s and `soldier_target[0] == -1`. ⚠ 3.0 and 6.0 both sit under 8.06, and all three of its rows fall
  together the day either exceeds it, so **that fixture's gap is re-derived from the detect number**
  rather than left standing as a coincidence
- **A new control beside the beasts-walk-at-the-성채 row.** That row survives only because **no 검사
  is ever ashore on it** — it builds with `ISLE_MUSTER`, but the automatic muster was deleted on
  2026-09-02 and the row calls neither `place_ashore` nor `recruit`, so the four starting 검사 stay
  RESERVE through its 8 s + 3 s. It never meets this change. **The new one puts a 검사 inside 6.0 of a
  landed 늑대 and watches the gap between them close over three seconds**: on the landlocked board,
  the 늑대 at (2,2) (3.61 from the 성채, so today it walks there) and the 검사 at (2,7), **5.0 apart**.
  After one sub-step the 늑대's target names him; after 3 s the gap is **inside 1.75 + 0.067** and
  `soldier_hp[0]` is below full, while `keep_hp` stays at **120** throughout. ⚠ **「Its position moves
  on the next step」 was the row's first draft and is green for the freeze** — a 늑대 whose walk is
  gated off still glides out the 조각 it reserved, at most 1.414. A gap that closes by more than 3 and
  a 검사 who was hit are what a glide-out cannot produce. Without this row, 「beasts walk at the 성채」
  and 「beasts notice a defender」 are one untested pair
- **A screenshot beside this file**: a 늑대 off its line to the 성채, and a 검사 turned to face it. **That
  is what says the facing question was answered**
- **Two pieces of prose go false with no red to announce it** and are rewritten in this ticket: the
  island net's recorded reason a path jam is not a stall (*"soldiers carry `Rules.NO_DETECT`"*), and the
  tier net's warning that a wide detect radius turns its climb fixture into a chase measurement — **that
  fixture is safe only while its enemy list stays empty**
- **Added 2026-09-02, after the HELD answers.** **A 늑대 already in reach of the 성채 keeps hitting it
  while a 검사 stands 3.0 away** — `_at_the_keep`'s 늑대 at (4,4) with a 검사 on the doorstep (7,4):
  `enemy_target` is `TARGET_KEEP` at one sub-step and at 5 s, the 성채 has taken three blows (**108**),
  the 검사 is at full HP and has not been walked at. **The two loss rows stay green untouched**
- **Added 2026-09-02, after the HELD answers.** **A blow between bodies crosses no more 눈금 than a body
  climbs, in both directions** — a 늑대 on 눈금 0 under a 검사 on 눈금 2, **1.414** apart, lands no blow
  in 5 s and takes none; the same pair with the 늑대 on the 계단 (눈금 1), **1.118** apart, both bleed
- **Added 2026-09-02.** **A 늑대 whose target it cannot strike walks to the stop distance and stands
  there** — on the flat, in reach, target still named, no blow either way, not one 조각 further in 5 s
- **Added 2026-09-02, after the adversary's second pass.** **The shipped island is measured, not
  argued** — the four 검사 stood at the door as the run stands them, the first boat landed, thirty
  seconds read: every 늑대 named a 검사 at least once, `keep_hp` is still **120**, the run is not lost,
  and nobody on either side has lost a point of HP. **Today: no 늑대 ever names a 검사 and the run is
  lost at 38.98 s.** The row is **[watch]** and it is the observation the ticket's own 6.0 warning
  never had
- **Added 2026-09-02.** **Two screenshots, each with one question the user answers** — see the plan's
  Acceptance for what is in each and what answer closes it. ⚠ **A screenshot alone closes nothing**;
  the user's words on it, written into the Answer section, are what closes

⚠ **Not done when the 검사 moves.** He does not walk in this ticket.

⚠ **An ordered 부대 aims too** — the targeting phase carries no order guard, so a 검사 crossing the
board names what he passes. **He still cannot hit past 1.75 and he does not stop.**

⚠⚠ **Whether an order is interruptible is already ANSWERED and it is 07-02's build, not an open
question** (2026-09-02, the user: *"it stops and fights, and when it is over it carries on where it was
going."*). **This ticket only widens what an ordered 검사 AIMS at.**

## ⚠⚠ HELD 2026-09-02 — the plan is written and the build is STOPPED

**`adversary` read the plan against the code and found two things the user decides.** ⚠ **The plan below
stands and is not withdrawn** — everything in it that is buildable is still buildable. **What stops it is
that the 늑대's half, as planned, takes the loss condition off the board.**

### ⚠⚠ 1. The island stops being losable, and the row that measures it goes red

**Measured from the fixtures' own constants, not run.** `net_fight`'s keep board musters a 검사 at
**(7,4)** on the 20-second muster clock, and its 늑대 stands at **(4,4)** — **3.0 조각 apart on flat
ground, inside both new radii.**

- The 늑대 lands ten blows before t = 20 s, taking the 성채 from **120 to 80**
- Then the 검사 appears, and **검사-first ordering takes `TARGET_KEEP` away from the 늑대**
- The blow gate refuses at 3.0 > 1.75, the board is landlocked so no second 늑대 ever comes, and the
  duel goes to the 검사 (18 HP / 5.0 dmg against 14 / 4.0)

⇒ **The 성채 stops burning at 80 and the run is never lost.** Two green rows go red —
**`_the_keep_burns_and_the_run_is_lost`** and **`_a_lost_run_stops_the_clock`** — and the first is
**the only place in the repo that measures that a run can be lost at all.**

⚠⚠ **This is not a fixture to be patched. It is the rule being asked for, showing what it costs.**

> ⚠ **Repair note, 2026-09-02 (the model, reading the code after this block was written).** The arithmetic
> above stands on `MUSTER_PERIOD_SEC`, and **that constant was deleted the same day** (the user: 「자동
> 병사 생성 지워줘」 — *"delete the automatic soldier spawning"*). Today `_at_the_keep` stands **no 검사
> at all** — `recruit` is the only door and nothing in `src/` or in that fixture calls it — so **neither
> burn row goes red under the plan.** The question is unchanged and still the user's; what changed is
> that **no existing row measures it.** See the Acceptance section below.

**The question, and it is one sentence:** ***when a 늑대 is already close enough to hit the 성채, does a
검사 three 조각 away pull it off?*** **Yes** is what the plan wrote — and it means a single defender
standing near the house stops the wave, which is 티켓 07-02's leash question arriving one ticket early.
**No** — the 성채 keeps a 늑대 that can already reach it, and only a 늑대 still walking notices a
defender — keeps the loss condition and is one clause in the targeting order.

⚠ **The ticket's own Acceptance names the wrong tripwire.** It says
`_the_beasts_walk_at_the_keep_and_stop_in_reach` going red means the edit reached too far. **That row is
safe** (it steps 11 s against a 20 s muster clock, a 9-second margin) and **the two rows that do go red
are not named anywhere.** The two reasons the ticket gives for that row being safe — 「muster tile −1」
and 「a 10 s climb step」 — belong to a different fixture and are both false of it.

> ⚠ **Repair note, 2026-09-02.** The row IS safe, but the 「9-second margin」 is also of the deleted clock:
> that fixture builds with `ISLE_MUSTER` and steps 8 s + 3 s, and **nothing musters on it now or ever
> did by hand.** The corrected tripwire is in the Acceptance section below.

### ⚠⚠ 2. A 늑대 beats a 검사 on the plateau without using the 계단

**The body-against-body blow has no `can_strike` guard — only the 성채 got one.** On 눈금 2, the height
term makes `_dist` **1.414** straight and **1.732** diagonal, and **both are under reach 1.75.**
`_walk`'s stop test is the same distance, so **a chasing 늑대 stops exactly in that band and stands
there swinging upward.**

⚠⚠ **The gap is real today and unreachable in play, because a beast only ever walked at the 성채. The
chase is what walks it there** — every time a defender is posted high.

⇒ **This reverses what 티켓 02-01 bought.** That ticket closed 2 주 on 「눈금 2 is safe ground」 and put
this exact gap in its Out of scope with a reason — 「its Done when names the 성채 alone」 — **that stops
being true here.**

### ⚠ Three more the plan gets wrong, and these are the model's to fix, not the user's

- **`2.5 조각` is not attainable.** Both bodies snap to tile centres, so a gap is `sqrt(dx²+dy²)` over
  integers and **6.25 is not a sum of two squares.** The neighbours are **2.236** and **2.828**; either
  sits above 1.75 and below 3.0, and the plan's 「no closer than 2.43」 arithmetic is written off a number
  the fixture cannot hold
- **The movement row cannot see the movement half.** A frozen 늑대 **still glides out the 조각 it already
  reserved**, so 「its position moves on the next step」 is green for the freeze. What a glide-out cannot
  produce is **the gap shrinking across several seconds**, or `soldier_hp` falling
- **The flow field, not the scan, is the cost.** The plan says the scan 「does not change shape」 — true and
  beside the point. `_walk` needs **a field to the target's 조각**, and a field is **3.72 ms** on the real
  island with a 0.5 s cache keyed by tile. **A 검사 walking at 3.2 조각/s invalidates his entry every
  0.31 s** — nine of them is about **29 builds a second**. ⚠ Nothing in the acceptance rows would see it:
  the frame-rate row measures determinism, not time

### Answered 2026-09-02

**Both questions were put to the user with a recommendation on each, and the user took both**:

> 「추천댜로 ㅇㅇ」 — *"As recommended, yes."* (2026-09-02, the user)

⚠ **Nothing above this heading is withdrawn.** The two blocks stay as the record of what the build was
stopped on; what follows is what they became.

**1. A 늑대 that can already hit the 성채 is NOT pulled off by a 검사 three 조각 away.** A 늑대 whose
성채 gap is within `reach_of` keeps `TARGET_KEEP`. **Only a 늑대 still walking** — the 성채 not yet in its
reach — **names the nearest 검사 within its 6.0 detect** and walks at him. ⇒ **The loss condition stays
on the board**, and `_the_keep_burns_and_the_run_is_lost` and `_a_lost_run_stops_the_clock` stay green
**untouched**. ⚠ **What this does not change**: a 검사 who steps INSIDE a 늑대's 1.75 reach outranks the
wall today, and still does — the answer is about a defender **three 조각 off**, not one at arm's length.
**A 검사 in reach → the 검사; the 성채 in reach → the 성채; a 검사 in detect → the 검사; else nothing.**

**2. A 늑대 on the flat CANNOT hit a 검사 standing on 눈금 2 without using the 계단.** The
body-against-body blow gets the same height guard the 성채 blow got in 02-01 — `Grid.can_strike`, the
눈금 gap against `Rules.MAX_CLIMB_LEVELS` — **in both directions**. `can_strike` is written as an absolute
gap, so a 검사 above cannot hit down either, and `net_fight` already holds that row for the 성채
(「위에서 아래로도 못 때린다」). ⇒ **02-01's 「눈금 2 is safe ground」 survives the chase.**

**Everything else the ticket decided stands**: 검사 detect **3.0**, 늑대 detect **6.0 raw** and never plus
`REACH_BONUS`, the 검사 does not walk (07-02), the target choice and the movement gate change in one
edit, and the nets are written first.

## Implementation plan

⚠ **Repaired 2026-09-02** — the three defects the HELD block marked as the model's are fixed in place
below (an attainable gap of 2.236 instead of 2.5, a defender row that measures the gap closing and HP
falling rather than one step of position, and the flow-field cost named in Risk with a row that counts
builds). ~~**The plan still waits on the two HELD questions and nothing is built.**~~
⚠ **Revised 2026-09-02, after the answers** — the 늑대's target order and its movement gate obey answer 1,
the body blow gets answer 2's height guard, and three rows measure them (marked **[A]**, **[B]** and
**[stands]** below). **Nothing is built yet; the nets come first.**
⚠ **Revised again 2026-09-02, after the adversary's second pass** — two findings, both taken: **the
shipped board is now a row (`[watch]`) and a screenshot** instead of a warning, and **the facing's
other shape is priced honestly** (a view guard, in a named file, taken only if the user's eye says
no). **The rest of the plan is unchanged.**

⚠⚠ **This is `sim` work, so the nets are written FIRST** (CLAUDE.md's folder rule: what measures
input → state survives; what asserts pixels does not). **No new seam.** Everything below is measured at
`src/sim/`, which is constructible with `.new()` and never touches the tree.

### Seams

**One, and it already exists**: `src/sim/` driven by `Battle.new()` + `Army.new()` inside
`tests/nets/net_fight.gd`. `net_fight`'s own `_battle_on` helper builds the board; nothing new is needed.
**The shipped board is the same seam**: `net_fight._real()` already loads the island file into a
`Grid`, and `Battle.stand_at_keep` is the exact call `Run._stand_the_watch` makes — so the opening the
player sees is drivable headless without touching `Run`.
⚠ **No net is written at `src/view/`** — the facing change arrives through the still-body branch that
already reads the target column, and that branch is judged by eye, not by a net. **That stays true on
the 1.75 branch too**: a guard on which target the facing reads is a view fact, and the view's
measuring surface does not reach a body's heading.

### Files to touch and why

| File | Why |
|---|---|
| `tests/nets/net_fight.gd` | **Written first.** The rows below — the three the plan always had, the build-count row, the three the answers added (**[A]** · **[B]** · **[stands]**), and the shipped-board row (**[watch]**) — plus re-deriving the 8.06 fixture's gap from the detect number instead of leaving it a coincidence |
| `src/sim/rules.gd` | `detect_of` comes back as an accessor over the existing detect column; the 검사 row's cell goes `NO_DETECT` → `3.0`; two prose blocks are rewritten |
| `src/sim/battle.gd` | `_phase_targeting`, `_phase_movement` and the body blow in `_phase_attacks` change **in one edit** — the target order and the movement gate for answer 1, `Grid.can_strike` on the body blow for answer 2; two prose blocks are rewritten. ⚠ **`grid.gd` is not touched**: `can_strike` already exists and already refuses both directions |
| `tests/nets/net_islands.gd` | Its recorded reason a path jam is not a stall cites `NO_DETECT` on 검사. **That sentence goes false with no red** |
| `tests/nets/net_tiers.gd` | Its climb fixture's guard says a wide detect radius would turn the climb into a chase measurement. **Rewrite it to say the fixture is safe only while its enemy list is empty** — which is true, and is why it stays green |
| `src/view/field_view.gd` — **conditional** | ⚠ **Touched ONLY if the user's eye says the 3.0 facing is wrong.** The body rows in `_fx_step` hand `_aim_of` the target column and nothing else, so the 1.75 shape is **one guard at that call**: aim at the target only when it is inside `reach_of`, measured the way `Battle._dist` measures (planar plus the level height). ⚠ **Not `_aim_of` itself** — it also answers the 성채 case, and a guard inside it would have to know both sides' reach. **No net**: the heading is not on the view's measuring surface, and this is the one edit in the plan that is judged by eye and by nothing else. ⚠ **This ticket was scoped to `src/sim/` and three nets** — this row is the one place it can leave that, and it is written here so that leaving it is a decision the user made on a screenshot and not a builder's drift |

⚠ **No new per-body column.** `soldier_target` and `enemy_target` already exist and are already reset in
`setup`, appended in `recruit` and `land_beast`, and cleared in `_phase_deaths`. **Nothing is added to
`net_fight`'s column-length guard.**

### Order

**1. The nets, before any `src/` edit.**

- **`detect_of` returns the table's number** — `Rules.detect_of(Rules.SWORDSMAN)` is `3.0` and
  `Rules.detect_of(Rules.WOLF)` is `6.0`. ⚠ **The 6.0 row is the one that catches the reach bonus being
  added a second time**: `detect_of` reads the column raw and never touches `REACH_BONUS`
- **The complaint** — a 검사 at (2,2) and a 늑대 at (3,4) on `ISLE`, **2.236 조각** apart, board built
  with **no 성채** (empty keep tiles, muster `-1`). ⚠ **Not 2.5** — tile centres are integers and
  `sqrt(dx² + dy²)` cannot make 2.5; the attainable gaps between 1.75 and 3.0 are 2.0, 2.236 and 2.828,
  and 2.236 is the nearest to the 「two 조각」 the user watched. Today: after 5 s both sit at full HP
  (18.0 and 14.0) and `soldier_target[0] == -1`. **After: neither is at full HP.** ⚠ At this gap the 늑대
  is in reach inside 0.2 s and the 검사's third blow (period 2.4) lands at about 4.9 s — so the row reads
  HP, never `enemy_alive`, at 5 s
- **Aiming is not hitting** — the same placement, **one sub-step** (1/60 s). Both target columns name
  each other and **both HP are still full.** The 늑대 covers at most 0.067 조각 at speed 4.0 and the 검사
  does not move, so the pair is **no closer than 2.17** and reach is 1.75
- **A defender pulls a landed 늑대 off its line** — `LAND` (landlocked, so no boat lands mid-row) with
  the 성채 at `ISLE_KEEP` and the doorstep at `ISLE_MUSTER`; `land_beast` at (2,2), which is 3.61 from
  the nearest 성채 조각 (outside 1.75, so today it walks at the house and is hitting it by 0.5 s);
  `place_ashore` the 검사 at (2,7), **5.0 from the 늑대** — inside 6.0, outside 3.0, and off the 늑대's
  line to the house. Measured over three seconds, not one step:
  - after one sub-step, `enemy_target[0] == 0`
  - after 3 s, `_dist(enemy_pos[0], soldier_pos[0]) <= reach_of(WOLF) + speed_of(WOLF) / 60` — the
    gap closed from 5.0 by more than 3 조각
  - after 3 s, `soldier_hp[0] < max_hp_of(0)` — the 늑대 arrived at about 0.8 s and hit him
  - `keep_hp == 120` at both reads. ⚠ **This is the discriminating read against today's code**, which
    has the 늑대 at the wall by 0.5 s and the 성채 at 112 by 3 s
  ⚠⚠ **The first draft of this row read 「its position moves on the next step」, and that is green for
  the freeze**: a 늑대 whose target is set but whose walk is gated off still glides out the 조각 it
  reserved, up to 1.414. **A gap shrinking by three and a 검사 with less than full HP are what a
  glide-out cannot produce.** ⚠ **Without this row 「beasts walk at the 성채」 and 「beasts notice a
  defender」 are one untested pair**
- **The 8.06 control keeps its meaning** — the existing out-of-reach pair stays green, and its gap is
  re-derived in the net from `maxf(detect_of(SWORDSMAN), detect_of(WOLF))` rather than left as a bare
  literal, so the day either number passes 8.06 the fixture says so instead of going quietly wrong
- **[A] A 늑대 at the wall keeps the wall** — `_a_wolf_at_the_wall_keeps_the_wall`. `_at_the_keep()` as
  it stands (`LAND`, the 늑대 at (4,4), **1.0** from the 성채's (5,4)), then `place_ashore(0, ISLE_MUSTER)`
  — the doorstep (7,4), **3.0** from the 늑대 on flat ground, the HELD block's own arithmetic. Three reads:
  - **the wall holds its 늑대.** After one sub-step `enemy_target[0] == TARGET_KEEP` and `keep_hp` is
    116; after 5 s still `TARGET_KEEP`, `keep_hp` is **108** within `HP_NEAR` (three blows at 0, 2 and
    4 s — the burn row's own arithmetic over 5 s), `soldier_hp[0]` is full, and `enemy_pos[0]` has not
    moved by more than `NEAR`. `soldier_target[0] == 0` — the 검사 at 3.0 AIMS (`EPS` puts 3.0 inside) and
    cannot hit; that read is what says the 검사's half is live on this board and the 늑대 still chose the
    wall. ⚠ **This is the tripwire the HELD block said did not exist.** Written 검사-first, this row goes
    red on its first line
  - **the wall takes a 늑대 walking past it.** The same board with the 늑대 landed at (2,4) instead —
    **3.0** from (5,4), outside reach, **5.0** from the doorstep, inside 6.0. After one sub-step
    `enemy_target[0] == 0` (it noticed the man and set off); after 3 s `enemy_target[0] == TARGET_KEEP`,
    `keep_hp < KEEP_MAX_HP` and `soldier_hp[0]` is full — the house sits between them and the
    wall-in-reach clause caught it on the way. ⚠ **This is the instrument's inversion**: an order written
    「성채 only when no 검사 is seen」 is green on the first read and red here
  - **a 검사 at arm's length still outranks the wall, exactly as today.** The 늑대 at (4,4) and the 검사
    placed at (4,3), **1.0** apart and 1.414 from the 성채. After 3 s `enemy_target[0] == 0`,
    `soldier_hp[0] < max_hp_of(0)` and `keep_hp == KEEP_MAX_HP` — read at 3 s and not 5, because the
    검사's third blow kills the 늑대 at 4.8 s. **Without this read, answer 1 could be built as 「the
    wall beats everything」 and nothing would say so**
- **[B] A blow between bodies crosses no more 눈금 than a body climbs** —
  `_a_blow_between_bodies_crosses_no_more_notches_than_a_body_climbs`. The `PLATEAU` grid with **no
  성채** (`_battle_on(g, PackedInt32Array(), -1)`), so its four 눈금 2 조각 are free ground and
  `place_ashore(0, tile(3,2))` stands the 검사 on 눈금 2 — `_free_tiles_from` seeds at the wish's own
  level, so he lands exactly there. `land_beast(WOLF, tile(3,1))` puts the 늑대 on 눈금 0 directly
  north: planar 1.0, height 1.0, **3D 1.414 — under 1.75 both ways.** Reads:
  - after one sub-step **both target columns name each other** (1.414 is inside 3.0 and inside 6.0) and
    both HP are full
  - after 5 s **both HP are still full** — `soldier_hp[0] == 18.0`, `enemy_hp[0] == 14.0` — both still
    name each other, and neither position moved by more than `NEAR`. ⚠ **「0 is ready」 is what makes 5 s
    a real read**: without the guard the first blow lands on the first sub-step, so full HP at 5 s
    means the guard refused every one of about 150 chances
  - **symmetric, and the row says so**: `can_strike` is `absi(level gap) <= MAX_CLIMB_LEVELS`, so the
    검사 above lands none either — `enemy_hp[0]` full is that read. The 성채 row 「위에서 아래로도 못
    때린다」 already pins the predicate; this row pins that the body blow READS it
  - **the floor under it**: the 검사 at (3,3) and the 늑대 on the 계단 tread (2,3), 눈금 1, planar 1.0,
    **3D 1.118**. After 5 s **both are below full HP.** ⚠ **A guard written 「same 눈금 only」 is green
    on the first three reads and red here**, and that is the reach-from-a-계단 rule 02-01 refused to
    give up
- **[stands] A 늑대 whose target it cannot strike walks to the stop distance and stands** —
  `_a_wolf_that_cannot_strike_its_target_walks_up_and_stands`. Same board as **[B]**: the 검사 at (3,2)
  on 눈금 2, the 늑대 landed at **(3,0)** — planar 2.0, height 1.0, **3D 2.236**: outside 1.75, inside 6.0.
  Derived on the field, not guessed: the field from (3,2) reaches the flat only through the 계단 at
  (2,3), so in step costs (2,3) is 14, (2,2) is 24, (2,1) is 34, (3,1) is 38, (2,0) is 44 and (3,0) is
  48 — **(3,0) has three cheaper neighbours and `step_toward` takes the cheapest, (2,1)** — which is
  **1.732** from the 검사, inside 1.75. So the 늑대 walks one diagonal and the stop test ends the walk
  on the flat; the 계단 is two field steps further and it never gets there. ⚠ **「only cheaper
  neighbour」 stood here and was wrong** — the conclusion holds on the ranking, not on uniqueness. Reads:
  - after one sub-step `enemy_target[0] == 0`
  - after 1 s: `enemy_pos[0]` is more than **1.0** from (3,0) (the walk happened — a glide-out from
    `land_beast` is zero, so this is the floor that proves it walked), `_dist(enemy_pos[0],
    soldier_pos[0]) <= reach_of(WOLF) + NEAR`, and `grid.level_of(_tile_of(enemy_pos[0])) == 0` — **on the
    flat, not on the 계단 and not on the plateau**
  - after 5 s: `enemy_pos[0]` equals its 1 s position within `NEAR` (it stood), `enemy_target[0] == 0`
    still, `soldier_target[0] == 0` (1.732 is inside 3.0 — he aims down and cannot hit either), and
    **both HP are full**. ⚠ **The level read is the discriminating one**: a 늑대 that reached the 계단
    would be at 눈금 1, 1.5 from him, and would land a blow — which is the 07-02 chase arriving early,
    not this ticket
  - ⚠ **This row is also the measurement of the Risk line 「a path to something unreachable」** — a
    field to a 눈금 2 조각 is asked for by a body on 눈금 0 and the body neither spins nor stalls
- **[watch] The shipped island's first wave meets the watch at the door** —
  `_the_shipped_island_s_first_wave_meets_the_watch_at_the_door`. ⚠⚠ **This is the row the adversary
  said was missing, and it is the only row in the plan on the board the player actually plays.** Every
  other fixture is one 검사 and one 늑대 on a hand-drawn rectangle; the run stands **four** 검사 at the
  door and lands **four** 늑대 every 30 s. **Measured 2026-09-02 on today's code, headless, before a
  line was written** — every number below is read off that run, not derived:
  - **The board.** `_battle_on(_real(), Islands.keep_tiles(), Islands.beside_home_tile(g.w))`, then
    `stand_at_keep(i)` for the four — **the exact calls `Run.begin_island` and `_stand_the_watch`
    make.** Self-checks: the doorstep is (10,14) on **눈금 0** south of the 성채, the four stand three on
    (10,14) and one on (9,14), the 성채's four 조각 are all **눈금 2**, and the only 계단 is
    (6,6)·(7,6)·(6,7)·(7,7) — **north-west of the house, on the far side from the door**
  - **The landing, unchanged by this ticket.** Stepped one sub-step at a time: the first boat comes to
    the west beach and the four 늑대 stand at (4,9)·(4,9)·(4,9)·(4,8) at **22.75 s**. ⚠ The nearest 검사
    is then **7.07** away, outside 6.0, so nothing about the crossing or the landing can move — the row
    asserts four landed and `enemy_type.size() == BOAT_CAPACITY` as a self-check
  - **What today's code does next, measured**: the 늑대 climb the 계단, cross the plateau, come down the
    spine to (10,11)·(11,11) — **1.0 from the wall on 눈금 2** — and burn it at 8 HP a second from 24 s.
    **`keep_hp` is 104 at 26 s and the run is LOST at 38.98 s.** The four 검사 at the door never target
    anything (`soldier_target` is -1 on every read: the 늑대 on the spine are **3.16** from the door in
    3D, outside 1.75) and never lose a point. **02-01's own closing note recorded the same fall at
    38.8 s by eye.**
  - **Where the change bites, measured**: each 늑대 first comes inside 6.0 of a 검사 at **24.10 s at
    (8.16, 8.16), on the plateau, one 조각 past the top of the 계단**, gap 5.99 to the 검사 on (9,14),
    with the wall **4.26** away — outside reach, so answer 1's wall tier does not hold it. **Derived
    from there on the field, and this is the part the build confirms or reddens**: the field to (9,14)
    from the plateau leads back down the 계단 — the door is on 눈금 0 and the 계단 is the only way
    down — so the 늑대 walks back to (8,8), where the 검사 is **6.16** away and out of detect; the
    target drops, it walks at the 성채 again to (9,9), sees him at 5.1, turns back. **It paces between
    (8,8) and (9,9)** — 1.414 each way, at 4.0 조각/s. **No plateau 조각 outside the door's 6.0 band
    leads to the spine** ((8,9) is 5.2 away, (10,10) is 4.2), so **no 늑대 ever reaches (10,11)** and
    the wall tier is never entered
  - **Reads at landing + 30 s** (about 52.75 s), stepped one sub-step at a time with a per-늑대 flag
    set the first sub-step `enemy_target[e] >= 0`:
    - **every one of the four flagged** — they noticed the watch. ⚠ **This is the discriminating read
      against today**, where the flag is never set on any of them
    - **`keep_hp == KEEP_MAX_HP` and `not lost`** — the wave did not reach the house. ⚠ **Today's code
      is lost at landing + 16.2 s**, so the read is red today and green after — **and the label says
      what it is**: the wave that burned past the watch does not reach it, and it does not because
      it went after the watch. **This is the sentence the ticket's 6.0 warning wrote as a risk, and it
      is what the shipped board does**
    - **all four 늑대 alive at 14.0 and all four 검사 at 18.0** — nobody fought. The door is on 눈금 0
      and the pacing is on 눈금 2, 5.1 or more apart: no reach, no blow
    - **`keep_gap(enemy_pos[e]) > reach_of(WOLF)` for all four** — none is at the wall
    - **positions and 눈금 printed as a label, never asserted**: 「e0 (8.0,8.0) L2 …」. The pacing
      band is derived above, not measured, and a row that pinned the exact 조각 would go red on a
      reservation jam between four bodies on two 조각 while measuring nothing the reads above missed
  - ⚠⚠ **What this row is and is not.** It is **the measurement of Screen item 2** — 「whether the wave
    still reaches the 성채 at all」 — on the one board that matters, and its reads are all facts the
    decided rules guarantee. **It is not a claim that the picture is right.** Four 늑대 pacing at the
    plateau's edge while the 성채 stands untouched and four 검사 look up from the door is **the 07-02
    leash question arriving on the opening frame**, and whether it is acceptable for the days between
    this ticket and that one is the user's, off the screenshot in Acceptance. ⚠ **The row is rewritten
    the day the rule that produces the pacing changes** — it is named for what it measures so that
    rewrite is a rewrite and not a patch

**2. `rules.gd` — the number and the reader.** `detect_of` is added beside `reach_of`, reading
`_COL_DETECT` and returning it unchanged. The 검사 row's detect cell becomes `3.0`. ⚠ **`NO_DETECT`
stays declared** — it is the answer for a body that has none, and the constant's own header already says
so; nothing carries it after this ticket, and that is not a reason to delete it.

**3. `battle.gd` — `_phase_targeting`, `_phase_movement` and the body blow in `_phase_attacks`, one
edit.** ⚠⚠ **The game does not run
between these two halves**: widening the target choice alone freezes every 늑대 six 조각 short of what
it just noticed AND stops it walking at the 성채, which is a board-wide freeze.

- ~~**Target choice** — the 늑대's order is: nearest 검사 within its detect → that 검사; else 성채 if the
  keep gap is within `reach_of` → `TARGET_KEEP`; else `TARGET_NONE`. The 검사-first ordering is the whole
  behaviour change.~~ ⚠ **Rewritten 2026-09-02 for answer 1.** The choice now has **two tiers, and the
  reach tier is today's rule unchanged**:
  - **the 늑대**: **a 검사 inside `reach_of` that it can strike** → that 검사 (today's first clause, plus
    the guard); **else the 성채 inside `reach_of`** through `keep_gap` → `TARGET_KEEP` (today's second
    clause, untouched); **else — new — the nearest 검사 inside `detect_of`** → that 검사; **else**
    `TARGET_NONE`. ⚠ **The detect tier sits BELOW the wall**, which is the whole of answer 1: a 늑대 that
    can hit the house keeps hitting it, and a defender three 조각 off is seen only by a 늑대 still walking
  - **the 검사**: **a 늑대 inside `reach_of` that he can strike** → that 늑대; **else the nearest 늑대
    inside `detect_of`** → that 늑대 (aim only); **else** `TARGET_NONE`
  - **「that it can strike」 is `Grid.can_strike` on the two bodies' 조각**, in the reach tier ONLY. That
    is 02-01's shape — `keep_gap` carries the guard so choosing and landing cannot disagree — brought
    to the body blow. ⚠ **Without it in the reach tier, a body aims at the nearer thing it cannot hit
    and never swings at the further thing it can**: a 검사 on the plateau with a 늑대 below at 1.414
    and one on the 계단 at 1.5 would stand aiming down forever. The detect tier does NOT carry it — a
    body it cannot strike is still a body it notices, walks at and faces (answer 2's [stands] row)
  - **One helper, `_can_hit(a, b, reach)`** = `grid.can_strike(_tile_of(a), _tile_of(b)) and _dist(a, b)
    <= reach + EPS`, read by the reach tier of both scans **and by both body blows** below. ⚠ **One
    place**, for the reason `reach_of` is one place — two copies of the strike rule drift. `_nearest_enemy`
    and `_nearest_soldier` take a `must_strike` flag rather than growing a twin each
- **The movement gate** — today the beast's stand-still branch swallows **any** non-`TARGET_NONE`.
  After: it swallows `TARGET_KEEP`, and a body-target **only when that body is already inside
  `reach_of`** — strikable or not, which is what makes [stands] stand. A 늑대 whose target is a 검사
  further off walks at that 검사: the same `_walk` call the 성채 path uses, `field_to(_tile_of(
  soldier_pos[who]))` for the field, the 검사's position as `stop_at` and the same `reach_of` stop
  distance. ⚠ **A 늑대 walking at a 검사 whose route passes the house stops at the house** — the next
  targeting pass finds the wall in reach and the wall outranks the detect tier. That is answer 1's
  consequence, it is measured by [A]'s second read, and it is not a defect
- ⚠⚠ **`anchor < 0` is no longer sufficient on its own.** Today a board with no 성채 freezes every
  늑대; the complaint row above is exactly that board, so the no-anchor case must still walk when a
  검사 is targeted. **This is the half a careless edit drops, and the 2.236 조각 row is what catches it**
- **The body blow, same edit** — in `_phase_attacks`, both body-against-body blows go through
  `_can_hit` instead of the bare `_dist` test: the 검사's blow on a 늑대 and the 늑대's blow on a 검사.
  **The 성채 blow is untouched** — `keep_gap` already carries `can_strike` from 02-01. ⚠⚠ **Why the
  guard lands in the same edit as the chase and not a later one**: the plateau gap is unreachable in
  play today because a beast only ever walked at the 성채; **the chase is what walks it there**, so a
  commit with the chase and without the guard is a commit where 눈금 2 is not safe ground

**4. The prose.** Four blocks are **rewritten in place, never deleted** — the repo's rule and this
ticket's own line:

- `rules.gd`'s 검사 row comment, *"`NO_DETECT` because the player's bodies are commanded, not
  triggered"* — reversed 2026-09-02 by the user off the screen (「너무 그냥 지나감 얼추 비슷하면
  싸워야하는데」)
- `rules.gd`'s accessor header, *"`detect_of` stays deleted — the detection radius has no reader"*
- `battle.gd`'s *"THE BEASTS WALK AT THE 성채 AND AT NOTHING ELSE … there is no detect radius in this
  path"*
- `battle.gd`'s deleted-reach block, whose last line — *"a beast walks at the 성채 and never at a body,
  so nothing can ask the flow field for a path to something unreachable"* — **is the one that turns into
  a live risk rather than a stale sentence.** See Risk

### Risk

- ⚠⚠ **A path to something unreachable.** The deleted-reach tombstone says nothing could ask the flow
  field for a path to a body, because a beast only ever walked at the 성채. **That is no longer true.**
  A 늑대 on the beach can now target a 검사 standing on a 눈금 it cannot climb to. `_walk` must fall
  back the way it already does when the next 조각 is blocked; **if it instead spins or stalls, that is
  this ticket's defect and not 07-02's.** ⚠ **Measured by the [stands] row since 2026-09-02**, not
  argued — a field to a 눈금 2 조각 asked for from 눈금 0, and the body ends on the flat and still
- **Distance is 3D.** `_dist` adds the height difference, so 3.0 and 6.0 see across a storey — a 검사 on
  the plateau and a 늑대 below, 2.0 apart on the flat, are 2.236 apart to this code (a 층 is 1.0 조각 of
  height — `TIER_STEP_TILES` is 0.5 a 눈금). ⚠ **That is the same function the blow already uses**, so
  the two agree; it is written down here because it is a rule nobody chose and someone will read it as
  a bug. ⚠ **2.06 stood here and was wrong** — it was a 눈금 1 height, not a 층
- **What answer 2 leaves standing on screen.** A 늑대 that noticed a 검사 on the plateau walks to
  1.732 on the flat below and **stands there aiming up with nothing landing** — and the 검사 aims down
  the same way. **That is the picture until 07-02 gives the walk a reason to go round by the 계단**, and
  it is a body standing still, not a body swinging at air: the view draws a blow off the cooldown
  rising, and a refused blow never touches the cooldown. ⚠ **Whether a standing stare reads as a
  freeze is judged by eye**, beside the facing question this ticket already carries
- ⚠⚠ **The flow field is the cost, not the scan.** `_nearest_soldier` already scans the whole roster
  each sub-step, so the scan's shape does not change — **and that is beside the point.** `_walk` needs
  a field to its target's 조각, and `field_to` caches one field per target 조각 for `FIELD_TTL` = 0.5 s.
  What the code says, number by number:
  - **one field is 3.72 ms** on the shipped island (48 x 32, 1536 조각 — measured, and written on
    `field_to` and on `Hand.routes`)
  - **today every 늑대 shares ONE key**, the 성채's anchor 조각: at most 2 builds a second, **7.4 ms per
    simulated second**
  - **after, each targeted 검사's 조각 is its own key.** A standing 검사 costs 2 builds a second (the TTL);
    a 검사 walking at 3.2 조각/s crosses a 조각 every **0.3125 s** straight (0.44 s diagonal), so his
    chaser builds about **3.2 a second**
  - **nine 검사 walking, each chased, is about 29 builds a second — 107 ms per simulated second**, and
    a build lands whole inside one frame: nine 검사 crossing a 조각 edge in the same frame is **33 ms in
    a 16.7 ms frame**
  - ⚠ **Nothing in the rows as first written would see it** — the frame-rate row measures determinism,
    not time. The build-count row in Acceptance is what sees it
- ⚠ **A field to a 조각 the beast cannot reach is `UNREACHABLE` everywhere but the seed.** `_next_goal`
  then finds no cheaper neighbour and `_walk` returns `here` — the body stands, which is the queue at a
  neck and not a spin. The Risk row above (「a path to something unreachable」) is measured, not argued,
  by the plateau case once HELD question 2 is answered
- **The 검사's facing is a free rider.** The still-body branch points the head at whatever the target
  column names, so a 검사 turns to face a 늑대 3.0 away the moment this lands, with no view edit.
  ⚠⚠ **Whether that reads right is the eye judgement in this ticket** — ~~and it is one line either
  way~~ **and the other shape is a view guard, priced in the Files table, taken only on a 「no」**
- ⚠⚠ **The shipped opening cannot be lost until the watch moves, and nobody fights.** Measured before
  the build (the **[watch]** row): today four idle 검사 at the door lose the run at 38.98 s; after,
  the four 늑대 notice them from the plateau's edge, the only way to the door is back down the 계단,
  and they pace between (8,8) and (9,9) with the 성채 whole and both sides at full HP. **The ticket's
  6.0 warning said this could happen; it happens on the first boat of every run.** ⚠ **It is not this
  ticket's to fix inside its decisions** — 6.0 stands, the 검사 does not walk, and a 늑대's leash is
  07-02's open question — **so it is measured, screenshotted, and put in front of the user rather than
  argued about.** A player who orders the watch up the 계단 gets a fight on the plateau; the row and
  the shot are about the opening with no order given, which is the first 25 seconds of every run
- **The field keys on the shipped board.** With the watch standing still there are three keys — the
  anchor and the two door 조각 — at 2 builds a second each, **22 ms per simulated second**. The
  pacing adds none: a 늑대's key is the 검사's 조각, and the 검사 does not move. The build-count row's
  arithmetic is the walking case; this is the resting one

### Acceptance

- The new and changed rows above are green — the three the plan always had, the build-count row,
  **[A]**, **[B]**, **[stands]** and **[watch]** — and **the whole suite is 1567 passed · 4 failed or
  better**: that is this round's measured baseline, and the four reds all predate it
- ⚠⚠ **The tripwire, corrected 2026-09-02.** `net_fight` today holds **no row** that stands a 검사 within
  6.0 of a 늑대 already in reach of the 성채 — `_at_the_keep` (the two burn rows) and
  `_the_beasts_walk_at_the_keep_and_stop_in_reach` build with `ISLE_MUSTER` but call neither
  `place_ashore` nor `recruit`, and the twenty-second muster clock the HELD arithmetic used was deleted
  the same day. **So `_the_keep_burns_and_the_run_is_lost`, `_a_lost_run_stops_the_clock` and the
  beasts-walk row all stay green whichever way HELD question 1 is answered, and none of them is a
  tripwire.** ⚠ **That is the gap, not a comfort**: the only row that measures 「a 늑대 already at the
  wall」 against a defender is one that does not exist yet, and it is written the day the user answers.
  > **The day came, 2026-09-02 — that row is [A].** `_the_keep_burns_and_the_run_is_lost` and
  > `_a_lost_run_stops_the_clock` **stay green and are not edited**; [A] is the tripwire beside them, and
  > it is the row that goes red the day the detect tier is moved above the wall.
  Of the rows that DO stand a 검사 near a 늑대, the ones this edit touches and that must stay green are
  `_a_dead_swordsman_stands_again_at_the_keep` (the revived 검사 stands 3.5 from the parked 늑대 with
  0.2 s left in the row — too far to be hit) and `_the_fight_is_the_same_at_any_frame_rate` (the 검사
  at (4,3) is what a landed 늑대 walks at once inside 6.0 of him; fine and coarse must still agree)
- **[A] measured**: on `_at_the_keep` with a 검사 on the doorstep 3.0 away, `enemy_target[0]` is
  `TARGET_KEEP` at one sub-step and at 5 s, `keep_hp` is **108** at 5 s, the 검사 is at full HP and the
  늑대 has not moved; the same 늑대 landed at (2,4) names the 검사 first and is `TARGET_KEEP` with the
  성채 bleeding by 3 s; a 검사 at 1.0 from the 늑대 at the wall is hit and the 성채 is not
- **[B] measured**: 늑대 on 눈금 0 under a 검사 on 눈금 2, 3D **1.414**, both target columns naming each
  other after one sub-step, **both HP full at 5 s, neither moved** — **symmetric**: `can_strike` is an
  absolute gap, so the 검사 above lands none either, and `enemy_hp[0] == 14.0` at 5 s is that read. The
  floor: the same pair with the 늑대 on the 계단 at **1.118**, both below full HP at 5 s
- **[stands] measured**: 늑대 landed at (3,0) under the 검사 at (3,2) walks more than 1.0, ends at 눈금
  0 inside 1.75 by 1 s, and is at the same spot at 5 s with both HP full and both still aiming. **The
  level read is the one that separates 「stands on the flat」 from 「went round by the 계단」**
- ⚠ **Rows around a fixed defect are suspects, not controls** (`how-nets-lie`). After the edit the
  builder re-runs `net_fight` whole and reads every row that stands a 검사 near a 늑대, not only the
  three above — a row that reddens was measuring the bare `_dist` blow, and one that stays green has
  been re-verified
- **The build-count row, measured in code.** On `_real()` with the 성채 and doorstep the island file
  gives: `place_ashore` one 검사 and `land_beast` one 늑대 5 조각 from him, `order_walk` the 검사 six
  조각 straight, then step **one sub-step at a time for 3 s** and, each sub-step, count the keys of
  `b._fields` that were not there the sub-step before, and the set of distinct keys seen. **The first
  count is the number of fields built** — a key lives at least 0.5 s, so sampling at 1/60 s misses
  none. ⚠ **Two keys exist on this board today, not one**: `_phase_orders` asks `field_to(dest)` every
  sub-step while the 검사 walks (1.9 s at 3.2, so about 4 builds on the TTL) and every 늑대 asks for the
  성채's anchor 조각 (6 or 7 builds in 3 s). **Today: 2 distinct keys, 10 or 11 builds.** After, the
  늑대's key is `_tile_of(soldier_pos[who])` and follows the 검사: **one new key per 조각 he crosses**
  (6 straight), plus the TTL on the 조각 he stops on — **about 8 distinct keys, 13 to 15 builds.** The
  distinct-key count is the discriminating read (2 against 8); the build count is the bound that
  catches a key written off the 검사's position instead of his 조각, which would build **sixty times a
  second**. The row prints `Time.get_ticks_usec()` around one `grid.flow_field` on the same grid as a
  label — **reported, never asserted**: a timing assert is flaky across machines and the counts are
  the deterministic half. ⚠ **Nine 검사 each ordered and each chased is the same arithmetic times nine
  — about 120 builds in 3 s, 40 a second, 150 ms per simulated second, of which the nine order keys
  are a cost the 검사 already carry today** — and that is the Risk line above, not a row: this row measures the shape with one pair so its numbers can be read by hand
- **[watch] measured** — on `_real()` with the four 검사 stood by `stand_at_keep` and the first boat
  landed at 22.75 s: at landing + 30 s **every 늑대 has named a 검사 at least once** (today: none, ever),
  **`keep_hp` is 120 and the run is not lost** (today: lost at 38.98 s), **all eight bodies are at full
  HP**, and **no 늑대 is within reach of the wall**; positions and 눈금 are printed, never asserted.
  ⚠ **The label on the wall read says what it measures**: the wave that burned past the watch now goes
  after the watch and never arrives. **A builder who finds this row red on the 「not lost」 line has
  found a 늑대 that reached the spine, and that is the derivation above being wrong, not the row** —
  report it with the position trace, do not move the 검사 or the number
- ⚠⚠ **Two screenshots beside this file, and what the user is shown with each.** A screenshot is a
  picture; **the acceptance is the user's sentence about it, written into the Answer section word for
  word** (the repo's rule on the user's own words about a game). ⚠ **Each is shown with ONE line and
  no recommendation** — the ticket's own reply rule:
  - **`07-01-noticed.png` — the facing.** The shipped island, one 검사 ordered from the door to
    **(6,11)** before the first boat lands (he arrives in about 1.6 s; the boat takes 22.75). The 늑대
    come ashore at (4,9), **2.83** from him — inside 3.0 and inside 6.0, outside 1.75 — so on the
    landing sub-step he turns to face them and they turn off the 계단 line at him. **What the user is
    shown**: that frame, and 「검사가 세 조각에서 고개를 돌린 것 — 이게 알아챈 그림 맞아?」 (*"the 검사
    turned his head at three 조각 — is that the noticing picture?"*). **What closes it**: **yes** closes
    the facing question as built; **no** takes the conditional view guard in the Files table (the head
    turns only inside 1.75, the target column still carries 3.0), the frame is retaken, and the 「no」
    and the retake both go into the Answer. ⚠ **No third shape is offered**
  - **`07-01-shipped-first-wave.png` — the wave.** The shipped island with **no order given**, at about
    30 s: four 늑대 on the plateau's west edge, the 성채 whole, four 검사 at the door. **What the user is
    shown**: that frame, the number beside it — 「오늘은 39 초에 지고, 이 뒤로는 안 진다: 늑대 넷이 고원
    끝에서 왔다갔다 하고 성채는 안 맞는다」 (*"today the run is lost at 39 s; after this it is not: four
    늑대 pace at the plateau's edge and the 성채 takes nothing"*) — and 「이대로 닫아도 돼?」 (*"can it close
    like this?"*). **What closes it**: **either answer, written down.** **Yes** closes the ticket with the
    picture as the opening's known shape until 07-02. **No** still closes THIS ticket — what it built is
    what was decided — and the 「no」 goes verbatim onto **07-02's first line as its leash question**,
    with the [watch] row named as the one that gets rewritten. ⚠ **What does not close it**: the model
    deciding that pacing is fine, or the model changing 6.0, the watch's 조각, or the movement gate to
    make the picture nicer. **Three numbers were decided by the user and none of them is this
    ticket's to move**

### Out of scope

- **The 검사 does not walk.** No chase, no leash — that is 07-02
- **Nothing is drawn.** No ring, no highlight, no line saying what a body noticed
- ~~**Neither blow gate moves.** Both stay at reach 1.75~~ ⚠ **Rewritten 2026-09-02 for answer 2**:
  **neither reach number moves** — both stay 1.75 — but **the body blow gains the 눈금 guard**. What is
  out of scope is the number, not the gate
- **A 늑대 that cannot strike its target does not go round by the 계단.** It walks to the stop distance
  and stands ([stands]). **Choosing the 계단 over the shortest line is the chase, and the chase is
  07-02's** — as is whether the 검사 above comes down to meet it
- **No new unit, no new column, no new constant** beyond the accessor. `_can_hit` and the `must_strike`
  flag are readers of constants that exist, not constants
- ⚠ **Whether a 늑대 wears a leash too is 07-02's open question and is not answered here** — **and
  the pacing the [watch] row measures on the shipped opening is that question's first evidence, not
  this ticket's defect.** A 늑대 that turns back because the field to the body it noticed leads away
  from that body is doing exactly what 「walk at the 검사」 with no leash says. Making it hold, give up,
  or go round is 07-02
- **The 1.75 facing shape is not built unless the user's eye asks for it.** It is priced (one view
  guard, in a named file), it is not measured (the heading is off the view's surface), and it is taken
  on a 「no」 to the noticing screenshot and on nothing else. **Until that 「no」 the view is not
  touched and this ticket stays inside `src/sim/` and three nets**

## Answer

**Built and verified 2026-09-02. Both sides notice each other; nothing new gets hit; the 검사 does not
walk.** `Status: resolved` — with one line held open for the user's eye, named at the end of this section.

### What was built

- **`Rules.detect_of` is back**, reading the unit table's detect column raw. The 검사's cell went
  `NO_DETECT` → **3.0**; the 늑대's stays **6.0**. The accessor never adds `REACH_BONUS`, so the 늑대's
  detection is 6.0 and not 7.75 — a mutant that added it went red on the shipped-board row itself
- **Target choice has two tiers, and the reach tier is today's rule unchanged.** The 늑대: a 검사 inside
  1.75 that it can strike → that 검사; else the 성채 inside 1.75 → the 성채; else the nearest 검사
  inside 6.0 → that 검사; else nothing. The 검사: a 늑대 inside 1.75 that he can strike → that 늑대;
  else the nearest 늑대 inside 3.0 → aim only
- **The movement gate walks a 늑대 at the 검사 it named.** Before, ANY target other than 「nothing」
  froze the beast; now only the 성채 in reach, or a body already inside 1.75, holds it still. A board
  with no 성채 no longer freezes every 늑대 — the 2.236 complaint row is exactly that board
- **One helper, `_can_hit`**, is read by both scans' reach tier and by both body blows: the level gap
  test `Grid.can_strike` plus the 1.75 distance. **The 성채 blow is untouched** — it already carried the
  guard from 02-01
- **Four prose blocks rewritten in place** — the unit table's 「commanded, not triggered」 comment, the
  accessor header that said `detect_of` stays deleted, the movement phase's 「walk at the 성채 and at
  nothing else」, and the deleted-reach tombstone; plus the island net's `NO_DETECT` reason and the tier
  net's climb-fixture warning. One comment block added on `_phase_targeting`'s header saying why reach
  is per body and detect is per type

### The two decisions, as answered by the user (「추천댜로 ㅇㅇ」 — *"as recommended, yes"*)

- **(A) A 늑대 already close enough to hit the 성채 is NOT pulled off by a 검사 three 조각 away.** The
  detect tier sits BELOW the wall tier. Measured by the **[A]** row on the landlocked keep board: the
  늑대 at 1.0 from the house holds `TARGET_KEEP` with a 검사 standing at 3.0 (the 검사 aims, cannot hit,
  the 성채 is at 108 by 5 s); a 늑대 landed at 3.0 from the wall names the 검사 first and is caught by the
  wall on its way past; a 검사 at arm's length still outranks the wall, as today. **The two loss rows
  stay green and were not edited.** A fourth read stands a 늑대 at (7,5) so that a gate letting
  `TARGET_KEEP` fall through to the anchor walk goes red — that read is the only one that catches it
- **(B) A blow between bodies crosses no more 눈금 than a body climbs, in both directions.** Measured
  by the **[B]** row: a 늑대 on 눈금 0 under a 검사 on 눈금 2, 3D 1.414 — inside reach — lands nothing and
  takes nothing in 5 s, about 150 refused chances; the same pair with the 늑대 on the 계단 tread at
  1.118 both bleed. And on the real island file: a 검사 at (8,4) on 눈금 2, a 늑대 at (8,3) below, HP
  18.0 / 14.0 unchanged for five seconds, both naming each other the whole time. **02-01's 「눈금 2 is
  safe ground」 survives the chase**

### The nets, before and after

| Net | Before | After |
|---|---|---|
| `net_fight` | **419 / 0** at the start of the bounce round (the plan's rows already in) | **446 / 0** — 30 checks added, 3 tautologies removed; the file is pure addition against `main` |
| `net_islands` | 21 / 0 | **21 / 0** (prose rewrite only) |
| `net_tiers` | 110 / 0 | **110 / 0** (prose rewrite only) |
| whole suite | **1567 / 4** when the plan was written | **2668 / 4**, 18 nets — the same four reds, none 07-01's |

- **The four reds are the same four on `main`** with this ticket's two sim files swapped back to
  `main`'s content: `net_boats` (7 beaches where no hull can stand), `net_fx_view` twice (the 늑대's
  16 px and its walk texture), `net_shell` once (「몸마다 자리·색·크기」, 4 against 0 — possibly ticket
  03-11's, which owns that file and was building in the same tree). `net_camera` aborts one function and
  `net_wake` barks `!is_inside_tree()` on both trees too
- **Eleven mutants, each applied alone and restored with the file hashes equal, all went red**:
  `REACH_BONUS` added to `detect_of` (440/6), `can_strike` dropped from `_can_hit` (429/17), the detect
  tier moved above the wall (434/12), `TARGET_KEEP` allowed to fall through to the anchor walk (445/1),
  the 검사's detect tier deleted (441/5), the 늑대's detect tier made strike-only (435/11 — and it
  reproduces today's lost board exactly), the gate re-frozen on a keepless board (441/5), `must_strike`
  dropped from the 검사's reach tier (443/3), `must_strike` off on both scans (440/6), the in-reach stand
  gate made unconditional (444/2), and `elif anchor >= 0` (445/1)

### What the headless verifier measured, on the shipped island file

- **detect 검사 3.00 · 늑대 6.00, reach 1.75 both** — printed from `Rules`
- **The opening with nobody ashore is unchanged**: the first boat lands at 22.75 s, the four 늑대 climb
  the 계단, reach the wall by 25.77 s, and the run is **lost at 38.98 s** — the loss condition is still on
  the board when no 검사 stands
- **Four 검사 stood 3.0 from the landing**: within one second every 늑대 names a 검사 and closes to
  contact; `keep_hp` stays 120 through the whole beach fight; three 늑대 die; the survivor loses its
  target and goes back to the house at 42.77 s and burns it
- **The 검사 still does not walk**: one 검사 sat at (4,12) for the whole 30 s while three 늑대 came to
  him and died on him

### What the screen verifier saw, in a real window

- **`07-01-noticed.png`** (beside this file). The 검사 was walked from the door to (5,11) and then one
  step EAST to (6,11) on purpose, so his resting heading was east. Nine sub-steps after the first boat
  lands: he names e0 at a gap of 2.16, all four 늑대 name him (2.16 and 2.95), everybody at full HP,
  `keep_hp` 120 — aiming, not fighting, under 3.0 and over 1.75. The frame 0.25 s earlier
  (`tools/shot/out/t0701/01_before_the_landing.png`) shows him with the eyes-visible sprite of his last
  eastward step; in the saved frame he wears the back sprite. **The only thing that changed between the
  two frames is the target column.** The four 늑대 wear the 「south」 picture — muzzle to the camera —
  which is the heading they got walking south-east at him, not the east profile a body that had not
  turned would wear
- **`07-01-shipped-first-wave.png`** (beside this file, copied from `tools/shot/out/t0701/
  03_shipped_first_wave.png`). No order given, landing + 29.5 s: the four 늑대 stand on the 눈금 2
  plateau north of the house, every one naming a 검사; the four 검사 are still at the door on 눈금 0;
  the 성채 is whole at 120, not lost; all eight bodies at full HP; no 늑대 within 1.75 of the wall.
  **Today's code loses the same board at 38.98 s with no 늑대 ever naming a 검사** — the [watch] row
  and this frame measured the ticket's own 6.0 warning, and it happens on the first boat of every run
- **The instrument is kept**: `tools/look/capture_0701.gd` took both frames through `root.push_input`
  and the shell's own `_process`, no OS capture, and shot the title as its known-answer frame first

### Not done, and why

- **The 검사 does not walk.** The soldier half of the movement phase is unchanged; the chase and the
  leash are **07-02**
- **A 늑대 that cannot strike its target stands at the stop distance** — it does not go round by the
  계단. Choosing the 계단 is the chase, and the chase is 07-02
- **The 1.75 facing shape was not built.** The view is untouched (empty diff) — it is taken only on a
  「no」 to the noticing frame
- **Nothing is drawn.** No ring, no highlight, no line saying what a body noticed
- **Two minor latencies, reported not fixed**: the [watch] row reads at about 52.75 s and the second
  boat launches at 35 s, so a coast or speed change that lands the second wave a second earlier reddens
  that row under the wrong label; and the build-count row asserts `>= 5` distinct keys where the plan
  derived about 8 — it separates 2 from 8, but loosely

### ⚠ The eye judgement is still the user's

**No sentence from the user is written here yet**, and the ticket's own rule says a screenshot alone
closes nothing. The screen verifier flagged the noticing frame as **ambiguous**: the 늑대 stand
north-west of the 검사, four-direction quantisation turns north-west into 「up」, and the man's back
sprite is the only one of his four pictures that is distinct — so the turn reads as 「he turned away
from the camera」 rather than as a face. The question the ticket wrote, and it is one line:

> 「검사가 세 조각에서 고개를 돌린 것 — 이게 알아챈 그림 맞아?」 — *"the 검사 turned his head at three
> 조각 — is that the noticing picture?"*

**Yes** leaves everything as built. **No** takes the one view guard priced in the Files table (the head
turns only inside 1.75, the target column still carries 3.0) and the frame is retaken. The second frame's
question — 「이대로 닫아도 돼?」 (*"can it close like this?"*), four 늑대 pacing at the plateau's edge with
the 성채 untouched — is likewise unanswered; a 「no」 to it goes verbatim onto 07-02's first line as its
leash question, with the [watch] row named as the one that gets rewritten. **Neither answer reopens this
ticket: what it built is what was decided.**

### Acceptance — the user played it and said nothing about the eye question

**The user ran the game after this landed. Their only words were about what happens AFTER noticing**:

> 「근데 알아채고 와서 싸워야하는데 이게 그냥 보고 기나가네 이게 붙어서 싸워야하는거 아님?」
> *"But it should notice, come over and fight — this one just looks and walks past. Shouldn't it close in
> and fight?"*

**That sentence is 07-02's, and it is written there.** On this ticket's own two questions — the 검사
turning his head, and the four 늑대 pacing at the plateau's edge — **they said nothing.** Silence is a row,
not a pass:

| What shipped | How to see it | Landed |
|---|---|---|
| **A 검사 turns to face a 늑대 within 3.0 조각; a walking 늑대 diverts onto a 검사 within 6.0** | Start a run. Before the first boat lands (about 23 s), send one 검사 toward the north-west beach and leave him there. When the boat lands, **he turns toward the 늑대 and the 늑대 come at him** instead of past him to the 성채. The frame `07-01-noticed.png` beside this file is that moment — ⚠ the 늑대 stood north-west, so with four-direction art his back is what shows. **The question is whether that reads as noticing** | 2026-09-02 |
| **Four 늑대 that noticed the door guard stand at the plateau's edge and the 성채 is untouched** | Start a run, give no order, wait 30 s after the first landing. `07-01-shipped-first-wave.png`. **The question is whether that can stand as it is** — the 늑대 not walking down at the 검사 is the chase, 07-02 | 2026-09-02 |
