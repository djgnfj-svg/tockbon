# 08-02 — resource 칸 stand on the generated island

Ticket: 08-02
Name: resource 칸 stand on the generated island
Task: 08 — 섬이 판마다 새로 생성된다
Type: task
Status: open
Blocked by: 01

✅ **THE BOARD HALF IS BUILT AND MEASURED (2026-09-03).** The generator cuts its resource 칸 out of the
walkable board and stands `tree_pine` · `rock` · `ore` props on the hole, and `net_island_gen` asserts the
counts, that every 조각 of one is impassable and dry, and that each has a walkable 조각 beside it — **over
200 seeds, passing.** That closes both of this ticket's net bars. **The convention that makes it a
resource rather than scenery is passability** — the drawn island's 54 props all stand on walkable ground
and stay 풍경.

⚠⚠ **NOBODY HAS SEEN THE THREE KINDS.** Nothing stands a generated island on screen yet — 08-01's other
half. **The last `Done when` bar is untouched.**

## What this ticket builds

**On every generated island, a tree-dense 칸, a rock 칸 and an ore 칸 stand where the seed put them,
each one blocks, and a body can stand on the 조각 next to it.**

### Where it came from

> The user, 2026-09-02, when asked whether trees cluster into one forest or scatter:
> ***"It should go like this — a 칸 thick with trees, a rock 칸, an ore 칸."***
> (「이게 나무가 무성한 칸 돌 칸 철광석 칸 이렇게 행햐할듯」)

⚠⚠ **This replaces what the island holds today.** Today the trees are thirty-one separate pines in one
north-east forest, the rocks are three loose stones, and the ore is one rock on a detached 2x2 block
nobody can walk to. **After this ticket a resource is a 칸** — a 2x2 block with one kind of thing in it —
and the counts are per 칸, not per pine.

## ✅ What is decided (2026-09-02, from 08-01's round)

| What | Value | The user's words |
|---|---|---|
| **Tree 칸 per island** | **1~3** | 「나무 1~3」 |
| **Rock 칸 per island** | **1~3** | 「돌 1~3」 |
| **Ore 칸 per island** | **1~2** | 「철 1~2 로 일단 지정」 |
| **Where** | **On the island**, joined to it | 「섬에 부텅야함」 |
| **Passable?** | **No — it blocks.** Bodies gather from the 조각 beside it | 「막힌다」 |

⚠ **「일단 지정」 — 「set for now」.** The user marked the counts as first values. They change when
gathering (task 05) gives them a cost to be measured against.

## What is NOT in this ticket

- **Taking anything out of a 칸.** Gathering is task 05; this ticket only stands the 칸 and makes it block
- **What a tree-dense 칸 LOOKS like.** The pines are a mesh and the bushes are cards; **how a 칸 of them
  reads from the camera is a 시안 round**, and it is not this ticket's to pick
- **The ore's pick-axe** — 05-06 dissolved on 「the ore is on the island」, and nothing replaces it yet

## Screen

**A generated island with a visibly wooded 칸, a visibly rocky 칸, and a visibly ore-banded 칸**, and a
body ordered onto one of them stops on the 조각 beside it instead.

## Done when

- **A net drives the generator over hundreds of seeds and every board has tree 칸 in 1~3, rock 칸 in
  1~3, ore 칸 in 1~2, all on the walking piece**
- **Every 조각 of a resource 칸 is impassable, and at least one 조각 beside each 칸 is passable** — asserted
  by the same net
- **The user sees the three kinds and can tell them apart**

## Implementation plan

*`plan-into-ticket` writes this section and sets `Status: claimed`.*
