# 03-16 — the board is turned and the order lands slightly wrong

Ticket: 03-16
Name: the board is turned and the order lands slightly wrong
Task: 03 — 부대를 조종한다
Type: task
Status: claimed
Blocked by: —

## What this ticket builds

**A press that lands on the 칸 the player aimed at, at every one of the four turns — not only at 0°.**

⚠⚠ **NOTHING IS MEASURED YET.** What follows is one observation from the user on the game screen and a
list of the places the defect could live. **The first job of this ticket is to reproduce it with a
number**, and no fix is written before that number exists.

### What was observed, 2026-09-02

> The user, on the game screen: ***"There is a bug where movement does not quite work once you have
> turned the angle."*** (「이거 각도 꺾었을때 살짝 이동이 안되는 버그 있음」)

| What | What is known |
|---|---|
| **When** | **After the board has been turned** — Q or E, which arrived the same day in 03-10 |
| **How bad** | **「살짝」 — slightly.** Not 「nothing moves」; the press lands near the aim and not on it |
| **At 0°** | **No complaint.** The gesture was accepted on screen when 03-08 landed |
| **Measured** | **Nothing.** No count, no offset, no direction |

### ⚠⚠ What a first measurement should be

**The round trip, per 조각, at each of the four notches.** `FieldView.tile_to_screen_px(tx, ty)` is the
forward projection and `screen_to_terrain_px` → `world_to_tile` is its inverse; **the two must name the
same 조각.** Sweep every land 조각 that is on the glass, at yaw 0 · 90 · 180 · 270, and count how many
come back as a different 조각 and by how much.

⚠ **A probe that does exactly this was written and thrown away on 2026-09-02 without producing a
number** — it died before its first line of output because `Game`'s children are built in `_ready`, and
a `SceneTree` script that reads `game.field_view` on the frame it adds the node reads `null`. **Pump a
frame first.** That is the whole of what the failed attempt is worth.

### The places it could live — **none of these is accused**

- **`world_to_screen_px` / `screen_to_world_px`** — the pair carries the yaw through `_ground_right()`
  and `_ground_down()`. **Both look turned.** ⚠ A half-tile origin error is invisible at 0° and doubles
  along the diagonal once the axes rotate
- **`screen_to_terrain_px`'s stepped ray** — it walks the height ceiling down in `TERRAIN_PICK_STEPS`
  and answers at the first 조각 whose ground reaches that height. **A step coarse enough to skip a ledge
  moves the answer by a 조각, and which ledge is in the way changes with the turn**
- **`Grid.block_of`** — 조각 → 칸. Turn-blind by construction, so an error here would show at 0° too
- **The sweep** — `_sweep_the_yaw` writes the yaw every frame for 0.22 s. **A press DURING the sweep is
  aimed at an angle that is no longer current by the time it is read**

⚠⚠ **The last one is the cheapest to rule in or out and it is the one this ticket should test first**:
press mid-sweep versus press after it settles.

## Measured, 2026-09-02 — **the number exists, and it is not where the list above looked**

**Three probes, headless, against the real `Game` with two frames pumped**, on the shipped island (284 land
조각, 71 land 칸), at yaw 0 · 90 · 180 · 270 and three zooms. Anchored on `Camera3D.unproject_position`,
never on the view's own round trip, because `how-nets-lie` records that a round trip cancels a defect
present in both halves.

| What | Result |
|---|---|
| **Forward projection vs the engine** | `tile_to_screen_px` and `unproject_position` agree to **0.0003 px** at every yaw, mid-sweep included. **The axis and origin math in `world_to_screen_px` / `_ground_right` / `_ground_down` is right** |
| **조각 round trip** | Misses: yaw 0 → 8 · 90 → 12 · 180 → 8 · 270 → 12, of 284. **Every miss is an L0 조각 hidden behind an L2 cliff** (1.0 of height hides 1.19 조각 of ground at pitch 40°); the pick answered the visible face, which is what the player sees. **Visible 조각 miss 0 at all four yaws.** 90/270 count more only because the 2층 has longer east-west edges |
| **칸 round trip** | 4-6 of 71, the same hidden 칸. **Visible 칸 miss 0** |
| **The sweep** | Mid-sweep presses pick what is drawn that frame, engine and pick agreeing to 0.0003 px. **Rejected** |
| **Real press → order, one body, 68 reachable 칸** | Wrong 칸: 4 · 6 · 4 · 5. All either a hidden 칸 (the 칸 in front is picked) or the 성채 칸 spilling to a neighbour via `_seats` — **03-17's domain**. **No yaw-specific 칸 error** |
| **Picking a BODY** — four 검사 on open L0 ground, pressed at the drawn foot · chest · head | **yaw 0**: 4/4 · 4/4 · 4/4. **yaw 90**: 4/4 · **0/4** · **0/4**. **yaw 180**: 4/4 · **0/4** · **0/4**. **yaw 270**: 4/4 · 4/4 · 4/4 |

### ⚠⚠ The cause — **a half-조각 origin error in `Game._point_at`, invisible at 0°**

`Game._point_at` returns `screen_to_terrain_px(at) / Look.TILE_PX`, and in that unit a 조각's centre is
`(tx + 0.5, ty + 0.5)`. **`soldier_pos` is in the OTHER convention** — `Battle._point_of_tile` is the
integer pair, and `Look.tile_point_px`'s header says the integer IS the 조각 centre. `Hand.body_at`
compares the two against `PICK_BODY_TILES = 0.80`.

⇒ **A press dead on a body's foot already measures 0.71 조각 away, leaving 0.09 of slack.** A press on
the chest or head lands the ground ray 0.44-0.80 조각 further UP THE SCREEN, and screen-up is a world
direction that turns with the yaw: at 0° it is world -y and cancels the +0.5 bias (0.50, picked); at 90°
it is world +x and adds to it (1.07, nothing picked); at 180° it adds on +y; at 270° it cancels on -x.
**So the bias is a defect at every yaw, and a MISS only at 90 and 180** — 「살짝」 and 「각도 꺾었을 때」
both. Subtracting `(0.5, 0.5)` in the probe made it **12/12 at every yaw.**

⚠ **This is the inference step, said out loud**: the user's words fit these numbers, and the user has not
yet confirmed that this is what they saw. Their press on the turned board is the confirmation.

### ⚠ A second defect, yaw-blind and small — **the stepped ray misses the 2층 surface by 0.071 조각**

**Each half of the inverse was then anchored on the engine separately**, so a round trip could not hide
anything: the forward projection against `unproject_position` (0.0007 px), the flat inverse against the
engine ray meeting the plane `y = h` (0.0006 world px, random heights, whole screen), and the terrain
inverse against the engine ray marched 0.01 조각 at a time over the same heights.

| | |
|---|---|
| **The transforms** | Exact at every yaw and every screen point. **Not the 0°-only kind of right** — 229 of 284 조각 lie more than 300 px from centre at ZOOM_MAX |
| **`screen_to_terrain_px`** | **1-2% of screen points name a different 조각 than the engine**, all within 0.13 조각 of an edge, counts alike at 0° (9 · 17) and 90° (9 · 41) — the spread is which cliff edge faces away, not the yaw |
| **Why** | The ray drops in height steps of `ceiling / TERRAIN_PICK_STEPS` = 5.02 / 48 = **0.1046**. The 1층 surface 0.21 sits 0.001 above a step (no bias). **The 2층 surface 1.21 sits 0.0596 above step 37 (1.1504), so every 2층 pick lands 2.84 world px = 0.071 조각 up the screen**, 48 조각 alike, mean = min. The 계단 조각 at L1: 0.098 |
| **What it does** | (a) In the far 0.071-조각 band of a 2층 조각 the neighbour answers. (b) **At a 2층 덩어리's far cliff edge the ray steps clean over the surface and falls to the 1층 land 1.19 조각 behind** — the 「ledge skip」 this ticket's suspect list named, and it is real |
| **On screen** | 2.2 px at survey zoom, 6.3 px at ZOOM_MAX, on a 칸 that is 60-176 px wide. **Present at 0° identically** — so not what the user described, but a defect in the exact function the ticket named |

⚠ **Two things seen on the way, neither this ticket's** — `_clamp_cam` uses the unturned visible
half-extent (640, 560) at every yaw, so the pan stops 80 px early on one axis and late on the other at
90/270, over open sea; and `Game._process` runs before `FieldView._process`, so the hover lags the sweep
by one step (about 6.8°) for 0.22 s.

## Implementation plan

*Written 2026-09-02 by `plan-into-ticket`, from the measurement above.*

### What already stands

`Game._point_at` (`src/shell/game.gd`, the only caller is body selection) · `Hand.body_at` (`src/sim/hand.gd`,
whose header already says 「the shell converts」 — **the contract exists and the shell breaks it**) ·
`Game._tile_at` (hover and orders — **correct**, it rounds down to the 조각) · `Look.tile_point_px`.

### What already died here

The `probe_pick.gd` of 2026-08-25, deleted after finding three camera defects — `how-nets-lie` says to write
the probe again. It was, three times, in the job's tmp folder; **the body-pick one becomes the net below**
so it does not die a second time.

### Which net measures it now

`net_shell` presses 칸, deliberately not 조각, and never a body. **`Hand.body_at` appears in no file under
`tests/` at all** — the survey's first draft said `net_hand` drove it, and `adversary` grepped and found
zero. **Nothing covers the body press. Empty at the defect.**

### ⚠⚠ Attacked, and one thing came out that the fix does not reach — **the pitch**

**Every number above is at the opening pitch of 40°.** The chest/head ray lands `h / tan(pitch)` 조각 past
the feet, 1.19 at 40°; **R and F tilt the board from 20° to 80° in 5° steps**, and **at 20° the factor is
2.75** — the same head press measures 1.0-1.85 조각 from the feet, **outside `PICK_BODY_TILES` at every yaw,
with fix 1 in or out.** The user will tilt down and see the same defect again.

**This is a fork and it is the user's**: the body is picked **on the ground** (today — a radius around the
feet in 조각, so a tall sprite is only pickable at its feet once the board tilts down) or **on the glass**
(a press within some px of where the sprite is drawn, which is how the press reads to the player and does
not depend on the pitch). **The second changes `body_at`'s contract** — the shell would compare screen
points, using the view's forward projection at the body's height — and is more than one line.
✅ **Answered 2026-09-02 by the user: 「몸은 화면에서 잡자」 — *"let us pick the body on the glass."***
⇒ **Fix 1 is no longer the half-조각 line. It is the glass pick**, and `_point_at` goes away with its only
caller. The plan below was rewritten to that answer; the half-조각 finding stays above as the measurement
that found the defect.

### Fix 1 as built — **the press finds the body by where it is DRAWN**

- **`FieldView.body_at_px(at: Vector2) -> int`** — reads the pooled sprites the view itself placed (the
  agreed measuring surface: `Sprite3D.position`, `scale`, `visible`) and returns the soldier id whose drawn
  rectangle — foot to top, the sprite's width — contains the press, **nearest foot on ties, -1 for none.**
  The rectangle is the sprite's own: its world position projected through `world_to_screen_px` at the
  sprite's height, half-extents from the sprite's world size times the pixels-per-조각 at the current zoom.
  No new constant — the sprite's size IS the pick area. ⚠ Only living, ashore 검사; 짐승 are not pickable
  (unchanged)
- **`Game._press_the_island`** calls `field_view.body_at_px(at)` where it called
  `hand.body_at(_point_at(at))`. **`_point_at` is deleted** — that was its only caller
- **`Hand.body_at` and `Look.PICK_BODY_TILES` are deleted** — nothing reads them, and a function nobody
  calls is the repo's own named fake. `Hand.pick(id)` stays: the pick is still a sim fact, only the
  question 「which body is under the finger」 moved to the glass
- **What this buys**: the pitch no longer matters (a press on the chest at 20° is inside the rectangle),
  the yaw no longer matters, and **when 03-17 moves a body onto its lattice seat the pick follows the
  drawing by construction** — no sim function has to agree with the view

### Which green went false here

`how-nets-lie` 2026-08-25: **「a press at screen centre measures nothing」** and **「a round trip cancels a
defect in both halves」**. This net presses drawn sprites off-centre and anchors on the engine.

### Seams

- **`src/shell/`** — `Game._ready()` builds the real wiring; the net feeds `_unhandled_input` a press at
  the engine-unprojected sprite position and reads `hand.ids`. ⚠ **The ticket's 「Done when」 named the
  `src/view/` seam; the defect is in the shell and the code is true** — the net sits at `_ready()`

### Two fixes, and the second is the user's to strike

**Fix 1 — the body press (the yaw-dependent one, the user's).** ~~`_point_at` subtracts half a 조각.~~
**→ The press finds the body on the glass** (the user's answer above; the section 「Fix 1 as built」 is
the spec).

**Fix 2 — the stepped ray (yaw-blind, 0.071 조각).** It is in the function the ticket named as a suspect
and it was measured here; **leaving it out would be cutting scope on my own.** It is listed as its own step
with its own net so the user can strike it in one word. `screen_to_terrain_px` stops answering at a
quantised height. **Between two consecutive heights `h_prev` and `h_cur` the ray's ground point moves
0.125 조각 along `_ground_down`; walk every 조각 that segment crosses, in order, and for each compute the
ray height where it enters and where it leaves that 조각. If the 조각's surface lies between those two
heights, the ray met the top of that 조각 — the answer is the ray at exactly that surface height, and it
is over that 조각 by construction.** If not, the ray cleared it and the walk goes on. `TERRAIN_PICK_STEPS`
stays 48 — the count bounds the work, the per-조각 walk removes the bias. ⚠ Not a re-tune of the count:
96 steps halves the bias and keeps the skip.

⚠⚠ **Two wordings that were tried and are wrong, so builder does not re-try them** (`adversary`,
2026-09-02): **「snap to the found 조각's surface」** moves the point 0.071 조각 further along
`_ground_down`, so a press inside the last 0.071 of a 2층 조각 snaps past its far edge onto the hidden
1층 behind and answers height 1.21 over a 0.21 surface; **「test the 조각 crossed between the last two
steps」** reads as the 조각 newly entered, which at a far cliff edge is the low land — the skip survives.
**The 조각 that owns the surface is the one the ray was over at the HIGHER step**, and at any non-notch
yaw (the 0.22 s sweep) the segment can cross a corner, so **「the 조각」 singular is not enough** — hence
the walk over every crossed 조각.

### Files to touch and why

| File | Why |
|---|---|
| `src/shell/game.gd` | `_press_the_island` asks `field_view.body_at_px(at)`; **`_point_at` deleted** (its only caller) |
| `src/view/field_view.gd` | **`body_at_px`** new — reads the pooled sprites and `world_to_screen_px`; `screen_to_terrain_px` walks the 조각 between two steps — **inside the one function, nothing else** |
| `src/sim/hand.gd` | **`body_at` deleted** — no caller left; a function nobody calls is the repo's named fake |
| `src/look.gd` | **`PICK_BODY_TILES` deleted** — no reader left |
| `tests/nets/net_pick.gd` — **new file, both rows below** | **Row 1** `_a_press_on_a_drawn_body_picks_it_at_every_yaw_and_pitch` — **at yaw 0·90·180·270 AND at pitch 20·40·80**, and a second body two 조각 away is pressed too so the rectangle is shown not to pick a neighbour. Red today: chest/head 0/8 at 90·180 at 40°, everything but the foot at 20°. Otherwise as the next row describes it: four bodies on open L0 ground with no 눈금 ≥ 1 조각 within ±3, pressed at foot · chest · head via `unproject_position` of the pooled sprite, at yaw 0·90·180·270, after the sweep settles, **through the real press path** (`_unhandled_input` press and release → `hand.pick`), reading `hand.ids`. Asserts every press picks its body. ⚠⚠ **The fixture pairing the probe needed and a net written from this row alone would miss**: `game.set_process(false)` so the sim does not walk the hand-placed bodies between press and assert, **and** `field_view.set_process(true)` so the sweep settles and the sprite pool fills; bodies placed by writing `soldier_pos` directly. ⚠ **The probe's 12/12 figure called `body_at` directly and skipped `hand.pick`** — the net does not, and **builder sees it red before code goes in**, and says so |
| same file | **Row 2** `_the_terrain_pick_answers_the_engine_ray_at_every_yaw`: the engine ray marched 0.01 조각 over `_ground_h` against `screen_to_terrain_px` → `world_to_tile`, a 64x36 jittered grid with a pinned seed, yaw 0·90·180·270, survey zoom and ZOOM_MAX. **Asserts zero disagreements more than 0.02 조각 from a 조각 edge, and a mean 2층 bias of 0.00 ± 0.01 조각.** Red at 0.071 today. ⚠ **Not appended to `net_camera`**: that net already aborts mid-way (`_setup_opens_at_the_survey_zoom` feeds a grid-less `Battle` into `setup`, 19 undeclared stderr lines, stamped 「잰 것의 일부」) — a row added there is counted as part of an incomplete net. **Both new rows drive the real `Game` in a fresh file whose count is whole** |

⚠ **The first draft named `net_slots` as the guard for fix 2. It does not exist** — it survives only as
prose in two nets and `how-nets-lie`. The guards are `net_shell`'s 칸 rows and the new `net_pick` row.

### Order

1. **Nets first, red** — both rows in `net_pick.gd`, both seen red and said so
2. `FieldView.body_at_px`; `_press_the_island` calls it; delete `_point_at`, `Hand.body_at`,
   `Look.PICK_BODY_TILES`; run `net_pick`, `net_shell`, `net_hand`
3. `screen_to_terrain_px`; run `net_pick`, `net_shell`, `net_boats`
4. The suite
5. `verify-look`: pick a body by its chest at 90°, then at 20° pitch, order it, watch it go; press the far
   edge of a 2층 칸 and see the 2층 칸 light, not the 1층 behind it

### Risk

- **Two sprites can overlap on the glass** — three 검사 on one 조각 today, nine on a 칸 after 03-17. The
  rule is the nearest foot to the press among the rectangles that contain it, and the net's second body
  two 조각 away shows a non-overlapping neighbour is not picked. **Overlap picking the front-most is the
  same rule the eye uses**
- **The rectangle is the sprite's, so a body mid-sway or mid-hurt-knock is picked where it is drawn** —
  `_body_offset_of` is in the pooled position already. Correct by definition; named so nobody 「fixes」 it
- **Why a failed pick reads as 「이동이 안 된다」**: 03-11 is open, so today a move is a left press with a
  full hand. An empty hand means the next press picks-or-nothing and never moves — the user's words fit
- **The half-조각 finding stays true and is now moot** — `_point_at` is gone. If anything ever converts
  a ground point to `soldier_pos` units again, `Look.tile_point_px`'s header is the convention
- **Fix 2 touches every press and hover on the island** — `net_shell`'s 칸 rows and the new `net_pick`
  row are the guard; 0.071 조각 is below what the 칸 rows assert, so **they should not move**. If one
  does, the walk changed more than the bias
- **The per-조각 walk costs more than one lookup per step** — 2-3 조각 per 0.125 조각 of travel, 48
  steps, per press and per hover frame. Builder measures the hover frame before and after

### Acceptance

- `net_pick`'s body row green at four yaws and three pitches, seen red before the code
- `net_pick`'s terrain row green: 0 disagreements off-edge, 2층 bias 0.00
- Suite at 통과 ≥ 1679 with the same four reds, no new stderr; `body_at` · `_point_at` · `PICK_BODY_TILES`
  have **no live reference** under `src/` or `tests/` — tombstone comments in the repo's 「STOOD HERE AND IT
  IS DELETED」 shape are allowed and are where the names survive (reworded 2026-09-02 after `verify`
  read the original literally and found seven comment hits, zero live)
- **The pick rectangle equals the drawn sprite, width included** — `verify-look` found it 70.7 px wide on a
  33 px body, and `verify` found no press in the net off the sprite's centre line; both went back to
  builder in one batch
- The user turns the board, presses a body on its chest, and it is picked

### Out of scope

- **The hidden-칸 picks** (a 칸 behind a cliff picks the 칸 in front) — that is what the screen shows
- **The 성채 칸 spilling to a neighbour** — 03-17 / 03-14
- **`_clamp_cam`'s unturned extents** and **the one-step hover lag** — no ticket yet, written above
- **`TERRAIN_PICK_STEPS`** — the number does not change (`PICK_BODY_TILES` is deleted, not retuned)
- **Picking 짐승** — the glass pick answers 검사 only, as `body_at` did

## What is NOT in this ticket

- **The seating order inside the 칸.** That is **03-17**
- **The tilt keys and the wheel.** Only the four yaw notches
- **Any change to how the board turns.** 03-10 is closed and the 90° notch stands

## Done when

- **The defect is reproduced as a number** — how many 조각 miss, at which yaw, by how much
  ✅ **Done 2026-09-02**, and the number turned out to be a body-press number, not a 조각 number: chest and
  head presses **0/8 at yaw 90 and 180**, 12/12 at 0 and 270; the 조각 round trip is 0 for every visible
  조각 at all four yaws
- ~~**That number goes to 0 at all four notches**, and the 0° number is unchanged~~ → **The body-press
  number is 12/12 at all four notches at 40° pitch**, and the visible-조각 round trip stays 0. ⚠ Rewritten
  2026-09-02 after `adversary` read the original against the measurement: the 8·12·8·12 조각 misses are
  hidden ground and are not going to 0 — they are what the screen shows
- ~~**A net holds it** at the `src/view/` seam, driving `FieldView` with `.new()`~~ → **A net holds it at
  the `src/shell/` seam**, driving the real `Game._ready()` wiring and the real press path. The defect is
  in the shell; the code is true
- **The user presses on a turned board and says it lands** — ⚠ **the one line still open, 2026-09-02.**
  Everything above it passed `verify` and `verify-look`; this line is the user's

## Answer

**Built, verified twice, and the last word is the user's press.** 2026-09-02, all in this worktree.

### What the defect was

**Not the 판 pick.** Every visible 조각 and 칸 round-trips exactly at all four yaws; the transforms agree
with the engine to 0.0007 px. **The body press measured a ground point against `soldier_pos` in the wrong
unit convention — half a 조각 off — and the miss only showed once the board turned**, because the
chest-press displacement rotates with the yaw and cancelled the bias at 0° and 270°. At 90° and 180° the
chest and head picked **0/8**; at 20° pitch even the foot was the only pickable point at any yaw.

### What was built

| Where | What |
|---|---|
| `FieldView.body_at_px` | **The body is picked on the glass** (the user's answer): the press is tested against the drawn sprite's rectangle — **the ink's width** (first to last opaque texel column, measured once at load for all 248 textures), the canvas's height — nearest foot on overlap, 검사 only |
| `Game._press_the_island` | calls it. **`_point_at`, `Hand.body_at`, `Look.PICK_BODY_TILES` are deleted**, tombstones only |
| `FieldView.screen_to_terrain_px` → `_ground_met` | **Fix 2**: between two height rungs the ray walks every 조각 its ground segment crosses and answers at the exact surface height it meets; a cliff face names the tall 조각 (`FACE_HIT_INSET`). 48 rungs kept, though they no longer move the answer |
| `tests/nets/net_pick.gd` | **New, 600 rows**: presses at foot·chest·head and 1 px inside / 3 px outside the ink edge, four yaws × three pitches, through the real press path; the terrain pick against the engine ray marched 0.01 조각 over a 64x36 lattice, plus every cliff face. **Seen red first**: 64/144 presses missed, 96/96 outside presses picked; 2층 bias -0.0710 |
| `tests/nets/net_shell.gd` | the click row now aims at the drawn foot — its old point was ground 0.58 px under the sprite, which only ever picked because the deleted radius had 0.8 조각 of slack |

### Measured

| What | Number |
|---|---|
| **Suite** | **1679 / 4 → 2280 / 4**, the same four reds (`boats` 1 · `fx_view` 2 · `shell` 1), no new stderr |
| **Body press** | 144/144 at 4 yaws × 3 pitches; outside-edge presses 96/96 pick nobody |
| **Mutations that redden the net** | rectangle back to the canvas → 96 red · `FACE_HIT_INSET = -0.001` → 8 red · face branch off → 16 red · fix 1 reverted → 56 red · fix 2 reverted → bias -0.0710 |
| **Drawn vs pickable** | 33 × 47 px drawn at survey zoom; the pick boundary within 1 px of it left and right; 1·2·3·8·18 px outside → nobody |
| **Hover cost** | 87 → 104 µs per `screen_to_terrain_px` at 40° |
| **Screenshots** | 15 beside this file, `03-16-1` … `03-16-14`; `03-16-12` is the before of the width defect |

### ⚠ What came out on the way and is NOT closed

- **The pick rectangle was first the 72-texel canvas, 2.2× the body** — `verify-look` saw a press 8 px
  beside a body pick it; `verify` had already found no net press off the centre line. One bounce closed both
- **Rectangle height is still the canvas** — a frame with empty rows above the head would over-reach by
  them; no 검사 frame has any today
- **The idle strip moves the ink edge a texel or two every 0.12 s**, so a press exactly on the edge can flip
  across a frame change. By design — the press reads the frame drawn this frame
- **`_clamp_cam`'s unturned extents · the one-step hover lag · the 성채 칸 spill** — recorded above, no
  ticket yet / 03-17 · 03-14
- **`net_camera` still aborts mid-way** (a grid-less `Battle` into `setup`) — pre-existing, why `net_pick`
  is its own file
