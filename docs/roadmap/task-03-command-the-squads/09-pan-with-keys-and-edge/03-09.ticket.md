# 03-09 — the board is panned with WASD and the screen edge

Ticket: 03-09
Name: the board is panned with WASD and the screen edge
Task: 03 — 부대를 조종한다
Type: task
Status: resolved
Blocked by: —

## What this ticket builds

**W, A, S and D pan the 판, and so does the pointer parked against a side of the window.** Both were
deleted on 2026-08-31 and both come back; nothing else about the camera moves in this ticket.

⚠⚠ **THIS TICKET IS THE CAMERA'S REPLACEMENT AND IT LANDS FIRST.** 03-04 takes the left drag for the
selection box, and **the left drag is the only thing moving the camera today.** Build 03-04 before
this and there is a window where the board cannot be panned at all.

### ⚠⚠ It is a resurrection, and the reversal is the user's own

**Both were removed at the user's own request, in one conversation, one after the other:**

> ***"That one too"*** (2026-08-31, the user: 「그것도 지워줘」) — the screen edge, **which had been the
> user's own idea the day before**: ***"rather than WASD, the mouse going to the edge and moving on its
> own seems right"*** (2026-08-30, the user: 「wasd 보다는 마우스가 끝으로 가면 자동으로 이동이 맞을듯」)
> ***"Delete WASD as well"*** (2026-08-31, the user: 「wasd 도 지워줘」) — the keys followed the same day

**They come back because the left drag is being taken away**, and because the user asked what RimWorld
does and was told it pans with the keyboard and the screen edge. ⚠ **This is one reversal of one day's
decision, shared with 03-10** — not two separate ones.

### Where the deleted code actually is

⚠⚠ **The plan file names `3a28f8e9`, and the code says that commit is the DELETION.** `3a28f8e9` is
「WASD 도 지웠다」 — its tree already carries the tombstones. Its parent `d06ff6fd` is
「화면 끝 자동 이동을 지웠다」, so it holds WASD but **not** the band.

⇒ **`b15b09c4` is the one commit that holds both, working, side by side** — including the single
summed `pan_by` call. **Recover from there.**

### What was measured, in code

| What | Number |
|---|---|
| **WASD speed** | **900.0 screen px/sec** — restored unchanged (`Look.CAM_PAN_KEY_PX_PER_SEC`) |
| **⚠ It is already live in a second place** | `tools/look/capture_boat.gd` holds `PAN_PX_PER_SEC := 900.0`, put there *because* the constant went away. **Restore anything else and that tool's pictures fork from the game** |
| **Edge band depth** | **28.0 px** from each side — **2.2%** of the 1280 px viewport and **3.9%** of the 720 px one. ⚠ **The plan calls the inert middle 96% and that is one side's arithmetic.** Banded on all four sides the inert rectangle is **1224 x 664**, which is **88%** of the glass |
| **Edge band speed** | **the keys' own constant**, written as `CAM_PAN_KEY_PX_PER_SEC` and **never as a second 900** |
| **Lip factor** | **0.30** at the inner lip, ramping linearly to **1.0** at the glass |
| **East-west roam, measured in 03-03** | **1120 px of camera travel**, narrower than one screen. ⚠⚠ **That 1120 is WORLD px and 900 is SCREEN px, and dividing one by the other is a unit error.** 03-03's own drag row converts them: a **300** screen px drag moved the board **393.8** world px, so 900 screen px/sec is **1181 world px/sec** and the whole roam crosses in **0.95 sec** |
| **North-south travels further than east-west** | `pan_by` divides the vertical by `sin(cam_pitch_deg)` and divides the horizontal by nothing. The pitch opens at **40°** (`Look.CAM_PITCH_DEG`), where `sin` is **0.6428**, so one held key buys **1.56x** as much ground north-south. ⚠ **R and F change that pitch**, so the ratio is not a constant |
| **A corner of the band** | **two edges, un-normalised — 1.41 times a side**, which matches two keys held and matches a diagonal drag |

⚠⚠ **900 WAS NEVER JUDGED ON A SCREEN** — `look.gd`'s own tombstone says so of all four deleted travel
constants — **and it was chosen while a left drag also panned.** With the drag gone it is one of
only two ways to travel. **It is a first value, not a settled one.**

### ⚠⚠ Two sources, ONE `pan_by` call

`b15b09c4` summed the keys' velocity and the band's into one call and spent it once. **Two calls in
one frame is 1800 px/sec through two clamps**, and a camera already sitting on the roam edge then eats
one of the two inputs with nothing on screen to say so.

### ⚠ The tombstones are REWRITTEN, never deleted

CLAUDE.md: **a reversal is written onto the record, never by erasing it.** These say the opposite of
what is about to be true, and every one of them carries the new line:

| Where | What it says now, and is about to stop being true |
|---|---|
| `game.gd`, at `_pan_keys` | ***"Delete WASD as well"*** 「wasd 도 지워줘」 · "the left-button drag is the only thing that moves the camera now" |
| `game.gd`, the per-frame pan spend | "Both sources are gone ... nothing needs a per-frame call" |
| `game.gd`, the WASD key branch | "TAB is the only two-edged key left" |
| `game.gd`, at `_on_pan_key` | "The drag is now the only caller" |
| `game.gd`, at `_edge_pan_dir` / `_edge_ramp` | ***"That one too"*** 「그것도 지워줘」 · and what the band took with it |
| `game.gd`, at `_pointer_inside` / `_window_focused` | "the edge pan is gone, so what read them is gone" |
| `look.gd`, the four camera-travel constants | "Nothing in `src/` moves the camera on a clock any more" |

⚠ **Q and E's two tombstones belong to 03-10, and `_press_orders`' belongs to 03-11.** Do not touch
them here.

### ⚠ Three things come back WITH the band, or it is broken

- **`_pointer_inside`, `_window_focused` and the `_notification` that sets them.** Their tombstone says
  outright they existed for the edge pan and for nothing else — **without them the camera slides while
  the player has alt-tabbed away**
- **Off the glass entirely answers zero**, not "as deep as it goes". A pointer dragged past the frame
  still delivers motions, and clamping its depth pans at full speed for as long as it stays out there
- **`_pointer_at` is already live and must NOT be re-derived** — it is not a tombstone, and `_process`
  already rebuilds the 이동선 from it every frame

⚠⚠ **The band's old gesture gate does not carry over unexamined.** It answered zero while a press was
open, because **the left press was the walk order** and a camera sliding between press and release
commanded a different 조각 than the one under the finger. **03-04 makes the left drag a box and 03-11
gives the order to the right button**, so that reason is gone — a new one is written, or the gate goes
on purpose.

### ⚠ The hover plate goes stale, and nothing catches it

**`set_hover_tile` is written on mouse motion only.** A keyboard or edge pan slides the board under a
motionless cursor, so **the plate stays on the 조각 the cursor used to be over.** The 이동선 was given
a per-frame rebuild for exactly this reason; the plate never was. **Rebuild it per frame beside the
route, or accept the stale plate and write down which** — saying nothing here is the defect shipping.

## Screen

**The 판 moves, and nothing new is drawn.** No mark, no widget, no chrome — this ticket restores two
inputs into `field_view.pan_by`, which the drag has been calling all along.

⚠ **The edge band has no visible band.** 28 px of glass behaves differently and looks identical. **If
it turns out to need a mark, that is a different ticket and it is made in a tool.**

## Done when

**Four things are looked at.**

**1 — On screen, in the running game.** W held pans north and keeps panning for as long as it is held;
the four keys make eight directions; the pointer in the bottom 28 px pans south and stops the moment
it leaves the band; **nothing moves while the window is alt-tabbed away.**

**2 — The five rows the deleted net's own tombstone specifies**, rebuilt in `net_shell`:

- the camera keeps moving **across frames** from one press, not one step per event
- the **release** stops it
- an OS auto-repeat **echo adds no second direction**
- **two keys held make a diagonal**, and letting one go leaves the other running
- a key **held into the roam ring stops at the bound** instead of running out to sea

⚠⚠ **`_key_edge` has to be rebuilt before a single one of those can be written.** `net_shell`'s `_key`
builds `pressed = true` and nothing else — **no release edge and no `echo` flag** — so a WASD row
written against it measures the press half and stays green over a camera that never stops.

**3 — One frame, one `pan_by`.** A row that holds a key AND parks the pointer in the band, and reads
the frame's travel as **one** summed velocity rather than 1800 px/sec.

**4 — `tools/look/capture_boat.gd` reads `Look.CAM_PAN_KEY_PX_PER_SEC`**, and the repo holds **one** 900.

⚠ **Not done when the keys move the camera.** The band, the alt-tab guards and the single call are the
same ticket.

## Implementation plan

⚠⚠ **This is screen work, so the nets are written AFTER the code** (CLAUDE.md's folder rule). ⚠ **The
recovery source is verified**: commit `b15b09c` holds `_pan_keys`, `_on_pan_key`, `_edge_pan_dir`,
`_edge_ramp`, `_pointer_inside`, `_window_focused`, `_notification`, the single summed `pan_by`, and all
four `look.gd` constants at their stated values. **Read it, do not reinvent it.**

### Seams

**Two, both already agreed.** `src/shell/` measured through `_ready()` and hand-built `InputEvent`s
handed straight to `_unhandled_input` — which is how `net_shell` already drives every other key. And
`src/view/` through `FieldView.new()` for `pan_by` and `_clamp_cam`, which is how `net_camera` already
drives the camera. **No new seam.**

### Files to touch and why

| File | Why |
|---|---|
| `src/look.gd` | The four deleted constants come back at their measured values — key speed **900** px/sec, band **28** px, band speed **= the key speed**, inner-lip factor **0.30**. Its four-constant tombstone is rewritten |
| `src/shell/game.gd` | The held-key vector, the key edges, the band direction and its ramp, the two window flags with the `_notification` that sets them, and **one summed `pan_by` per frame** in `_process`. Six tombstones rewritten |
| `tests/nets/net_shell.gd` | `_key_edge` rebuilt first, then the five rows plus the one-call row. Two tombstones rewritten |
| `tools/look/capture_boat.gd` | Its private `PAN_PX_PER_SEC := 900.0` reads `Look.CAM_PAN_KEY_PX_PER_SEC` instead, so **the repo holds one 900** |

⚠⚠ **The constants go in `look.gd` and nowhere else.** `net_draw_leaf` sweeps `src/shell/` for
pixel-and-time suffixes, so a literal named `*_PX_PER_SEC` or `*_BAND_PX` written into `game.gd` reddens
it. ⚠ `game.gd` has no function table in that net, so new shell functions need no table entry — **this
is the opposite of 03-10, where they do.**

### Order

**1. `look.gd` — the four constants.** Restore the names and the values from `b15b09c`; the band speed
is written as `:= CAM_PAN_KEY_PX_PER_SEC`, deliberately linked, exactly as it was. Rewrite the
four-constant tombstone in place: it says all four are deleted and that nothing moves the camera on a
clock; **both halves stop being true here.**

**2. `game.gd` — the two sources.**

- **The keys.** The held direction is a `Vector2` field written on **both edges** of W/A/S/D, so the
  branch sits **above** the `if not key.pressed: return` gate — the same place the tombstone says it
  sat, and the reason it sat there. ⚠⚠ **An `echo` press adds nothing**: the field is written whole per
  key rather than accumulated, so OS auto-repeat cannot stack a second direction. ⚠ **The signs are
  `pan_by`'s, which are a DRAG's** — 「look north」 is a POSITIVE y, and the tombstone for `_on_pan_key`
  says this is what its `+=`/`-=` got wrong
- **The band.** The remembered pointer is `_pointer_at`, which already exists and is already written by
  the motion branch above the turn's early return. **Nothing new remembers the cursor.** The direction
  is four ramped sides, the corner **not normalised**, off-glass is nothing rather than deepest, and the
  ramp runs from the lip factor at the inner edge to full at the glass
- **The two guards.** `_notification` comes back with the mouse-enter/exit and focus-in/out cases
  writing the two flags. ⚠ **There is no `_notification` anywhere in the repo today** — this is the only
  one, and without it the camera slides while the player is alt-tabbed away
- **One call.** `_process` sums `keys * key_speed + band * band_speed` and pushes **one**
  `field_view.pan_by(vel * delta)`. ⚠⚠ **It goes ABOVE the `battle == null` return**, where the
  tombstone says it stood. ⚠ **The reason written here first was wrong and is corrected**: `_process`
  returns on `run == null` before either, so the pan never runs on the title board at all. The placement
  is right for `battle == null` — an island with no fight running still pans — and the title has nothing
  to do with it. `pan_by` ends in
  `_clamp_cam`, so the roam bound is already enforced and nothing new clamps

**3. `capture_boat.gd`** reads the constant instead of its own copy. ⚠ Its own comment already says its
900 is the keys' old speed and that its signs are `pan_by`'s — so this is a one-line change, not a
behaviour change.

**4. The nets, last.** ⚠⚠ **`_key_edge` is built before any of the five rows.** `net_shell`'s `_key`
sets `pressed = true` and nothing else — no release edge, no `echo` flag — so **a WASD row written
against it measures the press half and stays green over a camera that never stops.** The new helper
takes the keycode, the pressed edge and the echo flag.

Then, using `_park` (which survived its callers) to start each row off the stop:

- the camera keeps moving **across frames** from one press — two hand `_process(dt)` calls move it twice
- the **release** stops it — a third `_process` after the release moves it 0
- an **echo** press adds no second direction
- **two keys make a diagonal**, and letting one go leaves the other running
- a key **held into the roam ring stops at the bound**, and pushing again moves < 0.001 — the shape
  `net_camera`'s hand-literal row already uses
- **one frame, one `pan_by`**: a key held AND the pointer parked in the band travel **one** summed
  velocity, not 1800 px/sec

Rewrite the two `net_shell` tombstones in place.

### Risk

- ⚠⚠ **The gesture gate does not carry over unexamined.** The band used to hold itself still for the
  length of a press so a press near the edge still commanded. **That rule was written when the left
  button both panned and ordered**; 03-11 splits those. **This ticket restores the gate as it was** and
  does not re-decide it — if it turns out wrong after 03-11, that is 03-11's line to write
- **The band is 28 px of glass that looks identical to any other 28 px.** Nothing marks it. A pointer
  resting near an edge while the player reads the board will drift the camera, and **that is the
  behaviour, not a defect** — it is what the user asked back after seeing RimWorld
- **`_process` gains a camera line again**, which is the first clocked camera change since 2026-08-31.
  A hand-driven `_process(dt)` in a net needs `await t.pump_frames(1)` after it or a frame is read at two
  instants — `net_shell`'s own header records that trap
- ⚠ **The hover plate still has no per-frame rebuild.** The 이동선 is redrawn every frame from
  `_pointer_at`; the plate is written only from the motion branch. **A camera panned by a key under a
  still cursor leaves the plate on the 조각 the cursor left**, and nothing catches it. **Not fixed
  here** — it is written down because this ticket is what makes it reachable without moving the mouse

### Acceptance

- The six rows above are green and **the suite is 1567 passed · 4 failed or better** — this round's
  measured baseline
- `net_camera`'s existing pan rows stay green untouched: the rate row, the turned-axes row and both
  clamp rows. **`pan_by` itself is not edited**
- On screen: W held pans north and keeps panning; the four keys make eight directions; the pointer in
  the bottom band pans south and stops the moment it leaves; **nothing moves while alt-tabbed away**
- **One 900 in the repo**, and it is in `look.gd`

### Out of scope

- **Q, E and the right button.** All of 03-10
- **The left drag.** It still pans in this ticket; 03-12 takes it
- **Any mark for the band.** If it needs one it is made in a tool, and it is a different ticket
- **The stale hover plate**

## Answer

**Built 2026-09-02. `1567 → 1637 통과 · 4 실패`, and the four are the same four that predate this round**
(`net_boats` 1, `net_fx_view` 2, `net_shell` 1). **`net_shell` went 154 rows to 224 and no older row
disappeared** — the whole row-label set was diffed against the commit before this ticket and only the
timing line differs.

**What is on screen**: W, A, S and D pan the 판 and keep panning while held; the four keys make eight
directions; the pointer parked in a 28 px band against any side pans that way and stops the moment it
leaves; nothing moves while the window is alt-tabbed away. **Five screenshots sit beside this file** —
the opened island, W held north, W and D diagonal, the bottom band, and an alt-tab with W still down.

### ⚠⚠ Verified by mutation, not by reading — twenty mutations, nineteen reddened a row

**The three defects a literal restore would have shipped are each pinned by their own row:**

| What was mutated | Rows that went red |
|---|---|
| The `lost` gate deleted | **5** — the held-key latch, the swallowed release, the press flag, the dead-island band |
| The gate weakened to a bare `return` that skips instead of clearing | **2** |
| `_pointer_at` back to `(0, 0)` | **14** — a fresh shell with no motion at all lands in the north-west corner inside one second |
| Focus read only inside the band, the way `b15b09c` had it | **2** — the alt-tab row and the came-back row |
| `_notification` gutted | **5** |
| The band widened 28 → 200 | **3** · 28 → 29: **1** · 28 → 27: **3** |
| The lip 0.30 → 0.60 | **1** |
| The band given a speed of its own | **1** |
| The corner normalised | **2** |
| The off-glass guard deleted | **14** |
| The held-key vector's y dropped after the sum | **both diagonal rows** |
| A release clearing the whole set instead of one key | **1** |
| The old `+=`/`-=` form with no echo guard | **22**, the echo row reading six presses stacked |
| One `pan_by` split into two | **1** — and it is the AT-THE-STOP row, not the additive one |
| A `*_PX_PER_SEC` literal planted in the shell | **2**, `net_draw_leaf` naming both |

⚠⚠ **「One frame, one `pan_by`」 is measured by the row at the roam stop, not by the additive row.**
`pan_by` is linear away from a clamp, so **the additive row passes with two calls** — it is the stop that
separates them, where the first call is eaten by the clamp and the second travels from the clamped point.

### ⚠⚠ The one mutation that did NOT go red — and it is the honest result

**Halving `CAM_PAN_KEY_PX_PER_SEC` to 450, with the band following the link, leaves the whole suite
green.** ⇒ **The value 900 is pinned by nothing.** What the rows hold is **the single source** (one 900
in the repo, and the boat tool reads it), **the key-to-band link**, and **every ratio** — 28 ± 1 px, the
0.325 lip, the un-normalised corner. **This matches the ticket's own line that none of the four was ever
judged on a screen**, and it is written here so nobody reads the green as 「900 is measured」.

### ⚠⚠ Looked at on screen, 2026-09-02 — the band is not livable, and 900 is not the problem

**The nets pin the band's every ratio and pin the value of nothing.** This is the first and only judgement
either number has had.

**The band — measured at 0.1 px, at both zooms.** The boundary is exact: **27.9 px in still pans, 28.0 px
in is dead.** Off the glass is dead, the middle is dead, a corner runs 1.8× a side. The rate is the same
fraction of screen at every zoom, **0.70 screen widths a second hard against the glass.**

⚠⚠ **And at the INNERMOST pixel of the band it is already 0.21 screen widths a second.** ⇒ **It does not
ease in from nothing. The slowest the band ever goes is a fifth of a screen every second.**

⇒ **A hand resting 27 px from an edge slides the board a fifth of a screen a second with nothing on
screen saying why. It reads as the camera drifting on its own.** ⚠ **The missing mark is not what makes
it feel like a fault** — the unmarked 28 px is survivable. **The 0.30 lip starting at a fifth of a screen
per second is.**

**900 px/sec — right where panning matters, wrong where it does not.**

| | opening zoom | zoomed in 2.2× |
|---|---|---|
| the whole east-west roam | **1120 ground px — 0.67 of ONE screen** | 2218 px — 3.8 screens |
| a held key crosses it in | **0.97 s** | 5.45 s |
| the whole north-south roam | 1170 px, **0.67 s** | 2131 px, 3.37 s |

- The speed itself reads as an ordinary scroll and **feels fine zoomed in**
- ⚠⚠ **At the opening zoom there is less than one screen of ground to travel, and one held key crosses
  all of it in under a second.** The key is a jump to the far wall, not a scroll — **a small correction
  is impossible because the bound arrives first**
- **The vertical is 1.56× the horizontal**, which is very noticeable: W and S cross their roam in
  two-thirds the time A and D take

⇒ **The eye's verdict: 900 is not wrong, the PAIRING is.** It is a good number where there is something
to pan across and too fast where there is not. ⚠ **Nothing is changed here** — the ticket restored four
measured values and this is the first look any of them has had.

### ⚠ What could not be measured, and is not claimed

- **A real window-manager alt-tab.** There is no `xdotool` here. What was measured instead: a real
  windowed run reports the pointer inside, the window focused and the pointer at the sentinel after two
  seconds — **so no spurious exit silently kills the band at startup** — and pushing the notification the
  way the engine's own window callback does flips both flags and restores them. **The override is on the
  engine's real path; the WM event itself was not fired**
- **Render cost.** Not measurable headless. At rest it is four float compares and a skipped call
- **The band on glass.** The rows read the camera at the agreed view seam, which is state and not pixels

### ⚠⚠ Amended 2026-09-02 after `adversary` — three defects the restore ships on its own

**All three are in `b15b09c`'s code meeting a repo that moved on. 「Read it, do not reinvent it」 stands;
these three are where reading it literally is the defect.**

**1 — ⚠⚠ The clocked pan obeys 「끝」. Decided, and it is not a new decision.** `_unhandled_input`
returns early on `battle.lost`, so **a key release during GAME OVER is swallowed and the pan latches on
for the life of the process** — the band needs no event at all, so a cursor left near an edge slides a
dead island, and the drift is still running when the next island opens. The gate that swallows it was
written 2026-09-01 with its own reason: *a player left able to tilt, zoom, turn and pan a dead island is
「끝」 saying one thing while the hand is told another.* ⇒ **The summed pan is gated on the same `lost`,
and the held keys and the press flag are cleared wholesale when a run ends.** ⚠ `_press_open` latches
by the identical route and, once latched, kills the band **permanently** through the gesture gate — same
clear, same line.

**2 — ⚠⚠ `_pointer_at` starts at `(0, 0)` today and the band's off-glass test is `< 0.0`.** It was
`(-1, -1)` when the band was deleted; it came back on 2026-09-01 for the 이동선, where the origin is
harmless. **`(0,0)` is not less than `0.0`, so it passes the guard at depth 1.0 on BOTH axes** — every
run pans north-west at **1.41 × 900 px/sec from its first frame** until the mouse is moved. ⇒ **The
`(-1, -1)` sentinel comes back with the band**, and a row pins it. ⚠⚠ **This claim was WRONG and verification disproved it.** It said the keep-falls row would catch the
origin, because that row feeds no motion and compares a sprite count across the loss. **Mutated back to
`(0,0)`, that row stays green.** Fourteen rows redden and **every one of them is new** — the sentinel row
and the drift row written for this ticket. **Nothing older sees it**, which is why the sentinel needed a
row of its own rather than being assumed covered.

**3 — ⚠ 「Nothing moves while alt-tabbed away」 is not what the flags do.** In `b15b09c` the two flags are
read by the **band only** — `_pan_keys` is never consulted against focus, and a focus loss delivers no
key-up. **Hold W, alt-tab, and the camera travels the whole time the player is away.** The Done-when row
says otherwise, so ⇒ **the focus flag gates the whole summed velocity**, and a row measures the key half
specifically.

**⚠ And the held-key form: neither `+=`/`-=` nor a whole write.** The tombstone is right that a whole
write loses the other axis, and `+=`/`-=` is exactly what cannot be cleared after a swallowed release.
⇒ **Four held flags, one per key, and the vector is derived from them.** Diagonals hold, W and S held
together cancel, one released leaves the other, **an echo changes nothing because a flag is set not
added**, and the whole thing is cleared in one line when focus or the run goes.

**⚠ Two acceptance rows are wrong as written and are corrected here:**

- **「One frame, one `pan_by`」 is untrue of the frame.** The left drag still calls `pan_by` from the
  input handler and stays in scope. **The claim is that the two RESTORED sources sum into one call** —
  which is what the Done-when row actually describes. Hold W and drag and there are two calls, and that
  is this ticket's ceiling, not its defect
- **The band cannot be measured by placing the pointer 「in the band」.** This repo already measured that
  fake green — *the check read the same constant the code read, and widening the band to 200 px changed
  nothing.* ⇒ **Hand literals**: 28 px at both ends and 29 px at neither, the 0.30 lip against full depth,
  the corner **not** normalised, and off-glass giving nothing rather than the deepest value
- **The roam-bound row does not borrow `net_camera`'s numbers.** Those are a bare `FieldView.new()` at
  zoom 1.0 on the 48x32 default; `net_shell` drives the real shell, which surveys its own zoom. **The
  shape carries — arrive at a literal, then push again and move < 0.001 — the literals do not**
