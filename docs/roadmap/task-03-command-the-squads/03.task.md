# Task 03 — 부대를 조종한다

Task: 03
Name: 부대를 조종한다
Status: ⏳ 하는 중 — 명령과 이동선이 섰고, 무리 자체가 없다
Chunk: 3 주 — **부대를 쪼개고 합치고 명령한다 · 이동선** (⏳)
Days: **아직 안 정해졌다** — 사용자가 정한다

## What is on screen when this task ends

**손이 부대 하나를 골라 판 위 어디로든 보내고, 쪼개고 합친다.**

## Which line it moves

**「부대를 조종하고 지어서 몬스터를 잡는 게임」의 첫 낱말이 「조종」이다.** 이 태스크 전에는 조종이라 부를
것이 없었다 — 누른 자리에서 가장 가까운 몸이 답했을 뿐이다.

## Tickets

| # | What it is | Status |
|---|---|---|
| **03-01** | 몸을 골라 갈 수 있는 자리를 보고 보낸다 | resolved |
| **03-02** | 고른 것의 정보가 화면에 뜬다 | open |
| **03-03** | 판을 무엇으로 움직이나 — 왼쪽 끌기 하나만 남았다 | open |
| **03-04** | 무리를 끌어서 고르고 보낸다 — 왼쪽 버튼에 세 번째 일이 붙는다 | open |
| **03-05** | 단축키 1~4 로 부대를 등록한다 | open ✅ **`Hand` 가 이미 목록이라 아래는 안 바뀐다** |
| **03-06** | 무기를 들면 부대로만 움직이나 | open — `Type: grilling` ⚠⚠ **캐러 보내는 데 값이 붙느냐가 여기 걸려 있다** |
| **03-07** | 이동이 재미있어야 한다 | open — `Type: grilling` ⚠ **사용자가 화면을 보고 낸 판정이다** |

⚠⚠ **A SECOND TASK-03 FOLDER STOOD BESIDE THIS ONE AND WAS FOLDED IN ON 2026-09-01.**
`worktree-drop-qe-turn` opened `task-03-commanding-squads` with one ticket it numbered `03-01`, and
`worktree-pick-then-move` opened this folder with two tickets numbered `03-01` and `03-02` — the same
night, neither able to see the other. **Git merged both folders in silently because the paths
differed.** The two built tickets kept their numbers and the unbuilt one became `03-03`.
⚠ **03-03 is a camera question, and it was filed here because the camera and the walk order share one
button.** Whether it belongs to this task at all is still the user's to answer.

⚠⚠ **무리(부대) 자체와 쪼개기·합치기는 아직 티켓이 없다.** 무리를 어떻게 조종할지가 미정이라
(2026-08-31, 사용자: 「아직도 무리를 어떻게 컨트롤할것인지는 미정이여서」) **`grilling` 이 먼저 돈다.**
⚠ **여기에 줄을 미리 만들어 두지 않는다** — 없는 티켓의 줄은 이 표가 금지하는 것이다.

## What the user still has to answer

- **무리를 어떻게 조종하나** — 쪼개고 합치는 손동작이 무엇인가
- **이 태스크가 티켓 몇 개로 잘리나** — ⚠ 티켓 수는 날짜가 아니다
- **정보 패널에 무엇이 뜨나** — 그리고 그것을 무슨 도구로 만드나
