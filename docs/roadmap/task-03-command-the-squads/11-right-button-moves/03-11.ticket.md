# 03-11 — the right button orders the 부대 onto a 칸

Ticket: 03-11
Name: the right button orders the 부대 onto a 칸
Task: 03 — 부대를 조종한다
Type: task
Status: resolved
Blocked by: 10

## What this ticket builds

**A right press on a lit 칸 sends the picked 부대 there, and the left button stops ordering.** Today one
button does both jobs: `_press_the_island` (`src/shell/game.gd`) asks `hand.is_empty()` and branches
— a full hand walks, an empty hand picks. **This ticket cuts that branch in half along the button.** The
full-hand arm (673-690: `Grid.block_of` → `hand.can_reach_block` → `hand.order` → `_let_go`) becomes the
right button's whole job; the empty-hand arm (692-697: `hand.body_at` → `hand.pick`) stays on the left.

⚠⚠ **The right button has ONE job and therefore NO press-vs-drag split.** `Look.DRAG_PAN_THRESHOLD_PX`
is **6.0** (`src/look.gd`) and it is never consulted for the right button — no travel gate is
written for it. ⚠ **The left button keeps its own split**: a short press picks one body, a drag
box-selects. That half is **03-12** and none of it is built here.

⚠ **Until 03-12 lands, `hand.ids` is length 1**, so "the whole 부대" is a claim about a list of one.
⚠⚠ **A list of one can still be sent NOWHERE.** `_standable` stopped asking capacity on 2026-09-01, so
a full 칸 lights; `order` then stops at `seats.size()` (`src/sim/hand.gd`) and `_seats`' own
header calls fewer seats than bodies **not an error** (481-482). ⚠ **`_let_go()` runs at `game.gd`
BEFORE the count is read at 685**, so a press that seated nobody empties the hand anyway — which is why
the row below asks where the body went and not only whether the hand is empty.

### ⚠⚠ One thing here is NOT decided, and the user is the one who decides it

⚠⚠ **DECIDED SINCE THIS SECTION WAS WRITTEN — by the user, 2026-09-02, and the first of the three
answers below won.** `GLOSSARY.md`'s 뒤집힌 낱말 table carries it as its own row: 「ESC 아니면 부대를
안 놓는다」 → 「빈 땅을 왼쪽으로 누르면 놓는다」 — *"a left press on empty ground lets go"*. StarCraft,
which the user named, is the reference; **ESC stays as the second way.** Ticket 03-04's Answer records
the same reversal. **The section below is kept as it was written**, and the plan at the bottom builds
the decided rule rather than the fall-through this section describes.

**What a LEFT press on empty ground does to a full hand is an open question.** The live rule is the
user's own sentence:

> The user, 2026-08-31: ***"unless I press ESC, the move should stay the priority."***
> (「이게 esc를 하지 않는 이상 이동 우선으로 해줘야할듯한데」)

That sentence is written into the shell at `game.gd` as **"A PRESS THAT CANNOT BE A WALK KEEPS THE
HAND, AND ESC IS THE ONLY WAY TO LET GO"**, and the precedence it argues for sits above it at 664-672.

⚠⚠ **This ticket reverses nothing and writes no line for it.** After the split, a left press on empty
ground falls into the empty-hand arm, `hand.body_at` (692) answers -1, and the press returns false —
**the hand survives, and `game.gd`'s ESC still lets go.** ⚠ **A left press on a BODY does change**,
and that half is the split itself rather than a decision: the pick arm re-picks, so a full hand no
longer swallows the press.

⚠ **The reason the old rule existed is spent all the same** — it existed because ONE button did both
jobs, so a press aimed a little wide lost the 부대 the player had just picked. **Three answers are on
the table and the model picks none of them:**

- **A left press on nothing clears the hand** — StarCraft, which the user named; ESC stays as a second
  way;
- **A left press on nothing does nothing** — today's rule, and what this ticket builds by falling
  through;
- **A left press on a body replaces the selection, a press on nothing does nothing.**

⚠⚠ **Whichever the user picks, the old line is written onto and never deleted**, and the rejected
branch goes to `docs/design/`.

### What the wiring has to get back, in code

| What | Where it stands now | Why this ticket moves it |
|---|---|---|
| `_press_orders` | **deleted**, tombstone at `game.gd` — *"a gate that can only be true is not a gate"* | `_begin_press` (535) must carry which button opened the press, and `_end_press` (569) must ask |
| the `at` parameter of `_end_press` | **deleted**, tombstone at `game.gd` | ⚠⚠ `_rrelease` in the nets (`tests/nets/net_shell.gd`) builds a right release with **no position** — so the order is aimed from the point stored on the DOWN edge, exactly as the left press already is at 576-577, or a press commands **(0, 0)** |
| *"NOTHING HERE ASKS WHICH BUTTON, AND THAT IS WHAT KEEPS THE RIGHT ONE FROM COMMANDING"* | `game.gd` | **that guarantee is exactly what this decision spends** |
| the lost-board blanket `return` | `game.gd`, two tabs inside `if battle != null and battle.lost:` (270), **outside** the mouse-button branch | ⚠ It swallows release edges too. `_press_open` (540/578) and `_panning` (405/541/579) are reset by neither `_back_to_title` nor `_start_run`, so a right button held when the 성채 falls **latches its press across the return to title and releases into an order aimed at a stale `_press_at`.** Cosmetic today; a wrong order the moment the right button orders |

## Screen

**none.** Nothing is drawn by this ticket — it moves a job from one mouse button to the other.

⚠ **What the player sees change is only what already draws**: the reach lighting up on a pick, and the
이동선 that `_show_route` (`game.gd`) rebuilds on hover. Both exist and neither is remade here.

⚠⚠ **The selection box is NOT this ticket.** Its picture is **03-13** and its gesture is **03-12**;
`CLAUDE.md` sends that rectangle to `tools/pixel/` or pixellab and it is not typed.

⚠ **03-10 gives one thing back for free**: `set_hover_tile` (393) and `_show_route` (398) sit below the
yaw drag's `return` at 385 today and **freeze while the board turns.** Deleting that `return` un-freezes
them.

## Done when

**Every row below is measured headless through `net_shell`, driving `_unhandled_input` on the real
shell.** There is no screenshot here — nothing new is on screen.

- **A right press-and-release in place on a lit 칸 sends the 부대**: for every id that was in
  `hand.ids`, `b.grid.block_of(int(b.soldier_order[id]))` equals `b.grid.block_of(dest)`, and
  `hand.is_empty()` is true afterwards. ⚠ **Every `soldier_order` is set to -1 first**, because
  `block_of(-1)` is -1 and a press that ordered nobody must not be able to pass.
  ⚠⚠ **Read from the user's StarCraft reference, 2026-09-02, and written onto this row rather than
  replacing it: the order fires on the right PRESS, at the 칸 under the press point, and the release
  orders nobody.** So 「press-and-release」 here is two edges with the order on the first — the row is
  measured with `soldier_order` read BEFORE the release is sent, and the release is sent afterwards to
  prove it adds nobody.
- **A left press-and-release on that same 칸 orders nobody**: the count of `soldier_order[i] >= 0`
  is **0**. ⚠ **Aimed at a 칸 with no body standing on it**, or the left press picks that body and the
  hand it held is gone — which is the split working, not a failure.
  ⚠ **The 2026-09-02 reading changes nothing on this row**: the left pick is read on the release since
  03-16, so the left pair stays a press-and-release with the pick on the second edge.
- **A left press on empty ground with a full hand leaves the hand alone**: `hand.ids.size()` and
  `hand.reach.size()` are the same before and after. ⚠ **That is the 2026-08-31 rule carried over, not
  a new one**, and ESC still empties the same hand in the row after it.
  ⚠⚠ **REVERSED BY THE USER ON 2026-09-02, and this row is written onto rather than deleted.** The
  decided rule is the glossary's 「빈 땅을 왼쪽으로 누르면 놓는다」: **a left press-and-release in place
  on ground with no body under it EMPTIES a full hand** — `hand.ids.size()` and `hand.reach.size()` are
  both **0** afterwards and `soldier_order` counts **0** bodies ordered (it let go, it did not walk).
  **ESC still empties the same hand in the row after it** — that half of the old row stands.
  ⚠ **Left pair, so the 2026-09-02 DOWN-edge reading does not touch it** — the let-go is read on the
  release, exactly as the pick is.
- **No threshold on the right button**: right press, a **60 px** motion, then a release — and it still
  orders, aimed at the 칸 under the DOWN point rather than the release point.
  ⚠⚠ **Read from the user's StarCraft reference, 2026-09-02, and written onto this row: the order has
  already gone out on the DOWN edge before the 60 px arrive**, so 「aimed at the 칸 under the DOWN
  point」 is true by construction and the release point cannot enter into it. The row reads
  `soldier_order` right after the press, then moves 60 px and a D pan, then releases — and the release
  adds nobody, measured on a body re-picked between the two edges.
- **The board does not turn across any of it**: `fs.cam_yaw_deg` unchanged. 03-10 deletes the turn;
  this is the row that catches it coming back under the order.

### ⚠⚠ Three greens that certify the rule this ticket deletes — *"the right button never sends anybody"*

**`docs/how-nets-lie` is about exactly this**, 2026-08-25: **"rename a thing and the check written
against that name quietly starts measuring something else"** (「이름을 바꾸면 그 이름으로 짜인 검사가
조용히 다른 것을 재기 시작한다」). Here the word *press* stops meaning *order*, and three rows survive
the change while measuring nothing.

| Row | What its label says | Why it stays GREEN over a right button that orders |
|---|---|---|
| **`net_shell.gd`** | `t.eq(ordered, 0, "…오른쪽은 절대 보내지 않는다")` — *"the right button never sends anybody"* | `_the_right_button_turns_and_never_commands` (909) contains **no `hand.pick` and no `pick_many` anywhere in 909-982**, and `run()` calls it at **329** right after a function whose last hand rows are `_key(KEY_ESCAPE)` at 887 and `t.ok(game.hand.is_empty())` at 888. **The hand is empty when the right press lands, and an empty hand orders nobody whatever the button does.** |
| **`net_shell.gd`** | `t.eq(ordered, 0, "끌고 나서도 아무도 명령받지 않는다")` — *"nobody is ordered after the drag either"* | The same empty hand, after the drag. |
| **`net_shell.gd`** | `t.eq(fs.cam_px, before, "문턱 아래로 움직인 것은 아직 이동이 아니다")` — *"travel under the threshold is not a pan yet"* | Its own comment at 805-806 says that **without it the threshold could be zero and every check below would still pass.** ⚠ **It is 03-12's row and not this ticket's** — but this ticket is what says out loud that the right button has no threshold, so **nobody may point at 808 as evidence about the right button.** |

⇒ **Both `ordered == 0` rows must pick a body BEFORE the right press, or they are the shape that let
sixteen shove rows pass over an empty `Rules.SPECIES_SHOVE`** — `how-nets-lie`, 2026-08-27: **"empty
the table and the check that read it turns into an inequality that is always true"** (「표가 비면 그
표를 읽던 검사가 항상 참인 부등식으로 변한다」).

⚠ **Overtaken before this plan was written, measured in the file on 2026-09-02**: the function is now
`_the_right_button_turns_nothing_and_commands_nobody`, **it picks a body first, finds a 칸 that body
can be sent to, and fires the LEFT button at the same point at its end as a positive control** — so
its two `ordered == 0` rows measure a real refusal today. **The plan below rewrites that function into
the positive rows rather than re-arming it**, and keeps its pick-first shape.

⚠ **Two more rows go RED and are rewritten rather than patched**:

- **`net_shell.gd`** — a LEFT `_press`/`_release` pair asserting `block_of(soldier_order[walker])
  == block_of(dest)`. It becomes `_rpress`/`_rrelease`, **and gains an assertion that the left click did
  not order.**
- **`net_shell.gd` `_a_full_hand_moves_instead_of_picking`** — its whole SUBJECT is the move-first
  precedence, not just the button that carried it. **Its destination is deliberately another body's own
  조각 (655), and on the left button that press now picks that body instead of walking — the exact
  opposite of what it asserts.** ⇒ it becomes a right-button row.

⚠ **A throw mid-`run()` abandons every row under it and `net_shell` has done it twice** — once
asserting **56** rows and then throwing while the old detector stayed silent (`tests/run_nets.gd`).
**`_reached_end` catches it now** (111, 117-120), so a new function goes where a throw above it cannot
abandon it, with `t.done()` still last.

## Implementation plan

*Written 2026-09-02 by `plan-into-ticket`, from `src/shell/game.gd`, `src/sim/hand.gd` and
`tests/nets/net_shell.gd` as they stand after 03-09 · 03-10 · 03-16 · 03-17. The line numbers in the
sections above predate those tickets and are not used below. **Revised the same day after an adversary
pass returned three findings.** The first — that the 이동선 drawn at the press and the 칸 ordered at
the release could disagree — is **dissolved by a reading, not answered by a row**: **read from the
user's StarCraft reference, 2026-09-02, StarCraft issues the move order on the right button's DOWN
edge.** So **the order fires ON THE RIGHT PRESS, at the 칸 under the press point, and the release does
nothing but close the gesture.** There is no press-vs-release divergence left to measure: the 이동선
on screen at the instant of the press is drawn from the same `_tile_at` → `Grid.block_of` →
`can_reach_block` chain, on the same camera, in the same frame, and that is the 칸 the order goes to.
A right press with 60 px of travel afterwards still counts as ordered at the DOWN point, because the
order already fired. The other two findings stay answered as before: the band gate's left half (Seams,
and net item 4) and the drawn body under a let-go point (Seams, and net items 1 · 2 · 4 · 5).
**Revised a second time the same day, after a second adversary pass on that revision returned two
more findings, both about a row that was never driven.** First: the reversal's own case — the press
that MISSES, on water, off the board, on a dark 칸 — was never left-pressed; every let-go row aimed at
a lit 칸 inside the hand's reach, so a builder who left the 2026-08-31 rule standing for exactly the
miss (`_let_go()` only inside the reachable arm) passed every row. **Answered by two rows in net item
1**, one per class of miss, and by an inversion that narrows the let-go and must redden them while the
lit-칸 row stays green. Second: a LEFT press on a drawn body while the hand is already FULL was never
driven, so the order of the two arms in the new `_press_the_island` was unmeasured — swap them and a
full hand pressing a body drops the 부대 instead of re-picking it, with every row green. **Answered by
two rows in net item 2** (the same body's foot, then another body's) and by an inversion that swaps
the arms. Measured before writing: `powershell -ExecutionPolicy Bypass -File tests/run_nets.ps1
shell` is **248 passed · 1 failed**, the red being 「몸마다 자리·색·크기가 sim 의 그 몸에게서 나왔다」 —
one of the roadmap's four standing reds, not this ticket's, and it does not move.*

**What stands today, read out of the code and not out of the sections above:**

- **The right button has NO branch.** `_unhandled_input`'s `InputEventMouseButton` arm handles the
  wheel and `MOUSE_BUTTON_LEFT` (`_begin_press` / `_end_press`); where the right button's two arms stood
  is a four-line dated tombstone ending 「stops being an argument the day 03-11 puts the order on it」.
  A right press reaches no code. `_press_at` / `_press_open` / `_panning` are the left button's alone.
- **`_press_the_island(at)` still has both arms**: full hand → `_tile_at` → `Grid.block_of` →
  `hand.can_reach_block` → `hand.order` → `_let_go()` → `sent > 0`, else keep the hand and return
  false; empty hand → `field_view.body_at_px(at)` → `hand.pick` → `_tell_the_view()`. 03-16 replaced
  the ground pick with the glass pick and nothing else in it moved.
- **`FieldView.body_at_px(at)` is the only hit test a left press gets.** It answers the body whose drawn
  rectangle covers `at`, read off the pooled sprites as last painted, or -1. **A left press on a point a
  body is drawn over picks that body**, whatever ground is under it — which is why every let-go row
  below proves the point is bare first.
- **`_pan_the_board` already drops `_press_open` and `_panning` on a lost board** and
  `_a_lost_board_stops_travelling` measures it. **`_back_to_title` and `_start_run` reset neither
  flag.** The lost-board blanket `return` in `_unhandled_input` swallows every release edge.
- **`_edge_pan_dir` answers zero while `_press_open or _panning`**, and its header says 03-11 is where
  that gate is next argued about. **The only behavioural row on it is the band block of
  `_the_edge_of_the_window_pans`**, a LEFT press held in the band.
- **The motion branch reads `_press_open` for `Look.DRAG_PAN_THRESHOLD_PX`** and nothing else about
  buttons. The hover plate and the 이동선 have no early `return` above them — 03-10 deleted the yaw
  drag's.
- **`Hand.order(battle, block)` writes `block_face`, seats the 부대 and answers the count sent.** It is
  not touched. `Hand.pick_many` calls `clear()` first, so a pick over a full hand is a replacement by
  construction.
- **Deleted and not revived**: the right-button yaw drag (03-10), `_press_orders`, the `at` parameter
  of `_end_press`, `Hand.body_at` and `_point_at` (03-16). The `_press_orders` tombstone ends 「03-11 is
  what picks it up, and the flag it needs is this one」 — **it is not revived**, see Seams, and that
  tombstone is written onto.
- **Which greens went false at this spot**: `how-nets-lie` 2026-08-25 (a renamed thing's checks
  measure something else) and 2026-08-27 (an empty table turns a check into a tautology). Both are why
  every `ordered == 0` row below picks a body first and every order row zeroes `soldier_order` first.

### Seams

- **`src/shell/` — `_unhandled_input` on the real shell**, the seam `GLOSSARY.md` names for the shell.
  Every row is a built `InputEventMouseButton` / `InputEventMouseMotion` / `InputEventKey` handed to
  `game._unhandled_input(ev)` on a `Game` that ran `_ready()` and opened the island through the title
  press — the fixture `net_shell` already stands up. **No new seam.**
- **`src/sim/` is read, never driven**: the rows read `battle.soldier_order`, `hand.ids`, `hand.reach`
  and `hand.is_empty()`, and the camera's pure fields `fs.cam_px` / `fs.cam_yaw_deg`. `hand.pick` is
  called directly only to fill the hand as a fixture, exactly as the file does today.
- **`src/view/` is read on one function**: `fs.body_at_px(pt)` — the same call the shell's left press
  makes — after `fs._paint_bodies()` and `fs._place_camera()`, which is how `_a_drag_looks_around_...`
  already refreshes the pool mid-file. **This is the answer to the drawn-body finding**: a let-go row
  is only a let-go row if nothing is drawn under its point, and the destination searches say so rather
  than hoping the island's layout does.
- **The order fires on the DOWN edge, and that is the structural decision** (read from the user's
  StarCraft reference, 2026-09-02). `_begin_order(at)` converts the press point through `_tile_at` →
  `Grid.block_of` and calls `_order_the_island` **in the same call**; `_end_order()` clears one flag and
  reads nothing. What that makes true by construction: **the 칸 the 이동선 was drawn to at the instant
  of the press is the 칸 ordered** — `_show_route` and `_begin_order` walk the identical chain on the
  identical camera — and **nothing between the two edges can re-aim anything, because the release has
  nothing left to aim.** A 60 px drag, a D pan, a band slide: all arrive after the walk has gone out.
  The release therefore needs no position, which is what `_rrelease()` in the nets already builds. ⚠
  **The net shape this dictates**: every order row reads `soldier_order` BETWEEN the two edges, and one
  row re-picks between them so a release that ordered a second time has somebody to send.
- **Two gestures, two flag sets, and the right one is ONE flag.** The right button does NOT go through
  `_begin_press` / `_end_press`, and `_press_orders` is not revived: **both buttons can be down at
  once**, and one slot would let a right press in the middle of a left drag overwrite `_press_at` and
  `_panning`. The right button gets `_order_open` and nothing else — **no `_order_block`**: the release
  reads no 칸, so a field carrying one from the press to the release would be the shape the `at`
  tombstone in `_end_press` names, a value kept for nobody to read.
- **The band gate keeps BOTH halves measured — the answer to the band finding.** The gate becomes
  `_press_open or _panning or _order_open`, and the band block keeps a LEFT press-held row beside the
  new RIGHT one. **The two halves protect different things, and the plan says which.** The LEFT half
  is a defect row: since 03-16 the pick is resolved on the glass at release, so a band that slid under
  a held left press would change which body is under the finger. The RIGHT half is a decision row, not
  a defect row: the order has already fired when the band would slide, so what `_order_open` holds
  still is **the rule that a held button does not travel the board** — a right button held on water or
  on a dark 칸 (an order that went nowhere) does not push the island out from under the hover plate.
  Deleting `_press_open` from the gate reddens the left row; deleting `_order_open` reddens the right
  row; the comment on each says what it guards.

### Files to touch and why

| File | Why |
|---|---|
| `src/shell/game.gd` | **The whole change.** One field, three new functions, one branch, one gate, one drop helper with two callers, and every comment that would lie afterwards |
| `tests/nets/net_shell.gd` | **Every row in Done when**, written after the code; one helper gains a filter |
| `docs/design/a-left-press-on-nothing-keeps-the-hand.md` | **The rejected branch**, new, English, `Status:` line first |
| `docs/design/README.md` | **One row** in the 「남은 갈래」 table so the new doc can be found, and the heading's count goes from six to seven. The row is English; the table around it is not translated by this ticket |

**Nothing in `src/sim/`, `src/view/`, `src/look.gd` or any other net changes.**

#### `src/shell/game.gd`, in detail

- **Field**, declared directly under `_press_open`: `var _order_open := false` — true from a right press
  to its release, and the only thing the release touches. **The `_press_orders` tombstone above it is
  written onto**, dated 2026-09-02: 03-11 did not revive it — the right button got its own flag, for
  the reason in Seams. The `_press_at` / `_press_open` header gains one line: this pair is the left
  button's, and the right button's flag sits below it.
- **`_unhandled_input`**, inside the `InputEventMouseButton` arm, after the left branch:
  `elif click.button_index == MOUSE_BUTTON_RIGHT:` → `_begin_order(click.position)` on `pressed`,
  `_end_order()` otherwise. **The tombstone where the right button's two arms stood keeps its four dated
  lines and gains a fifth** — 2026-09-02, 03-11: the button carries the move order, on its DOWN edge —
  and its last paragraph (「stops being an argument the day 03-11 puts the order on it」) is answered in
  place: that day is this one, and what keeps the right button out of `_end_press` is now that it has
  its own branch.
- **`_begin_order(at: Vector2) -> void`**: `_order_open = true` **first** (the band holds still for the
  length of any right press, water included, exactly as it does for any left press); `if battle ==
  null: return`; `var tile := _tile_at(at)`; `var block := battle.grid.block_of(tile) if tile >= 0
  else -1`; `_order_the_island(block)`. **The order fires here**, and the dated comment on that line
  says why: read from the user's StarCraft reference, 2026-09-02 — the move order goes out on the
  right button's DOWN edge, so the 이동선 on screen at this instant and the 칸 ordered are one number.
  ⚠ The bool `_order_the_island` answers is not read here and not stored: the right button has no
  second gesture competing for the press, so 「consumed」 has no reader — say so on the line rather than
  keeping the value.
- **`_end_order() -> void`**: `_order_open = false`. **One line, and its header says what it does not
  do**: it orders nothing, because the order went out on the press; it reads no position, no
  `_panning`, no threshold — the right button's motion is read by nothing, which is how 「no travel
  gate」 is true by construction rather than by a constant nobody consulted.
- **`_order_the_island(block: int) -> bool`** — the full-hand arm of `_press_the_island`, moved out
  whole: `if battle == null: return false`; `if not hand.can_reach_block(block): return false`;
  `var sent := hand.order(battle, block)`; `_let_go()`; `return sent > 0`. **The two dated comments that
  arm carries move with it** (the 2026-08-31 「the order lets go」 reversal and the 칸 conversion note).
  ⚠ An empty hand answers false through `can_reach_block` — **the right button never picks and never
  lets go of nothing.** `body_at_px` is not called here and the header says so. ⚠ A 칸 the hand cannot
  reach keeps the hand: that is the old 2026-08-31 rule surviving on the button it now belongs to, and
  it is not re-decided here (Out of scope).
- **`_press_the_island(at: Vector2) -> bool`** becomes the LEFT button's function alone:
  `if battle == null: return false`; `var who := field_view.body_at_px(at)`; if `who >= 0` →
  `hand.pick(battle, who)`, `_tell_the_view()`, return the pick — **with a full hand this re-picks,
  which is `pick_many`'s `clear()` and needs no line**; else if `not hand.is_empty()` → `_let_go()`,
  return true (**the 2026-09-02 rule: a left press on nothing lets go**); else return false.
  ⚠ 「nothing」 is anything `body_at_px` answers -1 for — lit ground, dark ground, water, off the board.
  One rule and no special case for the sea: the glossary row says 빈 땅, StarCraft clears on any click
  on nothing, and a hand kept only over water would be one more rule nobody asked for.
  ⚠⚠ **The let-go reads NO 칸, NO `_tile_at` and NO `can_reach_block`** — `if not hand.is_empty():
  _let_go()` and nothing in front of it. A let-go written inside `if hand.can_reach_block(block)` is
  the 2026-08-31 rule left standing for the one press the reversal exists for — the press that misses —
  and it passes every lit-칸 row; **net item 1's two miss rows are what catch it**, and the header
  says so in one line.
  ⚠⚠ **The arm order is a rule, not a tidiness: `body_at_px` FIRST, the let-go SECOND.** A full hand
  pressing a drawn body re-picks (`pick_many`'s `clear()` then the new id); with the arms swapped the
  same press drops the 부대 and picks nobody, so a player could never change what the hand holds
  without ESC in between — and every existing row stays green over that, because every existing pick
  lands on an empty hand. **Net item 2's full-hand-on-a-body rows are what pin the order**, and the
  comment between the two arms names them.
  **Its header is rewritten**: the three-line table (empty hand / full hand / full hand elsewhere)
  becomes two lines for the left button and points at `_order_the_island` for the right; the 2026-08-31
  「A PRESS THAT CANNOT BE A WALK KEEPS THE HAND, AND ESC IS THE ONLY WAY TO LET GO」 paragraph is
  **written onto with the 2026-09-02 reversal and kept**, never deleted; the 「A FULL HAND MOVES; AN EMPTY
  HAND PICKS」 paragraph is written onto the same way — the full hand moves on the RIGHT button now, and
  on its press.
- **`_drop_the_gestures() -> void`**: `_press_open = false`, `_panning = false`, `_order_open = false`.
  **Called from two places**: the lost branch of `_pan_the_board` (replacing its two flag lines;
  `_pan_held.clear()` stays there) and **`_back_to_title`**, whose header calls itself the line-for-line
  inverse of `_start_run` — a run that ends with a button down must not hand its flag to the next run,
  and the blanket `return` in `_unhandled_input` guarantees the release never arrives. ⚠ **What a latched
  `_order_open` costs is no longer a stale order** — the release orders nothing — **it is the band**: a
  flag left true kills the edge pan for the life of the process through the gate, the same way
  `_press_open` already could. ⚠ **`_start_run` gets no call**: the only path into it is the title, and
  the only path from a run to the title is `_back_to_title`. Say so in `_drop_the_gestures`' header
  rather than adding a second caller for symmetry. ⚠ **The blanket `return` itself is not moved** — 「끝」
  (2026-09-01) is one line above every branch on purpose, and it is also what makes 「a right press on a
  lost board orders nobody」 true: the press never reaches `_begin_order`.
- **`_edge_pan_dir`**: the gate becomes `if _press_open or _panning or _order_open`. **Its header's
  「03-11 is where this gate is next argued about」 is answered in place**: the gate stays and grows one
  flag, so a right click near the edge is a click and the band does not slide under it — the same rule
  the left button has, and nothing new on screen. The header's example (「`_end_press` would resolve
  `_press_at` against a camera that had moved」) gains the left button's 2026-09-02 reason (the pick is
  resolved on the glass at release, so a slid band changes which body is under the finger) **and the
  right button's honest one**: its order has already fired, and the flag holds the band still because a
  held button does not travel the board, not because anything is left to aim.
- **`_end_press`**: unchanged in code. **Its 「NOTHING HERE ASKS WHICH BUTTON, AND THAT IS WHAT KEEPS
  THE RIGHT ONE FROM COMMANDING」 comment is written onto** — still true, reason changed: the right
  button has its own branch and never enters this function. Its header's 「The right button ends nothing
  now」 line is written onto the same way: it ends its own flag, in `_end_order`.
- **The motion branch's 03-10 paragraph** (「a left pan and a right press can now run together ... stops
  being harmless the day 03-11 puts the move order on it」) is answered in place: the order goes out on
  the right press before any motion can arrive, so a left pan under a held right button re-aims nothing,
  and the band holds still through it.
- **`_begin_press`'s header** (「the right button turned the board and never got here ... what changed is
  the reason, not the fact」) gains the 2026-09-02 line: the right button has its own `_begin_order`.
- **ESC needs no line.** A right press that ordered has already let go; a right press that ordered
  nobody kept the hand, and ESC empties it exactly as before. Say so in `_begin_order`'s header rather
  than testing for it.

#### `tests/nets/net_shell.gd`, in detail — **written AFTER the code, every row driving `_unhandled_input`**

⚠ **Placement rule**: no new function is added to `run()`. Every new row goes INSIDE one of the
functions `run()` already calls — items 1 · 2 · 4 sit between `_the_pan_keys_move_the_camera_and_stop`
and `_the_edge_of_the_window_pans`, above the terrain block a throw has abandoned twice; item 3 is
`_a_full_hand_moves_instead_of_picking`, already called below it and running today (the file's one red
is a `t.eq`, not a throw); items 6 and 7 are the two self-standing fixtures at the top. `t.done()`
stays last.

⚠ **`fs._paint_bodies()` and `fs._place_camera()` are safe mid-file**: the `_boats_used` row they once
reddened was moved above every camera function on 2026-09-02, and `_a_drag_looks_around_...` already
calls both. **They are called after every `_park`**, because `body_at_px` reads the pool as last
painted against the camera as last placed.

⚠⚠ **The reading rule for every order row**: `soldier_order` is read **after `_rpress` and before
`_rrelease`**. A row that reads it only after the release cannot tell an order that fired on the press
from one that fired on the release, and the whole reading this plan builds is the difference.

1. **`_the_right_button_turns_nothing_and_commands_nobody` → renamed
   `_the_right_button_orders_on_the_press_and_never_turns`** (and its call in `run()`); header rewritten
   with the fourth gesture on the same button, the three old ones kept as dated lines, and the
   StarCraft reading cited. Its pick-first shape stays. Rows, in order:
   - self-checks as today: a body ashore, `_park(fs, Vector2.ZERO)`, then `fs._paint_bodies()` and
     `fs._place_camera()`, `game._let_go()`, `hand.pick` on the first pickable id; `dest` searched from
     `hand.reach` in a **different 칸** from the body's, the screen point round-tripping through
     `_tile_at`, **and `fs.body_at_px(at) < 0`** — a point a body is drawn over is skipped. After the
     search, the self-check row `t.eq(fs.body_at_px(dest_at), -1, ...)` names the filter so a builder
     who drops it sees a row and not a layout accident;
   - **the press ALONE orders** (the new Acceptance row): `soldier_order` all -1; `var squad :=
     game.hand.ids.duplicate()`; `_rpress(dest_at)` — **and nothing else is sent before the reads**:
     for every id in `squad`, `b.grid.block_of(int(b.soldier_order[id])) == b.grid.block_of(dest)`
     (「the order went out on the DOWN edge — the button is still held」); `game.hand.is_empty()`;
     `game.hand.reach.size() == 0`; `game._order_open` (self-check: the gesture really is still open,
     so the row above was read between the edges); `fs.cam_px == held`; `absf(fs.cam_yaw_deg -
     held_yaw) < 0.001`. ⚠ `squad` is a list of one until 03-12 and the row loops anyway;
   - **the release orders nobody a second time**: `hand.pick` the same id again while the button is
     still held (self-check `not is_empty()`); orders -1; `_rrelease()`; `ordered == 0` (「the release
     closes the gesture and sends nobody — a second order here would have sent the re-picked body」);
     `not game.hand.is_empty()` (the release did not let go either); `not game._order_open`;
   - **no threshold, and nothing between the edges re-aims anything**: `game._let_go()`; re-pick; orders
     -1; `walker := hand.ids[0]`; `_rpress(dest_at)` → `block_of(soldier_order[walker]) == block_of(dest)`
     read at once; `_motion(dest_at + Vector2(60, 40), Vector2(60, 40))` → yaw and `cam_px` unchanged,
     `not game._panning` (「the right button latched no pan」); **the hover row stays** —
     `_motion(dest_at, Vector2(-60, -40))`, `fs._hover_cell == want_cell` (the plate keeps up under a
     held right button because no motion reader exists for it); **the 이동선 row changes shape**: the
     hand is empty after the order, so `hand.pick` the same id again by hand while the button is held,
     then `_motion(dest_at, Vector2.ZERO)` → `fs._move_lines.size() > 0` (「a held right button does not
     freeze the route — the same early `return` that froze it is gone」); orders -1 (the re-picked body
     is the bait for the release); then a motion to `far_at`, searched outward from `dest_at` along
     `(60, 40)` in 60 px steps, at most 12 steps, until `b.grid.block_of(game._tile_at(far_at)) !=
     b.grid.block_of(dest)` (-1 off the board counts as different) — **self-check row that it was
     found**; `_key_edge(KEY_D, true)`; `game._process(0.05)` in a loop of at most 40 until
     `b.grid.block_of(game._tile_at(dest_at)) != b.grid.block_of(dest)` — **self-check row that the
     camera really moved the point off the 칸**; `_key_edge(KEY_D, false)`; `_rrelease()`; `ordered ==
     0` (「a release over another 칸, on a board that has slid, orders nobody — there was nothing left to
     aim」); `not game.hand.is_empty()`; the existing 「a motion after the release turns nothing」 row
     stays; then `_park(fs, Vector2.ZERO)`, `fs._paint_bodies()`, `fs._place_camera()` to put the
     fixture back where the rows below expect it, self-checked with `game._tile_at(dest_at) == dest`;
   - **the left button at the same point orders nobody and LETS GO** (the reversed Done-when row):
     `game._let_go()`, re-pick, orders -1; `_press(dest_at)`, `_release(dest_at)`; `ordered == 0`;
     `game.hand.is_empty()`; `game.hand.reach.size() == 0`; `fs.cam_px` unchanged. Labelled with the
     2026-09-02 reversal;
   - **ESC still lets go**: re-pick; `t.ok(not game.hand.is_empty())` self-check; `_key(KEY_ESCAPE)`;
     `game.hand.is_empty()`;
   - **the miss itself — the reversal's own case, one row per class of nothing** (the second adversary
     pass: every let-go row above aims at a lit 칸 in the hand's reach, and a let-go narrowed to the
     reachable arm passes all of them). `_park(fs, Vector2(-99999.0, 0.0))` — the western roam edge,
     where `Look.CAM_ROAM_TILES` of sea sit between the island and the glass — then `fs._paint_bodies()`
     and `fs._place_camera()`. **Two points are searched along the row `mid.y`, from `x = 2.0`
     eastward in 20 px steps, at most 64 steps, both inside the viewport and both with
     `fs.body_at_px(p) < 0`**:
     `off_at`, the first point `game._tile_at(p) == -1` answers for (**off the board** — no 조각 at
     all); and `dark_at`, the first point whose 조각 is IN the grid but whose 칸 the hand refuses —
     `t >= 0 and not game.hand.can_reach_block(b.grid.block_of(t))`, which is water inside the grid or a
     조각 the flood never reached, and which implies `not game.hand.can_reach(t)` because `reach_blocks`
     is the union of `reach`. **Self-check rows that each was found, and that each answers what it was
     searched for** (`_tile_at(off_at) == -1`; `_tile_at(dark_at) >= 0`; `body_at_px` -1 on both) — a
     builder who drops a filter sees a row. Then, for each of the two, in this order: `game._let_go()`;
     `hand.pick` the same id (self-check `not is_empty()`); orders -1; `_press(p)`, `_release(p)`;
     `game.hand.ids.size() == 0`; `game.hand.reach.size() == 0`; `ordered == 0` (「a left press that
     misses lets go — it does not walk and it does not keep the hand; the 2026-08-31 line 『the sea used
     to drop the selection here』 is the rule again, on the left button」); `fs.cam_px` unchanged.
     ⚠ **These two rows and the `dest_at` let-go row above are read as a set**: the `dest_at` row stays
     green under a let-go narrowed to `can_reach_block`, and these two go red — that separation is the
     whole reason they exist. Afterwards `_park(fs, Vector2.ZERO)`, `fs._paint_bodies()`,
     `fs._place_camera()`, self-checked with `game._tile_at(dest_at) == dest`, so the rows below aim
     where they did;
   - **the right button never picks**: `game._let_go()`, orders -1; `_rpress(on_land)` / `_rrelease()` on
     the body's own 조각; `ordered == 0`; `game.hand.is_empty()`;
   - **both buttons down at once**: re-pick, orders -1, `walker` taken; `_press(dest_at)` (left held on
     ground with no body under it — never released yet); `_rpress(dest_at)` →
     `block_of(soldier_order[walker]) == block_of(dest)` and `game.hand.is_empty()` **read before the
     right release**; `_rrelease()`; then `_release(dest_at)`; `ordered` still counts exactly one body
     (the left release found no body and an empty hand, so it neither picked nor let go of anything);
     `not game._press_open`; `fs.cam_px` unchanged;
   - cleanup as today: `game._let_go()`, orders -1.
2. **`_a_drag_looks_around_and_a_click_commands`**: the destination search gains the same
   `fs.body_at_px(at) < 0` criterion (the pool was painted and the camera placed just above it, and
   nothing moves the camera between). The second-press block (the LEFT pair on `dest_at` asserting
   「불이 들어온 조각을 누르면 그 몸이 그 칸으로 간다」) is **rewritten, not kept beside**: first
   `_press(dest_at)` / `_release(dest_at)` → `ordered == 0` and `game.hand.is_empty()` (「a left press on
   a lit 칸 with nobody on it lets go — it is not an order」); re-pick through `_press(click_at)` /
   `_release(click_at)` with the `not is_empty()` self-check; **then, with the hand FULL, the two
   full-hand-on-a-body rows** (the second adversary pass: every pick in this file lands on an empty
   hand, so the arm order in `_press_the_island` was unmeasured):
   - **the same body's own foot**: orders -1; self-check `fs.body_at_px(click_at) == sid` (the point
     resolves to that body on the glass, so the row is about the body arm and not about bare ground);
     `_press(click_at)`, `_release(click_at)`; `not game.hand.is_empty()`; `game.hand.ids.size() == 1
     and int(game.hand.ids[0]) == sid` (「a full hand pressing a drawn body re-picks him — it does not
     let go; the body arm is asked first」); `ordered == 0`; `fs.cam_px == held`;
   - **another body's foot, when the island has one**: `other` searched over `b.ashore_ids()` for an id
     `!= sid` with `_drawn_foot_px(fs, other)` finite and `fs.body_at_px(that foot) == other`; a
     self-check row says whether one was found, and the two rows below are skipped without one (the
     same-body row above already pins the arm order; this pair is the stronger claim that the pick
     REPLACES). Orders -1; `_press(other_foot)`, `_release(other_foot)`; `game.hand.ids.size() == 1 and
     int(game.hand.ids[0]) == other` (「a full hand pressing another drawn body swaps to him — one press,
     no ESC」); `ordered == 0`; then `_press(click_at)` / `_release(click_at)` again so the hand holds
     `sid`, self-checked `int(game.hand.ids[0]) == sid`, because `dest` was searched from `sid`'s reach
     and the right press below is his;
   then `_rpress(dest_at)` →
   `block_of(soldier_order[walker]) == block_of(dest)`, `fs.cam_px == held`, `game.hand.is_empty()`,
   `game.hand.reach.size() == 0` **all read before** `_rrelease()`, which is then sent. The ESC rows
   after it stay as they are. Header: 「a click commands」 → 「a right press commands」, the old sentence
   written onto, and one line added for the full-hand rows: the pick is the left button's on an empty
   hand AND on a full one.
3. **`_a_full_hand_moves_instead_of_picking`**: `_press(at)` / `_release(at)` → `_rpress(at)`, both
   assertions unchanged and read at once, then `_rrelease()`. Header rewritten: the subject is now 「the
   right button walks onto a 조각 with another body drawn on it and does not pick him」 — the right
   button never asks `body_at_px`, which is the split itself — and the 2026-08-31 move-first precedence
   it used to certify is written onto as the rule the left button no longer carries.
4. **`_the_edge_of_the_window_pans`**, the band block, becomes two pairs on one point:
   - `band_pt` comes from the helper in item 5, so no body is drawn over it; the `pick_ok` loop keeps the
     id it picked as `band_body`;
   - **LEFT pair, the `_press_open` half of the gate**: `_motion(band_pt, ZERO)`, `_press(band_pt)`;
     self-check `game._press_open and not game._panning` (「only the press flag can be holding the band
     now」); `game._process(0.2)` → `fs.cam_px == pressed_at` (「a held left press in the band holds the
     camera still — since 03-16 the pick is read at release, so a slid band would change who is under
     the finger」); `_tile_at(band_pt) == band_tile`; `_release(band_pt)` → `game.hand.is_empty()` and
     `ordered == 0` (「and a left release on bare ground lets go, it does not order」); anti-vacuity:
     `game._process(0.1)` → `fs.cam_px` moved by more than 0.5 px (the point really is in the band);
   - **RIGHT pair, the `_order_open` half**: `_park(fs, Vector2(-99999.0, 0.0))` again, self-check
     `_tile_at(band_pt) == band_tile` (the same camera, so the same 조각); `game.hand.pick(b, band_body)`
     self-checked; orders -1; `_motion(band_pt, ZERO)`, `_rpress(band_pt)` → **`sent > 0` read at once**
     (「a right press inside the band orders on the press — the band does not swallow it」); self-check
     `game._order_open and not game._press_open`; `game._process(0.2)` → `fs.cam_px == pressed_at`
     (「a held right button holds the band still too — the order is already out; this is the rule that
     a held button does not travel the board」); `_rrelease()`; the existing anti-vacuity row
     (`_process(0.1)` moves) stays last;
   - the block's comment is rewritten: the left half guards the pick, the right half is the decision
     written down, and the order went out on the press; the cleanup (`_motion(mid)`, `_let_go`, orders
     -1) stays.
5. **`_a_walkable_point_inside_a_band(game, b, fs: FieldView)`** gains the field and a filter: it calls
   `fs._paint_bodies()` and `fs._place_camera()` once before the walk (its one caller has just parked
   the camera), and skips a point `fs.body_at_px(p) >= 0` answers for. Header: a point a body is drawn
   over would turn the left row above into a pick.
6. **`_a_lost_board_stops_travelling`**: before `b.keep_hp = 0.0`, pick a body (`game.hand.pick(b,
   ashore[0])` from `b.ashore_ids()`, self-checked), zero `soldier_order`, find `aim_at` from
   `hand.reach` by the same round-trip search (self-checked — a right press on a 칸 the body cannot
   reach would leave the press below with nothing to prove), then `_rpress(aim_at)` → `sent > 0` (the
   press was a real order, so the flag below is a real gesture) and `t.ok(game._order_open)`. After the
   loss frame and the `_process(0.2)` the existing rows already pump: `not game._order_open` (joined to
   the existing 「열려 있던 누름도 같이 놓였다」 row). Then **a right press on the dead board**: re-pick by
   hand (self-checked — `hand.pick` is a sim call and the door is on input, not on the hand), orders
   -1, `_rpress(aim_at)` → `ordered == 0` and `not game._order_open` (「끝 — a right press on a lost
   board orders nobody and opens nothing」); `_rrelease()` → still 0 and still false.
7. **`_the_keep_falls_and_the_screen_says_so`**: after the loss, `_rpress(mid)` → `not game._order_open`
   (the twin of the existing `_press_open` row); and before the back-button click, `game._order_open =
   true` and `game._press_open = true` written by hand — no event path can latch them past the lost
   frame, and `_process` is off on that fixture, which is exactly why the hand-write is worth one row —
   then the click → both back to false (「the way back drops every gesture」).
8. **`_rpress` / `_rrelease` headers**: rewritten — the release carries no position because the shell
   reads nothing on it: the order went out on the press.

### Order

1. `game.gd`: the field → `_begin_order` / `_end_order` / `_order_the_island` → the right branch in
   `_unhandled_input` → `_press_the_island` cut to the left arm plus the let-go line →
   `_drop_the_gestures` and its two callers → the `_edge_pan_dir` gate → every comment listed above.
   Run `powershell -ExecutionPolicy Bypass -File tests/run_nets.ps1 shell` once here: **every new red
   sits inside the four functions items 1-4 rewrite** — the right-button function, the drag-and-click
   function, the full-hand function and the band block — and the pre-existing red stays where it was.
   **A red anywhere else is a defect in the code, not in the nets.** ⚠ Some rows in those four stay
   green for the wrong reason (a hand emptied by a let-go reads `is_empty()` exactly as a hand emptied
   by an order does); that is why they are rewritten and not patched.
2. `net_shell.gd`: items 1-8 above, then the shell net green but for the one pre-existing red.
3. **Inversions, each run and each reddening before moving on** — and confirm each mutation landed
   before trusting the red (`how-nets-lie`: string replacement has matched zero times, twice):
   - delete the `MOUSE_BUTTON_RIGHT` branch → every order row red;
   - **move the `_order_the_island` call from `_begin_order` into `_end_order`** (a release-edge order
     from a stored 칸) → the press-alone rows in items 1 · 2 · 3 · 4 red, because they read
     `soldier_order` before the release;
   - **call `_order_the_island` on BOTH edges** → item 1's 「the release orders nobody a second time」
     row red (the re-picked body walks);
   - resolve a release-edge order from `_pointer_at` → the same item 1 row red, on the `far_at` / D-pan
     block;
   - drop `_order_open` from `_edge_pan_dir` → the band's RIGHT stillness row red;
   - drop `_press_open` from `_edge_pan_dir` → the band's LEFT stillness row red;
   - delete the `_let_go()` line in `_press_the_island` → the left-on-nothing rows in items 1 · 2 · 4 red;
     ⚠ this one does NOT separate a narrowed let-go from the rule, so it is not the only one;
   - **narrow the let-go to the reachable arm** — `if hand.can_reach_block(block_of(_tile_at(at))):
     _let_go()` else keep the hand, the 2026-08-31 rule left standing for the miss → item 1's two miss
     rows (`off_at`, `dark_at`) red, **while the `dest_at` let-go row and items 2 · 4's stay green** —
     write that contrast into the Answer, because it is the reason the two rows exist;
   - **swap the two arms of `_press_the_island`** — `if not hand.is_empty(): _let_go(); return true`
     above the `body_at_px` test → item 2's same-body row red (the hand is empty where it should hold
     `sid`) and its other-body row red where the island has two bodies; **every other row in the file
     stays green**, which is the second adversary pass's finding measured;
   - route the right press through `_begin_press` / `_end_press` → the press-alone rows red (nothing
     is ordered until a release) and the both-buttons row red (the right press overwrites the left
     press's slot);
   - delete the `_drop_the_gestures()` call in `_back_to_title` → item 7's back-button row red;
     delete the call in `_pan_the_board` → item 6's flag row red.
4. The fork doc and the README row.
5. The whole suite: `powershell -ExecutionPolicy Bypass -File tests/run_nets.ps1`, and the roadmap's
   baseline of four reds is the comparison. ⚠ 07-01 is built in this same tree and owns `net_fight`,
   `net_islands` and `net_tiers`: a red in one of those is re-run alone once and reported as possibly
   the other ticket's, never fixed from here.

### Risk

- **A comment that still says the right button carries nothing, or that the release orders, is the
  2026-08-25 failure in prose.** Eight sites are named above; grep `game.gd` for 「right button」,
  「오른쪽」 and 「release」 after the build and read every hit.
- **An order row that reads `soldier_order` only after the release measures nothing this plan is
  about** — a release-edge order and a press-edge order look identical from there. Every order row
  reads between the edges, and the second-time row re-picks between them so the release has bait.
- **`_let_go()` runs before `sent` is read** (the ticket's own warning), so every order row measures
  `soldier_order`, never the hand's emptiness — a hand emptied by a press that seated nobody reads
  identical on `is_empty()`.
- **Zeroing `soldier_order` before every order row** — `block_of(-1)` is -1 and a row that forgot the
  zero passes over a body already standing where it was sent last time.
- **A drawn body under a let-go point turns the row into its opposite** — the pick is read on the glass
  since 03-16 and the island has more than one body ashore. Every let-go point is filtered through
  `body_at_px` after a fresh paint; a builder who fires a left press at a point found some other way
  has re-opened the adversary's finding.
- **`_paint_bodies` after `_park`, never before** — the pool is placed against the camera as it was when
  painted, and `body_at_px` answers for that camera.
- **A let-go row on a lit 칸 cannot tell the rule from its narrowing.** `dest_at` is in the hand's reach,
  so a let-go gated on `can_reach_block` passes it; only a press that MISSES — off the board, or a 조각
  whose 칸 the hand refuses — separates the two, and item 1 drives both classes with the point proven
  bare and proven to be the class it claims. A builder who cuts either miss row has re-opened the
  finding.
- **The arm order in `_press_the_island` is invisible to every pick on an empty hand**, which is every
  pick this file made until item 2's two rows. With the arms swapped a full hand can never re-pick,
  and only a press on a drawn body WHILE HOLDING ONE sees it — so item 2 presses `sid`'s own foot with
  `sid` held, and another body's foot when there is one, and self-checks `body_at_px` on each point
  first so a row on bare ground cannot stand in for a row on a body.
- **Ashore may be one body.** The other-body row in item 2 is guarded by its own self-check and skipped
  without a second body; the same-body row is not guarded, because one body is enough to see the arm
  order and a skipped row there would be the hole with a lid on it.
- **The old 이동선-under-a-held-right-button row goes red for the fixture if kept as it was**: the hand
  is empty the instant the press lands, so the line is measured on a hand re-picked while the button is
  held, and the row's comment says so.
- **A left press that finds no body now empties the hand**, so every existing row that left-presses
  ground while holding a body must be read again: the ones in items 2 and 4 are the only ones —
  `net_pick`'s bare-ground rows already assert an empty hand and stay true, and the drag pair at the top
  of `run()`'s camera block travels past the threshold and never reaches `_press_the_island`.
- **The lost-board drop lives in `_pan_the_board`**, which only runs from `_process`; a row driven
  without a `_process` between the loss and the release still sees the latch — item 6 reads its flag
  after the `_process(0.2)` the existing rows already pump, and item 7 is the `_back_to_title` caller
  measured on its own.
- **The D-pan block moves the camera**, so it re-parks and re-paints before the rows under it, and
  self-checks the round trip; a builder who orders the rows differently has to keep that restore.
- **Sim untouched**: `Hand.order`, `Battle.order_walk`, `block_face` and the seating are not this
  ticket's, and `net_hand` stays at its count.
- **The screen**: nothing drawn, nothing to look at. The reach lighting and the 이동선 already exist and
  are not remade. **No verify-look.**

### Acceptance

- `powershell -ExecutionPolicy Bypass -File tests/run_nets.ps1 shell`: **exit code 1 only for the one
  pre-existing red row named above**, the pass count above today's **248** by the new rows, and every
  Done-when row present under its label:
  - **a right press ALONE — no release sent yet — has already ordered**: `block_of` per id in `squad`
    equals `block_of(dest)` and the hand is empty, read with `_order_open` still true; **and the release
    afterwards orders nobody a second time**: a body re-picked between the two edges has `soldier_order
    == -1` after `_rrelease()` and is still held;
  - the 부대 goes on a right press (the same reads, in items 1 · 2 · 3 · 4);
  - the left button on the same 칸 orders nobody **and lets go** (the reversed row), and the point is
    proven bare by `body_at_px` first;
  - **the miss lets go too, in both classes**: a left press-and-release with a full hand on a point
    `_tile_at` answers -1 for (off the board), and on a 조각 in the grid whose 칸 `can_reach_block`
    refuses, each proven bare by `body_at_px` and proven to be its class by a self-check row — `hand.ids`
    and `hand.reach` both empty and nobody ordered, both rows; **and a let-go narrowed to the reachable
    arm reddens exactly these two while the lit-칸 row stays green** (Order step 3);
  - **a full hand pressing a drawn body re-picks and orders nobody**: with `sid` held, a left
    press-and-release on his own drawn foot leaves `hand.ids == [sid]`, and on another body's foot leaves
    `hand.ids == [other]`, `soldier_order` counting 0 both times, each point self-checked to resolve to
    that body on the glass; **and swapping the two arms of `_press_the_island` reddens these and nothing
    else** (Order step 3);
  - ESC still lets go;
  - 60 px of motion and a D pan between the two edges, released over a different 칸: the body ordered on
    the press stays aimed at the 칸 under the DOWN point and the release adds nobody;
  - `fs.cam_yaw_deg` unchanged across every one of them, `fs.cam_px` unchanged across the in-place ones;
  - the right button never picks; both buttons down at once do not lose the order and the left release
    does not add one;
  - the band holds still under a held LEFT press and under a held RIGHT press, each with its own flag
    self-checked as the only one set, and a right press inside the band orders on the press;
  - a right press open when the 성채 falls is dropped, a right press on the dead board orders nobody
    and opens nothing, and the way back to the title drops every gesture.
- **The eleven inversions in Order step 3 each reddened**, written into the ticket's Answer with the
  row that caught each — and for the narrowed let-go and the swapped arms, the rows that STAYED green
  are written down beside the ones that went red, because the contrast is the finding.
- The whole suite at the roadmap's baseline: **four reds, none of them new** — a red in 07-01's three
  nets re-run once and named as possibly that ticket's.
- `docs/design/a-left-press-on-nothing-keeps-the-hand.md` exists, English, opens with `Status:` (reversed
  by the user on 2026-09-02), states the rule it rejects — a left press on nothing keeps the hand and
  ESC is the only way to let go — cites the user's 2026-08-31 sentence translated with the Korean kept,
  says why the rule existed (one button did both jobs) and why the reason is spent, and points at the
  glossary's 2026-09-02 row 「ESC 아니면 부대를 안 놓는다」 → 「빈 땅을 왼쪽으로 누르면 놓는다」, ticket 03-04's
  Answer and this ticket; `docs/design/README.md` lists it and its heading counts seven.

### Out of scope

- **The left press-vs-drag split and box selection** — 03-12; **the selection box picture** — 03-13.
  The left drag still pans through `_begin_press` / `_panning` and `Look.DRAG_PAN_THRESHOLD_PX` is not
  touched or re-decided.
- **A right press on a 칸 the hand cannot reach keeps the hand.** That is the 2026-08-31 rule surviving
  on the button the order moved to, through `can_reach_block`; it is not re-decided here, and the
  let-go-on-nothing rule is the LEFT button's alone.
- **Keys or the band panning while a right button is held**: the order has gone out on the press, so
  nothing they do afterwards can touch it; the band is gated by the rule that a held button holds it
  still, the keys are not gated, exactly as for the left button today.
- **The three defects 03-14 holds** (a 부대 split across walking components, a full upper 칸 seating
  downstairs, the one-frame preview gap).
- **`Blocked by: 10`** on this ticket and the map row — `wrap-up` writes those; 03-10 is resolved on the
  map.
- **03-12's stale step table** (「the only button that opens a press」) — that ticket's own plan
  rewrites it.
- **Translating `docs/design/README.md`** — one English row joins a Korean table; the table is not this
  ticket's.
- **Nothing is drawn**, no `look.gd` change, no `verify-look`.

## Answer

**Built 2026-09-02. `net_shell` went `248 → 326 passed`, the one red both times is the pre-existing 「몸마다
자리·색·크기…」 row, and the whole suite sits on the roadmap's four reds with none new.** Nothing is drawn
and the game was not launched — the ticket moves a job from one mouse button to the other, and every
row is measured headless through the real `_unhandled_input`.

### What was built, in plain words

- **The right button orders, and the order goes out on the PRESS.** A right press on a lit 칸 sends every
  body in the hand to that 칸 in the same frame the button goes down, then empties the hand
- **The right release does nothing.** It closes the gesture and reads no position, no threshold, no pan.
  A body re-picked between the two edges is still held after the release and has no order
- **There is no threshold on the right button.** 60 px of travel and a D pan between the edges leave the
  order aimed at the 칸 under the DOWN point, because it had already gone out
- **The left button no longer orders.** A left press-and-release on a drawn body picks it (a full hand
  re-picks), and a left press-and-release on bare ground with a full hand **lets go** — the user's
  2026-09-02 reversal of 「ESC 아니면 부대를 안 놓는다」 ("no ESC, no letting go of the 부대"). ESC still lets go
- **A right press on a 칸 the hand cannot reach keeps the hand** — water, a dark 칸, off the board.
  The 2026-08-31 rule survives on the button the order moved to
- **The two buttons do not share state.** `_press_at` / `_press_open` / `_panning` are the left button's
  and `_order_open` is the right's, so both can be down at once and neither loses the other's job
- **The band holds still under either held button**, and the keys keep panning under a held right
  button — the order is already gone, so nothing they do afterwards can touch it
- **A lost 성채 and the way back to the title drop every open gesture**, right button included
- **The board never turns** under any of it — `cam_yaw_deg` is read across every row

### The numbers

- `net_shell` before any edit: **248 passed · 1 failed** (the pre-existing red)
- `net_shell` after the shell change alone, before the net rewrite: 240 passed · 9 failed — all eight new
  reds inside the four functions the plan rewrites, nothing red elsewhere
- `net_shell` after the net rewrite: **326 passed · 1 failed** — the same pre-existing red. **+78 rows**
- Whole suite, builder: 2519 passed · 4 failed, run twice on one fingerprint. Whole suite, verifier:
  **2641 passed · 4 failed**, run twice on fingerprint `AF759F834089` — the count moved between the two
  because 07-01 was being built in the same tree and its nets grew; **the four reds are the same four by
  name in both**: `net_boats` 「선체가 못 서서 버린 해변이 7 곳이다」 (seven beaches discarded because a hull
  could not stand), `net_fx_view` ×2 (the wolf at 16.0 px at that zoom; the walk texture row),
  `net_shell` ×1 (the pre-existing row above)
- 07-01's three nets (`net_fight` 419, `net_islands` 21, `net_tiers` 110) were all green, so nothing was
  re-run as possibly the other ticket's

### The inversions of Order step 3, each landed once and each restored

| # | Mutation | Red | What caught it |
|---|---|---|---|
| 1 | delete the `MOUSE_BUTTON_RIGHT` branch | 19 | every order row in net items 1 · 2 · 3 · 4 · 6 |
| 2 | order moved to `_end_order` from a stored 칸 | 18 | every press-alone row, item 1's "release orders nobody a second time", the `far_at` / D-pan row |
| 3 | order on BOTH edges | 4 | item 1's second-time row (the bait walked) and its `far_at` row, each with its "hand kept" twin |
| 4 | release-edge order from `_pointer_at` | 2 | item 1's `far_at` / D-pan row and its "hand kept" twin |
| 5 | drop `_order_open` from the `_edge_pan_dir` gate | 1 | the band's RIGHT stillness row |
| 6 | drop `_press_open` from the gate | 2 | the band's LEFT stillness row and its same-조각 row |
| 7 | delete `_let_go()` in `_press_the_island` | 8 | the let-go rows in items 1 (`dest_at`, `off_at`, `dark_at`), 2 and 4 |
| 8 | **narrow the let-go to the reachable arm** | 4 | **exactly item 1's two miss rows** (`off_at` ids + reach, `dark_at` ids + reach) |
| 9 | **swap the two arms of `_press_the_island`** | 3 | **item 2's same-body pair and its other-body row** |
| 10 | route the right press through `_begin_press` / `_end_press` | 23 | every order row plus the right-button camera / hover / `_panning` rows — the right press had become a pan |
| 11a | delete `_drop_the_gestures()` in `_back_to_title` | 1 | item 7's back-button row |
| 11b | delete `_drop_the_gestures()` in `_pan_the_board` | 3 | item 6's flags row and its two dead-board `_order_open` rows |

**The contrast the Acceptance asks for:**

- **Inversion 8**: the `dest_at` let-go row and the let-go rows in items 2 and 4 **STAYED GREEN** — a
  narrowed let-go still lets go on a lit 칸. Only the two miss rows went red. That is why those two rows
  exist: a let-go that only fires where the hand can reach is indistinguishable from the rule on a lit 칸
- **Inversion 9**: **every other row in the file STAYED GREEN** — the swapped arms are only visible where
  a full hand presses a drawn body, and item 2's other-body row needed the hand refilled through
  `hand.pick` before its press to redden on its own (the same-body row above it had already emptied the
  hand otherwise). That refill is one row the plan did not name and is written here

Inversions 2, 3, 10 and 11b reddened more rows than the plan predicted; each extra red is a downstream
row of the same mutation (a "hand kept" twin, a camera row once the right press became a pan, a
dead-board `_order_open` row), none a row outside the mutated behaviour.

### What the verifier measured on its own

- **Five mutations** of `game.gd`, each restored and the file's SHA-256 checked back after every one:
  delete `_order_open = true` → 5 red; **delete the reach guard → 0 new red (see below)**; swap the two
  arms → 4 red and nothing else; move the order to the release edge with a stored 칸 → 17 new red,
  including the row "on the press it already went to that 칸 — before the 60 px arrive"; narrow the
  let-go → exactly the two miss rows red, every lit-칸 let-go row green — the contrast above, observed a
  second time
- **A throwaway probe** through the real `_unhandled_input`, deleted afterwards: right press → Q → right
  press ordered the 칸 under the recomputed point after the notch settled; press then 60 px then release
  ordered block 17 on the press, the release point resolved to a different 칸, orders stayed 1 and the
  re-picked bait stayed held; a right press on an unlit 칸 and off the board with a full hand left ids and
  reach unchanged and ordered nobody; an empty hand's right press ordered nobody; a right press with D
  held ordered the 칸 under the cursor at that instant while the key pan kept running; a right press held
  when the 성채 fell was dropped and stayed dropped across the back-to-title click and the swallowed release
- **A read pass**: water and off-board reach `_order_the_island` as block -1 and stop there; the DOWN edge
  cannot fire at the title or on a lost board; `_press_the_island`'s new return value reaches nobody;
  the fork doc opens with `Status:`, keeps the user's Korean beside its translation, and the design
  README's heading counts seven

### ⚠ One hole in the net, left open on purpose and named here

**Deleting the reach guard in `_order_the_island` leaves `net_shell` byte-identical to the clean run.**
With it gone, a right press on water, a dark 칸 or off the board falls through `hand.order` (which
refuses on its own) into `_let_go()` and silently drops the 부대 — the exact rule the Out of scope says
survives. **The code is right** — the verifier's probe measured ids 1→1 and reach 272→272 on both miss
classes — **but no row drives it**: every `_rpress` in the file aims at a reachable 칸 or fires with an
empty hand. One row (a full hand, `_rpress` on `dark_at` and `off_at`, ids and reach unchanged, nobody
ordered) would close it; the two miss points are already searched a few lines above for the LEFT button.
**Not added in this round because it is outside the plan's eight net items**, and it goes to the map as
its own line rather than being slipped in under a resolved ticket.

### What was NOT done

- **03-12 — the left press-vs-drag split and box selection.** The left drag still pans through
  `_begin_press` / `_panning`, and `Look.DRAG_PAN_THRESHOLD_PX` is untouched
- **03-13 — the selection box picture.** Nothing is drawn by this ticket
- **03-14's three defects** (a 부대 split across walking components, a full upper 칸 seating downstairs,
  the one-frame preview gap) — untouched
- **The reach-guard row above** — a hole in the net, not in the code
- **A minor net-shape nit**: item 2's self-check row ("there is another body on the glass that resolves
  on its own") promises to skip the two rows under it when no second body is found, but it is a hard
  assertion; on this island it finds one, so it passes today
- **Render cost** cannot be measured headless and the ticket has no verify-look, so nothing is handed on
- **`Blocked by: 10`** on this ticket and the map row are `wrap-up`'s to write
- Nothing in `src/sim/`, `src/view/`, `src/look.gd` or any other net was touched; the temp inversion
  scripts lived outside the repo

## Reversed 2026-09-02, the same day

**The right button does nothing again.** After this ticket was built and closed, the user tried the drag
in the selection-box lab and reversed the split in one sentence:

> 「동작이 단순해야함 왼쪽으로 드래그 왼쪽으로 칸누르기로 해야할듯」
> *"The actions must be simple — left drag, and left press on a 칸."*

- **The order went back to a short left press on a lit 칸**, with the left drag boxing the 부대 — ticket
  **03-12** built it the same evening
- **The code this ticket added was written onto with tombstones, not deleted**: the `MOUSE_BUTTON_RIGHT`
  branch, `_begin_order`, `_end_order` and `_order_open` are gone from the shell, and the name
  `_order_open` survives in a comment that a net row asserts
- **The let-go-on-nothing rule this ticket built was reversed with it**: a full hand's press on dark
  ground, water or off the board keeps the hand again (the user: 「추천대로 ㅇㅇ」 — *"As recommended,
  yes."*). The fork doc `docs/design/a-left-press-on-nothing-keeps-the-hand.md` carries the second
  reversal on its `Status:` line
- **This ticket's Status stays `resolved`** — what it built was built and measured; what happened next is
  03-12's record. **Read 03-12's Answer for what the buttons do today**
