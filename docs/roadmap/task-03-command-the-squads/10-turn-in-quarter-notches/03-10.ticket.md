# 03-10 — Q and E turn the board a quarter, and the right drag stops turning it

Ticket: 03-10
Name: Q and E turn the board a quarter, and the right drag stops turning it
Task: 03 — 부대를 조종한다
Type: task
Status: claimed
Blocked by: —

## What this ticket builds

**Q and E come back and turn the 판 in 90° steps, and the right button's yaw drag is deleted in the
same edit.** After it, the board stands at exactly one of **four** yaws — **0, 90, 180, 270** — and the
right button never moves the camera again, which is what frees it to carry the move order in 03-11.

⚠⚠ **TWO REVERSALS IN ONE TICKET AND BOTH ARE THE USER'S.** Neither old line is erased; the new one
goes on top of both.

| What is reversed | When it was decided | The user's own words |
|---|---|---|
| **Q and E were deleted**, and they turned **15°** a notch | 2026-08-31 | *"Remove this QE feature"* (「QE 이거 기능제거해줘」) |
| **The right-drag yaw** — itself born 2026-08-26 and **re-taken 2026-08-30** | 2026-09-02 | *"rotating with the right mouse does not really have any advantage"* (「오른쪽 마우스로 회전을 하면 뭔가 장점이 별로 없어서」) |

**Deleting the right drag is reversing a reversal**, so `game.gd`'s two blocks that record the
2026-08-26 birth and the 2026-08-30 re-taking are rewritten with the new line on them, not cut.
⚠ **The user named Don't Starve for the 90° feel.**

### What the camera actually reads, measured in code

| What | Number |
|---|---|
| **Opening yaw** | `Look.CAM_YAW_DEG` **0.0** |
| **The new notch** | a NEW `Look.CAM_YAW_SNAP_DEG` **90.0** |
| **The old notch, and it STAYS** | `Look.CAM_YAW_STEP_DEG` **15.0** — the shot tool turns the camera by it |
| **The drag's rate being deleted** | `Look.CAM_YAW_PER_PX_DEG` **0.18** — a 60 px drag turns **10.8°**, a 1280 px one **230.4°** |
| **Everything that reads the yaw** | **three call sites** — `_ground_right`, `_ground_down`, `_place_camera` |
| **Everything that WRITES it in `src/`** | **two** — `turn_by`, which does `fmod(yaw + deg, 360.0)`, and `setup()`, which resets it to `Look.CAM_YAW_DEG` when an island opens |
| **Shader uniforms carrying a yaw** | **zero across all four** — water, pads, prop card, body outline |

⇒ **Four exact values are a CONSEQUENCE of the deletion, not a second rule.** ±90 is exact in float, so
once the drag is gone nothing can put the yaw anywhere but {0, 90, 180, 270} and nothing drifts.

⚠ **Anything that TURNS calls `turn_by`, never writes the yaw field.** `turn_by` holds the ground point
at the middle of the screen and re-clamps; writing the field raw skips both, and that raw write is the
exact mutation `net_camera`'s turn row already exists to catch. ⚠ **`setup()`'s write is not a turn** —
it is the opening reset, and the row that already asserts it (`setup` returns the yaw to `CAM_YAW_DEG`)
must stay green after this ticket.

### ⚠ The one thing the right drag was quietly holding

Its motion branch ends in an early `return`. Deleting the branch also un-skips the **hover plate** and
the **이동선**, which both sat below it and **froze for the whole length of a turn**. That is a fix this
ticket gets for free, and it is worth looking at rather than assuming.

## Screen

**No new asset is made.** The picture is the authored Blender island seen from a fourth of a turn
further round — the mesh, the trees, the 성채 and the 계단 are all already there.

⚠⚠ **What IS new on screen is every body changing which way it is drawn.** `_facing_index` picks a
검사's or a 늑대's sprite by dotting **that body's own heading** against the screen's right and down
axes, and it swaps when the two dots cross — **45° between the body's heading and screen-right.**
**Both rows in `BEAST_TEX` carry four pictures**, so a 90° notch moves every body across **exactly one**
boundary. ⚠ **Not all at the same instant.** The boundary is at 45° in the body's own heading, not at a
yaw of 45°, so a board full of bodies walking different ways flips them at different moments.

## Done when

**One press of E turns the board a quarter and the island is looked at from the new side.** These are
the rows and the shot that say so.

### The four values

- After any number of Q and E presses, `absf(fmod(cam_yaw_deg, 90.0))` is **< 0.001** — never 15, never 10.8
- **Four E presses return the yaw to 0.0**, and the existing row that wraps 300 + 60 to 0 stays green
- **A held Q does not spin the board**: an `echo` press adds **0°**, the same guard the tilt keys carry

### The turn holds the island still

- Across a 90° step the **ground point at the middle of the screen moves < 0.01 px**, the zoom does not
  change, and the real camera's look target does not move — the drift-count row that already asserts
  this at accumulated yaws **15·45·90·165·300**
- ⚠ **That row's own steps are 15·30·45·75·135 and none of them is 90.** It reaches yaw 90 but has
  never taken a 90° step, so **a 90 has to be added to its list** — the plan calling it "the row that
  already proves an instant 90° holds the centre" is reading the label, not the loop

### The right drag is gone, and gone means measurably gone

- A **60 px right drag turns the board 0.00°** — it turned **10.8°** before
- `_turning` has **zero writes left** in the shell; today the right arm is its only one
- On a motion with the right button held, the **hover plate and the 이동선 both still update** — they
  did not, because of the deleted early `return`
- ⚠⚠ **The net function that asserts 「the right button never commands」 certifies nothing today.** Its
  two `ordered == 0` rows sit over an **empty hand** — nothing in that function ever picks a body, and
  the function before it ends on ESC. **They must pick a body first or they stay green after the right
  button starts ordering.**

### ⚠⚠ The one number a screen has to judge — INSTANT or ANIMATED

**Both sides, and the user picks by looking:**

- **INSTANT (0.0 s)** — the sprite flip is **one frame**. Nothing interpolates the yaw today and there
  is no tween anywhere near it, so this is also the smaller change
- **ANIMATED** — Don't Starve, which the user named, does animate it. The plumbing exists: the camera
  is placed every frame from `_process`, so a per-frame `turn_by(step)` needs nothing new. ⚠ **The
  cost is that the sprite crossing becomes visible** — every body flips its picture somewhere inside
  the sweep, each at its own heading's 45°, which reads as the bodies turning rather than the board

⇒ **Done when the user has seen one of the two on screen and said which.** ⚠ **Not done when the model
picks one.**

### ⚠ What the code says and the plan does not

- **`Look.CAM_YAW_PER_PX_DEG` cannot simply be deleted with its shell reader.** The plan calls the
  shell its only reader; **`tools/look/piece_viewer.gd` reads it too, and that reader is live.**
  ⇒ Leave the constant standing with a tombstone, the way the 15° notch already stands for the shot
  tool. **Deleting it breaks the piece viewer.**
- The plan says a grep for a yaw in the view shaders returns nothing. **Three comment lines in the pads
  shader do mention a yaw** — they record a seam measured at yaw 0 and yaw 45. The conclusion still
  holds (**no shader uniform carries a yaw**), but the grep does not.

## Implementation plan

⚠⚠ **THE USER CHOSE ANIMATED, 2026-09-02**: 「즉시 돌 거 같아. 도는 것이 보여」 — *"it starts turning
right away, and the turning is visible."* ⇒ **the press begins the sweep on the same frame and the sweep
itself is seen.** ⚠ **This is the one number the ticket says a screen has to judge, so the shot below is
what closes it** — the flip to instant is one constant if the user looks and says no.

**Screen work, so the nets are written after the code.** ⚠ **This ticket lands AFTER 03-09** — both edit
the same two files, and the WASD/edge pan is what the board is panned with while the right drag is being
taken away.

### Seams

**`src/view/` through `FieldView.new()`** — `turn_by` and the yaw are pure and already driven that way by
`net_camera`. **`src/shell/` through `_unhandled_input`** for the keys and the dead right button.
**No new seam.**

### Files to touch and why

| File | Why |
|---|---|
| `src/look.gd` | A **new** `CAM_YAW_SNAP_DEG` at **90.0**, and the sweep's own duration. ⚠⚠ **`CAM_YAW_STEP_DEG` (15.0) STAYS** — four live readers outside `src/`, and its tombstone gets the reversal written onto it rather than being deleted |
| `src/view/field_view.gd` | The sweep: a target yaw, and `_process` walking `cam_yaw_deg` toward it through the existing `turn_by` |
| `src/shell/game.gd` | Q and E in the key branch; **the right button's two arms deleted** — the button edges, the motion arm with its early `return`, and the two drag fields. Eight prose blocks rewritten |
| `tests/nets/net_camera.gd` | A **90 added** to the drift-count row's step list, and the new sweep rows |
| `tests/nets/net_shell.gd` | The right-drag row rewritten to assert **0.00°**, and its two 「never commands」 rows **made to pick a body first** |
| `tests/nets/net_draw_leaf.gd` | ⚠⚠ **Its function table over `src/view/` is CLOSED.** Any new `field_view.gd` function goes in it or the net reddens |
| `tools/shot/shoot_pick.gd` | It pushes **three** `KEY_E` presses that have done nothing since 2026-08-31. Restoring the key silently makes that **270°** |

### Order

**1. `look.gd`.** `CAM_YAW_SNAP_DEG := 90.0` and a duration for the sweep. **Start at 0.22 s** — a
quarter turn fast enough to feel like a snap and slow enough to be seen; it is a first value to look at,
not a measured one, and the user's eye is what settles it. Rewrite `CAM_YAW_STEP_DEG`'s tombstone: its
「NO KEY READS THIS ANY MORE」 is still true — **the keys come back on a different constant** — and its
list of readers is one short, missing the piece viewer's two and the palette prototype's one.

**2. `field_view.gd` — the sweep.** A target-yaw field, and one step per frame inside the existing
`_process`, advancing through **`turn_by`** so the ground centre is held every frame by the code that
already holds it. ⚠ **`turn_by` is not edited.** When the remainder is under the frame's step the last
call takes exactly the remainder, so **the yaw lands on the target and not near it** — the four-values
rows below are what say so. ⚠⚠ **Every new function name goes into `net_draw_leaf`'s table in the same
edit**, or the round goes red for a reason that reads like broken code.

**3. `game.gd` — the keys in, the drag out, one edit.**

- **Q and E** sit in the key branch **below** the `if not key.pressed: return` gate and carry the same
  `echo` guard the tilt keys carry — Q is −90, E is +90. ⚠ **They add to the TARGET, not to the yaw**,
  so two quick presses turn 180 rather than one of them being eaten mid-sweep
- **The right button goes**: both button edges, the motion arm, the early `return`, and the two drag
  fields. ⚠⚠ **Deleting that `return` un-suppresses four things that were quietly frozen for the length
  of a right drag** — the hover plate, the 이동선, the left-drag pan threshold, and an in-flight left
  pan. **The first two are Done-when rows. The fourth is new behaviour nobody asked for**: a left pan and
  a right press can now run together. It is harmless because the right button no longer does anything —
  **and it stops being harmless the day 03-11 puts the move order on it**, which is why it is written
  here rather than found there
- **Eight prose blocks rewritten**, not deleted: the two 「both drags」 lines, `_press_orders`' 「the right
  button turns the board」, the 「left drag and right turn are the whole camera」 line, `_begin_press`'
  and `_end_press`' notes about the right button, the key-branch and wheel-header notes that say Q/E are
  deleted, and `field_view`'s note that the right-button drag writes the yaw

**4. The nets.**

- **The four values** — after any run of Q and E presses **and the sweep settled**, `absf(fmod(cam_yaw_deg,
  90.0)) < 0.001`; four E presses return to 0.0; an `echo` press adds 0°. ⚠⚠ **Each row pumps the
  sweep to a stop before asserting** — asserting mid-sweep is asserting a number nobody chose
- **The sweep is visible, measured** — one E press then **one** frame leaves the yaw **strictly between**
  0 and 90. **That single row is the whole of the user's choice**; it is the row that goes red the day
  somebody makes the turn instant
- **The turn holds the island still** — **add 90.0 to the drift-count row's step list.** ⚠ Its steps are
  15·30·45·75·135 and **none is 90**; its label names the accumulated yaws, so the row has never taken
  the step this ticket ships
- **The drag is measurably gone** — a 60 px right drag turns **0.00°** (it turned 10.8°); the shell has
  **zero** writes of the turn flag left
- **The plate and the line update under a held right button** — they did not, because of the return
- ⚠⚠ **The 「right button never commands」 rows are rewritten to PICK A BODY FIRST.** Today that function
  wipes every order and never picks anything, and the function before it ends on ESC — **so both
  `ordered == 0` rows run over a provably empty hand and would stay green after the right button starts
  ordering.** Fixing this is inside this ticket because this ticket is what makes them the only guard

**5. `shoot_pick.gd`** turns once, not three times. ⚠ Its three E presses were dead code that still ran;
restoring the key makes them 270° and its `4_turned` shot becomes a photograph of a board turned the
wrong way. **One line, and skipping it leaves a tool quietly lying.**

### Risk

- ⚠⚠ **`_facing_index` flips every body mid-sweep, one boundary each, at different moments.** The
  boundary is 45° between a body's **own heading** and screen-right, so a board of bodies walking
  different ways does not flip together. **This is the cost the user accepted by choosing animated**, and
  it is exactly what the shot has to show
- **`_process` now writes the yaw every frame while a sweep runs.** `net_camera` sets `cam_yaw_deg`
  directly in fifteen places and `net_fx_view` in four; **none of them calls `_process`**, so none is
  disturbed — but a net that starts calling `_process` after this will find its hand-set yaw walked back
  toward a target it never set. **The target field is initialised from the yaw in `setup`**, which is
  what keeps that from biting
- **The pads shader's measured seam note cites yaw 0 and yaw 45.** A four-notch board can no longer
  reach 45 by hand. **The measurement stands and the angle is no longer reachable** — write that onto
  the comment; do not delete a measurement
- **The right button is now completely dead in the shell.** ⚠ It is dead for exactly one ticket — 03-11
  picks it up — and a build that stops here leaves a mouse button that does nothing

### Acceptance

- Every row above green, and the suite **1567 passed · 4 failed or better**
- The existing yaw rows stay green untouched: `setup` resets the yaw to the opening angle; 300 + 60 wraps
  to 0; the wheel does not turn the board; the rider faces the screen and not the compass
- **A screenshot beside this file**: the same island at 0° and at 90°, and **one mid-sweep frame** — that
  third shot is what the user answers 「도는 것이 보여」 against

### Out of scope

- **The right button ordering anything** — 03-11
- **Any yaw but the four.** No free rotation, no snapping back from an arbitrary angle
- **The tilt keys**, the wheel, and the pan. 03-09 owns the pan
- **The pads shader's seam.** Only its comment is corrected

## Answer

*`build-loop` writes this section when the nets pass and sets `Status: resolved`.*
