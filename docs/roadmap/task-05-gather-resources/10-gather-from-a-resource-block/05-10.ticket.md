# 05-10 — 자원 칸을 눌러 캐러 보낸다

Ticket: 05-10
Name: 자원 칸을 눌러 캐러 보낸다
Task: 05 — 자원을 캔다 — 채집·농사·낚시·광석
Type: task
Status: open
Blocked by: —

**BUILT 2026-09-03, NOT VERIFIED.** ⚠⚠ **This ticket was written AFTER the code, which is backwards and
is said out loud here.** 05-05 settled WHERE the three come from and was closed as a decision; nobody
ever stood up a ticket for the building of it, so the build had no bar to be checked against. **This is
that bar.**

## What this ticket builds

**A press on a resource 칸 sends the 부대 to the walkable 조각 around it, and standing there is gathering.**

### Where it came from

> The user, 2026-09-03: ***"So the trees should be generated by 칸 unit too. Rocks and so on as well —
> that way when you press it and go to gather, they will go properly. Go there and it is gathering.
> Hover the mouse and the outline lights up, press it and it gathers — a bit like Warcraft."***
> (「그러면 그 나무들도 블록 단위로 생성되게 해야 되는데 ... 딱 눌렀을 때 채집하러 갔을 때 잘 갈 거 아니야.
> 거기 가면 채집이다. 딱 마우스 올리면 그 테두리 활성화되면서 이거 딱 누르면 그 채집하고 그렇게 살짝
> 워크래프트처럼」)

## ✅ What is decided

- **A resource 칸 is pressed like any other 칸**, and it is not lit — it blocks
- **The 부대 goes to the ring around it**, because gathering happens from beside (05-05: 「막힌다」)
- **Ten seconds a unit**, the same number fishing uses (05-05)
- **The 칸 never runs out** (05-05: 「계속 나와야 될 거 같아」)

## ⚠ Not decided — **the user answers before the next build**

⚠⚠ **「A 칸 whose trees are all cut」 collides with 「the 칸 never runs out」.** The user offered it as an
alternative feel — 「아니면 특정 블록에 있는 나무를 다 캔다」 — and the two cannot both be true. **The code
is on 「never runs out」, which is what was decided on 2026-09-02.**

**What the hover looks like.** ⚠ **Made, never typed** — the outline that lights up under the cursor is
a picture and needs a 시안 round. **Nothing is drawn for it today.**

**Whether a gathering body should be shown as gathering.** Nobody asked. There is no picture for it and
the five animations do not include one.

## ⚠⚠ What is already in the code, 2026-09-03

- **`Grid.resource`** — a board-sized layer filled by `Grid.set_resources` from the island's props.
  **A resource 칸 is an impassable, DRY 조각 carrying a prop**, which is what separates it from the drawn
  island's 54 pieces of 풍경 standing on walkable ground
- **`Battle._phase_gather`** — a body ashore, not walking, not fighting, with a 창고 standing, puts one
  unit in every `Rules.GATHER_SEC`: the 칸 beside it, or fish on the coast
- **`Hand.can_gather_block` · `Hand.gather_ring` · `Hand._gather_seats`** — the press, and the seats on
  the ring. The ring is filtered by `Grid.can_hold`, so a building's own 조각 is never offered
- **`net_gather`** carries every row

## Screen

**A resource 칸 whose outline lights under the cursor, and a 부대 standing around it.** ⚠ The outline is
not built and is a 시안 round of its own.

## Done when

- **A net presses a resource 칸 with a 부대 of three and asserts all three are seated on the ring, none on
  the 칸** — written, `net_gather`, **and it has never run**
- **A net walks one body there and asserts wood arrives after the gather time** — written, never run
- **The user presses a tree 칸 on screen and the 부대 goes and the count rises**

## Implementation plan

*Written before this ticket existed. See 「What is already in the code」 above.*

## Answer

*The two open questions above are the user's.*
