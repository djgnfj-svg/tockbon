# 05-11 — 자원 칸이 마르고, 시간이 지나면 다시 선다
Ticket: 05-11
Name: 자원 칸이 마르고, 시간이 지나면 다시 선다
Task: 05 — 자원을 캔다 — 채집·농사·낚시·광석
Type: task
Status: open
Blocked by: 10

## What this ticket builds
**A resource 칸 that empties.** Gather it out and the 칸 is gone — plain walkable ground — and 3 분
later the whole 칸 stands there again.

### Where it came from
> The user, asked which of two collided lines lives: *"I think it regenerates once time
> passes."* (「시간이 지나면 다시 생성될듯」)

**The collision it settles**: said 「계속 나와야 될 거 같아」 — the 칸 never runs out — and the
same user later said 「특정 블록에 있는 나무를 다 캔다」. **Both were written into 05-10 and only one can live.**

## ✅ What is decided
- **The 칸 runs out.** Gathering subtracts from it
- **It comes back WHOLE, not a bit at a time** — the recommendation, taken
- **3 분** after it empties — the recommendation, taken
- **While it is gone the 칸 is walkable ground**, not a blocked 칸

⚠ **Why whole and not gradual**: a half-grown 나무 would have to be drawn, and 나무 is a 3D mesh with
no such state. **Made, never typed** applies and there is no picture.

## ⚠ What is NOT decided — **the user answers before this is built**
**How much one 칸 holds.** Unasked. `GATHER_SEC` 10.0 and `GATHER_PER_TURN` 1 stand, so a 칸 of twenty
takes one body 200 초 to strip — near the 3 분 it then waits. **Nobody chose twenty.**
**Whether the 3 분 clock starts when the 칸 empties or when the last body walks away.**
**Whether a 칸 that is gone shows anything** — a stump, bare rock, nothing at all.
**Whether all three kinds share the numbers** — 나무 · 돌 · 철 have one rule between them today.

## ⚠⚠ What is already in the code
**Nothing subtracts from a resource 칸 and nothing counts what a 칸 holds.** 05-10 built the press and
the walk-to-it; the yield is endless. **This ticket is the whole of the reversal.**

## Screen
**The 칸 disappears when it is stripped and the ground under it is walkable**, then the 칸 is back.

## Done when
**A 부대 gathers a 나무 칸 until it is gone, walks across where it stood, and the 칸 is standing again
3 분 later.** Measured headless at the `src/sim/` seam, and looked at once on screen.

## Implementation plan
*`plan-into-ticket` writes this section and sets `Status: claimed`. It is not written by hand here.*

## Answer
*`build-loop` writes this section when the nets pass and sets `Status: resolved`.*
