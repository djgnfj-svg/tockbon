# Task 00 — `<one sentence the user could say out loud>`

Task: 00
Name: `<the same sentence, so the number and the name are never separated>`
Status: example
Chunk: `<the roadmap chunk this serves, and its mark>`
Days: `<how many — the user says this, never the model>`

⚠ **The file is named after its number** — `00.task.md`, not `TASK.md`. Every task would otherwise open
a tab called the same thing and none of them could be told apart.

⚠⚠ **This folder is the shape a task takes. Nothing in it is planned, agreed, or to be built.**

## What is on screen when this task ends

**One sentence.** *짐승이 오고 싸운다* · *부대를 쪼개고 합친다* · *포탑* — that grain, and no finer.

⚠ **If it takes three sentences it is two tasks**, and **where it splits is the user's call.**

## Which line it moves

**The core fun line at the top of the roadmap.** Name how this task moves it, in one line.
A task that does not move it is a task to say so about.

## Tickets

**One day each.** Only tickets that exist go in this table — **never a row invented to fill it.**

| # | The one day it is | Status |
|---|---|---|
| **00-01** | `<what one day builds>` | open |
| **00-02** | `<what the next day builds>` | open |

## What the user still has to answer

- **How many tickets this task is cut into, and in what order**
- **How many days it takes**, and which day each ticket lands on
- **Where this task stops and the next begins**

⚠⚠ **These are not the model's to fill.** `grilling` asks them; `wrap-up` writes the answers down.
