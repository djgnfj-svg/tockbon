# 00-02 — `<the next piece of the task, in a phrase>`

Ticket: 00-02
Name: `<the same phrase, so the number and the name are never separated>`
Task: 00 — `<what the task is>`
Type: grilling
Status: open
Blocked by: 01

⚠⚠ **This file is the shape a ticket takes. Nothing in it is planned, agreed, or to be built.**

**It is here to show two things the first ticket does not**: **`Blocked by:` naming a sibling by its
short number**, and **`Type: grilling`.**

## The question

⚠⚠ **A `grilling` ticket is a question only the user can answer**, and it holds no implementation plan.
It is written down so it is asked, not so it is guessed at.

- ⚠ **A question the user must answer never becomes a `task`** — it gets guessed at, and the guess
  reaches the screen before anyone notices it was a guess
- **`Blocked by: 02`** is how a later ticket in this task says it waits on this answer. ⚠ **The short
  number, not the two-tier one** — a `Blocked by:` line never leaves its own task

## Answer

*Written when the user answers. Then this ticket is `resolved`, and what it settled becomes the ticket
that builds it.*
