# 00-01 — `<one piece of the task, in a phrase>`

Ticket: 00-01
Name: `<the same phrase, so the number and the name are never separated>`
Task: 00 — `<what the task is>`
Type: task
Status: open
Blocked by: —

⚠ **The file is named after its two-tier number** — `00-01.ticket.md`, not `TICKET.md`. Task `04` also
has an `01`, and a tab called `TICKET.md` cannot tell you which one you are looking at.

⚠⚠ **This file is the shape a ticket takes. Nothing in it is planned, agreed, or to be built.**

**A ticket is a folder, and this file is its description.** ⚠ **What the ticket produced lives beside
it, in this same folder** — the screenshot `verify-look` took, the note a probe left, the numbers a run
measured. **That is what says how the ticket went**, and it is why a ticket is not a single file.

## What this ticket builds

**One paragraph at most.** What is different when it is done.

⚠⚠ **A ticket is NOT a day** (2026-09-01, the user: 「티켓은 여러 개여도 돼. 태스크가 두 개라는 거지.
주당」). **Two tasks a week; a task carries as many tickets as the work takes.** ⚠ **Where it splits is
still the user's call, never yours.**

## Screen

**What the player looks at, and what it is made in.** `verify-look` compares screenshots against this
section. ⚠⚠ **Anything the player looks at is made in a tool and loaded** — Blender for a shape in the
world, the pixel tools for anything flat.

**Where nothing is on screen, write `none` here** — that is a measurement, not an omission.

## Done when

**What you look at to know it is done.** Nets and review consume this.

## Implementation plan

*`plan-into-ticket` writes this section and sets `Status: claimed`. It is not written by hand here.*

## Answer

*`build-loop` writes this section when the nets pass and sets `Status: resolved`.*
