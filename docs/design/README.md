# docs/design index — what is designed, and what actually runs

This folder holds **concepts**. Unlike `docs/plans/`, **nothing moves between folders** — a concept stays alive.
But **every doc carries "how much of this is built" in its header.**

**Why carry status**: without it, **being written reads as being present.**
This project has been reset twice, and both times a doc kept describing a game that no longer existed. If that gap is recorded nowhere,
the next person (and the next session) **plans on top of something that doesn't exist.**

## Two separate axes — `Implemented` and `Accepted`

The distinction `CLAUDE.md` pins down holds here too. **Implementation finished ≠ acceptance passed.**

| Axis | Value | Meaning |
|---|---|---|
| **Implemented** | `none` | Doc only. Not one line of code |
| | `partial` | Some of it runs. **Write in the doc what does and doesn't** |
| | `full` | Everything the doc describes runs |
| **Accepted** | `unseen` | The user has never confirmed it on screen |
| | `pass (date)` | The user looked and said yes |
| | `fail (date)` | They looked and it wasn't. **Write the story in the doc** |

**`Implemented: full` + `Accepted: unseen` is a normal combination.** Merge those two into one column and
"it runs but nobody has looked" becomes inexpressible — this repo got burned exactly there.

## Header format

Goes directly under `# Title` and `**One line**:`.

```markdown
**Implemented**: partial — circles 1/3 · runes 2/N · glyphs 2/17
**Accepted**: unseen
```

For `partial`, **always attach what works and what doesn't, briefly.** "Partial" alone carries no information.

---

## Index

⚠ **The first vertical slice is built and runs** — three islands, one run, end to end, with the twelve
presentation items on top. **Each row below carries its own status**; the GDD stays `Implemented: partial`
because the slice built a straight-line subset of it, not the whole design.

**These docs are English, one file each.** The Korean twins were deleted on 2026-08-19 by the user's
decision — every twin was verified to carry the same facts and the same section structure first.
**Chat with the user stays Korean.**

| Concept | Implemented | Accepted |
|---|---|---|
| **[The cell army GDD](cell-army-gdd.md)** — **the live GDD.** One line: **「먹을 것을 고르러 간다」** (*going out to pick what to eat*). An autobattler on a node map of islands. A squad of square cells lands **by boat, on the coastline**; combat is automatic; **soldiers carry across islands, HP included, and a dead one is dead for good.** Reward axes are three and an island gives one: **세포 · 오브젝트 (a part bolted onto one of five slots) · artifact (bound to the whole army)**. ⚠ **The first two were replaced by the user on 2026-08-18** — they read 「count · 특산물 bolted onto one soldier」 until then, and 1~5 summoned by unit type rather than by slot. See `session-loop`. ⚠⚠ **Later on 2026-08-18 the user deleted the 1~5 summon keys outright and moved every combat decision in front of a start button** — its *Controls* section is overturned, undecided 8 (real-time vs pre-set) reverses to **pre-set**, undecided 2 (intervention after the drop) closes at **none**, and what an object bolts onto is open again. See `plan-then-watch`. Loss condition is a **time limit** | **partial** — the straight-line slice runs (3 islands, 2 soldier types, 3 enemies, the beak, boats, permadeath, HP carry). **Not built**: chest/elite islands, artifacts, map branches, meta unlocks, tiers/ramps/flying, fog, a recovery path | **partial** — the user played it twice. 2026-08-17: the presentation passed, **the game did not** (*"침공하는 느낌이 전혀 없어서"*). 2026-08-18, after the boat round: ***"참 애매하네. 그래도 그동안 중에서 제일 평범하네."*** 「미정」 15·16 are closed; **18 (「애매하다」) is the open one**, and 17 (2D/3D) was not raised — which is not closed |
| **[Twelve pieces of combat juice](combat-juice.md)** — **the twelve effects the user picked after playing the first slice**, because *"액션을 보는 맛이 있어야 돼"*. Its heart is one rule: **the sim reports only what happened (a one-frame `Battle.events` list of three kinds), and every duration, colour and pixel is the view's.** Carries the full closed hook table (**68 functions, 20 leaves when it shipped; the tree is now 77 / 21** — `boat-and-landing` deleted `_paint_boat` and added four leaves, and both twins carry a superseding box saying so), the draw-layer order, the shell's `_hold_sec` pseudocode, all **44** new `look.gd` timing and size constants plus **7** colours with their pixel conversions, and a per-item "what must redden when this breaks" table where **every row has a floor as well as a ceiling**. ⚠ **Item 2 is two layers, not one** — the user corrected it after the doc was finished (*"이팩트가 있어야 할 듯"*), so the body's lunge (2①) now carries **a hit spark thrown out of the contact point (2②)** with its own leaf, constants and net rows. ⚠ **All twelve are closed — the user chose to add the lion's wind-up (2026-08-17), knowing it weakens the boss.** ⚠ **Boats, terrain and 3D are out of scope — the user deferred them in the same conversation**, and juice fixes none of that layer. **Its seventeen refutation boxes are the most valuable paragraphs in it** — each one disproves an earlier claim of its own with arithmetic (a quotation that did not exist, "no window stretch" against `project.godot`, a lunge that swallowed bodies, "forty lines" against six, a gait amplitude under its own visibility floor, and **a shard fan that would have been drawn inside the attacker's own body**) | **full** — all twelve plus the lion wind-up; the round was **9 nets / 725 checks** green when it shipped, and is **12 / 1328** now. ⚠ **Item 8 was relocated on 2026-08-18** — its key half became the start button's chip family and its berth half was deleted with the fleet | **pass (2026-08-17)** — the user played it: *"연출은 좋아."* ⚠ **The presentation passed, the game did not** — see the doc header |
| **[The boat and the landing](boat-invasion.md)** — **carries GDD 미정 15** and **replaces the GDD's 「선착장」 rule.** The user played the slice and said *"배가 그냥 곁다리로 있는 느낌"*; with two dock tiles, *where to land* is a two-way button press. **Decided**: the whole unblocked coastline is landable · terrain gains a second level (cliffs are what "blocked" means) · a few types can land on a cliff · the player scrolls the camera during combat · island above, sea below, a long visible crossing · a bigger boat with its soldiers visible · boats have individual speeds. **Decided**: the whole unblocked coastline is landable · a second level, where **a cliff is simply a wall this round, but some cliffs carry ramps** · **a 48×32 grid** (closing GDD 미정 16) · **one control — drag a boat to the shore**, carrying which boat and where together · **drag-pan and wheel-zoom**, without which 48×32 cannot be surveyed at all · **two boats that differ in capacity as well as speed (4/slow and 2/fast)** · a bigger boat with its soldiers visible · **the clock is not retuned this round.** **Deferred by name**: mid-crossing redirect, hotkey loading as a feel item, a cliff-ignoring type, a clifftop watcher. ⚠ **Its two sharpest findings are both refutations of its own earlier drafts.** Enemies move only inside their detect radius while soldiers have none, so **the landing point already picks which enemies engage** — piecemeal is real in shipped code, the bigger grid raises its walk-cost 1.7× (a 44×28 diagonal is 13.0 s), and **it still does not bind**, so **the doc stated outright that the cost of an open coastline was not proven this round** — ⚠ **and the probe has since measured it: half a decision.** The landing point really does move casualties (island 3: **53.0 vs 79.0, a 49% spread**) and the fleet really does relocate, **but nothing makes the player pay** — all twelve controlled runs won inside **half** the limit, avoiding detection bought **zero** HP on island 2 (26.0 either way), and **"land next to a harbour" dominates 3 of 3.** ⇒ **a clock failure, not a coastline failure**; see the doc's section 1-A. And **capacities of 4+2 deleted the "roster over 10" threshold**: a wave is 6, not 10, so the boat limits from island 1 and crossings go from 6.7% of the clock to 20%. Also derives `speed_fast < 2 × speed_big` as the acceptance condition (a 2× fast boat exactly ties), names the four code changes per-boat boats need, and finds a rule the screen never shows — **fire into a boat concentrates on one soldier, and a TRANSIT body is never drawn** | **full (2026-08-18)** — six stages, the round is **11 nets / 967 checks** green, 59 mutations run by two independent verify-read passes, and the probe rewritten and run. Plan: `boat-and-landing` | **partial (2026-08-18)** — the user launched it and looked. ✅ **「배가 곁다리다」 did not come back** — the one sentence this doc exists to answer. ⚠ **But the verdict was *"참 애매하네. 그래도 그동안 중에서 제일 평범하네."*** — this doc hit its target and the game still has not landed. verify-look never ran; the user's own eyes replaced it |
| **[The session loop](session-loop.md)** — ⚠⚠ **half of it died later the same day; read its top refutation box before anything else in it.** The 1~5 summon keys were deleted, which takes the floor out from under its central feature ("you design five slots" — there were five because there are five fingers), voids its whole arithmetic section's variables, kills its combat-bar screen section, and **reverses its decided 10 ("the boat is not fixed") — the boat is now the centre.** What survives: refit and evolution as screens, the 15-kill count (reread as *how many things there are to plan against*), the dormant "ten cells cannot win island 1", and `panel_view` painting a red 「패배」 over any new run state. See [plan it, then watch it](plan-then-watch.md). Its original subject — **what one run is, between the islands.** Written after the user played the boat round and said *"참 애매하네. 그래도 그동안 중에서 제일 평범하네."*; the diagnosis was that **the part loop is built and everything above and below it is missing.** ⚠ **It renames the three loops to the user's own names** — outside in, **main → session → part** — and both GDD twins were edited to match. **Decided by the user**: eating yields **cells**, not soldiers · objects drop occasionally · **you design slots `1`–`5`** in the refit time between nodes, and pressing a key summons that slot's template for cells · **each object bolted onto a slot makes that slot more expensive** · it must read as a cell **evolving**, not equipping · a **Slay the Spire node map** · far fewer glyphs and larger targets on every screen · **variety through combination** (a family of objects unlocks a family ability) · **the boat is not fixed, so nothing here depends on its shape**. ⚠ **It weakens the GDD's own one line**: the user rejected *"what you ate becomes the build"* on level-design cost, so **"the path is the build" is dead** and the verb "eat" no longer carries the differentiation alone — the fork is recorded in [the build is designed, not inherited](../decisions/build-is-designed-not-inherited.md). ⚠ **Its arithmetic refutes itself twice before it lands**: a linear "each object costs a fixed amount" makes one extreme strictly dominant either way (**not a decision**), and even a convex cost yields a single interior optimum (**a calculation, not a decision**) — what saves it is objects being **axes rather than power**, plus a family unlock that pays for concentration against a cost that punishes it, **and only while objects stay scarce** (derived: total drops per run < slots × family threshold). **Two open questions would invert all of it** — if a boat's capacity counts bodies, stacking dominates whatever the curve; and if the clock does not bind, cells are not scarce either | none | **nothing chosen** — every number in it is derived, none measured |
| **[Plan it, then watch it](plan-then-watch.md)** — **the part loop (one island), replaced.** Written later on the same day as `session-loop`, after a conversation that moved every decision in front of the start button. **Decided by the user, ten rules and all ten quoted**: the hand does not move during combat · you lay the plan out on the map, drag boats onto a placeable region, set the order they go in, and **the plan is visible before you commit** · loading and placing are unrestricted before the start · **a pause exists and you can do nothing with it** · **no preview and no pre-run simulation** · get wrecked and you lose, that is all · *"losing is being wrong"*, so *when* you found out is not a design question · a fight is about a minute and **there is a speed-up** · **the 1~5 summon keys are dead** (the user asked *"why am I pressing a slot at all? is it because of the boat?"* and the answer was yes) · variety is parked. ⚠⚠ **Its sharpest section is the case against itself**: the two verified plan-then-execute games both hand the player what this refuses — **Frozen Synapse gives a pre-commit simulation, Door Kickers lets you switch back to planning at any time** — so **neither can be cited as evidence**, and the whole weight falls on the plan being readable before the commit. ⚠ **It also breaks [planning principles](../planning-principles.md) line 1 (「손이 놀면 안 된다」) on purpose**, taking dead air from 46–72% to **100%**, and says so head-on: three candidate payments (the hand works before the fight · the principle measured frustration rather than input rate · a fight is short) **none of which is proven**, plus **three replacement metrics because the dead-air ratio becomes a constant and stops being an instrument** — time until maximum speed is pressed · time spent planning · pause press count, **and all three are human numbers, so this design is not probe-verifiable.** Derived findings: the speed-up is `step(delta × k)` and **is not free** — a unit strikes at most once per `step` and resets the whole cooldown, so the rate loss is up to `dt/period` (**6.7% at 4×, 13% at 8×**, and asymmetric: the bison's 2.0 s period loses half of what everything else does) — and **the lion's 0.6 s telegraph falls under this repo's five-frame visibility floor past 7×, which sets the ceiling** | **built, all four stages** — the commit gate inside `step()`, unlimited boats created by a drag, `recall`, the sub-step and the 0/1/2/3/6× ladder, the plan drawn as ghosts · routes · rings · the army at the harbour, `ZOOM_MIN` 0.45, enemy counts **8 · 12 · 14** and `TARGET_LINE_MAX_COUNT` 14. **Deliberately absent: any brake on the boats, and any mother ship.** 12 nets / 1328 checks | **nothing chosen** — the user has not looked at a screen. Every 「user only」 row of the plan's acceptance table is open, the last one included |
| **[The title and the map](title-and-map.md)** — **the main loop: the frame around a run.** Written because the user launched the game and said *"지금 너무 그냥 떵하니 나와서 답이 없네"* — island 1 is on screen at frame 1 with nothing around it — and asked for **슬더슬식 노드선택 + 메인 시작 타이틀**, *"대충하지말고 제대로"*. ⚠ **Half its value is that it exists at all**: the title and the map were decided in conversation and written into no doc, so a plan scoped the map out and the user had to ask why it was not built. **Title**: three slots the user named — **시작하기 · 설정하기 · 종료** — and 설정하기 is the honest problem, since `src/` has no audio, no window mode, no input map and no persisted config, and `FX_GAIN` is a `const` Array that cannot be written at runtime ⇒ **recommended greyed, and left as an open item.** **Map**: **five floors, seven nodes, four routes**, branches that split and rejoin, the whole map visible always, node types **by shape AND colour AND size** — and only **three of the GDD's four** appear, because **an elite's two other rewards do not exist in code.** ⚠⚠ **The island shortage is decided, in the user's words** (*"섬은 그냥 네가 예시로 몇 개 더 만들어주고, 추 내가 차차 추가할게"*): **six grids are required because no two nodes may share one**, three exist, **three are hand-authored this round**, no generator — and because the user grows the set themselves, **cutting the cost of adding one becomes a design goal**: **nineteen** places today, **fourteen** of them demanding a measurement, taken to **four** by moving the reward onto the NODE, replacing the fifteen per-island expectation tables with literal floor-AND-ceiling invariants plus a per-island fingerprint, and adding the node-table row that an island needs in order to appear in a run at all. ⚠ **Its sharpest paragraph is a refutation of its own earlier draft**: that draft said a Slay the Spire map is a schedule of HP and **the probe had measured that islands here do not cost HP** — but that number predated the enemy raise, and **the probe re-run on 2026-08-18 says islands cost 27–77 HP apiece and two of five landing policies lose the run outright.** ⇒ the doc buys *"the map gives a run a shape"* AND, now, *"the fork asks what you are short of and how much HP you can spend"* — the second **conditional on the three new grids being as costly as the shipped three**, which only the probe can say once they exist. It carries an **HP schedule** node by node, and ⚠ **its conclusion was inverted on 2026-08-18 by the user deciding the chest pays an artifact, not a heal** (*"아티팩트 녗개중 선택"* — **four come up, you take one**): the cells-beak-beak route now passes the chest still on **43 pool against a wipe threshold between 61.5 and 84**, so **that route loses unless "army-wide HP up" raises CURRENT HP** ⇒ **the GDD's undecided 14 (the recovery route) is not closed — this map is the first thing to make it bite.** The chest section works out **what stops four artifacts being rankable** (the routes arrive on 115.0 and 43.0, a factor of 2.7), **why a pool of exactly four kills the choice on run two** (recommended eight, drawn four, `C(8,4) = 70`), the **four cards and their hit rects** (240x320, +8px, a 16px gap, all inside 1280x720), and states plainly that **artifacts do not exist in code and this round builds them**, because floor 4 is a fixed node every route steps on. Every pressable thing carries a named minimum size and contrast **as numbers** (hit rect ≥ picture + 8px, pressable alpha 1.0 vs non-pressable 0.30, node hit radius 48px against a **120px** minimum spacing — an (x, y) table for all seven nodes in 1280×720, because at 160px five floors need 748px and do not fit), because the previous round shipped a 10px grab target and a 0.18-alpha drop zone and **a human could not operate it** | **most of it — 14 nets, 1933 checks green.** `RunMap`, `map_view`, `title_view`, the node/edge tables, the shell's two new branches, `net_map` and `net_title`. ⚠ **NOT the three new grids**: the island column is still `[0, 1, 2, 1, 2, -1, 2]`, three grids across six island-opening nodes, and the probe measured that this makes the beak branch lose the run on its second node | **nothing chosen — the user has not looked at either screen.** ⚠ **what a chest gives is DECIDED** (*"아티팩트 녗개중 선택"*, user, 2026-08-18); **two open items wait on the user**: **the 「두 줄」 reading** and **what 설정하기 contains** |
| **[What makes placement a decision](what-makes-placement-a-decision.md)** — nine shipped games and the different rule each used to make **choosing a position** interesting, split by whether they need unit control after commitment. ⚠ **Bad North is not "place and watch"** — corrected from the developer's own words. The sharpest finding: **TFT and Despot's Game share a rule and land in opposite places**, and the only difference is whether units have abilities that respond to distance and direction | none | **nothing chosen** |

---

## What was deleted on 2026-08-17

**Eleven design docs were deleted**, all describing the two games that no longer exist (tags `v1-sim`,
`v2-openfield`): the previous GDD and its review, the stage ladder, the hunting and boss doc, the level curve,
the melee legibility study, the play notes, the presentation audit, the swarm diagnosis, the round-by-round
argument, and the magic-circle glyph doc.

**What they measured survives** in [what two dead games left behind](../lessons-from-two-dead-games.md) — measured numbers and repeating failure shapes, and **nothing in
it is a spec.**

⇒ **Read that file for shapes. Never recover a deleted doc from a tag as a spec.**
