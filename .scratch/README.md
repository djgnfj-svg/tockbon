# .scratch — 지도와 티켓이 사는 곳

**2026-08-22에 `docs/plans/`를 없애고 여기로 왔다.** `wayfinder`가 쓰는 자리이고, 그 스킬과 함께 들어온
로컬 트래커 규약을 그대로 따른다.

⚠ **이름이 「scratch」지만 버리는 곳이 아니다.** git이 추적한다. **계획은 여기서만 산다.**

---

## 생김새

```
.scratch/
  <일-이름>/
    map.md                    ← 지도 하나. 목적지 · 메모 · 지금까지의 결정 · 안개 · 범위 밖
    issues/
      01-<영문-이름>.md       ← 티켓 하나 = 질문 하나. **슬러그는 영문이다**
      02-<영문-이름>.md
```

**티켓 파일 맨 위 세 줄이 상태 전부다.**

```markdown
Type: grilling          ← grilling · research · prototype · task
Status: open            ← open · claimed · resolved
Blocked by: 01, 03      ← 없으면 이 줄을 뺀다
```

- **막힘이 풀렸다** = 여기 적힌 티켓이 전부 `resolved`
- **지금 잡을 수 있는 것(프론티어)** = 열려 있고, 안 막혔고, 안 잡힌 것. **번호 빠른 순**
- **잡기** = 일 시작 **전에** `Status: claimed`로 바꾸고 저장
- **풀기** = 맨 아래 `## Answer`에 답을 쓰고 `Status: resolved`, 그리고 **지도의 「지금까지의 결정」에 한 줄**

---

## 왜 폴더로 상태를 안 나누나

**전에는 `1.ready` `2.active` `3.done` 세 폴더를 옮겨 다녔다.** 그 색인이 스스로 적어둔 문장이
***「문서를 옮기는 건 세 군데를 고치는 일이고, 링크는 매번 샌다」***였고, **실제로 샜다.**

Kiro도 spec-kit도 이 트래커도 **전부 파일을 안 옮기고 파일 안의 한 줄로 상태를 적는다.**

⚠ **대신 약점이 생겼다.** 폴더는 `ls` 한 번으로 상태가 보이는데 `Status:` 줄은 **고치는 걸 잊어도
아무도 모른다.** `wayfinder`가 「일 시작 전에 먼저 바꾸고 저장하라」고 못 박아둔 게 그 때문이다.

---

## 지금 상태

⚠⚠ **지도는 하나뿐이다 — `island-hold/`.** 앞선 지도들은 2026-08-26 에 방향이 뒤집히면서 전부 지웠다.
**여기 없는 폴더 이름을 계획의 출처로 삼지 마라.**

| 지도 | 상태 |
|---|---|
| `island-hold/` | **살아 있다. 섬 하나를 지킨다, 12월 데모가 목적지다.** 티켓 셋, 전부 열려 있다 |

**티켓 셋**

| 티켓 | 질문 |
|---|---|
| `01-what-one-piece-is` | 판 한 조각이 무엇인가. **블렌더로 굽기 전에 읽는다** — 여섯 번 뽑아 여섯 번 불합격한 이유가 여기 다 있다 |
| `02-hands-move` | 부대를 어떻게 명령하나. **명령이 이 게임의 조작이다** |
| `03-the-body-moves` | 몸이 걷는다. 절반은 지어졌고 남은 것이 일곱이다 |

---

### 이 지도가 서 있는 방향 (2026-08-26)

**사람 회사가 섬 하나를 지키고 키운다. 짐승이 배를 타고 적으로 온다.** 플레이어는 **검사 하나**,
적은 늑대 · 곰 · 까마귀 · 사자다. **배는 적의 것이다 — 내가 놓지 않는다.** 판의 원천은 **블렌더**이고,
스크립트 하나가 메시와 좌표를 굽어 내면 게임은 그것을 읽기만 한다.

⚠⚠ **뒤집힌 것 셋 — 양쪽을 다 적어둔다.**

- **「12월은 데모다」(2026-08-22) → 「12월은 출시다」(2026-08-26).** 출시는 데모가 안 지던 것을 진다 —
  저장 · 옵션 화면 · 소리 · 스토어 페이지와 빌드 · 언어
- **「전투 중에는 손이 안 움직인다」 → 죽었다 (2026-08-25, Bad North 를 해본 뒤).** 후계 문장은
  **「어디에 세우느냐가 결정이다」** — 부대는 판 위에서 **언제든** 명령받는다
- **「내가 짐승이 되어 섬 여덟을 먹는다」 → 「사람이 섬 하나를 지킨다」 (2026-08-26).** 섬 여덟 · 지도
  화면 · 노드 걷기 · 노드 보상 · 종족 카드는 **코드에서 지워졌다**

**넘어온 틀 넷**: 12월 **데모** · 로그라이크 · 그다음 펀딩 · **다른 섬 약탈은 들어간다**.
⚠ 멀티플레이가 들어가므로 **시뮬레이션은 결정론을 지킨다**.
