# tockbon

## 응답 규칙

- 무조건 짧고 핵심만.
- 알아야 할 사항이 있으면 **마지막에** 최대한 깔끔하고 간결하게.
- 이모지를 쓰지 않는다. 강조는 굵게로만.

## 기획 문서

`docs/GDD.md` 가 게임 전체의 기준이다. **상태가 없고 계속 살아 있다.** 개별 기능 명세는 아래에 있다.

| 위치 | 뜻 |
|---|---|
| `docs/design/` | **개념 정리.** 폴더를 안 옮긴다. 계속 살아 있다 — 다만 **머리에 구현·판정 상태를 단다** |
| `docs/plans/1.ready/` | 기획 완료, 대기 |
| `docs/plans/2.active/` | 구현 중 |
| `docs/plans/3.done/` | 완료 |

⚠ **`docs/design/` 은 상태 이동을 하지 않는다.** 아래 세 폴더 규칙은 `docs/plans/` 만의 것이다.
🔴 GDD에는 **게임 쪽에서 보이는 얼굴만** 적고 세부는 `docs/design/` 을 가리킨다 — 겹쳐 적으면 갈라진다.

### 🔴🔴 기능 얘기가 나오면 `docs/design/` 에 문서를 남긴다 (2026-08-06, 사용자가 정했다)

**사용자가 어떤 기능을 설명하면 그 기능의 개념 문서를 `docs/design/` 에 만든다.** 대화에만 두지 않는다.

그리고 **모든 `docs/design/` 문서는 머리에 두 줄을 단다** — 형식과 값은 `docs/design/README.md` 가 기준이다:

```markdown
**구현**: 없음 | 일부 — <무엇이 되고 무엇이 안 되나> | 완료
**판정**: 안 봤다 | 통과 (날짜) | 실패 (날짜)
```

⚠ **이것은 「상태 이동」이 아니다.** 문서는 여전히 폴더를 안 옮기고 영원히 산다 —
달라진 것은 **「지금 얼마나 도나」를 문서가 스스로 들고 있다**는 것뿐이다.

🔴 **이유**: 개념 문서가 상태를 안 들면 **적혀 있다는 것이 곧 있다는 뜻으로 읽힌다.**
GDD에 문양이 열일곱 개 적혀 있는데 실제로 도는 것은 둘이고, 그 간격이 어디에도 안 적혀 있었다.
⇒ 다음 세션이 **없는 것을 있다고 믿고 그 위에 계획을 세운다.**

🔴 **문서를 새로 만들면 `docs/design/README.md` 의 표에도 한 줄 넣는다.** 목록에 없으면 아무도 못 찾는다.
그리고 **아직 문서가 없는 기능**도 그 README 아래쪽 표에 남긴다 — 빈 자리가 보여야 채운다.

한 문서는 `docs/plans/` 셋 중 한 곳에만 있다. 상태가 바뀌면 **파일을 옮기고** 문서 안의 `**상태**:` 줄도 같이 고친다.

🔴 **옮긴 쪽이 그 자리에서 세 폴더 현황을 표로 보고한다.** 옮겼다는 말만 하지 않는다.

```
1.ready/   <문서>  — 한 줄 요약
2.active/  (비어 있음)
3.done/    <문서>  — 한 줄 요약
```

⚠ **이유**: 문서 이름이 비슷하면(`spell-circle-minimum` ↔ `spell-pipeline-minimum`) 두 곳에 있는 게
「같은 문서가 두 번 있다」로 읽힌다. 실제로 그 오해가 한 번 났다. **한 줄 요약이 있어야 서로 다른 문서인 게 보인다.**

## 🔴🔴 눈으로 본 판정은 그 자리에서 문서에 적는다

**사용자가 「확인했다 · 보인다 · 달라졌다」고 말하면 그 말을 들은 쪽이 즉시 기획 문서의
「판정」 절 아래에 결과를 적는다.** 다음 턴으로 미루지 않는다.

```
### 결과 — <날짜>, 사용자가 눈으로 확인
판정 N 통과/실패. <무엇이 어떻게 보였나>
⚠ 확인 범위: 눈으로만인가 · 스크린샷이 있나 · 어느 판정은 안 봤나
```

⚠ **이유는 구조다. 대화는 안 남고 리포만 남는다.**
사용자가 세션에서 「확인했다」고 말해도 그건 **그 세션에만 존재하고 끝나면 사라진다.**
다음 세션은 리포만 보므로 **없던 일이 된다.**

🔴 **`3.done/` 에 있다는 것은 「구현이 끝났다」이지 「판정이 통과했다」가 아니다.**
둘을 구별하지 못하면 다음 단계가 **근거 없이 시작된다.**
⚠ 실제로 났다 — A(`spell-pipeline-minimum`)의 판정 1·2를 사용자가 확인했는데 기록이 없어
B를 시작할 근거가 비어 있는 상태였고, **사용자는 이미 말했다고 기억하고 있었다.**

🔴 **verify 에이전트의 보고도 같다.** `build-feature` 가 verify-look/run/read를 돌리면
그 결과는 대화에만 남는다 — **문서로 돌려놓는 것까지가 그 작업이다.**

**뼈 먼저, 살 나중.** 각 부분을 「돌아가기까지만」 만들고 넘어간다. ⇒ 기획 문서의 **「미정」을 구현 전에 다 채우라고 요구하지 않는다** — 미정이 남는 건 아직 그 차례가 아닌 것이다(GDD 「만드는 순서」).

## 폴더가 곧 계약이다

| 폴더 | 계약 | 형 |
|---|---|---|
| `src/sim/` | **정수 결정론.** float · `Vector2` · `sqrt` · `sin` · `randi` · `OS.` · `Time.` 금지. 씬 트리 모름 | `RefCounted` |
| `src/actor/` | float 허용(GDD 멀티 표: 플레이어는 호스트 권위). **씬 트리는 여전히 모름** | `RefCounted` |
| `src/view/` | 화면만. 시뮬을 **읽기만** 한다 | `Node` |
| `src/stage/` | 껍데기 — 틱 루프 · 입력 · HUD · 무대. **본편에 안 남는다** | `Node` |

**그물이 폴더를 재귀 스캔한다.** 파일을 새로 만들어도 자동으로 걸린다 — 등록 목록을 손으로 관리하지 않는다.

🔴 **정수 쪽으로 들어가는 문은 `src/actor/aim.gd` 의 `fire_cmd()` 하나뿐이다.** float px가 들어가고 정수 커맨드가 나온다.
연출 상수는 전부 `src/view/fx_tuning.gd`, 시뮬 상수는 전부 `src/sim/sim_tuning.gd` 에 있다.

## 주석

주석은 이 리포의 자산이다. 줄이는 게 목표가 아니다. **다만 자리가 틀리면 갈라진다.**

- **"왜 다르게 하면 조용히 죽는가"를 적는다.** "무엇을 하는가"는 코드가 말한다
- **실측은 그 자리에 남긴다.** 다시 재려면 비싸다
- **같은 설명이 두 파일에 나오면 한 곳으로 옮긴다.** 여러 곳에 걸리는 규칙은 `CLAUDE.md` 나 스킬로. 반복된 주석은 반드시 갈라진다
- **문서를 요약해 넣지 않는다. 가리키기만 한다.** 가리킬 문서가 없으면 그 참조를 지운다. 없는 문서를 가리키는 주석이 이 리포에 이미 여럿 있다

## 가짜 코드 금지

되는 척하는 코드는 안 되는 코드보다 나쁘다. 전부 금지:

- 이번 입력·이번 테스트에만 맞춘 하드코딩
- 계산하지 않고 그럴듯한 값을 돌려주기
- 스텁·TODO를 완성이라고 보고하기
- 에러를 삼켜서 성공한 것처럼 보이게 하기
- 화면만 바뀌고 시뮬은 안 바뀌기 (또는 그 반대) — 이 리포의 대표 가짜

못 하면 **"못 한다"고 말한다.** 막혔으면 막혔다고, 안 해봤으면 안 해봤다고 한다.

## 🔴🔴 가짜 그물 금지 — 검사가 재는 것과 라벨이 말하는 것이 같아야 한다

위가 **코드의 거짓**을 막는다면 이건 **그물의 거짓**을 막는다. **라벨이 재는 것보다 넓으면 그 초록은 거짓 보증이고, 없느니만 못하다** — 다음 사람이 그 줄을 읽고 「이건 지켜진다」로 믿는다.

한 기능에서 이 형태가 넷 나왔다:

- **값 단언이 우연히 맞는다** — 표의 값이 마침 2라 접근자를 `return 2` 로 박아도 `2 == 2` 다
- **사면 문자열이 넓다** — `expect_error("모르는 룬")` 이 `SpellSim:` 과 `SpellCircle:` 을 둘 다 덮어, 짖으면 안 될 곳이 짖어도 「깨끗함」이 나왔다. **사면은 짖는 쪽 접두사까지 좁게**
- 🔴 **그리고 사면의 수명이 무제한이다 — 폭보다 이게 더 넓다.** 러너가 stdout의 `[EXPECT]` 를 **먼저 다 모은 뒤** stderr를 그 목록 **전부**와 맞댄다 ⇒ **그물별·시점별·횟수별 범위가 하나도 없다.** 실측: 첫 그물에 위조 짖음을 넣고 선언은 **세 번째** 그물이 했는데 **초록**이었다. ⇒ **짖음에 기대지 마라. 흔적을 값으로 남겨서 재라**
- **「부르나」만 보고 「쓰나」는 안 본다** — 함수를 부르기만 하고 값은 안 쓰면 통과한다. ⚠ **텍스트 검사를 늘려서는 못 막는다.** 「그 값이 결과에 닿나」는 거동이고 텍스트는 문법만 본다
- **씬을 못 세우면 검사가 「실패」가 아니라 없어진다** — 조용히 `return` 해서 **통과 수만 줄고 아무도 안 짖는다.** 폴더 스캔의 첫 줄이 늘 「0개가 아니다」인 이유가 이것이다

⇒ **새 검사를 넣으면 뒤집어 봐라.** 안 뒤집어 본 검사는 「돈다」까지고 「잰다」가 아니다.

🔴 **그리고 뒤집기가 안 물리면 검사부터 의심하지 마라 — 뮤테이션이 실제로 들어갔는지 먼저 봐라.**
⚠ 한 세션에 **두 번** 났다: PowerShell 문자열 치환이 **패턴 0매치로 조용히 실패**했고 그물은 당연히
초록이었다. 「이 검사는 안 물린다」로 보고할 뻔했고, 그건 **가짜 그물보다 나쁘다** — 멀쩡한 검사를
지우게 만든다. ⇒ **치환한 줄이 실제로 바뀌었는지 확인하고 나서 그물을 돌려라.**

⚠ **그리고 `[그물] 통과 N개` 를 초록으로 읽지 마라.** `load()` 는 파스 실패해도 null이 아니라, `src/` 파일이 통째로 깨져도 「읽힌다」가 통과하고 **통과 수가 똑같다.** 잡는 것은 **래퍼의 마지막 줄 하나뿐**이고, 여러 번 돌려도 안 잡힌다.

## 에이전트를 부를 때 모델을 골라서 붙인다 (2026-08-07, 사용자가 정했다)

**`Agent` 를 부르는 쪽이 그 작업에 맞는 `model` 을 판단해서 넣는다.** 안 넣으면 부모를 상속한다 —
`.claude/agents/*.md` 다섯 개에 `model:` 이 하나도 없으므로 **지금은 전부 Opus로 돈다.**

| 작업의 성격 | 모델 |
|---|---|
| 판단이 결과를 바꾼다 — `spec` · `verify-read` · `verify-look` · 기획 대화 | **opus** |
| 계획이 이미 서 있고 그대로 옮긴다 — `builder`(계획이 촘촘할 때) | sonnet |
| 기계적이다 — 값 관측 · 파일 찾기 · 목록 뽑기 | haiku |

🔴 **싸게 하려다 검증을 낮추지 마라.** 이 리포의 대표 사고는 「돌아가는 척」이고, 그걸 잡는 것이
`verify-read` 와 `verify-look` 이다. **거기서 아낀 값은 가짜 초록으로 돌아온다**(위 「가짜 그물 금지」).
⚠ 반대로 **`verify-run` 은 값을 읽어 표로 옮기는 일**이라 낮은 쪽이 맞는 경우가 많다.

⚠ **표는 기본값이지 규칙이 아니다.** 작업이 표와 다르게 생겼으면 다르게 붙이고,
**왜 그렇게 붙였는지 한 줄 말한다** — 안 말하면 다음 세션이 표만 보고 따라 한다.

## 🔴 godot MCP는 화면을 보는 검증만 쓴다

`127.0.0.1:6550` 브리지는 **한 클라이언트만** 문다. ⇒ **`godot_*` 는 verify-look 전용이다.**

**나머지는 전부 헤드리스다** — `Godot_*.exe --headless --script` (`tests/run_nets.ps1` 과 같은 방식). builder · spec · verify-run · verify-read 전부.

⚠ **도구를 안 불러도 MCP 서버 프로세스가 스스로 브리지에 재연결한다**(실측: 세 세션의 godot-mcp가 세션 시작 시각에 다 떠 있었고, 에디터가 올라오자 도구를 안 부른 쪽이 포트를 잡았다). **「안 쓰겠다」는 다짐만으로는 부족하다.**

🔴 **그리고 사용자의 마우스·키보드를 뺏지 않는다.** 창을 포커스하거나 키를 주입하거나 OS 화면을 캡처하는 우회로는 금지다 — 사용자가 같은 컴퓨터를 쓰고 있다. 실제로 전역 키 주입이 포그라운드 잠금에 막혀 **키가 사용자의 Chrome으로 갔다.**

**브리지를 못 잡으면 거기서 멈추고 보고한다.** 「멈춰서 못 했다」가 「사용자 입력을 뺏어서 했다」보다 낫다. 못 본 것은 「못 봤다」로 문서에 적으면 된다.

🔴 **그리고 세션이 끝나면 자기가 띄운 에디터를 끈다.** 남겨 두면 다음 세션이 그 브리지를 두고 다투고 **사용자가 연결 허락을 반복해서 묻게 된다.** ⚠ 사용자가 띄운 에디터는 예외다 — 모르겠으면 끄지 말고 묻는다.
