# TODO — 앞으로 할 것

한 줄에 하나. 한국어로 쓴다. 할 때가 되면 티켓으로 옮기고 여기서 지운다.

- **레퍼런스를 태스크별 폴더로 나누고, 관리하는 담당을 둔다** — 지금은 `research`·`wrap-up`·`scout`·`prototype` 넷이 넣기만 하고 정리하는 쪽이 없다. 파일 29개 중 25개가 어디서도 안 불린다. (2026-08-30)
- **`docs/design` 문서 양식과 폴더 양식을 정한다** — 지금 공통된 건 `Status:` 첫 줄 하나뿐이고, 접은 갈래 여섯과 눈으로 고른 결정 열이 몸통이 다르다. 번호 붙은 파일 이름이 가리키던 티켓은 전부 지워졌다. (2026-08-31)
- **디자인 문서가 언제 쓰이는지를 정한다** — 스킬 다섯(`compass`·`grilling`·`build-loop`·`wrap-up`·`roadmap`) 중 이 폴더를 읽거나 쓰는 게 하나도 없다. 열 개는 2026-08-30에 손으로 한 번 옮긴 것이다. (2026-08-31)
- **`docs/design`은 한국어로 관리한다고 `CLAUDE.md`에 적는다** — 파일은 이미 전부 한국어인데 언어 규칙은 GDD만 예외라고 말한다. 낱말도 「결정」이 아니라 「디자인」으로 맞춘다. (2026-08-31)
- **결정 로그를 지울지 줄일지 정한다** — 사용자: 「내가 만든 게 아니어서 별로 필요 없을 거 같고 왜 있는지도 모르겠다, 너무 길다」. **1779 줄이고 열다섯 곳이 읽는다** — `CLAUDE.md` · `CONTEXT.md` · 지도 · 스킬 일곱 · 에이전트 하나 · 검사 하나. ⚠ 지우면 그 열다섯을 다 손봐야 하고, 사용자가 한 말이 전부 거기 있다. (2026-08-31)
- **지도의 「아직 태스크에 안 붙은 것들」 열여섯 줄을 태스크로 옮긴다** — 왼쪽 번호는 지워진 티켓의 것이라 죽은 참조이고, 무엇이 안 됐나는 그 표에만 적혀 있다. (2026-08-31)
- **`docs/개발지식` 폴더를 `CLAUDE.md` 의 문서 표에 한 줄로 넣는다** — 폴더와 스킬은 섰는데 `CLAUDE.md` 는 그 폴더를 모른다. ⚠ 그 파일은 사용자만 고친다. (2026-08-31)
- **실험대에 아직 안 붙은 기법을 채운다** — 01 은 28 중 16, 02 는 26 중 15, 03 은 26 중 13 만 스위치가 있다. 소리와 진동은 하나도 안 붙었다. (2026-08-31)
- **`CLAUDE.md` 의 언어 규칙에 예외를 하나 더 적는다** — `docs/개발지식/` 은 한국어다 (2026-08-31, 사용자: 「그 폴더는 한국어로 되어있고 내가 읽기 좋게 되어있는 거예요」). ⚠ **`CLAUDE.md` 는 사용자만 고친다.** (2026-08-31)
- **개발지식 그림을 다시 뽑는다** — 자리표시 png 아홉 장과 `그림.svg` 셋. ⚠ **파이썬으로 안 찍는다**, `tools/pixel/` 이나 pixellab 으로 만든다 (2026-08-31, 사용자). 지금 `그림.svg` 01·02 는 글자가 겹쳐 있다. (2026-08-31)
- **기법 여든 개에 이름을 붙인다** — `한국어(영어)`. 01 이 28, 02 가 26, 03 이 26. ⚠ **영어 이름은 밖에서 확인하고, 통용되는 게 없으면 「통용 이름 없음」이라 적는다** — 지어내는 게 `knowledge` 스킬이 막는 실패다. (2026-08-31)
- **실험대의 스위치를 손잡이로 바꾼다** — 값이 있는 기법은 ← → 로 돌리고 숫자를 찍는다. 지금 여섯 개 값(`12.0`·`1.10`·`Color(1.18,1.14,1.22)`·`3.2`·`0.35`·`0.02`)이 출처 없이 박혀 있다. (2026-08-31)
- **실험대 01 의 「기준점을 발에」가 몸을 땅에 파묻는다** — 켜면 판때기가 키 하나만큼 통째로 땅 아래로 간다. 2D 셈법을 3D 에 그대로 옮긴 것이다. (2026-08-31)
- **실험대 03 에 몸이 하나도 안 보인다** — 바닥 격자와 목록만 뜬다. (2026-08-31)
- **실험대 셋 다 제목 둘째 줄과 목록 첫 줄이 겹쳐 찍힌다** — 켜진 개수를 못 읽는다. (2026-08-31)
- **병합 끝난 원격 가지 일곱을 지운다** — 권한 분류기가 `git push --delete` 를 거부했다. 전부 `main` 안에 들어 있어 잃을 게 없다. (2026-08-31)
