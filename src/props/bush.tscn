[gd_scene load_steps=2 format=3]

[ext_resource type="Texture2D" path="res://assets/sprites/field/bush.png" id="1_tex"]

; 🔴🔴 **장식이다 — 물리 몸도 감지 Area도 없다** (근거는 `rock_big.tscn` 머리말과 같다:
;  world 레이어면 캐리어가 덤불마다 터지고, 「뒤로 가면 비친다」는 42px짜리에겐 거짓 손잡이다).
;
; 🔴 **원점 = 접지선** — `offset.y = -(접지선 41 − 프레임높이/2 24) = -17`.
;  그물 = `test_prop_layout_auto [7]`.
; ⚠ 이 `;` 주석은 에디터 저장 시 증발한다(takbon-rules §5).
[node name="Bush" type="Sprite2D"]
texture = ExtResource("1_tex")
offset = Vector2(0, -17)
