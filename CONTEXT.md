# CONTEXT — 이 저장소가 쓰는 낱말

**용어집이다. 이 파일에는 낱말만 산다.** 왜 그렇게 정해졌는지와 사용자가 한 말은 `docs/roadmap/log.md` 에
있고, 무엇이 지어졌는지는 `src/` 와 `tests/nets/` 에서 읽는다. **`naming` 스킬이 이 파일을 쓴다.**

⚠ **한국어와 코드 두 칸이 다 필요하다.** 영어 낱말만 쓴 답은 사용자에게 답이 아니고, 한국어로 지은
이름은 심볼이 아니다.
⚠⚠ **코드와 이 파일이 다르면 코드가 참이고 이 파일을 고친다.** 반대가 아니다.
⚠⚠ **뒤집힌 낱말은 지우지 않고 맨 아래 표에 한 줄로 남긴다** — 죽은 낱말을 다시 제안하지 않으려는
것이고, `판`·`조각`·`블록`·`해안선`이 실제로 자리를 맞바꾼 적이 있다. **오래된 문서는 그 표부터 본다.**

---

## 판과 땅

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **판** | `Grid` + 바닥의 자국 | **걸어 다니는 바닥 전체.** 조각마다 통행·물·항구·눈금·예약을 들고, 흐름장이 얹혀 몸은 낮은 쪽으로 한 조각씩 내려간다 | **「노드」가 아니다** — 그래프가 아니라 격자다 |
| **조각** | `tile` · `TILE_PX` | **몸이 서는 정사각형 하나, 한 변 1 미터.** 바닥의 자국 하나가 조각 하나다 | **블록이 아니다** — 블록 하나가 조각 넷이다 |
| **블록** = **칸** | `Rules.BLOCK_TILES` · `Grid.block_of` · 블렌더의 `PIECES` · `block()` | **2x2 덩어리.** 섬의 윤곽은 이 단위로만 꺾이고, 높이는 덮는 조각 넷 중 **짝수** 눈금의 최대다. **몸은 여기에 아홉까지 선다** (`Rules.BLOCK_CAPACITY`) | ⚠⚠ **「칸」과 「블록」이 둘 다 산다** (2026-08-31 사용자: 「칸이랑 블록 둘 다 되는 걸로 하자」). 아래 뒤집힌 낱말 표의 2026-08-29 줄은 **되돌려진 게 아니라 이름이 둘이 된 것이다** |
| **눈금** | `level` | **반 조각.** 이 게임의 모든 높이가 이 정수에서 나온다 | — |
| **층** | — | **눈금 둘.** 1층이 0, 2층이 2 | **3층은 없다** (2026-08-29 사용자: 「기획 변경 3층 없음」) |
| **계단** | — | **눈금 하나.** 몸은 눈금 차이 1 까지만 넘으므로 **계단이 층 사이의 유일한 문이다.** 블록 위에 얹은 물체다 | **계단 조각에는 자국이 없다** — 지나는 자리지 머무는 자리가 아니다 |
| **섬** | `Islands` | **글자표가 아니라 파일이다.** 그 파일과 화면의 메시는 블렌더 한 번이 같이 쓴다 | — |
| **건물** | 섬 파일의 `builds` | 성채·집·탑·창고·성벽 다섯. **지금 서 있는 것은 집 하나뿐이다** | — |
| **나무 · 돌** | 섬 파일의 `props` | **풍경.** 게임이 도는 동안 이것에 대해 정하는 것이 없다. ⚠ **지금 하나도 안 놓여 있다** | **「오브젝트」라고 안 부른다** — 블렌더에서는 블록도 오브젝트다 |

## 바다

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **해안선** | `line_*` · `second_*` | **흰 선 둘.** 하나는 바위에 붙고 하나는 0.22 조각쯤 떨어져 선다. 사이는 어두운 물이다. **바깥 선은 끊긴다** | **안 움직인다** — 파도가 왔을 때만 두꺼워진다. **윤곽이 아니다** |
| **윤곽** | `coast` · 블렌더의 `waterline_point` | **땅 자체의 모양.** 블록 단위로만 꺾이고, 블렌더가 구우면서 같이 내보낸다 | **눈에 보이는 선이 아니다** — 윤곽 자체는 아무것도 안 그린다 |
| **파도** | `swell` | **열린 바다가 크고 느리게 오르내리는 것.** 섬이 없어도 있다 | **흰 선이 아니라 바다 전체의 굴곡이다** |
| **여울** | `shallow` | **땅에 가까워지며 바다가 옅어지는 넓은 띠** | **선이 아니라 색이다.** **모래사장이 아니다** |

## 몸

| 한국어 | 코드 | 무엇인가 |
|---|---|---|
| **검사** | `SWORDSMAN` | **플레이어의 몸, 그리고 유일한 병종.** 창병과 방패병 사이에서 잡은 숫자이고 사거리만 0 이다 |
| **짐승** | `WOLF` · `BEAR` · `CROW` · `LION` | **적.** 배를 타고 와서 내리고, 걷고, 때린다 |

⚠⚠ **몸의 종류는 데이터지 타입이 아니다.** 코드는 `UNITS` 표의 줄 하나를 알 뿐이고, **검사도 늑대도 그
표의 한 줄이다.** 둘째 병종이 생기는 날 고치는 것은 표 한 줄이다.

## 몸이 하는 것 — **다섯 가지고 끝이다**

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **대기** | `Anim.IDLE` · `<방향>_idle_<n>.png` | **안 움직일 때 숨 쉬는 것.** 여덟 장, 0.96 초 | **서 있는 그림 한 장이 아니다** — 2026-08-31 까지는 그랬다 |
| **이동** | `Anim.WALK` · `<방향>_walk_<n>.png` | **걷는 것.** 네 장, 0.48 초 | — |
| **공격** | `Anim.ATTACK` · `<방향>_attack_<n>.png` | **때리는 것.** 네 장, 한 번 돌고 돌려준다 | **「물기」가 아니다** — 검사는 안 물어서 이름이 바뀌었다 |
| **피격** | `Anim.HURT` · `<방향>_hurt_<n>.png` | **맞고 몸을 움츠리는 것.** 네 장. **체력이 줄면 시작하고 공격을 끊는다** | **흔들림이 아니다** — 몸이 밀리던 `knock` 은 2026-08-29 에 지워졌다 |
| **죽음** | `Anim.DEATH` · `<방향>_death_<n>.png` | **쓰러지는 것.** 여섯 장, 0.72 초. 끝나면 몸이 사라진다 | **시체가 아니다** — 바닥에 남는 것은 없다 |

⚠⚠ **이 다섯은 파일 이름이기도 하다.** `<몸>_<동작>_<번호>.png` 가 규칙이고, 서 있는 그림은
`<몸>.png` 다. **한 줄도 안 가진 몸은 서 있는 그림을 입고 그대로 굴러간다.**
⚠⚠ **순서가 규칙이다**: 죽음 → 피격 → 공격 → 이동/대기. **맞으면 때리던 것을 놓는다.**
⚠ **시뮬레이션은 이 다섯을 모른다.** `battle` 은 상태만 들고 있고, **공격은 재사용 대기가 올라간
것 · 피격은 체력이 내려간 것 · 죽음은 살아있음이 꺼진 것**으로 그리는 쪽이 한 프레임 차를 읽어서 알아낸다.

---

## ⚠⚠ 한국어와 코드가 엇갈리는 자리

**낱말을 뒤집으면서 심볼은 안 바꿨다.** 이름만 보고 잡으면 반대되는 것을 잡는다.
⚠⚠ **라벨이 값과 어긋나면 그 라벨로 짜인 검사가 조용히 다른 것을 재기 시작한다** — `how-nets-lie` 가
이 실패형을 실측 네 건으로 갖고 있다.
⚠ **이 표는 어긋남을 적어 둔 것이지 심볼을 바꾼 것이 아니다.** 그것은 `src/` 를 건드리는 별도 라운드다.

| 이름만 보면 | 실제로는 |
|---|---|
| `coast` · `waterline` · `Islands.coast()` | **해안선이 아니라 윤곽이다.** 흰 선이 아니라 땅의 모양을 돌려준다 |
| `foam_*` | **셰이더에서는 흰색 하나뿐이다.** `look.gd` 의 `foam_lip_*` 은 2026-08-28 에 갈린 옛 해안선의 것이고 **아무것도 안 읽는다**. 지금 해안선은 `line_*` 과 `second_*` 이 그린다 |
| `tile` · `tiles` · `TILE_PX` | **블록이 아니라 조각이다.** 한국어와 반대로 읽힌다 |
| `soldier` · `army` · `unit` | **진영 중립으로 지었지만 플레이어 쪽이다.** `battle.enemy_*` 가 짐승이다 |
| `cell_at` · `cells` · `title_cell_*` | **격자와 화면의 칸이다. 생물이 아니다** — 건드리지 않는다 |

⚠ **코드 주석 두 줄에 「세포」가 남아 있다** (`look.gd`·`rules.gd`). **사용자의 말을 그대로 인용한
자리**라 안 고친다 — 낱말이 바뀌면 인용이 아니다.

## 뒤집힌 낱말 — **지우지 않고 한 줄로 남긴다**

| 언제 | 무엇에서 무엇으로 | 왜 |
|---|---|---|
| 2026-08-31 | **「물기」와 「때리기」 → 「공격」** | 사용자: 「물기 때리기 -> 그냥 공격이라는 것으로」. **검사는 안 물고 늑대는 안 때린다** — 한 낱말로 둘을 다 부를 수 있어야 `_body_tex` 에 종 이름이 안 들어간다. `Anim.BITE` 가 `Anim.ATTACK` 이 됐고 **피격과 죽음이 같은 날 같이 생겼다** |
| 2026-08-31 | **한 블록의 정원이 열둘 → 아홉** | 사용자: 「병사 아홉 개가 최대일 거 같아」. ⚠ **조각당 셋은 그대로다** — 조각 천장만으로는 열둘이 서므로 **천장을 블록에 하나 더 뒀다** (`Rules.BLOCK_CAPACITY`). ⚠ 성채가 든 블록은 여덟만 받는다 |
| 2026-08-31 | **「칸」이 죽은 낱말에서 → 「블록」의 둘째 이름으로 돌아왔다** | 사용자: 「칸이랑 블록 둘 다 되는 걸로 하자. 어차피 그렇게 할 거고」. **2026-08-29 의 「칸 → 블록」은 취소된 게 아니라 이름이 둘이 된 것이다** — 사용자가 두 낱말을 다 쓰고 있고, 하나를 금지하는 규칙이 대화를 멈추게 하고 있었다 |
| 2026-08-30 | **「죽으면 영영 죽는다」 → 「부활한다」** | 사용자가 둘 다 대 보고 골랐다 — 「죽으면 끝나는 걸로 할까? 부활로 하자. 부활로 하는 게 맞겠다」. **죽음이 영구 손실에서 시간 손실이 됐다.** ⚠ `Battle.setup` 이 앞 섬의 시체를 다시 안 내보내던 자리가 그 줄을 지키던 코드다 |
| 2026-08-30 | **「빌드」 → 「테크트리」** | 사용자가 올리는 줄기를 「빌드」라 불렀는데 **그 낱말은 2026-08-26 에 이미 죽었다** — 그때는 「플레이어가 무엇으로 구성되는가」였다. 같은 글자가 두 것을 가리키기 전에 사용자가 직접 바꿔 불렀다 |
| 2026-08-30 | **「설계도」 → 「보스 아이템」** | 보스가 떨구는 것을 사용자가 먼저 「오브젝트」라 불렀고, 그 낱말은 이 파일이 이미 금지해 둔 것이다. 「설계도」를 한 번 대 보고 본인이 물렸다 |
| 2026-08-30 | **「농사와 낚시」 → 「채집」** | 무엇을 · 어디에 · 무슨 값으로가 하나도 안 정해져 있던 덩어리가, **나무·돌·철 셋을 부대가 가져오는 일** 하나로 정리됐다 |
| 2026-08-30 | **성장 고리가 돌아온다** | 2026-08-29 에 통째로 지웠는데, **카드가 아니라 연구대와 테크트리로** 돌아온다. ⚠⚠ **등급은 안 돌아온다** (2026-08-30, 사용자: 「등급 필요 없고」) |
| 2026-08-29 | **해안선이 「빠짐없이」를 잃었다** | 스물일곱 벌 중 사용자가 `27-gaps` 를 골랐다. 그전까지 해안선은 **물이 늘 땅에 닿아 있으니 빈틈이 없다**고 정의돼 있었다. 고른 것은 바깥 선을 끊고 안쪽 선까지 그 자리에서 얇게 만든다. ⚠ **얇아질 뿐 사라지지는 않는다** — 그 절반은 아직 산다 |
| 2026-08-29 | **판 자국이 칸마다 하나 → 조각마다 하나** | 사용자가 둘 다 화면에서 보고: ***"이번에 해보니 판이 조각단위로 뜨고 그것으로 이동할 수 있는게 좋을 것 같아"***. ⚠ **이동 명령이 원래 조각을 받는다** — 자국만 칸이라 단위가 어긋나 있었다. 71 장이 284 장이 됐고, **호버도 조각 하나**다. ⚠ **판끼리 잇던 다리는 같이 없어졌고 아직 안 정해졌다** |
| 2026-08-29 | **칸 → 블록** | 2x2 덩어리의 이름 |
| 2026-08-28 | **파도 → 거품** (섬 테두리의 흰 선) | 사용자: ***"파도가 근데 나중에 바다를 항해도 할꺼여서 사실 겹친달까"***. 「파도」가 열린 바다로 갔다 |
| 2026-08-28 | **물가 → 해안선**, **해안선 → 윤곽** | 사용자: ***"물가를 해얀선으로 단어를 변경하고"***. 흰 선을 계속 「해안선」이라 부르는데 「물가」는 기억이 안 났다. **입에 안 붙는 이름은 이름이 아니다** |
| 2026-08-27 | **칸 ↔ 조각** (맞바꿈) | 그전까지 「칸」이 1 미터 정사각형이고 「조각」이 2x2 덩어리였다 |
| 2026-08-27 | **한 판 → 회차** | 「판」이 걷는 바닥 하나만 가리키게 정해졌다 |
| 2026-08-25 | **「손이 안 움직인다」 → 「손은 움직인다」** | 사용자가 Bad North 를 해 보고: ***"손이 논다고 했는데 배드노스 보니까 손이 놀면 안될듯"*** |

## 죽은 낱말 — **다시 제안하지 않으려고 남긴다**

| 낱말 | 언제 | 무엇이었나 |
|---|---|---|
| **거품** · **물주름** | 2026-08-30 | `foam_*` 과 `ripple_*` 이 셰이더와 함께 지워졌다. **낱말도 코드도 남은 게 없다** |
| **설계도** | 2026-08-30 | 보스가 떨구는 것. 사용자가 말하다 말고 **「보스 아이템」** 으로 바꿨다 |
| **광산** | 2026-08-30 | 자원이 나오는 자리. 섬에 **철광석 하나만 박힌다**로 정리돼 건물 한 종이 안 됐다 |
| **세포** | 2026-08-22 | 몸의 종류. 게임이 세포에서 짐승으로 바뀌었다 |
| **캐릭터** | 2026-08-24 | 종족을 가리키던 말. 사용자: *"뭔가 캐리터는 아님"* |
| **종족 · 빌드** | 2026-08-26 | **플레이어가 무엇으로 구성되는가.** 몸이 검사 하나뿐이라 채울 무리가 없다 |
| **지도** | 2026-08-26 | 5층 7노드. 섬이 하나가 되면서 없어졌다. **계획은 「로드맵」이라 부른다** |
| **일반 늑대 · 무거운 늑대** | 2026-08-25 | 한 종을 둘로 가르는 대신 다섯 종이 됐다. 「먼저 닿는 것과 오래 버티는 것」은 늑대와 곰이 한다 |
| **부리** | 2026-08-25 | +1 사거리 보상. 사용자: ***"부리 보상 없지 끝나면 카드보상으로 통일했잖아"*** |
| **무리사냥** | 2026-08-27 | **대형이 아니라 표적 규칙이었다** — 어디서 보고 고르느냐만 바꾸고 서는 자리는 안 바꿨다. 짐승에게 다시 줄 때 이 정의부터 읽는다 |
| **보상 화면 · 정비 화면** | 2026-08-29 | 카드 세 장 중 하나, 그리고 정비 판. **성장 고리가 통째로 나갔다.** ⚠⚠ **등급은 2026-08-30 에 사용자가 직접 물렸다** — 강화는 병사 수를 늘리는 쪽이다 |

---

## Where the seams are

⚠⚠ **No check is written at an unagreed seam.** **These are the agreed ones**, and they come from the
folder rule in `CLAUDE.md`:

- **`src/sim/`** — constructible with `.new()`, never touches the tree. **The main seam.** A net drives the
  whole game here in seconds
- **`src/view/`** — reads `sim`, never writes it. **The measuring surface is three-fold**
  (2026-08-24, user-approved — the old `_paint_*` hook died with the 3D move): **the pure camera
  functions** (drivable with `.new()`), **pooled node state** (the Sprite3D fields the engine consumes:
  position, modulate, scale, texture, visible), and **the fx buffers plus committed surface counts**
  (buffers prove geometry was built; surface count proves the flush committed it — buffers alone stay
  green when the flush is deleted)
- **`src/shell/`** — the only reader of `Input`. **Seam is `_ready()`**, which builds the real wiring

**Do not add a seam inside a file.** If something is hard to test, it is in the wrong folder.

⚠ **These three survived three deletions and are not re-decided per game.** The 2026-08-24 change
above moved the view seam's measuring surface, not the seam itself — the folder boundary stands.
