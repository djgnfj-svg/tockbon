# CONTEXT — 이 저장소가 쓰는 낱말

**용어집이다. 이 파일에는 낱말만 산다.** 왜 그렇게 정해졌는지와 사용자가 한 말은 `docs/plan/log.md` 에
있고, 무엇이 지어졌는지는 `src/` 와 `tests/nets/` 에서 읽는다. **`naming` 스킬이 이 파일을 쓴다.**

⚠ **한국어와 코드 두 칸이 다 필요하다.** 영어 낱말만 쓴 답은 사용자에게 답이 아니고, 한국어로 지은
이름은 심볼이 아니다.
⚠⚠ **코드와 이 파일이 다르면 코드가 참이고 이 파일을 고친다.** 반대가 아니다.
⚠⚠ **뒤집힌 낱말은 지우지 않고 맨 아래 표에 한 줄로 남긴다.** 죽은 낱말을 다시 제안하지 않으려는 것이다.
⚠ **`판`·`조각`·`블록`·`해안선`은 서로 자리를 맞바꾼 적이 있다.** 오래된 문서를 읽을 때는 아래
「뒤집힌 낱말」을 먼저 본다.

---

## 판과 땅

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **판** | `Grid` + 바닥의 자국 | ⚠⚠ **자국은 조각마다 하나다** (2026-08-29). **걸어 다니는 바닥 전체.** 조각마다 통행·물·항구·눈금·예약을 들고, 목적지에서 번져 나온 흐름장이 얹혀 있어 몸은 길을 계산하지 않고 낮은 쪽으로 한 조각씩 내려간다. **보이지 않는 조각 표와 화면의 흰 자국이 둘 다 판이다** | **「노드」가 아니다** — 그래프가 아니라 격자다. **흰 자국만 따로 부르는 낱말은 없다** |
| **조각** | `tile` · `tiles` · `TILE_PX` | **몸이 서는 정사각형 하나, 한 변 1 미터.** 화면 바닥의 둥근 자국 하나가 조각 하나다 | **블록이 아니다** — 블록 하나가 조각 넷이다 |
| **블록** | 블렌더의 `PIECES` · `P_x_y` · `block()` | **블렌더가 물체 하나로 세워 마지막에 합치는 2x2 덩어리, 곧 조각 넷.** 섬의 윤곽은 이 단위로만 꺾인다. 높이를 하나만 갖고, 덮는 조각 넷 중 **짝수** 눈금의 최대를 쓴다. ⚠⚠ **홀수는 무시한다** (2026-08-29) — 그래야 계단 조각이 블록을 통째로 들어올리지 않고, 같은 블록의 다른 조각 자국이 땅에 안 묻힌다 | **2층 블록은 블록 둘이 쌓인 게 아니다** — 블록 하나가 높은 것이다. ⚠ **게임 코드에는 이름이 없다**: 게임은 구워진 섬을 읽기만 한다 |
| **눈금** | `level` | **반 조각.** 조각마다 정수 하나를 들고, **이 게임의 모든 높이가 그 정수에서 나온다** | — |
| **층** | — | **눈금 둘.** 1층이 0, 2층이 2. ⚠⚠ **2층이 끝이다** (2026-08-29 사용자: 「기획 변경 3층 없음」) | **3층은 없다** — 눈금 3·4 는 나중에 붙는 것이 아니라 안 붙는다 |
| **계단** | — | **눈금 하나.** 곧 홀수 눈금이 계단이고, 몸은 눈금 차이 1 까지만 넘으므로 **계단이 층 사이의 유일한 문이다**. ⚠⚠ **블록이 아니라 블록 위에 얹은 물체다** (2026-08-29) — 계단이 든 블록은 평지로 서고 계단 메시가 그 위에 놓인다. **블록 하나 크기, 단 넷** | **계단의 단은 판이 아니다** — 판에는 눈금 하나로 적힌다. ⚠ **계단 조각에는 자국이 없다** — 지나는 자리지 머무는 자리가 아니다 |
| **섬** | `Islands` | **글자표가 아니라 파일이다** — `assets/terrain/island.json` 을 읽는다. 그 파일과 화면의 메시는 블렌더 스크립트 한 번이 같이 쓴다 | — |
| **건물** | 섬 파일의 `builds` | **섬 위에 서는 지어진 것.** 성채·집·탑·창고·성벽 다섯이고, **지금 서 있는 것은 집 하나뿐이다** | **땅과 칠하는 법이 다르다** — 땅은 꼭짓점마다, 건물은 부분마다 단색 |
| **나무 · 돌** | 섬 파일의 `props` | **풍경.** 플레이어가 놓지 않고, 게임이 도는 동안 이것에 대해 정하는 것이 하나도 없다. ⚠ **지금 섬에 하나도 안 놓여 있다** | **「오브젝트」라고 안 부른다** — 블렌더에서는 블록도 오브젝트다. 묶어 부를 때만 **「장식」** |

## 바다

⚠⚠ **여섯 중 넷은 그리는 코드가 없다** (2026-08-28). 해안선을 그리는 방법 일곱 벌을 나란히 세워 놓고
사용자가 **평평한 바다에 경계선 하나**를 골랐다. **낱말과 상수는 남겨 둔다** — 하나하나가 후보를 눈으로
보고 고른 실측이고, 다시 그리는 날 읽을 것이 그것뿐이다.

| 한국어 | 코드 | 무엇인가 | ⚠ 무엇이 아닌가 |
|---|---|---|---|
| **해안선** ✅ | `line_*` · `second_*` | **흰 선 둘.** 하나는 바위에 붙어 있고, 하나는 0.22 조각쯤 떨어져 선다. 사이는 어두운 물이다. ⚠⚠ **바깥 선은 끊긴다** — 천천히 흐르는 문과 해안의 굽이가 같이 자르고, 만 안쪽에는 아예 없다. **안쪽 선은 그 자리에서 얇아지기만 하고 없어지지는 않는다** | **안 움직인다** — 파도가 왔을 때만 두꺼워진다. **윤곽이 아니다** — 윤곽은 땅이고 해안선은 물이 그 위에 그린 선이다. **바깥 선은 띠가 아니라 획이다** |
| **윤곽** ✅ | `coast` · 블렌더의 `waterline_point` | **땅 자체의 모양.** 블록 단위로만 꺾이고, 블렌더가 구우면서 같이 내보낸다 | **눈에 보이는 선이 아니다** — 윤곽 자체는 아무것도 안 그린다 |
| **거품** ⛔ | `foam_*` (`band`) | **섬 테두리로 밀려 들어오는 흰 선.** 자락마다 다른 순간에 마루가 서고, 바람을 맞는 쪽에만 짙다 | **해안선이 아니다** — 거품은 움직이고 어떤 자락에는 아예 없다 |
| **물주름** ⛔ | `ripple_*` | **바다 전체에 깔린 잔주름.** 바람 방향으로 늘어나고 토막으로 끊긴다. 넓은 얼룩이 얹혀 어떤 자락은 거의 잔잔하다 | **거품이 아니다** — 물주름은 어디에나 있고 섬과 상관없다 |
| **파도** ⛔ | `swell` · `wave_scale` · `wave_speed` | **열린 바다 자체가 크고 느리게 오르내리는 것.** 섬이 없어도 있다 | **흰 선이 아니라 바다 전체의 굴곡이다** |
| **여울** ⛔ | `shallow` | **땅에 가까워지며 바다가 옅어지는 넓은 띠.** 땅과 바다를 이어 주는 것이 이것이다 | **선이 아니라 색이다.** **모래사장이 아니다** — 땅은 여전히 수직 벽에서 끝난다 |

✅ 화면에 있다 · ⛔ 낱말만 남고 그리는 코드가 없다

## 몸

| 한국어 | 코드 | 무엇인가 |
|---|---|---|
| **검사** | `SWORDSMAN` | **플레이어의 몸, 그리고 유일한 병종.** 창병과 방패병 사이에서 잡은 숫자이고 사거리만 0 이다 |
| **짐승** | `WOLF` · `BEAR` · `CROW` · `LION` | **적.** 배를 타고 와서 내리고, 걷고, 때린다 |

⚠⚠ **몸의 종류는 데이터지 타입이 아니다.** 코드는 `UNITS` 표의 줄 하나를 알 뿐이고, **검사도 늑대도 그
표의 한 줄이다.** 둘째 병종이 생기는 날 고치는 것은 표 한 줄이다.

## 회차

| 한국어 | 코드 | 무엇인가 |
|---|---|---|
| **회차** | `Run` | 한 회차. 무슨 상태에 서 있고(섬·승·패) 명부가 무엇을 들고 있는가. **상태는 셋이다** |
| **전투** | `Battle` | 한 섬의 싸움. `step(dt)` 하나가 배·상륙·조준·이동·공격을 전부 민다 |
| **명부** | `Army` | 섬을 건너 살아남는 병력. **죽으면 영영 죽는다.** 칸 수는 회차 상태(`Army.slots`)이고, 하나로 열려 다섯에서 멈춘다 |
| **정비** | `Loadout` | **병종 하나에 정비가 하나** — 꽂으면 그 병종 전부가 낀다. 몸이 죽어도 정비는 안 죽는다. 이름 없는 칸 여섯에 아무 장비나 꽂고, 올리는 숫자는 칸이 아니라 장비가 든다 |
| **출혈** | `Rules.Status.BLEED` | 장비 태그가 문턱을 넘으면 문 자리에 남는다. ⚠ **적의 몸에만 붙는다** |

⚠⚠ **손은 움직인다.** 부대를 판 위에서 아무 때나 명령하고, 쿨타임도 횟수 제한도 없다. 명령은 **무리
단위지 한 마리씩이 아니다.** ⚠ **아직 코드에 한 줄도 없다** — 결정이지 구현이 아니다.

⚠⚠ **성장 고리가 없다** (2026-08-29 삭제). 이긴 뒤 무엇을 얻느냐가 게임에 하나도 없고, 명부의 칸을
늘리는 길도 없다. `Rules` 의 등급·아이템 표는 읽는 쪽 없이 남아 있다.

## 화면

| 한국어 | 코드 | 무엇인가 |
|---|---|---|
| **섬 화면** | `FieldView` | 바다의 배·몸·체력, 그리고 **블렌더가 만든 섬 메시를 불러다 놓는 것.** ⚠ **지형을 만들지 않는다** |
| **껍데기** | `Game` | **입력을 읽는 유일한 파일**이고 `sim` 과 `view` 를 잇는 유일한 자리 |

## 도구

| 한국어 | 코드 | 무엇인가 |
|---|---|---|
| **그물** | net | 시험 하나. **라벨이 말하는 것보다 적게 재는 초록은 빨강보다 나쁘다** |
| **로드맵** | roadmap | 12 월 데모까지의 계획, `docs/plan/roadmap.md` 한 장. ⚠ **이것을 「지도」라고 부르지 않는다** |
| **결정 로그** | log | 왜 그렇게 됐나, `docs/plan/log.md`. **사용자의 말이 그대로 남는 자리** |
| **티켓** | ticket | 질문 하나 또는 작업 하나. 상태는 파일 안의 `Status:` 줄이고, 파일은 폴더를 안 옮겨 다닌다 |

---

## ⚠⚠ 한국어와 코드가 엇갈리는 자리

**낱말을 뒤집으면서 심볼은 안 바꿨다.** 이름만 보고 잡으면 반대되는 것을 잡는다.
⚠⚠ **라벨이 값과 어긋나면 그 라벨로 짜인 검사가 조용히 다른 것을 재기 시작한다** — `how-nets-lie` 가
이 실패형을 실측 네 건으로 갖고 있다.
⚠ **이 표는 어긋남을 적어 둔 것이지 심볼을 바꾼 것이 아니다.** 그것은 `src/` 를 건드리는 별도 라운드다.

| 이름만 보면 | 실제로는 |
|---|---|
| `coast` · `waterline` · `Islands.coast()` | **해안선이 아니라 윤곽이다.** 흰 선이 아니라 땅의 모양을 돌려준다 |
| `foam_*` | **셰이더에서는 흰색 하나뿐이다.** `look.gd` 의 `foam_lip_*` 은 2026-08-28 에 갈린 옛 해안선의 것이고 **아무것도 안 읽는다**. 지금 해안선은 `line_*` 과 `second_*` 이 그린다 |
| `tile` · `tiles` · `TILE_PX` | **블록이 아니라 조각이다.** 한국어와 반대로 읽힌다 |
| `soldier` · `army` · `unit` | **진영 중립으로 지었지만 플레이어 쪽이다.** `battle.enemy_*` 가 짐승이다 |
| `cell_at` · `cells` · `title_cell_*` | **격자와 화면의 칸이다. 생물이 아니다** — 건드리지 않는다 |

⚠ **코드 주석 두 줄에 「세포」가 남아 있다** (`look.gd`·`rules.gd`). **사용자의 말을 그대로 인용한
자리**라 안 고친다 — 낱말이 바뀌면 인용이 아니다.

## 뒤집힌 낱말 — **지우지 않고 한 줄로 남긴다**

| 언제 | 무엇에서 무엇으로 | 왜 |
|---|---|---|
| 2026-08-29 | **해안선이 「빠짐없이」를 잃었다** | 스물일곱 벌 중 사용자가 `27-gaps` 를 골랐다. 그전까지 해안선은 **물이 늘 땅에 닿아 있으니 빈틈이 없다**고 정의돼 있었다. 고른 것은 바깥 선을 끊고 안쪽 선까지 그 자리에서 얇게 만든다. ⚠ **얇아질 뿐 사라지지는 않는다** — 그 절반은 아직 산다 |
| 2026-08-29 | **판 자국이 칸마다 하나 → 조각마다 하나** | 사용자가 둘 다 화면에서 보고: ***"이번에 해보니 판이 조각단위로 뜨고 그것으로 이동할 수 있는게 좋을 것 같아"***. ⚠ **이동 명령이 원래 조각을 받는다** — 자국만 칸이라 단위가 어긋나 있었다. 71 장이 284 장이 됐고, **호버도 조각 하나**다. ⚠ **판끼리 잇던 다리는 같이 없어졌고 아직 안 정해졌다** |
| 2026-08-29 | **칸 → 블록** | 2x2 덩어리의 이름 |
| 2026-08-28 | **파도 → 거품** (섬 테두리의 흰 선) | 사용자: ***"파도가 근데 나중에 바다를 항해도 할꺼여서 사실 겹친달까"***. 「파도」가 열린 바다로 갔다 |
| 2026-08-28 | **물가 → 해안선**, **해안선 → 윤곽** | 사용자: ***"물가를 해얀선으로 단어를 변경하고"***. 흰 선을 계속 「해안선」이라 부르는데 「물가」는 기억이 안 났다. **입에 안 붙는 이름은 이름이 아니다** |
| 2026-08-27 | **칸 ↔ 조각** (맞바꿈) | 그전까지 「칸」이 1 미터 정사각형이고 「조각」이 2x2 덩어리였다 |
| 2026-08-27 | **한 판 → 회차** | 「판」이 걷는 바닥 하나만 가리키게 정해졌다 |
| 2026-08-25 | **「손이 안 움직인다」 → 「손은 움직인다」** | 사용자가 Bad North 를 해 보고: ***"손이 논다고 했는데 배드노스 보니까 손이 놀면 안될듯"*** |

## 죽은 낱말 — **다시 제안하지 않으려고 남긴다**

| 낱말 | 언제 | 무엇이었나 |
|---|---|---|
| **세포** | 2026-08-22 | 몸의 종류. 게임이 세포에서 짐승으로 바뀌었다 |
| **캐릭터** | 2026-08-24 | 종족을 가리키던 말. 사용자: *"뭔가 캐리터는 아님"* |
| **종족 · 빌드** | 2026-08-26 | **플레이어가 무엇으로 구성되는가.** 몸이 검사 하나뿐이라 채울 무리가 없다 |
| **지도** | 2026-08-26 | 5층 7노드. 섬이 하나가 되면서 없어졌다. **계획은 「로드맵」이라 부른다** |
| **일반 늑대 · 무거운 늑대** | 2026-08-25 | 한 종을 둘로 가르는 대신 다섯 종이 됐다. 「먼저 닿는 것과 오래 버티는 것」은 늑대와 곰이 한다 |
| **부리** | 2026-08-25 | +1 사거리 보상. 사용자: ***"부리 보상 없지 끝나면 카드보상으로 통일했잖아"*** |
| **무리사냥** | 2026-08-27 | **대형이 아니라 표적 규칙이었다** — 어디서 보고 고르느냐만 바꾸고 서는 자리는 안 바꿨다. 짐승에게 다시 줄 때 이 정의부터 읽는다 |
| **보상 화면 · 정비 화면** | 2026-08-29 | 카드 세 장 중 하나, 그리고 정비 판. **성장 고리가 통째로 나갔다.** 되살릴 때: 등급을 먼저 굴리고 아이템을 나중에 굴려야 새 아이템이 늘 때마다 레전더리가 희귀해지지 않는다 |

---

## Where the seams are

⚠⚠ **No check is written at an unagreed seam.** **These are the agreed ones**, and they come from the
folder rule in `CLAUDE.md`:

- **`src/sim/`** — constructible with `.new()`, never touches the tree. **The main seam.** A net drives the
  whole game here in seconds
- **`src/view/`** — reads `sim`, never writes it. **The measuring surface is three-fold**
  (2026-08-24, user-approved — the old `_paint_*` hook died with the 3D move): **the pure camera
  functions** (drivable with `.new()`), **pooled node state** (the Sprite3D fields the engine consumes:
  position, modulate, scale, texture, visible), and **the fx buffers plus committed surface counts**
  (buffers prove geometry was built; surface count proves the flush committed it — buffers alone stay
  green when the flush is deleted)
- **`src/shell/`** — the only reader of `Input`. **Seam is `_ready()`**, which builds the real wiring

**Do not add a seam inside a file.** If something is hard to test, it is in the wrong folder.

⚠ **These three survived three deletions and are not re-decided per game.** The 2026-08-24 change
above moved the view seam's measuring surface, not the seam itself — the folder boundary stands.
